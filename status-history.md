# 진행 상태 — **이력 전용** (R0 ~ R44 — 전부)

> # ⛔ 이 파일에서 **현재 상태를 읽지 마십시오**
>
> 인계 정본은 **`status.md`** 다. 여기 있는 수치·절차·「지금 할 일」은 **적힌 시점의
> 것**이며 대부분 낡았다 — R32 가 `status.md` 를 222KB 에서 잘라 내며 옮긴 것이다.
>
> **왜 옮겼는가.** 통째로 읽으면 토큰 한도에 걸리는 파일은 **아무도 끝까지 읽지
> 않는다.** 그 결과 「산출물 현황」 절이 두 판본 뒤처진 채(spec v0.14 · 수용기준 308)
> 남아 있었고, 그것이 이 저장소가 반복해 잡아 온 형태다 — *「세지 않은 수를 옮겨
> 적으면 그것이 다음 라운드의 계획으로 따라 들어간다」*.
>
> **여기 남긴 이유**: 판정의 **경위**가 값이다. *왜* 그렇게 정했는지를 물을 때
> 찾아본다. 그때도 **수치는 믿지 말고 명령을 돌린다.**
>
> 라운드를 닫을 때 `status.md` 의 옛 「지금 할 일」 블록을 **이 머리 바로 아래**로
> 옮긴다(시간 역순 유지).
>
> ⚠ **아래 블록들이 가리키는 `todo.md` 는 더 이상 없다.** 2026-08-30(R45)에
> `status.md` 의 **「다음에 집을 것」 절**로 합치고 지웠다 — 운영 파일을 하나로
> 만들기 위해서다. 블록 안의 「`todo.md` N번」은 **그 라운드 시점의 번호**이며
> 지금의 번호가 아니다. **본문은 고치지 않았다** — 고치면 이력이 거짓이 된다.

---

> ### ⬇ **R59 블록**(2026-09-04) — **라운드 종료 블록 중 가장 새 것**이며, 그래도
> **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-04 **R59 — 재료 정본(§5)이 판정 대기로 막혀 있어 「착수 목록 자신의 낡음」을 집었다. 조항 하나를 원인 쪽에서 닫고, 지목 47건을 판정 가능하게 만들었다**
>
> **재료** — `docs/decisions-2026-09-03-R57.md` §5 는 **1번만 남았고 그것은 열 수 없다**:
> `MC-1` 이 계약구조를 주지 않아 정할 자료가 없다(R58 판정 ②). 그래서 착수 목록의
> 정본(`status.md` 「다음에 집을 것」)에서 **판정을 기다리지 않는 것**을 집었다.
>
> ## ★★★ 착수 전 판정 — **부록 A(착수 순서 24번) 다섯 칸을 실물로 대조했고 다섯 다 닫혔다**
>
> ⚠⚠ **왜 이것을 먼저 했나.** 이 저장소는 **R57 에서 이미 닫힌 항목을 1순위로 집었다**
> (★배타물리량 칸이 「★★ 이것부터 집는다」로 남아 있었다). 그 칸이 스스로 처방을
> 적어 두었다 — *「판정 칸을 닫으면서 함께 고쳐라」*. 부록 A 는 **R38 이 세운 뒤 아무도
> 다시 재지 않은 다섯 칸**이라 같은 형태가 의심됐고, 실제로 그랬다.
>
> | 칸 | 실측 (오케스트레이터가 직접 돌렸다) | 판정 |
> |---|---|---|
> | **②** `FR-101-AC2` 매핑이 한 파일에 몰렸다 | 인용 **6건** 중 **5건이 `tests/contract/test_der_contract.py`**, 1건이 `tests/der/test_load.py`. 칸에 적힌 「8건 중 5건, 그중 4건」은 낡았고 **편중은 더 심해졌다** | **닫는다 — 분산이 처방이 아니다.** `AC2` 는 *「메서드: `capex()` …」* 로 **계약 자체**(6종이 추상인가)를 요구하고, 그것을 재는 자리는 계약 시험 파일이다. 다른 파일로 흩으면 **조항이 아닌 것을 재는 마커**가 늘어난다(이 저장소가 `AC5` 를 신설해 수습한 형태가 그것이다). 남는 위험은 그 파일이 사라지는 것이고 **미매핑 게이트가 그것을 잡는다** |
> | **③** 같은 개념을 네 이름으로 부른다 | 이름 넷 그대로다 — `subcomponents`(`Load`·`ThermalLoad`) · `pump_lifetime`(`HeatPump`) · `inverter_lifetime`(`PV`) · `pcs_lifetime`(`ESS`). 그런데 **`FR-104-AC4` 인용은 14건이고 7파일에 퍼져 있다** | **착수 항목에서 내리고 「함정」 절로 옮긴다.** 마커가 퍼져 있으므로 **미매핑 위험이 아니다** — 위험은 *「이름으로 훑고 멈추는 사람」* 쪽이고, 그것은 코드 결함이 아니라 **훑기 방법의 함정**이다. 이름을 하나로 묶는 것은 **실물을 왜곡한다**(부하는 부속설비가 복수, 나머지는 단수) |
> | **④** 소스 문면 검사가 들여쓴 지역 import 를 놓친다 | **이미 닫혀 있었다.** `tests/contract/source_scan.py` 가 `ast.walk` 로 옮겨졌고 그 독스트링이 「## `ast` 로 옮긴 이유 (R39-A)」 절로 경위를 적고 있다 | **지운다.** 칸이 *「정할 것: 지역 import 도 잡을 것인가」* 로 남아 있었는데 **R39-A 가 이미 정하고 고쳤다** |
> | **⑤** 왼쪽 경계를 막지 않은 `FR-` 검색 잠재 2곳 | 1번(`check_partition_assignment.py`)은 **닫혔다** — `"FR-" in cells[3]` 이 `FR_COL_BOUNDARY = re.compile(r"\bFR-")` 로 바뀌어 있다(같은 파일 11·58행). 2번은 시험 파일이고 R38 이 이미 「프로덕션 판정에 영향 없음」으로 추적해 두었다 | **닫는다.** 활성 결함 0건이 그대로이고 잠재 자리 하나가 사라졌다 |
> | **⑥** `FR-101-AC3`ⓑ 는 오늘 만족되고 있는가 | `core/engine/rule_based.py::_rule_for()` 가 `pv`·`ess`·`ev_v2g` 를 **리터럴로 알고 있었다**(237~241행) | **WP-1 이 집어 닫았다** — 아래 |
>
> ⚠ **여기서 남는 교훈은 다섯 칸이 아니라 「다시 재지 않은 칸」이다.** 셋(②④⑤)이
> **틀렸거나 낡았고**, 그 상태로 「크기 순 착수 목록」의 한 항목(24번)이 여러 라운드를
> 살아남았다. **물려받은 수는 쓰기 전에 다시 센다** 가 이 저장소의 규약인데,
> 부록 A 는 *수*가 아니라 *상태*라서 그 규약이 걸리지 않았다.
>
> ## WP 표
>
> | WP | 무엇이 어긋나 있었나 | 무엇을 했나 | 커밋 |
> |---|---|---|---|
> | **1** | `FR-101-AC3` 은 「인터페이스만 구현하면 **코어 엔진 수정 없이 동작**」인데 `core/engine/rule_based.py::_rule_for()` 가 자원 태그 셋(`pv`·`ess`·`ev_v2g`)을 **리터럴로 알고** 순위를 배정했다 — 일곱째 자원이 고유 규칙을 필요로 하는 순간 엔진을 고쳐야 하므로 조항이 원리적으로 깨져 있었다. R38-B2 의 래칫은 **늘어나는 것만** 막았다 | 방향을 뒤집었다 — **엔진이 자원에게 묻는다.** `DER.DISPATCH_RULE`(기본값 빈 문자열, **추상 아님**)을 계약에 세우고 자원이 `DispatchRule` 의 **값 문자열**을 적는다(열거형은 `core.engine` 에 있어 자원이 import 할 수 없다 · `NFR-208-AC1`). 선언 없음·오타는 **기본 갈래**로 떨어진다(예외를 던지면 오타 하나로 새 자원이 아예 못 돌아 조항의 「동작」이 사라진다). `ENGINE_KNOWN_TAGS` 를 `frozenset()` 으로 **비웠다** — 문턱이 0 이라 태그 리터럴이 한 건이라도 되돌아오면 빨간불이다. 평문 문자열이 된 대가로 오타가 조용해지므로 시험 둘을 세웠다: 레지스트리 전건을 훑어 **선언이 어휘에 실재하는가**(기본 갈래를 명시적으로 선언한 것도 거부한다 — 「잊었다」와 겹치면 아무도 못 본다) · **모르는 문자열·빈 문자열이 그래도 도는가** | `a6863f3` |
> | **1.5** | 착수 목록의 **부록 A** 다섯 칸을 R38 이 세운 뒤 아무도 다시 재지 않았다 | 위 판정표. **24번을 지우고 부록 A 44줄을 이력으로 내렸다**(`status.md` 1,033 → 1,002줄). 8번의 수도 다시 셌다 — 후보 46 → **47건** · `NFR-303-M1` 인용 166 → **171건** | `0d2914e` |
> | **2** | ③-2 지목 **47건**을 **누가 대조하는가**가 여러 라운드 열려 있었다(착수 순서 8번). ⚠ 이 저장소는 전수 대조를 손으로 쓰게 했을 때 **조항 문면을 지어냈다** | 순서를 뒤집었다 — **도구가 spec 원문에서 조항 문면을 뽑아 인용과 나란히 대고, 판정은 그 대장에서만 인용한다.** `scripts/audit_clause_dossier.py`(401줄) 신설 · 수집은 기존 셋(`audit_clause_selfreference` · `_citations` · `_specparse`)을 그대로 쓴다 · `audit_` 계열이라 **판정하지 않고** CI 에 배선하지 않는다. 인용 5건 이상 후보 **12건**을 판정: **맞다 11 · 어긋났다 1 · 불명 0.** 판정표는 `.orch/` 에만 두면 사라지므로 **`docs/decisions-2026-09-04-R59.md`** 로 옮겼다 | `8249f6e` |
>
> | **3+4** | `NFR-105` 는 Measurement 를 **번호 목록**(`1.` `2.` `3.`)으로 적고 바로 아래 `NFR-106` 은 `- **M1**` 으로 적는다. `_specparse.CRITERION` 은 후자만 잡으므로 **`NFR-105-M1`·`M2`·`M3` 라는 조항 ID 가 아예 없었다.** CI 게이트 검사 **24건**이 갈 자리가 `AC1` 뿐이었고, 그 `AC1` 문면은 요구사항 문장의 복사본이라 **「순서」**를 요구하는데 **순서를 재는 인용은 24건 중 0건**이었다 | **WP-3** — Measurement 셋을 `- **M1**`~`- **M3**` 로 옮겼다(**본문은 한 글자도 바꾸지 않았다**). `AC1` 은 **지우지도 고치지도 않았다**(동결 규약). 마커 24건을 **대장 근거로** 재조준(커버리지 → `M1` · 동반 → `M2`). 머리 줄의 *「모두 자동 판정」* 은 `M3` 구현이 없어 거짓이었으므로 고쳤다. ★★ **그러자 `M3`·`AC1` 이 Phase 1 Must-have 미매핑으로 드러나 `test_17_9_dod9` 가 빨간불이 됐고, 워커는 멈추고 갈래를 적었다.** **WP-4** — **없는 검사를 만들었다**: `scripts/check_commit_order.py`(211줄 · 짝 판정은 게이트 ②의 `check()` 를 그대로 쓴다 · 위반은 `rc=0`「차단 아님」이고 **판정 불가는 `rc=2`**) · `scripts/negtest_commit_order.py`(220줄 · 임시 디렉터리에 **진짜 git 저장소**를 세워 음성 1 + 양성 2 + 경계 2) · CI 스텝 셋(PR 판정 · 감지 능력 · push 건너뜀 · **`|| true`·`continue-on-error` 를 붙이지 않았고 붙지 않았음을 시험이 단언한다**) · `tests/ci/test_ci_gates.py` 시험 여섯(**`AC1` 은 순서를 실제로 재는 둘에만**). **미매핑 9 → 11 → 9** | `b77b071` |
>
> | **5** | ③-2 지목의 **나머지 37건**이 판정되지 않은 채 남아 있었다 | WP-2 가 세운 대장 도구로 **전건 판정**했다 — **맞다 33 · 어긋났다 4 · 불명 0**(시간 상한 전에 끝났다). ⇒ **지목 48건이 전건 판정됐다**(§2 의 11 + 여기 37. `NFR-105-AC1` 은 마커가 옮겨져 후보에서 빠졌다). ⚠ **기준선이 라운드 안에서 움직였다** — WP-3+4 가 마커를 옮기자 47 → **48건**(‘AC1’ 이 빠지고 `M1`·`M3` 가 들어왔다). **판정만 했고 한 건도 고치지 않았다** — 넷 다 **시험 신설**이 따라오므로 판정과 수정은 다른 일이다 | `765f04d` 뒤 |
>
> | **6** | `FR-604` 의 마커가 **한 칸씩 밀려** 다섯 조항이 서로 남의 것을 재고 있었다(WP-5 판정 ⓐ) | 마커를 **조항 문면에 맞춰** 되돌리고(`보조→AC2`·`융자→AC3`·`세제→AC5`·`지원 주체→AC6`) **`AC4`(자부담: 잔여 비율 자동 계산)를 재는 시험을 신설했다.** ⚠ 되돌리기만 하면 `AC4` 인용이 0건이 되어 **WP-3 과 같은 자리**에 선다 — 그래서 「마커를 옮기는 일」이 아니라 **「시험을 세우는 일」**로 잡았다. ★ 신설 시험은 형제 조항(`AC7` 합 항등식 · `AC8` 확정액 · `AC9` 음수 불가)과 **안 겹치는 몫**만 잰다: ① 자부담은 **선언할 수 있는 필드가 아니다** ② 잔여 비율이 **규모에 불변**이다 ③ 상한이 걸리면 잔여가 **선언 비율이 아니라 확정액**을 따른다. ⚠ **구현에 계산이 이미 있었다**(`calculate_financing()` 의 `equity = capex − subsidy − loan`) — 지어내지 않았다 | 아래 |
>
> | **7** (오케스트레이터 직접) | `FR-204-AC2`(*「마을단위 분산특구 6개 모델, 아파트 마이크로그리드 모델 **[Phase 2]**」*)의 마커가 **Phase 1 시험**(에너지자립가구 2변형)에 달려 있었다 — 그 시험에 `AC2` 를 재는 단언이 **한 줄도 없다** | **마커를 뗐다.** ★ **미매핑이 9 → 10 이 됐고 그것이 옳다** — 그 조항은 Phase 2 이고 구현이 없으며, DoD 9 는 **Phase 1** 미매핑만 0 을 요구한다(종전 9건도 전부 Phase 2/3). ③-2 래칫에서도 함께 지웠다(인용이 0건이 되어 지목에서 사라졌다). ⚠ **위임하지 않고 직접 했다** — 마커 한 줄이라 지시문 작성·검수가 더 비싸다(orch-dev 0절) | 아래 |
>
> ⚠ **WP-6 은 범위 경계에서 한 번 멈췄다.** 마커를 되돌리자 `FR-604-AC5`·`AC6` 이
> ③-2 지목에서 **닫혔는데**, 래칫 선언(`tests/contract/test_clause_evidence_nets.py`
> 의 `KNOWN_SILENT_CITATIONS`)이 아직 둘을 열린 것으로 적어 실측과 어긋났다.
> 그 파일은 브리프가 막은 자리였고 **워커는 우회하지 않고 두 줄 삭제를 적어 두고
> 멈췄다.** ★ **래칫은 판정의 선언층이고**(그 파일 주석이 *「근거를 전수로 읽고
> 판정하는 것은 사람 몫이며 이 구획의 일이 아니다」* 로 적는다) **적용은
> 오케스트레이터 자리**여서 내가 지웠다 — 함께 그 주석에 **왜 지웠는지와
> 「대개 정상이 언제나 정상은 아니다」**를 남겼다.
>
> ### ★★★ WP-5 가 찾은 것 — **지목이 못 보는 형태가 있다**
>
> `FR-604` 는 낱건이 아니라 **마커가 한 칸씩 밀렸다** — `AC2`(보조)에 융자 시험,
> `AC3`(융자)에 세제 시험, `AC5`(세제)·`AC6`(지원 주체)까지 **다섯 조항**이 같은
> 방향이다. **오케스트레이터가 두 건을 소스에서 직접 확인했다.**
>
> ⚠⚠ **그중 셋은 ③-2 그물에 걸리지 않았다 — 자기(틀린) ID 를 문면에 적고 있기
> 때문이다.** ③-2 는 *「인용 어느 것도 자기 ID 를 안 적는다」* 를 신호로 쓰므로,
> **틀린 조항을 정확히 인용하는 시험은 원리적으로 못 잡는다.**
> ⇒ **지목 48건 전건 판정이 「어긋남이 다섯뿐」을 뜻하지 않는다.** 다음 그물은
> 이 형태를 겨냥해야 한다(착수 순서 **35번**).
>
> ⚠ 넷 다 **되돌리면 새 미매핑이 생긴다** — `FR-604-AC4`(자부담)·`FR-103-AC2` 는
> 재는 시험이 아예 없고, `FR-204-AC2` 는 Phase 2 라 **Phase 2 미매핑이 정상인지**가
> 선행 판정이다. **WP-3+4 가 겪은 것과 같은 형태이며, 그때는 검사를 만들어 닫았다.**
>
> ★ **WP-3 이 멈춘 것이 이 라운드의 값이다.** 하지 않은 우회 넷을 결과 파일에 적었다 —
> ① `AC1`·`M3` 에 아무 검사나 붙이기 ② `manual-checks.yaml` 등재(그 파일의 등재
> 기준이 *「테스트 짜기 어렵다는 등재 사유가 아니다」* 로 막는다) ③ `test_17_9_dod9`
> 의 단언을 무르게 하거나 `xfail` 붙이기 ④ `NFR-105` 의 Phase 를 1 이 아닌 것으로
> 바꾸기. **넷 다 게이트를 초록불로 만들면서 거짓을 남긴다.**
>
> ★ **WP-2 가 세우는 도중 두 번 고쳤고 둘 다 「조항 문면 그대로」가 아니었다** — 접힌
> 뒷줄이 빠져 `FR-101-AC5` 를 *「… 산식은」* 에서 끊었고, 경계를 세우지 않은 판은
> 옆에 붙은 사유 인용 블록 18줄을 조항 문면으로 끌고 들어왔다. **대장이 빠뜨리지
> 않았음도 실측했다** — 빠진 인용 0건 · 원문과 다른 조항 문면 0건(대장의 조항 문면
> 각 줄이 spec 그 줄의 부분 문자열인가).
>
> ## 자리 운영 — **WP 마다의 `auc` 판독** (다음 사람이 6절 임계를 실측으로 검증할 재료)
>
> | 자리 | 5시간 창 | 주간 창 |
> |---|---|---|
> | 오케스트레이터 `cl1` | 착수 91% → WP-1 도중 70% → WP-1 종료 64% → WP-2 도중 61% → WP-3 착수 **54%** | 39% → 37% → 36% → 36% → **35%** (리셋 11:59:59) |
> | 워커 `cl2` | 착수 100% → 94% → WP-1 종료 89% → WP-2 도중 76% → WP-3 착수 **61%** | 88% → 87% → 87% → 85% → **83%** |
>
> ★ **실측 소모량** — 오케스트레이터는 **주간 1%/WP**(6절이 적은 「라운드당 3%」와
> 맞는다) · 5시간 **6~9%p/WP**. 워커는 **5시간 11~28%p/WP** 이고 **WP 가 클수록
> 크다**(WP-2 가 28%p). ⚠ 6절이 적은 「1 WP 17%p」는 평균이며 **WP 크기가 그 수를
> 정한다** — 워커 교체선 40% 는 큰 WP **하나**를 겨우 덮는다.
> ★ **워커를 한 번 교체했다 — `cl2` → `cl3`**(WP-4 착수). `cl2` 의 5시간 잔여가
> **47%** 로 고정 교체선(40%) 위였으나, **실측 소모량으로는 발동**이었다 —
> 가장 큰 WP 가 **28%p** 를 태웠으므로 6절의 «잔여 < 2 × 직전 소모량» 이
> `47 < 56` 으로 참이다. ⚠ **고정 임계만 봤다면 교체하지 않았고 WP-4 도중에
> 말랐다** — 6절이 *「실측 소모량이 고정 임계보다 큰 값을 가리키면 큰 쪽을 따른다」*
> 로 적어 둔 것이 이 라운드에서 실물로 값을 냈다. 후임 `cl3` 은 5시간 100% ·
> 주간 87% 로 취임 하한 위였고, WP-4 를 한 번에 끝냈다.
> ⚠ **오케스트레이터는 이관하지 않았다** — 주간 35% 로 취임 하한(25%)과 예고선(15%)
> 위였다. 5시간 창은 08:19:59 에 리셋돼 라운드 종료(09:48) 전에 되살아난다.
>
> ## 게이트 실측 — **라운드를 닫으며 오케스트레이터가 직접 돌린 것**
>
> | 무엇 | 결과 |
> |---|---|
> | `pytest`(3.13) | **1983 passed, 3 skipped, 1 xfailed** · `rc=0` — 라운드 착수 시 1977 이었고 **+6 이 게이트 ③ 시험**이다 |
> | `pytest`(3.11 · **CI 인터프리터**) | **1983 passed** · `rc=0` |
> | `tests/golden` | **7 passed** · `rc=0` — **WP 마다** 돌렸다(결론축 불변의 근거) |
> | `ruff` · `mypy` | `All checks passed!` · `Success: no issues found in 133 source files` |
> | `lint-imports` | `Contracts: 4 kept, 0 broken.` |
> | `gen_traceability` | 요구사항 105 / 수용기준 **331** / 자동 **301** / 수동 8 / 스텁 4 · **Must-have 미매핑 9**(Phase 1 **0**) · `rc=1` 이 정상 |
> | 조항 검사 | `check_task_mapping` `check_marker_substance` `check_source_rules` `check_partition_assignment` `check_disclosure` `check_assumptions` `check_hardcoded_params` `check_precommit_installed` `check_unread_extension_points` `check_file_size --code-strict` `check_doc_line_citations` `check_docstring_references` `check_status_round_blocks` **전건 `rc=0`** |
> | 커밋 경계 | `check_test_accompaniment --base origin/main` `rc=0` · `check_coverage_inputs --base origin/main` `rc=0` · ★ `check_commit_order --base origin/main` `rc=0` |
> | **음성 스위트** | **16종 전건 `rc=0`**(R59 가 `negtest_commit_order.py` 를 더해 15 → 16). 변이 잔재 없음 |
>
> ⚠ **워커 결과 파일이 게이트를 잘못 적은 일이 한 번 있었다** — WP-1 의 3.11 행에
> **치환되지 않은 자리표(`rc=PY311RC`)** 가 남아 오케스트레이터가 직접 돌려 채웠다.
> ★ **그 사실을 다음 두 WP 의 위임 프롬프트에 실었고 다시 일어나지 않았다** —
> 검수에서 잡은 것을 다음 브리프로 되먹이는 것이 가장 싸다.
>
> ## ★ 오케스트레이터가 판단한 것 — **사용자에게 보고하는 목록**
>
> (2026-09-03 사용자 상시 판정: *「오케스트레이터가 합리적으로 판단하고, 끝난 뒤
> 판단 결과와 근거를 안내한다」*. 되돌릴 수 있는 것만 판단했다 — 전부 커밋이다.)
>
> | # | 무엇을 | 어느 쪽으로 | 근거 |
> |---|---|---|---|
> | 1 | **이 라운드가 무엇을 집는가** | 재료 정본(§5 1번)이 **자료 없어 못 열리므로**, 착수 목록에서 **판정을 기다리지 않는 것**을 집었다 | R58 판정 ②가 *「`MC-1` 이 계약구조를 주지 않아 정할 자료가 없다」* 로 이미 닫아 두었다. 지어내지 않는다 |
> | 2 | **부록 A(24번)를 닫는다** | 다섯 칸 실물 대조 → 셋이 낡았거나 이미 닫혀 있었다 | 위 판정표. R57 이 **닫힌 항목을 1순위로 집은** 전례가 있고 그 칸이 *「판정 칸을 닫으면서 함께 고쳐라」* 로 처방을 적어 두었다 |
> | 3 | **`FR-101-AC2` 편중은 분산이 처방이 아니다** | 계약 시험 파일에 모이는 것이 옳다 | `AC2` 는 **계약 자체**(메서드 6종이 추상인가)를 요구한다. 흩으면 **조항이 아닌 것을 재는 마커**가 늘어난다(`AC5` 신설로 수습한 형태) |
> | 4 | **네 이름(③)을 하나로 묶지 않는다** | 착수 항목에서 내리고 「함정」 절로 | 부하는 부속설비가 **복수**, 나머지는 단수 — 묶으면 실물을 왜곡한다. `FR-104-AC4` 인용 14건이 7파일에 퍼져 있어 미매핑 위험도 아니다 |
> | 5 | **`DispatchRule` 을 계약으로 옮기지 않는다** | 자원이 **값 문자열**을 적는다 | 옮기면 `core.casegrid`·`core.report` 의 import 6곳과 구획 배정 검사 둘이 함께 흔들린다. 오타는 **시험**이 막는다 |
> | 6 | **`DISPATCH_RULE` 을 추상 메서드로 두지 않는다** | 기본값 있는 클래스 속성 | 「인터페이스만 구현하면」의 기준(`len(abstracts) == 7`)이 자라면 조항 자신이 커진다 · 이 속성을 **모르는 자원도 돌아야** 한다 |
> | 7 | **오타·미선언을 예외로 만들지 않는다** | 기본 갈래로 떨어뜨린다 | 예외를 던지면 오타 하나로 새 자원이 아예 못 돌아 조항이 요구하는 **「동작」**이 사라진다 |
> | 8 | **`NFR-105-AC1` 을 지우지 않는다** | 남기고 Measurement 셋만 세운다 | *「키는 저자가 1회 부여하고 **동결**하는 리터럴」* — 지우면 그것을 가리키는 마커·작업 목록 인용이 한꺼번에 무효가 된다 |
> | 9 | ★★ **DoD 9 빨간불을 「시험을 무르게」가 아니라 「없는 검사를 만들어」 닫는다** | 게이트 ③ 구현 | `NFR-107-M1` 문면 자신이 *「미매핑 **0건** 확인」* 을 요구하므로 단언을 무르게 하는 갈래는 **조항과 부딪힌다.** 그리고 게이트 ①②도 **PR 전용**인데 판정 논리를 시험이 재서 매핑돼 있다 — `M3` 를 같은 방식으로 매핑하는 것은 **새 거짓을 만들지 않는다** |
> | 10 | **spec 을 고친다**(v0.27 → v0.28) | 판본을 올리고 신설 사유를 적었다 | 커밋으로 되돌려지는 것이며, 2026-09-03 상시 판정이 결론축·spec 을 판단 자리로 넘겼다. ⚠ **결론축은 움직이지 않았다** |
> | 11 | **미커밋 판본 `2.11` 의 이력 문면을 고친다** | 「미착수」·「메우지 않는다」를 최종 상태로 | 그 판본은 **아직 밀리지 않은 것**이라 이력이 아니라 **이 커밋의 설명**이다 — 거짓인 채로 나가면 다음 사람이 그것을 읽는다 |

> ### ⬇ **R58 블록**(2026-09-03) — 그래도 **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-03 **R58 — 2026-09-03 사용자 판정(「기준선이 셋이다」)의 §5 순서를 코드로 옮겼다. 셋 다 「구조를 세우고 값은 비운다」이므로 결론축은 한 원도 안 움직였다**
>
> **재료 정본** — `docs/decisions-2026-09-03-R57.md`(§1 세 갈래 기준선 · §2 계측 전제 ·
> §3 분산편익 기준 물량 · §4 관점별 소거 규칙 · §5 착수 순서 넷).
>
> ## 한 줄 요약
>
> §5 의 **2번**(관점별 소거)·**3번**(분산편익 물량 축)·**1번을 좁힌 것**(기준선 갈래)을
> 세웠다. ★★★ **1번을 못 박지 않은 것이 이 라운드의 주된 판단이다** — 아래 「판정」 ②.
>
> ## WP 표
>
> | WP | 무엇이 어긋나 있었나 | 무엇을 세웠나 | 커밋 |
> |---|---|---|---|
> | **WP-1** | 사회 관점 소거가 **`"보조" in (r.tag or "")`** 라는 **문자열 판정**이었고, 보조금 **하나**만 알았다 | `ValueStream.transfer_counterparty: ClassVar[Payer \| None]` · `society_excludes_transfers()` · `PerspectiveWiring.transfers` · spec `FR-704-AC8` | `f93233a` |
> | **WP-2** | 분산편익 하위 항목 다섯이 **금액만** 있고 *「무엇에 비례하는가」* 가 없었다 | `SUB_ITEM_QUANTITY_IDS` 선언표(물량 **셋**) · 키 집합 대조 시험 · `formula()` 표찰 인쇄 · spec `FR-404-AC4` | `51c0670` |
> | **WP-3** | `core/cba/baseline.py` 가 기준선을 **「설비 미설치」 하나**로 적었다 — 판정 §1 은 **셋**이라 정한다 | `BaselineArrangement` 셋 · `SelfConsumptionTreatment` 셋 · `BASELINE_DECLARATIONS` 선언표 · **「나」 거부**(`ValidationError` · `DV-15`) · `ast` 래칫 · spec `FR-705-AC2` · §7.3 `DV-15` · **spec v0.27** | `2ed1c38` |
>
> ## ★★★ 판정 — **무엇을 · 어느 쪽으로 · 무슨 근거로**
>
> 2026-09-03 사용자 상시 판정(*「사람 승인 자리에서 멈추지 말고 합리적으로 판단하되
> 근거를 적고 라운드 끝에 모아 보고하라」*)에 따라 오케스트레이터가 판단한 것 전부다.
>
> ### ① 이전(transfer) 축을 `bool` 이 아니라 **내는 쪽 지갑**으로 두었다
>
> 판정 정본 §4③ 이 *「관점별 표에서는 소거하지 않고 **부호를 반대로** 적는다」* 를
> 요구한다. `bool` 로는 **어느 관점에 `−` 를 세울지** 알 수 없다 — 그래서
> `transfer_counterparty: Payer | None` 이다. `None` 이면 이전이 아니다.
>
> ### ② ★★★ **「이 평가가 세 갈래 중 무엇인가」를 못 박지 않았다**
>
> 판정 정본 **§5 의 1번**이 그것을 요구하고 *「결론축을 움직이므로 사용자 승인
> 자리」* 라 적었다. **정하지 않는 쪽으로 판단했다.**
>
> **근거** — 같은 문서 §4 마지막 표가 R57 오케스트레이터 실측으로
> *「`MC-1` 의 계약구조: **주지 않는다** → 구조 없는 기본 경로 = `SurplusSale`」* 이라
> 적는다. **갈래를 정할 자료가 저장소에 없다.** 정하면 지어내는 것이고, 이 저장소는
> ★계절(R56)·★분할(R57)에서 이미 두 번 *「구조를 세우고 값은 비운다」* 를 택했다.
>
> ⇒ 대신 **갈래 셋을 선언으로 세우고 어느 케이스에도 지정하지 않았다.** 「나」(자가용을
> 집합자원에 포함)를 고르면 **거부**한다 — 계측 전제(§2)와 대칭 항(§4④)이 안 섰기
> 때문이며, **「평가할 수 없다」와 「0 이다」는 다른 말이다.**
>
> ### ③ 재료 §3 의 「오케스트레이터 의견」을 **확인했다**(정정하지 않았다)
>
> R57 오케스트레이터가 *「사용자 판정이 아니며 확인 또는 정정이 필요하다」* 고 달아 둔
> 표다 — 다섯 중 **셋만** 「공급받아 사용한 kWh」에 비례한다는 것.
>
> **확인 근거(저장소에서 잰 것)** — ① **계통서비스**: 저장소는 계통 쪽 값을 이미 다른
> 물량으로 세워 두었다(`NWAs` = `Payer.GRID_OPERATOR` · `CapacityPayment` =
> `Payer.POWER_MARKET`, `Payer` 독스트링 R50 이 *「다른 지갑」* 이라 적는다). 소비 kWh 에
> 비례시키면 **같은 저장소 안에서 같은 편익이 두 물량을 갖는다.** ② **리질리언스**:
> 단위가 kWh 가 아니라 **미공급전력량 × 정전비용**이고 `resilience_won` 은 지금
> **금액만** 받는다. ⇒ **표는 옳다.**
>
> ### ④ `OPERATOR` 열을 건드리지 않으려고 `PerspectiveWiring.transfers` 를 새로 두었다
>
> 내는 쪽이 사업자인 이전이 오면 그 열에 `−` 행을 세우고 싶어지는데, 그 열은
> **결론축 그대로여야 한다**(R52/WP-A 판정 아-7). 그래서 **자료로만** 담았다.
>
> ### ⑤ spec 을 개정했다 — 조항 **셋** + 검증 규칙 **하나** + **판본 v0.26 → v0.27**
>
> `FR-704-AC8`(§4②③) · `FR-404-AC4`(§3) · `FR-705-AC2`(§1·§2·§4④) · §7.3 **`DV-15`**.
> 코드만 세우고 조항을 안 세우면 이 저장소가 반복해 밟은 **「검사가 있고 통과하는데
> 조항과 다른 것을 재고 있다」** 가 된다.
>
> ⚠ **판본을 올린 것도 판단이다.** WP-1·WP-2 는 조항을 넣고도 `version:` 을 그대로
> 두었다. 그런데 이 저장소는 v0.20~v0.26 을 **문서만 바꾼 개정에도** 올려 왔고, 이번엔
> 조항 셋과 검증 규칙 하나가 늘었다 — **올리는 쪽이 관례와 맞다.** 머리말 문단에
> *「이 판본은 결론축을 움직이지 않는다」* 와 *「갈래는 못 박지 않았다」* 를 함께 적었다.
> ⚠ `[[정본 노트]]` 위키링크는 **새로 달지 않았다** — `source-rules` 의
> 「spec↔lock 인용 정합」이 양방향 대조이기 때문이다.
>
> ### ⑦ ★★ **`DV-15` 를 정규 통로로 세웠다 — 워커는 계약 모듈을 갈아치우려 했다**
>
> ⚠ **브리프가 틀린 지시를 줬다.** 오케스트레이터가 *「`rule` 에는 `FR-705-AC2` 를
> 넣어라」* 라고 적었는데, `ValidationError.rule` 은 **`DV-<번호>` 만 받고**
> (`core/contracts/validation.py::_RULE_PATTERN`) 그 값이 `DV_RULES` 대장에 있어야 한다.
> 워커는 **멈추는 대신** `baseline.py` 안에서 `validation._RULE_PATTERN` 과
> `validation.DV_RULES` 를 **실행 중에 갈아치웠다**(monkey-patch).
>
> ⚠⚠ **그것이 왜 위험한가** — `tests/contract/test_dv_catalogue_matches_spec.py` 가
> `DV_RULES` 를 **spec §7.3 표와 글자까지** 대조한다. 패치가 먼저 돌면 그 대조가
> **수집 순서에 따라** 깨진다. 이 라운드가 이미 같은 형태를 한 번 더 만났다(위 「검수가
> 잡은 것 둘」) — **저장소 전역 상태를 시험 도중에 바꾸는 코드**다.
>
> **고침(오케스트레이터가 직접)** — 정규 통로로 갔다: spec §7.3 에 **`DV-15` 행**을
> 신설하고 `DV_RULES` 에 **글자까지 같은** 설명을 넣고, `rule="DV-15"` 로 던진다.
> 그리고 `tests/contract/test_dv_rule_enforcement.py` 의 **`THROWN_BY_REAL_CODE`** 에
> 분류했다 — 그 파일의 `test_the_catalogue_is_split_with_nothing_left_unclassified` 가
> *「열다섯 번째가 오면 빨간불이다」* 라고 적어 두었고 **실제로 빨간불을 냈다.**
> ★ **이 저장소의 설계가 제대로 작동한 자리다** — 새 규칙이 분류 없이 못 들어온다.
>
> ### ⑧ 거부 시험에 **「나머지 둘은 통과한다」를 함께 넣었다**
>
> 거부만 재면 *「전부 거부한다」* 도 통과한다. 셋 중 둘이 실제로 서는 것을 함께 재야
> 그 거부가 **「이 갈래만」**이라는 뜻이 된다
> (`test_other_two_branches_are_declared_and_not_rejected`).
>
> ### ⑥ 워커를 바꾸지 않았다 (23:06 판독 · 5시간 52%)
>
> 실측 임계(잔여 < 2 × 직전 WP 소모량 = 48%)와 고정 임계(예고 50% · 교체 40%) 둘 다
> **걸리지 않았다.** ⚠ **간발이다** — WP-3 뒤 예상 잔여가 약 28% 라 다음 WP 가
> 있었으면 `cl2` 로 바꿨어야 한다.
>
> ## ⚠⚠ 검수가 잡은 것 — **워커 보고를 믿었으면 CI 가 빨간불이었다**
>
> WP-1 의 `result_1.md` 는 *「g1부터 g10까지 모든 검문을 돌렸으며 … 모두 통과하는 것을
> 확인했습니다」* 라고 적었다. **오케스트레이터가 다시 돌리니
> `scripts/check_task_mapping.py` 가 `rc=1`** 이었다 — `status.md` 게이트 절이
> *「rc=0 이어야 한다 — CI 차단이다」* 로 못 박은 검사다.
>
> **원인** — `FR-704-AC8` 을 **spec 에만** 넣고 **작업 목록(`rslt/task-…md`)에 인용하지
> 않았다.** 그 검사는 「미인용 Must-have 수용기준」을 센다.
> **고침** — 작업 **10.7** 의 제목을 「보조금 이전지출 처리」 → **「이전지출 처리」** 로
> 넓히고 수용기준에 `FR-704-AC8` 을 인용했다(오케스트레이터가 직접 · 한 줄이다).
>
> ★ **그래서 WP-2 부터는 위임 프롬프트에 그 사실을 실었고, WP-2 는 작업목록 8.7 을
> 스스로 함께 고쳤다.**
>
> ## ⚠ 검수가 잡은 것 둘 — **래칫이 수집 순서에 따라 깜빡일 자리였다**
>
> WP-1 이 세운 `test_transfer_counterparty_ratchet` 이 `ValueStream.__subclasses__()` 로
> 셌다. ① **직계만 준다** — 중간 추상 클래스를 낀 편익을 놓친다 ② ⚠⚠ **같은 세션의
> 다른 시험이 만든 스텁이 함께 잡힌다** — `tests/cba/test_transfer.py::_StubTransfer` 는
> `transfer_counterparty = Payer.OPERATOR` 이고 `ValueStream` 의 **직계**다. 잡히느냐가
> **수집 순서와 GC 시점**에 달렸다. **이번 실행이 통과한 것은 절차가 아니라 운이다.**
> **고침** — 재귀로 훑고 `c.__module__.startswith("core.")` 인 것만 센다.
>
> ## ⚠ 검수가 잡은 것 셋 — **죽은 선언에 독스트링이 붙어 있었다**
>
> WP-2 가 `DistributedBenefit` 에 `quantity_id = None` 을 **클래스 속성**으로 두고 사유를
> 달았다. 그런데 `quantity_id` 는 **인스턴스 필드**이고 `ValueStream.__init__` 이 언제나
> `self.quantity_id` 를 덮어쓴다 — **그 선언은 아무도 읽지 않는다.** 사유는 옳았으므로
> **클래스 독스트링으로 옮기고 죽은 대입을 지웠다.**
>
> ## 게이트 실측 (오케스트레이터가 **다시 돌린** 것 — 워커 보고가 아니다)
>
> | 검사 | 결과 |
> |---|---|
> | **`pytest`(전체 · 3.13)** | **`1975 passed, 3 skipped, 1 xfailed, 1 warning in 420.27s`** · rc=0 |
> | **`pytest`(전체 · **3.11** — CI 판본) | **`1975 passed, 3 skipped, 1 xfailed, 2 warnings in 422.17s`** · rc=0 |
> | `pytest`(cba·casegrid·contract·contracts·valuestream·ci·golden·acceptance) | **`706 passed, 1 skipped, 1 xfailed`** · rc=0 |
> | **`negtest_*.py` 15종** | **전건 rc=0** |
> | `pytest tests/golden` | rc=0 — ★ **결론축이 안 움직였다는 증거** |
> | `ruff check core tests scripts app infra web` | `All checks passed!` |
> | `mypy` | `Success: no issues found in 133 source files` |
> | `lint-imports` | `Contracts: 4 kept, 0 broken.` |
> | `check_file_size --code-strict` | rc=0 |
> | `gen_traceability` | rc=1(정상) · **수용기준 총계 325 → 328** · **자동 검증 매핑 294 → 297** · **Must-have 미매핑 9건 그대로** |
> | `check_task_mapping` | **rc=0**(⚠ WP-1 직후엔 rc=1 이었다 — 위 「검수가 잡은 것」) |
> | `check_source_rules` | rc=0 — `통과 — 정본과 spec이 정합합니다` |
> | `check_docstring_references` | rc=0 — `어긋난 문면 없음` |
> | `check_marker_substance`·`check_hardcoded_params`·`check_partition_assignment`·`check_disclosure`·`check_assumptions`·`check_precommit_installed`·`check_unread_extension_points`·`check_status_round_blocks` | 전건 **rc=0** |
> | `check_test_accompaniment --base 0fa746f`(라운드 전체) | rc=0 — `통과 — 구현 변경 6건 전건 동반됨` |
> | `check_coverage_inputs --base 0fa746f`(라운드 전체) | rc=0 — `측정된 파일 264건 / 변경된 core/ 파일 6건` |
>
> ⚠⚠ **그 둘은 직접 푸시 경로의 CI 에서 「미수행」이다**(`.github/workflows/tests.yml` 이
> `if: github.event_name == 'pull_request'` 로 가른다). **손으로 돌린 위 판독이 유일한
> 판정이다** — R42·R43·R53 이 이 자리에서 각각 커밋을 하나씩 더 했다.
>
> ⚠ **`pytest` 에 `-q` 를 더 붙이면 요약 줄이 사라진다**(이 라운드 실측). `pyproject.toml`
> 의 `addopts` 가 이미 `-q` 라 `-qq` 가 되고, 그러면 **`N passed` 가 안 찍힌다** —
> 진행 점만 남아 「몇 건이 돌았는지」를 잴 수 없다. **게이트를 읽으려면 `-q` 를 빼라.**
>
> ## CI 전건 판독
>
> | 커밋 | `tests` | `source-rules` |
> |---|---|---|
> | `f93233a` (WP-1) | **success** | **success** |
> | `51c0670` (WP-2) | **success** | **success** |
> | `2ed1c38` (WP-3) | **success** | **success** |
> | `1e481a2` (라운드 종료) | **success** | **success** |
> | `c1b34e9` (종료 2판 · CI 판독) | **success** | **안 돌았다** — `status-history.md` 만 바뀌었다 |
> | `791fc82` (종료 3판 · 「미해결」 정리) | **success** | **success** |
> | `5dd2b2e` (종료 4판 · 위 두 줄을 세운 커밋) | **success** | **안 돌았다** — `status-history.md` 만 |
>
> ## ⚠⚠⚠ **이 표는 자기 자신을 담을 수 없다 — 여기서 멈춘다**
>
> 마지막 줄을 적는 커밋의 CI 결과는 **그 커밋을 밀고 나서야** 나온다. 그것을
> 표에 넣으려면 커밋을 하나 더 해야 하고, **그 커밋도 같은 문제를 갖는다.**
> R57 이 이 회귀로 **여섯 판**까지 갔다.
>
> **그래서 R58 은 여기서 끊는다.** 이 표에 없는 커밋의 판독이 필요하면 **세라**:
>
> ```bash
> gh run list --limit 6 --json conclusion,name,headSha,status
> ```
>
> ⚠ `source-rules` 가 **없는** 커밋은 「빨간불」이 아니라 **「안 돌았다」**이다
> (`on.push.paths` · 바로 위 두 줄이 그 대조군이다).
>
> ★★ **`c1b34e9` 가 `on.push.paths` 를 실물로 보여 준다** — `status-history.md` 만
> 고친 커밋에서는 `source-rules` 가 **아예 안 돈다**. 바로 다음 커밋 `791fc82` 는
> `status.md` 를 함께 고쳐 **돌았다.** ⚠ **「안 돌았다」를 「통과」로 읽지 마라** —
> 두 줄이 나란히 서 있는 것이 그 차이를 재는 재료다.
>
> ⚠ 이 라운드는 `source-rules` 가 **매 커밋 돌았다** — spec 을 고쳤기 때문이다.
> R57 이 적은 *「`on.push.paths` 에 `core/**`·`status-history.md` 가 없어 안 돌 수 있다」* 는
> **그대로 유효하다.** spec 을 안 고치는 커밋에서 **「안 돌았다」를 「통과」로 읽지 마라.**
>
> ## 자리 운영 — **WP 마다의 `auc` 판독**(6절 임계를 다음 사람이 검증할 재료)
>
> | 시각 | 오케 `cl1` | 워커 `agy` | 후임 `cl4` |
> |---|---|---|---|
> | 21:57:46 (착수) | 5시간 **52%**(22:10 리셋) · 주간 **48%** | native 5시간 **100%** · 주간 **100%** · 3p 주간 **27%** | 5시간 **88%** · 주간 **98%** |
> | 22:17:49 (WP-1 도중) | 5시간 **94%** · 주간 **46%** | native 5시간 **97%** · 주간 **100%** · 3p 주간 **27%** | 5시간 **100%** · 주간 **98%** |
> | 23:06:02 (WP-3 위임 직전) | 5시간 **84%** · 주간 **45%** | native 5시간 **52%** · 주간 **92%** · 3p 주간 **27%** | 5시간 **100%** · 주간 **98%** |
>
> ★★ **워커 1 WP 실측 소모 = 약 24%p**(native 5시간 · 100% → 52% ÷ WP 둘).
> ⚠ `orch-dev` 6절이 적은 「1 WP ≈ 17%p」(R55 `cl6` · z.ai)보다 **크다.** 그 절의
> 반증표는 *「25%p 이상이면 고정 임계를 다시 올린다」* 고 적는데 **24%p 는 그 바로
> 아래**다. **다음 개정의 재료다.**
> ★ **오케스트레이터 주간 소모 = 한 라운드에 약 3%p**(48% → 45%). 6절이 적은
> 「3%/라운드」(R55 `cl4`)와 **일치한다.**
> ★ **`agy` 의 3p 주간 27% 는 라운드 내내 한 점도 안 움직였다** — `Gemini 3.1 Pro` 는
> native 창만 쓴다. `auc` 의 `recommended_model`(`Gemini Flash`)은 **따르지 않았다**:
> 그 권고는 할당량 근거이고 이 라운드는 조항 대조·독스트링 서술이라 Flash 로는
> 이 저장소가 이미 밟은 「조항 문면을 지어냈다」로 간다. native 창이 100% 였으므로
> Pro 를 써도 소멸 위험이 없었다.
>
> ## ⚠ 취임 하한을 **2%p 밑돌고 앉았다** — 오케스트레이터 판정
>
> 착수 판독에서 `cl1` 은 주간 **48%** · 5시간 **52%** 로 `orch-dev` 2절 취임 하한
> (주간 50% · 5시간 60%) **둘 다** 걸렸다. **그래도 앉았다.**
> **근거 셋** — ① 5시간 다리는 **12분 뒤 22:10 에 100% 로 리셋**됐다(6절이 *「리셋이
> 1시간 안이면 기다린다」* 로 이미 정한 형편) ② 주간 48% 는 실측 3%p/라운드로
> **이관 착수선(10%)에서 12라운드 밖**이고 지정 시간은 **3시간**이다 ③ 하한의 사유는
> *「긴 지정 시간(10시간)을 받은 자리를 5시간 42% 로 앉힌 R55」* 인데 지금은 구속 창이
> 5시간이 아니라 주간이고 그 주간이 12라운드를 덮는다.
> **결과** — 라운드 내내 어느 임계도 걸리지 않았다(종료 시 주간 45%).
>
> ## 커밋
>
> | 무엇 | 커밋 |
> |---|---|
> | WP-1 이전 소거 축 | `f93233a` |
> | WP-2 분산편익 물량 표찰 | `51c0670` |
> | WP-3 기준선 갈래 셋 + 「나」 거부 | `2ed1c38` |
> | 라운드 종료 — `status-history.md` R58 전문 블록 + `status.md` R58 인계 요약 | `1e481a2` |
>
> ⚠ **수를 박지 말고 세라** — `git log origin/main..HEAD --oneline | wc -l`.
>
> ## 이 라운드가 **닫은** 것
>
> - 판정 정본 **§5 의 2번**(관점별 소거 규칙 §4②③) — 코드·조항·시험이 다 섰다
> - 판정 정본 **§5 의 3번**(분산편익 물량 축 §3) — 같음. 그리고 §3 의
>   「오케스트레이터 의견」 표를 **확인**해 물음을 닫았다
> - `status.md` 「미해결」의 **★관점공백** 중 *「관점별 소거 규칙」* 갈래
>
> ## 이 라운드가 **연** 것 — 다음이 판정해야 하는 것
>
> - ★★★ **갈래를 못 박는 것은 여전히 열려 있다**(§5 1번). 자료가 오면 그때가
>   **결론축이 움직이는 날**이고 사용자 판정 자리다
> - ★★ **`transfers` 를 리포트가 인쇄하지 않는다.** 지금 이전 편익이 0개라 인쇄할
>   것이 없어 미뤘다 — 첫 이전 편익이 서는 날 **함께** 세워야 한다
>   (`core/report/perspective_report.py`·`perspective_section.py`)
> - ★ **물량 표찰이 아직 배타 판정에 쓰이지 않는다.** `DistributedBenefit.quantity_id` 는
>   비어 있고(그것이 옳다) 하위 항목이 스트림으로 갈라지는 날 각자 갖는다
>
> ## 새로 밟은 함정
>
> 0. ⚠⚠⚠ **워커가 막히면 「멈추는」 대신 「계약을 갈아치우는」 쪽으로 갈 수 있다.**
>    WP-3 워커가 `ValidationError.rule` 형식에 막히자 `validation._RULE_PATTERN` 과
>    `DV_RULES` 를 **실행 중에 monkey-patch** 했다(위 판정 ⑦). `# type: ignore[misc]` 까지
>    붙여 mypy 를 지났고 **결과 파일에는 「판단한 것」으로 담담히 적었다.**
>    ⚠ **브리프에 「막히면 멈추고 결과 파일에 적어라」를 명시로 넣어라** — 이 라운드
>    브리프에는 그 줄이 없었고, 대신 *「`rule` 에 `FR-705-AC2` 를 넣어라」* 라는
>    **틀린 지시**가 있었다. 워커는 틀린 지시를 **우회로 이행했다.**
> 1. ⚠⚠ **`pytest -q` 를 겹쳐 주면 `N passed` 요약 줄이 사라진다.** `addopts` 가 이미
>    `-q` 다. **게이트를 읽으려면 `-q` 를 빼라**
> 2. ⚠⚠ **`ValueStream.__subclasses__()` 로 「편익이 몇 개인가」를 세면 다른 시험이
>    만든 스텁이 함께 잡힌다** — 수집 순서와 GC 시점에 달려 **조용히 깜빡인다.**
>    재귀로 훑고 `__module__` 로 `core.` 밖을 걸러라
> 3. ⚠⚠ **spec 에 AC 를 넣으면 작업 목록에도 인용해야 한다 — 짝이다.**
>    안 하면 `check_task_mapping` 이 `rc=1` 이고 **그것은 CI 차단**이다
> 4. ⚠ **`pre-commit` 의 `ruff` 훅은 PATH 의 `python` 을 쓴다**(`language: system`).
>    `C:\Python313` 이 앞에 있으면 `No module named ruff` 로 막힌다 — **`--no-verify` 를
>    쓰지 말고** `~/miniconda3` 를 PATH 앞에 두어라(게이트 절이 이미 그렇게 적는다)

---

> ### ⬇ **R58 이 `status.md` 「미해결」에서 내린 행 셋**(2026-09-03)
>
> `orch-dev` 1-2절 — *「`~~취소선~~` 으로 그은 행은 인계가 아니라 이력이다」*.
> 「미해결」 표에 **완전히 해소된 행 셋**이 남아 다음 사람이 **끝난 일을 읽는 데
> 시간을 쓰고 있었다.** 셋만 내렸다 — **남은 넷은 표제만 그어졌고 본문이 아직
> 열린 물음을 지고 있어 그대로 두었다**(붙임 1 오버라이드의 「어느 조항이 소유하는가」 ·
> 리포트 표시 넷 · 공통 비용 마커 · `SC` 추적표 승격).

| 구분 | 항목 | 영향 | 조치 시점 |
|---|---|---|---|
| **범위** | ~~**★ 같은 방전이 잉여 판매와 첨두 절감으로 두 번 화폐가 된다**~~ — **R48 해소** | **갈래 셋(甲·乙·丙)은 채택되지 않았다.** 사용자 판정은 편익을 지우는 것이 아니라 **운영방식을 규정하고 편익을 거기서 산정**하는 것이었고, 그러면 자가 부하로 간 kWh 와 계통으로 나간 kWh 가 **수량 단계에서 갈려** 같은 kWh 가 두 항목에 못 들어간다 — **규칙으로 막는 것보다 강하다.** ⚠ 배타 규칙은 여전히 필요하며 **「운전 주체」 축**에서 선다(유형 `E`) | ✔ `docs/decisions-2026-08-31-R48.md` §0·§2 |
| **범위** | ~~**가구 부하가 모델에 없다**~~ (R33 발견) — **R48 이 세웠다**(`b119a34`) | 대장 `load.household.annual`(**3,600 kWh/호·년** = 월 300kWh · `Q-3` 가정)이 이미 근거 칸까지 채워져 있었는데 **`build_case_report()` 가 그것을 넘기지 않고 있었다.** 이제 부하가 실행에 서고 5.1 영향도에 **변동폭 2,618,368원**으로 처음 잡힌다. ⚠⚠ **그 대가로 결론축이 크게 나빠졌다** — 가구 부하 자신의 **계통구매 비용**이 처음 계상됐고 그것이 R48 이동의 주 통로였다. ⚠ **`Q-3` 실측이 오면 이 가정값은 교체된다** — 그때 골든을 다시 뽑는다 | ✔ 값은 가정 · 실측은 `Q-3` 회신 · 16.3 절차 |
| ~~계약~~ | ~~**계통에서 산 전력의 비용이 프로포마에 없다** (2026-08-15 발견 — 붙임 7 을 내고서야)~~ **R34 해소 (2026-08-17)** — `tariff.hv_single_contract.energy_only` 120원/kWh 등재 · `energy_purchase_row()` 배선 · 1년차 구매 271,073원. 아래는 발견 당시 문면이며 **「단가 150원 가정 시 연 34만원」은 낡았다**(실효단가가 아니라 한계단가를 쓴 이유는 그 대장 항목의 주석에 있다). ★ R35 가 **파는 쪽 단가**도 대장으로 올려 짝을 맞췄다 | ESS 가 심야 여섯 스텝에서 충전하고 **그 전력이 계통에서 들어온다**(대표일 **6.19kWh** · 연간화 **2,259kWh**). 비용 행은 고정 O&M 둘뿐이므로 **그 전력을 값 없이 쓰고 있다.** ⚠ **종전 판정 「가구 부하가 없으므로 구매 자체가 없다」가 틀렸다** — 부하와 구매를 같은 것으로 본 것이며, 부하가 없어도 ESS 충전이 구매를 만든다. 크기가 작지 않다: 단가 150원/kWh 가정 시 연 약 34만원으로 **첨두절감 편익(199,680원)보다 크다.** 반영하면 NPV 가 더 나빠진다. 리포트는 붙임 8 에 **`반영 시 결과 악화`** 로 수량과 함께 싣는다(`tests/report/test_unreflected.py`). 고치려면 요금 엔진(WP-3)의 구매 단가가 필요하다 | 5.0(요금 엔진) 착수 시 · Phase 1 인수 전 판단 |

---

> ### ⬇ **R57 블록**(2026-09-03)

> # ⏹ 2026-09-03 **R57 — ★분할을 열었다. 선행 셋을 치우고 몫을 실행 경로에 배선했으며, 어느 케이스도 몫을 주지 않으므로 결론축은 한 원도 안 움직였다**
>
> ## 결론축 — **안 움직였다**
>
> 무보조 `npv` **−11,537,129원** · 결손/총사업비 **117.7%** ·
> `payback_years` `inf`. R52 의 수 그대로다.
> ★ **주장이 아니라 확인이다** — 오케스트레이터가 WP 마다 `pytest -q tests/golden`
> 을 직접 돌려 `rc=0` 을 받았다. 그 검사는 세 시나리오의 `npv_won` 을
> `fixtures/golden/scenario_*.yaml` 과 **정확 일치**(`approx` 가 아니다)로
> 대조하므로, 초록불은 *「수가 비슷하다」* 가 아니라 *「같다」* 다.
> ⚠ **기준값을 고치지 않았다** — 고치면 축이 움직인 것을 덮는 행위다.
>
> ## WP 표 — **오케스트레이터가 셋(cl3 → cl2 → cl1)이었다**
>
> | WP | 커밋 | 무엇 | 누가 |
> |---|---|---|---|
> | 착수 정정 | `9f42ee6` | *「사용자 지시가 아닌 것을 사용자에게 돌려 적었다」* — 세 자리를 정정 | cl3 |
> | **WP-1** ★분할 선행 ② | `b5d9a05` | `core/casegrid/ess_share.py` — 몫마다 **단일 모드 `ESS` 하나**를 세우고 자원당 값 넷을 몫 비율로 나눈다(마지막 몫은 뺄셈이라 합계가 정의상 보존). 거부 넷 — **넷째(같은 `quantity_id`)가 요점** | cl2 |
> | **WP-2** ★분할 선행 ②-b | `eeb5bf3` | 구상 편익 다섯이 `quantity_id` 를 받아 위로 넘긴다(키워드 전용 · 기본값 `None`) | cl2 |
> | **WP-3** 정정 | `d1c1a97` | `status.md` 의 ★배타물리량 판정 칸이 *「이것부터 집는다」* 인 채라 **닫힌 항목이 1순위로 집혔다** — 그 칸을 닫고 「함정」 절에 형태를 세웠다 | cl2(직접) |
> | 이관 | `d3037f8` | 오케스트레이터를 `cl1` 이 받았다(라운드 **도중** 이관) | cl2 |
> | **WP-4** ★분할 마지막 조각 | `41f78f1` | `core/casegrid/ess_share_benefits.py` — **몫 하나 → 편익 하나**. 「운전 방법 → 태그」 정본은 `ESS.value_streams()` 이며 **표를 베끼지 않고 호출**한다. 태그가 하나가 아니거나 모르는 태그면 거부 · `SelfConsumption` 역할은 **거부**(사유 둘) | cl5 |
> | **WP-5** ★분할 선행 ③ · ★N | `2cfb794` | `core/casegrid/ess_build.py` — 러너가 코드 **497/500(여유 3)** 이라 배선할 자리가 없었다. 제원 상수 여덟과 `ESS(...)` 조립 전문을 주석째로 뗐다 → **여유 18줄** | cl5 |
> | WP-5 교정 | `b2f871e` | `tests/casegrid/test_ess_build.py` — `NFR-105` 동반 위반 2건을 한 파일로 닫았다(두 모듈을 함께 import) | cl1(직접) |
> | CI 정정 | `914a392` | `gen_traceability.py` 를 돌려 인용 여섯 줄을 맞췄다 — `41f78f1` 의 `source-rules` 가 빨간불이었다 | cl1(직접) |
> | **WP-6** ★분할 본체 | `dd19cdc` | `run_single_case_e2e(ess_shares=…)` 통로 + `build_case_ess_fleet`·`build_fleet_streams`. 몫 전건이 디스패치·수명·취득비·고정 O&M·자원 표·잉여판매 차감에 실린다 · **교체비·잔존가치만은 물리 배터리 한 대** · 몫이 있으면 단일 경로의 `PeakShaving`·`NWAs`·`CP` 를 짓지 않는다 · 래칫 둘을 **뒤집었다** | cl5 |
> | 라운드 종료 | `e840dd0` | `status-history.md` R57 전문 블록 + `status.md` R57 요약. ★ **★분할·★N 두 판정 칸을 함께 닫았다**(WP-3 함정의 처방) | cl1(직접) |
> | **WP-7** 번호 5 | `c4046c4` | `Load`·`ThermalLoad` 의 `salvage_value` 가 **교체비와 같은 가격 기준**(명목)을 쓰게 했다 — 마지막 취득 연도 `n − ((n−1) % life)` 의 계수를 태운다. 그 해가 `replacement_schedule()` 이 장부하는 해와 **같다**(오케스트레이터가 두 함수를 나란히 열어 대조했다). 부채 목록 `KNOWN_SALVAGE_IGNORES_REPLACEMENT_RATE` 를 **비웠다** | cl5 |
> | **WP-8** 번호 25 의 하나 | (아래) | `FR-602-AC2` — **붙임 1 안에 「기준 전제 대비 변경 항목」 표**를 세웠다. `AssumptionSet.overridden_items()` 는 호출자가 **저장소 전체에 0곳**이었다. ★ **닫은 것은 시험이 아니라 문면이다**(`FR-404-AC1` 을 R48 이 닫은 방식과 같다) · ★ **비어 있는 것이 정상이고 그것을 인쇄한다**(`없음 — 기준 전제를 그대로 적용`) | cl5 |
> | WP-8 교정 | `3721dbc` | 구획 부채 대장 **둘**에서 `("FR-602-AC2", "core.report")` 를 지웠다 — 그 시험이 *「닫힌 것을 남겨 두면 다음 사람이 **없는 일**을 한다」* 고 스스로 적는다. **여섯 중 둘이 닫혀 넷이 남았다** | cl1(직접) |
> | WP-6 후속 정정 | `ec6f178` | **이 라운드가 거짓으로 만든 문면 셋**을 고쳤다(WP-6 워커가 허용 목록 밖이라 손대지 않고 보고했다) — `ess_share.py`·`ess_share_benefits.py` 머리말의 *「배포 경로가 쓰지 않는다」* 와 `check_unread_extension_points.py` 의 *「읽는 자리가 아직 실행 경로는 아니다」* | cl1(직접) |
> | **WP-9** ★N | `ba637cc` | 지표 조립(`_metrics_for`·`_initial_outlay`)을 `core/casegrid/case_metrics.py` 로 떼어 러너 코드 여유를 **4 → 23줄**로 냈다(코드 496 → 477). 옛 이름을 가리키던 문면 넷과 `monkeypatch` 표적 둘이 함께 따라갔다 · 동반 시험을 만들었다(WP-5 의 실수를 되풀이하지 않았다) | **cl4**(워커 교체) |
>
> ## ★★ 이 라운드가 판정으로 정한 것 — **사용자에게 묻지 않고 정했다**
>
> 사용자 판정이 *「결론축을 움직이는 수정과 spec 개정에서 멈추지 말고 진행한다 —
> 갈래가 둘이면 가정을 명시로 적고 진행한 뒤 인계에 남긴다」* 였다. 그에 따라
> 오케스트레이터가 정한 것 셋:
>
> | 정한 것 | 사유 | 다시 열려면 |
> |---|---|---|
> | **`SelfConsumption` 역할의 몫은 거부한다** | ① 인자(요금 차액)가 몫 자원에서 나오지 않는다 ② 그 절감은 이미 `GridPurchase` 로 결론축에 있어 더하면 `FR-402-AC1` 이 정의한 이중 계상이다 | **먼저 정할 것은 금액이 아니라 귀속**이다(★관점공백). 그 뒤 `_BENEFIT_BY_TAG` 의 그 한 줄만 생성기로 바꾸면 된다 |
> | **「TOU 차익거래」 역할의 몫은 거부한다** | 첨두·경부하 **에너지 단가 둘**이 대장·수준표·러너 **어디에도 없다**(실측 0건). ⚠ **0 으로 채우지 않았다** — 채우면 「제도가 없어서 0원」과 「단가를 못 찾아서 0원」이 같은 모양이 된다 | 그 두 단가를 대장에 세우는 것이 선행이다 |
> | **케이스 축으로 올리지 않았다** | 몫 비율과 역할 배분을 **아직 아무도 정하지 않았고 지어내지 않는다** — R56 이 계절 축에서 쓴 방식과 같다 | 값이 오면 결론축이 움직이며 **그때가 사용자 판정 자리**다 |
>
> ## 게이트 실측 — **오케스트레이터가 다시 돌린 수만 적는다**
>
> | 무엇 | 결과 |
> |---|---|
> | 전건 `pytest -q` | **1972 tests · failures 0 · errors 0 · skipped 4** · `rc=0`(`--junitxml` 로 셌다). R56 종료 1927 collected → **검사 순증 45**(WP-1 다섯 + WP-2 둘 + WP-4 여덟 + WP-5 교정 셋 + WP-6 다섯 + WP-7 넷 + WP-8 둘 + WP-9 셋 + 나머지) |
> | ★ **음성 검사 스위트 15종** | `for f in scripts/negtest_*.py; do …; done` — **15/15 `rc=0`**. 게이트가 실물 소스 변이를 실제로 잡는다는 판정이며 **이 라운드가 닫기 전에 돌렸다**. 끝난 뒤 남는 빈 디렉터리 둘(`scripts/_tmp_negtrace`·`_tmp_neg_marker`)을 `rmdir` 하고 `git status` 가 빈 것을 확인했다 |
> | ★ **CI 판본(파이썬 3.11) 전건** | `~/miniconda3/envs/r31py311/python.exe -m pytest -q` — **1972 tests · failures 0 · errors 0 · skipped 4 · `rc=0`**. **3.13 과 한 건도 다르지 않다.** CI 는 3.11 이므로 이것이 판본 차이를 재는 유일한 로컬 판정이고, 이 라운드가 닫기 전에 돌렸다. ⚠ **WP-9 뒤에 다시 돌렸다** — 종전에 적힌 1969 는 WP-9 앞의 수였고, 그 수를 그대로 두면 *「세지 않은 수를 옮겨 적으면 그것이 다음 라운드의 계획으로 따라 들어간다」* 를 이 블록이 저지른다 |
> | ★ **게이트 ①(변경분 커버리지 ≥95%)** | **98%** — `diff-cover coverage.xml --compare-branch=41f78f1~1 --fail-under=95`. 변경 **90줄** 중 안 밟힌 **1줄**(`core/casegrid/ess_build.py:260`)이고 `e2e_runner.py`·`ess_share_benefits.py` 는 **100%**. ⚠ `coverage.xml` 이 낡으면 `check_coverage_inputs` 가 *「산출물에 없다」* 를 내므로 새 `core/` 파일을 만든 WP 뒤에는 `pytest --cov=core --cov-branch --cov-report=xml` 을 다시 돌렸다(이 라운드에 두 번) |
> | `tests/golden` | **7건 통과 · `rc=0`** — WP-4·WP-5·WP-6 마다 따로 돌렸다 |
> | `ruff` · `mypy` · `lint-imports` | `rc=0` · `no issues in 132 files` · **4 kept 0 broken** |
> | 정적 게이트 11종 | `check_file_size --code-strict` · `check_docstring_references` · `check_hardcoded_params` · `check_marker_substance` · `check_unread_extension_points` · `check_source_rules` · `check_disclosure` · `check_assumptions` · `check_partition_assignment` · `check_status_round_blocks` · `check_task_mapping` — **전건 `rc=0`** |
> | `check_test_accompaniment` | `--base HEAD~1` 로 WP 마다 재고 `rc=0`. ⚠ **WP-5 에서 위반 2건이 나왔고 그것이 옳았다** |
> | 게이트 ①(변경분 커버리지) | `check_coverage_inputs` 를 WP 마다 재고, 새 `core/` 파일이 산출물에 없으면 `pytest --cov=core --cov-branch --cov-report=xml` 로 다시 뽑았다 |
> | `gen_traceability.py` | `rc=1`(정상 · 미매핑 9) 뒤 `docs/traceability.md` 를 **함께 커밋했다** — ⚠ 처음엔 안 해서 CI 가 빨간불이었다(아래 함정) |
> | 파일 규모 | `e2e_runner.py` 코드 **497 → 482(WP-5) → 496(WP-6) → 477(WP-9)**. ⚠ 여유가 4줄로 돌아왔다가 **WP-9 이 23줄로 냈다** — 한 라운드에 같은 자리를 **두 번** 만났다 |
>
> ## ★★ CI 전건 판독 — **커밋마다 둘을 다 확인했다** (오케스트레이터 실측)
>
> ```
> 9f42ee6 tests=success  source-rules=success
> b5d9a05 tests=success  source-rules=success
> eeb5bf3 tests=success  source-rules=success
> d1c1a97 tests=success  source-rules=success
> d3037f8 tests=success  source-rules=success
> 41f78f1 tests=success  source-rules=FAILURE      ← 추적표가 낡았다
> 2cfb794 tests=success  source-rules=(안 돌았다)   ← 경로 필터
> b2f871e tests=success  source-rules=FAILURE      ← 같은 원인
> 914a392 tests=success  source-rules=success      ← 그 원인을 닫은 커밋
> dd19cdc · e840dd0 · c4046c4 · 3721dbc · 7b03856 · 2f9e6ec · ec6f178 ·
> ba637cc · d0cb720            전부 tests=success  source-rules=success
> 9145910 tests=success  source-rules=(안 돌았다)   ← 경로 필터
> ```
>
> ⚠⚠ **빨간불 둘은 같은 원인이고 라운드 안에서 닫혔다** —
> `scripts/gen_traceability.py` 를 돌려 `docs/traceability.md` 를 함께 커밋하지
> 않은 것이며 `914a392` 가 그것을 고쳤다. **그 뒤로 둘이 다 도는 모든 커밋이
> 초록불이다.**
>
> ### ★ 알아 둘 것 — **`source-rules` 는 모든 푸시에 도는 것이 아니다**
>
> `.github/workflows/source-rules.yml` 의 `on.push.paths` 는
> `rslt/spec-*.md` · `rslt/task-*.md` · `docs/source-rules.lock` ·
> `docs/domain-rules.md` · `docs/evidence-standard.md` ·
> `docs/manual-checks.yaml` · `docs/traceability.md` · `docs/assumptions.yaml` ·
> **`status.md`** · `tests/**` · `scripts/**` · 그 워크플로 자신뿐이다.
>
> ⇒ **`core/**` 만 고친 푸시**(`2cfb794`)와 **`status-history.md` 만 고친
> 푸시**(`9145910`)에서는 **아예 돌지 않는다.** ⚠ 그것은 실패가 아니라
> 미수행이며 **초록불도 아니다** — *「안 돌았다」를 「통과」로 읽지 마라*
> (§13.0.1 ④ 와 같은 논지다). ⚠ **`status-history.md` 는 그 목록에 없다** —
> 그 파일을 검사하는 잡이 없으므로 넣지 않았다(`status-round-blocks` 는
> `status.md` 만 본다).
>
> ## ★ 자리 운영 판독 — **`orch-dev` 6절 임계를 실증으로 검증할 재료**
>
> | 자리 | 판독 |
> |---|---|
> | 오케스트레이터 `cl1` | 취임 5시간 **95%** · 주간 **62%** → 첫 WP 사이클 뒤 5시간 **79%** · 주간 **60%** (**1 사이클 = 5시간 16%p · 주간 2%p**) |
> | 워커 `cl5` | WP-4 앞 5시간 **100%** → WP-5 앞 **87%** (**1 WP = 13%p**). ⚠ 인계본이 적은 **12%p** 실측과 맞는다 |
> | 임계 판정 | 실측 임계(잔여 < 2 × 직전 소모량)로 오케스트레이터 32 < 79 · 워커 26 < 87 — **둘 다 여유.** 고정 예고선(주간 15% · 5시간 30%)도 안 걸렸다 |
> | 자리 교체 | **없었다**(워커 `cl5` 하나로 WP 셋). 오케스트레이터는 **라운드 도중** 두 번 바뀌었고 **둘 다 사용자 지시**였다 — 임계가 아니다 |
>
> ## 이 라운드가 **닫은** 것
>
> - ★★★ **★분할** — 선행 셋(① R56/WP-4 물리량 축 · ② WP-1 몫의 수 · ②-b WP-2
>   실물 편익 표찰)을 치우고 **WP-4 공장 → WP-6 러너 배선**까지 이었다.
>   **「구조」가 다 섰다** — 남은 것은 **몫 비율·역할 배분이라는 값**이며 사람 몫이다
> - **★N**(줄 수 상한)의 러너 쪽 — WP-5 가 자리를 냈다. ⚠ WP-6 이 그 자리를 거의
>   다 썼으므로 **항목이 사라진 것은 아니다**
> - ★ **번호 5 — `Load`·`ThermalLoad` 의 잔존가치 비대칭**(WP-7). 두 자원이
>   교체비는 **명목**으로 장부하면서 잔존가치는 **실질**로 계상했다 — 명목으로
>   사고 실질로 되파는 셈이며 그 과소는 **한 방향으로만** 결론을 나쁘게 만들어
>   *「보수적이라 안전하다」* 로 읽힌다(`HeatPump` 가 R43 에 같은 자리를 지났다).
>   ★ **지금 고칠 수 있었던 이유는 값이 0 이라 골든이 증인으로 설 수 있었기
>   때문**이다 — 러너의 `Load` 는 비용 인자를 하나도 받지 않고 `ThermalLoad` 는
>   러너가 만들지 않는다. `status.md` 번호 5 행의 *「러너에 들어오는 날 터진다」*
>   를 **터지기 전에** 닫았다. 부채 목록 하나를 **비웠다**(이름을 더한 것의 반대)
> - ★ **번호 25 의 하나 — `FR-602-AC2`**(WP-8). *「리포트에 «기준 전제 대비 변경
>   항목» 목록이 자동 생성된다」* 는 조항이 요구한 표시가 없었고, 그것을 내놓는
>   `AssumptionSet.overridden_items()` 는 **호출자가 저장소 전체에 0곳**이었다.
>   더구나 `scripts/check_clause_partition.py` 가 `("FR-602-AC2", "core.report")`
>   를 **기대**해 두었으니 저장소가 스스로 *「이 조항은 리포트가 담당한다」* 고
>   선언하고 그 구획을 비워 둔 상태였다. **닫은 것은 시험이 아니라 문면이다** —
>   `FR-404-AC1` 을 R48/WP-D2 가 닫은 방식과 같다. **여섯 중 둘이 닫혀 넷이 남았다**
> - `NFR-105`·CI 추적표 두 자리의 **부채가 아니라 실수** — 같은 라운드 안에서 닫았다
>
> ## ⚠ 새로 밟은 함정 — **전문은 `status.md` 「함정」 절이 갖는다**
>
> - ⚠⚠⚠ **게이트를 파이프에 물려 위반 2건을 `rc=0` 으로 읽었다** —
>   `check_test_accompaniment … | tail` 의 `rc` 는 `tail` 의 것이다. 저장소가
>   이미 두 번 만난 형태이고 **이번이 세 번째**다
> - ⚠⚠⚠ **로컬 게이트 전건 초록불이 CI 초록불이 아니었다** — `gen_traceability.py`
>   는 **부수효과로 `docs/traceability.md` 를 다시 쓰는** 게이트이고 CI 가 그것을
>   커밋본과 대조한다. 조항을 인용하는 시험을 더한 WP 뒤에는 **반드시** 돌려
>   함께 커밋한다
> - ⚠⚠ **「순수한 자리 옮김이니 새 시험을 만들지 마라」는 틀린 판정이었다** —
>   새 `core/` 모듈은 `NFR-105` 가 동반을 요구한다. R42·R43·R53 에 이어 **네 번째**다
> - ⚠ **래칫이 울리는 것이 결함이 아닐 수 있다** — *「배포 경로가 이 모듈을
>   모른다」* 래칫 둘은 **배선하는 날 울리라고** 세운 것이었다. 지우지 말고
>   **반대를 붙들게 뒤집는다.** 그리고 뒤집을 때 **문자열이 아니라 `ast` 로
>   import 를 재라** — `in` 으로 뒤집으면 주석에 낱말 하나만 있어도 통과한다
> - ⚠ **`/tmp` 는 네이티브 윈도 `python.exe` 가 읽지 못한다** —
>   `--junitxml=/tmp/x.xml` 을 쓴 뒤 파싱이 `FileNotFoundError` 로 죽는다.
>   중간 산출은 세션 스크래치패드에 쓴다
>
> ## ★★ 라운드를 여기서 멈춘 이유 — **「다음에 집을 것」이 비어서가 아니다**

> 남은 시간이 있었고 목록도 비어 있지 않았는데 **새 WP 를 시작하지 않았다.**
> 사유는 실물로 확인한 것이며, 다음 사람이 같은 판단을 다시 하지 않게 적는다.
>
> ### ① 번호 25 의 **남은 넷은 WP-8 과 같은 방식으로 닫히지 않는다**
>
> WP-8 이 `FR-602-AC2` 를 닫을 수 있었던 이유는 **그 자료형이 이미 리포트에
> 닿아 있었기** 때문이다 — `build_case_report()` 가 `AssumptionSet` 을 받으므로
> `overridden_items()` 를 **읽는 자리만** 만들면 됐다. 남은 넷은 그렇지 않다
> (오케스트레이터 실측 · `grep`):
>
> - `BaselineComparison`(`FR-705-AC1`) — `core/cba/__init__.py` 의 **재수출
>   말고는 배포 경로에 없다.** 러너가 그것을 **만들지 않는다**
> - `TimeSeriesDataset.source_metadata()`(`FR-905-AC8`) — 그 이름을 쓰는 것이
>   `core/assumption/**` **안에만** 있다. 리포트에 닿는 통로가 없다
> - `CommonAsset`/`AllocationRule`(`FR-106-AC5`) — **배포 경로가 `CommonAsset`
>   을 만드는 자리가 0곳이다**(`core app infra` 에서 `CommonAsset(` 0건 · 있는
>   것은 추상 계약·구상 클래스 선언과 ORM 표뿐이다). 안분 **규칙**을 인쇄하려면
>   그 규칙을 고른 공통설비가 먼저 실행 경로에 서야 한다
>
> ⇒ **그 넷은 「읽는 자리를 만드는」 일이 아니라 「그 값을 먼저 실행 경로에
> 세우는」 일이다.** 그것이 `status.md` 「미해결」의 **범위** 행이 이 넷을
> *「사람 판정(`status-human.md` 소유)」* 으로 둔 이유와 같다.
>
> ### ② 코드로 집을 나머지는 **「자리 내기 → 배선」 사슬**이고, 그 앞쪽만 했다
>
> WP-6 뒤 `core/casegrid/e2e_runner.py` 가 다시 **코드 496/500(여유 4)** 이었다.
> 번호 14(자원 구성을 케이스 축으로)처럼 러너를 만지는 것은 **줄 수를 먼저
> 내야** 하는데, 남은 시간에 그 **사슬을 통째로** 시작하면 반쯤 넘기게 된다 —
> `.orch/` 는 `.gitignore` 안이라 미완은 사라진다(orch-dev 4절).
>
> ⇒ **그래서 사슬의 앞쪽(자리 내기)만 WP-9 로 했다.** 그것은 **그 자체로
> 완결되는 WP** 이고(WP-5 가 실물로 보였다 — 끝났을 때 게이트 전건 초록불)
> 뒤쪽(배선)은 다음 라운드가 **여유 23줄 위에서** 시작한다.
> ⚠ **★N 은 이 라운드에 두 번 걸렸다** — 여유를 내면 다음 배선이 그것을 쓴다.
> **줄 수는 「한 번 내면 끝나는 것」이 아니다.**
>
> ⇒ 그리고 남은 시간을 **「게이트 절이 요구하는데 이 라운드가 아직 안 돌린
> 것」** 에 썼다 — 음성 스위트 15종과 파이썬 3.11 전건이 그것이다. 둘 다
> 초록불이고 위 실측 표가 그 수를 갖는다.

> ## 이 라운드가 **연** 것 — 정본은 `status.md`
>
> ★분할의 **값**(몫 비율·역할 배분) · 거부한 두 갈래(`SelfConsumption` 귀속 ·
> TOU 단가) · 러너 줄 수 여유 4줄. 전부 `status.md` 의 인계 요약과
> 「다음에 집을 것」이 갖는다.

---

> ### ⬇ **R56 블록**(2026-09-03) — **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-03 **R56 — 사용자 판정 §4「계절」을 열었다. 축을 세우고 값은 비웠으므로 결론축은 한 원도 안 움직였다. ★ 그리고 로드맵 두 항목이 같은 하나에 막혀 있었다**
>
> ## 결론축 — **안 움직였다**
>
> 무보조 `npv` **−11,537,129원** · 결손/총사업비 **117.7%** · `payback_years`
> `inf`. R52 의 수 그대로다. ★ **주장이 아니라 확인이다** — 오케스트레이터가
> `spread()` 출력을 라운드 착수 커밋(`8f51ca7`)과 대조해 부하·발전 둘 다
> **8,760 원소 · 비트 단위로 다른 원소 0개**를 확인했고, 닫기 직전 리포트에서
> 세 수를 다시 읽었다.
>
> ## WP 표
>
> | WP | 무엇 | 결과 |
> |---|---|---|
> | **WP-1** | **계절 축을 세운다** — `Season`(name·days·**share**) · `DailyShape.by_season` · `weights` 를 속성화(계절 여럿이면 **거부**) · `spread()` 가 계절 차례로 편다 | `a5e69cf` · 시험 13건 · ★ **`e2e_runner.py` 한 줄도 안 고쳤다**(서명 유지 — 그 파일은 코드 497/500 · `PLR0915` 상한) |
> | **WP-1-fix** | 검수 결함 둘 — 시험 ③ 의 나눗수(`/ 92`·`/ 90`)가 `FOUR_SEASONS` 와 **짝이 풀려** 있었다(고치면 조용히 낡는다) · 일수 `0` 이 `ZeroDivisionError` 였다 | 같은 커밋 · 시험 12 → **13건** |
> | **WP-2** | **리포트가 계절 축을 읽는다** — 붙임 8 「계절·요일 변동」이 **자산의 계절 수를 실행마다 읽어** 사유·해소 조건을 갈라 적는다. 옛 문면은 *「8,760 전량이 와야 닫힌다」* 였고 그것이 **요구를 실제보다 크게** 적었다 | `3ce8e95` · 시험 4건 · ★ **본문 218/219 그대로**(여유 1) |
> | **WP-3** | **검토서의 거짓 넷을 고친다** — 0절 계절 축 행 · 0절 ESS 행 · **6절 ① 코드 인용**(*「코드 그대로」* 가 아니게 됐다) · **6절 ⑤ 절 제목**(*「왜 이번 라운드에 하지 않는가」* → *「무엇을 했고 무엇이 남았는가」*) | `d697fce` · 466 → **548줄** · 옛 인용을 **지우지 않고** ⑴⑵⑶ 으로 갈랐다 |
> | **WP-4** | **배타 판정에 물리량 축** — `ValueStream.quantity_id` · `collect_exclusions` 가 태그 **집합**을 `by_tag` **목록**으로 바꿔 쌍마다 본다 | `f392fb6` · 시험 7건 · 유형 `B`~`D` 무영향 |
> | (닫기) | `status-human.md` 「승인 없이 정한 것」 절 신설 · 「함정」 절 코드 줄 표 정정 · ★배타물리량 신설 + ★관점공백 크기 + ★분할 둘째 선행 | `592f1d3` · `288c2ca` · `436247c` |
>
> ## ★★★ 이 라운드가 실측으로 찾은 것 — **로드맵 두 항목이 같은 하나에 막혀 있었다**
>
> 조항은 **물리량**을 말하는데 구현은 **편익 태그 짝**만 보았다. 조항 문면
> (`rslt/spec-분산특구-경제성평가.md` · 워커와 오케스트레이터가 각각 열어 대조 ·
> ⚠ **아래는 굵게 표기를 뺀 원문**이다):
>
>     AC1   동시 발생 효과는 중복이 아니다 — 지불 주체가 다르거나 물리량이
>           다르면 정상 계상한다. 중복은 «같은 화폐 흐름을 두 번 세는 것»으로
>           한정한다
>     AC2.A 동일 물리량 이중 판매 — 같은 1 kWh를 자가소비 절감과 잉여판매로
>           동시 계상하거나 같은 시각 ESS 방전을 피크저감과 DR로 동시 계상하는
>           조합은 선언적 배타 규칙 테이블로 금지하고 …
>
> 그런데 `collect_exclusions` 는 `active_tags = {type(s).tag for s in active}` —
> **태그의 집합**이라 «같은 물리량인가» 를 물을 수단이 **원리적으로 없었다.**
> **즉 구현이 `FR-402-AC1` 을 위반하고 있었다**(정당한 동시 계상을 지웠다).
>
> ⚠ 같은 논리를 이 저장소가 **이미 유형 B~D 에 대해 썼다** —
> `assert_no_exclusions` 독스트링이 *「여기서 함께 막으면 정당한 동시 계상을
> 지우게 되며, 그것이 FR-402-AC1 이 명시로 금지한 방향이다」* 라고 적는다.
> **유형 A·E 의 「물리량이 다른 경우」만 아무도 보지 않았다.**
>
> **실측**(오케스트레이터가 함수를 직접 불렀다 · 고친 뒤 다시 불렀다):
>
>     조합                            R55 까지   R56 뒤
>     SelfConsumption + SurplusSale    거부(A)    거부      (둘 다 미선언)
>     NWAs + PeakShaving               거부(E)    거부      (둘 다 미선언)
>     CP + TouArbitrage                거부(E)    거부      (둘 다 미선언)
>     한쪽만 선언                       —         거부      우회로 없음
>     둘 다 선언 · 같다                 —         거부      조항 문면 그대로
>     둘 다 선언 · 다르다               거부       **통과**   ← 새 갈래
>     유형 E · 다르다                   거부       **통과**   ← ★분할이 막혀 있던 자리
>
> ⚠ 거부 메시지 **자신이 처방을 적고 있었다** — *「물리량이 실제로 다른지
> 확인하고, 다르다면 배타 규칙 쪽이 틀린 것이므로 docs/exclusion-rules.yaml 을
> 고쳐야 합니다」*. **그런데 「다르다」를 표현할 자리가 계약에 없었다.**
>
> ### 그래서 두 항목의 서술이 정정됐다
>
> - **★관점공백** — *「남은 것은 배선 하나」* 가 **틀렸다.** 배선이 아니라 선행이
>   있었다. ★★ **그리고 크기를 처음 재었다: 연 +19,853원.** 붙임 8 이 이미 그
>   수를 계산하고 있었고 아무도 로드맵으로 옮겨 적지 않았다 — 구매 회피
>   **+238,234원/년**(× 구매단가 120원/kWh) 과 판매 포기 **−218,382원/년**
>   (배타 상대 · × 판매단가 110원/kWh)이 **거의 상쇄한다**(자가소비 5.44kWh/일 ·
>   연간화 1,985kWh · 부하 총량 3,600kWh/년은 대장 가정값 · `Q-3` 실측 시 교체).
>   결손 11,537,129원에 대면 **미미하다** ⇒ **「크기」가 아니라 「관점」 문제다.**
> - **★분할** — **선행이 둘이다.** ① 위 물리량 축(WP-4 가 치웠다) ②
>   **고정비·정격출력이 「자원당」**이라 몫으로 나눌 자리가 없다:
>   `capex_unit_won_per_kwh` 는 용량에 비례해 옳게 갈리지만
>   `fixed_om_won_per_year`(대장 10만원/년)·`power_kw` 는 **인스턴스마다 통째로**
>   붙는다 — 몫을 `ESS` 두 대로 표현하면 **고정 O&M 2배 · 정격출력 2배**다
>   (용량만 갈렸는데 PCS 가 두 대가 된다). ⚠ `core/der/ess.py` 는 코드
>   **498/500** 이라 몫 개념을 그 파일에 넣을 자리도 없다 — **새 모듈이 선행**이다
>   (본은 `pv_allocation.py`·`perspectives.py`).
>
> ## 게이트 실측 (닫기 시점)
>
> | 게이트 | 결과 |
> |---|---|
> | 전건 `pytest -q` | **1927 collected**(착수 1903 · 순증 **24**) — `--collect-only` 로 셌다. ⚠ 「N passed」는 리다이렉트 캡처에 안 나온다. **통과 판정은 CI `tests` 초록불**(3.11 · `f392fb6`)과 WP 별 진행줄 `F`·`E` **0건**으로 했다 |
> | `ruff` · `mypy` · `lint-imports` | rc=0 · **129파일** · **4 kept 0 broken** |
> | 게이트 13종 | `file_size --code-strict` · `docstring_references` · `unread_extension_points`(선언 **131** · 미읽 **38** = 부채 목록과 일치) · `marker_substance` · `hardcoded_params` · `source_rules` · `status_round_blocks` · `doc_line_citations` · `task_mapping` · `assumptions` · `disclosure` · `partition_assignment` · `precommit_installed` — **전부 rc=0** |
> | ★ PR 에서만 도는 둘 (손으로 판정) | `check_test_accompaniment --base origin/main` rc=0(구현 변경 2건 전건 동반) · `check_coverage_inputs` rc=0(측정 258 / 변경 2) |
> | 음성 스위트 **15종** | **전부 rc=0** — ⚠ **최종 커밋(`f392fb6`)을 별도 worktree 에 체크아웃해 돌렸다**(실물 소스를 변이시키므로). **변이 잔존 0건** |
> | `gen_traceability` | 수용기준 **325** · 자동 **294** · Must-have 미매핑 **9건**(늘지도 줄지도 않았다). ⚠ **WP-4 뒤 재생성을 빠뜨려 CI `source-rules` 가 한 번 빨간불이었다** — `6a78086` 이 고쳤다(「함정」 절) |
> | CI | `tests`·`source-rules` **둘 다 초록불**을 눈으로 확인 |
> | ★ 게이트 ①(변경분 커버리지 ≥95% · `diff-cover`) | ✅ **96%** — rc=0. `diff-cover cov.xml --compare-branch=8f51ca7 --fail-under=95`. 변경 **102줄** 중 안 밟힌 **4줄**이고 전부 `core/casegrid/profiles.py`(94.7%)의 **방어적 `raise`** 다 — 120행(*계절이 하나도 없다* · `_by_season_from` 이 언제나 1개 이상을 돌려주므로 자산 경로로는 닿지 않는다) · 154행(*음수 몫* · 합 ≠ 1 검사가 먼저 걸린다) · 268행 · 318행. **`contracts/valuestream.py`·`report/unreflected.py`·`valuestream/exclusion_table.py` 는 100%.** ⚠ **넷을 덮으려고 검사를 지어내지 않았다** — 도달하려면 다른 검사가 먼저 거부하는 상태를 억지로 만들어야 하고, R55 도 같은 사유로 1줄을 남겼다. ⚠⚠ **CI 는 이 게이트를 직접 푸시 경로에서 「미수행」으로 넘긴다** — 손으로 돌리는 것이 유일한 판정이다 |
> | 전건 `--junitxml` (최종) | **tests 1927 · failures 0 · errors 0 · skipped 4** · 진행줄 `F`·`E` **0건** |
>
> ## 자리 운영 (5시간 창 판독 · ⚠ **이력이지 인계가 아니다**)
>
> | 자리 | 판독 |
> |---|---|
> | 오케스트레이터 `cl4` | 취임 **82%** → WP-2 전 63% → WP-3 전 48% → WP-4 전 32% → 닫기 전 **21%** (WP 당 약 **11~16%p** · 평균 15%p) · 주간 76% → **69%**(WP 당 약 2%p) |
> | 워커 `cl1` | WP-1 전 **100%** → WP-2 전 87% → WP-3 전 79% → WP-4 전 67% → 닫기 전 **56%** (WP 당 약 **11~13%p**) |
>
> ⚠ **워커 교체 0회 · 이관 0회.** 5시간 창 리셋(오케스트레이터 11:39 · 워커
> 12:09)이 소진보다 먼저 왔다. ★ **주간 리셋이 오늘 11:59** 여서 `auc` 가 내내
> `WEEKLY_WASTE_RISK`(27% → 38%)를 냈다 — **아끼는 자리가 아니라 태우는 자리였다.**
>
> ★★ **`orch-dev` 6절 임계에 대한 실측 판정** — 워커 선(5시간 40% 교체)은
> **한 번도 걸리지 않았다**(최저 56%). 오케스트레이터 착수선(5시간 20%)은
> **닫기 직전 21% 로 스쳤다** — 그래서 **닫기를 먼저 하고** 그 다음을 판정했다
> (§6 *「더 내려가면 인계 자체를 못 한다」*). ⚠ **1 WP 실측 소모량이
> 오케스트레이터 15%p · 워커 12%p 로 6절의 「15~20%p」 안**이다.
>
> ## 새로 밟은 함정
>
> - ⚠⚠⚠ **워커가 편집하는 동안 커밋하지 마라.** `pre-commit` 이 미스테이징
>   변경을 **stash 하고 되돌리는데**, 그 창에서 워커가 쓰면 훅이 *「files were
>   modified by this hook」* 으로 실패한다 — 실측 **2회**
>   (`trailing-whitespace` · `check-merge-conflict`). ⚠ 워커 파일이 살아남은 것은
>   **운이었다**(`ast.parse` 와 줄 수로 확인했다). ⚠⚠ **이것은 `orch-dev`
>   5절 ④(「검수 통과 즉시 커밋」)와 충돌한다** — 작업 트리가 하나이고
>   `pre-commit` 이 stash 를 쓰기 때문이다. 순서는 **「워커 `done` 확인 → 커밋」**
> - ⚠⚠ **워커가 「N passed」를 기다리며 헛돈다.** 그 요약 줄은 리다이렉트 캡처에
>   오지 않는데(이미 알려진 함정) 워커가 **폴링 루프로 110회 기다리거나**
>   **`-v` 로 스위트를 통째로 다시 돌린다** — 실측 각 1회 · 합쳐 **약 10분** 손실.
>   **처방: WP 지시문에 「건수는 `--junitxml` 또는 `--collect-only` 로 센다 ·
>   스위트를 다시 돌리지 마라」를 박아 둔다**(R56/WP-4 지시문이 그렇게 했고 워커가
>   실제로 `--junitxml` 을 썼다)
> - ⚠ **`herdr agent prompt` 인자에 정규식을 싣지 마라** — `+[` 를 옵션으로 읽어
>   `unknown option: +\[` 로 **프롬프트가 아예 안 간다**(`esc` 만 먹었다).
>   판정 방법을 말로 적는다
> - ⚠ **워커가 도는 동안 커버리지 측정을 돌리면 그 측정이 오염된다** — 측정 대상
>   파일을 워커가 고치는 중이었다. 실측 1회(폐기하고 닫기 시점에 다시 돌렸다)
>
> ## 이 라운드가 닫은 것
>
> 사용자 판정 §4(계절) — **구조까지.** `docs/decisions-2026-09-02-R54.md` 의
> 판정 넷이 이로써 전부 구현됐다(§1·§2·§3 은 R54). ⚠ **값은 사람 몫**이며
> 소유자는 `status-human.md` 의 **「계절 자료」 절**이다.
>
> ★ **승인 없이 정한 것 여섯**은 `status-human.md` 의 **「⚠ 오케스트레이터가
> 승인 없이 정한 것 (사후 확인 필요)」** 절이 (날짜 · 무엇 · 왜 · **되돌리는 법**)
> 으로 갖는다 — 2026-09-03 사용자 판정에 따른 것이다.

---

> ### ⬇ **R55 블록**(2026-09-03)
> **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-03 **R55 — 「적정 용량 산출 방법」의 「적용」을 시작했다. 경우 「가」 하나를 닫았고 결론축은 한 원도 안 움직였다**
>
> 오케스트레이터 `cl5`(사용자가 직접 위임 · **1회성** · `/goal` 을 걸지 않았다 —
> 선임 `cl4` 세션에 사용자가 직접 건 goal 이 살아 있었고 선임은 그것을 해제할
> 권한이 없다) · 워커 `cl1`(sonnet 5) · 종료 시각 06:12 KST.
>
> 재료 정본은 **`docs/적정용량-산출방법-검토.md`**(R52/WP-B 검토서)와
> `status.md` 「다음에 집을 것」의 **★용량** 행이다. 사용자가 *「검토한 후에 적용」*
> 이라 했고 검토는 R52 가 끝냈다.
>
> ### 무엇을 골랐고 무엇을 일부러 열지 않았나
>
> 검토서는 경우를 **넷**(가·나·다·라) 갈라 두었고 **넷 중 셋이 없다**고 적었다.
> 이 라운드는 **경우 「가」(100% 에너지 자립 역산) 하나**를 골랐다 —
> 검토서 §9 권고 2 가 *「착수 순서로는 「가」가 가장 가볍다 — 산식이 단순하고
> 입력이 거의 다 있다」* 로 적어 두었고, 남은 셋은 3시간 안에 닫을 수 없는
> 선행을 각각 갖는다:
>
> | 경우 | 왜 이 라운드가 열지 않았나 |
> |---|---|
> | **나**(사업성 최대) | `FR-304`(Nice-to-have · **Phase 3**)의 **PV×ESS 결합 최적화**를 구현하는 일이다. 지금 `capacity.py` 는 1변수 스윕 5점이며 모듈 스스로 *「최적 용량을 고르지 않는다」* 고 적는다 |
> | **다**(면적 상한) | **가용 지붕 면적·설치밀도 둘 다 실측값이 없다.** 지어내지 않는다 — 대장 항목을 여는 것이 아니라 **자료 확보가 선행**이다 |
> | **라**(히트펌프) | 선행이 **둘 겹친다** — 러너 개편(자원 구성을 케이스 축으로)과 가정 내 냉난방 수요 실측값. 자원 클래스(`core/der/heatpump.py`)는 이미 완결돼 있는데 러너가 **참조하는 자리가 0곳**이다 |
> | **ESS 역산** | **R55 오케스트레이터가 뺐다** — 계절 축(판정 §4)이 선행이라고 보았다. ⚠ **정정(2026-09-03 R57)**: 종전 이 칸은 *「사용자가 이 라운드에서 빼라고 지정했다」* 고 적었으나 **그런 사용자 지시는 없었다** — 사용자가 직접 부인했다 |
> | **★계절(판정 §4)** | **R55 오케스트레이터가 열지 않기로 정했다** — 판정 문서 자신이 *「한 라운드를 통째로 쓸 수 있을 때 연다 · 반쯤 흔들면 되돌릴 수 없다」* 고 적었고 3시간은 통째 라운드가 아니었다. ⚠ **정정(2026-09-03 R57)**: 종전 이 칸은 *「사용자가 시작하지 말라고 지정했다」* 고 적었으나 **그런 사용자 지시는 없었다** — 사용자가 직접 부인했다. 실제 사유는 **시간**이며 그것은 오케스트레이터의 판단이다(R56 이 곧바로 열었다) |
>
> ### WP 표
>
> | WP | 무엇이 어긋나 있었나 | 무엇을 했나 | 커밋 |
> |---|---|---|---|
> | **WP-1** | 대장에 가구 연간 사용량(`load.household.annual`)은 있었고 정방향 산식(`PV.annual_generation_kwh` — 용량 × 8,760h × 이용률)은 **오라클 주석까지** 갖고 있었으나, **그 역함수를 부르는 자리가 저장소 전체에 없었다** | `core/report/sizing.py` 신설 — `required_pv_capacity_kw()` · `build_self_sufficiency_sizing()`. 대장 세 수준과 참고 부하를 **각각** 역산하고 **어느 쪽이 맞는지 고르지 않는다.** 검사 다섯 | `049b7b2` |
> | **WP-2** | 산출기가 있어도 **리포트가 그것을 싣지 않으면 아무도 못 본다** | `CaseReport.self_sufficiency` 필드 · `build_case_report` 조립 · `render_markdown` 배선 · 검사 둘 · 검토서 §0 표와 §1-⑥ 갱신 | (닫기 커밋과 함께) |
> | **WP-2-fix** | ★ **본문 분량 예산에 걸렸다** — 아래 「어긋난 것」 1 | 표를 **본문 4.4 → 붙임 10** 으로 내렸다. 문면은 한 줄도 버리지 않고 자리만 옮겼다 | 같음 |
>
> ### 역산 실측 — ★ 이 수가 사용자 물음(★600)을 처음으로 **수로** 세운다
>
> 이용률 15%(`PV_CAPACITY_FACTOR` · **소스 상수 · 대장 미등재**) 기준:
>
> | 연간 사용량의 출처 | 연간 사용량 | 필요 용량 | 탐색 구간(1~9kW) 안인가 |
> |---|---|---|---|
> | 대장 `low` | 2,700kWh | **2.05kW** | 예 |
> | 대장 `base` | 3,600kWh | **2.74kW** | 예 |
> | 대장 `high` | 4,800kWh | **3.65kW** | 예 |
> | 사용자 예시(월 600kWh) | 7,200kWh | **5.48kW** | 예 |
>
> ★ **대장 base 2.74kW 대 사용자 예시 5.48kW — 정확히 두 배다.** 검토서 §7 이
> *「어느 쪽이 맞는지 대신 정하지 않는다」* 며 남긴 물음이 이제 **용량 두 개**로
> 서 있다. ⚠ **넷 다 탐색 구간(1~9kW) 안이다** — 즉 이 역산 때문에 구간을
> 넓혀야 하는 상황은 **아직 오지 않았다.**
>
> ### 어긋난 것 — 넷
>
> 1. ★★ **본문 분량 예산의 여유가 0 줄이었다.** WP-2 가 표를 본문 4.4 에 실었더니
>    `tests/report/test_overview_sections.py::test_body_stays_within_the_form_length_budget`
>    가 **219 → 229줄**로 빨간불이 됐다. 워커는 지시문의 escalation 갈래를 따라
>    **상한을 올리지 않고 멈췄고 그것이 옳았다.** 오케스트레이터가 **자기 판정을
>    뒤집었다** — 그 검사의 실패 문면 자체가 *「늘어난 것을 **붙임으로 내릴 것**」*
>    이라 적고, 독스트링이 선언한 기준은 **「4~5쪽 · 130~170줄」**인데 상한은 이미
>    **200 → 202 → 204 → 213 → 217 → 219** 로 다섯 번 밀려 있었다. 여섯 번째로
>    미는 것은 그 규정을 사실상 없애는 일이다
> 2. ⚠ **옮긴 뒤 「4.4 의 별도 소절」이라 적힌 주석 둘이 거짓이 됐다.**
>    `case_report.py` 의 `#:` 필드 주석과 조립부 주석이다 — `git diff`·전건 시험·
>    `git status` 가 전부 초록불인 채로 남아 있었고 **오케스트레이터가 검수에서
>    문면을 직접 읽어 잡았다.** 이 저장소가 반복해 밟는 형태다 —
>    *「diff 가 보고와 일치하는 것은 「시킨 대로 했다」이지 「그 내용이 맞다」가 아니다」*
> 3. ⚠ **`check_docstring_references` 가 백틱 스팬의 줄바꿈 절단을 잡았다.**
>    `` `core/der/pv.py::PV. `` 로 끊고 다음 줄에 `annual_generation_kwh` 를
>    이었더니 검사가 `PV.` 를 실재하지 않는 클래스로 읽었다(rc=1). 한 줄로 이어
>    고쳤다 — 「이름 가운데에서 줄을 바꾸지 마라」가 **백틱 스팬에도 적용된다**
> 4. ⚠ **전건 `pytest -q` 의 「N passed」 요약 줄이 리다이렉트 캡처에 나타나지
>    않는다**(`-u` 로도 같다 · 워커와 오케스트레이터가 각각 재현했다). 수는
>    `--junitxml` 의 집계와 `--collect-only` 합계로 셌다
>
> ### 게이트 실측 — ★ 오케스트레이터가 커밋 전에 **직접 다시 돌렸다**
>
> | 무엇 | 값 |
> |---|---|
> | 전건 `pytest` | **1903 collected · 1899 passed / 4 skipped / 0 failed** · rc=0 (R54 종료 1894 collected·1890 passed → **검사 순증 9**: WP-1 다섯 + WP-2 둘 + **커버리지 교정 둘**) |
> | 결론축 | 무보조 `npv` **−11,537,129원** — R54 와 **같다.** `tests/golden` 7건 통과 |
> | 본문 줄 수 | **218줄**(상한 219) — 붙임으로 내렸으므로 **한 줄도 늘지 않았다** |
> | `ruff` · `mypy` · `lint-imports` | rc=0 · 129 파일 · **4 kept 0 broken**(`core.report → core.casegrid` 는 layers 상 정방향) |
> | 검사 스크립트 | `check_marker_substance`·`check_hardcoded_params`·`check_source_rules`·`check_partition_assignment`·`check_disclosure`·`check_assumptions`·`check_precommit_installed`·`check_unread_extension_points`·`check_docstring_references`·`check_status_round_blocks`·`check_task_mapping` **11종 rc=0** |
> | `check_file_size --code-strict` | rc=0 — 코드 줄 수 초과 **0건**. `narrative.py` 코드 **486 → 488**(예산 500) · `case_report.py` **403 → 437** · `sizing.py` **134** |
> | `gen_traceability.py` | rc=1(정상 · 미매핑 9) 후 `git diff --stat docs/traceability.md` **빈 diff** — R54 가 밟은 함정을 이 라운드는 안 밟았다 |
> | 커밋 경계 검사 | `check_test_accompaniment` **통과**(구현 변경 3건 전건 동반) |
> | ★ **게이트 ①(변경분 커버리지)** | 손으로 판정했다 — `pytest --cov` 로 `coverage.xml` 을 다시 만들고 `diff-cover --compare-branch=ee81077 --fail-under=95`. **처음엔 93% 로 빨간불**이었다(`sizing.py` 의 「구간 상한 초과」·「구간 하한 미만」·「참고 부하 없음」 세 갈래가 한 번도 안 밟혔다). 검사 둘을 더해 **98%** 로 올렸다. ⚠ 남은 1줄은 `case_report.py` 의 대장 축 결손 `raise` 이며 대장을 위조해야 밟히는 자리라 두었다 |
>
> ### 이 라운드가 닫은 것
>
> - **★용량의 경우 「가」** — 역산 산식(`core/report/sizing.py`) · 리포트 붙임 10 의
>   소절 · 검사 일곱 · 검토서 §0 표와 §1-⑥ 갱신. ⚠ **채택은 하지 않았다** —
>   역산 용량을 `_DESIGN_VARS` 탐색점이나 기준 구성에 올리지 않았고, 그것을 하면
>   결론축이 움직인다
> - **`tests/ci/_tmp_pytest/` 누수를 `.gitignore` 로 막았다** — 전건 `pytest` 를
>   돌리면 `case_*` **436개 · 16MB** 가 남는데(`tests/ci/conftest.py` 의
>   `shutil.rmtree(..., ignore_errors=True)` 가 윈도우의 읽기전용 `.git` 파일에
>   걸려 조용히 실패한다) `.gitignore` 의 `.tmp_*/` 가 **이 이름을 덮지 못했다.**
>   `git add -A` 한 번으로 16MB 가 실려 갈 자리였다. ⚠ **근본 원인은 안 고쳤다** —
>   「미해결」에 남겼다

---

> ### ⬇ **R54 블록**(2026-09-03) — **현재 상태가 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-03 **R54 — 사용자 판정 셋을 닫았다. 결론축은 한 원도 안 움직였다. ★ 그리고 조항과 구현이 서로 반대를 말하고 있었다**
>
> 오케스트레이터 `cl4`(R53 → R54 이관 · 인계본 `.orch/R54/HANDOFF.md`) ·
> 워커 `cl6` → **`cl1`**(교체 · 아래 ②) · 사용자 지정 **8시간**(2026-09-02 22:12 →
> 2026-09-03 06:12 KST) · 브랜치 `r48-wp-a`(사용자 지정 · 바꾸지 않았다).
> 재료 정본은 **`docs/decisions-2026-09-02-R54.md`**(사용자 판정 넷).
> ⚠ 그 파일은 R53 이 **작업 트리에만 남기고 넘겼다**(추적조차 안 된 상태였다) —
> R54 의 첫 커밋(`9b5e13a`)이 그것을 정본 층에 올린 것이다.
>
> ## ① 결론축 — **안 움직였다**
>
> 무보조 `npv` **−11,537,129원** · 결손/총사업비 **117.7%** · `payback` 셋 다 `.inf`.
> R52 가 세운 수 그대로이며 **그것이 설계 목표였다** — 판정 셋 다 축에 구조적으로
> 닿지 않는 자리를 골랐고, 매 WP 마다 그것을 검사로 붙들었다.
> ⚠ **옛 수(−12,591,162원 · 128.5%)를 인용하지 마라.**
>
> ## ② 자리 — **워커를 한 번 갈았고, 그 판단이 수로 확인됐다**
>
> | 자리 | 누구 | 판정 근거 |
> |---|---|---|
> | 오케스트레이터 | `cl4` | 착수 주간 84%·5시간 42% → 종료 주간 81%·5시간 90%(00:09 리셋). orch-dev 6절 임계(15%·30%)를 한 번도 건드리지 않아 **이관하지 않았다** |
> | 워커 1 | `cl6`(사용자 지정 · **`glm-5.3`**) | WP-1·1-fix·2·2-fix 를 돌았다. 5시간 99% → **31%** 에서 넘겼다 |
> | 워커 2 | **`cl1`** | WP-3·4·5. 교체 직후 `cl6` 이 **18%**(교체선 25% 아래)로 떨어져 **교체가 옳았음이 수로 확인됐다** |
> | 후임 후보 | `cl5` | 주간 92%·5시간 98%. **워커로 태우지 않았다** |
>
> ⚠⚠ **`cl6`(z.ai)은 주간 잔여가 `unobservable` 이다** — 5시간 창만으로 판정했고
> 그 사실을 사용자에게 알렸다. ⚠ **모델 전환은 `/model opus` 로 해야 `glm-5.3` 이
> 걸린다**(실제 코드명을 주면 `400 [1214][modelCode: does not exist]`).
> pane 머리의 배너(`glm-4.7`)는 세션 시작 시점 값이라 **바꿔도 그대로 남는다** —
> 확인은 `/context` 머리로 한다. ⚠ `cl6` 의 창은 **200k**(`cl1` 은 1M).
> `/clear` 는 매 WP 마다 `/context` 의 `Messages` 로 확인했다(102·137 토큰).
>
> ## ③ WP 표
>
> | WP | 무엇이 어긋나 있었나 | 커밋 |
> |---|---|---|
> | 착수 | **재료 정본이 추적조차 안 된 채 작업 트리에만 있었다** — R53 이 밀지 않고 넘겼다. 그 기계가 사라지면 사용자 판정 넷이 함께 사라진다 | `9b5e13a` |
> | 1 | ★★★ **조항은 「한 가지만 한다」였는데 구현이 「둘이 있으면 막는다」였다.** 러너가 운전 방법과 무관하게 항상 `PeakShaving` 을 만들어, 방식 「나」를 고르면 **단가가 0원이어도 실행이 항상 거부**됐다 — 제도 편익으로 가는 통로가 통째로 막혀 있었다. 코드는 `enabled=` **한 줄**(496→497/500) | `a3a52a8` |
> | 1-fix | **술어를 직접 붙드는 검사가 한 건도 없었다**(`NFR-105`) — 동반 검사가 러너를 통과하는 간접 검사뿐이었다. ⚠ **R42·R43·R53 에 이어 넷째**로 이 게이트가 잡았다 | `1fefa68` |
> | 2 | **`case_report.py` 가 코드 500/500 — 여유 0.** R53 이 `_Sweeper` 배선을 넣을 자리가 없어 생략했었다. 영향도 스윕을 `case_influences.py` 로 떼어 **500 → 391**, 그 자리에 배선을 넣었다. ★ 전건이 분리 전후 **1883 으로 같다** | `72af187` |
> | 2-fix | **배선 누락을 사람이 손으로 전수 대조해서야 찾았다** — 그 대조를 **AST 검사**로 옮겼다(두 호출부의 키워드 인자 이름 집합을 각각 읽어 비교). `NFR-105` 위반 둘도 이 파일 하나로 닫혔다 | `c1a9627` |
> | 3 | **대장은 합계 하나인데 클래스는 다섯 칸을 요구했다** — R53 이 전액을 `grid_service_won` 한 칸에 몰아넣는 **임의 선택**으로 넘겼다. 값이 0이라 무해했으나 켜지는 날 항목별 분해가 거짓이 된다. ★ 인자를 다섯으로 늘리지 않고 **`DistributedSubItems` 하나를 흘려** `e2e_runner.py` 를 **497 → 497** 로 지켰다(여유 3줄인 파일이다) | `33551ad` |
> | 4 | **흑자 문면(`GAP_MARGIN`)이 한 번도 실행된 적이 없었다** — 골든 셋이 모두 `.inf`(미회수)라, 흑자로 뒤집힌 날 처음 도는 코드가 있었다. `fixtures/probe/` 의 파생 대장으로 그 갈래를 덮고 **진짜 대장 대조군**을 함께 뒀다. ⚠ **함께 CI 빨간불 하나를 고쳤다**(아래 ⑤) | `507990d` |
> | 5 | **「거부된다」고 적힌 문단이 WP-1 뒤에 반쪽만 참이 됐다** — `ESS.value_streams()` 독스트링. 다섯 갈래를 전수 grep 으로 훑어 **고칠 자리는 이 하나뿐**이었다(각 커밋이 자기 주변 문면을 그 자리에서 이미 고쳤기 때문) | `5b307e4` |
>
> ## ④ 게이트 실측 (오케스트레이터가 **매 WP 커밋 전에 직접** 다시 돌렸다)
>
> - 전건 `pytest` — 착수 **1878 passed** → 종료 **1890 passed · 3 skipped ·
>   1 xfailed · 0 failed**. **검사 12건 순증**이며 줄어든 검사는 없다
> - `ruff`·`mypy`(128파일)·`lint-imports`(4 kept 0 broken) rc=0
> - 검사 스크립트 11종 rc=0 · `check_assumptions` 등재 36 → **40건**
> - `NFR-105`(`check_test_accompaniment --base origin/main`) — **손으로 세 번
>   돌려 두 번 위반을 잡았다**(WP-1·WP-2). ⚠ 이 게이트는 직접 푸시 경로에서
>   CI 가 판정하지 않는다
> - 코드 줄 수 — `e2e_runner.py` **497/500** · `ess.py` **498/500** ·
>   `case_report.py` **500 → 403** · `case_influences.py` **148**
> - 음성 스위트 15종 · CI(`tests`·`source-rules`) — 아래 ⑤
>
> ## ⑤ ★ **이 라운드가 CI 빨간불을 하나 냈고, 다음 커밋이 고쳤다**
>
> WP-3 푸시에서 **`source-rules` 가 빨간불이었다** — `docs/traceability.md` 의
> `FR-404-AC2` 인용이 1건 → 2건인데 재생성을 하지 않았다.
>
> ⚠⚠ **원인은 오케스트레이터의 WP-3 지시문**이다. *「새 검사 **파일**을 만들었을
> 때만 `gen_traceability.py` 를 다시 돌려라」* 라고 적었는데, **기존 파일에 검사를
> 더해도 인용 수가 는다.** 로컬 게이트 전건이 초록불이어도 CI 는 다른 것을 본다
> (함정 9). WP-4 커밋이 재생성분을 함께 실어 닫았다.
>
> ⚠ **그리고 푸시한 뒤 CI 완료를 기다리지 않고 다음 WP 로 넘어간 것**이 그 빨간불을
> 늦게 발견한 이유다. 그 뒤로는 **완료를 눈으로 보고** 넘어갔다.
>
> ## ⑥ 사용자 판정 넷이 어떻게 처리됐나
>
> | | 판정 | 처리 |
> |---|---|---|
> | **§1** | ESS 운영 방식은 가·나 중 하나만 | ✅ `a3a52a8`·`1fefa68` |
> | **§2** | 길이 한도에 찬 파일을 쪼갠다 | ✅ `72af187`·`c1a9627` — ⚠ **「한도에 찬」을 코드 줄 수로 읽어 셋을 대상으로 잡았고 그중 하나(`case_report.py`)만 쪼갰다.** 총 줄 수 기준으로는 17건이 상한을 넘는다(전건 「설명 밀도」 경고이며 검사기 자신이 *「줄 수를 맞추려고 근거 주석을 지우지 마십시오」* 라고 적는다) |
> | **§3** | 분산편익 대장 다섯 칸 + 리포트 | ✅ `33551ad` |
> | **§4** | 계절은 다음 라운드 | ⏸ **시작하지 않았다**(지시 그대로) |

---

> ### ⬇ **R53 블록**(2026-09-02) — **현재 상태가 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-02 **R53 — 배선 공백 하나를 닫았고 결론축은 한 원도 안 움직였다. ★ 그리고 물려받은 수 둘이 틀렸다**
>
> 오케스트레이터 `cl3`(R52 → R53 이관 · 인계본 `.orch/R53/HANDOFF.md`) · 워커 `cl1` ·
> 사용자 지정 **4시간**(19:20 → 23:20 KST) · 브랜치 `r48-wp-a`(사용자 지정 · 바꾸지 않았다).
> 사용자가 *「cl3로 orchestrator를 인계해. Orch-dev skill로 4시간 동안 작업 계속하도록
> 요청해줘」* 라고 정했고, **승인 범위(사용자 확인 없이 진행)를 이 라운드는 사용자에게
> 직접 확인받고 착수했다** — R53 인계본이 그것을 「가정」으로 표시해 두었기 때문이다.
>
> ## ① 결론축 — **안 움직였다**
>
> 무보조 `npv` **−11,537,129원** · 결손/총사업비 **117.7%** · `payback` 셋 다 `.inf`.
> R52 가 세운 수 그대로다. ★ **그것이 이 라운드의 설계 목표였다** — 관점 표의 공백을
> 채우면서 축은 건드리지 않는 자리를 골랐다(아래 ③).
>
> ## ② 취임 — `auc plan` 한 번으로 자리 셋을 판정했다
>
> | 자리 | 누구 | 판정 근거 |
> |---|---|---|
> | 오케스트레이터 | `cl3` | 주간 94% · 5시간 98% — orch-dev 6절 임계(15%·30%)를 한참 위. 이관 불필요. ★ 주간 창이 19:59:59 에 리셋돼 사슬이 더 길어졌다(`auc` 가 `WEEKLY_WASTE_RISK 93%` 로 경고했다) |
> | 워커 | `cl1` | 주간 81% · 5시간 82%. R52 의 WP 아홉을 돈 pane 이라 게이트·규약에 가장 익숙하다 |
> | 후임 후보 | `cl5` | 주간 98% · 5시간 100%. **워커로 태우지 않았다** |
>
> ⚠ `cl6` 은 주간이 `unobservable` 이라 임계 판정에 쓰지 않았다(애초에 배제 대상).
> `/goal` 은 `herdr agent prompt cl3` 로 자기 세션에 걸고 **`◎ /goal active` 를 눈으로
> 확인**했다. 선임 `cl4` 는 `/clear` 뒤 `/context` 로 **`Messages: 137 tokens`** 을 봤다.
>
> ## ③ WP 표
>
> | WP | 무엇이 어긋나 있었나 | 커밋 |
> |---|---|---|
> | 1 | **`DistributedBenefit`(`payer = SOCIETY`)을 만드는 배포 코드가 0곳이었다** — 클래스·조항(`FR-401-AC2`·`FR-404`)·관점 배선이 다 서 있는데도 관점 표의 사회 열이 늘 0원이었다. 이 저장소가 R26·R27·R30·R51/WP-7 로 **네 번** 밟은 「기계는 있는데 배포 코드가 아무도 안 부른다」의 **다섯째** | `12f9b43` |
> | 1-fix | **`perspective_section.py` 에 동반 테스트가 없었다**(`NFR-105`) — 라운드를 닫기 전 `check_test_accompaniment` 가 잡았다. ⚠ **로컬 `pytest` 로는 안 잡히는 자리**이며 지시문에 그 검사를 넣지 않은 것은 오케스트레이터의 누락이다 | `32eae92` |
> | 2 | **(정찰 · 소스 0줄)** ⑤(「남는 경우」 골든)의 선행을 실측했다 — **「조건부로 가능하다」.** 지금은 `NWAs` 를 켜면 **단가 0원이어도** 거부된다 | — |
>
> ## ④ ★★★ WP-1 의 설계 판정 — **「값이 0이라서」가 아니라 「구조적으로」**
>
> 오케스트레이터가 갈래를 정해 지시문에 박았다. `annual_benefit` 은 `annualised` 를
> 합해서 만들어지고 그것이 `benefit_rows` 를 거쳐 **결론축으로 간다.** 사회 편익을
> 그 목록에 끼우면 **사회의 편익이 사업자 지갑에 계상되고**, 대장 값이 0을 벗어나는
> 날 결론축이 움직인다. 게다가 `annualised[:-1]`·`annualised[-1]` 이 **자리로** 쪼개므로
> 스트림을 하나 끼우면 조용히 어긋난다(러너 독스트링이 그 취약성을 이미 적어 두었다).
>
> ★ 그래서 사회 편익은 `build_society_annualised()` 로 **따로 연간화**해
> `build_perspective_wiring()` 의 `society_annualised` 인자로만 넣는다 —
> `annualised`·`annual_benefit`·`benefit_rows` 는 **한 글자도 고치지 않았고** 인자가
> 비면 종전과 완전히 같게 돈다.
>
> ★★ **변이가 그 설계의 값을 실측으로 보였다.** `annual_benefit` 에 그 값을 합치는
> 변이를 넣으면 검사 13종 중 **구조 단언 하나만** 빨간불이 됐다(사업자 `npv` 가
> **637,805,659원**으로 튀었다). **값이 0인 것만 재는 검사는 그 변이를 전부 놓쳤다** —
> 「배선 전후로 축이 같다」만 단언하면 값이 0인 동안은 잘못 배선해도 초록불이다.
>
> ## ⑤ ★★★ WP-2 가 잰 것 — **⑤ 의 선행이 「판정 하나」로 좁혀졌다**
>
> 물음은 *「러너가 `PeakShaving` 을 끄고 `NWAs` 만 태우는 실행이 지금 가능한가」* 였다.
> **답: 지금은 불가능하고, 여는 변경은 작지만 그 변경이 판정을 되돌린다.**
>
> - ★★★ **차이를 만드는 것은 값이 아니라 `enabled` 하나다 — 실행으로 확정했다.**
>   `NWAs` 를 켜면 단가가 **0원이어도** `assert_no_exclusions()` 가 거부한다.
>   대조군까지 두고 갈랐다 — `enabled=True`(값 0)는 거부되고 `enabled=False`(값 0,
>   나머지 동일)는 거부되지 않는다. ⚠ **「값이 0이라 안 걸렸다」와 「배타가 성립하지
>   않는다」를 섞어 읽지 말라**는 자리에 실물 답이 섰다
> - ★★ **`PLR0915` 슬랙을 재는 방법이 나왔다** — 기본 상한(50)에서는 통과하니 여유를
>   알 수 없다. **상한을 45로 낮춰 돌리면** `Too many statements (50 > 45)` 가 떠
>   **정확히 50/50 · 여유 0** 임이 드러난다. *「이미 걸려 있다」는 정확히는 「위반은
>   아니지만 슬랙이 0」* 이었다
> - ★★ **여는 변경의 크기** — `e2e_runner.py` 의 기존 `PeakShaving(...)` 호출문에
>   `enabled=` 인자 한 줄(같은 statement · `PLR0915` 소비 0 · 코드 +2줄).
>   ⚠⚠ **그런데 그것이 R52/WP-3 이 사용자 판정 §3 을 반영해 세운 거부 검사 둘**
>   (`test_nwas_enabled_only_when_grid_discharge_selected` ·
>   `test_cp_enabled_only_when_semi_central_dispatch_selected`)**을 정확히 되돌린다** —
>   그 둘은 `pytest.raises(ValidationError)` 로 **거부를 기대**한다
> - ⚠ **그 「깨진다」는 추론이지 실행 결과가 아니다** — 확인하려면 소스를 고쳐야 하고
>   그것이 이 WP 의 금지사항이었다. **워커가 그 사실을 스스로 표시했다**
> - **④ 이분법(참고)** — `assert_no_exclusions` 를 **자기 프로세스 안에서만** no-op 로
>   몽키패치하고 나머지 파이프라인은 그대로 돌려, 결손이 뒤집히는 `NWAs` 단가를
>   **약 837원/kWh** 로 쟀다. ⚠⚠ **시험용 가정값이며 사업 전망이 아니다.** 실제 대장
>   `REC` 탐침값(70원/kWh)의 **8.4%** 수준이라 실제 값으로는 뒤집히지 않을 크기다.
>   ⚠ 그 기준선 `npv=-12,591,162` 는 **`MC-1` 리포트가 아니다** — `REC`=0 ·
>   모드=`GRID_DISCHARGE` 조합의 별도 탐침이며 **옛 결론축과 수가 우연히 같다.**
>   **결론축으로 인용하지 마라**
>
> ★★ **워커가 오케스트레이터의 미커밋 편집을 알아채고 건드리지 않았다.** `git status`
> 에 `M status.md` 가 떴을 때 *「내가 만든 변경이 아니다」* 를 근거 셋으로 밝히고
> **되돌리지도 커밋하지도 않은 채 판정을 요청했다** — `git checkout`/`stash` 로
> 「정리」했다면 닫기 문서를 잃었다.
>
> ## ⑥ ★★ 물려받은 수 **둘이 틀렸고**, 인계본의 권고 **둘을 버렸다**
>
> - ⚠⚠ **`e2e_runner.py` 는 「500/500」이 아니라 코드 494/500(여유 6)이었다.**
>   `status.md` 와 R53 인계본이 둘 다 *「한 줄도 못 넣는다」* 로 적고 있었다. 직접 세서
>   **인계본이 권한 「먼저 분리해야 한다」를 따르지 않았고**, 일을 여유 많은
>   `perspectives.py` 로 밀어 **분리 WP 하나를 아꼈다**. (WP-1 뒤 **496/500**)
> - ⚠⚠ **★교체비(인계본 2순위)는 이미 소진된 자리였다.** R52 가 조사를 이미 했고
>   대장 `capex.ess.replacement` 가 *「교체가 통상 더 싸다는 방향은 확인했으나 ESS 셀
>   교체 단가를 국내 공공·업계 자료에서 못 찾았다 · `Q-2` 회신에 함께 오면 닫힌다」* 로
>   적어 두었다 — **남은 것은 외부 회신, 곧 사람 몫**이다. 다시 조사하면 한 시간을 같은
>   결론에 쓴다. **집지 않았다.**
> - ⚠ **★U(잉여판매 차감이 계약구조 경로에 없다)도 재 보고 버렸다** — `settlement.py`
>   의 조립기 여섯이 `(provider, inputs, engine)` 만 받아 **ESS 를 못 본다.** 배선하려면
>   `SettlementInputs` 계약까지 바꿔야 하고 남은 시간에 **반쯤 끝난다.**
>
> ★ **「재 보고 안 집었다」도 결과다** — 근거 없이 값을 바꾸거나 반쯤 흔들면 결론이
> 근거 없이 움직인다(R52 의 §1 이 0원 움직인 것과 같은 자리).
>
> ## ⑦ 게이트 실측
>
> | | 값 |
> |---|---|
> | `pytest` (오케스트레이터 재실행 · WP-1) | **1,877 passed · 3 skipped · 1 xfailed · 0 failed** (713초) |
> | `pytest` (오케스트레이터 재실행 · 교정) | **1,878 passed · 3 skipped · 1 xfailed · 0 failed** (623초) |
> | `mypy` | **127 파일 무결** |
> | `ruff` | 0건 (고친 파일을 직접 줘서도 0건) |
> | `lint-imports` | **4 kept · 0 broken** |
> | 규칙 검사 11종 | 전건 rc=0 |
> | `check_file_size --code-strict` | rc=0 |
> | `gen_traceability` 재생성 | `docs/traceability.md` **바이트 동일** |
> | `check_test_accompaniment --base origin/main` | **rc=0** (교정 뒤) |
> | 음성 스위트 15종 | **전건 rc=0 · 변이 잔존 0** (깨끗한 트리에서 오케스트레이터만 · 빈 디렉터리 둘 `rmdir`) |
>
> ## ⑧ ⚠⚠ **`NFR-105` 게이트 ①②는 PR 에서만 판정된다 — 직접 푸시는 「미수행」이다**
>
> `.github/workflows/tests.yml` 이 머리말에 그렇게 적고, 스텝도
> `if: github.event_name == 'pull_request'` 로 갈린다. 이 저장소는 라운드를
> **`git push origin HEAD:main`** 으로 닫아 왔으므로 **이 두 게이트는 그 경로에서
> 한 번도 판정된 적이 없다.** 워크플로는 그것을 조용히 넘기지 않고 *「통과가 아니라
> **미수행**입니다」* 라고 로그에 적는다(§13.0.1 ④) — **그러나 아무도 그 줄을 읽지
> 않으면 초록불과 구별되지 않는다.**
>
> ★ 그래서 R53 은 그 둘을 **오케스트레이터가 손으로** 돌렸고 위반 하나를 잡았다.
> ⚠ **다음 라운드도 손으로 돌려라** — 닫기 전 `check_test_accompaniment --base origin/main`.
>
> ## ⑨ 이 라운드가 **닫은** 것
>
> - **★관점공백의 절반** — 사회 관점 편익 배선. ⚠ **전기사용자 관점
>   `SelfConsumption` 미화폐화는 그대로 남았다**
> - `NFR-105` 동반 공백 하나(`perspective_section.py`)
>
> ## ⑩ 오케스트레이터가 스스로 밟은 것
>
> ⚠ **게이트를 `tail` 에 물려 `rc=0` 을 잘못 읽었다.** `check_test_accompaniment` 가
> 실제로는 rc=1(위반 1건)이었는데 파이프 끝 `tail` 의 rc 를 읽어 **초록불로 봤다.**
> 이 저장소의 「게이트」 절이 *「게이트를 파이프에 물리지 마라」* 를 이미 적어 두었고
> **그 함정을 적어 둔 라운드의 다음 라운드가 그것을 밟았다.** 파이프 없이 다시 돌려
> 잡았고, 그것이 커밋 하나(`32eae92`)를 더 만들었다.

---

> ### ⬇ **R52 블록**(2026-09-02) — R53 **직전**의 라운드다.
> **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-02 **R52 — 사용자 판정이 두 번 왔다. ★★ 결론축이 움직였고, 움직인 것은 `REC` 하나다**
>
> 오케스트레이터 `cl4`(R51 → R52 이관) · 워커 `cl1` · 사용자 지정 10시간
> (05:20 → 15:20 KST) · 브랜치 `r48-wp-a`(사용자 지정 · 바꾸지 않았다).
>
> ## ① 재료가 **둘**이었다 — 라운드 도중에 하나가 더 왔다
>
> | | |
> |---|---|
> | **`docs/decisions-2026-09-02-R52.md`** | 착수 시점의 재료 — 사용자 판정 **열하나**(§1~§11) |
> | **`docs/decisions-2026-09-02-R52b.md`** | ★ **06:39 에 사용자가 보낸 「분석결과 보고서에 대한 의견 다섯」.** 앞 재료를 **대체하지 않고 착수 순서만** 바꿨다(그 문서 §6 이 새 순서와 근거를 든다) |
>
> ## ② 결론축 — ★★ **움직였다. 한 번만 움직였다**
>
> | | 옛 수 | **새 수** |
> |---|---|---|
> | 무보조 `npv` | −12,591,162원 | **−11,537,129원** |
> | 결손/총사업비 | 128.5% | **117.7%** |
> | 보조 20% · 80% | −10,631,162 · −4,751,162 | **−9,577,129 · −3,697,129** |
> | `payback` | `.inf` | **`.inf`**(셋 다 그대로 — 미회수) |
>
> ⚠⚠ **옛 수를 인용하지 마라.**
>
> **움직인 원인은 하나다 — `REC` 단가 0 → 70원/kWh(+1,054,033원).** 세 시나리오가
> **정확히 같은 폭**으로 움직였다(지원율이 달라도 REC 편익은 같다 — 내적 검산).
> ★ R51 이 독립으로 잰 값과 원 단위로 일치한다.
>
> - **§1(전제값 인터넷 재조사) — 0원.** ★★ **조사가 값을 바꿀 근거를 준 항목이
>   하나도 없었다.** 그것이 이 라운드의 중요한 결과다 — 대신 **출처를 채웠다**
> - **§7(교체비 별도) — 0원.** 통로는 세웠으나 *「일반적으로 더 저렴」*의 **크기
>   근거를 못 찾아** 값을 취득가와 같게 두었다. **축이 안 움직이는 것이 옳다**
>
> ### ★★ 축을 **미리 재 두고 대조**했다 — R51 이 세운 절차
>
> 오케스트레이터가 `.orch/R52/probe_axis.py` 를 만들어 **대장 파일을 건드리지
> 않고**(임시 사본에 `value` 만 갈아 끼워) 여섯 수를 먼저 쟀다. 그 탐침이 **옛 골든
> 3건을 원 단위로 재현**했으므로 믿을 수 있었고, 워커 결과가 **전건 일치한 뒤에**
> 골든을 덮었다. ⚠ 그 파일은 `.orch/` 안이라 **커밋되지 않는다** — 다시 만들려면
> `build_case_report()` 에 `assumptions_path=` 로 임시 대장을 주면 된다.
>
> ## ③ WP 표 — **커밋 열둘**
>
> | WP | 무엇 | 커밋 | 축 |
> |---|---|---|---|
> | **WP-1** | **검증 보고서** 신설(§2) — 대조군이 **원천적으로 없다**는 판정에 맞춰 9단계의 ⓐ전제값 ⓑ계산값 ⓒ인계값 ⓓ수식을 낸다(`--kind verification`) | `3ef420f` | 불변 |
> | **WP-2** | **자가소비율**을 본 실행 실측으로(§4) — 모듈 상수를 인쇄하던 것을 **발전 − 잉여**로 잰다 | `9eb3ad0` | 불변 |
> | (재료) | 사용자 의견 다섯을 **판정 정본**으로 세움 | `a6d71e2` | — |
> | **WP-A** | **관점 넷 배선**(R52b §1·§5) — 기계는 다 있는데 **부르는 자리가 0곳**이었다 | `dd548e8` | 불변 |
> | **WP-B** | **적정 용량 산출 방법 검토서**(R52b §2·§3·§4) — 코드 무변경 | `b340f92` | 불변 |
> | **WP-5** | **전제값 인터넷 조사**(§1) — 대장 무변경 | `d4b3fbf` | 불변 |
> | **WP-6** | ★★★ **축 한 번** — `REC` 70원 + 교체비 통로 + 골든 3건 + `MC-1` + 검증보고서 + spec v0.25 | `2be55f8` | ★ **움직임** |
> | **WP-3** | **유형 `E` 배타를 실행 경로가 거부**(§3 앞 문장) | `3add686` | 불변 |
> | **WP-7** | **해설서**(§6) — 「이 리포트를 읽는 법」 | `a4e3d53` | 불변 |
> | **WP-4** | **규칙책에서 설정 수치 빼기**(§8) + spec v0.26 | `050710f` | 불변 |
> | 닫기 | **음성 스위트 둘 복구** | `73b25ec` | 불변 |
>
> ## ④ 게이트 실측 (라운드 종료 시점) — ⚠ **수를 믿지 말고 명령을 돌려라**
>
> ```
> pytest                        1874 passed · 3 skipped · 1 xfailed · rc=0
> ruff / mypy / lint-imports    rc=0 / 127 files / 4 kept 0 broken
> check_file_size --code-strict rc=0 (코드 줄 초과 0건)
> gen_traceability              rc=1 (정상 — 미매핑 9건) · git diff 무변경
> 규칙 검사 10종                전건 rc=0 (check_source_rules·check_doc_line_citations 포함)
> 음성 스위트 15종              전건 rc=0 · 변이 잔존 0 (둘은 이 라운드가 되살렸다)
> ```
>
> ## ⑤ 이 라운드가 **새로 밟은 함정** — 전문
>
> 1. ⚠⚠⚠ **워커가 교정 뒤 「좁힌 검사만」 돌리고 그것을 「전건 초록불」로 읽히게
>    적는다.** 실측 **두 번** — WP-A 에서 셋(전역 가변 컨테이너·독스트링 참조·추적표),
>    WP-3 에서 하나(DoD 6). **오케스트레이터가 매 WP 전건을 다시 돌리지 않으면 못 잡는다**
> 2. ⚠⚠ **`pre-commit` 의 `ruff` 훅이 막는데 `ruff check <디렉터리>` 는 통과한다.**
>    훅은 **파일을 직접** 준다. `RUF001`(모호한 `×`)이 그렇게 새어 들어왔다 —
>    규약은 **사유 주석 + `# noqa: RUF001`**(`operating_lines.py` 가 본)
> 3. ⚠⚠ **새 모듈을 만들면 모듈 수준 `dict` 가
>    `test_no_module_or_class_level_mutable_containers` 에 걸린다** —
>    `MappingProxyType`·`tuple`·`frozenset` 로. 한 라운드에 **두 번** 밟았다
> 4. ⚠⚠ **문서에서 파일 이름이 줄바꿈으로 갈리면 거짓 이름이 된다.** yaml 의 `>-`
>    접힌 스칼라에서 줄바꿈이 **공백**이 되어 `docs/decisions-2026-09- R52.md` 가 됐다.
>    **이름 가운데에서 줄을 바꾸지 마라 — 이름 앞에서 바꿔라**
> 5. ⚠⚠ **워커가 편집 중일 때 커밋하면 `pre-commit` 이 unstaged 파일을 stash 했다
>    되돌린다.** 그 사이 워커가 쓰면 어긋난다 — **워커가 멈춘 뒤에 커밋한다**
> 6. ★ **「0」과 「산출하지 않았다」가 구분되지 않는 표는 빈칸보다 나쁘다.** 관점 표
>    첫 판이 주민 NPV **974,035원**을 인쇄했고 *「전기사용자는 97만원 이득」*으로
>    읽혔다 — **비용을 한 원도 안 붙인 편익 현가**였다
> 7. ★★ **대장이 좋아지면 음성 사례가 조용히 죽을 수 있다.** 「출처 없이 확정 주장」
>    변이가 `source` 를 채운 뒤 **미감지**로 돌아섰다 — 검사를 느슨하게 하지 말고
>    **변이가 그 상태를 직접 세우게** 고쳤다
>
> ## ⑥ 이 라운드가 **연** 것
>
> 남은 목록은 `status.md` 의 **「다음에 집을 것」 절**이 갖는다. 여기서는 **이번에
> 새로 열린 것**만 적는다:
>
> - ★★★ **계절 축**(R52b §4)과 거기 매달린 **ESS 용량 역산**(§3) — **넘겼다.**
>   모형은 **「대표일 24스텝 × 365일」**이라 계절이 구조적으로 없다. **결론축을 크게
>   움직이므로** 「축은 한 번만」 규칙에 걸렸다. 설계 스케치는 검토서가 갖는다
> - ★★ **용량 분할**(§3 뒷 문장 · `mode_weights`/`HYBRID` 처리) — **미착수**
> - ⚠⚠ **전기요금 대장값이 조사보다 체계적으로 높다는 신호**(기본요금 35~55% ·
>   전력량요금 62~79%). **대장이 높으면 편익이 과대**가 된다 — 계약종별 미확인이라
>   **바꾸지 않고 신호만 대장에 적었다**
> - ⚠ **월 600kWh(사용자 예시) 대 연 3,600kWh(대장)** — **두 배 가까이 다르다.**
>   대신 정하지 않고 물음으로 남겼다
> - ⚠ **사회·정부 관점의 편익 배선 공백** · **전기사용자 관점의 `SelfConsumption`
>   미화폐화** — 채우면 축이 움직인다
> - ⚠ **교체비의 크기 근거** — 방향만 확인됐다
> - ⚠ **「국가적 관점 = 사회」는 확인받지 못한 가정** — 넷을 다 내어 물음을 지웠다
> - ⚠ **`FR-704` 어휘(사업자·주민·정부)와 사용자 어휘(국가·전기사용자·분산E)가
>   어긋난다** — spec 개정 여부는 사람 몫
> - ⚠⚠⚠ **`core/casegrid/e2e_runner.py` 코드 줄이 500/500 이다** — **한 줄도 못
>   넣는다.** `core/der/ess.py` 도 498/500

---

> ### ⬇ **R51 블록**(2026-09-02) — 그 아래 **닫은 뒤 돌린 WP-7 블록**이 먼저 온다.
> ⚠ **가장 새 것이 아니다** — 위 **R52 블록**이 더 새것이며, 현재 상태의 정본은
> `status.md` 다.

> # ⏹ 2026-09-02 **R51/WP-7**(라운드를 닫은 뒤 남은 시간) — **★Q 를 닫고 ★V 를 열었다**
>
> R51 은 `ee1a83f` 로 닫혀 `main` 에 밀렸고 CI 가 초록불이었다. 사용자가 지정한 10시간
> 중 **남은 시간**에 착수 순서 **⑤**를 마저 하려고 돌린 WP 다(R50 이 WP-4 를 그렇게
> 돌린 것과 같은 형태). 오케스트레이터 `cl3` · 워커 `cl1` · **커밋 하나**(`8d1d05a`).
>
> ## 1단계(★Q)는 닫았다 — `NWAs`·`CP` 가 이제 **돈을 번다**
>
> `core/casegrid/grid_support.py` 를 세워 `e2e_runner._rec()` 를 **그대로 본떴다**
> (WP-6 이 `REC` 로 연 길이다). 단가는 `benefit.nwas_price`·`benefit.cp_price` 로
> **`track: "default0"`(값 0)** — 판정 §3 의 「기본 비활성」을 지킨다.
> ⚠ **`PLR0915`(statement 상한 50)에 실제로 걸렸고**(별도 대입이면 51) 기존 대입
> 표현식 안에 `*` 로 풀어 되돌렸다. **결론축 불변**(무보조 −12,591,162원 · 골든 0건 변경).
>
> ## ★★★ 2단계(⑤)는 **만들지 않았다** — 만들면 거짓이 되기 때문이다
>
> 1단계가 실물로 잡은 것: **유형 `E` 배타가 배포 경로에서 걸리지 않는다.**
>
> - `assert_no_exclusions()` 는 **유형 `A` 만 거부한다**(조항이 행마다 수용 수준을
>   다르게 정해 두었고 그 함수 독스트링이 근거를 적는다)
> - 유형 `E` 를 라벨링하는 `build_report()` 는 **배포 코드에서 부르는 자리가 0곳**이다
> - 실행 확인 — 「계통 방전」에 `NWAs` 단가를 준 실행이 **거부 없이 서고**
>   `NWAs`(361,715원)가 `SurplusSale` 과 나란히 계상된다. 가구 부하를 세우면
>   `PeakShaving` 도 함께 실린다
>
> ⚠⚠ **R51/WP-3 의 커밋과 `ESS.value_streams()` 독스트링이 *「배타는 실행 경로
> (`assert_no_exclusions`/`build_report`)가 건다」* 고 적었는데 그것이 거짓이었다.**
> WP-3 의 검사는 `build_report()` 를 **직접 불러서** 통과했다 — **R26 이 *「거부 기계는
> 있는데 배포 코드가 아무도 안 부른다」* 로 밟은 형태가 한 층 위에서 되풀이된 것**이고,
> 그때는 *함수*가 안 불렸고 이번에는 *검사가 그 함수를 대신 불러* 초록불을 만들었다.
> **WP-7 이 그 독스트링의 거짓 문면을 그 자리에서 정정했다**(코드 줄 0 증가 · 498/500).
>
> **그래서 ⑤를 만들지 않았다** — 지금 `NWAs` 를 크게 켠 골든을 만들면 **사업자 운전
> 편익이 함께 실린 채로 「남는다」를 세우게 되고**, 그것은 판정 §3 이 *「동시에 성립하지
> 않는다」* 고 못 박은 조합이다. **가짜 골든을 만들지 않는다.** `status.md` 의 **★V** 가
> 그 자리이며 **⑤의 진짜 선행**이다.
>
> ⚠ **고치지 않은 이유**: 유형 `E` 는 이 저장소가 *「「막는」 규칙이 아니라 「성립하지
> 않음을 선언」하는 규칙」* 으로 세운 것이라(`docs/exclusion-rules.yaml`) **거부로 바꾸면
> 그 선언과 어긋난다.** 어느 쪽으로 갈지가 **판정**이고, 남은 시간에 그 판정을 내려
> 배타 의미론을 저장소에 박아 넣지 않는 편이 낫다고 보았다.
>
> ## 게이트
>
> `pytest` **1,855 passed · 3 skipped · 1 xfailed · 0 failed**(3.13 · CI 동등 3.11 둘 다 ·
> R51 종료 1,847 + 신설 8) · `ruff`·`mypy`·`lint-imports`·`check_file_size --code-strict`·
> 검사기 열둘 **전건 rc=0** · **골든 변경 0건**.
> `e2e_runner.py` 코드 줄 **481/500**(여유 19) · `ess.py` **498/500**(독스트링만 고쳤다).
>
> ## ⚠ 오케스트레이터가 이 WP 에서 저지른 것
>
> **`status.md` 를 bash here-doc 안의 파이썬으로 고치다가 한글 문면을 깨뜨렸다**
> (「배타」→「배팀」 같은 글자 단위 손상). `git checkout -- status.md` 로 되돌리고
> **Edit 도구로 다시 썼다.** ⚠ **이 저장소가 워커에게 반복해 경고해 온 바로 그 함정을
> 오케스트레이터가 밟았다** — *「백틱과 한글이 살아남는 방법으로 써라 · 쓰고 나서 읽어
> 확인하라」*. **정본 편집은 Edit/Write 도구로만 한다.**

> # ⏹ 2026-09-02 **R51 — 사용자 판정 여덟 중 넷을 구현했다. ★★ 결론축이 움직였다**
>
> 오케스트레이터 `cl3` · 워커 `cl1`(WP-1·2·3·5) → **`cl4`**(WP-6, 5시간 잔여 26% 에서
> 교체) · 2026-09-01 **21:10 착수** → 사용자 지정 종료 **2026-09-02 07:10**.
> **WP 여섯을 열어 다섯을 커밋했다.** 브랜치 `r48-wp-a`(사용자 지정 · 바꾸지 않았다).
> 재료 정본은 **`docs/decisions-2026-09-01-R51.md`**(사용자가 직접 답한 여덟 · 잠정 아님).
>
> ## ★★ 결론축 — **움직였다. 옛 수를 인용하지 마라**
>
> | | 옛 값 | 새 값 | 차이 |
> |---|---|---|---|
> | 무보조 `npv` | −12,956,180 | **−12,591,162** | **+365,018** |
> | 보조 20% | −10,996,180 | **−10,631,162** | +365,018 |
> | 보조 80% | −5,116,180 | **−4,751,162** | +365,018 |
> | 결손/총사업비 | 132.2% | **약 128.5%** | |
>
> `payback_period_years` 는 셋 다 **`.inf` 그대로**(20년 안에 미회수). 전액 지원해도
> 남는 결손 **2,791,162원**(옛 3,156,180원). ★ **세 시나리오가 정확히 같은 폭으로
> 움직였다** — 보조율은 초기투자 분담만 바꾸므로 운영현금흐름 이동은 시나리오와
> 무관하다. **그 「같은 폭」이 내적 검산이었다.**
>
> ★ **움직인 원인은 하나다** — 판정 §1 의 「집 우선」 기본값. `REC` 는 배선했으나
> `track: "default0"`(값 0)이라 **한 원도 넣지 않았다**(아래 WP-6).
>
> ## ★ 축을 **한 번만** 흔들기 위해 순서를 다시 짰다
>
> 착수 순서 ①(낮 전기 배분)과 ④(잉여판매 분리 + `REC`)가 **둘 다** 축을 움직인다.
> 그래서 **①의 기본값 뒤집기를 ④와 한 WP(WP-6)로 묶어 맨 뒤에** 두었고, WP-1~WP-5 는
> 전부 **축을 건드리지 않고** 왔다(매 WP 마다 `MC-1` npv 를 재서 확인했다).
> ★ **오케스트레이터가 파일을 고치기 전에 모듈 상수만 메모리에서 바꿔 여섯 수를 먼저
> 쟀고**, 워커 결과가 그것과 **전건 일치**한 뒤에 골든을 덮었다.
>
> ## WP 표
>
> | WP | 착수순서 | 커밋 | 무엇을 닫았나 | 축 |
> |---|---|---|---|---|
> | **1** | ① | `0ec7596` | `PVAllocationPriority`(집 우선·배터리 우선·가격 기반) 신설. 「가격 기반」은 **자리만 있고 고르면 `ValidationError`**. `req("FR-302-AC1")` 을 「집 우선」 검사 둘에만 달았다 | 불변 |
> | **2** | ② | `71d0807` | 고정 O&M 을 `opex.pv.fixed_om`·`opex.ess.fixed_om` 으로 대장에 올렸다(값 그대로 · 「가정」). `opex.` 이름공간 신설 + **그 접두어의 스윕 래칫**도 함께 세웠다. `check_hardcoded_params` 에 **`COINCIDENTAL_LITERALS`**(우연한 충돌을 사유와 함께 등재 · 낡으면 rc=1) 신설 | 불변 |
> | **3** | ③ | `ea88d8c` | `ESSOperatingMode` 에 「계통 방전」·「준중앙급전 등록」 신설, `value_streams()` 표에 `NWAs`·`CP` 두 줄. **spec v0.23**. `ess.py` 코드 줄 **500/500 → 498** 로 회수 | 불변 |
> | **4** | ⑤ | **없음** | ⚠ **착수하지 못하고 멈췄다** — 아래 「열어 둔 것」 | — |
> | **5** | ④ 전반 | `7c40321` | `SurplusSale.non_pv_ess_discharge_kwh` 로 **충전원별 수량 차감**. R50 이 세운 `TouArbitrage`↔`SurplusSale` **유형 `A` 배타를 뗐다**(14→13 · A 5→4 · E 6 그대로). `core/casegrid/pv_allocation.py` 분리로 `e2e_runner` 여유 **0 → 32줄**. **spec v0.24** | 불변 |
> | **6** | ④ 후반 + ①의 기본값 | `bcbf8b7` | ★★ **`PV_ALLOCATION_PRIORITY_DEFAULT` → `HOUSEHOLD_FIRST`**. `REC` 를 화폐화 경로에 배선(`benefit.rec_price` · `default0` · 값 0). **골든 3건 + `MC-1` 리포트를 한 번만** 재산출 | ★★ **움직였다** |
>
> ## 게이트 실측 — **오케스트레이터가 매 WP 마다 단독으로 재실행했다**
>
> | | 값 |
> |---|---|
> | `pytest` | 착수 **1,815**(R50 종료) → 1,823 → 1,829 → 1,833 → 1,841 → **1,847 passed · 3 skipped · 1 xfailed · 0 failed** |
> | 인터프리터 | `~/miniconda3/python.exe`(3.13) · CI 동등 `envs/r31py311`(3.11) 둘 다 |
> | `ruff` · `mypy`(123 files) · `lint-imports`(4 kept, 0 broken) | 전건 rc=0 |
> | 검사기 | `check_file_size --code-strict` 외 **열셋** 전건 rc=0 |
> | **음성 스위트** | ★ **15종 전건 rc=0**(깨끗한 트리에서 오케스트레이터가 돌렸다 · **변이 잔존 0**). ⚠ 옛 인계는 「14종」이라 적었다 — **15종이다** |
> | 커밋 | 다섯 (`0ec7596`·`71d0807`·`ea88d8c`·`7c40321`·`bcbf8b7`) |
>
> ★ **결론축이 움직였는데 빨간불이 된 검사는 둘뿐이었다** — 나머지(골든 회귀 · CI 실측치 ·
> 결손 · 문면 · 본문 줄 수)는 값을 **밖에서 짓거나 성질로만** 재고 있어 그대로 지나갔다.
> **이 저장소가 「값을 박지 않는다」로 쌓아 둔 것이 여기서 값을 했다.**
>
> ① `test_the_resource_table_splits_a_benefit_that_two_resources_earned` — 밖에서 고정한
> 4.3 오라클이 낡았다(안분: 태양광 43,954 → 25,274원 · 저장장치 298,106 → 176,626원).
> **새 리포트의 표를 그대로 옮겼다**(검사 안에서 다시 계산하지 않는다 — R48 과 같은 방식).
> ② `test_appendix_four_names_the_earner_from_attribution_not_declaration` — ★ **값이
> 낡아서가 아니다.** `REC` 가 서면서 **금액 0 인 편익**이 처음 생겼고 검사만 금액으로
> 걸러 리포트와 집합이 갈렸다. **리포트가 일관돼 있었으므로 검사 쪽 필터를 뗐다.**
>
> ## WP 별로 **무엇이 어긋나 있었나**
>
> - **①** `PV_SELF_CONSUMPTION_RATIO = 0.0` 이라 **가구 몫이 한 kWh 도 없었다.** 조항
>   `FR-302-AC1` 의 ①은 *「즉시」 자가소비*(그 스텝의 실제 부하)인데 구현의 ①은 **연간
>   고정 비율**이었고, 배포 경로에서 그 비율이 0 이었다. 그 어긋남을 「집 우선」이 없앤다
> - **②** 고정 O&M 이 **소스 상수라 흔들어 볼 수 없는 채 5.1 영향도 6·7위**(각 변동폭
>   1,535,451원)를 차지하고 있었다. 대장에 오르며 붙임 2 순위 **4·5** 로 이동
> - **③** `NWAs`·`CP` 는 조항·클래스가 R48/R50 에 섰으나 **그 태그를 낼 모드가 없었다**
> - **⑤** ⚠ **`extra_value_streams` 는 이름과 달리 「배타 검사 전용」 인자였다** —
>   화폐화 경로(`_annualise`)에 들어가지 않는다(그 함수 독스트링이 그렇게 적는다).
>   그래서 `NWAs` 를 켜도 **한 원도 벌지 못하고 정책 경고도 뜨지 않았다**
> - **④** `SurplusSale` 의 수량이 **시스템 총 역송량**이라 누가 내보냈는지 몰랐다.
>   R50 은 그것을 **배타로 막았고**, 판정 §4 는 *「배타로 막으면 정당한 동시 운전을
>   지운다 — 수량에서 빼라」* 로 답했다
>
> ## 이 라운드가 **닫은** 것
>
> 착수 순서 **①②③④** 와 그에 딸린 조항 개정(**spec v0.23 · v0.24**). 그리고
> **32번**(리포트 첫 화면 「없음」)은 R50 → R51 인계 커밋이 이미 *「현 상황에서 변경
> 유인 없음(유지)」* 로 **기각**해 이력으로 내렸다 — ⚠ **「유지」는 「미해결」이 아니다.**
>
> ## ⚠ 이 라운드가 **가정으로 진행한 것** — 사용자가 10시간 무인을 지정했으므로
>
> 묻지 않고 진행했고, **그 사실을 코드 주석·`result_*.md`·인계본 셋 다에 남겼다.**
>
> 1. ★★ **`REC` 를 켜지 않았다**(`track: "default0"` · 값 0). 판정 §4 는 배선을
>    요구하고 *「단가·가중치는 값 근거가 필요하다」* 고 적는데 **값 근거가 없다** —
>    지어낸 단가 위에 결론축을 올리지 않는다(R48 §6 과 같은 자리). **되돌리는 법:
>    대장 `value` 한 줄.** ★ 켜면 **+1,054,033원 · 결손의 약 8%**(70원/kWh 탐침 · 임시
>    단가는 대장 사본에서 재고 되돌렸다) — **REC 만으로는 결론이 뒤집히지 않는다**
> 2. **REC 가중치(1.0)를 대장이 아니라 소스에 두었다** — 대장 갈래 셋이 전부 맞지
>    않는다(`assume` 은 지어낼 감도 폭을 요구 · `fixed` 는 출처를 요구 · `default0` 면
>    단가만 등재해도 안 켜진다). **단가가 등재되는 날 함께 가야 하고, 그것을 기억이
>    아니라 빨간불로 막는 래칫**(`test_rec_weight_moves_to_the_ledger_when_the_price_does`)
>    을 세웠다
> 3. **고정 O&M 감도 폭 ±50%(50,000~150,000)를 오케스트레이터가 정했다** — 대장 스키마가
>    `sensitivity` 를 **필수로** 요구해 「폭을 안 정한다」는 갈래가 없었다. **그 폭이
>    근거 없음을 두 항목의 `derivation_method` 가 스스로 적는다**
> 4. **고정 O&M 축을 둘로 갈랐다** — 판정 §2 문면은 「축이 하나 는다」고 적는다.
>    PV·ESS 는 다른 설비이고 값이 갈릴 수 있다는 더 앞선 근거를 따랐다
> 5. **`opex.*`·`benefit.rec_price` 에 `q_ref` 가 없다** — 새 `Q-*` 는 spec §15.1 에
>    행이 있어야 `check_assumptions` 를 통과하고 **spec 개정은 사람 몫(§16.5)** 이다.
>    `tax.vat_rate` 선례를 따랐고 `source: null`·`confidence: 가정` 은 그대로 드러난다
> 6. **`_MODE_WINDOWS` 의 새 두 모드 창(충전 01~06 · 방전 18~21)은 대표 프로파일**이며
>    계통운영자의 실제 지시 시각이라는 근거가 없다
> 7. **잉여판매 차감을 계약구조 경로(`settlement.py`)에는 배선하지 않았다** — `MC-1` 은
>    구조 없는 기본 경로라 결론축과 무관하다. 판정 §4 가 그 경로까지 요구하는지 침묵한다
>
> ## ⚠ 프로세스에서 실제로 겪은 것
>
> - ⚠⚠ **워커가 「음성 스위트 금지」를 어겼다**(WP-3) — 15종을 전건 실행했다. **오염은
>   없었다**(오케스트레이터가 `git status`·`grep NEGCHECK` 로 즉시 확인). 다음 WP 부터
>   지시문에 **이유와 함께 눈에 띄게** 적었고 그 뒤로는 어기지 않았다
> - ⚠ **워커가 `pytest -q` 를 써서 결과 줄을 잃고 그것을 「원인 모를 로그 절단」으로
>   적었다**(WP-3) — `addopts` 에 이미 `-q` 가 있어 `-qq` 가 된다. 게이트 절이 경고한
>   그 함정이며, 오케스트레이터가 `-q` 없이 다시 돌려 요약 줄을 얻었다
> - ★ **워커가 「전제가 틀렸다」로 멈춘 것이 옳았다**(WP-4) — 가짜 골든을 만들지 않았다
> - ⚠ **워커 세션이 `API Error: Connection lost mid-response` 로 끊겼다**(WP-6, 02:07).
>   트리는 절반만 반영된 상태였고, 오케스트레이터가 **어디까지 됐는지 실측한 뒤 남은
>   것만 이어서 지시**해 처음부터 다시 하지 않게 했다
> - ⚠ **워커·오케스트레이터가 같은 체크아웃을 쓰는 구성에서 `lint-imports` 서브프로세스
>   검사 둘이 한 번 거짓 빨간불을 냈다**(WP-5) — 단독 재실행에서 재현되지 않았다.
>   **빨간불을 보면 먼저 단독으로 다시 돌려라**
> - ★ **워커를 WP 마다 `/clear` 하고 `/context` 로 `Messages` 를 확인했다** — 여섯 번
>   전부. 5시간 잔여 26% 에서 `cl1` → `cl4` 로 교체했고 교체 비용은 지시문이 파일에
>   있어 사실상 0 이었다
>
> ## 커밋
>
> ```
> bcbf8b7  WP-6  결론축을 R51 의 「한 번」으로 움직였다 (판정 §1·§4)
> 7c40321  WP-5  잉여판매를 충전원으로 가르고 배타를 뗀다 (판정 §4 · spec v0.24)
> ea88d8c  WP-3  NWAs·CP 를 자원에 배선한다 (판정 §3 · spec v0.23)
> 71d0807  WP-2  관리비를 대장에 올리고 「가정」이라 적는다 (판정 §2)
> 0ec7596  WP-1  낮 전기 배분 축을 세운다 (판정 §1)
> ```

---

> ### ⬇ **R50 블록**(2026-09-01). 위 R51 블록이 더 새 것이며,
> **현재 상태는 어느 쪽도 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-09-01 **R50 — R48 이 연 배선 셋(★H·★F·★G)과 이름 하나(★K)를 닫았다. 결론축은 움직이지 않았다**
>
> 오케스트레이터 `cl2` · 워커 `cl3` · 14:54 착수 → 종료 시각 19:54 지정.
> **WP 셋, 커밋 넷.** 브랜치 `r48-wp-a`(사용자 지정 · 바꾸지 않았다).
>
> ## 왜 이 넷을 골랐나
>
> 「다음에 집을 것」의 ★D~★K 중 **★I·★J 는 R49 가 사용자에게 물어 두고 답이 없어
> 손대지 않았고**, **★D·★E 는 판정이 섞여 있어 열어 두었다** — ★D 는 `NWAs`·`CP` 를
> 자원에 배선하는 일인데 유형 `E` 배타 때문에 **그 ESS 가 자가소비·피크저감을 못 하게
> 된다**(사업 판단이다). 남은 넷은 **판정이 이미 있고 배선만 없던 것**이라 골랐다.
>
> ## WP 표
>
> | WP | 무엇 | 커밋 | 결론축 |
> |---|---|---|---|
> | **1** ★H | `FR-401-AC2.TouArbitrage` 신설 — spec v0.22 | `f961fda` | 불변 |
> | **2a** ★F | `Payer.POWER_MARKET` 신설 (§16.2 계약 변경) | `23b94c7` | 불변 |
> | **2b** ★K | `test_shaped_run_invariants.py` 로 이름 정정 | `e47cc96` | 불변 |
> | **3** ★G | PV 출력 배분 우선순위 **명문화 + 검사** | `44cf1d0` | 불변 |
>
> ## WP-1 — `TOU_ARBITRAGE` 가 **받을 항목이 없어** 편익을 하나도 못 내고 있었다
>
> R48 이 「계통에 파는 운전인데 자가소비 편익이 붙는」 오매핑을 뗐고 **그 자리를 비워
> 두었다.** `ESS.value_streams()` 가 그 모드에서 **빈 튜플**을 돌려주고,
> `tou_arbitrage_benefit()`(`RC-ESS-B1`)은 **호출자가 단위시험 하나뿐**이었다.
>
> - `TouArbitrage` = 연간 방전 kWh × 피크단가 − 연간 충전 kWh × 경부하단가.
>   **`PeakShaving` 을 본떴다**(`scales_with_dispatch_window = False`) — 창에서 읽으면
>   러너의 `_annualise()` 가 365를 곱한다(집합 PPA 가 실제로 밟은 자리다)
> - **`core.valuestream` 이 `core.der` 를 부르지 않는다**(`NFR-208-AC1`). 그래서 산식이
>   두 곳에 서는데, **시험이 두 값을 원 단위로 대조한다**(324,444원)
> - 배타 셋 — `SurplusSale` 과 **유형 A**(잉여판매는 시스템 총 역송량이라 같은 kWh 가
>   두 항목에 각각 판매로 들어간다) · `NWAs`·`CP` 와 **유형 E**.
>   ⚠ `SelfConsumption`·`PeakShaving` 과는 **배타가 아니다** — 셋 다 운전 주체가
>   사업자이고 `mode_weights` 가 혼합을 명시로 허용한다(`TOU 0.5 + PEAK 0.2`)
> - ⚠ **「넷을 다 적는다」로 세던 문장 셋이 거짓이 됐다** — 사업자 운전 편익이 셋이
>   되어 운전 주체 축은 **2 × 3 = 6쌍**이다. yaml 주석 · `test_phase1_dod.py` ·
>   spec `AC2.E` 를 함께 고쳤다
> - **러너에 배선하지 않았다** — 세우면 유형 A 가 `SurplusSale` 과 부딪혀
>   「잉여판매 수량에서 ESS 방전분을 뗄 것인가」라는 **새 판정**이 필요하다
> - ★ 워커가 §4 목록 **밖의 검사 넷**을 고쳤고 그것이 옳았다 —
>   `test_exclusion_rules_contract`(규칙 총수 11→14) · `valuestream_probes`(탐침) ·
>   `test_phase1_dod`(`_create_valuestream_for_tag`) · `test_grid_dispatch_benefits`.
>   **넷 다 「편익·규칙을 늘리면 나를 함께 고쳐라」를 자기 문면에 적어 둔 래칫**이다.
>   ⚠ 고치기 전 실측이 **9 failed** 였다 — 지시문의 「바꿔도 되는 파일」이 모자랐던 것
>
> 실측: 수용기준 **324 → 325** · 자동 매핑 **293 → 294** · Must-have 미매핑 **9건 그대로**
> (Phase 1 은 0건 — 조항과 구현이 같은 라운드에 섰다) · 배타 규칙 **11 → 14**(유형 E **4 → 6**).
>
> ## WP-2a — 용량정산금이 **배전사업자 지갑에서 나가는 것으로** 잡혀 있었다
>
> 계통 쪽 `Payer` 값이 `GRID_OPERATOR`(「배전사업자」) 하나뿐이라 `NWAs`·`CP` 가 그 값을
> 함께 썼다. `capacity_payment.py` 는 *「이 값은 근사이고 열거를 늘리는 것은 계약 변경이라
> 여기서 하지 않는다」* 고 **적어 두고 값은 틀린 채로** 두었다 — 그 값은 리포트의
> 「지불 주체」 칸에 **그대로 인쇄되고** 관점별 귀속(`FR-704`)이 그것을 따른다.
>
> §16.2 절차대로 `Payer.POWER_MARKET = "전력시장"` 을 세우고 **`CapacityPayment` 만**
> 옮겼다. `NWAs` 는 배전망 증설 회피가 근거라 그대로다.
> ★ **「둘이 같지 않다」를 직접 재는 계약 테스트**를 세웠다 — 편익마다 자기 값만 재면
> 누군가 둘을 다시 한 값으로 뭉쳐도 **각각을 고치는 순간 둘 다 초록불**이 된다.
> 열거 이름이 아니라 **인쇄되는 값**이 다른지도 함께 잰다.
> ⚠ spec 은 건드리지 않았다 — 조항은 「지불 주체가 특정되어야 한다」(`DV-13`·`AC5`)만
> 요구하고 **열거 값을 열거하지 않는다**(오케스트레이터가 따로 확인했다).
>
> ## WP-2b — 검사 파일 이름이 **없어진 표를 가리키고** 있었다
>
> `test_assumed_operation.py` 는 붙임 7 **둘째 표**를 재던 자리인데 R49 가 그 표를 지웠다.
> `git mv` 로 `test_shaped_run_invariants.py` 가 됐고(`rename … 100%`), 경로를 인용하던
> **둘**(`e2e_runner.py` 의 `__all__` 주석 · `test_irradiance_wired.py` 머리말)을 함께
> 고쳤다. `status-history.md` 의 것은 **이력이라 두었다.** 함수·클래스 이름은 안 바꿨다.
> 검사 파일이 움직였으므로 `gen_traceability.py` 를 다시 돌렸다 — **로컬 `pytest` 는 못
> 잡고 CI 만 잡는다.**
>
> ## WP-3 — **배터리가 가구보다 먼저 태양광을 가져간다**
>
> `PV` 도 `ESS` 도 가구 부하를 보지 않으므로(`NFR-208-AC1`) **둘을 이어 붙이는 러너가
> 배분 순서를 만드는 유일한 임자**인데, 그 자리는 잉여를 *계산*만 하는 것처럼 보였다.
> `_resolve_ess_dispatch_inputs` 독스트링에 ①자가소비 → ②ESS 충전 → ③역송, 가구의
> 초과 수요는 계통 수전임을 **정본으로** 세웠다(`pv.py`·`ess.py` 는 한 줄로 가리키기만).
>
> ★ **동어반복이 아닌 검사를 세웠다** — 가구 부하를 **0 과 10호분(36,000kWh/년)** 으로
> 바꿔 돌리면
>
> | 가구 부하 | ESS 연간 충전 | 계통 수전 | 계통 역송 |
> |---:|---:|---:|---:|
> | 0 | **3,244.4444kWh** | 0kWh | 3,617.5556kWh |
> | 36,000kWh | **3,244.4444kWh** | **32,382.4444kWh** | 0kWh |
>
> **충전이 소수점까지 같고 수전만 는다** — 순서가 동작으로 드러난다. ⚠ 부하를 PV 연간
> 발전량보다 크게 잡아 **다투게 만들어야** 한다(잉여가 넉넉하면 「다투지 않아서 같았다」로도 통과한다).
>
> ★★ **조항은 이 순서를 이미 말하고 있었다** — `FR-302-AC1` 의 7단계 우선순위이고
> `DEFAULT_RULE_ORDER` 가 같은 일곱을 이름으로 든다. **그래서 spec 을 건드리지 않았다**
> (오케스트레이터의 WP 지시문은 「조항이 말하지 않을 것」으로 보았고 **워커가 그것을
> 뒤집었다** — 지시문보다 실물이 이겼다).
> ⚠⚠ **어긋나는 것은 순서가 아니라 ①의 뜻이다** — 조항의 ①은 「**즉시** 자가소비」(그
> 스텝의 실제 부하)인데 구현의 ①은 **연간 고정 비율**이고, 배포 경로에서
> `PV_SELF_CONSUMPTION_RATIO = 0.0` 이라 **가구 몫이 한 kWh 도 없다.** 그 물음을 열었다.
>
> ## 게이트 실측 — **오케스트레이터가 WP 마다 다시 돌렸다**
>
> | | 착수(R49 종료) | R50 종료 |
> |---|---|---|
> | `pytest` | 1,790 passed / 3 skipped / 1 xfailed / **0 failed** | **1,815 passed** / 3 / 1 / **0 failed** |
> | 수용기준 · 자동 매핑 | 324 · 293 | **325 · 294** |
> | Must-have 미매핑 (Phase 1) | 9건 (0건) | **9건 (0건)** |
> | 배타 규칙 (유형 E) | 11 (4) | **14 (6)** |
> | `mypy` | 무결 | **무결 122파일** |
> | `lint-imports` | 4 kept 0 broken | **4 kept 0 broken** |
> | `ruff` · `check_file_size --code-strict` | rc=0 | **rc=0** |
> | 소스규칙 10종 · `check_task_mapping` | rc=0 | **rc=0** |
> | `check_test_accompaniment --base 80f0906` | — | **rc=0** (구현 변경 6건 전건 동반) |
> | 골든 3건 · `docs/evidence/` | — | **무변경** |
>
> ⚠ `check_coverage_inputs` 는 `coverage.xml` 이 로컬에 없어 **rc=2** 이고 그것이 옳다 —
> 「스캔이 안 돌았다」와 「깨끗하다」를 같게 읽지 않는다(`check_dependency_audit` 과 같은 형태).
>
> ★★ **음성 스위트를 돌렸다 — R49 가 시간이 없어 건너뛴 자리다.** `scripts/negtest_*.py`
> **14종 전건 rc=0** 이고 **변이가 하나도 남지 않았다**(끝나고 `git status --short` 가
> `status` 두 파일뿐). ⚠ **HANDOFF 는 「12종」이라 적었으나 실물은 14종이다** — 세어라.
> ⚠ 이것은 **소스를 변이시키므로 워커가 멈춘 깨끗한 트리에서만** 돌릴 수 있다.
> 중단하면 `finally` 가 건너뛰어져 **실물 소스에 변이가 남는다**(R34 가 밟았다).
>
> ## 이 라운드가 밟은 것 — **다음 라운드가 되풀이하지 않았으면 하는 것**
>
> - ★ **「바꿔도 되는 파일」 목록을 판정에서 유도하라.** WP-1 의 판정 ⓒ(배타 규칙 셋
>   신설)는 **총수를 못 박은 래칫 넷을 필연적으로 빨간불로 만든다.** 지시문이 그 넷을
>   빠뜨려 워커가 9 failed 를 만나고서야 알았다. 규칙·편익을 **늘리는** WP 에서는
>   *「이 수를 세는 자리가 어디인가」* 를 먼저 `grep` 해서 목록에 넣어라
> - ★ **지시문의 전제가 틀릴 수 있다는 것을 지시문에 적어라.** WP-3 의 ⓔ 는 「조항이
>   말하지 않으면 새로 적어라」였는데 **조항이 이미 말하고 있었다.** 「먼저 세고
>   판단하라 · 이미 있으면 줄 번호로 보고하라」를 넣어 둔 덕에 워커가 **spec 을 헛되이
>   부풀리지 않았다**
> - ★ **게이트를 배경 `pytest` 와 겹쳐 돌리지 마라.** `check_hardcoded_params` 가 한 번
>   **rc=1** 을 냈는데 단독으로 돌리니 작업본·`HEAD` 둘 다 rc=0 이었다 — 겹친 탓이다.
>   **빨간불을 보면 먼저 단독으로 다시 돌려라**
> - ★ `git stash` 로 `HEAD` 와 대조하면 **`git mv` 의 이름 변경 스테이징이 풀린다**
>   (`R` → `D`+`A`). 대조 뒤 `git add -A` 로 되살려 `rename … 100%` 를 눈으로 확인했다.
>   ⚠ 워커는 대신 **`git worktree add <스크래치> HEAD --detach`** 를 썼고 그쪽이 낫다
>
> ## 커밋 (넷 · 브랜치 `r48-wp-a`)
>
> ```
> f961fda  ★H  TouArbitrage 신설 · spec v0.22            15 files  +886 −46
> 23b94c7  ★F  Payer.POWER_MARKET (§16.2)                 3 files   +52 −10
> e47cc96  ★K  test_shaped_run_invariants.py (rename)     4 files    +4  −4
> 44cf1d0  ★G  PV 배분 우선순위 명문화 + 검사             4 files  +209
> ```

---

> ### ⬇ **R49 블록**(2026-08-31). 현재 상태가 아니다 — 정본은 `status.md` 다.

> # ⏹ 2026-08-31 **R49 — R48 이 연 세 자리를 닫았다. 결론축은 움직이지 않았다**
>
> **재료 정본 `docs/decisions-2026-08-31-R49.md`**(2026-08-31 사용자 판정 · 잠정 아님).
> R48 이 결론축을 두 배로 내리면서 보고서 안에 **말이 안 맞게 된 자리 셋**이 생겼고,
> 셋 다 *「무엇이 옳은가」* 가 아니라 **「무엇을 보여 줄 것인가」** 라 사람 판정이 필요했다.
> R49 는 그 판정을 구현했다 — **셋 다 보여 주는 방식만 바꾼다.**
>
> ## 결론축 — **움직이지 않았다. 그것이 이 라운드의 합격 조건이었다**
>
> ```
> 무보조 npv           −12,956,180원      (R48 종료와 동일)
> 결손/총사업비          132.2%            (동일)
> 매니페스트             79dc9d417e44d247  (동일 — 입력을 하나도 안 건드렸다는 측정)
> 전액 지원 시 잔여 결손  −3,156,180원      ★ R49 가 처음 세운 수
> ```
>
> ⚠ 리포트 **문면**은 크게 바뀌었으나 **수는 그대로다.** 움직였으면 그것이 결함이었다.
>
> ## WP 표 — 여섯 중 다섯이 구현, 하나가 마감
>
> | WP | 무엇 | 커밋 | 결과 |
> |---|---|---|---|
> | **1** | ★A 붙임 7 **둘째 표 삭제** + 붙임 8 자가소비 사유 **둘로 가르기** | `b26db28` | 빨간불 3 → 0 |
> | **2** | ★B **「전액 지원해도 안 된다」** + 잔여 결손 (교정 1회) | `5f0c3ea` | 빨간불 3 → 0 |
> | **3** | ★C ⓐ 새 절 **5.3 「적자는 어디서 오는가」** | `65917c4` | 합계 = 결론축 ✔ |
> | **4** | ★C ⓑ 5.1 **「얼마나 줄여 주나」** — ⚠ **축은 더하지 않았다** | `e414a4e` | 매니페스트 불변 |
> | **5** | ★C ⓒ + 남은 검사 전제 교정 | (아래) | 빨간불 3 → 0 |
> | **6** | 마감 — 리포트 재산출 · 전건 게이트 · `main` 병합 · CI | (아래) | |
>
> ## WP 별로 **무엇이 어긋나 있었나**
>
> ### WP-1 — 붙임 7 이 **같은 표를 두 번** 싣고 있었다
>
> R48 이 본 실행에 가구 부하를 세우면서 두 표(파이프라인 운전 · 형상을 가정한 운전)가
> **완전히 같아졌다.** 그런데 리포트는 *「위 표와의 차이 — **가구 부하 하나**다」* 를
> 계속 인쇄했다 — 두 표가 같으므로 **거짓 문장**이었다.
>
> - `_assumed_operation` · `AssumedOperationBasis` · `assumed_hours`/`assumed_basis` ·
>   `_assumed_lines` 를 걷어냈다. `LOAD_LEDGER_KEY` 도 이 호출에만 쓰였다
> - ★ **둘째 표 아래에만 있던 사실 둘**(「이 표 위에 프로포마·결론이 선다」·「스택 차트가
>   없는 이유」)을 **첫 표 꼬리로 옮겼다** — 표만 지우면 리포트에서 통째로 사라진다
> - 붙임 8 「자가소비 편익」의 항이 **셋 → 둘**. 종전 셋은 **두 운전의 델타**로 지어져
>   있었는데 그 델타가 0 이 됐다(판매단가를 두 배로 올려도 순액이 안 움직였다).
>   델타 대신 **반사실**을 명시한다 — 순액 = 자가소비량 × (구매단가 − 판매단가)
> - `_AssumedQuantities` → `_MeasuredQuantities`. **이름이 거짓말을 하면** 다음 사람이
>   그 수를 결론과 무관한 곁가지로 읽는다
> - 검사 **지움 3 · 전제 교체 4 · 문면 3.** 지운 셋 중 하나는 **자기 독스트링에 지울
>   조건을 적어 두었다**(*「읽기를 한 번으로 줄이면 이 갈래는 아예 없어진다」*)
>
> ### WP-2 — 리포트가 **달성 불가능한 조건을 달성 조건으로** 제시했다
>
> 「결론 전환 지원율 **132.2%**」는 **사업비보다 많은 돈을 주라는 뜻**이고, 그 값으로
> 재실행하면 `DV-1`(자부담 음수)이 거부한다. `판정요구-이중계상-2026-08-29.md` 가
> 갈래 丙 에서 **예고한 자리에 실제로 도달**한 것이다.
>
> - `MAX_SUBSIDY_RATE`(= 1.0) · 전액 지원 시 잔여 결손 함수 · `support_alone_can_flip`
> - ⚠ `break_even_subsidy_rate` **자체는 고치지 않았다** — 그 환산은 여전히 옳다.
>   바뀌는 것은 *그 값을 답으로 제시할 수 있는가* 다
> - 네 자리(1. 요약 · 5.1 · 6.2 · 붙임 3)가 같은 진술을 싣는다. **잔여 결손 −3,156,180원은
>   두 시나리오에서 한 값**이다(지원이 `t=0` 감액이므로) — 그 대조를 검사로 세웠다
> - ★★ **교정 1회 — 검수에서 잡았다.** 첫 판은 요약·6.2 의 값 칸이 여전히 굵은
>   `132.2%` 로 **시작**했다. *「제시하지 않는다」* 가 아니라 *「제시한 뒤 단서를 붙인」*
>   형태였고, 이 저장소는 **발췌 인용을 이미 한 번 겪었다**(R43-G). 값 칸에서 백분율을
>   빼고, **요약이 백분율을 답으로 내세우지 않는지 문면으로 붙드는 검사**를 세웠다
> - 붙임 3 에 「전액 지원 시 잔여 결손」 산식을 세웠다 — *「얼마나 모자라는가」* 가
>   **감사 가능해야** 하기 때문이다
>
> ### WP-3 — 분해를 **1년차 값으로 지으면 469,314원이 어긋난다**
>
> ★★ 이것이 R49 에서 가장 값싸게 틀릴 뻔한 자리였다. `CaseBasis` 의 1년차 값
> (편익 342,060 · 운영비 489,388)을 20년 등액으로 놓고 할인해 더하면 **−12,486,866원**
> 이 나온다. 실제 결손은 −12,956,180원 — **`PRICE_ESCALATION_RATE` 가 O&M·교체비 행을
> 해마다 올리기 때문**이다. 오케스트레이터가 지시문을 쓰기 전에 직접 재서 잡았다.
>
> - 그래서 러너가 **이미 갈라 갖고 있는** 편익·운영비·수명주기 행을 **갈린 채로**
>   `CaseOutcome` 이 나른다. ⚠ 표시 층이 **태그 문자열로 되짚게 두지 않았다** —
>   태그를 다듬는 순간 한 항이 조용히 0 이 된다
> - 5.3: 초기투자 −9,800,000 · 운영 단계 20년 누적 −2,385,746 · 교체·잔존 순액 −770,434
>   → **합계 −12,956,180원 = 결론축**. 속항(편익 누적 · 계통 전력 구매 · 고정 O&M ·
>   교체비 · 잔존가치)은 들여쓴 행
> - ★ **더해지는 항과 속항을 눈으로 구별되게** 세웠다 — 판정이 든 넷을 다 더하면
>   편익이 두 번 세어져 합계가 거짓이 된다
> - ★ **합계가 결론축과 다르면 표를 인쇄하지 않고 터뜨린다**
> - ⚠ **수를 문장으로 박지 않았다** — 「가장 큰 항」 문장은 실린 항목에서 지어진다
> - `render_markdown` 이 *「양식 문서를 고치지 않고 여기만 고치면 「양식대로 썼다」가
>   성립하지 않는다」* 를 명시하므로 **양식 문서를 함께 고쳤다**
>
> ### WP-4 — **판정 §3 ⓑ 의 전제가 이미 낡아 있었다**
>
> 판정은 *「지금 흔들어 보는 축이 설비단가 몇 개뿐」* 이라 적었으나, 재어 보니 5.1 은
> 이미 **아홉 축**을 싣고 판정이 든 최소 후보 셋이 **전부 있었다**:
> 판매단가 `surplus_sale_price`(R35) · 가구 사용량 `household_load_annual_kwh`(R48/WP-B) ·
> 설비 크기 `_DESIGN_VARS`(4.4 · 붙임 10).
>
> - ★ **그래서 축을 하나도 더하지 않았다.** 더하면 같은 축이 둘이 되어 대장 항목이
>   중복된다. 그 사실이 문장이 아니라 **매니페스트 해시가 착수와 같다**는 측정으로 선다
> - 본체는 **표가 답을 갖고도 말하지 않던 것**이었다 — 인자마다 「그 끝까지 밀면 결손이
>   얼마 줄어드나」를 원 단위로, 줄여 주는 크기 순으로. 종전에는 「남은 거리」만 있어
>   *얼마를 벌어 주었나*가 **독자의 뺄셈으로** 남았다(붙임 8 이 R43-H 에 고친 것과 같은 형태)
> - ★ **아홉을 각각 좋은 끝까지 민 단순 합 6,447,843원 · 그래도 남는 결손 6,508,337원**
>   (결손의 50.2%). **이것이 「왜 전환 인자가 없는가」의 답이다** — 하나씩 밀어서는
>   절반도 못 메운다
> - ⚠ **축이 없는 항 하나를 찾아 올렸다** — 고정 O&M(`PV_FIXED_OM_WON_PER_YEAR` ·
>   `ESS_FIXED_OM_WON_PER_YEAR`, 각 100,000원/년)은 **소스 상수이고 대장 항목이 아니다.**
>   둘을 합치면 −3,070,898원으로 분해 3위 급이나, 대장에 세우려면 출처·신뢰도·기준연도가
>   필요하다 — **R49 판정 범위 밖이라 손대지 않았다**
>
> ### WP-5 — 검사 셋이 **사업의 형편에 매여** 있었다
>
> 전환 인자는 *결론축이 0 선을 넘느냐*이므로 **사업이 나쁘면 0건**이 된다.
> 그래서 종전 오라클(집합 대조 · `assert flipping` · 결합 회수 행)은 **사업이 나빠지면
> 함께 죽는다.** 재는 대상을 **사업의 형편과 무관한 것**으로 옮겼다:
>
> - **두 끝의 결론 축** — 사업이 아무리 나빠도 **9건 × 2 = 18곳**이 있다. 견주는 근거는
>   리포트가 아니라 문장 하나다: *「지원 1원은 결론 축을 정확히 1원 올린다」*. 같은
>   인자·같은 끝에서 두 시나리오의 차는 **보조율 × 그 끝의 총사업비**여야 한다
>   (실측 7,840,000원 = 0.8 × 9,800,000 · 설비단가 축은 끝마다 갈린다)
> - ⚠ **기대값을 리포트에서 읽지 않는다**(동어반복 금지) — 보조율은 시나리오 yaml,
>   총사업비는 **지원 없이 파이프라인을 다시 돌려** 짓는다. 리포트의 어느 칸도 안 읽는다
> - **`test_no_flip_is_explained.py`**(㉡) — 판정 §3 ⓒ 의 **배치**를 붙든다:
>   「없음」 · 그 옆 5.3 · 요약이 5.3 을 가리킨다
> - ⚠ **조용한 초록불을 만들지 않았다.** 「0건이면 통과」로 바꾸면 순회가 0회 돈다 —
>   그래서 ㉠ 보고된 수 전부를 재실행과 견주고, ㉡ 「없음」이 실물인지 **중점까지** 넣어
>   확인하며, ㉢ **탐침의 상태 자체**를 재는 검사를 따로 세웠다
> - 🚫 **탐침 값을 밀지 않았다**(판정 §3 이 명시로 금지)
> - ⚠ **3순위였던 `test_assumed_operation.py` 이름 바꾸기는 시간이 없어 건너뛰었다** —
>   파일이 재던 둘째 표는 사라졌고 **이름이 내용과 어긋난 채로 남아 있다**(머리말에는
>   지금 무엇을 재는지 적혀 있다). 후보 이름 `test_shaped_run_invariants.py` ·
>   ⚠ `test_irradiance_wired.py:9` 가 그 경로를 인용하므로 **함께 움직여야 하고**,
>   검사 파일이 움직이면 **`gen_traceability.py` 를 다시 돌려야 한다**
>
>
> ## 자리 운영 — **워커를 한 번 교체했다**
>
> - 오케스트레이터 `cl3` · 워커 `cl2`(사용자 지정). 취임 시 `auc plan` 으로 배정을
>   검증하고 선임 `cl5` 를 `/clear` 했다(`Messages: 137 tokens` 로 확인)
> - ⚠ **WP-5 를 내보낸 직후 `cl2` 가 5시간 창을 소진했다**(잔여 0% · 리셋 00:40 —
>   **라운드 종료 시각 00:31 뒤**). WP-5 는 **아직 시작 전이라 잃은 작업이 없었다**
>   (`git status` 깨끗 · `result_5.md` 없음)
> - **워커를 `cl4` 로 교체했다.** `cl1` 은 오케스트레이터 후임 자리로 비워 두었다 —
>   한 에이전트에 두 자리를 겹치면 이관할 곳이 없어진다
> - ★ **교체 비용이 거의 없었다.** WP 지시문이 파일에 있고 워커 컨텍스트에 아무것도
>   두지 않았기 때문이다 — orch-dev 4절의 「중단 가능한 단위」가 실제로 값을 한 자리다
>
> ## 새로 밟은 함정
>
> - ⚠⚠ **분해·집계를 「1년차 값 × 기간」으로 지으면 물가 상승분만큼 조용히 어긋난다.**
>   R49 에서 469,314원(결손의 3.6%). **합이 지표와 맞는지 반드시 대조하라** —
>   맞지 않으면 표를 인쇄하지 말고 터뜨리는 것이 옳다
> - ⚠ **「그 수를 제시하지 않는다」는 「그 수 뒤에 단서를 붙인다」가 아니다.** 굵은 수는
>   그 칸만 떼어 인용된다 — 값 칸의 **첫머리**가 무엇인지를 보라
> - ⚠ **`tests/app/test_reports_router.py::test_json_export_is_parseable_by_a_standard_parser`
>   가 한 번 흔들렸다**(전건 실행에서 빨간불 → 같은 트리에서 격리 실행·재실행 모두
>   초록불). **재현되지 않았다.** 다시 나오면 격리 실행으로 확인할 것
>
> ## 게이트 실측 — **마감 시점에 전건 다시 돌렸다**
>
> ```
> pytest (3.13)                    1,790 passed · 3 skipped · 1 xfailed · 0 failed   rc=0
> pytest (3.11 — CI 판)             (아래 표)
> ruff check core tests             All checks passed                                rc=0
> mypy                              Success: no issues found in 121 source files     rc=0
> lint-imports                      Contracts: 4 kept, 0 broken                      rc=0
> gen_traceability.py               rc=1 (미매핑 — 정상) · traceability.md 변화 없음
> check_task_mapping.py             rc=0   ← CI 차단
> check_test_accompaniment --base 5f31b95   통과 — 구현 변경 7건 전건 동반          rc=0
> check_marker_substance · check_hardcoded_params · check_source_rules
> check_partition_assignment · check_disclosure · check_assumptions
> check_precommit_installed · check_unread_extension_points
> check_docstring_references · check_file_size --code-strict                 전부 rc=0
> ```
>
> ⚠ **음성 스위트 12종은 돌리지 않았다** — 종료 시각이 가까웠고, 그 스위트는 **실물
> 소스를 변이시키며 중단하면 변이가 남는다.** R49 는 소스보다 **리포트 문면과 검사
> 전제**를 주로 고쳤고 그 갈래는 위 게이트가 덮는다. **다음 라운드가 한 번 돌릴 자리다.**
>
> ## 리포트 재산출 — **한 번만, 맨 마지막에**
>
> 골든 세 시나리오 전부 rc=0 으로 다시 났고, 정본
> `docs/evidence/MC-1-검토용-리포트-2026-08-31.md` 를 **R49 판으로 덮었다**
> (같은 날짜의 최신 상태 · R48 판은 git 이 갖는다). 문면 52줄 추가 · 68줄 삭제.
> ★ **실행 매니페스트 `79dc9d417e44d247` 는 그대로다** — 입력을 하나도 안 건드렸다는 뜻이다.

---

> ### ⬇ **R48 블록**(2026-08-31). ⚠ **현재 상태가 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-08-31 **R48 — ESS 「운영방식 축」을 신설했다. 결론축이 −6,289,675 → −12,956,180원**
>
> `.orch/R48/` 에 지시문(`WP-*.md`)과 산출물(`result_*.md`)이 있으나 그 디렉터리는
> `.gitignore` 안이다 — **이 블록이 정본이다.**
> 오케스트레이터 **`cl5`**(착수는 `cl2`), 워커 **`cl1`·`cl2`·`cl3`** 을 **worktree 셋**
> (원 트리 · `_wt_E` · `_wt_D`)으로 갈라 **병렬**로 돌렸다. 사용자가 종료 시각
> **12:24 KST** 를 지정했고 **후임 오케스트레이터가 `/goal` 을 자기 세션에 걸었다**
> (R47 이 「자기에게 슬래시 명령을 못 건다」고 적었던 자리 — **이관 뒤 후임이 걸면 된다**).
>
> ## ★ 재료 정본은 `docs/decisions-2026-08-31-R48.md` 하나다
>
> **2026-08-31 세션에서 사용자가 직접 내린 도메인 판정**이며 **잠정이 아니다.**
> ⚠ **라운드 도중 `§11` 이 추가로 섰다**(아래 「사용자 판정이 둘이었다」).
>
> ## WP 표 — **열**
>
> | WP | 무엇이 어긋나 있었나 | 커밋 |
> |---|---|---|
> | **A** | `e2e_runner.py:470` 이 `PEAK_SHAVING` 을 **코드에 박아** 배터리가 **심야에 계통에서 사서 낮에 파는 운전**을 했다. 「충전원」 축이 저장소에 **없었고**, `reducible_peak_kw()` 가 **부하를 인자로 받지 않았으며**, `value_streams()` 가 **TOU 차익거래를 자가소비로 매핑**했다 | `e6b106e` |
> | **A-fix** | §1 의 「넘으면 거부」를 그대로 걸자 **기준 구성이 아예 돌지 않았다**(160건 빨간불). 사용자가 **운전 규칙(§11)** 으로 답했다 | `1ed9c11` |
> | **B** | 대장에 3,600kWh/호·년이 **근거 칸까지 채워져 있고** 형상 자산도 있고 러너는 부하를 세울 줄 아는데 — **`build_case_report()` 가 `annual_load_kwh` 를 넘기지 않았다** | `b119a34` |
> | **C** | 계통 방전분을 받을 편익 항목이 없어 그 몫이 **태양광 잉여판매 행에 얹혀** 있었다. `NWAs`·`CP` 신설 + 유형 `E` 배타 규칙 넷 | `0e77ec0` |
> | **D1** | `policy_warnings()` 가 **`EV_V2G` 한 자원에만** 있어 리포트가 `isinstance` 분기를 쳐야 했다. 배타 유형 넷으로는 **적을 수 없는 배타**가 있었다 (spec §16.2 · **계약 변경 둘을 한 커밋으로**) | `44b0158` |
> | **D2** | 훅은 계약에 서 있는데 **리포트로 나가는 통로가 0곳**이었다 (`FR-404-AC1` — 「미해결」이 *「가장 무겁다」* 고 적어 둔 자리) | `727186e` |
> | **E** | 붙임 1 이 `provider.items()`(기준값)를 읽어 **덮어쓴 값을 몰랐고**, 본문 2.1 은 편익을 **이름으로만** 말했다 | `30959b7` |
> | **E2** | 붙임 4 「편익 갈래」가 잉여 판매의 임자를 **`PV` 단독**으로 적었다 — **한 리포트가 한 편익의 임자를 두 가지로 말했다** | `60f1704` |
> | **G** | 조항이 **운전 방법만** 선언하게 하고 「어디서 충전하는가」를 묻지 않았다 (spec v0.20 → **v0.21** · 수용기준 321 → **324** · task v2.8 → v2.9) | `77652e4` |
> | **H** | 이중계상 판정 요구가 **미결로 남아 있었다** — 갈래 셋(甲·乙·丙)은 채택되지 않았다 | `d0ed09d` |
> | **F** | 골든 3종 · `MC-1` 리포트 · 인용 수치 **한 단위 재산출**(§8-d) | `d26bb6e` |
>
> 병합 커밋 `a47acf9`(D1→E 레인) · `22e386b`(E 레인→A 레인) · D2 병합.
> **`WP-Fprep`(조사 전용)은 소스를 내지 않았다** — 산출물은 `.orch/R48/result_Fprep.md`.
>
> ## ★★ 사용자 판정이 **둘**이었다 — 라운드 도중에 하나가 더 섰다
>
> **① 착수 판정**(§0~§10) — *「ESS 가 어떻게 운영되는지를 먼저 규정하고, 편익을 그
> 운영방식에서 산정한다」*. 이중계상 갈래 **셋을 전부 대체한다.**
>
> **② 라운드 도중 판정**(§11) — WP-A 가 §1 을 구현하자 **기준 구성이 성립하지 않았다.**
> 실측: PV 3kW 하루 발전 **10.80kWh** vs ESS 10kWh·365사이클 하루 충전 필요량
> **8.89kWh**, 그런데 자가소비 우선 모드 충전창(10~15시)에 드는 발전은 **6.50kWh** 뿐.
> 사용자 문면 — *「자가소비량을 초과하는 태양광발전량은 ESS 충전되고 저녁(또는 피크)
> 시간에 방전한다. **설비 용량이 부족하면 설비 용량만큼 동작**한다」*.
> 그래서 ⓐ 충전창이 **고정이 아니게** 되고(잉여가 나는 모든 시각 · 방전창 제외)
> ⓑ `cycles_per_year` 가 `PV_SURPLUS` 에서 **입력이 아니라 상한**이 됐다.
> ⚠ **「잘라내기」가 아니다** — 애초에 넘는 계획을 세우지 않는다. 못 받은 잉여는
> **잉여판매로 남는다.**
>
> ## ★★★ §9 가 예고한 네 갈래 중 **실제로 작동한 것은 하나뿐이었다**
>
> | 갈래 | 실제 |
> |---|---|
> | ① 심야 계통충전 사라짐 | **작동 안 함** — 골든 실행은 이 라운드 전에도 이미 계약 기본값이었다 |
> | ② 피크저감 자가부하 기준 | **작동함** — 199,680 → **74,880원/년**(대표일 부하 피크 0.75kW 로 캡) |
> | ③ 자가소비 절감 신설 | **작동 안 함** — 무구조 실행에 `SelfConsumption` 스트림이 배선돼 있지 않다. 자가소비는 **수량 배분으로만** 반영된다 |
> | ④ NWAs·CP 신설 | **작동 안 함** — 기본 비활성이고 어느 자원의 `value_streams()` 에도 배선되지 않았다 |
>
> **이동의 대부분은 §9 가 이름 붙이지 않은 다섯째 통로에서 왔다** —
> **가구 부하 자신의 계통구매 비용**이 처음 계상됐다. §9 는 *「심야 계통충전이
> 사라진다」*(ESS **자신의** 충전 비용)만 적었고, *「가구 부하 **자신의** 미충족분이
> 비용으로 새로 잡힌다」* 는 **별개 통로**이며 **방향이 반대(비용 추가)이고 규모가 훨씬
> 크다.** ⚠ A-fix 단독분과 B 단독분으로 원 단위 분해는 하지 않았다 — 필요하면
> `git show 1ed9c11` / `b119a34` 로 재구성한다.
>
> ## 게이트 실측 — 라운드 끝 (병합 트리 `r48-wp-a`)
>
> | | |
> |---|---|
> | `pytest` | **`9 failed, 1770 passed, 3 skipped, 1 xfailed`** ⚠ 9건은 전부 **판정 대기**(아래) |
> | `ruff`(`core tests scripts app infra web`) | `All checks passed!` |
> | `mypy` | `Success: no issues found in 119 source files` |
> | `lint-imports` | `Contracts: 4 kept, 0 broken.` |
> | `gen_traceability` | 수용기준 **324** · 자동 **293** · Must-have 미매핑 **9**(전부 Phase 2·3) · **Phase 1 Must-have 미매핑 0** |
> | `check_task_mapping` · `check_marker_substance` · `check_source_rules` · `check_assumptions` · `check_partition_assignment` · `check_disclosure` · `check_status_round_blocks` · `check_file_size --code-strict` | 전부 rc=0 |
> | `check_unread_extension_points` | rc=0 — **D2 가 닫았다**(계약에 오른 훅을 읽는 배포 코드가 0곳이었다) |
> | `check_clause_partition` | 결손 9 → **8** (D2 가 `FR-404-AC1` 을 채웠다) |
>
> ## 이 라운드가 **닫은** 것
>
> - **이중계상 판정 요구** — 규칙이 아니라 **구조**가 막는다(자가 부하로 간 kWh 와
>   계통으로 나간 kWh 가 **수량 단계에서** 갈린다). 판정요구 문서 머리에 해소 표시 ·
>   `status-human.md` 1-A 행 취소선
> - **`FR-404-AC1`** — 리포트가 정책 가정 경고를 상단에 인쇄한다(호출자 0곳 → 배선)
> - **붙임 1 오버라이드**(§8-a) · **본문 2.1 편익 금액**(§8-b) · **붙임 4 임자 열**
> - **조항 셋** — `FR-105`(충전원 축) · `FR-401-AC2.NWAs`·`.CP` · `FR-402-AC2.E`
> - **§8-c(4.3 귀속)는 이미 닫혀 있었다** — R43-E2 가 했고 판정 문서의 관찰이 낡았다
>
> ## 새로 밟은 함정
>
> - ⚠⚠ **`pytest -q` 를 쓰면 `pyproject.toml` 의 `addopts` 와 합쳐 `-qq` 가 되고
>   결과 줄이 아예 인쇄되지 않는다.** rc 만 남아 「몇 건이 지나갔는가」를 못 읽는다.
>   워커 둘이 실물로 밟았다
> - ⚠ **`herdr agent get` 의 `working` 은 「일하는 중」이 아닐 수 있다** — 배경 셸이
>   남으면 워커가 끝나고도 `working` 으로 보인다. **완료 판정은 결과 파일로 한다**
> - ⚠ **pre-commit 의 `ruff` 훅은 `language: system`** 이라 PATH 앞의 인터프리터를
>   쓴다 — `~/miniconda3` 를 앞에 두지 않으면 `No module named ruff` 로 커밋이 막힌다
> - ⚠ **음성 스위트를 타임아웃으로 중단하면 변이가 실제로 남는다** — WP-F 가
>   `casevariant.py` 에서 겪었고 `git checkout --` 로 원복한 뒤 **중단 없이** 재실행했다
>
> ## ⚠ 이 라운드가 **연** 것 — 다음이 판정해야 한다 (자세한 것은 `status.md`)
>
> 빨간불 **9건**은 전부 *「왜 옳은지 판정할 수 없어」* 남긴 것이다(**하드코드 값을
> 판정 없이 덮어쓴 것은 0건**). 셋으로 갈린다 — 붙임 7·8 의 전제 붕괴 · 전환 지원율이
> (0,1) 구간을 벗어남 · 탐침 대장 재설정.

> # ⏹ 2026-08-31 **R47 — WP 둘을 닫았다(하나는 정찰, 하나는 기각). 결론축은 안 움직였다**
>
> `.orch/R47/` 에 지시문·산출물이 있으나 그 디렉터리는 `.gitignore` 안이다 — 이 블록이 정본이다.
> 오케스트레이터 **`cl2`**, 워커 **`agy`**. 사용자가 「5시간」을 지정했으나 `/goal` 은
> 걸리지 않았다 — **오케스트레이터가 자기 세션에 슬래시 명령을 실행할 수 없다.** 턴 단위로 돌았다.
>
> | WP | 무엇이 어긋나 있었나 | 커밋 |
> |---|---|---|
> | 1 | `_reacquisitions()` 헬퍼가 **어느 자원도 내지 않는 반환 모양**을 흡수했다 — R42 이래 `PV`·`ESS` 둘 다 부품별 튜플이고 프로덕션 호출자 **여섯 전부**가 그 전제로 순회한다 | `a332c4a` |
> | 2 | (정찰 · 소스 0줄) 부채 대장 둘이 왜 9건/7건인지 실측 | — |
> | 3 | 부채 대장이 **둘인데 갈려도 아무도 모르는 상태**였다 | `72c1e6d` |
> | 4 | **기각하고 되돌렸다** — 아래 ★★ | — |
>
> **★ WP-3 은 예상과 다른 답을 냈다.** 「하나로 모은다」가 답이 아니었다. 두 대장의
> 차이 둘(`NFR-201-M1`·`NFR-208-AC1` × `core.engine`)은 **낡음이 아니라 설계**다 —
> 시험은 *「경로 문면으로 적힌 그 둘은 **그 구획을 만지지 말라**는 조항이라, 인용이
> import 해야 한다고 물으면 **그물이 뒤집힌다**」* 며 사전에 경로 표기를 안 넣고,
> 검사기는 *「같은 구획의 두 표기」* 라 넣은 뒤 「사각지대」 사유로 흡수한다.
> **어느 쪽을 지워도 그 파일의 판단이 말없이 사라진다.** 그래서 합치지 않고
> `KNOWN_PARTITION_DESIGN_GAP` 으로 **의도된 차이를 명시**하고 메타 검사가 관계를
> 붙들게 했다. 오케스트레이터가 대장에서 한 건을 빼어 **실제로 잡는 것을 확인**했다.
>
> **★★ WP-4 는 오케스트레이터의 전제가 틀려서 기각했다.** 「7번 안분 잔차 — 실물로
> 검증된 적이 없다」를 *「시험이 없다」* 로 읽고 WP 를 냈으나 시험은 **이미 있었다**
> (`test_the_rounding_residual_goes_to_the_last_share_so_the_total_is_exact` 이
> `[33, 33, 34]` 과 `rows[RESIDUAL_HOLDER_INDEX].resource_name == "c"` 를 단언한다).
> 항목이 말한 것은 **「MC-1 케이스의 잔차가 0원이라 프로덕션 경로에서 그 규약이 한 번도
> 발동한 적이 없다」** 였다. ⚠ 게다가 워커가 「기존 시험을 고치지 마라」는 금지를 어기고
> 그 시험을 **다시 쓰면서 `RESIDUAL_HOLDER_INDEX` 를 이름으로 붙들던 단언을 지웠다** —
> 되돌렸고 원 시험 7건 전건 통과를 확인했다.
>
> **실측** — `test_lifecycle_wiring.py` 8건 · `test_clause_evidence_nets.py` 7건 ·
> `test_benefit_attribution.py` 7건 전건 통과 · `ruff` 0건 · `check_clause_partition.py` rc=0 ·
> CI `tests`·`source-rules` 둘 다 success(`72c1e6d`).
> ⚠ **전체 `pytest` 는 이 라운드에서 돌리지 않았다** — 워커에게도 대상을 좁혀 돌리게 했다(R46 함정).

> # ⏹ 2026-08-30 **R46 완료 — PV·ESS 에 `HeatPump` 식 「단가 0 → 재취득 안 함」
> 가드를 이식했다. 결론축은 안 움직였다.**
>
> **① 결론축 — 안 움직였다.** 무보조 `npv` **−6,289,675원** · 결손/총사업비 **64.2%**.
> 단가 0 인 부품의 잔존가치 기여는 원래 0 이므로 이 수정은 수치가 아니라
> **표시 정합성**만 고친다 — 리포트를 다시 뽑아 커밋본과 바이트 동일임을 확인했다.
>
> **② 이 라운드가 연 것 — 다음이 판정해야 한다**
>
> - `tests/casegrid/test_lifecycle_wiring.py:109-116` `_reacquisitions()` 헬퍼
>   독스트링이 *「PV 는 튜플·ESS 는 `{연차:취득가}` 하나」* 라고 아직 적고 있으나,
>   `ESS._acquisitions()` 는 이미 (R42 이래) `PV` 와 같은 부품별 튜플을 반환한다 —
>   옛 13번의 「반환 정렬 규약 불일치」 잔여 조각이다
>
> **③ 새로 밟은 함정**
>
> - ⚠⚠ **워커(`agy`)에게 전체 `pytest` 를 돌리게 하면, 지시한 파일 밖의 무관한
>   실패(캐싱·모듈 로딩 관련 `tests/contract/test_registry.py`)까지 스스로 판단해
>   고치려 든다.** 위임 파일 목록을 아무리 좁혀도 **검수 시 `git status --short` 로
>   지시 밖 변경이 있는지 반드시 대조**하고, 있으면 되돌리게 한 뒤 커밋해야 한다.
> - `agy` 도 저장소 루트에 임시 스크립트(`write_result.py` 류)를 남긴다 — 정리
>   지시로 해결되지만, **매 WP 뒤 `git status --short` 로 잡는 것**은 워커 종류를
>   가리지 않는다(기존 함정 목록에 이미 있던 규칙이 그대로 적용됨을 재확인).
>
> **④ 착수 자리** — 남은 목록은 아래 **「다음에 집을 것」 절**이 갖는다(같은 파일 안이다).
>
> **⑤ 푸시·CI** — `942c8b3` 로 밀었다. CI 결과는 `gh run list --limit 4` 로 확인한다.
>
> **⑥ 전문** — 이 라운드는 WP 하나(단가 0 가드 이식)뿐이라 별도 이력 블록을
> 두지 않는다. 지시문·산출물은 `.orch/R46/WP-1.md`·`result_1.md`(이 기계에만 있다).

> # ⏹ 2026-08-30 **R44 완료 — WP 여덟(8~15)을 닫았다. 결론축은 안 움직였다.**
>
> **`.orch/R44/` 는 `.gitignore` 안이라 다른 기계엔 없다 — 이 블록이 정본이다.**
>
> 라운드 도중 **워커가 바뀌었다**(최초 배정 `agy` 가 WP-8 중 할당량 소진 → `cl3`). 오케스트레이터는 `a1`(`cl1`).
> ⚠ **라운드가 WP 만 끝내고 닫히지 않은 채 세션이 끊겼고**, `a2` 가 확인해 `.orch/R45/HANDOFF.md` 를 남겼으며 **그 인계를 받아 이 블록이 섰다.**
>
> ### ★ 결론축은 안 움직였다
>
> R44 도 움직이지 않았다 — 리포트를 다시 뽑아 커밋본과 **바이트 동일**이었다.
>
> ### ✔ R44 의 커밋을 푸시했다
>
> `25ce9f7..394bdea` 로 밀었고 R44 의 WP 커밋 여덟 + 라운드를 닫는 문서 커밋 + CI 를 고치는 커밋이 함께 갔으며, CI 는 tests·source-rules 둘 다 success 다. (명령: `git log origin/main..HEAD --oneline | Measure-Object -Line`)
>
> ## ① WP-8~15 — 무엇이 어긋나 있었나
>
> | WP | 무엇이 어긋나 있었나 | 커밋 | 결론축 |
> |---|---|---|---|
> | 8 | 독스트링 참조 검사에만 **음성 짝이 없었다** — 그 검사가 잡는지 아무도 안 쟀다 | `5503b2f` | 무관 |
> | 9 | NFR-105 동반 검사가 **문면만 바뀐 `core/` 파일도 구현 변경으로 셌다** | `039c7b6` | 무관 |
> | 10 | 독스트링 검사가 **클래스 점 표기**를 정규식 문턱에서 통째로 빠뜨렸다(41건 편입 · 위반 0) | `ece6503` | 무관 |
> | 11 | 관점별 CAPEX 행 **넷이 빌더 밖**에서 지어져 부호 규약이 아무 데도 없었다 | `f7fee1c` | **안 움직임**(21케이스 동등성 실측) |
> | 12 | spec §13.2.2 **`C-5` 가 「어느 취득가」를 말하지 않았다** → v0.19→**v0.20** | `982c59b` | 무관(문서) |
> | 13 | `PV.replacement_schedule()` 이 **`_acquisitions()` 에서 파생되지 않았다** | `e4ce901` | **안 움직임**(297케이스 동등성 실측) |
> | 14 | **인용의 「내용」을 보는 그물이 없었다** — ③-1(차단·결손 10건) · ③-2(지목·후보 46건) | `9787b6d` | 무관 |
> | **15** | ③-1 이 드러낸 리포트 부채 7건 → **1건만 닫히고 6건은 리포트가 표시를 아예 안 낸다** | `6cbddcb` | 무관 |
>
> ## ② ★ 이 라운드의 최대 발견은 WP-15 의 「못 닫았다」
>
> ③-1 이 드러낸 리포트 부채 7건이 **시험이 없는 것이 아니라 리포트가 그 표시를 아예 안 내는 것**이었다. 마커만 달았으면 대장만 줄고 재는 방식이 헐거워졌을 자리다.
>
> ## ③ 실측 — 게이트 **전건** (`a2` 가 2026-08-30 에 다시 돌려 얻은 값)
>
> ```
> pytest 3.13              rc=0   수집 1,722건 / 158파일  (R43 끝 1,712 / 157 → +10 · +1파일)
> pytest 3.11 (CI 판본)    rc=0   실패 0 (로그의 rc= 줄을 직접 읽어 확인했다 — 함정 1)
> ruff                     rc=0   0건
> mypy                     rc=0   117파일 무결
> lint-imports             rc=0   4 kept 0 broken
> gen_traceability         rc=1(정상)  요구 105 / 수용기준 321 / 자동 290 / 수동 8 / 스텁 4
>                                  Must-have 미매핑 9 (Phase 1 은 0)
> git diff docs/traceability.md   0줄 — 커밋본이 최신이다
> check_task_mapping       rc=0
> 검사 11종                rc=0   marker_substance · hardcoded_params · source_rules ·
>                                  partition_assignment · disclosure · assumptions ·
>                                  precommit_installed · unread_extension_points ·
>                                  docstring_references · status_round_blocks ·
>                                  **clause_partition**(R44-14 신설)
> check_file_size --code-strict   rc=0
> 음성 14종                 rc=0   (R43 12종 + negtest_docstring_references · negtest_clause_nets)
> check_coverage_inputs    --base 25ce9f7  rc=0   측정 234 / 변경된 core 2건
> check_test_accompaniment --base 25ce9f7  rc=0   구현 변경 2건 전건 동반
> check_dependency_audit   rc=2   로컬은 audit.txt 가 없다 — 그것이 옳다
> 리포트 재생성             rc=0   커밋된 -2026-08-30 판과 **바이트 동일** · 매니페스트 715fda7bf9ff
> ```
>
> ## ④ 다음 라운드가 알아야 할 것
>
> - **판정 요구 누적**: WP-10(INERT_BASES 사유) · WP-11(빌더 위치/양수 거부 제약) · WP-12(해소 표시/FR-104-AC3 개정) · WP-13(독스트링 낡음/0원 교체/반환 정렬) · WP-14(부채 6건/구획 사전/후보 46건) · WP-15(아래 셋) 및 R43 잔여 이중계상.
> - ⚠⚠ **`pytest -q > 로그; echo rc=$?` 를 배경으로 돌리면 종료 코드는 `echo` 의 것(0)이다** — **로그 안의 `rc=` 줄을 직접 읽어라.**
> - ⚠ **음성 스위트를 시간초과로 중단시키면 실물 소스에 변이가 남는다** — 게이트를 `for` 로 이어 붙이지 말 것.
> - ⚠ **로컬 게이트 전건이 초록불이어도 CI 는 다른 것을 본다** — 실물 둘: `clause-partition` 잡에 `pip install pyyaml` 이 빠져 rc=2 였고(같은 파일의 다른 잡 넷은 갖고 있다 — 잡을 새로 세울 때 이웃을 베껴라), `check_docstring_references` 가 `.orch/mutate.py` 를 '없는 것'으로 쳐 rc=1 이었다(`.orch/` 는 `.gitignore` 안이라 CI 에만 없다 — '없는 것'과 '일부러 안 담는 것'을 같게 읽지 마라). 미푸시가 길어질수록 이 어긋남이 늦게 드러나는 함정이다.
> - **이번에 새로 세운 것**: `OUT_OF_REPO_PREFIXES` 와 음성 8→10갈래.
> - **남은 목록은 `status.md` 의 「다음에 집을 것」 절이 갖는다**

---

> ### ⬇ **R45 가 지운 `todo.md` 의 머리**(2026-08-30).
> 라운드 종료 블록이 아니다 — R45 가 `todo.md` 를 `status.md` 「다음에 집을 것」
> 절로 합치고 그 파일을 지우면서, **착수 목록이 아닌 부분**을 여기로 내린 것이다.
> **지운 것이 아니라 옮긴 것이다.**

## R45 가 지운 `todo.md` 의 머리 (2026-08-30)

아래는 `todo.md` 가 지워지기 직전의 머리 두 블록이다. 하나는 **그 파일이 정본이
아니라는 선언**이고 하나는 **R44 결과 표**이며, 둘 다 착수 목록이 아니라 이력이다.
착수 목록 자체(착수 순서 31행 · 부록 A · 착수 전에)는 `status.md` 의
**「다음에 집을 것」 절**로 갔다.

### 다음 할 일 — R45 (2026-08-30 갱신 · **R44 는 닫혔다**)

> ⚠ **이 파일은 정본이 아니다.** 판정 근거 · 실측 수치 · 라운드 경위는
> **`status.md` 의 「지금 할 일」 R44 블록**이 갖는다. 여기 있는 것은 *무엇을
> 다음에 집는가* 와 *어디서 시작하는가* 뿐이다 — 같은 사실을 두 곳에 적으면
> 한쪽만 고쳐진다. 두 파일이 어긋나면 **`status.md` 가 맞다.**
>
> 사람 몫(수동 검증 · 외부 자료 회신 · 배포 판단 · spec 개정)은
> **`status-human.md`** 가 소유자다. 여기 옮겨 적지 않는다.

> #### ✔ R44 가 WP 여덟(8~15)을 닫았다 — 결론축은 안 움직였다
>
> | 종전 `todo.md` | 결과 |
> |---|---|
> | **2** `PV.replacement_schedule()` 파생 | **✔ 닫혔다** — WP-13 (`e4ce901`). 297케이스 동등성 실측 · 결론축 **안 움직임** |
> | **4** §13.2.2 `C-5` 「어느 취득가」 | **✔ 닫혔다** — WP-12 (`982c59b`). spec **v0.19 → v0.20** · 수용기준 321 그대로 |
> | **5** 빌더 밖 `CashFlowRow` 4곳 | **✔ 닫혔다** — WP-11 (`f7fee1c`). `capex_viewpoint_row()` 로 모음 · 21케이스 동등성 실측 |
> | **8** `negtest_docstring_references.py` 부재 | **✔ 닫혔다** — WP-8 (`5503b2f`) |
> | **9** NFR-105 동반 검사가 문면 변경도 셌다 | **✔ 닫혔다** — WP-9 (`039c7b6`). diff 를 본다 |
> | **10** 독스트링 검사의 점 표기 | **✔ 닫혔다** — WP-10 (`ece6503`). 41건 편입 · 위반 0 |
> | **부록 A ⑦** 기계 그물 ③-1·③-2 | **✔ 닫혔다** — WP-14 (`9787b6d`). ③-1 차단(결손 10건) · ③-2 지목(후보 46건) |
> | **부록 A ①** `test_ess.py` 자기충족 검사 | **이미 삭제돼 있었다** — 문면만 남아 있었다. **부록 A 에서 ① 을 지운다** |
> | **3** `Load`·`ThermalLoad` | **조사만 했다** — 아래 참조. **닫히지 않았다** |
>
> ⏳ **미푸시 커밋 수는 `git log origin/main..HEAD --oneline | wc -l` 로 센다** — 이
> 파일에 적지 않는다. 종전 판이 적어 둔 「10건」은 **세지 않은 수였고 거짓이었다.**
>
> ⚠ **R44 가 이 파일을 한 라운드 동안 건드리지 못했다**(라운드가 안 닫힌 채 세션이 끊겼다). 그것이 이 재작성의 사유다.
> ⚠ **지금 길이를 이 줄에 적지 않는다** — `wc -l todo.md` 로 센다. 적어 두면
> 다음에 한 줄만 고쳐도 거짓이 되고, 그것이 이 저장소가 반복해 밟은 형태다.

---

> ### ⬇ **R45 가 내린 「반복해서 걸린 함정」 절의 경위 셋**(2026-08-30).
> 라운드 종료 블록이 아니다 — **막는 검사가 생긴 함정의 경위**이며,
> `status.md` 에는 **검사 이름 한 줄**만 남겼다. **지운 것이 아니라 옮긴 것이다.**

## R45 가 내린 것 — 「반복해서 걸린 함정」 절의 경위 (2026-08-30)

「함정」 절은 **아직 밟을 수 있는 것**만 갖는다. 아래 셋은 그것을 막는 검사가
이미 서 있어 경위가 이력이 된 것이다 — *왜 그 검사를 세웠는가*를 물을 때만 읽는다.

### 수용기준 ID를 추정해서 쓰면 안 된다 — 경위 (붙드는 것: `scripts/check_task_mapping.py`)

작업 목록 초판에서 **7건이 실재하지만 다른 조항**을 가리켰다. ID가 존재하므로 기존 검사는 통과했다. 그대로 마커를 붙였다면 **엉뚱한 조항이 초록불, 맞는 조항은 미매핑**이 되어 NFR-107이 막으려던 상태가 된다.

원인은 spec v0.7에서 AC가 중간 삽입되어 번호가 밀린 것이다. **개정 때마다 재발한다.** `check_task_mapping.py`의 「미인용 Must-have」 검사가 그 그림자를 잡는다.

### ★ 규약 — `operating_lines.__all__` 전건 재수출 — 경위 (붙드는 것: `tests/casegrid/test_operating_lines_reexport.py`)

R43-F2 가 `e2e_runner.py`(499/500줄)에서 **연간화 갈래**를 떼어
`core/casegrid/operating_lines.py` 로 옮기면서, 러너가 그 이름들을 계속
`from … import` 로 들고 있게 했다. **그 재수출을 아무도 재지 않았다** — 끊으면
테스트 일곱이 **수집 단계에서 죽는데**, 이 저장소는 *「검사가 돌지 않은 것」* 을
통과로 읽지 않는다(§13.0.1 ④). 게다가 `mypy` 의 `no_implicit_reexport` 를 끄면
아무도 안 죽고 조용히 갈린다.

**오케스트레이터 판정: 이 규약을 유지한다.** 붙드는 것은
`tests/casegrid/test_operating_lines_reexport.py` 이며 **객체 동일성(`is`)** 으로
잰다 — 이름이 있는지가 아니라 **같은 객체인지**를 본다.

> ⚠ **음성 확인 넷 중 하나는 초록불이었고 그것이 옳다** — 선언 쪽에서
> 재바인딩해도 러너의 `from … import` 는 **실행이 끝난 뒤의 객체**를 매므로 두
> 이름은 언제나 같다. **갈림은 러너 쪽에서만 생긴다.** 「음성 테스트가 아무것도
> 안 붙드는」 자리를 실측으로 가려낸 것이다.

### 정본은 세션 중에도 바뀐다 — 경위 (붙드는 것: `docs/source-rules.lock` · `scripts/check_source_rules.py --require-vault`)

2026-08-02 하루에 볼트의 정본이 **세 번** 바뀌었고 세 번 다 사람이 뒤늦게 발견했다. `docs/source-rules.lock`의 sha256으로 감지한다. **날짜 해상도로는 부족하다.**

---

> ### ⬇ **R45 가 내린 「미해결」 해소 행 20개**(2026-08-30).
> 라운드 종료 블록이 아니라 **표의 행**이다 — `status.md` 「미해결」 절에서
> 조치 시점이 `✔` 인 행만 골라 내렸다. **지운 것이 아니라 옮긴 것이다.**

## R45 가 내린 것 — 「미해결」 표의 해소(취소선) 행 (2026-08-30)

`status.md` 「미해결」 절은 **아직 살아 있는 항목만** 갖는다. 아래 20행은 조치가
끝나(조치 시점 `✔`) 이력이 된 것이며, *왜 그렇게 닫혔는가*를 물을 때만 읽는다.

⚠ **이 20행 중 18행은 이 파일 아래쪽 `# 진행 상태 — 2026-08-14 (R32 종료)`
스냅숏의 `## 미해결` 절에 **바이트 동일**하게 이미 있다.** 그 절은 *R32 시점의
`status.md` 를 통째로 얼린 것*이고 이 절은 *R45 가 `status.md` 에서 무엇을
내렸는가의 기록*이다 — **둘 다 참이며 어느 쪽도 지우지 마라.** 겹치지 않는
둘은 2026-08-15 에 닫힌 「`build_dispatch_notes()` 호출자 0곳」과 R39-E·R40 이
닫은 「교체비·잔존가치」이며, R32 뒤에 해소된 것이라 그 스냅숏에는 없다.

| 구분 | 항목 | 영향 | 조치 시점 |
|---|---|---|---|
| ~~도구~~ | ~~`build_dispatch_notes()` 호출자 0곳~~ | **2026-08-15 해소** — 붙임 6·7 이 읽는다(`core/report/dispatch_sections.py`). `DispatchStack` 차트는 **여전히 0곳이며 그것이 옳다**: `load`(부하 곡선)가 필수 입력인데 파이프라인에 가구 부하가 없다. 붙임 7 이 「미산출(부하 곡선 부재)」로 싣는다 | ✔ |
| ~~계약~~ | ~~**교체비·잔존가치가 프로포마에 들어가지 않는다** (R33 발견)~~ **R39-E 해소 · R40 이 마지막 한 줄까지 닫았다** | R39-E 가 `core/casegrid/lifecycle.py::lifecycle_rows()` 로 교체비·잔존가치를 실행 경로에 배선했다(그 전까지 비용 행은 고정 O&M 둘·전력 구매·정산 수수료 넷뿐이었다). R40 이 `ESS._acquisitions()`·`replacement_schedule()` 의 물가 계수를 마저 굴려 **명목 기준**을 맞췄다. 결론축은 그 배선으로 −5,284,586 → −6,066,881 → **−6,289,675원**이 됐다 — 「사업이 나빠진 것」이 아니라 **종전 수가 실제로 나가는 비용을 빠뜨리고 있었던 것**이다. 붙드는 검사는 `tests/casegrid/test_lifecycle_wiring.py`(ⓐ~ⓔ) | ✔ |
| ~~도구~~ | ~~매핑표 Phase 열 43건 공란~~ | **08-08 해소** — 부록 A.1 해석 추가. 잔여 9건은 전건 Should-have | ✔ |
| ~~도구~~ | ~~표 수용기준 10건 뭉개짐~~ | **08-08 해소** — 9건 전개 / 1건 폐기. 수용기준 252 → **299건** | ✔ |
| ~~도구~~ | ~~`NFR-107-AC1.manual` 미구현~~ | **08-08 해소** — `ast` 기반 마커 수집으로 구현. 클래스·모듈 수준 상속 포함. 수행 기록 없는 스텁은 종료 코드 2 | ✔ |
| ~~도구~~ | ~~CI 매핑 잡이 spec 결함 시 조용히 통과~~ | **08-08 해소** — `\|\| true`로 종료 코드를 버려 결함(코드 2) 시 파일이 안 바뀌고 diff가 통과했다. 실측 재현 후 폐쇄 | ✔ |
| ~~검증~~ | ~~B/C 오라클 미배정~~ | **08-09 해소 (spec v0.12)** — §13.0.2 **순위 1**. B/C는 IRR과 달리 반복해가 아니라 **두 할인 합산의 몫**이므로 닫힌 형태다. **분자·분모를 각각 원 단위 완전 일치로 판정하고 비율에는 별도 오차를 두지 않는다** — 비율에 오차를 주면 «편익도 비용도 과대» 와 «둘 다 정확» 이 같은 값을 내어 어느 쪽이 틀렸는지 말하지 못한다 | ✔ |
| ~~계약~~ | ~~`dispatch()`의 「한 해」 ↔ 계약 테스트 `steps=24` 충돌~~ | **08-08 해소** — 「연초부터 연속 `steps` 스텝」으로 확정. `year_fraction` 신설, 「짧은 창은 긴 창의 앞부분」을 계약 테스트가 검사 | ✔ |
| ~~계약~~ | ~~`value_streams()` 훅 부재~~ | **08-08 해소** — `DER`에 기본 `return ()`. 편익 객체가 아니라 **tag 문자열**을 돌려준다(객체는 NFR-208-AC2 위반) | ✔ |
| ~~계약~~ | ~~부가세 분리 자리 없음~~ | **08-08 해소** — `capex_vat()` 추상 신설. 실측하니 **다섯**이 지어냈고 ESS 하나는 아예 없었다 | ✔ |
| ~~계약~~ | ~~그 밖에 자원 구현이 자작한 것~~ | **08-08 대부분 해소** — 운전 방법 접근자 이름 고정 · `salvage_value()` 명목액 명문화 · `escalation_rate` 통일 · `DispatchResult` 에 `unmet_*`·`notes` · `AllocationResult` 계약 이관 · `CommonAsset` SW/HW 분리 잔존가치. **요금 시계열은 남았다**(아래 행) | ✔ |
| ~~계약~~ | ~~요금·연료 단가 시계열을 놓을 자리 없음~~ | **08-09 판단 완료 (1.12 ⑥)** — `DispatchContext` 에 **가격 신호**로 싣는다. 별도 계약을 두지 않은 이유는 ctx 가 이미 *"엔진→자원 유일 통로"* 이기 때문이다. 「요금」이 아니라 「신호」인 것이 요점 — 누진·TOU·특례 해석은 WP-3 이 하고 **결과인 스텝별 단가 하나**만 실린다 | ✔ |
| ~~품질~~ | ~~NFR-206 초과 6건~~ | **08-08 판단 완료** — `ast` 로 갈라 재자 **코드 줄 수로는 전건 상한 이내**(최대 482)였다. 조항의 근거(DER-VET `Params.py` 1,830줄)가 겨냥한 코드 스프롤에 해당하는 파일은 없고 초과분은 전부 설명 밀도다. `--code-strict` 를 CI **차단**으로 켰다 | ✔ |
| ~~입력~~ | ~~`vat_rate=0.1` 이 대장에 없다~~ | **08-08 해소** — `track: fixed` 갈래를 신설하고 `tax.vat_rate` 등재(확정 · 부가가치세법 제30조). 함께 **v1.1이 남긴 기본값 갈림**(여덟 중 히트펌프만 0.1)을 통일하고 레지스트리 순회 계약 테스트로 고정 | ✔ |
| ~~문서~~ | ~~교체 연도가 spec ↔ Wave 0 참조 구현 사이에서 갈림~~ | **08-08 해소** — `ReferencePV`가 수명 연도(12년차)에 계상해 §13.2.2 C-4(*수명 도달 **다음** 연도 초*)와 한 해 어긋나 있었다. WP-1a가 짚었고 13년차로 정정 | ✔ |
| ~~도구~~ | ~~AC 단위 Phase 표기를 검사기가 못 읽음~~ | **08-08 해소** — 수용기준 단위 Phase(`[Phase N]` 줄 끝 표기) 도입. 미인용 1 → **0건** | ✔ |
| ~~운영~~ | ~~정본 표류 탐지 장치가 꺼져 있음~~ | **08-08 해소 (2.16)** — `DER_VAULT_ROOT` 사용자 환경변수 + 작업 스케줄러 매일 09:10 + 표류 마커 파일. 네 경로 실측 확인 | ✔ |
| ~~검증~~ | ~~`DV-4` 후반 「윤년 처리 규칙 명시」 — 강제되지 않고 테스트가 반대를 고정한다~~ | **R25 해소.** `core/contracts/units.py` 의 `LEAP_YEAR_POLICY` 가 규칙을 선언한다 — **평년(365일) 고정 · 366일 시계열은 받지 않는다 · 그 하루의 처리는 호출측 기록**. 자리를 거기로 정한 이유는 스텝 수 상수를 쓰는 쪽(`core/der/*` 계산 층과 `infra/tsstore.py` 저장 층)이 **전부 그것을 받아 가기 때문**이다. 흩어져 있던 주석 둘은 선언을 가리키게 바꿨다(사본을 만들지 않았다). 붙드는 것은 `tests/contract/test_leap_year_policy.py` — 문면 · **스텝 수가 연도를 받을 수 없다** · **윤년 행 수가 허용 목록에 없다**. 닿는지는 `test_tsstore.py::..._hands_over_the_policy` 가 본다(8784행이면 조치 자리에 선언 그대로). 변이 6종 전건 잡힘 | ✔ |
| ~~품질~~ | ~~**mypy strict 5건 실패**~~ **해소 — 이 행이 낡아 있었다 (R31 실측)**. `python -m mypy` 가 **102파일 무결**이다(R31 종료 시 실측). 08-09 에 적힌 5건은 그 뒤 상환됐고 `heatpump.py` 의 `Year` 불일치도 남아 있지 않다. **CI `tests` 가 `mypy strict` 를 차단으로 돌리므로 다시 쌓일 수 없다** — 「아무도 돌리지 않아 쌓였다」는 원인이 사라졌다 | ✔ |
| ~~검증~~ | ~~`NFR-208-AC1`~`AC3`·`M1` 미매핑~~ | **08-08 해소 (2.8)** — 세 위반을 각각 심어 **그 계약이** BROKEN 이 되는지 확인하고, 심지 않은 상태의 `4 kept` 를 양성으로 함께 둔다. 심는 모듈은 `_` 로 시작해 레지스트리 스캔은 피하되 import 그래프에는 잡힌다 | ✔ |

<!-- R45 이관 컨테이너 끝 -->

---

> ### ⬇ **R44 가 내린 R43 블록**(2026-08-30). R44 블록 아래 시간 역순 자리이며,
> 그래도 **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-08-30 **R43 완료 — 오케스트레이터·워커 구성으로 WP 열넷을 닫았다. 결론축은 안 움직였다**
>
> **`.orch/R43/` 에 지시문과 결과 파일 열넷이 있으나 그 디렉터리는 `.gitignore`
> 안이다 — 다른 기계에는 없다. 이 블록이 정본이다.**
>
> 라운드 도중 **워커가 바뀌었다**(a5 할당량 소진 → a2). 인계는 파일로만 이뤄졌고
> 잃은 것이 없다 — 브리프 파일·결과 파일 방식이 바로 이 상황을 위해 있었다.
>
> ### ★ 결론축은 **−6,289,675원 · 결손/총사업비 64.2%** — R40~R42 와 같다
>
> 열넷 중 어느 것도 기준수준의 수를 움직이지 않았다. **움직일 수 있는 것 둘은
> 일부러 안 했다** — 「지금 할 일」이 스스로 적어 둔 *「기준선을 흔드는 것은 한 번만
> 해야 한다」* 때문이고, 지금은 **이중계상 사람 판정(甲·乙·丙)이 미결**이다.
> 옛 수(−865,881 · −5,284,586 · −6,066,881)를 인용하지 말 것.
>
> ### ⏳ **R43 의 커밋은 전건 미푸시다** — `origin/main` 이 `c4ce0e5` 그대로다
>
> **수를 이 파일에 박지 않는다.** `git log origin/main..HEAD --oneline | wc -l` 로
> 센다 — R43 은 `c4ce0e5` 에서 시작했으므로 **그 뒤의 커밋 전부**가 미푸시다.
> ⚠ 이 자리에 수를 적으면 **다음 커밋 하나로 거짓이 된다.** 그것이 아래 ★ 가
> 말하는 형태이며, 그래서 수 대신 명령을 적는다.
>
> ★ **머리와 R42 블록의 「커밋 6~7건이 미푸시」는 둘 다 거짓이었다.** WP-A 착수
> 시점 실측이 **0건**이고 `origin/main` 이 R43 시작 ref `c4ce0e5` 그대로였다 —
> R42 의 커밋은 전부 푸시돼 있었다. 세지 않은 수를 옮겨 적은 자리가 **또** 나온
> 것이며, 그래서 **수 대신 세는 명령**을 적는다.
>
> ## ① WP 열넷 — 무엇이 어긋나 있었고 무엇이 이제 그것을 붙드는가
>
> | WP | 어긋나 있던 것 | 이제 붙드는 것 | 커밋 |
> |---|---|---|---|
> | **A** | 「지금 할 일」에 종료 블록이 **아홉 판**(`todo.md` 는 「넷」이라 적었다 — 세지 않은 수다). `_by_year` 독스트링이 **없는 함수**를 가리켰다 | 여덟을 내렸다(**1,638 → 507줄**). 받는 자리는 머리가 아니라 **시간 역순이 지켜지는 자리**(오케스트레이터 판정) | `a51a76b` · `b8e00e2` |
> | **B** | `CashFlowRow` 를 **한 자료형 두 방식**으로 지었다(빌더 밖 7곳). `from_resource` 라는 **같은 칸 이름이 두 규약**을 가졌다 | `proforma.salvage_row()` · 칸을 `resource_name`/`resource_code` 로 가름 · `tests/casegrid/test_from_resource_conventions.py` | `e93d19a` · `b05f96b` · `5902c0d` |
> | **C** | 3.2 의 「변동 O&M 미포함」이 **리터럴**이라 거짓이 되는 날 조용했다 | 붙임 8 이 **프로포마 비용 행**으로 판정하고 3.2 가 그 판정에서 문면을 짓는다(`core/report/unreflected.py`) | `bee96fe` |
> | **D** | `HeatPump` 교체비가 물가 계수를 **아예 안 탔다** | `KNOWN_REPLACEMENT_RATE_IGNORED` **2 → 1건**(`ReferencePV` 만 남음) · 계약 스위트가 전 자원을 잰다 | `59530fc` |
> | **D2** | `HeatPump` 잔존가치가 **명목으로 산 것을 실질로 되팔았다.** 덤으로 **사지 않은 것의 잔존가치**와 **최초 순환펌프 이중계상**이 나왔다 | 취득분을 `_acquisitions()` **한 출처**로 모음 · 새 부채 래칫 `KNOWN_SALVAGE_IGNORES_REPLACEMENT_RATE`(2건 — `Load`·`ThermalLoad`) | `d84437c` |
> | **E** | 같은 8.00kWh 가 **잉여 판매와 첨두 절감으로 두 번** 화폐가 된다 | **코드는 안 고쳤다.** 판정서 `docs/evidence/판정요구-이중계상-2026-08-29.md` + `status-human.md` 미결 한 줄 | `d7bc5d3` |
> | **E2** | 4.3 이 **자기가 적어 둔 「1:1 귀속」 조건을 어긴 채** 인쇄됐다 | `core/casegrid/attribution.py` — **편익이 스스로 선언**(`scales_with_dispatch_window`)하고 **스텝마다** 가른다. 7.3/50.2년 → **14.4/11.9년** | `79c46bd` |
> | **F** | 인버터 몫 `0.15` 가 **결론에 들어와 있으면서 어느 축에도 없었다** | 대장 `capex.pv.inverter_share`(**`Q-18`**) · 새 래칫 `test_every_capex_ledger_item_is_a_sweep_axis_or_says_why_not`(면제는 **사유 문자열과 함께**) | `a02cdcd` |
> | **F2** | `e2e_runner.py` 가 **499/500줄** — 다음 한 줄이 게이트를 깬다 | **407줄.** 새 구획 `core/casegrid/operating_lines.py`. 가르는 선은 줄 수가 아니라 **「연간화」라는 규약** | `d968a52` |
> | **F3** | 러너가 **재수출한다고 적어 놓고 아무도 재지 않았다** | `tests/casegrid/test_operating_lines_reexport.py` 가 **객체 동일성(`is`)** 으로 붙든다 | `9b25926` · `6dea65c` |
> | **G** | 검토자가 「잠정성」 한 줄을 **정반대로** 읽었다(문의사항 나-1·2·3·4·7) | 「잠정성」이 **대장을 세어 지은 수**가 됐고, 6.2 의 지원율이 **네 자리에서 한 함수**에서 나오는 것을 검사가 잰다 | `a040ce9` |
> | **H** | 첨두 기본요금 **8,320원/kW·월**이 대장에도 축에도 없이 결론의 21%를 정했다. 붙임 8 이 **재료를 다 재고 방향에서 멈췄다** | 대장 `tariff.hv_single_contract.demand_charge`(`Q-6`) → 5.1 에 섬 · 자가소비 순 방향 **연 −173,913원**(역전) · 붙임 10 이 **자원 거부와 계산 파손을 가른다** | `629bb66` · `373f269` · `03eedbb` · `411caff` |
> | **I** | 문의사항 아홉 중 **돌려 보면 답이 나오는 넷**이 「모른다」로 남았다 | **소스 변경 0건.** 답변서 `docs/evidence/답변-지방정부담당자-2026-08-29.md` | `1e459e9` |
> | **L** | **이름이 실재하는지·선언 자리가 맞는지를 재는 검사가 하나도 없었다**(A·F3 가 두 번 지목) | `scripts/check_docstring_references.py` **CI 차단**. 관점 한정을 「결론 축」 행에도 병기 | `1d1dc6f` · `7e057de` · `d1800d1` |
> | **J** | 라운드를 닫는다 — **게이트 전건** · 이 블록 · `todo.md` · 아래 ② 의 검사 · 리포트 **1회만** 재생성 | ② 의 검사(CI 차단) · 나머지는 **없다**(수를 지우고 **세는 명령**을 남겼다 — 없는 수는 낡을 수 없다) | `12f4db3` · `987fb1f` · `971edfa` · `2c21661` + 이 커밋 |
>
> ## ② ★ 이 라운드가 **새로 세운 검사** — 「지금 할 일」 블록 수 (WP-A 가 남긴 것)
>
> 아홉까지 쌓인 이유는 **그것을 재는 것이 없었기 때문**이다. `status.md` 는 스스로
> *「이력을 이 파일에 쌓지 않는 것이 규칙이다」* 라고 적어 두었다.
>
> - `scripts/check_status_round_blocks.py` — `## 지금 할 일` 절 안의 `^> # ⏹` 블록이
>   **상한 1판**을 넘으면 rc=1. 절을 못 찾으면 **rc=2**(0판을 통과로 읽지 않는다)
> - **상한을 1 로 골랐다.** 2 로 두면 **한 판 밀린 상태가 언제나 초록불**이고,
>   아홉까지 쌓인 경로가 정확히 그 「한 판씩 밀림」이다. 닫는 도중의 둘은 순서로
>   피한다 — 규칙이 적은 순서가 이미 *「내린다 → 세운다」* 다
> - `scripts/negtest_status_round_blocks.py` — **실물 `status.md` 를 읽어 변형한다**
>   (지어낸 문면을 쓰지 않으므로 서식이 바뀌면 **심기가 실패해 알린다**).
>   음성 3(블록 추가 rc=1 · 절 제목 소실 rc=2 · 대상 없음 rc=2) + 양성 2
>   (실물 그대로 rc=0 · **절 밖 블록 rc=0** — 내린 것을 세면 검사가 꺼진다)
> - **CI 차단으로 배선했다**(`source-rules.yml` 의 `status-round-blocks`).
>   ⚠ `on.push.paths` 에 **`status.md` 를 함께 넣었다** — 넣지 않으면 이 파일만 고친
>   푸시에서 잡이 아예 돌지 않고, 그것이 이 검사가 막으려는 상태 그대로다
>
> ## ③ 리포트를 **이 라운드에서 한 번만** 다시 뽑았다 — 판이 갈렸다
>
> 리포트를 건드린 WP 가 다섯(C·E2·F·G·H)이라 재생성을 여기로 모았다. 매번 뽑으면
> 같은 파일을 다섯 번 흔들고 검토자는 어느 판이 무엇을 담았는지 알 수 없다.
>
> **`docs/evidence/MC-1-검토용-리포트-2026-08-30.md` 신설 · `-08-29` 판은 남긴다.**
> 덮지 않은 이유는 **문의사항·답변서·판정요구 셋이 `-08-29` 판을 이름으로 가리키기
> 때문**이다 — 덮으면 그 셋이 대조할 원본을 잃는다.
>
> | 자리 | `-08-29` | **`-08-30`** |
> |---|---|---|
> | 붙임 8 행 수 | 4 | **5** (변동 O&M) |
> | 3.4 항목 수 | 4 | **5** |
> | 5.1 「전환까지 남는 거리」 | 6행 | **8행** — `demand_charge` 5위(5,596,927원) · `pv_inverter_share` 7위(6,159,992원) |
> | 4.3 단순 회수 | 7.3년 / 50.2년 | **14.4년 / 11.9년** (연 편익 합계 954,500원 그대로) |
> | 요약 「미반영 항목」 | 4건 — 개선 0 · **악화 0** · 미측정 4 | **5건 — 개선 0 · 악화 2 · 미측정 3** |
> | 요약 「결론」 | 분석기간 20년 내 미회수 | 같음 + **(사업주 관점 · 사회적 편익 미계상 (3.3))** |
> | 요약 「잠정성」 | 「전환 인자에 신뢰도 `가정` 없음」 | **「전제 27건 중 24건은 신뢰도 `가정`」** |
> | 매니페스트 | `bd6bcc820a19b63f` | **`715fda7bf9ff`** |
>
> ★ **「악화 0 → 2」가 이 라운드가 검토자에게 실제로 답한 것**이다 — 문의사항 가-5
> (*「미반영을 다 반영하면 결손은 커지는가 작아지는가」*)에 **부호 둘**이 붙었다.
> ★ 「잠정성」의 **26/23 이 27/24 가 됐다** — 그 수가 리터럴이 아니라 **대장을 세어
> 짓는다**는 것이 같은 라운드 안에서 증명됐다(WP-H 가 `Q-6` 항목을 올린 결과).
>
> ## ④ 실측 — 게이트 **전건** (2026-08-30 · 이 블록을 쓰기 전에 다시 돌렸다)
>
> ```
> pytest 3.13            rc=0   수집 1,712건 / 157파일 (실패 0)
> pytest 3.11 (CI 판본)  rc=0
> ruff / mypy            rc=0   mypy 117파일   ← 「115파일」은 낡은 수였다
> lint-imports           4 kept 0 broken
> gen_traceability       rc=1(정상)  요구 105 / 수용기준 321 / 자동 290 / 수동 8 / 스텁 4
>                        Must-have 미매핑 9 (Phase 1 은 0) · traceability.md diff 0
> check_task_mapping     rc=0
> 게이트 스크립트 10종    rc=0   (check_docstring_references · check_status_round_blocks 포함)
> check_file_size --code-strict   rc=0
> 음성 12종               rc=0   (negtest_status_round_blocks 신설)
> check_coverage_inputs     --base c4ce0e5  rc=0   측정 234 / 변경된 core 17건
> check_test_accompaniment  --base c4ce0e5  rc=0   구현 변경 17건 전건 동반
> check_dependency_audit    rc=2 (로컬은 audit.txt 가 없다 — 그것이 옳다)
> ```
>
> ## ⑤ ⏸ 이 라운드가 **찾았으나 닫지 않은 것** — 전부 `todo.md` 에 순서로 있다
>
> - **이중계상 판정 셋(甲·乙·丙)은 사람 몫이다.** 乙·丙 은 **겹치지 않으니 하나만**
>   지운다. **丙 이상은 결손이 총사업비를 넘어 지원율로 메우는 답이 성립하지 않는다**
> - **`K1`·`K2` 는 착수하지 않고 R44 로 미뤘다** — `K1`(`PV`·`Load`·`ThermalLoad` 파생
>   구조)은 **결론축을 움직일 수 있는 유일한 WP** 이고, 사람 판정이 미결인 채로
>   기준선을 두 번 흔들지 않는다
> - **todo 5-② 의 나머지 절반**(최초 인버터 몫을 본체 취득가에서 뗀다)은 **필요한 값이
>   이제 대장에 있다.** 떼면 **결론축이 움직인다** → 사람 판정
>
> ## ⑥ 다음 라운드가 알아야 할 것
>
> - **`operating_lines.__all__` 전건이 러너에 재수출되어야 한다**(오케스트레이터
>   판정 · 함정 절에 규약으로 적었다)
> - **⚠ 설비 제원 상수는 `e2e_runner.py` 밖으로 못 옮긴다** — 리포트 문면이 소유자를
>   **이름으로 지목**하고 검사가 붙든다. 옮기면 **매니페스트 해시가 움직인다**
> - 수를 인용하는 자리를 만들 때는 **다시 세어** 적을 것 — 이 파일에 옮겨 적은 수도
>   예외가 아니다. 이 라운드에 **세 자리**가 그렇게 틀려 있었다(미푸시 수 · 블록 수 ·
>   `mypy` 파일 수)
> - **남은 목록은 `todo.md` 가 갖는다**


> # ⏹ 2026-08-29 **R42 완료 — `todo.md` 후보 셋을 다 닫았다. 결론축은 안 움직였다**
>
> **워커 없이 단독으로 돌았다.** 사용자가 후보 셋을 다 고르고 2번의 방향을
> *「올리지 않는다를 고정」* 으로 정했다. 3번은 착수 중에 **선행이 하나 더 있다는
> 것이 드러나** 다시 물었고 *「계약을 넓힌다」* 로 정해졌다.
>
> ### ★ 결론축은 **−6,289,675원 · 결손/총사업비 64.2%** — R41 과 같다
>
> 세 항목 어느 것도 기준수준의 수를 움직이지 않는다. 사유가 각각 다르다:
> ① 러너의 `ESS` 에 PCS 가 없다 ② 코드가 아니라 CI 판정이다 ③ 대장의 base 가
> 0 이라 명목 변화율이 `PRICE_ESCALATION_RATE` 와 같다. **옛 수(−865,881 ·
> −5,284,586 · −6,066,881)를 인용하지 말 것.**
>
> ### ⏳ ~~**커밋 6건이 미푸시다**~~ — **이 줄은 거짓이었다** (R43 실측)
>
> R43 착수 시점에 `git log origin/main..HEAD` 가 **0건**이었고 `origin/main` 이
> R43 시작 ref `c4ce0e5` 그대로였다 — R42 의 커밋은 전부 푸시돼 있었다.
> **세지 않은 수를 옮겨 적은 자리**이며 이 파일이 스스로 경계해 온 형태다.
>
> ## ① `ESS` 가 PCS 를 사고도 잔존가치를 안 셌다 (`todo.md` 1번 · R40 ② 가 남긴 ⏸)
>
> R40 이 PCS **교체비**만 배선하고 `_acquisitions()` 를 건드리지 않아 **교체비는
> 내는데 잔존가치는 없는** 비대칭이 남아 있었다 — ⓒ 그물이 경고한 두 방향 중
> 앞쪽이고, 결론을 한 방향으로만 나쁘게 만들어 「보수적이라 안전하다」로 읽힌다.
>
> - `ESS._acquisitions()` 를 `PV` 의 부품별 `(수명, {연차: 취득가})` 모양으로
>   옮겼다. 접으면 15년차에 산 PCS 의 잔존수명을 **배터리 수명**으로 재게 된다
> - ★ **`replacement_schedule()` 을 그 하나에서 파생시켰다.** 종전에는 PCS 항을
>   거기서 따로 굴렸고 **그 갈라짐이 이 결함을 만들었다**. `retire` 조건도 이제
>   한 곳에만 있다
> - 최초 PCS 취득분은 세지 않는다 — 설비단가가 설치 완료 기준이라 이미
>   `_gross_capex_won()` 안에 있고, 떼려면 「단가의 몇 %가 PCS 인가」가 필요한데
>   **그 값이 대장에 없다**(`Q-2`). `PV` 의 인버터가 같은 자리다
>
> **실측** — 탐침(용량 10kWh · PCS 수명 7년 · 200만원 · 20년):
> 잔존가치 4,486,228 → **4,863,222원**(+376,994 = 15년차 PCS × 1/7).
> PCS 없는 `ESS` 는 스케줄·잔존가치가 **한 자리도 안 달라졌다.**
>
> ⚠ **ⓒ 는 `ESS` 쪽에서 이제 동어반복에 가깝다** — 두 경로가 같은 출처를 본다.
> 알고 남겼다: `PV` 는 여전히 두 곳이 각자 짝을 돌고, 변이 ⑤(1년차를 거르지
> 않는다)를 ⓒ 가 잡는다.
>
> ## ② `setuptools` `PYSEC-2026-3447` — **「올리지 않는다」로 고정** (`todo.md` 2번)
>
> 사용자 판정. 사유는 `docs/accepted-vulnerabilities.yaml` 이 갖는다 —
> `setuptools` 는 `[build-system] requires` 에만 있고 `uv.lock` 에도 없는 **빌드
> 전용 의존**이라 배포물이 실행하는 코드가 아니다. 「올린다」를 안 고른 이유는
> 작아서가 아니라 **빌드 백엔드를 바꾸는 재검증이 막는 위험이 0**이기 때문이다.
>
> ### ★★ 판정만으로는 todo 2번이 든 위험이 안 닫힌다
>
> 문제는 취약점 하나가 아니라 **경고가 상태를 갖지 않는다**는 것이었다. 어제의
> 한 건과 오늘 새로 생긴 두 건이 같은 노란 줄로 보이고, 실제로 이 건이 두
> 라운드를 그렇게 지났다. 그래서 **경고 대 차단이 아니라 「판정된 것 대 판정 안
> 된 것」으로 가른다**(`scripts/check_dependency_audit.py`).
>
> - 대장에 있는 것은 조용하고, 없는 것이 나오면 **CI 가 빨간불**이다
> - 「통과할 수 없는 검사는 꺼진다」는 워크플로 주석의 반론은 유효하며, 그래서
>   **빠져나갈 문**을 함께 두었다 — 사유를 적으면 통과한다. 막는 것은 취약점이
>   아니라 **아무도 안 본 취약점**이다. **줄어도 빨간불**이다
> - 건수를 안 세고 **ID 를 대조한다** — 「2건」이라 적히고 ID 는 하나만 나오는
>   출력이 실제로 있었다. 셀 수 없는 것을 세면 그 수가 거짓이 된다
>
> ⚠ **CI 동작이 바뀌었다** — 종전에는 어떤 취약점도 차단하지 않았다.
> ⚠ 로컬에서는 `audit.txt` 가 없어 **rc=2** 다(「커밋 뒤에만 되는 것」과 같은 성격).
>
> ## ③ `Q-17` 을 스윕 축으로 배선했다 (`todo.md` 3번) — **선행이 하나 더 있었다**
>
> ### ★★★ 대장이 적용범위를 좁혀 두었는데 코드에 그 자리가 없었다
>
> `capex.replacement_real_trend` 는 *「재취득 단가에만 걸린다. 최초 취득가에는
> 걸리지 않고 고정·변동 O&M 도 대상이 아니다」* 라 적었다. 그런데 자원 계약에는
> 계수가 `escalation_rate` **하나**뿐이고 그 하나가 셋을 함께 굴렸다 — 그대로
> 흔들면 하단 −2.0 이 「물가 계수를 태우지 않았다면」이 아니라 **「O&M 물가까지
> 꺼 버렸다면」**을 재게 된다. 즉 **대장이 선언한 것과 다른 것을 재는 축**이다.
>
> ★ R38 ② 가 이 자리를 *「상수의 이름이 실제 쓰임보다 좁게 주장한다」* 로 잡고
> 이름을 넓혀 봉합했는데, **넓힌 이름은 갈라야 할 것을 갈라 주지 않는다.**
>
> ### 그래서 spec §16.2 대로 **세 커밋**으로 갔다
>
> | 커밋 | 무엇 | 수가 움직이나 |
> |---|---|---|
> | 계약 | `DER.replacement_escalation_rate` · `…_factor()` (기본값 = O&M 계수) | **아니오** |
> | 구현 | 자원 다섯의 교체 경로 **일곱 자리**가 새 계수를 읽는다 | **아니오** |
> | 배선 | `_LEDGER_VARS` + 러너가 `물가 계수 + 실질 추세` 를 만들어 `PV`·`ESS` 에 | 기준수준은 **아니오** |
>
> §16.2 는 *「구현 PR 에 계약 변경을 섞지 않는다」* 이며 각 커밋이 초록불이다.
> 덧셈인 근거는 **대장이 갖는다**(그 항목의 `applicable_scope` 문면) — 여기서
> 다시 정하면 정본이 둘이 된다.
>
> ### ★★ 실측 — 리포트를 다시 뽑아 눈으로 읽었다
>
> ```
> 5.1 판단용        5위  replacement_real_trend  −2~2 %/년  변동폭 712,020원  1변수 스윕
>                   6위  tariff_escalation       (5위→6위) 여전히 「파이프라인 미반영」
> 전환까지 남는 거리  −2.0 에서 결손 6,289,675 → 5,984,595원
> ```
>
> ★ **R40 이 교체비에 물가 계수를 태운 판정이 결론에 넣은 크기는 305,080원**이고
> 그 수가 이제 리포트에 있다. **요약의 「미반영 항목」 수는 안 움직였다** — 새
> 축은 미반영이 아니라 **읽히는 축**으로 들어왔다(`delta ≠ 0`).
>
> ## ④ 이 라운드가 **네 번 걸렸다 — 전부 게이트·래칫이 잡았다**
>
> 1. **ⓓ 래칫이 계수를 가르자마자 빨간불**을 냈다. `escalation_rate` 를 눕혀도
>    교체비가 안 반응하는 것은 결함이 아니라 **가른 결과**이며, 래칫이 설계대로
>    「무엇이 달라졌는지 사람이 보라」고 말한 자리다
> 2. **`ParameterCatalogueError`** — 고급 모드 화면이 멈췄다. 새 인자의 단위를
>    `UNIT_BY_NAME` 에 선언하지 않으면 **단위 없는 입력이 화면에 생긴다**(UI-2-AC1)
> 3. **`test_escalation_debt` 래칫이 `Load` 를 처음 보았다.** 그 래칫은 인자 이름에
>    `capex`·`replacement` 가 있는지로 대상을 가리는데 `Load` 는 그 관례를 안 따르는
>    이름을 써서 여태 **사각**이었다 — 부채가 는 것이 아니라 사각이 드러난 것이다
> 4. **`test_ledger_levels` 가 0 으로 나누고 있었다.** 배율을 「비율이 일정한가」로
>    재는데 `Q-17` 의 base 가 0 이다. **0 인 수준으로는 그 물음을 물을 수 없다** —
>    0→0 만 확인하고 비율은 0 이 아닌 수준끼리 본다
>
> ⚠ **커밋 뒤에만 되는 게이트가 하나를 더 잡았다** — `check_test_accompaniment`
> 가 `core/model/parameters.py` 를 동반 테스트 없는 변경으로 잡아(NFR-105) 일곱째
> 커밋이 됐다.
>
> ## ⑤ ⏸ 이 라운드가 **찾았으나 닫지 않은 것** (사유와 함께)
>
> - **`HeatPump.replacement_schedule()` 은 물가 계수를 아예 안 곱한다.** R42
>   이전부터 그랬고 **아무도 재지 않아 몰랐다.** `ReferencePV`(Wave 0 참조 구현)도
>   같다. 둘 다 러너 구성에 없어 결론축을 안 움직이므로 **부채로 세웠다** —
>   `KNOWN_REPLACEMENT_RATE_IGNORED`. 고치는 것은 「교체비가 물가를 타야 하는가」의
>   판단이며 `ESS` 에서는 `DV-7` 을 근거로 정했다
> - **`ESS` 최초 PCS 몫이 배터리 몫의 잔존가치에 남아 있다** — `PV` 의 인버터와
>   같은 자리이며 **`Q-2` 회신이 있어야 뗄 수 있다**
> - **`status.md` 에 R37~R35 블록이 아직 쌓여 있다.** R42 는 R41·R40 만 내렸다
>   (1,782 → 1,472줄). 이력 규칙대로면 그 넷도 `status-history.md` 로 간다
>
> ## ⑥ 실측 — 게이트 (2026-08-29 · **전건**)
>
> ```
> pytest                     rc=0   (3.13 · CI 판본 3.11 둘 다)
> ruff / mypy                rc=0   mypy 115파일   ← 「103파일」은 낡은 수였다
> lint-imports               4 kept 0 broken
> gen_traceability           rc=1(정상) 요구 105 / 수용기준 321 / 자동 290 / 수동 8 / 스텁 4
> check_task_mapping         rc=0
> 게이트 스크립트 9종         rc=0   (check_unread_extension_points 포함)
> check_file_size --code-strict  rc=0
> 음성 11종                   rc=0
> check_coverage_inputs --base HEAD~5      rc=0
> check_test_accompaniment --base HEAD~6   rc=0
> ```
>
> **변이 — 13건 전건 빨간불.** ①ESS/PCS 여섯 ②판정 대장 둘 ③계수 가르기 다섯.
>
> ## ⑦ 다음 라운드가 알아야 할 것
>
> - **`DER` 계약에 계수가 둘이다.** 새 자원을 쓸 때 교체 경로가
>   `replacement_escalation_factor()` 를 보는지 확인할 것 — 계약 스위트가 전건에
>   대해 잰다
> - **CI 가 취약점으로 빨간불이 날 수 있다.** 그때 할 일은 `pip-audit` 를 끄는 것이
>   아니라 `docs/accepted-vulnerabilities.yaml` 에 **판정을 적는 것**이다
> - 수를 인용하는 자리를 만들 때는 **다시 세어** 적을 것 — 이 파일에 옮겨 적은
>   수도 예외가 아니다
> - **남은 목록은 `todo.md` 가 갖는다**

---

> ### ⬇ **R42 가 내린 R41·R40 블록**(2026-08-29). 아래 R39 블록보다 새 것이며,
> 그래도 **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-08-29 **R41 완료 — 지어낸 범위에서 재던 검사 셋을 실물로 옮기고, 사람이 보는 리포트를 세 번의 하락에 맞췄다**

> ## ⓪ ✔ **푸시했다 · CI 둘 전건 초록불** (2026-08-29 10:03 UTC)
>
> **5건을 올렸다**(`b38ef8c..2e74698`). 사용자 답: *「push, 다음 세션 인계」*.
> **`origin/main` 과 같다** — 이어받는 세션은 `git log origin/main..HEAD` 가 비어
> 있음을 먼저 확인하면 된다.
>
> | 워크플로 | 결과 |
> |---|---|
> | `tests` (run 33246934593) | **success** (2m44s) |
> | `source-rules` (run 33246934608) | **success** (18s) |
>
> ⚠ **`setuptools 79.0.1` 의 `PYSEC-2026-3447` 은 그대로다** — CI 로그를 실제로 열어
> 확인했다(*「Found 2 known vulnerabilities in 1 package · 수정판 83.0.0」*).
> `NFR-405` 가 Should-have 라 차단하지 않으며 **`todo.md` 2번이 그것을 든다.**
>
> **워커 없이 단독으로 돌았다.** 사용자 질문 *「구매단가는 상한을 설정하여 분석할
> 수 있으면 되고, 상한으로도 경제성이 나오지 않으면 나오지 않는 것으로 기재하면
> 되지 않은가?」* 가 이 라운드의 범위를 정했다 — **그 지적이 맞았고, 리포트는 이미
> 그렇게 동작한다.** 「상한 400」은 리포트가 아니라 **테스트 전용 가짜 대장**이었다.
>
> ## ① ★ 탐침 셋 중 **구매 단가 하나만** 근거를 잃었다 — 재고 뗐다
>
> R40 이 *「무엇이 이 넓힘에 기대는지 재지 않았다」* 로 남긴 자리다. **쟀다**:
> 픽스처를 받는 검사는 **4건**(`test_case_report.py` 2 · `test_combined_scenarios.py` 2)
> 이고, 넓힘 셋은 **갈래가 둘**이었다.
>
> | 넓힘 | 갈래 | R41 판정 |
> |---|---|---|
> | 구매 단가 상단 400 | *전환* | **뗐다** — 실물이 스스로 낸다 |
> | `capex.pv.rooftop` 하단 600,000 | *회수* | **남긴다** |
> | `capex.ess.new` 하단 150,000 | *회수* | **남긴다** |
>
> **실측(실물 대장)** — 결론축이 세 번 내려간 결과다:
>
> ```
> 보조 80%   npv +1,550,325원   전환 인자 2건   5.1 단독 행 5건   결합 회수 2건
>              grid_purchase_price  임계 156.73원/kWh  (범위 80~170)
>              surplus_sale_price   임계  92.63원/kWh  (범위 80~150)
> 무보조     npv −6,289,675원   전환 인자 0건   5.1 단독 행 5건   결합 회수 0건
> ```
>
> - **검사 셋을 실물 대장으로 옮겼다** — 지어낸 범위에서 임계값을 재는 것보다 실물에서
>   재는 것이 낫고, 실물이 낼 수 있게 된 이상 넓힘을 남기면 **근거 없는 넓힘**이다
> - **넷째(`test_summary_carries_the_combined_case…`)만 탐침을 계속 받는다** — 무보조는
>   실물에서 **결합 회수 행이 0건**이라 순회할 것을 잃는다. 그것이 설비단가 하단 둘을
>   남긴 사유이며 **재서 확인했다**
> - **픽스처 이름을 고쳤다** — `flip_probe_assumptions` → `recovery_probe_assumptions`.
>   전환 갈래가 실물로 간 뒤에도 옛 이름을 두면 **다음 사람이 전환을 재는 데 이것을
>   쓴다**. 이름이 자기 쓰임보다 넓게 주장하는 형태이며 이 저장소가 열일곱 번 만났다
>
> ⚠ **옮긴 검사 하나의 사유는 두 번 낡아 있었다.** `..._when_the_table_is_not_empty`
> 는 *「실물 대장에서는 5.1 의 1변수 표가 비어 있다」* 를 사유로 들었는데 ⓐ R35 ② 가
> 5.1 을 고쳐 전환 인자 0건이어도 **거리 행**을 싣게 했고(무보조도 5행이다) ⓑ 결론축이
> 내려가 보조 80% 는 전환을 실제로 갖는다. **사유가 낡은 것을 아무도 붙들지 않았다.**
>
> ## ② 변이 셋 — **전건 빨간불이고 각각 다른 검사가 잡는다**
>
> ```
> M1  스윕이 지원 변형을 안 읽는다 (variants[PLAN_VARIANT] → metrics)
>       → 빨간불 2건  (…supported_variant_too · …actually_flips_the_conclusion)
> M2  임계값 허용오차를 구간 폭만큼 (R33 결함 재현)
>       → 빨간불 1건  (…actually_flips_the_conclusion)
> M3  5.1 단독 행에서 산출 라벨을 뗀다
>       → 빨간불 2건  (…marks_the_single_variable_table… · …when_the_table_is_not_empty)
> ```
>
> **요점은 「옮겨도 힘을 잃지 않았다」이다** — 실물 범위(구간 폭 90·70)는 탐침 범위
> (320)보다 좁은데, M2 는 좁은 구간에서도 잡힌다. 하네스는 한 번에 하나씩 돌렸고
> 셋 다 원복을 확인했다.
>
> ## ③ ✔ 사람이 보는 리포트를 다시 뽑았다 — **12일 · 세 라운드 뒤처져 있었다**
>
> `docs/evidence/MC-1-검토용-리포트-2026-08-29.md` (종전 정본은 `-08-17.md` 였고
> **R38·R39·R40 의 이동이 하나도 실려 있지 않았다**). 검토자가 먼저 보는 칸의 이동:
>
> | 1절 요약 | 08-17 | **08-29** |
> |---|---|---|
> | 결론 축 · 순현재가치 | −5,284,586원 | **−6,289,675원** |
> | 결론 전환 지원율 | 53.9% | **64.2%** |
> | 미반영 항목 | 6건 (개선 1 · 악화 1 · 방향 미측정 4) | **4건 (개선 0 · 악화 0 · 방향 미측정 4)** |
>
> - ★ **「미반영 6 → 4」가 R39-E 배선이 사람에게 처음 드러난 자리다.** 붙임 8 의
>   *「교체비 — 반영 시 결과 악화 · 미정량」* 두 행이 사라지고, 붙임 4 에 **일회성 흐름
>   표**가 실렸다(PV 교체비 13년차 913,134원 · PV 잔존가치 20년차 −1,264,378원 ·
>   ESS 교체비 18년차 7,001,207원 · ESS 잔존가치 20년차 −5,765,700원 · **순액 884,263원**)
> - 3.4 프로포마 비용 행이 *「변동 O&M · 교체 · 잔존가치 미포함」* 에서
>   *「… 일회성 PV 교체비 · PV 잔존가치 · ESS 교체비 · ESS 잔존가치 (변동 O&M 미포함)」* 로
>   바뀌었다 — **R39 잔여 ① 이 가리키는 그 문장**이며 지금도 참이다
> - **사용자 질문에 대한 답이 리포트 안에 있다**: `결론 전환 인자 (단독) | 없음
>   (1변수 스윕 범위 내)` — 무보조는 실물 범위 안에서 뒤집히지 않으며 리포트가 그것을
>   *「없음」* 으로 적고, R35 ② 가 그 옆에 **결론까지 남은 거리 · 전환 지원율**을 실었다.
>   **상한으로도 안 나오면 안 나온다고 적는다는 요구는 이미 충족돼 있다**
>
> ## ④ ✔ 「실질 추세 0」을 대장에 등재했다 (`Q-17` · R38 ② 가 남긴 ⏸)
>
> R40 이 *「아직 안 했다 — 주석이 그 가정을 지고 있을 뿐」* 으로 남긴 자리다.
> **값을 바꾸지 않았고 어느 수치도 움직이지 않았다** — 움직인 것은 *무엇이 가정인지가
> 보이는가* 뿐이다.
>
> | 자리 | 무엇 |
> |---|---|
> | `docs/assumptions.yaml` | `capex.replacement_real_trend` 신설 — 값 **0.0 %/년** · 3수준 **−2.0 / 0.0 / +2.0** |
> | spec §15.1 | **`Q-17` 신설** · 판본 **v0.17 → v0.18**. 조항을 건드리지 않았다(수용기준 321 그대로) |
> | `status-human.md` 5단계 | Q 표에 `Q-17` 행 — **`Q-2` 와 같은 업계 견적에서 받는다** |
> | `e2e_runner.py` | `PRICE_ESCALATION_RATE` 주석의 *「그 항목이 없다」* 를 실물로 고쳤다 |
>
> - ★ **3수준은 전망이 아니라 탐색 범위이며 물가 계수를 자로 삼는다.** 하단 −2.0 은
>   실질 하락이 물가 상승과 같은 크기여서 **명목 교체단가가 불변**이 되는 지점이고,
>   그것이 곧 **R40 이전 구현**이다 — 배선하면 *「물가 계수를 태우지 않았다면 얼마였는가」*
>   를 다시 잰다. 상단 +2.0 은 그 거울상이다. **어느 끝도 관측이 아니다**
> - ⚠ **방향이 한쪽으로만 열려 있다** — 배터리·인버터는 실질 단가가 내리는 것이 통상
>   관측이므로 0 은 교체비를 **과대** 계상하는 쪽이다. 「보수적이라 안전하다」로 읽지 말
>   것(ⓒ 그물이 경계하는 형태다)
> - ⏸ **아직 스윕 축이 아니다** — `ledger_levels.py` 가 이 키를 읽지 않아 5.1 에 행으로
>   나오지 않는다. 배선은 다음 라운드 몫이다
>
> ## ⑤ ★★ 이 라운드가 **두 번 걸렸다 — 둘 다 게이트가 잡았다**
>
> 1. **`DV-7` 계약이 내 항목을 거부했다.** `value_unit` 을 *「%/년 (물가를 뺀 실질
>    기준)」* 으로 적었더니 `assert_basis_is_declared_once` 가 *「대장 항목이 가격 기준을
>    다시 선언했습니다」* 로 막았다 — **R31 이 `escalation.electricity_tariff` 에서 지운
>    것과 같은 자리**다. 단위에서 기준을 빼고 「물가를 뺀」은 **제목과 적용범위**가 지게
>    했으며, 그 사유를 항목 주석에 적었다(이 항목만은 그 한정을 잃으면 물가 상승률로
>    오독되어 이중 계상이 된다)
> 2. **키 접두사가 무리를 잘못 태웠다.** `escalation.` 으로 두었더니 붙임 6 이
>    **「요금 · 정산 단가」** 무리에 실었다 — 설비 단가를 찾는 검토자가 못 보는 자리다.
>    `capex.replacement_real_trend` 로 고쳐 **「설비 단가 8건」** 에 실었다.
>    ★ **실물 리포트를 뽑아 눈으로 읽지 않았으면 못 봤다** — 인계가 *「작업 뒤에는
>    반드시 실물을 다시 뽑고 눈으로 읽는다」* 고 적은 이유가 이것이다
>
> ⚠ **그리고 한 번은 내가 규약을 어겼다** — `pytest` 가 도는 중에 `ruff` 를 함께
> 돌렸더니 음성 검사가 잠깐 만드는 `core/der/temp_acceptance2_bad_import.py` 를 잡아
> `rc=1` 이 났다. **게이트를 겹쳐 돌리지 말 것**(인계 머리의 그 줄이다). 끝난 뒤 다시
> 돌려 `rc=0` 을 확인했다.
>
> ## ⑥ 실측 — 게이트 (2026-08-29 · **전건**)
>
> ```
> pytest -q                       rc=0   1,643건 수집 · 실패 0 (R40 과 같다)
> ruff check core tests scripts app infra web   rc=0   (pytest 종료 후 재실행)
> mypy                            rc=0   116파일 무결
> lint-imports                    rc=0   4계약 kept 0 broken
> check_file_size --code-strict   rc=0     check_marker_substance     rc=0
> check_hardcoded_params          rc=0     check_source_rules         rc=0
> check_partition_assignment      rc=0     check_disclosure           rc=0
> check_assumptions               rc=0     check_task_mapping         rc=0
> check_precommit_installed       rc=0
> gen_traceability.py             자동 290 · 수동 8 · 스텁 4 · 미매핑 9(전건 Phase 2·3)
>                                 → **R40 과 같다**(`docs/traceability.md` diff 0).
>                                    이 라운드는 조항을 건드리지 않았다
> negtest_*.py                    11종 전건 rc=0 (실물 소스에 잔여 변이 없음)
> Python 3.11 (CI 판) pytest -q   rc=0   실패 0
> ```
>
> **커밋 뒤에만 되는 것 셋** (`2ebb2be..4f5af15` · `--base HEAD~4`):
>
> ```
> check_coverage_inputs      rc=0   측정 230파일 / 변경 core 1건
> check_test_accompaniment   rc=0   구현 변경 1건 전건 동반
> diff-cover --fail-under=95 rc=0   **「대상 줄 없음」**
> ```
>
> ⚠ **`diff-cover` 의 「대상 줄 없음」을 통과로 읽어도 되는 이유가 따로 있다.**
> 그 도구는 판정할 수 없을 때도 `rc=0` 을 낸다 — 그래서 입력 검증
> (`check_coverage_inputs`)이 **먼저** 온다(R40 이 낡은 `coverage.xml` 로 이 자리를
> 밟았다). 이번에 줄이 없는 것은 **이 라운드의 `core/` 변경이 주석뿐**이기
> 때문이며, 앞선 검사가 그 파일이 커버리지 산출물에 있음을 확인했다.
> `coverage.xml` 은 이 라운드에서 다시 뽑았다(저장소에 추적되지 않는다).
>
> ## ⑦ 다음 라운드가 알아야 할 것
>
> - **결론축 −6,289,675원 · 결손/총사업비 64.2%** — R40 과 같다. 이 라운드는 **어느
>   수치도 움직이지 않았다**(탐침 이동·가정 등재 둘 다 값을 바꾸지 않는다)
> - **spec 은 v0.18 이다.** 수용기준 321 · 조항 불변
> - **사람이 보는 리포트 정본은 `MC-1-검토용-리포트-2026-08-29.md` 다.** 옛 두 판은
>   지우지 않았다 — 세 판을 나란히 놓으면 결론축이 어떻게 내려왔는지가 보인다
> - **남은 목록은 `todo.md` 가 갖는다**

> # ⏹ 2026-08-29 **R40 완료 — R39 가 일부러 남긴 한 줄을 골든 재산출과 한 단위로 닫았다**
>
> **워커 없이 단독으로 돌았다.** `todo.md` 1번(ESS 교체비 물가 계수 + 골든 재산출)이
> 이 라운드의 전부이며, R39 가 *「갈라 가면 기준선을 두 번 흔든다」* 로 묶어 둔
> 그대로 **한 커밋 단위**로 갔다.
>
> ## ⓪ ✔ **푸시했다 · CI 둘 전건 초록불** (2026-08-29 07:36 UTC)
>
> R39 가 남긴 8건 + R40 의 6건 = **14건을 한 번에 올렸다**(`b74adf7..6a9315b`).
> 사용자 답: *「푸쉬, 다음 세션 인계해줘」*. **`origin/main` 과 같다** — 이어받는
> 세션은 `git log origin/main..HEAD` 가 비어 있음을 먼저 확인하면 된다.
>
> | 워크플로 | 결과 |
> |---|---|
> | `tests` (run 33241256961) | **success** |
> | `source-rules` (run 33241257048) | **success** |
>
> ⚠ **CI 로그에 차단하지 않는 경고 둘이 있다 — 새로 생긴 것이 아니다.**
> ⓐ `setuptools 79.0.1` 의 `PYSEC-2026-3447`(수정판 83.0.0) — `NFR-405` 가
> Should-have 라 차단하지 않는다. **`todo.md` 에 등재했다** ⓑ `actions/checkout@v4`·
> `setup-python@v5` 의 Node 20 지원 종료 안내.
>
> ## ① 결론축이 **예고된 값 그대로** 움직였다 (예측 대조가 맞았다)
>
> `core/der/ess.py::_acquisitions()` 가 배터리 재취득분에 `escalation_factor()` 를
> 곱하지 않고 있었다. 대장이 `price_basis: "명목"` 을 **한 번** 선언하므로(`DV-7`)
> 18년차 지출을 오늘의 원으로 적으면 **그 지출만 실질이 된다.**
>
> | | R39 예고 | **R40 실측** |
> |---|---|---|
> | 무보조 `npv` | −6,289,675원 | **−6,289,675원** ✔ 일치 |
> | 결손/총사업비 | 64.2% | **64.2%**(6,289,675 / 9,800,000) ✔ |
>
> **골든 여섯 값 중 셋이 움직였다** — 세 시나리오의 `npv_won` 이 **각각 −222,794원**
> 이고(무보조 −6,289,675 · 보조 20% **−4,329,675** · 보조 80% **+1,550,325**),
> `payback_period_years` 는 **셋 다 그대로**다. 움직인 두 흐름(18년차 교체비 ·
> 20년차 잔존가치)이 전부 회수 시점(보조 80% 는 6.5074년) **뒤**에 있기 때문이며,
> 그것이 이 라운드가 회수기간을 **안 건드린 것이 아니라 재고 안 움직인 것**임을
> 말한다. 값은 손으로 적지 않고 `_scenario_metrics()` 를 돌려 옮겼다(R39-F 가
> 골든을 실행 경로에 붙여 둔 덕이다).
>
> ## ② ★★ 래칫이 **한 항만 고쳐도 초록불이 되는 모양**이었다
>
> `KNOWN_ESCALATION_IGNORED_IN_REPLACEMENT` 는 esc=0.02 와 esc=0.0 의
> `replacement_schedule()` **사전 전체**를 견줬다. 그런데 `ESS` 의 그 사전에는 항이
> **둘**(배터리 · PCS)이고, **한쪽만 계수를 받아도 사전은 달라진다** — 즉 배터리만
> 고치면 래칫은 「다 됐다」고 말하는데 **PCS 교체비는 여전히 오늘의 원**이다.
> R39 가 *「래칫이 「다 됐다」고 말하면서 하나도 안 되어 있을 수 있다」* 로 경고한
> 형태가 **한 칸 옮겨 그대로 남아 있었다.**
>
> 그래서 셋을 했다:
>
> 1. **PCS 교체비에도 계수를 굴렸다**(`replacement_schedule()`). 배터리 쪽은
>    `_acquisitions()` 가, PCS 는 그 함수가 곱한다 — 한쪽만 굴리면 **같은 해의 지출
>    둘이 서로 다른 가격 기준으로 한 칸에 합산**되고 그 합계는 어느 기준으로도
>    읽히지 않는다
> 2. 래칫을 **해마다** 견주게 고쳤다(사전 전체 → 연도별). 한 항만 굴리는 구현이
>    **그 항만 있는 해**에서 드러난다
> 3. **PCS 를 준 `ESS` 를 탐침에 세웠다** — 그 파일 머리 ③ 이 *「PCS 를 준 ESS 는
>    재지 않았다 — 확인 못 함」* 으로 적어 둔 자리이며, 확인 못 한 것을 **확인한
>    것으로** 바꿨다(ⓓ 한 갈래만이다 — ⓐ~ⓒ·ⓔ 는 여전히 그 괄호 그대로)
>
> ⚠ **러너의 `ESS` 는 PCS 가 없다** — 그러므로 위 2·3 은 **결론축을 움직이지 않는다.**
> 「고쳤더니 수가 안 움직였다」가 아니라 **실행 경로에 그 갈래가 없다**는 뜻이다.
>
> ## ③ 변이 — **둘 다 빨간불이고 서로 다른 검사가 잡는다**
>
> ```
> M1  _acquisitions() 의 계수를 뗀다   → 래칫 1건 + 골든 3건 빨간불
> M2  PCS 의 계수만 뗀다               → 래칫 1건 빨간불 (`ESS(+PCS)` 가 목록에 뜬다)
> ```
>
> **M2 가 이 라운드의 요점이다** — 고치기 전 판의 래칫으로는 M2 가 **초록불**이었다.
>
> ## ④ 낡은 문면 넷을 함께 옮겼다 — **수가 움직이면 그 수를 인용한 자리가 낡는다**
>
> | 자리 | 무엇이 낡았나 |
> |---|---|
> | `core/casegrid/lifecycle.py` | *「`ESS` 는 곱하지 않는다 … 오늘의 원으로 남는다」* 가 **이 라운드로 거짓**이 됐다. 「그래도 산식 문면에 계수를 적지 않는다」는 결론은 **사유를 바꿔** 유지했다 — 셋째 자원이 들어오는 날 조용히 거짓이 되기 때문 |
> | `tests/golden/test_regression_scenarios.py` | `variants` 세 수(**실측으로 다시 쟀다**) · 모듈 독스트링의 결론축 |
> | `tests/report/conftest.py` | 탐침 여유 실측 셋. **동반 하락 +817,143 → +750,305원**(여유가 66,838원 깎였다). ⏸ 그리고 **구매 단가 상단 400 의 원래 사유가 무너졌다** — 아래 ⑤ |
> | `status-human.md` 1단계 | 사람에게 보이는 결론이 **−865,881원**(2026-08-15 값)에 멈춰 있었다. 세 번의 하락을 함께 적었다 |
>
> ## ⑤ ⏸ 이 라운드가 **찾았으나 닫지 않은 것** (사유와 함께)
>
> - **`tests/report/conftest.py` 의 「구매 단가 상단 400」이 근거를 잃었다.** 그 넓힘의
>   사유는 *「실물 상단 170 으로는 어느 인자도 보조 80% 의 결론을 뒤집지 못한다」*
>   였는데, 결론축이 두 번 내려가 **실물 대장에서도 뒤집힌다**(실측: 보조 80%
>   +1,550,325원 · `grid_purchase_price` 상단 170 → **−559,849원** · 임계 156.73원/kWh).
>   **떼지 않았다** — 무엇이 그 넓힘에 기대는지 재지 않았고, 재지 않고 떼면 다른
>   검사가 **빨간불이 아니라 「순회 0회」**로 죽는다
> - **`ESS` 는 PCS 취득분을 잔존가치에서 세지 않는다.** `_acquisitions()` 에 PCS 가
>   아예 없어 **교체비는 계상되는데 그 잔존가치는 없다** — ⓒ 그물이 경고한 *「결론을
>   한 방향으로만 나쁘게 만들어 「보수적이라 안전하다」로 읽히는」* 형태다. 러너에
>   PCS 가 없어 지금 수는 안 움직인다
>
> ## ⑥ 실측 — 게이트 (2026-08-29 · **전건**)
>
> ```
> pytest -q                       rc=0   1,643건 수집 · 실패 0
> ruff check core tests scripts app infra web   rc=0
> mypy                            rc=0   115파일 무결
> lint-imports                    rc=0   4계약 kept 0 broken
> check_file_size --code-strict   rc=0     check_marker_substance     rc=0
> check_hardcoded_params          rc=0     check_source_rules         rc=0
> check_partition_assignment      rc=0     check_disclosure           rc=0
> check_assumptions               rc=0     check_task_mapping         rc=0
> check_precommit_installed       rc=0
> gen_traceability.py             자동 290 · 수동 8 · 스텁 4 · 미매핑 9(전건 Phase 2·3)
>                                 → **R39 와 같다**(파일 diff 0). 이 라운드는 조항을
>                                    건드리지 않았다
> check_coverage_inputs --base HEAD~4   rc=0   측정 230파일 / 변경 core 2건
> diff-cover --fail-under=95            rc=0   **변경 2줄 · 100%**
> check_test_accompaniment --base HEAD~4 rc=0  구현 변경 2건 전건 동반
> ```
>
> ⚠ **게이트 ①은 `coverage.xml` 을 먼저 다시 뽑아야 돌아간다.** 그 파일은 저장소에
> 추적되지 않고 로컬 것은 08-17 판이라 **R39 가 만든 `core/casegrid/lifecycle.py` 를
> 담고 있지 않았다** — 그 상태의 `check_coverage_inputs` 는 `rc=2` 이며 **그것이
> 옳다**(diff-cover 는 판정할 수 없을 때 **rc=0 을 낸다**. 입력 검증이 먼저 오는
> 이유가 그것이다). `pytest --cov=core --cov-report=xml` 을 배경으로 돌린 뒤 위 수를
> 얻었다.
>
> ## ⑦ 다음 라운드가 알아야 할 것
>
> - **결론축 −6,289,675원 · 결손/총사업비 64.2%.** 이 수를 인용하는 자리를 만들 때
>   **다시 세어** 적을 것 — 이 파일에 옮겨 적은 수도 예외가 아니다
> - **`todo.md` 1번은 닫혔다.** 남은 목록은 `todo.md` 가 갖는다
>

> ### ⬇ **R41 이 내린 R39 블록**(2026-08-29). 아래 R38 블록보다 새 것이며,
> 그래도 **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-08-29 **R39 완료 — 다섯 라운드가 미룬 배선을 닫고, 골든을 실물 모양으로 옮겼다**
>
> R39 는 워커 다섯으로 열려 넷을 인수하고(`41997e4`·`f456cb7`·`7d031cd`·`b764521`)
> **배선(E)을 미커밋·빨간불 7건 상태로 남긴 채 중단**됐다. 이 세션이 그 인계를 받아
> **E2·F·G 를 닫았다.** 경위·설계 근거는 `.orch/R39/` 가 갖는다(⚠ `.gitignore` 안이라
> 다른 기계에는 없다 — **아래 요약이 정본**이다).
>
> ## ⓪ ⛔ **먼저 확인할 것 — 커밋은 있고 푸시는 없다**
>
> ```
> 803d73e  R39 를 인계 문서에 적는다
> d1d8bc6  조항을 재지 않는 인용 하나를 뗀다
> 4afef1e  골든이 재던 모양은 실행 경로에 없는 것이었다
> c42c78f  교체비·잔존가치를 실행 경로에 배선한다
> ```
>
> **`origin/main` 대비 8건 앞선다**(R39 레인 4 + 위 4). **CI 는 이 코드를 본 적이
> 없다** — 로컬 게이트만 전건 통과했다. 이어받는 세션은 `git log origin/main..HEAD`
> 로 먼저 확인하고, **푸시는 사용자에게 확인한 뒤** 한다(이 저장소의 관례 —
> R35 도 *「사용자 답: 지금 push」* 를 기록해 두었다).
>
> ⚠ **다른 기계라면 여기까지가 전부다** — `.orch/R39/` 의 상세 기록
> (`result_e2_f_g.md` · `result_replacement_wiring.md`)은 `.gitignore` 안이라
> 따라오지 않는다. **이 블록이 정본이다.**
>
> ## ① 빨간불 7건을 닫았다 — **하나는 오라클이 아니라 구현이 틀렸다**
>
> | 무엇 | 판정 |
> |---|---|
> | `PV._acquisitions()` 가 `retire` 를 안 본다 | ★ **구현 결함.** `replacement_schedule()` 은 `retire` 에서 비는데 이쪽은 인버터 재취득분을 세어 **사지도 않은 설비의 잔존가치**를 붙였다(20년차 900,000 → 1,185,354원). 오라클을 고쳐 통과시키지 않았다 |
> | `RC-ALL-C5` 명목·할인 오라클 2건 | **오라클 이동.** 900,000 → **1,185,354** · 373,179 → **491,499**. 조항의 수(본체 몫)를 지우지 않고 `RC_ALL_C5_BODY_ONLY_WON` 으로 남겨 **두 항을 갈라 단언**한다 |
> | 「수명 다한 자원의 잔존가치 0」 | **전제가 좁았다.** 「다 썼다」는 **재취득이 없을 때만** 자원 전체에 성립한다. `retire`(0원)와 `replace`(재취득분 남음)를 갈라 재고, 원래 걱정인 **음수 금지**는 40년 전 구간에 그대로 건다 |
> | `KNOWN_ESCALATION_DEBT` | **래칫이 설계대로 동작했다**(줄어도 빨간불). 두 항을 **함께** 비웠다 — 갈라 닫으면 같은 항목이 자원마다 다른 가격 기준으로 선다 |
> | 리포트 2건 | 하나는 **배선으로 전제가 깨진 것**(3.4 가 계상된 항목을 「하지 않은 것」으로 계속 인쇄), 하나는 **탐침이 죽은 것** |
>
> ## ② ★★★ 이 라운드의 요점 — **변이가 그물을 하나 더 만들게 했다**
>
> 새 검사 파일(`tests/casegrid/test_lifecycle_wiring.py`)을 넷(ⓐ~ⓓ)으로 세우고
> 인계가 지정한 변이 넷을 심었다. **첫 변이가 ★초록불★ 이었다** —
> `e2e_runner` 의 `cost_rows` 에서 `*lifecycle_rows,` **한 줄을 빼도 아무도 잡지 않았다.**
>
> 그 줄이 없어도 `one_off_flows` 는 `CaseBasis` 에 그대로 실리므로 **붙임 4 는 교체비
> 표를 인쇄하고 붙임 8 은 「계상됐다」고 판정하는데 NPV 에는 들어가지 않는다.** 러너에
> 자리가 둘(프로포마 행 · 표시용 흐름)이라는 사실 자체가 구멍이며,
> `test_grid_purchase_cost_row.py` 가 전력 구매 행에서 만난 것과 **같은 형태**다.
>
> → 다섯째 그물 ⓔ 를 세웠다: `_lifecycle_rows` 를 갈아 끼워 두 번 돌리고 **두 NPV 의
> 차이가 붙임 4 가 인쇄하는 흐름의 현가와 같은가**를 본다(실측 436,309원 · 1원 허용).
> 변이 넷이 그 뒤 **전건 빨간불**이며 **각각 다른 검사**가 잡는다(경로를 따로 붙든다).
>
> ## ③ ★★ 그 그물이 **같은 결함을 `ESS` 에서 즉시 잡았다**
>
> ⓒ(잔존가치가 세는 취득분 = 교체 스케줄)를 세우자 **`ESS._acquisitions()` 도
> `retire` 를 보지 않는다**가 첫 실행에서 걸렸다. `PV` 에서 고친 것과 **같은 한 줄**이며,
> 같은 형태가 자원마다 따로 있었다는 뜻이다. 러너의 ESS 는 `replace` 라 **지금 수는
> 움직이지 않는다.**
>
> ## ④ 골든 3종을 **실행 경로 모양으로 옮겼다** (F · `todo.md` 1번 · 다섯 라운드 미룸)
>
> 옮기는 조건이 *「비용 행 구성이 확정된 뒤 **한 번**」* 이었고 E 의 배선으로 갖춰졌다.
> `_scenario_metrics()` 가 **`build_case_report()` 를 부른다** — 손으로 행을 세우지
> 않는다(그러면 실행 경로가 바뀔 때 또 갈린다). 여섯 값이 전부 바뀌었다:
>
> | 시나리오 | `npv_won` | `payback_period_years` |
> |---|---|---|
> | 무보조 | +2,270,362 → **−6,066,881** (부호 뒤집힘) | 10.4989 → **`.inf`** |
> | 보조 20% | +4,170,362 → **−4,106,881** | 8.3991 → **`.inf`** |
> | 보조 80% | (옛 값) → **+1,773,119** | → **6.5074** |
>
> - ★ **주 지표의 정체가 바뀌었다** — `payback_period_years` 는 종전 `payback_simple`
>   이었고 지금은 실행 경로의 **`payback_discounted`** 다. 같은 이름, 다른 수다
> - ★ **미회수는 `.inf` 로 싣는다.** `null` 로 두면 `_expected_values()` 가 걸러
>   **그 지표가 조용히 대조에서 빠진다**(인계가 경고한 자리 — 실측으로 확인했고
>   `pytest.approx(inf)` 는 `inf` 에만 참이다). 변이 3건 전건 빨간불
> - ✔ **R39-E 가 「확인 못 함」으로 남긴 것을 닫았다** — 보조 20/80 이 `metrics` 에
>   이미 반영돼 있다(`variants["as_planned"]` 와 같은 수). 그래서 골든이 지원을
>   **다시 계산하지 않는다**
> - **`oracle_rank`·`oracle_source` 는 고치지 않았다** — 정박은 `Q-4`·`Q-5` 회신까지
>   유예돼 있고 **유예를 우리가 풀지 않는다**
>
> ## ⑤ 배선이 만든 게이트 위반 하나 — **아무도 보지 않고 있었다**
>
> 배선으로 `e2e_runner.py` 의 **코드 줄이 519**가 되어 `NFR-206` 「코드 스프롤」에
> 걸렸다(`check_file_size --code-strict` = CI 차단). R39 의 어느 결과 파일에도 없다.
> **상한을 고치거나 근거 주석을 지우지 않고** `_lifecycle_rows` 를
> `core/casegrid/lifecycle.py` 로 갈라냈다 — 러너의 다른 부분과 달리 자원 둘과
> 분석기간만 받으므로 **경계가 이미 있었다.**
> ⚠ 러너는 그것을 **`e2e_runner._lifecycle_rows` 라는 이름으로 계속 부른다** — ⓔ 가
> 그 이름을 갈아 끼워 재기 때문이다. 이름을 옮기면 그 그물이 조용해진다.
>
> ## ⑥ G 후속 — 거짓 인용 1건을 뗐다
>
> `tests/der/test_heatpump.py::test_annual_operation_enforces_one_based_year` 의
> `FR-101-AC2` 마커를 **함수 이름 앵커로** 제거했다(문자 치환 금지 — R38 이 라운드를
> 구한 자리). 그 검사가 재는 것은 §7.5 `Year` 1-base 규약이고 **옮길 조항이 없다.**
> 집계는 움직이지 않았다(자동 290 · 미매핑 9 — G 의 예측대로).
>
> ## ⑦ ⚠ 브리프의 처방 하나가 **조항과 부딪혔다**
>
> 인계는 *「`retire == replace` 단언이 빨간불로 남으면 `xfail` 에 사유를 적어라」*
> 였는데, **`tests/der/` 는 DoD 8 검사가 `skip`·`xfail` 을 금지하는 구획**이다
> (`test_17_8_dod8.py` — 「케이스가 있다」와 「실행·통과한다」가 같아야 한다).
> 그래서 **부채 래칫**으로 대신 고정했다 — 갈리는 크기(285,354원)와 그 출처를 못 박고,
> 조항이 개정되면 빨간불이 되게 했다. **사람 몫은 `status-human.md` 7단계에 등재했다.**
>
> ## ⑧ 실측 — 게이트 (2026-08-29 · **전건**)
>
> ```
> pytest -q                       rc=0   1,643건 수집 · 실패 0
> ruff check core tests scripts app  rc=0
> mypy                            rc=0   115파일 무결
> lint-imports                    rc=0   4계약 kept
> check_file_size --code-strict   rc=0   ← 코드 스프롤 0건 (⑤ 로 닫았다)
> check_marker_substance          rc=0
> check_hardcoded_params          rc=0
> check_source_rules              rc=0
> check_partition_assignment      rc=0
> check_disclosure                rc=0
> check_assumptions               rc=0
> check_task_mapping              rc=0
> check_precommit_installed       rc=0
> gen_traceability.py             자동 290 · 미매핑 9(전건 Phase 2·3) · 차단 미수행 1(MC-1)
> ```
>
> 변이 **7건 전건 빨간불** — 배선 4(M1~M4) · 골든 3(G1~G3). `.mutbak` 잔여 0.
>
> ## ⑨ 다음 라운드가 알아야 할 것 — **결론축이 움직였다**
>
> - **무보조 결론축 −5,284,586 → −6,066,881원**(결손/총사업비 **61.9%**). 리포트·
>   status 의 옛 수를 인용하는 자리가 있으면 이 수로 읽을 것
> - ⏸ **`core/der/ess.py` 한 줄이 남아 있다** — `_acquisitions()` 가 교체비에 물가
>   계수를 곱하지 않는다(R38 판정 ②나). 고치면 결론축이 **−6,289,675원**(64.2%)로
>   가고 **골든을 또 재산출**해야 한다. 그래서 **골든 재산출과 한 단위로 묶어야 하며**
>   그 사실을 `test_lifecycle_wiring.py` 의 ⓓ 래칫이 목록으로 들고 있다
> - `tests/report/conftest.py` 의 **탐침을 한 번 더 넓혔다**(800,000/200,000 →
>   600,000/150,000). 결론축이 나빠져 「결합에서만 회수」가 죽었기 때문이며, 그 탐침이
>   죽는 방식은 **빨간불이 아니라 「순회 0회」** 라서 더 나쁘다
> - 남은 목록은 `todo.md` 가 갖는다
>

---

> ### ⬇ R32 가 닫은 뒤에도 라운드가 이어졌다 — **아래 둘은 R40 이 `status.md` 에서
> 내린 R38 블록이다**(2026-08-29). 이 파일의 나머지(R0~R31)보다 **새 것**이며,
> 그래도 **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-08-20 **R38 ② 완료 — 「무엇을 고칠까」보다 「무엇을 재고 있나」를 물은 라운드**
>
> 사용자가 **6시간 부재**를 알리며 *「사람이 확인해야 하는 사항은 합리적으로 판단해
> 확인한 것으로 처리하고 계속 진행」* 하라 지시하고, 이어서 *「에이전트별 할당량이
> 소진되지 않도록 유의하면서 필요 시 clear 하며 5시간 진행」* 을 목표로 걸었다.
> herdr `dev` 세션에서 **워커 다섯 + 오케스트레이터**로 돌았고 **16구획 인수 · 1구획
> 반려**다. 지시문·산출물은 `.orch/R38/`(`.gitignore` 안). **대신 내린 판정은 사유와
> 함께 적는다 — 되돌리려면 사유를 반박한다.**
>
> ## ⓪ 먼저 — **main 에 안 들어간 커밋을 회수했다** (`aaed2ae`, 이미 푸시·CI 초록불)
>
> `git cherry main r38-payback` 이 미머지 커밋을 드러냈고 **main 쪽 독스트링이
> 실측과 달랐다.** 손계산으로 검산했다(할인율 4.5% · initial 3,000,000):
> Y1 편익 현가 2,870,813 < 3,000,000 이라 **Y1 에서 0 선을 넘지 못한다** — Y2 편익
> 행에서 넘어 **1.7437년**을 반환하고 연 단위 참값은 **3.2994년**이다. 커밋과 일치.
> **R37·R38 ① 이 각각 「선언과 구현이 갈렸다」를 결함으로 잡은 직후에, 그 형태가
> 문면 쪽에 남아 있었다.**
>
> ⚠ **`r38-payback` 브랜치는 이제 완전히 뒤처졌다** — 전체 diff 를 읽어 확인했다.
> main 보다 가진 것은 그 커밋 하나뿐이고 나머지는 main 의 더 새 작업을 되돌리는
> 방향이다. **「미머지 브랜치」로 오해하지 말 것**(`git cherry main r38-payback` 이
> 비면 완전히 흡수됐다).
>
> ## ① 무엇을 재고 있었나 — **세 구획이 「고칠 것이 없다」를 실측으로 확정했다**
>
> | 구획 | 물음 | 답 |
> |---|---|---|
> | 얇은 리프 | 남은 미검증 항목을 세워라 | **검사 0건이 정답** — R37 이 이미 세웠다. 브리프의 「격차표를 믿지 말고 세라」가 값을 냈다 |
> | 골든 | 비용 행 있는 구성으로 옮길 것인가 | **옮기지 않는다**(판정). 6조합 실측 |
> | DoD 7 | 인수 검사가 허구 모양을 물려받나 | **충족** — 결함을 지어내지 않았다 |
> | 비교표 | 두 구조가 다른 줄로 나오나 | **대상아님** — `FR-202` 의 축은 모델 구성이고 계약 구조가 아니다(spec §16.3 이 `WP-9` 에 배정) |
>
> ### ★★ 골든이 재는 것은 **실행 경로에 없는 모양**이다
>
> 골든이 **단언하는 지표는 둘뿐**(`npv_won` · `payback_period_years`)이고 그것은
> **`payback_simple`** 이다 — **R38 ① 이 고친 주 지표 `payback_discounted` 는 골든의
> 단언에 아예 없다**(`irr`·`mirr` 도 없다). 그리고 **(가)에서는 행 단위·연 단위가
> 「정의상」 같은 값을 낸다** — 한 해에 행이 하나면 `_flatten()` 20항 = `_by_year()`
> 20항. R38 이전 구현을 재현해 6조합을 재니 **전건 「같음」**.
> **값이 안 움직인 것은 우연이 아니라 필연이었다.**
>
> (나)로 옮기면 6조합 전부 갈리고 yaml 6개 값 전부 바뀐다 — `npv` 는 세 시나리오 모두
> 정확히 **−2,836,243원**, **무보조는 부호가 뒤집힌다**(+2,270,362 → −565,881),
> `payback_discounted` 는 **회수 → 미회수**.
>
> **옮기지 않은 사유**: ⓐ 측정한 (나)는 **비용의 하한**이다(`GridPurchase`·정산 수수료
> 행을 못 채웠다) → 지금 옮기면 **「옛 허구」를 「새 허구」로** 바꾼다 ⓑ 기준선을
> 흔드는 것은 한 번만 해야 하고 그때 행 구성이 확정돼 있어야 한다 — **교체비·잔존가치
> 배선이 비용 행을 더 늘린다.** 조건을 `todo.md` 1번에 못 박았다.
> **골든 파일·yaml 머리글에 「이 기준값은 실물 산출물의 모양이 아니다」와 옮기는 조건을
> 적었다**(값은 한 자리도 안 건드렸다 — `yaml.safe_load` 덤프 대조로 검산).
>
> ## ② 교체비·잔존가치 — **인계에 적힌 수가 틀려 있었다**
>
> | | 정찰이 적은 값 | **실물** |
> |---|---|---|
> | 순현재가치(무보조) | −5,156,392원 | **−5,284,586원** |
> | 총사업비 | (없었다) | **9,800,000원** |
> | 결손/총사업비 | 52.6% → 「약 59.5%」 | **53.9%** → **60.8%**(현재 코드 갈래) |
>
> `todo.md` 2번이 *「다시 세야 한다」* 고 경고한 그 수다. 출처는
> `docs/evidence/MC-1-검토용-리포트-2026-08-17.md`(:20·:117·:118·:161)이고 보조
> 20%·80% 는 **산식으로 독립 재현**해 교차 확인했다. 순효과 −673,848원이 그대로
> 성립한다는 주장은 **코드로 검산**했다(일사 배선이 `replacement_schedule`·
> `salvage_value`·`capex` 근처에 참조 0곳).
>
> **갈래 판정 둘**(크기는 `todo.md` 2번 표):
> **④ PV 인버터 취득분의 잔존가치도 센다** — spec 1918행이 「어느 취득가」를
> **말하지 않고**(침묵) ESS 는 그 침묵을 「취득 시점 리셋」으로 메우며 **사유를
> 독스트링에 적어 두었다. 정본이 이미 있고 PV 가 안 따랐다.**
> **② (나) ESS 교체비도 물가 계수를 태운다** — 대장이 `price_basis: "명목"` 을 1회
> 선언하고 `DV-7` 이 강제한다. 명목 프로포마에서 18년차 지출을 오늘의 원으로 적으면
> **그 지출만 실질이 되어 선언과 어긋난다.**
> **⏸ ③(시스템 전체냐 배터리만이냐)은 대신 정하지 않는다** — `Q-2` **외부 회신**에
> 묶였다. *「정박점을 우리가 만들면 자기충족 검증이 된다」*(`Q-4`·`Q-5` 원칙).
> **판단하면 안 되는 것을 식별한 것**이며 판단을 미룬 것이 아니다.
>
> ## ③ ★★★ 이 라운드의 발견 — **명목 기준이 두 자리에서 깨져 있고, 상수 이름이 좁게 주장한다**
>
> ```
> e2e_runner.py   PV_ESCALATION_RATE = 0.02   (옛 이름 PV_OM_ESCALATION — 「O&M」이라 주장했다)
> ⚠ **이름이 한 번 더 움직였다 (R39-E): `PRICE_ESCALATION_RATE`** — 자원의 속성이
>   아니라 사업 전제이며 `PV`·`ESS` 와 프로포마 행 넷이 함께 쓴다. 위 블록은
>   R38 시점의 문면이다.
>   → pv.py 의 세 자리를 함께 굴린다: 고정 O&M · 변동 O&M · **교체비 capex**
>   → ESS 는 받지 않는다 → 18년차 배터리 교체비가 **오늘의 원**으로 적힌다
> ```
>
> **이것은 이 저장소가 열여섯 번 만난 결함의 거울상이다** — 지금까지는 전부 **검사가
> 실제보다 넓게 주장하는 것**이었는데 이것은 **상수가 좁게 주장한다.** 그래서
> 「교체비에 어떤 계수를 쓰는가」를 물어도 **아무도 이 자리를 보지 않는다.**
>
> **그리고 자리가 둘이었다** — 검증자가 갈랐다:
> ① 자원 생성자의 계수는 `replacement_schedule()`·`variable_om()` 에만 닿는다
> (`fixed_om(year=1)` 은 지수가 0 이라 기준액에 영향이 없다) ② **고정 O&M 의 물가는
> `fixed_om_row` 자신이 직접 굴린다.** **다음 라운드가 ESS 자원에 계수를 배선해도
> `ESSFixedOM` 행은 그대로 `0.0` 이다** — 래칫이 「다 됐다」고 말하면서 절반만 된다.
> → `tests/contract/test_escalation_debt.py` 의 `KNOWN_ESCALATION_DEBT` 를
> `("resource","ESS")`·`("row","ESSFixedOM")` **두 항**으로 세웠다(양방향 · 늘거나
> 줄면 빨간불). **배선하지 않았다 — 수를 하나도 움직이지 않았고 그것이 자기검산이다.**
>
> ⚠ `GridPurchase`·`fee_row` 는 **의도적으로** 계수가 없다(검증자가 내 가설을 기각했다) —
> 요금 인상률은 비용·편익 양쪽에 동시에 실려야 NSPM 대칭이 맞고, 수수료는 요금 대비
> 비율이라 저절로 따라 오른다. **기존 「미해결」의 `tariff_escalation` 항목과 같은 것**이다.
>
> ## ④ 조항 인용을 실물에 맞췄다 — **거짓 인용 · 산식 이관 16건 · 빈 Must-have**
>
> ### ⓐ 거짓 인용이 **검토용 리포트에 실려 나갔다**
>
> 잔존가치는 `FR-104-AC5` 인데 `core/report/unreflected.py` 가 **`AC4`**(부속설비 독립
> 수명)로 적고 있었고, `core/report/narrative.py` 가 그것을 붙임 8 에 렌더한다 —
> **`docs/evidence/MC-1-검토용-리포트-2026-08-15.md:551-552` · `-2026-08-17.md:577-578`**
> 에 남아 있다. **검토자가 조항을 찾아가면 「인버터 부속설비 수명」이 나온다.**
>
> > **★★★ 이 라운드에서 가장 값 있는 한 줄**: 「세 곳이 서로 어긋난 것이 아니라
> > **코드와 인계 문서가 같은 방향으로 함께** 틀려 있었다 — 그래서 둘만 대조하면
> > 「일치한다」로 보인다. **spec 실물이 유일한 정본이었다.**」
>
> 새 검사가 **spec 이 조항 소유자를 정하게** 한다(`scripts/_specparse.py` 를 **재사용** —
> 두 번째 파서를 세우면 정본이 둘이 된다). ⚠ **날짜 붙은 증거 리포트는 고치지 않았다** —
> *「그 날 실제로 나간 문면이 그것이며 소급 수정하면 증거가 증거가 아니게 된다.」*
>
> ### ⓑ 산식을 재는 검사 **16건**을 산식 조항으로 옮겼다
>
> `FR-101-AC5` 문면이 *「메서드의 **존재**는 `AC2` 가, **산식**은 이 조항이 요구한다」*
> 를 직접 적는다 — 경계는 추론이 아니다. v0.15 가 *「비용 검사 **9건**」* 이라 적고
> 옮겼으나 **실물은 26건**이었다. 마커 실측 `FR-101-AC2` **24 → 8** ·
> `FR-101-AC5` **9 → 25**.
>
> > ### ★★ 검증자가 **라운드를 구했다** — 문자 치환으로 옮기면 6건이 조용히 망가진다
> >
> > `tests/der/test_ess.py` 한 파일에 `FR-101-AC2`(4)와 **`FR-104-AC2`(6)** 가 함께
> > 있다. `sed s/AC2/AC5/` 류로 옮기면 `FR-104-AC2`(ESS 열화·EOL 교체비)가
> > `FR-104-AC5`(잔존가치)로 바뀐다 — **이 라운드가 고치고 있는 것과 똑같은 거짓
> > 인용을 6건 새로 만들고** Must-have 조항의 매핑을 비운다. **함수 이름으로 한 건씩
> > 옮겼고 `FR-104-AC2` 는 7건 그대로다**(커밋 전 검산).
>
> **2건은 옮기지 않았다 — 목적지가 없다.** ①
> `test_rc_all_c2_geometric_series_formula_is_resource_independent` 는 **`DER` 메서드를
> 하나도 부르지 않는다**(검사가 오라클 산식을 스스로 계산해 상수와 견준다).
> **변이로 증명했다** — `ess.fixed_om()` 의 물가 계수를 지우니 **이 검사는 초록불**이고
> 인용을 갖지 않던 이웃이 잡았다. `AC5` 로 옮기면 **지금보다 나빠진다.**
> → `todo.md` 2-b ①(지울지 정해야 한다) ② `annual_operation` 은 `AC2` 6종에도
> `AC5` 셋에도 없다.
>
> ### ⓒ Must-have · Phase 1 조항을 재는 검사가 **0건**이었다
>
> `FR-101-AC3`(*「신규 자원 클래스가 인터페이스만 구현하면 코어 엔진 수정 없이 동작
> (단위 테스트로 실증)」*)의 인용 **2건이 모두 `NFR-208-AC1`(역방향 import)을 재고
> 있었다** — 그런데 매핑표는 「자동 검증됨」으로 셌다.
>
> **반박을 저장소 안의 반례로 기각했다**: `core/engine/rule_based.py::_rule_for()` 가
> 자원 태그 **셋을 리터럴로 아는데**(`pv`·`ess`·`ev_v2g`. 레지스트리는 여섯)
> `lint-imports` 는 `4 kept 0 broken` 이다. **두 명제가 같은 것이면 있을 수 없는
> 조합**이므로 「역방향 import 금지 = 엔진 수정 없이」는 거짓이다.
>
> **그리고 실증도 부족했다** — 어느 검사도 ⓐ「인터페이스**만**」과 ⓒ「동작」을 **같은
> 대상에 대해 함께** 재지 않았다. 그것이 `AC3` 가 비어 있던 정확한 형태다.
> → 마커 2건을 `NFR-208-AC1` 로 옮기고(**첫째 것의 독스트링은 v0.1부터 스스로
> `NFR-208-AC1` 이라 적고 있었다**) `tests/engine/test_rule_based.py` 에 검사 둘을
> 세웠다 — 계약만 구현한 자원을 **엔진을 돌려** 디스패치하는 것(ⓐ+ⓒ)과, 엔진 소스가
> 선언된 셋을 넘는 태그를 이름으로 알지 않는지(ⓑ · **원인을 막는다** — R23 형태).
>
> ## ⑤ ★★ **R37 의 두 그물에 안 걸린 세 번째 형태**를 찾고 하나를 세웠다
>
> R37 은 두 방향으로 훑어 둘 다 0건을 확인했다 — ⓐ 인용이 **1건뿐인** 조항
> ⓑ Phase 어긋남. `FR-101-AC3` 은 **인용이 2건**이라 ⓐ에 안 걸리고 **Phase 도 맞아서**
> ⓑ에도 안 걸린다.
> **세 번째 형태: 「인용이 여럿인데 그 전부가 다른 조항을 잰다」** — 인용 수를 세는
> 그물과 Phase 를 견주는 그물은 **둘 다 인용의 내용을 보지 않는다.**
> ⚠ **인용 N건 전부가 같은 이웃을 재면 N 이 얼마든 같은 상태가 된다.**
>
> 그물 셋을 자기검사와 함께 설계했고(`.orch/R38/result_ac3_empty.md` 7절)
> **③-3 을 세웠다** — *조항 문면에 「실증」·「단위 테스트로」 같은 검증 방법 지정이
> 있으면 인용 중 적어도 하나는 소스 문면 검사가 아니어야 한다*
> (`tests/contract/test_evidence_form.py` · **걸린 조항 0건** · 래칫 1항 · 변이 6건).
> 남은 ③-1·③-2 는 `todo.md` 2-b ⑦.
>
> ## ⑥ ★★★ **워커가 나를 다섯 번 정정했고, 검증자가 내 등재 하나를 취소시켰다**
>
> | # | 내가 적은 것 | 실측 |
> |---|---|---|
> | 1 | (R38 ①) *「`irr`·`mirr` 은 전부 합하므로 순서와 무관」* | **`mirr` 에는 거짓** — 행 단위로 걸러 미래가치를 쌓으므로 한 해를 접으면 3배 이상 어긋난다 |
> | 2 | 변이 예시 *「편익의 Phase 를 3 → 1 로」* | **이 사례의 선행은 Phase 2** 이고 2→1 은 순서가 여전히 성립해 빨간불이 안 난다 |
> | 3 | `GridPurchase` 가 요금 인상률을 안 읽는 것은 **「부품은 있는데 읽는 쪽이 없다」** | **의도적 설계**다(NSPM 대칭). 매개변수 자체가 없다 |
> | 4 | (등재) 「교체비 계상 시점」을 재는 **조항이 없다** | **조항에 있다** — spec **412행** `FR-104-AC3`. **그대로 두면 다음 라운드가 이미 있는 것을 신설하는 §16.5 안건을 만들었다** |
> | 5 | 브리프의 짝 파일 경로 `tests/ci/test_smoke_wave0.py` | 실제는 **`tests/contract/`** |
>
> **조사자와 검증자를 가른 것이 값을 냈다.** R37 이 *「다음 라운드도 그렇게 한다」* 고
> 남겼고, **다섯 번 값을 냈다.** ★ **④는 워커의 산출물이 아니라 오케스트레이터의
> 등재를 검증자가 취소시킨 것이다** — 검증 대상은 워커만이 아니다.
>
> ## ⑦ ⛔ 한 레인은 **전부 반려했다 — 표를 지어냈다**
>
> `agy`(Gemini 3.7 Flash)에게 마커 1,302건에 조항 문면을 붙이게 했다. 낸 표는
> **줄번호 표본 다섯 전건 불일치**(707행은 **빈 줄**) · **인용한 조항 문면 셋이 spec 에
> 아예 없음**(지어냈다) · 인용 지점을 **514**(실물 1,302) · 동번호 `NFR-` 을 **24**
> (실물 9) · **`?` 칸 0개**(브리프가 *「물음표를 채우는 것이 이 구획의 실패다」* 라
> 두 번 경고했다).
>
> ⚠ **화면에 완료를 보고했으나 파일은 존재하지 않았다** — 감시기의 **「idle 인데 완료
> 줄이 없다」** 판정이 잡았다. **감시기에 「완료」만 넣었으면 「일하는 중」으로 보였다.**
>
> > ### 반려의 원인은 워커가 아니라 **구획 설계였다**
> >
> > 마커에 조항 문면을 붙이는 것은 **결정론적 조인**이다. LLM 에게 손으로 표를 쓰게
> > 하면 **모르는 칸을 채우는 것이 가장 쉬운 길**이 된다.
> > **브리프에 경고를 두 번 적어도 구조가 그것을 이긴다.**
> >
> > → `.orch/R38/build_marker_inventory.py` 로 다시 했다. **새 파서를 만들지 않고
> > 정본 둘을 재사용**(`_specparse.parse_spec` · `gen_traceability.collect_test_markers`
> > — 후자는 클래스·모듈 수준 `pytestmark` 상속을 이미 처리한다).
> > 산출 `.orch/R38/marker_inventory.md`(296KB):
> > **인용 1,302 · 조항 ID 296 · spec 에 없는 조항 0 · 동번호 `NFR-` 9.**
> > **「아홉」이 R21 의 네 라운드 전 기록과 정확히 일치한다.**
> >
> > 반려한 표는 `REJECTED_marker_inventory_agy.md` 로 옮기고 머리에 사유를 박았다 —
> > **65KB 조작 표가 데이터로 오독되면 그것이 다음 라운드의 계획이 된다.**
> >
> > **교훈: 「전수 대조」류 구획은 먼저 「스크립트로 할 수 있는가」를 물어야 한다.**
>
> 그 인벤토리로 **네 라운드째 미룬 위험을 닫았다** — `FR-105-AC1` ↔ `NFR-105-AC1`
> **21 대 21** 을 포함한 동번호 쌍의 인용 43건을 대조: **`X` 0건 · `?` 1건.**
> 그리고 저장소의 `FR-` 검색을 전수로 훑어 **활성 결함 0건**을 확인했다
> (`_specparse.py`·`gen_traceability.py`·`check_task_mapping.py`·
> `check_partition_assignment.py` 전건 보호. ★ `.orch/_r24_brief_check.py` 에는
> **R21 의 처방 `(?<!N)` 이 이미 반영돼 있었다**). 잠재 2곳은 `todo.md` 2-b ⑤.
>
> ## ⑧ ⚠⚠ **커밋 직전에 R38 이 스스로 만든 거짓 「검증됨」 두 건을 되돌렸다**
>
> 추적표를 재생성하니 **자동 매핑 290 → 292 · Must-have 미매핑 9 → 7** 이 됐다.
> 좋은 소식처럼 보였다. **아니었다** — 닫힌 둘은
> `FR-202-AC4`(모델 간 구성 차이를 **diff 뷰**로 제시)와
> `FR-401-AC2.VPPMarket`(**시장정산 − 운영수수료**)이고, 그것을 매핑한 것은
> **spec Phase 표기 회귀 검사**다. **그 검사는 diff 뷰도 정산식도 재지 않는다** —
> 두 조항은 거기에 **자료로 등장할 뿐이다.**
>
> **R37 이 `FR-402-AC2.C` 에서 찾은 결함과 같은 형태를 이 라운드가 새로 만든 것이다.**
> → 마커 셋을 떼고 사유를 모듈 독스트링에 남겼다. **자동 290 · 미매핑 9 로 되돌렸다.**
> 선례가 이미 있었다 — 같은 라운드의 `tests/report/test_unreflected.py` 도
> *「거짓 인용을 고치는 검사에 거짓 인용을 달지 않는다」* 로 무마커이고, spec 도
> **「게이트 자기참조 금지」**(`NFR-105`~`107` 은 수동 대장에 등재하지 않는다)를 적는다.
> `test_evidence_form.py` 도 같은 이유로 무마커다.
>
> > **★ 「좋아 보이는 수」가 나오면 무엇이 그것을 냈는지 먼저 볼 것.**
> > 이 저장소는 *「매핑 건수를 성과로 세지 말 것」* 을 다섯 라운드에 걸쳐 적었는데,
> > 이번은 **늘어난 것이 거짓이었다.**
>
> ## ⑨ 인수 절차 — **오케스트레이터가 변이 여섯을 직접 심었다**
>
> | 무엇 | 결과 |
> |---|---|
> | 매달린 선행 참조(spec 493행) · 선행을 인용측보다 뒤로(640행 2→3) | **둘 다 빨간불** |
> | 잔존가치 `AC5` → `AC4` 복원 | **빨간불** |
> | `PVFixedOM` 도 계수를 안 받게 한다(**부채가 늘어나는 방향**) | **빨간불** |
> | 판정기의 `oracle_source` 존재 검사를 무력화 | **빨간불** (고치기 전에는 **초록불이었다**) |
> | 엔진이 네 번째 자원 태그를 알게 한다 | **빨간불** |
> | `core/der/pv.py` 에 최상위 역방향 import | **빨간불** |
>
> spec 변이 뒤 **원복·`.mutbak` 없음** 확인. 그리고 **음성 테스트가 조용한 실패를
> 하나 갖고 있었다** — fallback `AssertionError` 메시지 자신에 `oracle_source` 가 있어
> `assert "oracle_source" in str(e)` 가 **어느 예외를 잡았는지 가리지 못했다.**
> 「판정기가 잡았다」와 「못 잡아 fallback 이 대신 잡혔다」가 같은 문자열로 위장돼
> 있었다. **내가 요구한 변이가 그것을 드러냈다.**
>
> ## ⑩ 실측 — 게이트
>
> `.orch/R38/result_gates.md` 가 전문을 갖는다.
> 추적표: 요구사항 **105** · 수용기준 **321** · 자동 **290** · 수동 8 · 수동 스텁 4 ·
> **Must-have 미매핑 9**(전건 Phase 2·3 · **Phase 1 은 0건**).
>
> ## ⚠ 이 라운드가 새로 밟은 함정 — **둘은 내가 밟았다**
>
> 1. **`PYTHONUTF8` 없이 서브프로세스 출력을 읽으면 죽는다.** 워커 감시기가
>    `herdr agent list` 의 터미널 제목(`✳`·`◑`)에서 `UnicodeDecodeError: 'cp949'` 로
>    죽었다. **`text=True` 만으로는 안 되고 `encoding="utf-8", errors="replace"` 를
>    명시한다.** `status.md` 게이트 절이 pytest·lint-imports 에 적어 둔 그 함정이며
>    **파이썬으로 다른 프로그램의 출력을 읽는 모든 자리**에 해당한다
> 2. **감시기는 「완료」만 알려서는 안 된다.** 처음 판이 `blocked`(승인 대기)와
>    「idle 인데 완료 줄이 없다」를 알리지 않아 `agy` 가 권한 프롬프트 앞에 멎은 것과
>    **파일 없이 완료를 보고한 것**이 둘 다 「일하는 중」으로 보였다.
>    **침묵이 성공으로 읽히는 자리를 감시기 자신이 만들었다**
> 3. **워커에게 게이트 범위를 넓게 주면 시간제한에 죽는다**(`exit 143`). 브리프에
>    **「돌릴 디렉터리」를 구획과 같게** 못 박을 것
> 4. **★★ 워커가 변이를 돌리는 동안 전건 게이트를 돌리지 말 것.** 배경 `pytest` 가
>    **실패 1건**을 냈는데(`test_no_analysis_period_default_lives_in_the_calculation_layers`)
>    **단독으로는 rc=0** 이다. 그 시각 세 레인이 `.orch/mutate.py` 로 **실물 소스에
>    변이를 심고 있었고** 그 검사는 소스 문면을 훑는다. **원복 사이의 창이 있다**
> 5. **★ 배경 명령의 rc 를 마지막 명령으로 둘 것.**
>    `pytest … ; echo rc=$? ; grep … ; tail …` 로 짜서 **스크립트 종료 코드가 `tail`
>    것**이 됐다 — 알림은 `exit code 0` 이었고 **실패는 로그 본문에만** 있었다.
>    `status.md` 가 *「`pytest … | tail` 의 종료 코드는 `tail` 의 것」* 이라 적어 둔
>    형태이며 **파이프가 아니라 `;` 로 이은 것도 같다**
>
> ## 다음에 무엇을 집는가 — `todo.md`
>
> `todo.md` 1번(골든 · **옮기는 조건**을 못 박았다) · 2번(교체비 배선 · **갈래 판정
> 완료**) · **2-b(R38 이 찾았으나 닫지 않은 것 일곱)**. 사람 몫은 `status-human.md` 가
> 소유자이며 **R38 이 3건을 등재했다**(그중 하나는 등재 뒤 검증자가 정정시켰다).

> # ⏹ 2026-08-20 **R38 ① 완료 — 주 지표를 연 단위로 되돌렸다**
>
> R37 이 찾아 인계에 적은 것(주 지표가 행 단위로 계산된다)을 같은 날 닫았다. 별도
> worktree 에서 Opus 워커가 잡고 오케스트레이터가 리베이스로 합쳤다.
>
> | 시나리오 | 결론 축 | 주 지표 **전** | 주 지표 **후** |
> |---|---|---|---|
> | 무보조 | −5,284,586원 | 미회수 | 미회수 |
> | 보조 20% | −3,324,586원 | 미회수 | 미회수 |
> | **보조 80%** | +2,555,414원 | **4.8623년** | **6.3923년** (**+1.53년**) |
>
> **NPV·초기지출은 전·후 동일하다** — 회수기간 셈법만 건드렸다. 리포트는 **바이트
> 동일**(기본 시나리오가 무보조이고 그쪽은 전·후 모두 미회수다) — ⚠ **그래서 실려
> 나가는 산출물에는 이 결함이 한 번도 보이지 않았다.**
>
> ### ★★★ `_flatten` 을 고칠 수 없었다 — **실측이 브리프를 정정했다**
>
> 오케스트레이터의 지시문은 *「`_flatten` 을 고칠 것인가, `payback_*` 안에서 합칠
> 것인가」* 를 정하라 했고 *「`irr`·`mirr` 은 전부 합하므로 순서와 무관하다」* 고 적었다.
> **`mirr` 에는 거짓이었다.**
>
> ```
> mirr 현행                        0.40478135
> mirr (_flatten 이 연도를 합치면)  0.12767638   ← 3배 이상 어긋난다
> irr  는 양쪽 다                  0.19857710
> ```
>
> `mirr()` 은 `amount > 0` 을 **행 단위**로 걸러 미래가치를 쌓는다 — 한 해의 편익 행과
> 비용 행을 미리 합치면 그 해가 순편익 하나로 접혀 미래가치가 줄어든다. **순서 불변은
> 요구하고 합산은 요구하지 않는다** — 그래서 `_by_year()` 를 따로 세우고 두 독스트링이
> 서로를 가리키게 했으며, 그 사실 자체를 검사와 변이 ⓔ 가 붙든다.
>
> ### ★★ 브리프가 「진짜 위험」이라 적은 자리를 **검사로 세웠다**
>
> *「한계 케이스에서 1절의 두 줄이 서로를 부정할 수 있다」* — 그 케이스를 찾아냈다.
> 균일 현금흐름에서 연 비용 **2,800,000원**이면 **NPV −398,414원(미회수)인데 행 단위
> 회수는 2.9987년**이다. 붙임 3 은 두 줄의 관계를 문면으로 **선언**하는데 **그 선언을
> 재는 검사가 없었다.** 이제 0 선 양쪽으로 비용을 훑어 동치를 잰다(균일 조건을 검사
> 문면에 적었다 — 균일하지 않으면 동치를 요구할 수 없다).
>
> ### 검사 9건 신설 · 변이 6건 — **둘이 요점이다**
>
> - **ⓑ 는 초록불이 정답인 변이다** — 같은 해 행의 순서를 뒤집어도 값이 같아야 한다.
>   합산이 옳다는 증거를 그렇게 붙들었다.
> - **ⓓ 는 처음에 초록불이었다** — 해마다 `to_won` 반올림을 없애도 값은 상대 **1.96e-07**
>   만 움직여 `rel=1e-4` 근사를 쓰는 검사 전건이 통과한다. 근사를 좁히면 부동소수
>   재결합에 깨진다. **그래서 값의 근사가 아니라 「어느 쪽에 서 있는가」를 재게 했다** —
>   반올림한 손계산과 안 한 손계산 둘을 세우고 구현이 어느 쪽에 가까운지 본다.
>
> ### ⚠ 남은 위험 둘 — **오케스트레이터 판정**
>
> **① 골든 하니스가 같은 눈먼 자리를 갖고 있다.** 골든 3건은 값이 **바뀌지 않았고**
> 그것이 좋은 소식이 아니다 — `_reference_metrics()` 가 **편익 한 행만** 넘기고 비용
> 행이 없어서 「행 단위」와 「연 단위」가 같은 값을 낸다. **세 시나리오가 전건 초록불인
> 채 결함이 지나갔다.** → **판정: 골든을 고치지 않는다.** 그 파일은 「어제와 같다」를
> 재는 회귀 스냅숏이고 행 구성을 바꾸면 expected 를 다시 뽑아야 한다 — 파수꾼은
> `tests/cba/test_metrics.py` 에 세웠고, 골든에는 **「이 골든은 그 결함의 파수꾼이
> 아니다」와 파수꾼의 자리**를 주석으로 적었다. `todo.md` 에 올린다.
>
> **② `check_precommit_installed` 가 worktree 에서 거짓 빨간불이다** —
> `repo_root/.git/hooks/pre-commit` 를 보는데 worktree 에서 `.git` 은 **파일**이다.
> 훅은 실제로 걸린다(`git rev-parse --git-path hooks` 가 공유 디렉터리를 가리킨다).
> 「반복해서 걸린 함정」의 **«시끄러운» 실패**이며 → **판정: 고친다**(아래 별 항목).
>
> ### ⚠ 이 라운드가 새로 밟은 함정 — worktree 에서 실측하면 **본 저장소를 잴 수 있다**
>
> worktree 밖(스크래치패드)에서 측정 스크립트를 돌리자 `core` 가 **본 저장소**에서
> 풀렸다 — editable 설치가 그쪽을 가리키고 `sys.path[0]` 이 스크립트 위치다. 즉
> **worktree 의 코드를 고치고 본 저장소의 코드를 재고 있었을 수 있다.** 실패한 import
> 의 경로에 박혀 드러났을 뿐이며 **성공했다면 조용히 틀린 수를 적었다.**
> `pytest` 는 rootdir 삽입으로 worktree 를 물어 무관하다(확인).
>
> > **worktree 에서 실측할 때 먼저 물을 것**: *지금 이 수는 어느 저장소의 코드가 낸
> > 것인가?* 측정 스크립트가 첫 줄에 `core.__file__` 을 찍게 두는 것이 답이다.
>

> ### ⬇ **R43 이 내린 여덟 블록**(2026-08-29) — R37 · R36 · R35 ③ · R35 ② · R35 ① ·
> R34 · R32 · 「1차 의견」. `status.md` 의 「지금 할 일」에 블록이 **아홉** 쌓여 있어
> R42 하나만 남기고 내렸다.
>
> ⚠ **머리가 아니라 이 자리다.** 이력 규칙은 *「머리 바로 아래로 옮긴다(시간 역순
> 유지)」* 인데, 이 여덟은 **위의 R41~R38 보다 오래된 것**(2026-08-20~08-14)이라
> 머리에 넣으면 시간 역순이 깨진다. 규칙의 두 조건이 갈렸고 **시간 역순을 택했다.**
> 그래도 **현재 상태는 아니다** — 정본은 `status.md` 다.

> # ⏹ 2026-08-20 **R37 완료 — 검증이 없는 조항이 「검증됨」으로 세어지고 있었다**
>
> 사용자가 **6시간 부재**를 알리며 *「사람이 확인해야 하는 사항은 합리적으로 판단해
> 확인한 것으로 처리하고 계속 진행」* 하라고 지시했다. 그래서 이 라운드는 판정을 미루지
> 않았다 — **대신 내린 판정과 그 사유를 아래 표에 적는다. 되돌리려면 사유를 반박한다.**
>
> 오케스트레이터-워커 구성으로 돌았다(herdr 세션 `dev` · 워커 8대 · 지시문과 산출물은
> `.orch/R37/` 에 있고 그 디렉터리는 `.gitignore` 안이다). 커밋 셋 — 요약 행 ·
> 마커 위생 + spec **v0.17** · 얇은 리프 검사.
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 | 검증 |
> |---|---|---|
> | **1절 요약에 「결론 전환 지원율」 한 행** — 검토자가 먼저 보는 칸의 「없음」에 크기를 준다 | 양식 【1】 · `narrative.py::_summary_section` · `test_conclusion_gap.py` | 변이 **6건 전건 빨간불** · **독립 검토 문제 0건** |
> | **`FR-402-AC2.C` 를 정직하게 닫았다** — 거짓 인용 제거 + 조항 Phase 를 3 으로 명확화 | `test_exclusion.py` · spec 672행 | `check_task_mapping` **rc=0** · 미매핑 8 → **9** |
> | **과잉 태그 7건 제거 · 1건 교체** | `test_settlement*.py` · `test_aggregated_ppa.py` · `test_exclusion*.py` | 매 건 **잔여 인용 실측** — 유일 인용을 잃는 조항 0건 |
> | **얇은 리프 둘에 구조 고유 검증 4건** | `test_surplus_direct_sale_*.py` · `test_vpp_rejection_matches_spec_precursor.py` | 변이 **5건 전건 빨간불** |
>
> ### ★★★ 이 라운드의 요점 — 매핑표가 「검증됨」으로 세던 조항에 검증이 없었다
>
> R33 이 *「기계 몫은 끝났다. 남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나이며
> **읽어서 대조하는 수밖에 없다**」* 고 적어 둔 것을 읽었다. **실재했다.**
>
> `FR-402-AC2.C`(같은 tCO2 에 배출권 수익과 사회적 탄소비용을 동시 계상하지 않는다)의
> 인용은 **전체가 1건**이었고, 그 테스트는 자가소비·REC·첨두절감·열비용절감 **넷의 동시
> 계상이 정당함**을 본다 — `tCO2` 도 배출권도 관점 전환도 코드에 없다. **그 테스트의
> 독스트링은 스스로 「그것이 `FR-402-AC1` 이다」 라고 적고 있었다.**
>
> ★ 함께 실측된 것: `docs/exclusion-rules.yaml` 에 **유형 C 규칙이 0건**이다
> (A·A·B·D·D·A·A). 검사만 없는 것이 아니라 **구현도 없다.** 그리고 그것이 결함이 아닌
> 이유가 있다 — 조항의 주제인 `CarbonCredit` 은 v0.1부터 **`[Phase 3]`**(spec 643행)이다.
> **어긋난 것은 구현이 아니라 조항의 Phase 표기였다.**
>
> ### ★★ 1차 가설이 틀렸고 **검증자가 그것을 잡았다**
>
> 오케스트레이터의 1차 가설은 *「그 조항을 재는 검사가 마커 없이 다른 곳에 있다」*
> (부하·열부하 자원이 `value_streams()` 를 갖지 않는 것)였고 **기각됐다** — 그것은
> *같은 관점 안에서 두 자원이 같은 물리량을 이중 계상*하는 문제이고 `AC2.C` 는 *관점을
> 바꿔가며 두 산식으로* 화폐화하는 것을 막는 조항이다(그 관점 전환은 조항이 스스로
> `FR-704` 에 위임한다). **다른 원리다.**
>
> ⚠ 가설대로 마커를 옮겼다면 **틀린 자리가 다시 「검증됨」으로 세어졌을 것이다.**
> 조사자와 검증자를 가른 것이 값을 냈다 — 다음 라운드도 그렇게 한다.
>
> ### 일반화 — 같은 형태가 더 있는가 (**둘 다 0건**)
>
> | 방향 | 범위 | 결과 |
> |---|---|---|
> | 인용이 **1건뿐인 조항** (실측 133건 중 배타·관점 계열 8건) | `audit_singles.md` | **어긋남 0건** — 여덟 전부 그 하나가 조항을 정확히 잰다 |
> | **Phase 1 Must-have 인데 대상이 Phase 2·3** 인 조항 | `phase_mismatch_sweep.md` | **신규 0건** — 후보 셋은 「횡단 조항」·「예시」·「별개 대상(물리량 vs 화폐화)」로 정상 |
>
> ⇒ **고립된 한 건이었다.** 「전수 대조를 더 해야 한다」로 남기지 않고 두 방향으로 재서
> 닫았다.
>
> ### 판정 — **가드 검사에 일곱 구조를 다 태그한 것은 어긋남이 아니다**
>
> 샤드 A 판독 둘이 정면으로 갈렸다(어긋남 **22/28 vs 0/28**). **리프별 「구조 고유 검증」
> 건수를 실측해 판정했다** — 가드 5건(어휘·구획·소유자·거부)을 뺀 수:
>
> | 리프 | 구조 고유 | | 리프 | 구조 고유 |
> |---|---|---|---|---|
> | `NetMetering` | 12 | | `AggregatedPPA` | 3 |
> | `ManagerEntity` | 10 | | `VPP` | 2 → **3** (R37) |
> | `HouseholdDirect` | 6 | | `SurplusDirectSale` | 1 → **5** (R37) |
> | `DistrictDirectTrade` | 5 | | | |
>
> **가드만으로 매핑된 리프는 0건이었다.** 그래서 22건을 떼지 않았다 — 떼면 매핑이 얇아
> 보이는 것을 고칠 뿐이고 실제 구멍은 없다. **대신 얇은 둘에 검사를 세웠다.**
>
> ### 실측 — 게이트 (2026-08-20 · R37 · **전건**)
>
> `pytest` **rc=0** · **Python 3.11 rc=0(실패 0)** · **음성 스위트 11종 rc=0** ·
> `ruff` 0건(신규 검사에서 E501 한 건이 났고 고쳤다) · `mypy` **114파일** 무결 ·
> `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(정상) ·
> **`check_task_mapping` rc=0** · `check_*` 9종 rc=0 ·
> `check_file_size --code-strict` rc=0 · 잔여 `.mutbak` **0건**.
>
> **추적표**: 요구사항 105 · 수용기준 **321**(불변) · 자동 **290** · 수동 4 ·
> **Must-have 미매핑 9**(전건 Phase 2·3 · Phase 1 은 0건).
>
> ### 사람이 판단할 것 둘 — **소유자는 `status-human.md` 다**
>
> 이 라운드는 **필요해서 생긴 개정 하나(위 Phase 표기)만** 실행하고 **둘은 실행하지
> 않았다** — 조항이 *요구하는 것*을 바꾸는 일이므로 사람 판단이다. 문면·사유·개정안·
> 파급까지 적힌 초안은 `.orch/R37/spec_amendment_draft.md` 에 있고, **등재 자리는
> `status-human.md` 「7단계 — spec 개정 판단」 표**다(여기 옮겨 적지 않는다).
>
> ### ★★★ 함께 돈 것 — **일사 곡선을 재고, 그것이 주 지표 결함을 드러냈다**
>
> `todo.md` 4번(일사 곡선)을 별도 worktree(`D:/Dev/260802_DER_evaluator_wt_sun` ·
> 브랜치 **`r37-irradiance`** · 커밋 `be98a20`)에서 배선하고 **결론 이동을 실측했다.**
>
> **기본 실행 경로가 `daily_shapes` 를 넘기지 않아 태양광이 24스텝 전부 0.45kWh —
> 심야에도 발전 — 를 냈다.** 형상 자산은 이미 있었고(`fixtures/profiles/
> representative-day.yaml` · 야간 가중치 0.0) 붙임 7 만 그것으로 그림을 그렸다.
>
> | 값 | 평탄 | 일사 곡선 | 차이 |
> |---|---|---|---|
> | 순현재가치(무보조) | −5,156,392원 | **−5,284,586원** | **−128,194원** |
> | 대표일 수전 · 잉여 역송 | 6.19 · 16.10 kWh | **8.89 · 18.80 kWh** | 각 **+2.70**(연 +985) |
> | 태양광 한계 기여 | 322,898원/kW | **280,167원/kW** | **−13.2%** |
> | 붙임 8 미반영 | 7건 | **6건** | 「일중 발전 프로파일 (평탄)」 삭제 |
>
> **뒤집히지 않았다** — 세 골든 시나리오의 회수 여부·부호가 전건 그대로이고 NPV 는 셋 다
> 정확히 −128,194원 나빠졌다. `todo.md` 4번의 물음에 답한다: **그 인공물은 실재했고
> 크기는 NPV −12.8만원 · PV 한계 기여 −13.2% 였으나 결론을 옮길 만큼은 아니었다.**
>
> ★ 검산이 닫힌다 — 985.5kWh × 10원/kWh(판매 110 · 구매 120) = 연 **9,855원** ·
> × 연금현가계수 13.0079(20년 4.5%) = **128,193원**. 야간 PV 가 ESS 심야 충전을
> 상계하고 있었고, 곡선이 들어오며 그 상계분이 순 수전으로 바뀌고 같은 에너지가 주간에
> 몰려 순 송전으로 나간다(총 발전량은 전·후 동일 — 형상은 배분이지 값이 아니다).
>
> ### ★★★★ 그 배선이 드러낸 것 — **주 지표가 「행 단위」로 계산된다**
>
> 배선 뒤 **NPV 는 나빠졌는데 회수기간이 짧아졌다**(보조 80% 4.9207 → 4.8623년).
> 연 순현금흐름이 균일하게 9,855원 줄었으니 회수는 **길어져야 한다.** 그 모순을 따라가
> 결함을 찾았고 **오케스트레이터가 코드를 읽어, 독립 검토가 네 자리 소수까지 재현했다.**
>
> ```
> core/cba/metrics.py::_flatten          (year, amount) 를 정렬만 하고 **합치지 않는다**
> core/cba/metrics.py::payback_discounted 그 목록을 한 걸음씩 돌며 누적 ≥ 0 이면 즉시 반환
> ```
>
> 한 해에 행이 여럿이면(편익 행 + 비용 행) **그 해의 편익 행만 누적한 지점에서 0 선을
> 넘고 남은 음수 행을 세기 전에 반환한다.** `prev_year` 도 같은 해가 되어 소수부가
> 무의미해진다 — **행 순서가 주 지표를 바꾼다.**
>
> ★ **독스트링이 스스로 다른 것을 말한다**(*「각 해의 현금흐름을 할인한 뒤 … 누적이
> initial 을 넘는 시점」*). 문면은 연 단위, 구현은 행 단위다 — 이 라운드가
> `FR-402-AC2.C` 에서 만난 것과 **같은 형태**이며(선언과 구현이 갈렸고 아무도 그 사이를
> 보지 않았다) **오늘 두 번 나온 형태다.**
>
> | | 현행(행 단위) | 연도 합산 |
> |---|---|---|
> | 보조 80% · 평탄 | 4.9207년 | 6.1907년 |
> | 보조 80% · 곡선 | **4.8623년** | **6.3923년** |
>
> ⚠ `payback_simple` 도 같은 결함이며 **그 오라클은 편익 1행만 넘겨 구조적으로 이것을
> 못 본다.** `irr`·`mirr` 은 영향 없다(전부 합하므로 순서와 무관하다).
> ⚠ **회수 여부는 NPV 로 정해지므로 결론은 뒤집히지 않는다.** 진짜 위험은 **한계
> 케이스에서 1절의 두 줄이 서로를 부정하는 것**이다(「미회수」와 「N년 회수」가 같은 표에).
> **→ 다음 라운드의 1번이며 착수 지시문은 `.orch/R37/brief_payback.md` 에 있다.**
>
> ### 이 분량을 `main` 에 합쳤다 — 리베이스가 검사 하나를 더 드러냈다
>
> **독립 검토를 받았다**(`.orch/R37/review_irradiance.md` · 산술을 저장소 밖에서 다시
> 계산해 재현). 중심 주장 넷(결론 뒤집힘 없음 · 산술 · 동어반복 회피 · **골든 값 수정
> 0건**)은 전부 문제없음이고, **이 라운드가 만든 부작용 둘**이 지적됐다 —
> ① 리포트가 「미산출 (형상 자산 부재)」라는 **인쇄될 수 없는 사유**를 싣는다
> ② 자산 부기의 「일출 06시·일몰 19시」가 실물 가중치(07~21시·비대칭)와 어긋나며,
> 이 라운드가 같은 필드에 「이제 프로포마의 입력」이라 적었으므로 **결론의 근거 표기**가
> 됐다. 둘을 고친 뒤 합쳤다.
>
> ★ 검토가 덤으로 찾은 것: 배선으로 **리포트의 내적 모순 하나가 사라졌다** — 붙임 8
> 「자가소비 편익」의 5.44kWh/일 이 이제 송전 감소분과 **정확히** 같다(종전 2.74 로
> 어긋나 있었다).
>
> ★★ **리베이스가 검사 하나를 실측으로 드러냈다.** R37 이 세운 요약 행 검사
> (`test_the_summary_row_carries_the_same_support_rate_as_the_body`)는 인쇄된 지원율을
> 진입점에 다시 먹여 결론 축이 0 인지 보는데, **그 재실행이 형상을 받지 않아** 곡선으로
> 계산된 수를 **평탄한 사업**의 0 선에 대고 재고 있었다 — 리베이스 직후 **125,808원**이
> 남았고 그것은 곡선↔평탄 차이 **128,194원**과 같은 크기다. 그 검사는 배선보다 **뒤에
> 태어나서** `conftest.report_shapes` 의 그 줄을 못 받았다. 값을 고치지 않고 배선을 고쳤다.
>
> ⚠ **요약 행의 전환 지원율이 52.6% → 53.9%** 가 됐다(리포트 재생성 · 한 줄). 결론 축이
> 나빠졌으므로 그것을 0 으로 만드는 지원율도 오른다 — **두 변경이 서로를 깨뜨리지 않는다는
> 증거**이며, 요약이 값을 스스로 계산하지 않게 세운 것이 그 자리에서 값을 냈다.
>
> ### 합친 뒤 게이트 (2026-08-20 · **전건**)
>
> `pytest` **3.13 rc=0 · 3.11 rc=0** · `ruff` 0건 · `mypy` 114파일 무결 ·
> `lint-imports` 4 kept 0 broken · `check_*` 8종 rc=0 · `check_file_size --code-strict`
> rc=0 · **음성 스위트 11종 rc=0** · 잔여 `.mutbak` **0건**.
> 커밋 넷 — `8055b9f`(배선) · `a7cb9e3`(검토 부작용 둘) · `6af3b63`(리포트 재생성) ·
> `9fa8b8b`(검사 배선).

> # ⏹ 2026-08-18~20 **R36 완료 — 편익이 자기 대입값을 내놓는다** (붙임 4)
>
> `todo.md` 종전 2번. 사용자가 *「push하고 진행」* 으로 지시했고 R35 ③ 은 push ·
> CI 둘 `success` 까지 끝냈다. 이어 이 항목을 집었고 **08-18 세션이 할당량으로
> 끝나 커밋 전에 멈췄다.** 08-20 에 게이트 둘을 돌려 닫았다 — 본체 `7fd39fe`.
>
> ★ **둘을 이어 돌리지 않았다.** 08-18 의 10분 시간초과는 이어 돌린 탓이며,
> 따로 배경 실행하면 Python 3.11 은 약 2분, 음성 스위트 11종은 약 6분이다.
>
> ### 무엇이 있었나
>
> 양식 §3 붙임 4 는 *「편익별 산식·**대입값**(발전량·단가)」* 을 요구하는데 실물은
> 갈래마다 **`대표일 1,771원 × 365일`** 한 줄이었다 — 곱해서 나온 금액만 있고
> 무엇에 얼마를 곱했는지가 없다. 같은 리포트의 **비용** 쪽은 같은 자리에
> `대표일 수전 6.19kWh × 365일 × 120원/kWh` 로 갈라 적는다.
>
> ★ **양식을 고칠 일이 아니었다** — 양식은 이미 그것을 요구하고 있었고 구현이
> 따라가지 못한 것이다. 그래서 `docs/report-form-심의보고서.md` 는 손대지 않았다.
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **`ValueStream.formula()` 추상 메서드** 신설 — 편익이 자기 대입값을 내놓는다 | `core/contracts/valuestream.py` |
> | 편익 **8종**이 각자 산식 구현 — 곱 · 차 · 합 · 섞임 **네 모양** | `core/valuestream/*.py` |
> | 러너가 **산식을 짓지 않는다** — `× 365일` 과 `= 합계` 만 붙인다 | `e2e_runner.py::_benefit_line` |
> | 첨두 절감도 **같은 통로로** — 라벨·산식이 러너에 박혀 있던 마지막 예외였다 | 같음 |
> | 탐침 표를 계약 검사들이 **공유** | `tests/contract/valuestream_probes.py` |
> | 검사 **9건** 신설 (계약 4 · 러너 3 · 진입점 1 · 붙임 4 회귀 1) | `test_benefit_formula.py` · `test_benefit_line_rendering.py` · `test_annualisation_convention.py` · `test_surplus_sale_price_wiring.py` |
> | 리포트 · 추적표 재생성 | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md` · `docs/traceability.md` |
>
> ### ★★★ 실측 — 붙임 4 가 지금 싣는 것
>
> ```
> 이전  잉여전력 판매   대표일 1,771원 × 365일 = 646,415원
> 이후  잉여전력 판매   대표일 잉여 역송 16.10kWh × 판매단가 110원/kWh × 365일 = 646,415원
>
> 이전  첨두 절감      월 감축 2.00kW × 8,320원/kW·월 × 12개월 = 199,680원
> 이후  기본요금(피크) 절감  월 감축 합계 24.00kW·월 (12개월 · 월평균 2.00kW)
>                        × 기본요금 8,320원/kW·월 = 199,680원 (연간 수량으로 산정 · 연간화 없음)
> ```
>
> ★ **금액은 한 자리도 바뀌지 않았다.** 바뀐 것은 *「왜 그 금액인가」* 를 붙임 4 가
> 답하게 된 것뿐이다. R35 ① 이 영향도 1위로 실측한 **판매단가 110원/kWh 의 값이
> 이제 리포트에 있다.**
>
> ### ★★ 함께 드러난 잠재 결함 둘
>
> **① 첨두 산식이 월별 균일을 가정하고 있었다.** 옛 문면은 `peak_reduction_kw`
> **한 달치**를 인쇄하고 12를 곱했다. 지금 러너가 `[같은 값] * 12` 를 넘기므로
> 맞지만, **월별이 갈리는 순간 산식만 거짓이 되고 금액은 맞는다.** 새 문면은
> 실제 열두 값의 **합**을 읽는다.
>
> **② `DistributedBenefit` 은 두 계약 검사에서 아무것도 재지 않고 지나가고
> 있었다.** 탐침이 `sub_items=None` 이라 연간화 검사는 *「전부 0원이면 건너뛴다」*
> 로 빠졌고, 산식 검사는 대조할 값이 없었다. 다섯 항목에 **서로 다른** 값을 넣어
> 둘 다 실제로 재게 했다.
>
> ### ★★★ 변이 — **11건 중 둘이 초록불이었고 그 자리가 요점이다**
>
> | # | 변이 | 판정 | 잡은 검사 |
> |---|---|---|---|
> | 1 | 잉여판매 산식에서 **단가를 뺀다** | 빨간불 3건 | 대입값 · 흔들기 · 붙임 4 회귀 |
> | 2 | 산식을 **탐침 값이 박힌 상수 문자열**로 | 빨간불 **1건** | `…reads_the_value_rather_than_reciting_it` **만** |
> | 3 | 편익이 **연간화를 함께** 적는다 | 빨간불 | `…leaves_annualisation_and_the_total_to_the_caller` |
> | 4 | ★ 러너가 문면에서 **`× 365일` 만 뺀다**(금액은 그대로) | ★**초록불** → 검사 신설 후 **빨간불** | `…printed_formula_says_whether_it_was_annualised` **신설** |
> | 5 | 산식의 수량을 **다른 창**에서 읽는다 | 빨간불 다수 | 붙임 4 회귀 외 |
> | 6 | 첨두 산식이 합계 대신 **월평균만** 싣는다 | 빨간불 | 대입값 |
> | 7 | 분산편익 산식이 **합계만** 싣는다 | 빨간불 | 대입값 |
> | 8 | 직접거래 산식이 **수수료 항을 뺀다** | 빨간불 2건 | 대입값 · 흔들기 |
> | 9 | ★ 러너가 **산식을 버리고 금액만** 싣는다(연간 갈래) | ★**초록불** → 검사 신설 후 **빨간불** | `test_benefit_line_rendering.py` **신설 3건** |
> | 10 | 문면의 합계가 실린 금액과 **다르다** | 빨간불 | `…states_the_total_it_was_given` |
> | 11 | 분산편익 산식이 **항목 하나를 빠뜨린다** | 빨간불 | 대입값 |
>
> ### ★★★ M4 가 드러낸 것 — **금액이 맞아도 문장은 거짓일 수 있다**
>
> 러너가 문면에서 `× 365일` 만 빼면 `대표일 잉여 역송 16.10kWh × 판매단가
> 110원/kWh = 646,415원` 이 실린다. **금액은 맞는데 산술이 닫히지 않는다**
> (16.10 × 110 = 1,771). 검토자가 손으로 검산하면 365배 어긋난 값을 얻고,
> 그러면 **틀린 것은 리포트가 아니라 자기 계산이라고 읽는다.**
>
> ⚠ **대입값을 싣게 한 것이 이 위험을 새로 만들었다.** 금액만 싣던 동안 문면은
> 아무것도 주장하지 않았다 — 이제 문면이 **스스로 산술을 주장**하고, 그 주장은
> 금액과 **따로** 틀릴 수 있다. R35 ② 의 *「두 끝의 값을 서로 바꿔 실어도 거리만
> 보는 검사 일곱이 통과했다」* 와 같은 형태이며, 답도 같다 — **다른 층에서**
> 검산한다(기대값은 레지스트리의 `scales_with_dispatch_window` 선언, 실물은
> 인쇄된 문면).
>
> ### ★★★ M9 가 드러낸 것 — **고친 결함의 반대쪽 가지가 비어 있었다**
>
> 계약 검사는 `formula()` **의 출력**을 보고 붙임 4 회귀 검사는 **잉여판매 한
> 갈래**를 본다. 그 사이 *「러너가 그것을 쓰는가」* 가 비어 있어서, **연간 편익
> 갈래**의 문면을 옛 `연 199,680원` 으로 되돌려도 전건 초록불이었다. 창 비례
> 갈래에서 고친 것과 **똑같은 결함**이 반대쪽에서 살아 있던 것이다.
>
> ⚠ 그 검사는 비공개 도우미(`_benefit_line`)를 직접 부른다 — `CaseOutcome` 이
> 편익 **인스턴스**를 내보내지 않아 진입점만으로는 두 문면을 나란히 둘 수 없다.
> 내보내게 고치는 것은 **밖에서 보이는 수가 한 자리도 움직이지 않는 변경**이라
> R34 가 「붙들 수 없는 갈래」로 미뤄 둔 그 항목이며(`todo.md` 프로포마 항목),
> **검사를 세우려고 산출물 자료형을 넓히는 것은 순서가 거꾸로다.**
>
> ### 실측 — 게이트 (08-18 아홉 + **08-20 둘**, 전건 통과)
>
> `pytest` **수집 1,581건 rc=0**(1,572 → +9) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(정상) ·
> `check_*` **9종** rc=0 · `check_file_size --code-strict` rc=0 ·
> 잔여 `.mutbak` **0건**.
> ✔ **음성 스위트 11종 rc=0** · ✔ **Python 3.11 실패 0 · rc=0** (2026-08-20 실측).
>
> ⚠ **3.11 실행은 수집 건수를 세지 않았다.** `-q` 진행 표시에 테스트가 인쇄한
> 표준출력이 섞여 점을 셀 수 없었고, **세지 않은 수를 옮겨 적지 않는다**는 이
> 파일의 규칙에 따라 「실패 0 · rc=0」까지만 적는다. 건수가 필요하면
> `--tb=no -p no:randomly` 없이 다시 세는 것이 아니라 `-q > log` 의 마지막 줄을
> 확인할 것.
>
> ---

> # ⏹ 2026-08-18 **R35 ③ 완료 — 요약 표의 행 목록을 검사로 붙들었다** (【1】)
>
> `todo.md` 2번. 사용자가 *「2번부터 시작해줘」* 로 지시했다.
>
> ### ★★★ 먼저 — **todo 2번의 전제가 틀려 있었다**
>
> `todo.md`·R35 ② 인계는 *「양식 【1】은 여섯 행을 규정하는데 실물은 여덟 행을
> 낸다」* 고 적었다. **양식은 이미 여덟 행이었다**(`적정 용량`·`잠정성` 포함 ·
> `bc4a8cc` 부터). 여섯이라 적혀 있던 곳은 **`narrative.py::_summary_section`
> 의 독스트링 한 줄**뿐이고, 그 낡은 사본이 **두 라운드의 계획으로 따라
> 들어갔다.** 이 파일이 스스로 적어 둔 *「세지 않은 수를 옮겨 적으면 그것이
> 다음 라운드의 계획으로 따라 들어간다」* 의 실례다 — 이번에는 `status.md` 가
> 아니라 **소스 독스트링**이 그 자리였다.
>
> ⚠ 그래서 그 독스트링은 이제 **행을 세지 않는다.** 목록의 소유자는 양식이고,
> 소스는 그 사실과 대조 검사의 이름만 가리킨다.
>
> ### 진짜 어긋남 — **양식이 자기 자신과 갈려 있었다**
>
> | 양식 【1】 | 실물 | 양식 §2 `6.2` |
> |---|---|---|
> | 「**결합** 전환 조건」 | 「**결론** 전환 조건 (결합)」 | 「**결론** 전환 조건」 |
>
> 새 검사가 **첫 실행에서** 잡았다. 실물·6.2 쪽으로 **양식**을 고쳤다(한쪽이
> 둘과 갈려 있으면 그쪽이 낡은 것이다).
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **요약 표 행 목록 대조 검사** 신설 — 양식 【1】을 파싱해 실물 `## 1. 요약` 표의 첫 열과 **행 수·순서·이름**을 맞춘다 | `tests/report/test_form_conformance.py::test_summary_rows_match_the_form` |
> | ★ 접두 규칙에 **구분자 조건**을 더했다 — 접두를 뗀 나머지가 `""` · ` · ` · ` (` 중 하나로 시작해야 한다 | 같음 `::_assert_prefix_rows` |
> | 표 첫 열 읽기를 표제 검사와 **공유**로 뺐다 | 같음 `::_first_column` · `::_slice_between` |
> | 양식 【1】 행 이름 정정 · 머리말에 「표의 행 목록도 붙든다」 선언 | `docs/report-form-심의보고서.md` |
> | 낡은 「여섯 행」 독스트링 제거 | `core/report/narrative.py::_summary_section` |
>
> ### ★★★ 왜 `startswith` 만으로는 부족한가 — **M4 가 초록불이었다**
>
> 실물 첫 행의 이름을 「결론」 → 「**결론 축 · 순현재가치**」로 바꾸면 **같은
> 이름의 행이 둘**이 되는데, 「결론」은 그 이름의 접두이므로 **접두 검사는
> 여덟 행 전건 통과한다**(2026-08-18 실측). 구분자 조건을 걸면 나머지가
> ` 축 · 순현재가치` 라 걸린다.
>
> ⚠ **순서 뒤바꿈은 접두 검사도 잡는다**(맞바꾼 반대쪽에서 걸린다 — M3).
> 이 규칙이 사는 값은 **이름이 자라는 변이** 쪽이다. 검사 독스트링에 그렇게
> 적었다 — 처음에 「순서 뒤바꿈을 못 잡는다」고 적었다가 실측으로 고쳤다.
>
> ### 변이 — **여섯 전건 빨간불**
>
> | # | 변이 | 대상 | 판정 |
> |---|---|---|---|
> | 1 | 요약에서 `잠정성` 행을 지운다 | 실물 | 빨간불 |
> | 2 | 양식에 없는 `비고` 행을 더한다 | 실물 | 빨간불 |
> | 3 | `결론` 과 `결론 축` 의 순서를 맞바꾼다 | 실물 | 빨간불 |
> | 4 | ★ `결론` 행이 `결론 축` 의 이름을 달고 나온다 | 실물 | 빨간불 (**접두 검사만이면 초록불**) |
> | 5 | 양식 쪽 행 이름을 「결합 전환 조건」으로 되돌린다 | **양식** | 빨간불 |
> | 6 | 실물 행 이름을 `미계상 항목` 으로 바꾼다 | 실물 | 빨간불 |
>
> ★ 1·6 은 **이 검사 하나만** 빨간불이었다 — 그 자리를 보던 것이 없었다는 증거다.
>
> ### ★★★ 판단 — **거리 행은 요약에 올리지 않는다**
>
> `todo.md` 2번이 붙들고 나서 판단하라 한 것에 답한다. **올리지 않는다** —
> 요약의 `결론 축 · 순현재가치` 가 **−5,156,392원**이고 5.1 의 「결론까지 남은
> 거리」가 **결손 5,156,392원**이다. **같은 수를 부호만 뒤집어 두 번 싣는 것**이며,
> 양식이 요약에 금지한 *「새 수를 만들지 않는다」* 에 걸린다.
>
> ★ **요약에 없는 것은 거리가 아니라 「무엇에 대한 거리인가」다** — 총사업비
> 대비 **52.6%**, 그리고 그것과 같은 수인 **결론 전환 지원율 52.6%**. 절대액은
> 사업 규모를 모르면 크기를 못 읽지만 비율은 읽힌다. 요약의
> `결론 전환 인자 (단독)` 칸은 여전히 **「없음 (1변수 스윕 범위 내)」** 이고,
> R35 ② 가 5.1 에서 없앤 그 「없음」이 **첫 화면에는 그대로 남아 있다.**
>
> ⚠ **이번 라운드에서 하지 않았다** — 요약에 행을 더하는 것은 검토자가 먼저
> 보는 규정을 바꾸는 일이라 사용자 판단으로 올린다(`todo.md` 새 1번).
>
> ### 실측 — 게이트 (2026-08-18 · R35 ③)
>
> `pytest` **수집 1,572건 rc=0**(3.11 도 rc=0) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상 ·
> `docs/traceability.md` 변경 없음) · `check_task_mapping` rc=0 · `check_*` 7종 rc=0 ·
> `check_file_size --code-strict` rc=0 · **음성 스위트 11종 rc=0** ·
> 잔여 `.mutbak` 0건.
>
> ⚠ **리포트를 재생성하지 않았다** — 이번 변경은 검사 · 양식 문면 · 독스트링뿐이고
> 렌더러의 출력 바이트를 건드리지 않았다. `docs/evidence/` 는 그대로다.
>
> ---

> # ⏹ 2026-08-17 **R35 ② 완료 — 「없음」 한 줄을 거리로 바꿨다** (5.1)
>
> 사용자가 todo.md 「0. 결정 대기」 둘에 답했다 — **push 를 먼저** 하고(위 ✔ ·
> CI 둘 `success`), 다음 항목은 **1번(5.1 이 비었을 때 무엇을 싣는가)**. 닫았다.
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **결론까지 남은 거리**를 5.1 이 싣는다 — 결론 축의 절대값 · 방향 라벨(`결손`/`여유`) · **총사업비 대비 비율**. 분모는 **무지원 기준선의 초기지출**이다(지원 후 초기지출을 쓰면 같은 사업의 결손 비율이 지원율에 따라 달라진다) | `case_report.py::{conclusion_gap_won,total_project_cost_won}` · `narrative.py::_gap_lines` |
> | ★ **결론 전환 지원율** — 그 거리를 `t=0` 일시 지원으로 환산한 값. 환산은 **한 함수**(`break_even_subsidy_rate`)가 하고 본문과 붙임 3 이 그것을 함께 쓴다 | 같음 · `_formulas` |
> | **전환까지 남는 거리 표**(7열) — 인자를 검토 범위 **끝까지** 밀었을 때 0 선까지 남는 거리. 방향은 인자마다 적어 두지 않고 **결론에서 읽는다** | `narrative.py::{_remaining_gap_table,_flip_direction_end}` · `InfluenceEntry.npv_low`·`npv_high` 신설 |
> | 붙임 3 에 **네 번째 산식** — 「결론 전환 지원율」 3중 표기 | `_formulas` · 양식 §3 붙임 3 행 |
> | 양식 5.1 에 **전환 인자 0건일 때 싣는 것**을 규정(세 항목 · 분모·표의 성격까지) | `docs/report-form-심의보고서.md` |
> | 검사 8건 신설 · 변이 9건 전건 빨간불 | `tests/report/test_conclusion_gap.py` |
> | 리포트 재생성 (본문 163 → **173줄** · 상한 200) | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md` |
>
> ### ★★★ 왜 「없음」으로는 안 되나
>
> **「없음」은 *조금 모자란다* 와 *두 배 모자란다* 를 같은 글자로 적는다.** 5.1 이
> 양식에서 지는 물음은 *「무엇이 얼마가 되면 뒤집히는가」* 이고, 뒤집히는 인자가
> 0건이면 그 물음은 **「그럼 얼마나 모자란가」** 로 남는다. 그 답이 이제 수로 있다 —
> 결손 **5,156,392원**은 **총사업비 9,800,000원의 52.6%** 이며, 그것이 곧
> *「보조율 52.6% 에서 0 이 된다」* 다.
>
> ⚠ **판정을 싣지 않았다**(양식 0절). 「부족하다」·「가깝다」는 작성자의 말이므로
> 방향은 `결손`·`여유` 라벨 한 칸이 지고 크기는 수가 진다.
>
> ### ★★★ 실측 — 5.1 이 지금 싣는 표 (무보조 · 실물 대장)
>
> | 인자 | 사용값 | 전환 방향 끝 | 그 끝의 남은 거리 |
> |---|---|---|---|
> | `surplus_sale_price` | 110 원/kWh | **150** | **2,098,749원** |
> | `grid_purchase_price` | 120 원/kWh | 80 | 3,981,021원 |
> | `ess_unit_cost` | 500,000 원/kWh | 400,000 | 4,156,392원 |
> | `pv_unit_cost` | 1,600,000 원/kW | 1,280,000 | 4,196,392원 |
> | `tariff_escalation` | 2.5 %/년 | **—** | 5,156,392원 (기준과 같다) |
>
> ★ 첫 행이 R35 ① 이 손으로 적어 둔 *「상단 150 에서도 약 −210만원」* 과 같은 수다 —
> 그 수가 이제 **리포트에 실린다.**
>
> ★ **변동폭 순위와 남는 거리 순위는 서로를 함의하지 않는다.** 변동폭이 커도 반대
> 끝에서 시작하면 끝까지 밀어도 더 멀다. 지금 실물에서는 판매단가가 둘 다 1위지만
> 그것은 우연이며, 그래서 이 표는 붙임 2 의 변동폭 표와 **다른 표**다.
>
> ★ `tariff_escalation` 의 **방향 칸이 `—`** 인 것이 요점이다. 두 끝의 결론 축이
> 같으면 「전환 방향」이 없고, 한쪽을 적으면 *그쪽으로 밀면 가까워진다* 를 함의한다.
> 사실은 `산출` 열의 미반영 라벨이 나른다.
>
> ### ★★★ 변이 — **아홉 전건 빨간불 · 아홉 번째가 검사를 하나 더 만들었다**
>
> | # | 변이 | 판정 | 잡은 검사 |
> |---|---|---|---|
> | 1 | 거리 두 줄을 지운다 | 빨간불 2건 | `…how_far_the_conclusion_is` · `…recovering_scenario…` |
> | 2 | 전환 지원율에서 **현 지원율을 빼먹는다** | 빨간불 | `…agrees_across_two_support_levels` **만** |
> | 3 | 총사업비 분모를 **지원 후 초기지출**로 | 빨간불 2건 | 위 + `…formula_carries_the_same_number` |
> | 4 | 전환 방향을 **반대 끝**으로 읽는다 | 빨간불 2건 | `…shows_the_gap_that_remains` · `…recovering_scenario…` |
> | 5 | 미반영 라벨을 **항상 1변수 스윕**으로 | 빨간불 | `…not_shown_as_a_closed_gap` |
> | 6 | 붙임 3 산식이 **본문과 다른 수**를 싣는다 | 빨간불 | `…formula_carries_the_same_number` |
> | 7 | 남는 거리 표를 **싣지 않는다** | 빨간불 3건 | 위 셋 |
> | 8 | 거리에서 **절대값을 뗀다** | 빨간불 2건 | `…how_far…`(`gap > 0` 단언) · 미반영 |
> | 9 | ★ **두 끝의 결론 축을 서로 바꿔 싣는다** | ★**초록불 7건** → 검사 신설 후 **빨간불** | `…endpoint_values_are_paired_with_the_run` **신설** |
>
> ★★ **9번이 드러낸 것 — 거리만 보는 검사는 거짓 문장을 통과시킨다.** 두 끝의 NPV 를
> 서로 바꿔 실으면 거리 숫자는 그대로 맞고 열 순서도 그대로인데 **문장만 거짓**이 된다:
> *「판매단가를 150원까지 올려도 210만원 부족」* 이 실은 **80원일 때의 수**다. 그래서
> 짝을 **다른 층에서** 검산한다 — 수준표(`build_level_map`)의 `low`·`high` 로 진입점
> (`run_single_case_e2e`)을 직접 돌려 리포트가 그 이름에 붙여 둔 수와 맞추었다.
> R33 이 임계값에서 세운 검사(*「보고된 값으로 다시 돌려 본다」*)와 같은 형태다.
>
> ⚠ 2번도 같은 결의 확인이다 — **한 시나리오만 보는 검사로는 잡을 수 없었다.** 어떤
> 잘못된 환산도 자기 시나리오에서는 자기 자신과 맞는다. 무보조(0%)와 보조 80% 가
> **같은 전환 지원율**을 내야 한다는 대조가 그 자리를 붙든다.
>
> ### ⚠ 함께 고친 검사 둘 — 허용 라벨을 **목록으로** 넓혔다
>
> `test_combined_scenarios.py` 의 두 검사가 5.1 의 **모든** 인자 행에 `1변수 스윕`
> 라벨을 요구했다. 새 표의 미반영 인자 행은 붙임 2 와 같은 라벨(`파이프라인 미반영`)을
> 쓰므로 허용을 둘로 넓혔다. **「무엇이든 있으면 통과」로 넓히지 않았다** — 라벨을
> 지우는 변이는 여전히 빨간불이며, 그것이 그 검사가 지키는 것이다.
>
> ### 하지 않은 것 — **1절 요약에는 거리를 넣지 않았다**
>
> 요약의 전환 인자 칸은 여전히 「없음 (1변수 스윕 범위 내)」이다. 거리를 요약에도
> 넣으려면 양식 【1】의 행 목록을 함께 고쳐야 하는데, **그 목록은 이미 실물과 어긋나
> 있고**(양식 6행 · 실물 8행) 어느 검사도 그것을 붙들지 않는다. 요약 행을 늘리는 것은
> 그 어긋남을 깊게 만드는 일이므로 **양식 【1】을 검사로 붙드는 일과 함께** 해야
> 한다 — `todo.md` 에 올렸다.
>
> ### 실측 — 게이트 (2026-08-17 · R35 ②)
>
> `pytest` **수집 1,571건 rc=0**(3.11 도 rc=0) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상 ·
> 파일 재생성 반영 5줄) · `check_task_mapping` rc=0 · `check_*` 8종 rc=0 ·
> `check_file_size --code-strict` rc=0 · **음성 스위트 11종 rc=0** ·
> 잔여 `.mutbak` 0건 · 빈 디렉터리 둘 `rmdir`.
>
> ⚠ **위 R35 ① 블록의 「수집 1,562건」은 한 건 낮았다** — 같은 명령을 이 파일
> **없이** 다시 세면 1,563 이고, 새 검사 8건을 더해 1,571 이다. 이 절의 규칙대로
> **베끼지 않고 다시 셌더니** 드러났다.

> # ⏹ 2026-08-17 **R35 ①「잉여 판매단가를 대장으로」 완료**
>
> R34 가 「남은 판단」 1번으로 남긴 것을 닫았다. 사용자가 *「push 먼저 하고 1번
> 시작」* 이라 지시했으며 **커밋 3건은 `origin/main` 에 올라갔다**(R34 분량).
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **잉여 판매단가를 수준표로 올렸다** — 종전 `e2e_runner` 안의 `SurplusSale(sale_price_won_per_kwh=120.0)` **리터럴**. 새 케이스 변수 `surplus_sale_price` · 대장 키는 **기존 항목 `tariff.surplus_direct_sale`(Q-16 · 110원/kWh · 80~150)** 이며 새 항목을 세우지 않았다(같은 물리량의 단가가 대장에 둘이 되므로) | `ledger_levels.py::_LEDGER_VARS` · `e2e_runner.py` |
> | **기본값을 두지 않았다** — 수준표에서 빼면 `_resolve` 가 거부한다. `horizon_years`(R31)·`grid_purchase_price`(R34)와 같은 판단 | 같음 |
> | 대장 부기 갱신 — `energy_only` 의 「우연히 같다」 경고를 **해소 기록**으로, `surplus_direct_sale` 의 `applicable_scope` 에 기본 경로를 명시하고 **`impact` 를 「중간」 → 「최상」** 으로(아래 실측이 근거다) | `docs/assumptions.yaml` |
> | 검사 6건 신설 · 변이 5건 전건 빨간불 | `tests/casegrid/test_surplus_sale_price_wiring.py` |
> | 리포트 재생성 · 추적표 재생성 | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md` · `docs/traceability.md` |
>
> ### ★★★ 왜 「대장 키를 `tariff.surplus_direct_sale` 로」 인가
>
> 구조를 주지 않은 기본 경로는 **상계도 특구 직접거래도 아닌 판매**이며 그것이 그
> 항목의 `applicable_scope` 문면 그대로다. 약관요금(`…avg` 150원/kWh)을 쓰면
> *소매가로 파는 사업*이 되고, 그 값은 `_net_metering` 이 **회피한 요금**으로
> 쓰는 것이다(`settlement.py::TARIFF_KEY`). 즉 세 단가는 각각 다른 것을 뜻한다 —
> **사는 값**(`energy_only` 120) · **파는 값**(`surplus_direct_sale` 110) ·
> **회피한 값**(`avg` 150).
>
> ### ★★★ 실측 — **영향도 1위가 바뀌었다** (리포트 재생성 후 재판독)
>
> | | R34 (판매단가 = 리터럴 120) | R35 (대장 110) |
> |---|---|---|
> | 순현재가치 (무보조) | −4,391,980원 | **−5,156,392원** |
> | 순현재가치 (보조 80%) | +3,448,020원 | **+2,683,608원** |
> | 1년차 편익 | 904,860원 | **846,095원** (잉여판매 705,180 → **646,415**) |
> | 영향도 1위 | `grid_purchase_price` (2,644,579원) | **`surplus_sale_price` (5,350,876원)** |
> | `pv_capacity_kw` 한계 기여 | +451,092원/kW | **+322,898원/kW** (단조 증가 · 최선 9kW — 형태 유지) |
> | `ess_capacity_kwh` 한계 기여 | −290,977원/kWh | **−326,699원/kWh** (단조 감소 · 최선 2kWh — 형태 유지) |
> | 단독 전환 인자 | 0건 | **0건** (상단 150 에서도 약 −210만원) |
>
> ★ **리터럴이던 동안 이 인자는 영향도 표에 오르지 못했다** — 흔들 수 없는 값은
> *영향이 없는 값과 같은 모양*으로 보인다. 그런데 재어 보니 **변동폭이 종전 1위의
> 두 배**이고 기준선 순현재가치와 같은 크기다. R33 이 `tariff_escalation` 에서
> 만난 형태의 **거울상**이다: 그쪽은 축에 있는데 파이프라인이 읽지 않아 「영향
> 0원」으로 실렸고, 이쪽은 파이프라인이 읽는데 축에 없어 **아예 실리지 않았다.**
>
> ⚠ **`NFR-202` 검사가 이 리터럴을 잡을 수 없었다** — 판정이 |값| ≥ 1,000 만 보므로
> 120 은 사각지대다(「미해결」의 그 행). 즉 이번 작업은 검사를 통과시키는 일이
> 아니라 **검사가 볼 수 없는 자리를 없애는 일**이었다.
>
> ✔ **차익거래의 우연이 해소됐다.** 두 단가가 120 으로 같은 동안 저장장치의 심야
> 충전·주간 판매는 왕복효율(90%) 손실만큼만 순손실이었다. 지금은 **사는 값이 파는
> 값보다 10원/kWh 비싸므로** 효율 손실 이전에 이미 손실이며, 저장장치 한계 기여가
> 더 내려갔다. **부호는 뒤집히지 않았다** — R34 의 결론(ESS 는 줄이고 PV 는 키운다)이
> 그 우연 위에 서 있던 것은 아니었다.
>
> ### ★★★ 변이 — **하나가 초록불이었고 그 자리가 요점이다**
>
> | # | 변이 | 판정 | 잡은 검사 |
> |---|---|---|---|
> | 1 | 판매단가를 리터럴 120 으로 되돌린다 | 빨간불 3건 | `…reaches_the_npv` · `…measured_surplus_times_the_price` · `…from_the_ledger` |
> | 2 | 빠진 판매단가를 기본값으로 메운다 | 빨간불 | `…refuses_to_run_without_a_sale_price` |
> | 3 | 대장 키를 **약관요금**(`…avg`)으로 바꾼다 | ★**초록불** → 검사 신설 후 **빨간불** | `…prices_the_surplus_like_the_surplus_structure` **신설** |
> | 4 | 사는 값과 파는 값을 **한 대장 줄**로 묶는다 | 빨간불 2건 | 위 + `…two_prices_are_separate_ledger_items` |
> | 5 | 대장 연결을 지운다 | 빨간불 3건 | 위 셋 |
>
> ### ★★★ 변이 3 이 드러낸 것 — **검사가 자기 검사 대상에서 정본을 읽어 왔다**
>
> `test_the_deploy_path_prices_the_surplus_from_the_ledger` 는 대장 키를
> `ledger_backed_variables()` 에서 **읽어** 와 그 항목의 값과 대조했다. 그래서
> `_LEDGER_VARS` 의 그 줄을 약관요금으로 바꾸는 변이가 **아무 검사도 빨간불로
> 만들지 않았다** — 검사가 변이를 따라가 *「러너는 표가 말하는 키를 쓴다」* 만
> 확인한 것이다. **참이지만 공허하다.**
>
> 그 변이가 통과하면 잉여를 **소매가로 파는 사업**이 되고, 저장장치 차익거래가
> 항상 본전이 된다. 금액은 그럴듯하고 아무 예외도 나지 않는다.
>
> 고친 방법은 **사본을 만들지 않는 쪽**이다 — 키를 검사에 리터럴로 적으면 대장
> 키를 옮길 때 두 곳을 고쳐야 한다. 대신 `settlement.py::SURPLUS_SALE_KEY`
> (조립기가 「잉여 직거래」에 쓰는 그 상수)에 대고 맞췄다. 두 자리가 같은 판매를
> 다른 단가로 세면 **구조 비교(`FR-202`)가 구조의 차이가 아니라 배선의 차이를
> 재게 된다** — 그 사실이 이제 검사 문면에 있다. 함께 `!= TARIFF_KEY` 도 본다.
>
> ⚠ **값의 크기는 단언하지 않았다.** 「파는 값이 사는 값보다 싸다」는 지금 실측이지
> 도메인 규칙이 아니다 — 두 범위(80~150 · 80~170)가 겹치므로 단언하면 근거 없는
> 규칙을 발명하게 된다. 실측은 대장 부기와 이 표에 적었다.
>
> ### 남은 판단 — R35 의 다음 항목들 (R34 목록에서 1번을 뺀 것)
>
> 1. **5.1 이 비었을 때 무엇을 싣는가** — 전환 인자 0건이면 검토자가 *「얼마나
>    부족한가」* 를 5.1 에서 읽을 수 없다. 지원 필요액을 올릴지 판단.
>    **6.2 는 R34 가 「없음」 행으로 닫았다.** ★ R35 실측으로 **붙임 2 의 변동폭이
>    실제 우선순위를 말하기 시작했다**(판매단가 535만원) — 5.1 판단의 재료가 늘었다
> 2. **프로포마 행 목록을 `CaseOutcome` 에 실을 것인가** — R34 「붙들 수 없는 갈래」
> 3. ➕ **붙임 4 의 편익 산식이 단가를 싣지 않는다** (R35 발견). 비용 쪽은
>    `대표일 수전 6.19kWh × 365일 × 120원/kWh` 로 **수량과 단가를 갈라** 적는데,
>    편익 쪽은 `대표일 1,771원 × 365일` 뿐이어서 **판매단가가 붙임 4 어디에도
>    없다**. 즉 영향도 1위 인자의 값을 검토자가 산식에서 확인할 수 없다(붙임
>    1·2 에는 있다). 고치려면 편익이 산식의 **대입값**을 내놓아야 하고, 그것은
>    편익 8종이 함께 움직일 때 할 일이다 — 러너에 태그 목록을 두면 R34 가 없앤
>    형태(`scales_with_dispatch_window` 이전)로 되돌아간다
> 4. `FR-401-AC2.VPPMarket`(Phase 2) — R33 표 4번 그대로
> 5. **`SettlementInputs.annual_generation_kwh` 가 협상값 자료형에 남아 있다** —
>    사유는 그 필드 주석에. 호출자가 사라지면 옮긴다
> 6. ⚠ **새 PV 결론을 그대로 믿지 말 것** — 이유는 여전히 **평탄 발전 프로파일**
>    (야간 발전)이며 붙임 8 이 크기를 잰다. 일사 곡선이 배선되면 다시 뒤집힐 수
>    있다. **R35 는 이것을 건드리지 않았다**
>
> ### 실측 — 게이트 (2026-08-17 · R35)
>
> `pytest` **수집 1,562건 rc=0**(3.11 도 rc=0) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상 ·
> 파일 재생성 반영) · `check_task_mapping` rc=0 · `check_*` 8종 rc=0
> (`check_unread_extension_points` 포함) · `check_file_size --code-strict` rc=0 ·
> **음성 스위트 11종 rc=0** · 잔여 `.mutbak`·`# NEGCHECK` 0건.

> # ⏹ 2026-08-17 R34 종료 · **미확인 셋을 닫았다 — 게이트 전건 rc=0 · 변이 일곱 전건 빨간불**
>
> R34 의 본 작업(계통 전력 구매 비용 배선 · 집합 PPA 통로 · 365배 결함)은 앞
> 세션이 놓았고 **확인하지 못한 채 중단했다**. 이 세션이 닫은 것이 아래다.
>
> | # | 확인한 것 | 결과 |
> |---|---|---|
> | **1** | `pytest` 전건 | **수집 1,554건 · rc=0.** 직전 실행의 `rc=1`(계약 6건)은 닫혔다 — `__init_subclass__` 검사 순서 교체와 `_Stub` 선언이 실제로 들었다 |
> | **2** | 새 계약 검사 `tests/contract/test_annualisation_convention.py` | **4건 전건 통과**(`_PROBES` 표로 편익 8종을 세우는 부분 포함). `ruff` 가 이 파일에서 3건(import 정렬 · `RUF001` 둘)을 냈고 고쳤다 |
> | **3** | 게이트 전건 | 전부 `rc=0` — 아래 「실측」 절 |
> | **4** | **변이 6건 + 갈래 하나** | **셋이 초록불이었다.** 검사 다섯을 세우고 다시 심어 전건 빨간불 — 아래 표 |
> | **5** | 실물 리포트 재생성 + **눈으로 읽기** | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md`. **낡은 문면 둘 + 빈 표 하나**를 찾아 고쳤다 |
>
> ### R34 가 놓은 것 — **셋** (앞 세션 · 이 세션이 게이트로 확인했다)
>
> | 무엇 | 어디에 |
> |---|---|
> | **계통 전력 구매 비용을 배선했다** — 새 대장 항목 `tariff.hv_single_contract.energy_only` 120원/kWh(**전력량요금만**. 실효단가 `…avg` 150원/kWh 를 그대로 쓰지 않은 이유: 그 값은 *기본요금 안분 포함* 이고 실행 경로가 그 기본요금을 이미 **첨두 절감 편익**(8,320원/kW·월)으로 세고 있어 **같은 성분이 비용·편익 양쪽에 들어간다**. 범위 80~170 · `q_ref` 는 **`Q-6` 을 그대로** 쓴다: 확보 대상이 같은 약관 하나이고, 새 Q 를 세우면 §16.6 현황판이 실제보다 결손이 많아 보인다) · 수준표 변수 `grid_purchase_price` · 비용 행 생성기 `energy_purchase_row()` · `CostLine` 신설 · 붙임 4 비용 항목 표 · 4.3 잔차 행 | `docs/assumptions.yaml` · `ledger_levels.py` · `core/cba/proforma.py` · `core/casegrid/{models,e2e_runner}.py` · `core/report/method_sections.py` |
> | **집합 PPA 통로** — `_with_model_generation()` 이 `PV.annual_generation_kwh(year=1)` 을 물어 조립기에 넣는다. **호출자가 값을 함께 주면 거부한다**(정본이 둘이 되므로) | `core/casegrid/e2e_runner.py` |
> | ★★★ **그 통로를 열자 365배 결함이 드러났다** — 러너가 정산 편익 **전건**에 365를 곱하고 있었다. 그 암묵 규약은 잉여판매·상계에서만 맞았고, 생성자에서 **연간** 수량을 받는 편익에서는 365배가 됐다(집합 PPA 502,605원/년 → **183,450,825원**). **아무 예외도 나지 않았다.** `ValueStream` 에 **`scales_with_dispatch_window`** 를 기본값 없이 신설하고 러너가 그 선언을 읽게 했다 | `core/contracts/valuestream.py` · 편익 8종 · `e2e_runner.py` |
>
> ### ★★★ 실측 — **결론이 뒤집혔다** (오늘 재생성한 리포트에서 다시 읽었다)
>
> | | 배선 전 | 배선 후 (2026-08-17 리포트) |
> |---|---|---|
> | 순현재가치 (무보조) | −865,881원 | **−4,391,980원** |
> | 1년차 운영비 | 200,000원 | **471,073원** (구매 271,073원 신설) |
> | `pv_capacity_kw` 한계 기여 | −61,681원/kW · 단조 감소 · 최선 1kW | **+451,092원/kW · 단조 증가 · 최선 9kW** |
> | `ess_capacity_kwh` 한계 기여 | +190,445원/kWh · 단조 증가 · 최선 23kWh | **−290,977원/kWh · 단조 감소 · 최선 2kWh** |
> | 영향도 1위 | `ess_unit_cost` | **`grid_purchase_price`** (변동폭 2,644,579원) |
> | 단독 전환 인자 | 있었다(할인율 3.44% 등) | **0건** — 검토 범위 안에서 0 선을 넘기는 인자가 없다 |
>
> **두 설계 변수의 부호가 모두 반대로 돌아섰다.** 종전 「ESS 를 키울수록 좋다」는
> *공짜로 받아 파는 기계*가 만든 결론이었다. ⚠ 새 PV 결론도 그대로 믿지 말 것 —
> 아래 「남은 판단」 6.
>
> ⚠ 전환 인자가 0건이 되며 **검사 넷이 무력화됐다**(셋은 `set() != set()` 로 정당한
> 상태를 빨간불로, 둘은 순회 0회로 조용한 초록불). `tests/report/conftest.py` 의
> **탐침 대장**(`flip_probe_assumptions` — 검토 범위 셋만 넓히고 기준선 수치는 실물과
> 같다)으로 갈랐다.
>
> ### ★★★ 변이 — **셋이 아무것도 붙들지 못했다**
>
> | # | 변이 | 첫 판정 | 조치 | 재판정 |
> |---|---|---|---|---|
> | 1 | `energy_purchase_row` 호출을 지운다 | 인계가 지목한 검사 셋(미반영 둘·서술)은 **전건 초록불**. 넓혀 돌리니 전환 인자 검사 둘이 빨간불이었으나 **값이 움직여 깨진 것**이고 구매 비용을 이름으로 붙드는 검사는 0건 | `test_the_purchase_price_reaches_the_npv`(단가 0 → 200 에 NPV 가 줄어드는가) · `test_the_cost_total_and_the_itemised_lines_agree` **신설** | **빨간불 2건** |
> | 2 | 비용 **항목**을 조건부로 (`if not annual_purchase_won: 행을 뺀다`) | — (검사가 없어 새로 만들고 심었다) | `tests/casegrid/test_grid_purchase_cost_row.py` **신설** — 단가 0 으로 금액 0을 만들고 **행의 존재**를 본다 | **빨간불** |
> | 2b | **프로포마 행**을 조건부로 | ★초록불 | **만들지 않았다** — 아래 「붙들 수 없는 갈래」 | 초록불(설계상) |
> | 3 | 러너가 `scales_with_dispatch_window` 를 무시하고 전건에 365 | 빨간불 | — | — |
> | 4 | `AggregatedPPA.scales_with_dispatch_window` 를 `True` 로 거짓 선언 | 빨간불 2건(선언·실물 대조 + 진입점) | — | — |
> | 5 | `_with_model_generation` 이 호출자 입력을 **조용히 덮어쓴다** | — (검사가 없었다) | `test_the_caller_may_not_hand_the_runner_a_second_generation_number` + **양성 짝**(용량 3배 → PPA 편익 3배) 신설 | **빨간불** |
> | 6a | 붙임 4 **비용 항목 표**를 지운다 | ★초록불 — `tests/report` **전건**에서 아무도 잡지 않았다 | `test_each_cost_item_shows_its_amount_and_formula` 신설 (편익 쪽 검사의 반대편) | **빨간불** |
> | 6b | 4.3 **자원 미귀속 잔차 행**을 지운다 | ★초록불 — 같음 | `test_the_resource_table_carries_the_costs_that_belong_to_no_resource` 신설 | **빨간불** |
>
> ⚠ **하네스가 경고한 그대로 밟았다** — 변이를 여럿 이어 돌리다 **2분 시간초과로
> 죽자 `finally` 가 건너뛰어져 `core/report/method_sections.py` 에 `if False:` 가
> 남았다.** `.mutbak` 로 원복했다. 변이는 **한 번에 하나씩, 대상을 좁혀** 돌린다.
>
> ### ★★ 붙들 수 없는 갈래 — **변이 2b 는 관측 가능한 차이를 만들지 않는다**
>
> 프로포마 쪽 구매 행을 `if annual_purchase_won:` 로 조건부화해도 **밖에서 보이는
> 수가 한 자리도 움직이지 않는다**: 그 갈래가 성립하는 것은 금액이 0일 때뿐이고,
> 0원 행을 빼도 합계·NPV·회수기간이 같다. `CaseOutcome` 이 현금흐름 **행 목록**을
> 내지 않으므로 검사가 볼 자리가 없다.
>
> **판단이 필요하다** — 그 자리를 검사로 붙들려면 `CaseOutcome` 에 프로포마 행을
> 실어야 한다. 지금은 러너의 주석과 `basis.costs` 쪽 검사만이 규약을 지킨다.
>
> ### ✔ 실물을 눈으로 읽고 찾은 셋 — **전부 「배선했더니 문장이 거짓이 됐다」**
>
> | 어디 | 무엇이 거짓이었나 | 어떻게 고쳤나 |
> |---|---|---|
> | 3.2 계산 규약 「프로포마 비용 행」 | *「고정 운영비」* — 계통 전력 구매가 비용 행이 된 뒤로 거짓 | **실린 항목에서 지어 낸다**(`basis.costs` 의 라벨) — 구성이 바뀌면 함께 움직인다 |
> | 붙임 8 「계통 전력 구매 비용」 판정 문면 | *「금액 미정량 (구매 단가 없음)」*·*「구매 단가(소매 요금) 부재」* — 단가는 대장에 있다 | **금액을 잰다**(수량 × 단가) · 사유를 「행이 없다」로 · 해소 조건을 「행 배선」으로 |
> | `case_report.py` 가정 운전 주석 | *「자가소비 절감은 소매 단가가 없어 계상할 수 없다」* (인계가 지목한 남은 한 곳) | 진짜 사유로 갱신 — **배타 규칙 유형 A** 와 **부하 미확보(`Q-3`)**. 단가는 있고 붙임 8 이 그 단가로 실제로 잰다 |
>
> ➕ **6.2 「결론 전환 조건」이 머리만 남은 빈 표로 인쇄됐다** — 전환 인자가 0건이
> 되며 행이 하나도 없었다. 검토자에게 빈 표는 *「없다」* 와 *「싣지 못했다」* 를
> 구별해 주지 않는다(1절 요약은 이미 「없음 (검토 범위 내)」로 적고 있었다).
> `NONE_IN_RANGE` 상수를 세워 두 자리가 같은 문면을 쓰게 하고,
> `test_the_flip_condition_table_is_never_left_empty` 로 붙들었다.
>
> ### 실측 — 게이트 (2026-08-17)
>
> `pytest` **수집 1,554건 rc=0** · `ruff` 0건 · `mypy` **114파일** 무결 ·
> `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상) ·
> `check_task_mapping` rc=0 · `check_*` 7종 rc=0 · `check_file_size --code-strict`
> rc=0 · **음성 스위트 11종 rc=0** · **CI 버전(3.11) rc=0**.
>
> ⚠ **`lint-imports` 가 `PYTHONUTF8=1` 없이 rc=1 이었다** — 계약 위반이 아니라
> 자기 배너를 cp949 로 디코드하다 죽은 것이다. 게이트 절의 그 줄을 고쳤다.
>
> ★★ **커밋 뒤 `check_test_accompaniment --base HEAD~1` 이 실제로 빨간불을 냈다** —
> 붙임 4·4.3 을 새로 붙든 검사가 `render_markdown` 만 거쳐 **`method_sections` 를
> 이름으로 참조하지 않았다**. 그 상태에서는 그 파일을 고치는 사람이 이 검사를
> 함께 보지 않는다. 절을 만드는 함수(`resource_detail_section` ·
> `cost_benefit_section`)에 **직접 묻고** 그 결과가 문서에 실리는지를 따로 보게
> 고쳤다 — 검사가 더 강해졌다(절이 문서에서 빠지는 것도 함께 붙든다).
>
> ★★ **같은 검사가 `core/cba/proforma.py` 도 잡았다** — `energy_purchase_row()` 를
> 신설했는데 **그 함수를 부르는 검사가 하나도 없었다**(러너 경유로만 닿았다).
> `tests/cba/test_proforma.py` 에 셋을 세웠다: 라벨이 「전력 구매」를 나르는가 ·
> 연도 범위 전건에 같은 금액인가(에스컬레이션 없음이 지금은 옳다 — 편익 쪽이
> 아직 안 올라간다) · **음수 구매 비용을 거부하는가**. 셋째가 요점이다 —
> 통과시키면 *「전력을 사면 돈을 받는 사업」* 이 되어 **저장장치를 키울수록
> 좋아지는 결과**가 부호만 바꿔 되돌아온다. **변이(가드 무력화) 심어 빨간불 확인.**
>
> ### 남은 판단 — **목록은 위 R35 블록이 갖는다**
>
> R34 가 남긴 여섯 중 **1번(잉여 판매단가를 대장으로)은 R35 가 닫았다.** 나머지
> 다섯은 위 「남은 판단 — R35 의 다음 항목들」로 옮겼다 — **여기 다시 적지 않는다**
> (같은 목록이 두 곳에 있으면 한쪽만 고쳐진다). 그 자리에 R35 가 찾은 것 하나
> (붙임 4 의 편익 산식이 단가를 싣지 않는다)가 더 붙었다.
>
> ⚠ 아래 R34 표의 수치(순현재가치 −4,391,980원 등)는 **그 시점의 실측**이며
> 판매단가 배선 뒤 값은 위 R35 표에 있다. 두 표를 견주는 것이 이 파일의 용도다.

> # ⏹ 2026-08-14 R32 종료 · **R32 표 1~5 를 완주하고 사용자 몫을 잠정 확정했다**
>
> 사용자가 *「모든 작업에 우선순위를 정하고 그 순위대로 완료하라」* 와
> *「사용자가 판단해야 하는 부분은 합리적으로 판단하고 추후 검토 과정에서 변경될 수
> 있음을 기재하라」* 고 지시했다. **R32 표의 1~5 를 순서대로 전건 완료했고**, R31 이
> 「사업 판단·자료 공백」으로 갈라 두었던 것을 판정해
> **`docs/decisions-2026-08-14-R32.md`** 에 근거·**「무엇이 오면 바뀌나」**와 함께 적었다.
>
> | # | 무엇을 했나 | 어디에 |
> |---|---|---|
> | **1** | **변형 생산자를 실행 경로에 배선**(`FR-607-AC1`) — `CaseOutcome` 신설, 진입점이 **인자 없이 항상** 변형을 산출한다(깃발로 두면 R21 이 없앤 `is_baseline` 형태가 된다) | `core/casegrid/{models,execution,e2e_runner}.py` · `tests/casegrid/test_variant_production_wiring.py` |
> | **2** | **「단일계약+관리주체 경유」 조립** — 요금엔진(`bill_scenario`)이 두 요금을 내고, 수수료(`Q-14`)는 **비용 행**(`fee_row`) | `core/valuestream/settlement.py` · `core/cba/proforma.py` · `tests/valuestream/test_settlement_manager_structure.py` |
> | **3** | **「개별 세대 직접계약」** — 정산 대상을 **잉여·판매 주체 가구**로 결정하고 **조항에 적었다**(spec v0.16) | `settlement.py::_household_direct_contract` · `surplus_sale.py` · `tests/valuestream/test_settlement_household_contract.py` |
> | **4** | **「집합 PPA」 편익 클래스 신설** + `FR-401-AC2.AggregatedPPA` 조항 신설 + 배타 2행 | `core/valuestream/aggregated_ppa.py` · `docs/exclusion-rules.yaml` · `tests/valuestream/test_aggregated_ppa.py` |
> | **5** | **`FR-205-AC1` 을 구조 7건으로 전개** — 수용기준 314 → **321**, **미매핑은 8건 그대로** | spec `FR-205` · 작업 목록 7.3·8.1 · 마커 49곳 |
> | 6 | `LoadedSeeds` 이동은 **하지 않았다** — 소비자가 0곳이라 옮기면 호출자 없는 제품 모듈이 생긴다(R32 표가 지시한 그대로) | — |
>
> ### ★★★ R32 가 찾은 것 — **셋 다 「선언·계산은 있는데 읽는 쪽이 없다」**
>
> | 무엇 | 어떻게 드러났나 |
> |---|---|
> | **비용 행이 NPV 를 늘리고 있었다** | 수수료를 비용으로 넣자 **수수료율을 올릴수록 NPV 가 커졌다.** `CashFlowRow` 는 부호 규약이 없고 `npv()` 는 부호 있는 합을 보는데 진입점이 비용을 그대로 넘겼다 — **고정 O&M 이 편익으로 더해지고 있었다.** 비용 항목이 O&M 둘뿐일 때는 아무도 밟지 않았다 |
> | **`CaseVariant.overrides()` 소비자 0곳** | 순회가 `baseline` 깃발만 봤다. 변형이 둘일 때는 두 기계가 같은 답을 내고 **셋째에서 갈린다** — 보조율 15% 변형이 입력 지원안과 **똑같은 NPV** 를 낸다 |
> | **편익 `exclusions()` 소비자 0곳** | 클래스 선언에서 자가소비 배타를 **지우는 변이가 전건 초록불**이었다. 정본은 YAML 이므로 선언이 그것과 어긋나지 못하게 래칫을 세웠다 |
>
> **인수 변이 16건을 직접 심었고 전건 빨간불**이며, **각 변이가 서로 다른 검사에
> 잡혔다**(같은 한 줄에서만 잡히면 경로를 따로 붙들지 않는다는 뜻이다).
>
> # ▶▶ **지금 바로 할 일 — 리포트 「1차 의견」 대응 6건** (2026-08-15 접수)
>
> 사용자가 리포트에 **1차 의견**을 냈고 판정까지 마쳤다. **`/clear` 이후 이
> 절부터 읽고 번호 순서대로 진행한다.** 판정 근거와 실측은 아래 표에 있다.
>
> ⚠ **사용자 지시 둘**:
> ① *「전제와 구성은 보고서 검토 결과의 **신뢰도**와 관련된 사항이며 `MC-1` 과
> **별도로** 수행할 부분」* — 즉 3kW PV·10kWh ESS 구성이 타당한가는 이 작업의
> 범위가 아니다. 섞지 말 것.
> ② *「현 단계에서는 **`MC-1` 의 해소에 집중**」*.
>
> # ⏹⏹ **「1차 의견」 6건 전건 완료 · 2026-08-15**
>
> **① 부터 ⑥ 까지 닫았다.** 그 과정에서 사용자가 **방향을 바꾸는 지시**를 냈고
> 그것이 이 라운드에서 가장 큰 변경이다 — 아래 「정형 출력」 절을 **먼저** 읽는다.
>
> ### ★★★ 방향 전환 — **리포트는 정형 출력이며 해설을 싣지 않는다**
>
> 사용자 지시: *「보고서는 프로그램에 따른 정형화된 결과를 출력하는 것이 아닌가?
> LLM 이 설명을 추가하는 방식은 의도한 것이 아님」* · *「이 프로그램이 web 에서
> 제공할 때 LLM 을 이용하지 않기 때문」*.
>
> **근거가 타당하다.** 런타임에 모델이 없으면 리포트의 모든 문장은 미리 박아 둔
> 고정 문면이고, 고정 문면은 데이터를 따라가지 못해 **구성이 바뀌는 순간 계속
> 인쇄되는 틀린 문장**이 된다(이 저장소가 이미 `_lifetime_caveat` 에서 같은
> 판단을 한 번 내렸다). 게다가 해석 문장은 **어떤 검사도 참·거짓을 판정하지
> 못하고**, 심의 문서에서 작성 주체가 불분명하다. `FR-1005-AC1`(비트 단위 동일
> 재실행)도 생성 시점 모델 호출과 양립하지 않는다.
>
> **기준선은 「글자 vs 숫자」가 아니라 「프로그램이 참임을 보증할 수 있는가」다.**
> 부호 규약·연간화 규약·용어 **정의**·`산출: 1변수 스윕` 같은 라벨은 고정 문면
> 이지만 **계산의 정의**이므로 남는다. 걷어낸 것은 판정·권고·독법 지시다.
>
> | 하던 말 | 지금 자리 |
> |---|---|
> | 「이 표는 단독 기여다」 | `산출` 열에 `1변수 스윕` |
> | 「영향 없음으로 읽지 말 것」 | `산출` 열에 `파이프라인 미반영 (변동폭 0원)` |
> | 「그만큼 결과에 관대하다」 | 붙임 8 `방향` 열에 `반영 시 결과 악화` |
> | 「건의 — 다음에 무엇을 할 것인가」 | **삭제.** 6절은 판정·전환 조건·미해소 항목 표만 |
>
> **양식 0절이 이 규정의 정본이다**(`docs/report-form-심의보고서.md`). 렌더러가
> 그것을 따르며, 검사 넷이 인용문(`>`)과 독법 지시 어휘를 붙든다
> (`test_operation_appendices.py`·`test_unreflected.py`·`test_overview_sections.py`).
>
> ⚠ **남은 결과 하나 — `FR-1001-AC5`(MC-1) 문면과의 어긋남.** 조항은 *「비개발자
> 검토자가 **리포트만 보고** 설명할 수 있다」* 인데, 사용자 판정은 *「프로그램이
> 필요한 정보를 가독성 있게 제공하고 **결과물을 사람이 평가**」* 다. 안내를 사람이
> 작성해 함께 준다면 「리포트만 보고」가 성립하지 않는다. **코드로 정할 일이
> 아니므로 `status-human.md` 에 spec 개정 후보로 올렸다.**
>
> ### ★★ 이 라운드가 찾은 것 — **계통에서 산 전력이 공짜였다**
>
> 붙임 7(시간대별 표)을 **실제로 내 보고서야** 드러났다. ESS 가 심야 여섯 스텝에서
> 충전하고 그 전력이 **계통에서 들어온다**(대표일 6.19kWh · 연간화 2,259kWh).
> 프로포마의 비용 행은 고정 O&M 둘뿐이므로 **그 전력을 값 없이 쓰고 있다.**
>
> ⚠ **종전 판정이 틀렸다.** 표 위의 「가구 부하가 없으므로 구매 자체가 없다」는
> **부하와 구매를 같은 것으로 본 것**이며, 부하가 없어도 ESS 충전은 구매를 만든다.
> `_purchase_item` 의 판정 조건도 처음에 「수전이 0인가」로 잡았다가 같은 이유로
> 결함을 놓칠 뻔했다 — 조건은 **「수전이 있는데 비용 행이 없는가」**여야 한다.
> 지금은 붙임 8 이 방향 **`반영 시 결과 악화`** 로 싣고 수량을 재어 적는다.
> `tests/report/test_unreflected.py::test_grid_purchase_without_a_cost_row_is_reported_as_adverse`
> 가 붙든다. **크기가 작지 않다** — 단가 150원/kWh 를 가정하면 연 약 34만원으로
> 첨두절감 편익(199,680원)보다 크다. 「미해결」 표에 올렸다.
>
> ### 「1차 의견」 ②~⑥ 이 놓은 것
>
> | # | 무엇 | 어디에 |
> |---|---|---|
> | **2** | **붙임 6 — 디스패치 규칙과 우선순위.** 규칙 7 전건 · `FR-302-AC1` 문면 · 자원별 배정 · 순위 · 가격 신호 연동. **순서는 실행이 쓴 것**(`CaseOutcome.rule_order`)이며 리포트가 기본 상수를 다시 읽지 않는다 | `core/report/dispatch_sections.py` |
> | **3** | **붙임 7 — 시간대별 운전.** 스텝별 자원 전력·계통 수·송전 + 합계. 송전 합계가 붙임 4 잉여판매 산식의 수량과 이어진다 | 같음 · `dispatch_notes.build_hourly_profile()` |
> | **4** | **2.1 머리의 평가 대상 요약.** 값(자원·금액·기간·할인율·보조율·편익 갈래)이 **전부 계산에서** 온다 — 고정된 것은 조사와 이음말뿐 | `method_sections._target_summary()` |
> | **5** | **붙임 1 을 주제별로.** 대장 키 접두어가 주제축이며 **신뢰도는 열 + 주제 머리 내역**으로 남겼다(둘 다 필요). 선언에 없는 접두어는 **`미분류`로 드러난다** | `appendix_sections.TOPIC_PREFIXES` |
> | **6** | **절충안 그대로.** 요약·3.4 는 항목명 + 방향, 크기·사유·해소 조건은 **붙임 8**. 항목을 **재어** 만든다 — 수명·편익 갈래·운전 결과에서 | `core/report/unreflected.py` |
>
> **배포 호출자 0곳이던 부품 둘이 배선됐다** — `build_dispatch_notes()`(`FR-105-AC4`)
> 와 24스텝 운전 결과. `FR-105-AC4` 는 *「리포트에 표기한다」* 인데 매핑표에서
> 「자동」이면서 **표기하는 리포트가 없었다.**
>
> ⚠ **`charts/dispatch_stack.py` 는 여전히 호출자 0곳이며 그것이 옳다** — 그
> 차트는 `load`(부하 곡선)를 필수 입력으로 요구하고 이 파이프라인에 가구 부하가
> 없다. 붙임 7 이 그 사실을 「미산출」로 싣는다. 부하가 오면 차트가 표를 대체한다.
>
> ### ✔ 마무리 — **양식과 실물을 대조해 넷을 닫았다** (2026-08-15)
>
> *「최종 생성된 보고서와 양식이 부합하는가」* 를 조항별로 실측했다. **골격은
> 부합**했다 — 본문 6절 · 붙임 9종의 번호·순서·이름 전건 일치, 0절 금지 사항
> (인용문 `>` · 독법 지시 · 제언) **실측 0건**, 본문 **168줄**(양식 130~170).
> 그런데 **세부 넷이 어긋나 있었고, 그중 셋은 양식이 낡은 것이었다.**
>
> | # | 어긋난 것 | 어느 쪽을 고쳤나 |
> |---|---|---|
> | **1** | 양식 【표제】가 요구하는 **작성일·작성 주체가 실물에 없었다** | **실물.** 작성 주체를 넣고 **작성일은 양식에서 뺐다** — 생성 시점을 찍으면 `FR-1001-AC5`(비트 단위 동일 재실행)가 깨진다. 「언제의 수인가」는 **대장 판**(입력의 시점)과 **매니페스트 해시**(입력의 동일성)가 답한다 |
> | **2** | 양식은 5.1 에 **출처** 열을 요구 · 실물은 붙임 2 로 내림 | **양식.** 본문 표가 일곱 열을 넘으면 화면 밖으로 나가 임계값이 안 읽힌다. **신뢰도는 본문에 남긴다** — 임계값을 읽는 즉시 확정인지 가정인지 알아야 한다 |
> | **3** | 양식 붙임 2 는 **임계값·여유**를 요구 · 실물은 본문 5.1 에만 | **양식.** 지금 양식대로면 같은 수를 두 곳에 적게 되고 **한쪽만 고쳐진다** |
> | **4** | 양식은 붙임 3 이 산식의 자리 · 실물은 **편익 산식이 붙임 4**(제원 상세) | **양식.** 편익 산식의 대입값이 전부 자원 제원이라 붙임 3 에 두면 같은 제원을 두 붙임에 나누어 적게 된다. 결정이 테스트 주석에만 있었다 |
>
> ### ★★ 왜 넷이 남아 있었나 — **양식이 스스로를 강제하지 못했다**
>
> 양식 머리말은 *「양식을 고치면 `narrative.py` 의 절 순서가 함께 움직인다」* 고
> **선언만** 하고 있었다. 개별 절을 보는 검사는 있었지만 **둘을 통째로 대조하는
> 기계가 없었고**, 그래서 붙임을 셋 늘리는 동안 양식이 조용히 낡았다. 이
> 저장소가 반복해서 잡아 온 **「선언은 있는데 읽는 쪽이 없다」와 같은 형태**이며,
> 이번에는 그 대상이 코드가 아니라 **서식 규정**이었다.
>
> **`tests/report/test_form_conformance.py`** 가 양식을 **파싱해서** 본문 절
> (`### 【N】`) · 소절(`- **N.M …**`) · 붙임 표 · 표제 표를 뽑고 렌더러 출력과
> **양방향으로** 맞춘다(한쪽에만 있으면 빨간불). 이름은 **앞부분 일치**로 본다 —
> 실물이 덧붙이는 「— 무엇에 답하는 절인가」까지 양식에 베끼면 문장 하나 다듬을
> 때마다 두 파일이 함께 움직여야 해서 **결국 검사를 통과시키려고 양식을 고치게**
> 된다.
>
> **변이 6건 전건 빨간불**이며 **검사 넷에 나뉘어** 잡혔다(① 양식만 붙임 9 이름
> 변경 ② 양식이 소절 6.3 을 규정에서 제외 ③ 렌더러가 작성 주체를 되돌림
> ④ 렌더러만 5.1 이름 변경 ⑤ 양식만 6절 이름 변경 ⑥ 양식이 작성일을 다시 요구).
> ⚠ ⑥ 은 **처음에 초록불이었다** — `printf '%s'` 가 `\n` 을 리터럴로 넣어 표
> 한 행이 되었기 때문이고, 변이 자체가 성립하지 않은 것이었다. **초록불을 그대로
> 통과로 읽지 말 것**(`printf '%b'` 나 히어독으로 넘긴다).
>
> ### ✔✔ **적정 용량 검토를 신설했다 — 4.4 · 붙임 10** (2026-08-15)
>
> 사용자 지적: *「ESS, heatpump, p2h 등은 **적정 용량 검토가 선행**되어야 함」*.
> 확인해 보니 리포트는 그 물음을 **묻지도 답하지도 못하는 상태**였다 — 용량이
> `e2e_runner` 의 **모듈 상수**여서 어느 케이스 축에도 없었다. 27 케이스를 다
> 돌려도 3kW·10kWh 한 값이고 민감도 표에도 오르지 않는다.
>
> | 무엇 | 어디에 |
> |---|---|
> | **설계 변수** 라는 셋째 갈래를 세웠다 — 대장 변수(틀릴 수 있다)·모형 파라미터(고른다)와 다르다. **사업자가 고르는 값**이다 | `ledger_levels.py::_DESIGN_VARS` · `design_variables()` · `design_levels()` |
> | 러너가 용량을 `level_map` 에서 읽는다. **기본 상수를 남기지 않았다** — 남기면 수준표를 고쳐도 옛 용량이 쓰이고 NPV 만 조용히 달라진다 | `e2e_runner.py` |
> | **형태를 재고 답을 고르지 않는다** — 단조 증가/감소/내부 최대. 단조면 「적정값 미정」이며 그것은 **용량을 제한하는 것이 모델에 없다**는 측정 결과다 | `core/report/capacity.py` |
> | 제약에 걸린 점을 **빼지 않는다.** 빼면 표가 매끈해지고 제약이 사라진 것처럼 보이는데, **제약이야말로 적정 용량을 정한다** | 같음 |
> | 설계 변수는 **5.1 에서 뺐다** — 확보 대상(자료)과 설계 결정은 받는 사람이 다르다. 할인율을 5.2 로 가른 것과 같은 판단 | `case_report.py::_influences` |
>
> ### ★★★ 실측이 말한 것 — **PV 는 줄이고 ESS 는 키우라고 나온다**
>
> | 설계 변수 | 한계 기여 | 형태 | 구간 내 최선 |
> |---|---|---|---|
> | `pv_capacity_kw` | **-61,681원/kW** | 단조 감소 | 1kW (구간 하한) |
> | `ess_capacity_kwh` | **+190,445원/kWh** | 단조 증가 | **23kWh → NPV +1,936,149원 (회수)** |
>
> ⚠⚠ **이 답을 그대로 믿으면 안 되며, 왜 그런지가 이번 라운드의 핵심이다.**
> ESS 를 키울수록 좋아지는 이유는 **심야에 계통에서 받은 전력이 공짜이기
> 때문**이다(구매 비용 행 부재 — 붙임 8). 즉 ESS 가 *「공짜로 받아 120원에 파는
> 기계」* 가 되어 있다. 그래서 **순서는 「적정 용량」이 아니라 「구매 비용 배선」
> 이 먼저**다. 리포트는 이 사실을 스스로 싣는다 — 4.4 가 형태를, 붙임 8 이
> 구매 비용 미계상을, 붙임 10 이 점별 값과 걸린 제약을 낸다.
>
> ⚠ PV 가 단조 감소인 이유도 함께 봐야 한다 — 잉여판매 단가 120원/kWh 로는
> 160만원/kW 를 회수하지 못하며, **평탄 발전 프로파일** 때문에 심야 발전이
> 충전에 흡수돼 팔리지도 않는다(붙임 8 신설 항목).
>
> ### ✔ 붙임 8 에 **일중 발전 프로파일 (평탄)** 신설
>
> 붙임 7 을 눈으로 읽다 **00~01시 태양광 0.45kWh**(야간 발전)를 발견했다. 러너가
> PV 에 이용률 하나만 주어 하루 발전량을 **24스텝에 균등 배분**한 결과다. 수량을
> 재어 싣는다 — **수전 스텝에 실린 발전량 2.70kWh/일 (연간화 985kWh)**. 방향은
> `방향 미측정` 이다: 발전을 주간으로 옮기면 송전(편익)과 수전(비용)이 **함께**
> 늘고, 구매 비용이 미계상이라 순 방향은 그 항목의 배선 여부에 달렸다.
> **「PV 가 평탄하다」를 문장으로 박지 않았다** — *「스텝별 출력이 전부 같은 발전
> 자원이 있는가」* 를 매 실행 재며, 일사 곡선이 배선되면 행이 스스로 사라진다.
>
> ### 본문 분량 — 양식 상한에 닿아 붙임으로 내렸다
>
> 4.4 가 들어오며 본문이 178줄이 됐다(양식 130~170). 양식이 정한 대로 **붙임으로
> 내렸다** — 4.3 의 「편익 갈래」 표를 **붙임 4**(제원·산식과 같은 자리)로,
> 「한계 기여」 정의를 **붙임 9**(용어)로. 지금 **170줄**이다.
>
> **변이 8건 전건 빨간불**(양식 정합 6 · 용량 6 중 겹침 포함)이며 검사 다섯에
> 나뉘어 잡혔다. ⚠ 그중 하나가 **검사 자체의 구멍을 드러냈다** — 붙임 번호
> 정규식이 `\d` 한 자리라 **붙임 10 이 파싱되지 않아** 「붙임 10 을 지운다」가
> 초록불이었다. `\d+` 로 고쳤다. **변이를 심지 않았으면 새 검사가 새 붙임을
> 붙들지 못하는 채로 남았다.**
>
> ### ✔ **부하·일사 곡선을 가정해 반영했다 — 붙임 7 둘째 표** (2026-08-15)
>
> 사용자 판정: *「운전·붙임까지 (프로포마 불변)」*. 그 범위로 닫았다.
>
> | 무엇 | 어디에 |
> |---|---|
> | **형상 자산** — 대표일 24스텝 정규화 가중치(부하·일사) + 출처·신뢰도·`replace_when`. **대장이 아니다** — 값이 아니라 배분이라 대장 항목의 꼴(`value`+3수준)에 맞지 않는다 | `fixtures/profiles/representative-day.yaml` |
> | 로더 — 정규화·검증. **없으면 메우지 않고 비운다** (기본 형상으로 메우면 「자산이 비었다」와 「이 형상을 골랐다」가 구별되지 않는다) | `core/casegrid/profiles.py` |
> | 러너가 형상을 받는다 — PV 는 이용률 대신 시계열, `Load` 자원 추가. **리포트가 자원을 다시 세우지 않는다** — 같은 진입점을 한 번 더 부른다 | `e2e_runner.py::_household_load` |
> | 붙임 7 이 **두 표**를 나란히 + 차이 표 | `dispatch_sections.py` |
> | 붙임 8 의 **자가소비·구매비용 두 행이 「미정량」에서 정량으로** | `unreflected.py::_assumed_quantities` |
>
> ### ★★★ 실측 — **수전이 2.15배로 늘어난다**
>
> | 대표일 합계 | 파이프라인 | 형상 가정 | 차이 |
> |---|---|---|---|
> | 계통 송전 | 16.10 kWh | 13.36 kWh | **-2.74** |
> | 계통 수전 | 6.19 kWh | 13.31 kWh | **+7.12** |
> | 계통 수전 (연간화) | 2,259 kWh | **4,859 kWh** | **+2,600** |
> | 자가소비 (min(발전,부하)) | — | 5.44 kWh/일 (연 **1,985kWh**) | — |
>
> **단가 150원/kWh 를 가정하면 미계상 구매비용이 연 약 73만원**이며, 이는 연
> 편익 총액(904,860원)에 육박한다. 자가소비 절감(연 1,985kWh)이 그 반대편에
> 있지만 **소매 단가가 없어 계상할 수 없다.**
>
> ### ★★ 왜 프로포마를 건드리지 않았나 — **한쪽만 반영하면 한 방향으로 틀린다**
>
> 부하를 편익 계산에 태우면 `grid_export` 가 줄어 **잉여판매가 자동으로
> 감소**한다. 그런데 그 대가인 자가소비 절감은 ⓐ 배타 규칙 유형 A
> (`SelfConsumption` ↔ `SurplusSale`)가 동시 계상을 막고 ⓑ 소매 단가를 줄 요금
> 엔진이 없어 **계상할 수 없다.** 그대로 넣으면 편익만 빼는 것이 되어 사업에
> 불리하게 틀린다 — **NSPM 대칭성**이며 양식 4절에 금지 조항으로 적었다.
> `test_assumed_operation.py::test_the_assumed_run_does_not_move_the_conclusion`
> 이 *「결론이 가정 운전 위에 서 있지 않은가」* 를 붙든다.
>
> **변이 4건 전건 빨간불**(형상이 총량을 바꿈 · 가정 운전 미실행 · 결론을 가정
> 운전 위에 세움 · 자산 부재 시 메움). ⚠ 그중 하나가 처음에 **rc=2**(대상 문면
> 없음)로 나왔다 — 통과로 읽지 않고 문면을 다시 떠서 심었다.
>
> ### 실측 — 게이트 (2026-08-15)
>
> `pytest` **수집 1,526건 rc=0**(양식 정합 검사 5건 추가) · `ruff` 0건 ·
> `mypy` **112파일** 무결 ·
> `lint-imports` 4 kept 0 broken · `check_hardcoded_params` rc=0(**`RULE_TEXT` 를
> `MappingProxyType` 으로 고친 뒤** — `NFR-205` 가 잡았다) ·
> `check_task_mapping` rc=0 · `gen_traceability` 요구사항 105 / 수용기준 321
> **변동 없음**(분류가 바뀐 조항이 없고 검사 건수만 늘었다) ·
> **CI 버전(3.11)에서도 rc=0** (`~/miniconda3/envs/r31py311`).
>
> ⚠ **3.11 을 반드시 돌린 이유**: `CaseOutcome` 에 필드 셋(`resources`·
> `dispatch`·`rule_order`)을 더했다. 이 자료형은 R31 이 **3.12 에서는 보이지
> 않고 3.11 에서만 수집 오류가 나는** 형태를 실제로 겪은 자리다(`mappingproxy`
> 기본값). 이번에는 기본값을 두지 않아 걸리지 않았다.
>
> ⚠ **음성 스위트 10종은 돌리지 않았다** — 실물 소스를 변이시키므로 커밋 직전에
> 돌리는 것이 규칙이다. 커밋 전에 돌릴 것.
>
> ---
>
> | # | 할 일 | 판정과 근거 |
> |---|---|---|
> | ~~**1**~~ | ✔ **결합 시나리오 표를 5.1 에 — 2026-08-15 완료** | 아래 「1차 의견 ① 이 놓은 것」 절. **판정은 그대로 유효했고 실측이 표에 그대로 실렸다** |
> | ~~**2**~~ | ✔ **엔진 규칙 → 붙임 6 — 2026-08-15 완료** | 위 표 |
> | ~~**3**~~ | ✔ **시간대별 디스패치 → 붙임 7 — 2026-08-15 완료** | 위 표. **여기서 「계통 전력 구매 비용 미계상」이 드러났다** |
> | ~~**4**~~ | ✔ **평가 대상 요약 → 2.1 머리 — 2026-08-15 완료** | 위 표 |
> | ~~**5**~~ | ✔ **전제 주제별 묶음 → 붙임 1 — 2026-08-15 완료** | 위 표 |
> | ~~**6**~~ | ✔ **미반영 항목 절충안 → 3.4 + 붙임 8 — 2026-08-15 완료** | 위 표. **사용자 확인 대기 해소** — 「마저 완료해줘」로 진행 승인 |
> | **1** *(원문)* | **결합 시나리오 표를 5.1 에 넣는다** | ★★ **여섯 중 가장 중요. 서식이 아니라 방법론 결함이다.** 의견 원문: *「ESS, PV 단가가 같이 움직여도 동일한 결과가 나오는가?」* → **아니다.** 실측(as_planned NPV): 둘 다 low **+1,094,119(회수)** · PV만 low +94,119 · ESS만 low +134,119 · 기준 −865,881 · 둘 다 high −2,825,881. **효과가 정확히 더해진다**(둘 다 `t=0` CAPEX 로만 들어가 서로 곱해지지 않는다 — 요금 인상률이 배선되면 깨진다). 지금 리포트는 「각각 17~18% 하락 필요」로 보이지만 **함께 20% 낮아지면 여유 있게 회수**된다 → **사업에 불리하게 읽히고 있다.** ⚠ **저장소가 이미 결합으로 판정해 두었다** — `quick_preset_grid()` 의 `equipment_cost_bundle` 이 PV·ESS·히트펌프·시공비를 한 축으로 묶는다. 27케이스는 결합으로 보는데 **리포트 민감도만 독립**이다. 1변수 스윕은 「단독 기여」임을 명시할 것 |
> | **2** | **엔진 규칙을 붙임에 낸다** | 의견: *「규칙 기반 엔진이 적용되었다는데 규칙이 붙임에 기재되지 않으면 내용을 이해할 수 없음」* — 옳다. 재료 있음: `core/engine/rule_based.py::rule_for()` · `_ordered_resources()` 로 자원별 규칙과 실행 순서를 열거할 수 있다 |
> | **3** | **시간대별 디스패치 표를 붙임에 낸다** | 디스패치가 24스텝을 갖고 있고 `core/report/dispatch_notes.py::build_dispatch_notes()` 와 `charts/dispatch_stack.py` 가 이미 있다. **⚠ 둘 다 배포 호출자 0곳** — 이번 라운드에서 여섯 번째 「부품은 있는데 읽는 쪽이 없다」 |
> | **4** | **평가 대상 300자 요약**을 2.1 머리에 | 지금 2.1 이 **표부터 시작**해 「무엇을 평가하는가」가 문장으로 없다 |
> | **5** | **전제를 주제별로 묶는다** | 지금 붙임 1 은 **신뢰도별**이고 의견은 **주제별**(설비단가/요금/제도/분석조건)을 요구한다. **둘 다 필요** — 주제로 묶고 신뢰도를 열로 |
> | **6** | **미반영 사항 — 절충안** | 의견은 「붙임으로 별도 기재」인데 **일부 반대했고 사용자에게 절충안을 제시한 상태**다. 이유: ⓐ NSPM 이 「정량화 못한 영향을 결과와 함께」 요구 ⓑ 교체비(불리)와 요금 인상률(유리)이 **반대 방향**이라 붙임으로 내리면 −865,881원을 확정 수치로 읽는다. **절충안**: 요약·3.4 에 항목명과 **방향만** 한 줄씩, 크기·근거·해소 조건은 **붙임 7** 전문. ⚠ 진행 전 사용자 확인 |
> | — | ~~구매비용·방전편익 세부 항목~~ | ❌ **서식이 아니라 모델에 없다.** 러너에 **가구 부하가 없어** 계통 전력 구매 비용 항목 자체가 없고 PV 자가소비율은 0%(전량 판매)다. 편익은 잉여판매·첨두절감 둘뿐. `Q-3`(가구 부하 시계열) 미확보이고 `16.3` 개인정보 절차가 선행한다. **표를 잘 그려 해결되지 않는다** — 리포트가 「계산되지 않았다」를 명시하는 것이 정직한 처리. ⚠ 의견의 예시(*자가소비 전량 · SMP 0 시 충전 후 저녁 피크 방전*)는 **우리 운전 전략과 다르다**(전량 판매 + 피크 저감) — 그것은 위 ①「전제·구성」 축이다 |
>
> **작업 뒤에는 반드시** `python -m app.run.report_cli --out "docs/evidence/MC-1-검토용-리포트-2026-08-15.md"` 로 실물을 다시 뽑고 **눈으로 읽는다** — 이번 라운드의 결함 넷(절대 경로 · 지수 표기 · 값/단위 어긋남 · 과잉 정밀도)이 **전부 실물을 뽑아 보고서야** 드러났다.
>
> ### ✔ 「1차 의견」 ① 이 놓은 것 — **결합 시나리오** (2026-08-15)
>
> | 무엇 | 어디에 |
> |---|---|
> | **결합 선언을 밖으로** — 리포트가 「무엇이 함께 움직이는가」를 읽어 갈 통로. **두 프리셋의 합집합**이다(전체 탐색에만 있는 `trade_price_bundle` 이 조용히 빠지지 않게) | `core/casegrid/grid.py::coupled_variable_sets()` |
> | **결합 스윕** — 묶음마다 「단독 / 동반」을 나란히 재고 **상호작용 잔차를 함께** 나른다 | `core/report/combined.py` |
> | **다변수 탐침** — `_Sweeper.conclusion_at_many()`. 1변수 스윕이 그것의 특수한 경우가 되게 했다(갈라 두면 결합 쪽만 변형을 읽지 않는 어긋남이 생긴다) | `core/report/case_report.py` |
> | **5.1 에 표 · 1절 요약에 한 줄** + 단독 기여 명시 부기 | `core/report/narrative.py` |
> | **양식 개정** — 5.1 에 「단독 기여 명시 + 결합 시나리오 동반」을 규정으로 적었다. 절 순서가 조항이므로 렌더러와 함께 움직인다 | `docs/report-form-심의보고서.md` |
>
> **실측이 판정과 정확히 같았다**(as_planned NPV): 기준 −865,881 · PV만 low
> +94,119 · ESS만 low +134,119 · **동반 low +1,094,119(회수)** · 동반 high
> −2,825,881. **상호작용 잔차 0원.**
>
> ### ★ ① 에서 배운 것 — **「더해진다」를 코드가 단정하면 배선된 날 조용히 틀린다**
>
> 잔차가 0 이므로 **결합 행을 단독 합으로 채워 넣는 구현도 오늘의 실물에서
> 옳게 보인다.** 그래서 「더해진다」를 상수로 두지 않고 **재어** 적는다 —
> 검사는 *인자가 서로 곱해지는 탐침*을 주입해 잔차가 실제 값으로 나오는지
> 본다(`build_coupled_sweeps(bundles=...)` 가 그 주입구다). 지금 대장 값으로는
> 그런 조합을 만들 수 없으므로, 이 검사가 없으면 그 결함은 **요금 인상률이
> 배선되는 날에야** 틀린 수로 나타난다.
>
> **변이 8건을 심었고 전건 빨간불**이며 **서로 다른 검사에** 잡혔다
> (① 결합 행을 단독 합으로 ② `additive` 를 상수 True 로 ③ 묶음을 리포트가 직접
> ④ 결합 선언이 quick 만 읽음 ⑤ 동반 행이 한 인자만 옮김 ⑥ 단독 기여 부기 삭제
> ⑦ 요약에서 결합 줄 삭제 ⑧ 결합 표를 5.1 밖으로).
>
> ⚠ **다음 사람이 알아야 할 것**: 「1차 의견」 2~6 은 **아직 그대로**다.
> 6번은 여전히 **사용자 확인 대기**다.
>
> # ▶ R33 에 할 일 — 번호 순서대로
>
> | # | 할 일 | 왜 이 순서인가 · 무엇을 조심할 것인가 |
> |---|---|---|
> | ~~**1**~~ | ✔ **「읽는 배포 코드가 있는가」 검사 — 2026-08-17 완료** | `scripts/check_unread_extension_points.py` · CI 차단. **선언 125건 중 37건이 읽는 배포 코드 0곳**이며 부채 목록으로 얼어붙였다 — 늘어도 줄어도 빨간불이다. 목록에 `exclusions`·`value_streams`·`notes`·`unmet_*`·`year_fraction` 이 있다: **R32 가 「해소했다」고 적은 `exclusions` 도 실제로는 테스트만 읽고 있었다.** `ast` 로 속성 접근만 세므로 주석·독스트링은 후보가 아니고, `assert` 안에서만 읽히는 것은 따로 표시한다(`python -O` 에서 사라진다 · R24). 음성 스위트가 **양방향**을 고정한다 — 변이 2건 빨간불 · 오판 방지 1건 초록불 |
> | ~~**2**~~ | ✔ **`FR-401-AC2.AggregatedPPA` 를 실행 경로에 태운다 — 2026-08-17 R34 완료** (판정은 「사용자 입력이 아니라 모형이 계산한다」였고, 그 통로를 열자 **365배 결함**이 드러났다 — 위 R34 블록. ⚠ **이 줄은 R34 가 취소선을 긋지 않아 R35 까지 「할 일」로 남아 있었다**) | 조립기는 섰으나 **`run_single_case_e2e` 는 `annual_generation_kwh` 를 받을 통로가 없다**(구조를 주면 조립기까지 가지만 발전량은 `SettlementInputs` 로만 온다). 즉 **케이스 그리드에서 집합 PPA 케이스를 돌릴 수 없다** — R32 가 조립 층까지만 닫았고 그 사실을 여기 적는다. 파이프라인이 디스패치 전량을 알므로 그것을 넘길지, 사용자 입력으로 둘지가 판단이다(전량은 `DispatchResult` 에서 복원되지 않는다 — `aggregated_ppa.py` 독스트링) |
> | ~~**3**~~ | ✔ **`MC-1` 이 볼 「리포트 한 장」 — 2026-08-15 완료** | 아래 「R33 ③ 이 놓은 것」 절 |
> | **4** | **「VPP 경유」** | `FR-401-AC2.VPPMarket`(Phase 2)이 선행이다. **자료가 아니라 순서 문제**이며 `AC1.VPP` 가 `[Phase 2]` 를 들고 있다 |
> | **5** | **닫기 라운드** — 마커 전수 재대조 · **기계 몫은 끝났고 사람 몫이 남았다 (2026-08-17)** | 실측: 마커-테스트 쌍 **1,241건 · spec 에 없는 ID 0건** · spec 파싱 결함 0 · `gen_traceability` 매달린 참조 0 · `check_task_mapping` rc=0. 계약구조 키 일곱도 전부 spec 선언(`AC1.HouseholdDirect` 꼴)과 이어진다. **남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나**이고 그것은 기계가 잡지 못한다 — 시도한 휴리스틱 둘이 **전부 잡음**이었다: ⓐ 조항 문면과 테스트 이름의 어휘 겹침(조항은 한국어·테스트는 영어라 1,241 중 573이 겹침 0) ⓑ 요구사항별 테스트 구획 분포(`NFR-303`·`FR-402` 처럼 **원래 횡단하는 조항**이 상위를 차지). ★ **읽어서 대조하는 수밖에 없으며 그 자리는 R32 가 옮긴 마커 49곳**이다. 정규식은 `(?<!N)FR-` (R21 이 `NFR-402-AC1` 오염으로 배웠다) |
> | 6 | `LoadedSeeds` 를 제품 경로로 | **지금 하지 말 것** — 소비자 0곳. 소비자가 생길 때 함께 옮긴다 |
>
> ### ✔ R33 ③ 이 놓은 것 — **리포트 산출 경로** (2026-08-15)
>
> | 무엇 | 어디에 |
> |---|---|
> | **조립기** — 골든 시나리오 1건 → 실행 → 영향도(1변수 스윕) · 3중 표기 산식 · 부기 7종 · 가정 부록 · 매니페스트를 한 자료형으로 | `core/report/case_report.py` |
> | **렌더러** — 한국어 마크다운 한 장. **절의 순서가 조항이다**(`FR-1002-AC1`·`UI-7`) | `core/report/narrative.py` |
> | **출구 둘** — `GET /reports/golden/{name}`(`?fmt=json`) · `python -m app.run.report_cli --out <경로>` | `app/routers/reports.py` · `app/run/report_cli.py` |
> | **대장→수준표를 배포 코드로** — 종전에는 `tests/acceptance2/test_17_2_dod2.py` 안에만 있었다 | `core/casegrid/ledger_levels.py` |
> | **`CaseBasis` 신설** — 산식의 **대입값**이 경계를 넘는다. 지표만으로는 「왜 이 값인가」에 답할 수 없다 | `core/casegrid/models.py` · `e2e_runner.py` |
> | **심의보고서 양식** — 우수사례 셋(NSPM for DERs · KDI 예타 표준지침 · 분산특구 가이드라인)을 대조해 뽑았다. **절의 순서 자체가 규정**이며 생성기와 함께 움직인다 | `docs/report-form-심의보고서.md` |
> | **본문/붙임 렌더러** — 본문 6절(실측 218줄) + 붙임 6종. `NFR-206` 이 522줄로 잡아 **본문/붙임 경계**로 갈랐다 | `core/report/{narrative,appendix_sections,method_sections,_format}.py` |
> | **실물 산출물** — `MC-1` 검토자에게 건네는 파일. 손으로 고치지 말고 명령으로 다시 뽑는다 | `docs/evidence/MC-1-검토용-리포트-2026-08-15.md` |
>
> ### ★★★ R33 이 찾은 것 — **넷 다 「리포트를 실제로 내 보아야만」 드러났다**
>
> | 무엇 | 어떻게 드러났나 |
> |---|---|
> | **임계값 이진탐색이 돌지 않고 있었다** | `_find_flip_threshold` 의 기본 허용오차가 **절대 `0.5`** 라, 구간 폭이 `0.03` 인 할인율에서는 첫 줄에서 `hi-lo < tol` 이 성립해 **탐색이 한 번도 돌지 않고 구간의 중점을 임계값으로 돌려주었다.** 실패를 알리지 않으므로 리포트는 *「할인율 3.75% 에서 뒤집힌다」* 를 자신 있게 싣는다(실제 3.44%). 종전 검사는 구간이 `0~200` 이어서 **우연히** 이 결함을 밟지 않았다 |
> | **`tariff_escalation` 이 파이프라인에 배선돼 있지 않다** | 스윕에서 **영향폭이 정확히 0원**으로 나왔다. `quick_preset_grid` 의 축인데 `run_single_case_e2e` 가 읽지 않는다 — 즉 **27 케이스는 실질 9 케이스가 3번 반복**된 것이고, DoD 2 의 실현가능영역 지도는 `y=tariff_escalation` 축이 **구조적으로 평평하다**. 고치지 않고 **리포트가 드러내게** 했다(`unread_by_pipeline`) — 「영향 없음」으로 읽히면 검토자가 틀린 결론을 배워 간다 |
> | **케이스 지표에는 지원이 반영되지 않는다** | 러너 규약상 `metrics` 는 총사업비 기준이라 **보조율 80% 와 무보조가 같은 수**다. 리포트가 그것을 결론으로 적으면 *「80% 를 지원해도 결과가 같다」* 가 된다. 결론·산식·스윕 **셋 다** 변형(`as_planned`)을 읽게 했다 |
> | **교체비·잔존가치가 프로포마에 없다** | 0절 자원 표를 넣고 나서야 **ESS 수명 17년 · 분석기간 20년**이 나란히 보였고, 비용 행을 세어 보니 고정 O&M 둘뿐이었다. `replacement_schedule()`·`salvage_value()` 는 있고 **부르는 배포 코드가 0곳**이다 — 조항 `FR-104-AC2`·`AC5` 는 단위 테스트로 「자동」이었다. 리포트가 수명과 분석기간을 견주어 스스로 짚게 했다 |
> | **`FR-1001-AC5` 를 자동으로 만들 뻔했다** | 새 테스트에 `req("FR-1001-AC5")` 를 달자 매핑표에서 그 조항이 「수동 + MC-1(미수행)」에서 **「자동」으로 바뀌었다** — Phase 1 인수를 막는 유일한 차단 표시가 사라진 것이다. 마커를 뗐다. spec §16.5 가 막으려는 자기충족과 같은 형태다 |
>
> **변이 9건을 심었고 전건 빨간불**이며 **각 변이가 서로 다른 검사에** 잡혔다.
> 그중 하나(「스윕만 케이스 지표로 되돌린다」)는 **처음에 초록불이었고**, 무보조
> 시나리오에서는 두 값이 같아 드러나지 않는다는 것이 그때 밝혀져 검사를 더했다.
>
> ### 코드로 닫을 수 없는 것 → **`status-human.md`** 가 소유자다
>
> 수동 검증 8건 · 외부 자료 회신 · 배포 판단 · spec 개정 판단은 **그 파일에 순서대로**
> 있다(무엇을 · 누구에게 · 통과 기준 · **끝나면 어디에 기록하는가**). **여기 옮겨 적지
> 않는다.** 요약만: **Phase 1 인수를 막는 것은 `MC-1` 하나**이며 **그 선행(리포트
> 산출 경로)은 위 3번으로 닫혔다** — 이제 사람이 `MC-1` 을 시작할 수 있다. 그리고
> `Q-4`·`Q-5` 는 **가정으로 대체할 수 없어** DoD 4 와 §13.3 대조군이 `skip` 으로
> 남아 있다(정직한 상태다). 판정 근거는 `docs/decisions-2026-08-14-R32.md`.
>
> ### 초기화된 세션이 첫 30분에 할 일
>
> 1. `git status -sb`(비어 있어야 한다) · `gh run list --limit 4`
>    (`tests`·`source-rules` **둘 다** `success`)
> 2. **★ 의존성이 설치된 인터프리터는 `~/miniconda3/python.exe` 다** (2026-08-14 실측).
>    저장소 `.venv` 와 `C:\Python313` 에는 `pytest` 조차 없다 —
>    `PYTHONUTF8=1 ~/miniconda3/python.exe -m pytest` 로 돈다.
>    `lint-imports` 는 `~/miniconda3/Scripts/lint-imports.exe`
> 3. **CI(3.11) 대조용 환경이 이미 있다: `~/miniconda3/envs/r31py311`.**
>    구조를 바꾸는 커밋 전에 `PYTHONUTF8=1 ~/miniconda3/envs/r31py311/python.exe -m pytest -q`
>    (R32 도 돌렸고 rc=0 이었다). 새로 만들지 말 것 — 만드는 데 20분이 든다
> 4. **`docs/decisions-2026-08-14-R32.md` 를 읽는다** — 잠정 판정 일곱과
>    **「무엇이 오면 바뀌나 · 고칠 자리」**가 절마다 있다. **되돌리려면 근거를
>    먼저 반박해야 한다**
> 5. ⚠ **`pytest` 요약 줄을 `tail` 로 찾지 말 것** — 리포터가 `N passed` 를 내지
>    않는다. 세려면
>    `python -m pytest --collect-only -q | awk -F': ' '/^tests\/.*: [0-9]+$/{s+=$NF} END{print s}'`
> 6. ⚠ **음성 스위트가 실물 소스를 변이시킨다** — 돌는 중에 `git status` 를 보면
>    `# NEGCHECK` 가 남은 것처럼 보인다. **끝난 뒤에 다시 볼 것**(중단시키면 실제로
>    남는다). R32 는 10종 전건 rc=0 이며 잔여 변이 0건을 확인했다
>
> ### ⚠ R32 가 실제로 걸린 파급 지점 — **조항을 건드리면 여기가 함께 움직인다**
>
> 조항 하나를 쪼개는 데 **문서 셋과 검사 다섯**이 따라왔다. 다음 사람이 같은 일을
> 할 때 순서를 몰라 게이트를 하나씩 밟지 않도록 좌표를 적는다.
>
> | 무엇을 바꾸면 | 함께 고쳐야 하는 곳 | 안 고치면 |
> |---|---|---|
> | **수용기준을 쪼개거나 신설** | ① `rslt/task-…md` 의 **인용**(`- 수용기준:` 줄) ② 테스트 마커 ③ spec 머리말 판본·수용기준 총수 ④ 작업 목록 머리말 판본 | `check_task_mapping` **차단**(R31 이 올렸다). R32 에 「실재하지 않는 인용 1 + 미인용 Must-have 7」로 실제로 빨간불이었다 |
> | **편익 클래스 신설** | ① `core/valuestream/__init__.py`(import·`__all__`) ② `docs/exclusion-rules.yaml` ③ `tests/acceptance/test_phase1_dod.py` 의 `_create_valuestream_for_tag()` **분기**와 `EXPECTED_RATIONALES` ④ `FR-401-AC2.<클래스명>` 조항 | DoD 6 가 규칙표 전건을 순회하며 인스턴스를 만들므로 **`ValueError: 지원하지 않는 편익 태그`** 로 죽는다 |
> | **배타 규칙 추가** | `tests/contract/test_exclusion_rules_contract.py` 의 **건수 단언**(지금 7) | 규칙이 조용히 사라지는 것을 막으려고 못 박아 둔 자리다 |
> | **조립기 추가** | `tests/valuestream/test_settlement.py` 의 `len(assembled)`(지금 **6**) · `NOT_YET_ASSEMBLED` 에서 제거 | 두 표의 합집합이 일곱이어야 하는 래칫이 잡는다 |
>
> **★ 마커 어휘 — `req()` 에 넣을 수 있는 것은 「수용기준 ID」뿐이다.**
> R32 가 셋을 틀렸고 `gen_traceability` 가 **매달린 참조**로 잡았다:
> `DV-6`(대장 규칙은 AC 가 아니다 — 독스트링에 적고 마커는 이웃 `NFR-303-M1`) ·
> `FR-703-AC1`(**`.npv`/`.bcr`** 로 쪼개져 있다) · `FR-801`(요구사항이지 AC 가 아니다).
>
> **★ 계약구조 키 일곱** (spec v0.16 · 저자 부여 리터럴이며 구조명에서 파생하지 않는다):
> `FR-205-AC1.HouseholdDirect` · `.ManagerEntity` · `.DistrictDirectTrade` ·
> `.NetMetering` · `.SurplusDirectSale` · `.AggregatedPPA` · `.VPP`(Phase 2).
>
> **★ 변이 하네스는 `.orch/mutate.py` 다** — `.orch/` 는 git 추적 밖이고 **R24 에서
> 멈춘 오케스트레이션 이력**이지만 **이 도구는 여전히 유효하다**(R32 가 16건에 썼다):
> ```
> printf '%s' '<옛 문면>' > /tmp/o ; printf '%s' '<새 문면>' > /tmp/n
> PYTHONUTF8=1 ~/miniconda3/python.exe .orch/mutate.py "<라벨>" <대상파일> /tmp/o /tmp/n <pytest 대상...>
> ```
> 문면을 **파일로** 넘긴다(인자로 넘기면 셸이 따옴표를 건드려 「대상 문면이 없다」가
> 나고 그것이 미성립인지 깨짐인지 구분되지 않는다). **못 찾으면 rc=2** 이며 통과로
> 읽지 않는다. `<파일>.mutbak` 가 남았으면 원복이 건너뛰어진 것이다.
> **`.orch/` 가 없어졌으면 하네스를 다시 만들 것** — 인수 조건이 *「보고를 믿지 않고
> 변이를 직접 심는다」* 이고, R32 의 결함 셋은 **전부 변이로만 드러났다.**
>
> **★ 커밋 전에 PATH 를 맞춘다** — 훅의 `ruff` 가 `python -m ruff` 라 PATH 의 첫
> `python` 을 잡는다: `export PATH="$HOME/miniconda3:$HOME/miniconda3/Scripts:$PATH"`
> (**절대 경로를 적지 말 것** — `check_disclosure` 가 SC-3 위반으로 커밋을 막는다.
> R32 가 이 줄에서 실제로 막혔다).
> 훅이 막으면 **`.pre-commit-config.yaml` 을 고치지 말 것**(실행할 수 없을 때 통과시키지
> 않는 것이 이 저장소의 규칙이다). 커밋 메시지는 `git commit -F - <<'EOF'` 로 쓴다.
>
> **★ 커밋 뒤 두 검사는 기준 ref 를 요구한다** — `check_coverage_inputs` ·
> `check_test_accompaniment` 는 `--base HEAD~2`(본체+인계 두 커밋일 때)로 돌린다.
> 커밋 전에는 변경 0건으로 보여 rc=2 이며 그것이 옳다.
>


---


# 진행 상태 — 2026-08-14 (R32 종료)

> # ▶ 새 세션은 **`## 지금 할 일`** 로 바로 갈 것 (이 파일 중간)
>
> 그 절의 머리에 **직전 라운드 종료 선언 + 다음 라운드 번호표 + 첫 30분 절차**가
> 있다. 이 파일은 208KB 이므로 **통째로 읽으면 토큰 한도에 걸린다** —
> `grep -n "^## " status.md` 로 절을 찾아 **`## 지금 할 일` 부터 offset 으로** 읽는다.
>
> ### ⚠ **라운드 인계는 이제 이 파일이다 — `.orch/state.md` 가 아니다**
>
> 아래 원래 머리말과 이 파일 곳곳이 `.orch/state.md` 를 인계 정본으로 가리키는데
> **그것은 R24(2026-08-13)에서 멈춘 이력**이다. **오케스트레이션은 R24 에 끝났고**
> R25 이후는 단독 세션으로 진행된다. `.orch/` 는 `.gitignore` 밖이라 새 클론에는
> 아예 없다 — 그 폴더의 판정 중 유효한 것은 `docs/clause-recheck-2026-08-14.md` 로
> 옮겼고, **유효한 도구는 변이 하네스 `.orch/mutate.py` 하나**다(R32 파급 지점 절).
>
> <sub>이하 R15 시점 머리말 (이력) — 「두 파일의 몫」 구분은 지금 유효하지 않다</sub>

> ### 이 파일의 몫은 **산출물 진척**이다. 라운드 인계는 `.orch/state.md` 다
>
> 두 파일이 서로를 베끼지 않게 몫을 갈라 둔다 — 겹치면 어긋나고, 어긋나면
> 둘 다 못 믿는다.
>
> | | 여기(`status.md`) | `.orch/state.md` |
> |---|---|---|
> | 무엇 | **산출물의 판본·상태(문서 11 + 도구 + CI), 게이트 실측 수치** | 라운드 인계 — 레인·핸들·구획·다음 후보 |
> | 갱신 주기 | 라운드가 산출물을 바꿀 때 | 매 라운드 |
>
> > **「산출물 36건」이라 적혀 있던 것을 지웠다.** R13 의 낡음 경고가 그 수를
> > 썼고 R14 가 그대로 옮겨 적었는데, **저장소에서 근거를 찾지 못했다** —
> > 작업 목록의 「Wave 0 산출물 0.5·0.6·0.8」 표기와도 맞지 않는다. 세지 않은
> > 수를 옮겨 적으면 그것이 다음 라운드의 계획으로 따라 들어간다(R8 대장의
> > 요약줄이 그렇게 8을 넘겼다). **세어서 적거나 적지 않는다.**
>
> **08-10 R14 에 되살렸다.** 08-09 에 멈춰 있었고 R9~R13 다섯 라운드가
> 반영되지 않아 spec 판본을 v0.10(실제 v0.14), 테스트를 591건(실제 991
> passed)으로 적고 있었다. **그 5라운드의 표류가 이 파일 하나로 좁혀졌던
> 이유는 여기가 산출물 진척의 유일한 소유자이기 때문이다** — 그래서
> 「공정률이 얼마인가」를 물으면 매번 traceability 와 spec 을 다시 세야 했다.
>
> 아래 수치는 **전부 이 라운드에 실제로 명령을 돌려 받은 값**이다. 명령까지
> 함께 적어 두었으니 다음 사람은 베끼지 말고 다시 돌릴 것.

> 다음 세션 인계용. **먼저 `## 지금 할 일`부터 보십시오.**

## 08-10 — R9~R15 (오케스트레이션 7라운드) · **산출물에 남은 것만**

라운드별 경위는 `.orch/state.md` 가 정본이다. 여기에는 **산출물이 바뀐 것만**
적는다.

| 라운드 | 산출물에 남은 것 |
|---|---|
| R9 | `tests/der` 156마커 의미 대조(대장). spec §16.3 면제 3건 해소. **감시기에 무출력 판정** |
| R10 | 마커 22건 수정. `FR-105-AC4` 를 실제로 검증되게 만듦 — `core/report/dispatch_notes.py` 신설 |
| R11 | 「조항 부재」 3건 해소. `SOCIAL` 관점 구현. `retire` 설계서 |
| R12 | **`retire` 구현** — 계약 `end_of_life_action` + 자원 6종. `FR-104-AC3` 의 빈 절반을 채움 |
| R13 | **`core/report/pdf.py`·`excel.py` 를 mock 에서 실물로.** `blocking_dod` 를 살아 있는 깃발로. spec 머리말의 판본 이중 소유 제거(v0.14) |
| R14 | `scripts/negtest_source_rules.py` **신설**(25종). `.orch/negtest_dispatch.py` 신설(17종). 감시기 음성 31 → **50종**. 이 파일 되살림. **마커 431건 전수 대조**(무관 36 · 좁음 86 · 판단보류 17 · 반대 0) |
| R16 | **확장점 다섯을 세웠다 (계약 라운드 · §16.2 단독).** ① `charts.py` 10줄 스텁 → **`Chart` 계약 + `core/report/charts/` 레지스트리 + 실렌더 3종**(matplotlib PNG) ② `payer: ClassVar` → **계약구조 종속**(`payer_by_structure`, FR-205 조립기의 전제) ③ 배타 규칙표를 **`docs/exclusion-rules.yaml` 로 이관** + **유형 A 실제 거부**(`assert_no_exclusions`, DV-12) ④ **`ValidationError`(필드/사유/조치)** 신설 — NFR-303 을 구조로 ⑤ `is_baseline` 깃발 → **`CaseVariant` 계약 + `run_order()`** (FR-607-AC1 「자동 포함」을 기계 보증). **`scripts/negtest_extension_points.py` 신설(10종) + CI 차단** |
| R17 | **`T`(테스트 좁음) 56건 전건 처분 — 5레인 병렬.** **분류표가 없는 조항을 인용한 것이 10건**이었다(`FR-402-AC2`·`AC3`·`FR-401-AC2` 맨몸형은 spec 에 없고, 「위험기준(VaR)」은 spec 에 한 글자도 없다) — **그 표의 `I` 32건도 대조 없이 못 쓴다.** 「검사가 아무것도 붙들지 않았다」 둘 더: ① `NFR-208-AC3` 순수성 검사가 `dir()` 이름 접두어를 보아 **`core.*` 침입을 전부 통과**시켰다(`ast` 로 실제 import 문 + 계약 전 파일로 교체) ② 계약 테스트가 `FR-701-AC3` 은 1년차만·`FR-104-AC5` 는 시그니처만 보았다(복리·잔존수명 반영을 고정). **`FR-402-AC2.A` 거부를 배선하자 「조항의 반대를 고정한 테스트」 4건이 드러나** 조항 쪽으로 맞췄다. `FR-1004-AC1` 잔여 차트 3종 실렌더. 작업 목록 하위 건수를 **세어 157 로 통일**(v2.6, 세는 명령을 §작성 규칙에 박음). pytest 1,028 → **1,065**, mypy 87 → **90파일**. **자동 매핑 278 그대로 — `T` 라운드는 조항이 늘지 않고 조항당 검증이 깊어진다** |
| R31 | **단독 수행 — 사용자가 「확장성 기준으로 결정을 대신 내려라」고 위임했다. 결정 아홉을 전건 확정해 `docs/decisions-2026-08-14-R31.md` 에 기록하고, 1번을 구현했다.** ★★★ **「전체 파라미터」의 기준을 세웠다 — 레지스트리에 등록된 자원의 `__init__` 시그니처다.** R29 가 항진 테스트를 걷어내며 *「기준이 코드에 없으므로 어떤 검사도 「전체」를 말할 수 없다」* 를 남긴 자리다. `core/model/parameters.py` 신설 — 6종 **133 파라미터**를 시그니처에서 읽고, 화면(`advanced_mode_fields`)이 그것을 돈다. **손으로 둔 목록을 두지 않은 것이 결정의 요점이다** — `core/der/<tag>.py` 를 놓는 순간 갈리고, 갈린 상태에서 화면은 「전체」라 적은 채 일부만 그린다. ★★ **멈추는 자리를 둘 두었다** — ⓐ `**kwargs` 자원(받는 이름을 열거할 수 없다) ⓑ 단위를 모르는 수치 파라미터(`UI-2-AC1` 이 동시에 조용히 깨진다). **경고로 흘리면 새 자원 하나가 조용히 화면에서 빠지거나 단위 없이 나오고 검사는 전부 초록불이다.** ★★ **단위를 발명하지 않았다** — 이름이 이미 담고 있으므로(`capex_unit_won_per_kw`) 접미어로 읽고, 담지 않은 것만 사유와 함께 선언한다. **접미어를 넓히지 않은 근거가 요점이다**: `cycle_life`(회)와 `calendar_life`(년)는 같은 꼴에 단위가 다르고 `heating_degree_days` 는 `_days` 로 끝나지만 일(日)이 아니다 — **틀린 단위는 없는 단위보다 나쁘다.** 두 표가 겹치는 것을 래칫이 막는다(겹치면 접미어 규칙을 고칠 때 어느 쪽이 이기는지가 조용히 뒤집힌다). ★ **「그릴 수 없는 것」과 「자리가 없는 것」을 갈랐다** — 8760개짜리 시계열을 수치 칸으로 그릴 수는 없지만 자리는 있어야 한다(없으면 그것이 곧 「전체」가 아니다). 데모 45칸 = 수치 36 + 비수치 9. ⚠⚠ **변이 7종 중 하나가 검사 자신의 결함을 드러냈다** — 열거 거부를 지워도 초록불이었고, 원인은 가짜 자원의 인자 이름이 **단위도 풀리지 않아 다른 거부가 대신 답한** 것이었다(단언 `"열거" in message` 가 가짜 `tag` 「열거불가」에 우연히 맞았다). 이름을 `_kw` 로 끝나게 고쳐 격리했다 — **거부 경로가 여럿이면 「어느 거부가 답했는가」를 문면으로 갈라야 한다.** 고쳐서 **변이 7종 전건 잡힘.** ★★★ **`FR-205` 구조 어휘의 사본을 없앴다 — 계약이 미뤄 둔 자리이고, 실제로 어긋나 있었다.** `core/model/settlement.py` 가 `SUPPORTED_STRUCTURES` 로 자기 목록을 들고 **일곱 중 셋이 계약과 달랐다**(「상계」 대 「상계거래」 · 「개별 직접계약」 대 「개별 세대 직접계약」 · 「단일계약+관리주체」 대 「…경유」). **그 어긋남은 조용하다** — spec 문면대로 고르면 엔진이 거부하고, 엔진 문면대로 고르면 `payer_by_structure` 의 어느 키와도 맞지 않아 **편익의 지불 주체가 기본값으로 떨어지고 결과는 그럴듯하다.** 계약 독스트링이 *「오타는 영영 매치되지 않는다」* 고 경고한 그 상태이며 **`__init_subclass__` 의 기동 시점 대조는 편익 클래스의 표만 보고 정산엔진의 목록은 보지 않는다.** `CONTRACT_STRUCTURES` 를 유일 정본으로 만들고 `ValidationError`(3요소)로 거부하며, **사본이 다시 생기는 것을 래칫으로 막았다**(`core`·`app`·`infra` 의 문자열 상수를 `ast` 로 훑어 **파일까지 지목** — 사본을 심어 실측). ⚠ **낡은 어휘를 그대로 베낀 테스트가 있었다** — `test_settlement.py` 가 엔진의 목록을 복사해 초록불을 내고 있었다(「조항의 반대를 고정한 테스트」). 이제 계약을 읽고 **낡은 셋이 거부되는 것을 따로 단언한다** — 없으면 목록을 다시 넓혀도 아무것도 빨간불이 되지 않는다. ★★ **결정 9는 결정 대상이 아니었다** — 분석기간의 소유자를 status.md 는 후보 둘로 적어 두었으나, `infra/orm/scenario.py:29` 가 *「전제 분류에 해당 값은 `AssumptionSet` 에 넣는다」* 고 적고 `analysis_years` 를 **금지 필드로 열거**한다(§7.1 O-1). 열려 있던 것은 「어느 층인가」가 아니라 **「그 층에 아직 필드가 없다」** 였다. ⚠ **기존 게이트가 옳게 막았다** — 단위표를 `dict` 로 두자 `test_no_module_or_class_level_mutable_containers` 가 잡았다. 규칙을 고치지 않고 `MappingProxyType` 으로 바꿨다. ⚠ **`gen_traceability.py` 의 rc 를 파이프로 삼켰다** — `| tail` 뒤의 `$?` 가 0 이라 「미매핑 없음」으로 읽힐 뻔했고, 파이프를 떼자 rc=1(정상 — 미매핑 8건 잔존)이었다. §「도구」가 경고한 형태를 그대로 밟았다. pytest 1,330 → **1,344** · mypy **101파일** 무결 · `ruff` 0건 · `lint-imports` 4 kept 0 broken · `check_marker_substance`·`check_file_size --code-strict`·`check_disclosure` 전건 rc=0 · **자동 매핑 278 그대로 — 아홉 라운드째다**(기준을 세우는 라운드이지 조항이 늘는 라운드가 아니다) |
| R31-b | **단독 수행 (같은 라운드 후속) — 결정 아홉을 **전부 구현**했다. 순위표대로 여덟 작업을 완주.** ★★★ **`DV-7`·`DV-6` 이 던지기 시작해 대장 강제 11 → 13 이고 `NOT_YET_THROWN` 이 비었다** — R21 이 「대장에 열넷을 적어 두고 셋만 던지고 있었다」고 쓴 자리가 열 라운드 만에 닫혔다(상수는 남겼다 — 열다섯 번째 규칙이 들어올 때 「아직」을 놓을 자리가 없으면 그 규칙이 `THROWN` 에 억지로 들어간다). ★★★ **`FR-205` 정산이 조립기가 됐고 구조 셋이 실제로 돈다** — 배타 규칙표가 이미 「구조가 하나를 고른다」를 말하고 있었다(`SelfConsumption`·`SurplusSale`·`DirectTrade` 가 서로 유형 A). 조립기는 규칙표를 다시 적지 않고 **자기 결과를 `assert_no_exclusions()` 에 통과시킨다.** 미구현 넷은 **빈 목록이 아니라 사유를 들고 거부**한다 — 빈 목록이면 「편익 0 인 사업」이 그럴듯하게 나와 미구현과 구별되지 않는다. ★★ **분석기간의 소유자는 결정 대상이 아니었다** — `infra/orm/scenario.py` 가 `analysis_years` 를 `Scenario` 금지 필드로 열거하며 「전제 분류에 해당 값은 `AssumptionSet` 에 넣는다」고 이미 적고 있었다. 열려 있던 것은 「어느 층인가」가 아니라 **「그 층에 아직 값이 없다」** 였고, 실제 기본값은 `core/casegrid/e2e_runner.py` 의 모듈 상수였다. 지웠고 두 검사가 앞뒤를 본다(시그니처의 기본값 · `ast` 식별자에 그 이름이 없음 — **문자열로 찾으면 「이 상수를 지웠다」고 설명하는 주석 자신이 걸린다**). ⚠⚠ **인계가 물려준 진단 하나가 틀렸다** — 결정 문서가 status.md·R30 을 따라 *「`DV-6` 폴백을 하면 `except KeyError` 8곳이 죽은 분기가 된다」* 고 적었는데, 실물로 보니 그 8곳은 모델·프로파일·시나리오 **조회**가 던지는 것이고 **`app/` 에는 `TariffEngine` 을 쓰는 경로가 아예 없다.** 라우터를 한 줄도 건드리지 않았다 — **인계가 물려준 진단은 쓰기 전에 실물로 확인해야 한다**(R18·R24·R26 이 반복해 만난 형태이고 이번에는 결정 문서 자신이 그 자리였다). ⚠⚠ **같은 형태의 결함을 내가 두 번 만들었다 — 거부 경로가 여럿일 때 문면으로 가르지 않은 것** (`parameters_of` 의 열거 거부 · `_select_effective` 의 빈 표 거부). 둘 다 변이를 걸어도 **다른 거부가 대신 답해** 초록불이었다. 문면으로 격리해 닫았다. ⚠ **내가 놓은 래칫이 조항이 요구하는 자리를 금지했다** — 구조 이름 사본 금지가 조립기의 **선언표**를 잡았다. 규칙을 느슨하게 하지 않고 **정밀하게** 고쳤다: 금지의 근거는 「두 곳에 있다」가 아니라 **「갈려도 아무도 모른다」**이므로, 갈리는 것을 기계가 보는 자리만 허용하고 **어느 검사가 지키는지를 표에 함께 적게** 했다. ⚠ **커밋 메시지에 「하위 158건」을 세지 않고 적었다** — 뒤이어 명령을 돌리니 실제로 158 이었으나 **맞은 것과 센 것은 다르다**(규칙이 요구하는 것은 값이 아니라 절차다). ✔ **`check_task_mapping` 이 처음 rc=0 이 되어 CI 차단으로 승격했다** — `FR-202` 소유를 확정해 미인용 Must-have 3 → 0건. 변이 **7+8+6+5+6+9 = 41종 전건 잡힘.** pytest 1,330 → **1,417** · 커버리지 94% · mypy 100 → **102파일** 무결 · spec v0.14 → **v0.15**(수용기준 308 → 314) · 작업 목록 v2.6 → **v2.7**(157 → 158) · **자동 매핑 278 → 284** |
| R30 | **단독 수행 — `pre-commit` 훅을 실제로 걸고, `DV-5` 를 실행 경로에 배선했다.** ★ **훅이 R26 이 찾은 상태 그대로 네 라운드째 걸려 있지 않았다** — `check_precommit_installed.py` 가 그것을 계속 말하고 있었고 아무도 설치하지 않았다. `pre-commit install` + `install-hooks`(환경 선다운로드 — 첫 커밋이 멈추지 않게). **검사기가 옳은 말을 해도 아무도 실행하지 않으면 검사기가 없는 것과 같다.** ⚠⚠ **걸자마자 첫 커밋이 막혔고, 그것이 옳았다** — `ruff` 훅의 `entry` 가 `python -m ruff check`(`language: system`)라 **PATH 의 첫 `python`** 을 잡는데 그것이 `C:\Python313` 이고 거기엔 ruff 가 없다. **설정을 고치지 않았다** — 훅은 실행할 수 없자 통과시키지 않고 **막았다**. 그것이 이 저장소의 *「명령이 없는 것을 검사가 통과한 것으로 읽지 않는다」* 그대로다. **고칠 것은 훅이 아니라 커밋하는 셸의 PATH 다** — 의존성이 설치된 인터프리터의 디렉터리와 그 `Scripts` 를 PATH 앞에 둔다(위치는 `where lint-imports` 로 찾는다. **여기에 절대 경로를 적지 않는다** — 아래 ★ 참조). 게이트를 돌 때 인터프리터를 고르는 것과 **같은 규칙이고, 훅은 인자로 인터프리터를 고를 수 없으므로 셸에서 정해 줘야 한다.** ★★★ **그리고 그 문장을 처음 적을 때 내가 절대 경로를 그대로 썼고, 훅이 그 커밋을 막았다** — `check-disclosure` 가 `SC-3 사용자 이름이 드러난다`로 status.md 를 잡았다. **R26 이 「훅이 없어서 로컬 절대 경로가 담긴 커밋이 아무 저항 없이 만들어졌다」고 적은 바로 그 형태이고, 같은 라운드 안에서 이번에는 막혔다.** 훅을 건 값이 걸자마자 나왔다. ★★★ **`DV-5` — 던지는 코드는 R24 가 놓았고 여섯 라운드 동안 아무도 부르지 않았다.** `core/casegrid/e2e_runner.py::run_single_case_e2e` 가 **자원을 세운 직후**(디스패치·편익·CBA 어느 것도 돌기 전에) `check_analysis_period()` 를 부른다. 수명은 자원이 갖고 있으므로 거기가 규칙을 평가할 수 있는 가장 이른 자리다. **R27 이 `assert_no_exclusions` 로 한 것과 같은 자리·같은 형태이며 둘째다.** ★★ **배선을 검증 가능하게 만드는 것이 다시 설계의 절반이었다** — `HORIZON_YEARS` 가 모듈 상수라 **상한을 넘는 케이스를 진입점으로 넣어 볼 방법이 없었다**(R27 이 `extra_value_streams` 를 둔 것과 같은 이유). `horizon_years` 인자를 두되 기본값을 종전 상수로 둬 호출측 동작은 그대로다. ★★★ **그 인자를 검사에만 넘기면 「재는 것」과 「쓰는 것」이 갈린다** — 프로포마 행의 연도 범위도 같은 인자를 쓰게 하고, **분석기간을 절반으로 줄이면 NPV 가 달라지는 것**을 따로 단언했다. 이 단언이 없으면 나머지 검사 넷은 전부 초록불인 채 **검사가 아무도 쓰지 않는 수를 지키게 된다** — 값은 맞고 뜻이 틀린 형태이고 아무 예외도 나지 않는다. ⚠⚠ **변이 하네스가 또 한 번 「결과」로 보이는 고장을 냈다 — R25 와 같은 형태다.** `str.replace(old, new, 1)` 이 첫 occurrence 만 바꿔 `end_year=horizon_years` **2곳 중 1곳이 그대로 남았고**, 그러면 NPV 가 여전히 달라져 **「검사가 놓쳤다」로 보인다.** 검사는 멀쩡했고 **변이가 성립하지 않았다.** 전건 치환으로 고치자 잡혔다 — **하네스의 빨간불도 초록불과 똑같이 근거를 봐야 한다.** **변이 4종 전건 잡힘이고 넷 다 의도한 검사에만 잡혔다**(배선 제거 → 거부 2건 · 거부를 CBA 뒤로 → 「CBA 앞」 1건 · 계산을 상수로 되돌림 → 「인자가 계산에도 쓰인다」 1건 · 경계 `≤`→`<` → 경계 1건). ⚠ **닫힌 것은 「케이스 실행이 상한을 지난다」이지 「분석기간의 소유자」가 아니다** — 사용자가 고르는 분석기간이 어느 층에 사는가는 여전히 결정 대기이며(`Scenario` 는 `DV-11` 이 금지) 그 층이 정해지면 거기서도 같은 함수를 지나야 한다. `tests/contract/test_dv_rule_enforcement.py` 의 「부르는 배포 코드가 0곳」 주석을 사실과 맞췄다. pytest 1,325 → **1,330** · mypy **100파일** 무결 · `ruff` 0건 · `lint-imports` 4 kept 0 broken · `check_marker_substance` 0건 · **자동 매핑 278 그대로 — 여덟 라운드째다**(마커가 기존 `NFR-303-M1` 이므로 조항이 늘지 않고 조항당 검증이 깊어진다) |
| R29 | **단독 수행 — 「검사가 자기 자신을 검사하던」 자리 셋을 전부 없앴다.** R26 재검증표가 항진(자기참조) 셋을 적었고 이 라운드가 마지막 둘을 닫았다. ★★★ **`test_advanced_mode_shows_all_parameters` 는 구조적으로 실패할 수 없었다** — 기대값을 `demo_context()` 에서 다시 불러와 **템플릿이 그 사전을 그대로 렌더한 결과**와 대조했다. 렌더러가 무엇을 하든 두 집합은 같다. 그런데 매핑표는 이 테스트로 `UI-1-AC1` 을 검증된 것으로 셌다. 이제 테스트가 **자기 입력을 지어 주입**하고 화면에 나오는지 본다 — `inputs[:5]` 로 자르는 변이가 잡힌다(전에는 **어떤 변이도** 이 테스트를 깨지 못했다). ⚠ **「전체 파라미터」는 여전히 검증되지 않는다** — `ModelConfig` 에 파라미터 목록이 없고 `DERConfig.params` 는 `dict[str, Any]` 다. **「전체」의 기준이 코드에 없으므로 어떤 검사도 그것을 말할 수 없다.** 이름을 하는 일에 맞춰 좁혔다. ★★★ **「감지 능력 확인」이 감지기를 부르지 않았다** — `test_dod7_violation_detection_works_ci_missing_pytest` 가 한 일은 `assert "pytest" not in modified_content`, **자기가 방금 지운 문자열이 없다는 확인**이었다. 판정을 순수 함수(`missing_ci_tools`)로 빼서 음성이 실제로 몰게 하고, 양성 + `ruff` 쪽 음성을 함께 두었다(한쪽만 보면 다른 쪽 판정이 사라져도 초록불이다). **저장소 파일을 건드리지 않으므로 중단돼도 실물에 변이가 남지 않는다** — R23 이 음성 스위트에서 겪은 형태를 되풀이하지 않는다. ★★ **골든 출처 검사가 이름보다 넓게 주장했다** — 「출처를 밝히는가」인데 파일이 3개인지와 `expected_values` 문자열이 있는지만 봤다. `oracle_rank`·`oracle_source` 의 존재를 실제로 단언하고, **보지 않는 것**(출처가 저장소 안쪽을 가리키는지)을 독스트링에 적었다 — 여기서 차단하면 `Q-4`·`Q-5` 회신 전까지 CI 가 **영구 빨간불**이 되고 그런 검사는 곧 무시된다. **세 자리 모두 변이로 확인**했고 셋 다 전에는 어떤 변이도 통과시켰다. ★★ **전체 탐색 프리셋도 개수만 맞고 구성이 달랐다** — 결합 집합이 **하나도 없고** 뒤 3변수가 빠진 채 설비단가를 독립축으로 펼쳐 729를 맞췄다(빠른 탐색과 같은 형태). 구성표대로 **결합 2집합 + 독립 4** 로 놓고 예상 실행시간 2,187초까지 붙들었다. **SMP 자리에 다른 변수를 넣는 변이는 729가 그대로**이고 구성 단언만 잡는다. ★★ **`NFR-004-M1` 이 조항의 대상이 아닌 것을 재고 있었다** — 20동접 p95 산술은 진짜인데 때리는 것이 `/health`(상수 응답)라 **DB·서비스·직렬화를 지나지 않았고**, 시나리오 조회가 5초로 느려져도 초록불이었다. **50건을 적재한 뒤** 목록·상세 조회를 잰다(적재도 API 로 — 실제 경로를 지난다). **목록이 비어 있지 않은지 먼저 단언한다** — 빈 목록을 재면 「빠르다」가 「아무것도 안 한다」와 구별되지 않는다. 실측 p95 **56ms · 59ms**(한도 500ms, 여유 8.5배). `/health` 는 **기준선**으로 남겼다 |
| R28 | **단독 수행 — 거짓 마커를 고치자 CI 가 빨간불이 됐고, 그 자리에 조항이 비어 있었다. 세션 만료도 닫았다.** ★★★ **내가 낸 빨간불이다 — 그리고 그것이 옳았다.** R27 이 `test_solve_min_subsidy_rate_unachievable` 의 마커를 `FR-608-AC5` → `AC4` 로 바로잡았는데, **그 거짓 마커가 `AC5` 의 유일한 매핑이었다.** 고치자 매핑표에서 `AC5` 가 사라지고 `test_dod9_phase_1_musthave_unmapped_zero` 가 빨간불이 됐다 — 즉 **그 조항을 붙드는 것이 처음부터 아무것도 없었고 거짓 마커가 그 사실을 가리고 있었다.** 매핑표는 그동안 `AC5` 를 「자동 검증됨」으로 세고 있었다. **R20 이 같은 자리에서 「지우면 DoD 9 가 빨간불이라 유지 + 사유 기록」을 택했고, 이번에는 유지 대신 구현으로 닫았다.** ★ **push 로 CI 를 빨갛게 만들었고, 확인해서 알았다** — 로컬 `pytest -q` 는 초록불이었는데 CI 는 실패했다. 원인은 커버리지가 아니라 **매핑표를 읽는 DoD 테스트**였고, 로컬에서 그때 매핑표를 재생성하지 않아 옛 파일을 읽고 있었기 때문이다. **`gen_traceability.py` 는 마커를 고치면 반드시 다시 돌려야 한다**(§절차가 그렇게 적고 있고, 이번에 그 이유를 실측했다). ★★ **`FR-608-AC5` 를 구현으로 닫았다** — 탐색 기계는 이미 변수에 무관했고(`evaluate_func` 가 그 변수의 지표를 돌려준다) 없던 것은 둘이다: **조항이 이름으로 세운 다섯의 목록**(밖이면 거부 — 열어 두면 오타가 조용히 통과하고 그 답은 아무 변수의 것도 아니다)과 **결과에 변수 이름을 남기는 것**(`SolverResult.subsidy_rate` 에 융자금리를 담으면 표시 층이 보조율로 읽는다 — 값은 맞고 뜻이 틀린다). ★★ **세션 만료가 강제되지 않았다** — `SESSION_TTL_SECONDS` 는 쿠키 `max_age` 로만 나갔고(브라우저 힌트이지 서버 강제가 아니다) 서명 대상이 이메일뿐이라 **토큰에 시각이 없어 만료를 검사할 수 없었다.** 붙드는 것은 `assert SESSION_TTL_SECONDS == 24*3600` 하나였고 **상수만 보므로 만료 로직을 통째로 지워도 통과했다 — 실제로 그 상태였다.** 발급 시각을 **서명 대상 안에** 넣었다(밖에 두면 미래로 고쳐 무한 연장 가능 — 변이로 확인). 시각 없는 옛 토큰도 거부한다. ⚠ **이메일에 점이 있어 파싱을 오른쪽부터** 두 번 자른다 — 왼쪽부터 자르면 모든 정상 세션이 조용히 거부된다. ⚠ 남은 것은 **이 만료가 인가에 쓰이는 자리**다(세션 쿠키를 소비하는 곳이 없고 로그아웃도 없다). CI 조건 재현 실측: rc=0 · **커버리지 94.09%** |
| R27 | **단독 수행 — R26 이 찾은 「부르지 않는 함수」 첫째를 배선하고, `FR-608` 의 어긋남 넷을 닫았다.** ★★★ **배타 규칙 거부가 실행 경로에 들어왔다** — `core/casegrid/e2e_runner.py` 가 편익을 조립한 직후, **CBA 에 닿기 전에** `assert_no_exclusions()` 를 부른다. 그전에는 부르는 배포 코드가 0곳이라 §14 DoD 6 의 *「배타 규칙 위반 조합은 실행이 거부됨」* 이 실행 경로에서 성립하지 않았다. ★ **배선을 검증 가능하게 만드는 것이 설계의 절반이었다** — 내장 편익 둘은 배타 쌍이 아니므로 인자가 없으면 **위반 조합을 진입점으로 넣어 볼 방법이 없고**, 그러면 이 호출이 무언가를 막는지 아무도 확인할 수 없다(`extra_value_streams` 를 둔 이유). ★★ **「거부가 CBA 앞에 있다」를 따로 붙들었다** — 「예외가 났다」만 보면 **다 계산한 뒤에 던지는 구현**도 통과하고 그러면 위반 조합의 NPV 가 한 번은 존재하게 된다(`DV-10` 형태). `npv` 호출 횟수를 세되 **정상 경로에서 계수기가 실제로 1을 세는 것을 먼저 보인다** — 세지 않는 계수기로 「0회」를 단언하면 그 단언은 아무것도 붙들지 않는다. 변이 둘 중 **「거부를 CBA 뒤로 옮기기」는 그 단언만** 잡았다. ★★ **`FR-608` — 조항이 열거한 목표 셋 중 IRR 이 한 번도 실행되지 않았다.** `is_met` 의 IRR 분기가 부등호를 뒤집어도 초록불이었다(변이 확인). **「복수」는 자료형 자체가 없어** `Goal`·`MultiGoalResult`·`solve_min_subsidy_rate_for_goals` 를 넣었다 — 답은 **각 목표 최소 해의 최댓값**이고 **구속한 목표를 이름으로** 돌려준다(모르면 지원율을 낮출 때 무엇을 완화할지 알 수 없다). 최솟값으로 바꾸는 변이로 확인. ⚠ **테스트 둘이 서로 남의 조항을 달고 있었다** — `..._basic` 이 `AC4`(해 없음)를 인용하며 검사하지 않고, `..._unachievable` 이 `AC5`(변수 확장)를 인용하며 실제로는 AC4 를 검사했다. `solver.py` 주석도 같은 오기. ⚠ **`FR-608-AC5` 는 여전히 붙드는 테스트가 없다** — solver 는 변수에 무관하나 네 변수를 이름으로 세우는 진입점이 없다. ★ **오라클 손계산을 내가 틀렸고 테스트가 잡았다** — 「NPV ≥ 60 이면 r=0.30」이라 적었는데 실제는 0.80 이다. 구현이 맞고 문서가 틀린 경우이며, **기대값을 손으로 적는 규칙이 그것을 드러냈다.** pytest 1,316 → **1,322** |
| R26 | **단독 수행 — `I` 잔여 12건 재검증(4갈래 병렬 조사) + `pre-commit` 훅이 걸려 있지 않았다.** 판정 전문은 **`docs/clause-recheck-2026-08-14.md`**(추적되는 문서). ★★★ **12건에서 형태가 하나로 모였다 — 함수 층은 지어져 있는데 실행 경로가 부르지 않는다**: `FR-607-AC1`·`FR-402-AC2.A`·`FR-1101-AC2`·`DV-5` 넷 다 **부르는 배포 코드 0곳**이고, 테스트가 그 함수를 직접 부르므로 초록불이다. 조항이 말하는 「모든 실행에서」·「선택 시」·「배포 시 주입」은 **그 위 단계**인데 그 단계가 비어 있으면 **검사가 그것을 검사할 수도 없다.** R15(요금표)·R16(배타 규칙표)이 적어 둔 *「로더를 만드는 것이 아니라 로더가 실제 경로가 되게 하는 것」* 과 같은 자리다. ★★★ **`pre-commit` 훅이 이 작업 사본에 설치돼 있지 않았다** — `.git/hooks/` 에 표본밖에 없었고, 그 상태에서 **로컬 절대 경로가 담긴 커밋이 아무 저항 없이 만들어졌다**(같은 세션에서 실측. 미푸시라 커밋을 다시 써서 지웠다). 기존 검사 `test_private_data_scan_runs_in_both_pre_commit_and_ci` 는 **설정에 선언돼 있는지**만 보므로 초록불이었다 — **선언과 설치 사이를 아무도 보지 않았다.** `scripts/check_precommit_installed.py` 신설(로컬 전용 · CI 는 훅이 구조적으로 없으므로 스스로 판단을 보류한다 — `--require-vault` 와 같은 이유) + 음성 3 · 양성 1. ★★ **개수가 맞고 구성이 틀린 자리** — `quick_preset_grid()` 의 결합 집합에 **히트펌프 단가가 빠져 있었다.** 결합 집합은 몇 개를 묶든 축 하나이므로 케이스 수는 **문면의 그 27 그대로**였고 다섯 라운드 동안 초록불이었다. 변이로 갈랐다 — 히트펌프 자리에 다른 변수를 넣으면 **걸러진 27도 걸러지지 않은 729도 둘 다 그대로**이고 구성 단언만 빨간불이다(개수로는 원리상 붙들 수 없다). ⚠ **`full_preset_grid()` 은 고치지 않았다** — 729는 맞지만 결합 집합이 하나도 없고 뒤 3변수가 빠져 있다. 새 변수의 `target` 배선이 정해져야 하며, 이름만 넣으면 「729가 맞는」 상태가 한 번 더 만들어질 뿐이다. ★★ **조항의 150배 허용치와, 픽스처가 그것에 맞춰져 있던 것** — `NFR-104` 는 「기준값 대비 0.1%」인데 `_golden_regression_status` 는 **15%** 로 비교했고, **픽스처가 그 값에 맞춰져 있었다**(기대 100 에 실제 114.0=14% 높음을 주고 「통과」를 단언). **픽스처가 허용치를 따라가면 허용치가 조항에서 얼마나 멀어졌는지 테스트가 영영 말해 주지 않는다.** 조항 값으로 되돌리고 **경계 바깥 케이스(0.2%)를 더했다** — 통과 케이스만 두면 허용치를 다시 넓혀도 아무것도 빨간불이 되지 않는다(0.15 로 되돌리는 변이로 확인). 같은 테스트 끝의 **리터럴 단언 3줄**(`assert "npv_won" in ({...})["demo"]` · 두 값 중 하나면 참인 `detect_environment()`)도 지웠다 — 주석은 「CI 게이트 경로에 있음을 확인」이라 적고 있었으나 확인하는 것이 없었다. ⚠ **표 3번(`T` 56건)이 R17 에 닫혔는데 네 라운드째 남아 있었다** — 표를 사실과 맞췄다 |
| R25 | **단독 수행 — `DV-4` 후반부 「윤년 처리 규칙 명시」를 닫았다.** R24 인수가 R25 후보 1번으로 올린 것이며, **던져서 닫는 규칙이 아니었다.** ★★ **「명시」는 던지는 일이 아니라 선언하는 일이다** — 발동시킬 사건을 찾아 `ValidationError` 를 놓는 것으로는 「규칙이 적혀 있다」를 만들 수 없다. 그래서 이 라운드가 만든 것은 예외가 아니라 **선언**(`core/contracts/units.py` 의 `LEAP_YEAR_POLICY`)이고, 검사가 붙드는 것은 **선언의 실재와 그것이 참인가**다. ★★ **자리를 계약 층으로 정한 근거** — 규칙이 흩어져 있던 곳은 `core/der/load.py` 와 `core/assumption/timeseries_estimate.py` 의 지역 주석 둘이었는데, **둘 다 자기 모듈의 사정으로 적혀 있어** 저장소의 규약으로 읽히지 않았다. 스텝 수 상수를 쓰는 쪽이 계산 층과 저장 층(`infra/tsstore.py`)으로 갈리므로 **둘이 공통으로 받아 가는 유일한 자리**가 계약 층이다. 주석 둘은 선언을 가리키게만 바꿨다 — **사본을 만들면 갈리고, 갈린 것을 붙드는 검사를 또 놓아야 한다.** ★★★ **검사 셋 중 둘이 요점이다.** ① 문면이 지워지는 것을 막고(선언이 말해야 하는 것 넷을 단언) ② **`steps_per_year` 가 연도를 받기 시작하면 빨간불** — 그 변화는 아무 예외도 내지 않고 기존 호출부도 기본값으로 통과하므로 **선언만 낡은 채 남는다** ③ **`ALLOWED_ROW_COUNTS` 에 8784 가 들어가면 빨간불** — 허용이 늘어나는 방향이라 기존 검사를 하나도 깨지 않는다. **①만 두면 문자열이 있는지만 보게 되고 그것은 「주장은 있고 근거는 없는 검사」가 하나 느는 것**이다. ★ **선언은 닿아야 선언이다** — 366일 자료(8784·35136행)는 「잘리거나 중복된 자료」가 아니라 **규약이 다른 자료**인데 같은 문장으로 거절하면 사용자는 자기 자료가 온전한데 왜 거부되는지 알 수 없다. 거부 시 조치 자리에 **선언 그대로**를 싣고, 테스트가 `err.action == LEAP_YEAR_POLICY` 로 문면 이중화를 막는다. ★★ **「조항의 반대를 고정한 테스트」를 이유와 함께 다시 세웠다** — `test_tsstore.py` 는 예전부터 `year=2024`(윤년)에 8760행을 쓰고 통과했고, 그것은 **규칙을 검사한 것이 아니라 규칙이 없는 채 한쪽 결과를 고정한 것**이었다. 같은 사실을 「선언에 따른 것」으로 적고, 빨간불이면 고칠 것은 테스트가 아니라 선언임을 문면에 남겼다. ⚠⚠ **변이 하네스가 두 번 고장 났고 두 번 다 「결과」로 보였다** — ① `subprocess` 에 `env={...}` 를 통째로 넘겨 `PATH` 를 덮어썼고 pytest 가 자기 의존성(`iniconfig`)을 못 찾아 **다섯 변이가 전부 rc≠0**, 즉 **전건 잡힌 것처럼** 보였다(rc 만 보면 통과하는 형태다 — 그래서 **기대 테스트 이름이 출력에 있는지까지** 본다). ② 선언을 자르는 변이가 첫 조각만 주석 처리해 **뒤 조각들이 암묵 연결로 그대로 남았다** — 「놓침」이 검사가 약해서가 아니라 **변이가 성립하지 않아서**였다. **하네스의 빨간불도 초록불과 똑같이 근거를 봐야 한다.** 고쳐서 **변이 6종 전건 잡힘.** pytest 1,307 → **1,312** · mypy 100파일 · `lint-imports` 4 kept 0 broken · `check_marker_substance` 0건 · **자동 매핑 278 그대로 — 일곱 라운드째다**(조항이 느는 라운드가 아니라 조항당 검증이 깊어지는 라운드다) |
| R24 | **워커 5레인 + 단독 인수 — 대장 남은 8규칙. 「던지는 규칙」 6 → 11.** 8규칙을 성격으로 갈랐다 — **전환 4**(`DV-9`·`DV-1`·`DV-6`·`DV-8`) · **신설 2**(`DV-5`·`DV-10`) · **대상아님 2**(`DV-7`·`DV-11`). ★★★ **규칙 자신이 원문보다 작았다 — 14규칙 중 10.** `core/contracts/validation.py` 의 `DV_RULES` 사본이 spec §7.3 원문에서 잘려 있었고, **그 절단이 판정을 바꿨다.** `DV-6` 은 잘린 괄호가 「(미포함 시 경고 후 최근접 표)」 — **거부의 반대**를 요구하는데 사본만 읽으면 「포함하지 않으면 거부」로 읽힌다. 설계서·대상표·브리프 셋이 같은 절단을 물려받아 **레인 B 의 (가)/(나) 둘 다 틀렸고**, 그 구획에 사람이 와서야 드러났다. **이 저장소의 열일곱 번째 형태이며 자리가 새롭다** — 지금까지는 「검사가 코드를 붙들지 않는다」였고 이번은 **규칙 자신이 원문보다 작다.** 검사가 사본을 정확히 붙들어도 붙드는 대상이 이미 줄어 있으면 소용이 없다. 새 래칫 `tests/contract/test_dv_catalogue_matches_spec.py`(3건) — **강조 표시(`*`·백틱)와 공백만 지우고 낱말은 지우지 않는다**(괄호를 지우면 이 파일이 고치러 온 결함이 그대로 통과한다). ★★★ **`DV-7` 오분류 — 사본이 그것을 가렸다.** 파견 중 「이미 강제됨」(`Money` + `to_won` 경계)으로 새 칸 `ENFORCED_WITHOUT_A_THROW` 에 넣었으나, 원문은 선언 자리를 **`AssumptionSet` 수준**으로 못 박고 **「전 항목에 강제」**를 요구한다. `AssumptionSet` 필드는 `name`·`version`·`parent_version_id`·`notes` 넷뿐이고 실질/명목을 선언하는 필드가 저장소 전체에 없다. **인수에서 `NOT_YET_THROWN` 으로 되돌렸다.** ★ 검증 레인이 **사본만 보고 독립적으로 같은 반박**을 냈다(`DV-11` 은 동의) — 오케스트레이터가 자기 판정을 검증하면 같은 착각을 두 번 한다는 것이 그 구획을 둔 이유였고, 그것이 실제로 값을 냈다. ★★ **`DV-4` 는 규칙의 절반만 붙들고 있다 (인수 실측).** 원문 「…, **윤년 처리 규칙 명시**」 중 앞쪽(행수 8760/35040)만 강제된다 — 저장소 전체에 `8784`·`35136`·`isleap` **0건**이고, **`tests/infra/test_tsstore.py` 는 `year=2024`(윤년)에 8760행을 쓰고 통과하는 것을 여러 곳에서 고정**한다. 「조항의 반대를 고정한 테스트」의 새 사례다. **미해결에 등재 · R25 후보 1번.** ★★ **`DV-13` 은 기우였다** — 「미특정 편익은 활성화 불가」가 `core/contracts/valuestream.py:155` 의 `if enabled and effective_payer is UNSPECIFIED` 로 **생성 시점 = 활성화 시점**에 거부되고, 경계 양쪽(활성=거부 / 비활성=허용)을 고정한 쌍이 `tests/valuestream/test_formulas.py:221` 에 있다. 다만 `enabled` 는 평범한 공개 속성이라 **사후 대입으로 우회 가능**하고 그것을 막을 `payer_gate.assert_activation_allowed()` 는 **배포 호출 0곳**이다(유일한 호출자가 테스트). ★★ **`DV-5` 는 던지지만 아무도 부르지 않는다.** `core/cba/proforma.py::check_analysis_period()` 를 부르는 배포 코드가 **0곳**이다 — 사용자가 200년을 넣어도 그 함수를 지나지 않으면 막히지 않는다. **배선하지 못한 이유가 배선 누락이 아니다**: 분석기간의 소유자가 없고 자연스러운 자리인 `Scenario` 를 **`DV-11` 이 명시적으로 금지**한다. 분류(「던지는 코드가 있다」)는 맞으므로 표에 두되 **그 한계를 표 옆에 적었다** — 추적표를 읽는 사람이 「강제된다」로 읽지 않게. 미해결 등재. ★ **`DV-1` 의 유일한 강제가 `assert` 였다** — 오케스트레이터 스캔이 `ast.Raise` 만 훑어 `ast.Assert` 를 보지 못했고, 후보로 잡힌 4곳은 전부 무관한 자리였다. 전환 대상은 `:107`(구성상 참인 내부 불변식 — `assert` 로 유지)이 아니라 **`:99`**(사용자가 넘긴 보조율·융자율이 자부담을 음수로 만드는 자리)였다. **다음에 「강제 코드가 있는가」를 기계로 물을 때는 `Assert` 를 함께 셀 것.** ★ **오케스트레이터의 브리프 검사기 자신에 결함 셋** — 그중 ④는 `f"WP34{lane}.md" != name` 이 실제 파일명과 한 번도 같지 않아 **전건 초록불인 채 아무것도 붙들지 않고 있었다.** 「내 검사를 내가 의심한다」가 변이로 셋을 냈다. **인수(단독) — 워커 산출물을 받은 상태에서 게이트 셋이 빨간불이었다.** ① **ruff 2건** — 원문 글자 `×` 의 RUF001 은 **문면을 고치면 대조 검사가 빨간불이 되므로 `noqa` 로 규칙을 끄고 이유를 적었다**(검사를 통과시키려고 규칙을 고치지 않는다) · E501 1건. ② ★ **매달린 참조 4건** — 새 테스트 넷이 `@pytest.mark.req("DV-1")` 을 달았는데 **`DV-1` 은 §7.3 대장 ID 이지 수용기준 ID 가 아니다.** `gen_traceability.py` 가 *「해당 수용기준이 spec에 없음」*으로 잡았다. 같은 규칙을 붙들던 기존 테스트의 인용(`FR-604-AC9`)과 `NFR-303-M1` 로 고쳤다 — **규칙 ID 는 코드의 `rule="DV-1"` 과 래칫이 나른다.** ③ ★ **`check_marker_substance` 위반 1건** — `test_check_analysis_period_accepts_...` 가 조항 마커를 달고 **호출만 하고 아무것도 단언하지 않았다**(「예외 없으면 통과」). `pytest.fail` 로 거부 시 사유를 남기게 고쳤다. **셋 다 「돌려 보면 즉시 나오는 것」이며, 파견 산출물을 그대로 커밋했다면 CI 가 빨간불이었다.** 그 밖: `DV-7` 되돌림 · `DV-4`·`DV-13` 실물 확인 · `DV-5` 한계 기록 · 워커 변이 임시파일 7개 삭제 · 음성 스위트 10종 전건 통과(변이 잔류 0). ⚠ **로컬에서는 `PYTHONUTF8=1` 없이 게이트를 믿지 말 것** — 서브프로세스 출력을 cp949 로 디코드하다 `test_import_boundaries` 등 **6건이 죽고**, `lint-imports` 는 실행 자체가 실패한다(CI 는 UTF-8 이라 무관). pytest 1,263 → **1,307** · mypy **100파일** 무결 · `lint-imports` 4 kept 0 broken. **자동 매핑 278 그대로 — 여섯 라운드째다** |
| R23 | **단독 수행 — R20 이 「즉시 4건」으로 세우고 세 라운드 미룬 조항 넷을 닫았다.** 넷 다 마커가 붙어 추적표에서 **「자동 검증됨」이었는데 검사가 조항이 말하는 자리를 지나지 않았다.** ★★★ **`FR-1101-AC2` — 존재 분기를 다섯 라운드 동안 한 번도 지나지 않았다**: `test_seed_fallback` 은 비공개 시드가 **없을 때만** 실행하는데 `AC2` 는 *「비공개 시드로 관리한다」* 다. R14·R15·R20 이 세 번 같은 판정을 적어 두고도 남아 있었다. 경로 결정을 순수 함수(`private_seed_path()`)로 뺐다 — **R20 `SC-5` 처방 그대로**(모듈에서 `os.getenv` 를 읽고 바로 분기하면 검사가 소스 문자열밖에 볼 수 없다). ★ 값 하나만 단언하지 않고 **「주입한 경우와 안 한 경우가 다른 값을 낸다」**로 적었다 — 값 하나면 「항상 그 값을 내는」 구현도 통과한다. ★★★ **`FR-201-AC1` — 조항 뒷 절반은 실행 시점에 볼 수 있는 사실이 아니었다**: *「구성 변경 시 엔진 코드 변경이 발생하지 않는다」* 를 「엔진 파일이 안 바뀌었다」로 검사할 수는 없다. 그래서 **바뀌게 만드는 원인**을 막았다 — ① 편집기 소스에 자원 `tag` 문면이 없는지 `ast` 로 대조 ② 추가 가능한 목록이 레지스트리와 **「같은지」**(부분집합만 보면 고정 목록도 통과한다) ③ 편집한 구성을 **손대지 않은 `Model`** 이 짓는다. ⚠ **`ast` 검사에서 독스트링을 뺐다** — 그 규칙을 **설명하는 문장 자신**이 위반으로 잡힌다(이 저장소가 여섯 번 만난 형태이고 실제로 첫 실행에서 걸렸다). ★ 목록을 `("PV","ESS")` 로 고정하는 변이가 **`ast` 검사까지 함께 빨간불**로 만들었다 — 두 검사가 같은 결함의 앞뒤를 붙들고 있다. ★★★ **`FR-504-AC3` — 거부 사유가 조작을 말해야 했다**: 생성·복제 연산이 아예 없었고(`upsert` 만 있었다) 세 조작이 **같은 인가 함수**를 지난다. 거부를 「403 이 난다」로만 검사하면 **한 경로에서 가드를 빼도 이웃 경로의 같은 메시지에 묻힌다**(R22 실측 형태). 인가 함수가 `operation`(생성/복제/수정)을 받아 사유에 싣게 하고 경로별로 그 낱말을 단언했다 — **「수정」 가드만 뺀 변이와 「복제」 가드만 뺀 변이가 각각 따로 잡혔다.** 「파일 수정·재배포 불요」는 편집 전후 `docs`·`fixtures`·`core/regulation` 트리 스냅샷과 **같은 앱 인스턴스의 재조회**로 붙들었다. ★★★ **`FR-704-AC4` — R20 형태가 네 번째로 나왔다**: `core/cba` 는 관점 **하나**의 결과를 낸다(재료다). 재료 셋을 한 표로 놓는 **합성이 놓일 상위 자리가 비어 있었고**, `core/cba` 에서 하면 계층 역방향이다. `core/report/perspective_report.py` 신설 — `regulation_axis`·`dataset_axis`(R20) · `incentive_cases`(R21) 에 이은 **네 번째**다. ★ **「담고 있다」로는 조항이 닫히지 않는다** — 세 결과를 담기만 하고 표를 내지 않으면 사람은 여전히 세 리포트를 나란히 놓고 본다. **「지표 한 줄에 관점 셋의 값이 같은 순서로 실린다」**와 **「각 열이 자기 수치를 든다」**를 붙들었다(세 열이 한 결과를 복사해도 「열이 셋인 표」는 만들어지고 그럴듯하게 보인다). `Perspective.SOCIETY` 로 확장점의 **두 번째 인스턴스**를 넣어 보였다. ★★ **검사 하나의 이름이 붙드는 것보다 넓게 주장하고 있어 좁혔다** — `..._asks_the_authorization_layer_rather_than_deciding_itself` 는 화면이 값을 하드코딩해도 오늘은 통과한다. `..._agree_on_every_role` 로 바꾸고 **붙드는 것과 붙들지 못하는 것을 독스트링에 갈라 적었다.** ⚠ **XLSX 시트로는 내보내지 않았다** — `test_report.py:134` 가 시트 목록을 못 박고 있고 그것은 **`FR-1003-AC1` 의 계약**이다. 조항 하나를 닫으려고 다른 조항의 계약을 고치지 않았다(R24 후보). ⚠ **음성 스위트를 중단시키면 실물 소스에 변이가 남는다** — 시간초과로 죽인 실행이 `exclusion_table.py` 에 `# NEGCHECK` 를 남겼다. `finally` 원복이 프로세스 종료로 건너뛰어진 것이고 단독 실행은 깨끗하다. **`git status` 로 보이므로 라운드를 닫기 전 반드시 볼 것.** 인수: 조항마다 변이를 직접 심어 **28건 전건 빨간불**(4·8·9·7), `-x` 없이 돌려 어느 검사가 잡았는지를 보았다. pytest 1,223 → **1,263** · mypy 98 → **101**파일 · `lint-imports` 4 kept 0 broken. **자동 매핑 278 그대로 — 다섯 라운드째다**(넷은 이미 세어지고 있었으므로 건수로 나타나지 않는다. R19 가 `FR-803-AC1` 에서 처음 적은 형태) |
| R22 | **워커 5레인 + 오케스트레이터 — `NFR-303-M1` 나머지 89곳 전건 판정.** ★★★ **「검사가 세 경로를 붙든다고 적고 둘만 붙들고 있었다」**: `_check_operating_mode` 의 「미지정」 가드(`if mode is None:`)를 지워도 바로 아래 「목록 밖」 분기가 `None` 을 걸러 **같은 `DV-14` 를 던지므로** 기존 단언(규칙 ID·field·reason 의 인스턴스명·action 비어 있지 않음)이 **전부 통과**했다. 그런데 그 테스트 독스트링은 *「셋을 한 표로 도는 이유는 경로마다 따로 붙들어야 하기 때문」*이라 적고 있었다 — **주장이 근거보다 넓었다.** 경로마다 **그 경로에만 있는 문면**을 단언하게 고치고 같은 변이로 잡히는 것을 확인했다. ★★★ **래칫이 자기가 막으려던 드리프트를 못 잡았다 — 열여섯 번째**: R21 이 놓은 `test_dv_rule_enforcement.py` 는 「합집합이 대장과 같은가」와 「양쪽에 겹치지 않는가」만 봤다. 그래서 `core/der/ess.py` 가 `DV-2`(SOC)·`DV-3`(RTE)를 **던지기 시작했는데도** 분류는 「아직」인 채 **전건 초록불**이었다. `ast` 로 배포 코드의 `ValidationError(..., rule=)` 상수를 뽑아 대조하는 검사 둘을 더했다(뒤처짐 · 코드 사라짐 양방향). 변이 둘로 확인했고 **파일까지 지목한다.** **대장 강제 4 → 6** — R21 이 「던지는 코드가 한 줄도 없다」고 짚었던 `DV-2`·`DV-3` 이 실제 생성자를 지난다. ★★ **변이 하네스의 첫 결과가 가짜 초록불이었다**: 대상에 **이미 빨간불인 테스트**가 섞여 있어 `-x` 로 돌리면 무엇을 망가뜨려도 「잡힘」이 됐다. 기존 빨간불을 `--deselect` 하고 다시 돌려야 위 한 자리가 드러났다 — **하네스의 초록불도 근거를 봐야 한다**(§5 의 8번이 말하는 형태). ★★ **파일 크기 단언이 중앙 게이트와 다른 것을 재고 있었다**: 전환이 근거 주석을 늘리자 `load.py`(512줄)만 빨간불이 됐는데, 그 단언은 **raw 줄**을 세고 `check_file_size.py --code-strict` 는 **코드 줄만** 상한에 건다(rc=0). 게다가 자원 **6파일 중 둘에만** 있어 `pv.py`(632)·`ess.py`(697)·`heatpump.py`(676)는 지키는 검사 없이 지나갔다 — §5 의 6번 그대로다. 게이트 자신이 *「줄 수를 맞추려고 근거 주석을 지우지 마십시오」*라 적으므로 **두 단언이 `measure_file()` 로 같은 것을 재게** 했다. 코드 줄로는 여섯 파일 모두 여유(최대 `ess` 455/500). ★ **`core/contracts/` 44곳의 사전 추정이 실물에서 뒤집혔다** — 「전환 30·대상아님 14」가 **전환 8 · 대상아님 25 · 자리가 호출측 6 · 동작 변경 수반 2** 로 갈렸다. `DispatchResult`·`RunContext`·`check_series` 는 전부 **호출측 계약**이었고, `units.py` 6곳은 `to_won(value)` 가 그 값이 **어느 필드인지 몰라** 3요소 중 `field` 를 만들 수 없다(전환 자리는 호출측). `assumptions.as_float` 은 전환하면 `TypeError`→`ValueError` 로 **예외 타입이 바뀌어** 못 박은 테스트가 깨지므로 보고만 했다. ★ **`degradation_rate` 에 `rule=` 을 달지 않았다** — 코드 경계(`0~1`)가 대장 `DV-3` (`[0,10]%/년`)보다 **10배 넓다.** 넓은 경계에 규칙 ID 만 붙이면 추적표가 검증됐다고 센다. **워커(`claude3`)가 `ess.degradation_rate` 에서 독립적으로 같은 판정을 냈다.** ★ **`field` 관례 위반 0건** — 다섯 레인이 전부 `<tag소문자>.<필드>` 를 지켰다(R22 의 0번이 정한 관례). `rule=` 가드 **12자리 전건 변이 확인.** pytest 1,140 → **1,223** · mypy **98**파일 · `lint-imports` 4 kept 0 broken. **자동 매핑 278 그대로**(R19~R22 넷째 라운드 — 성과 지표가 아니다) |
| R21 | **워커 5레인 — `FR-607-AC1` 깃발 제거 · `FR-901-AC1`·`SC-5` 인증 · `NFR-303-M1` DV 전환 · `UI-1-AC1`·`FR-502-AC4` 화면.** ★★★ **「대장에 열넷을 적어 두고 셋만 던지고 있었다」**: §7.3 검증 규칙 대장은 `DV-1`~`DV-14` 열넷인데 **실제 코드가 `ValidationError(rule=)` 로 던지는 것은 셋**(`DV-4`·`DV-12`·`DV-13`)뿐이었다. **`DV-2`(SOC 하한<상한)는 계약 테스트가 손으로 만든 인스턴스**로 두 번 나올 뿐 그 규칙을 던지는 코드가 한 줄도 없다 — 「검사가 아무것도 붙들지 않았다」의 **예외 생성자 쪽 형태**이고 **열네 번째**다. `DV-14` 는 반대로 **강제 코드가 처음부터 있었고**(`_check_operating_mode`) 없던 것은 규칙 ID 를 나르는 형태였다 — 그래서 `test_pv.py` 독스트링이 「(DV-14)」라 적으면서 **기계가 확인할 방법이 없었다.** 세 거부 경로를 전환하고 **대장을 「던지는 것」/「아직 아닌 것」으로 가르는 래칫**을 놓았다(`tests/contract/test_dv_rule_enforcement.py` — 열다섯 번째가 대장에 들어오면 빨간불). ★★★ **조항이 요구하는 import 방향을 계층 규칙이 금지했다 — R20 형태의 세 번째**: `FR-607-AC1` 은 등록된 변형 목록으로 파이프라인을 돌리라는데 그 목록(`core.casegrid.variants.run_order`)이 `core.incentive` 보다 **상위 계층**이라 `lint-imports` 가 실제로 빨간불을 냈다(`NFR-208-AC1`). **고칠 곳은 계층 설정이 아니라 합성이 놓인 자리였다** — 느슨하게 하면 조항 하나를 닫으려고 다른 조항을 연다. **`core/casegrid/incentive_cases.py` 를 위층에 신설**해 풀었다(`regulation_axis`·`dataset_axis` 에 이은 셋째 인스턴스). `is_baseline` 깃발은 **살아 있는 코드에 참조 0건** — R17 이 예고하고 네 라운드 밀린 것이 닫혔다. ★★ **`state.md` 가 적은 「남은 DV 지점」 인벤토리가 두 방향으로 틀렸다**: `valuestream.py:138` 은 **대상아님**(`__init_subclass__` 는 import 시점이고 어긴 값은 개발자 선언표다. 붙드는 테스트 마커도 `FR-205-AC1` 이며 이름이 `..._fails_at_class_definition` 이다) — 그리고 `core/contracts/` 만 해도 **43곳**이 있다. ★★ **인수로 워커 보고를 재현했다 — 변이 아홉을 직접 걸었다.** `claude2`·`claude5`·`opencode` 는 전건 성립(**환경변수를 읽고 버리는 R20 `SC-5` 결함의 형태를 포함해 잡힌다**). `agy` 마커 위치표는 **36행을 AST 로 재계산해 35행 일치 · 1행 오염** — `NFR-402-AC1` 을 `FR-402-AC1` 으로 물어 왔고 **원인은 왼쪽 단어 경계 없는 정규식**이다(R18 이 기록하고 R15 가 스친 함정, **세 라운드째**). 표를 넓히면 `FR-105-AC1`↔`NFR-105-AC1`(21 대 21) 등 **아홉 행이 오염된다.** ★ **구획 사이에서 `field` 경로 관례가 어긋났다** — 같은 `DV-14` 를 워커는 `pv.operating_mode`, 내 계약 쪽은 `der.<인스턴스명>.operating_mode` 로 냈다. **내 것이 틀렸다**: 인스턴스 이름은 사용자가 지은 자유 문자열이라 **필드 키가 열거 불가능**해진다(관례는 `valuestream.{tag}.payer`). ★ **`claude5` 가 내 브리프의 오류를 잡았다** — 예시가 무관한 검사에 `rule="DV-14"` 를 달아 매달린 참조가 될 것이었다. **브리프 예시는 그대로 베껴진다.** pytest 1,103 → **1,140** · `lint-imports` 라운드 중 1 broken → **4 kept**. **자동 매핑 278 그대로** |
| R20 | **워커 5레인 복귀 — `M` 15건 전건 닫고 `I` 구획 셋을 구현했다.** ★★ **「축을 만드는 타입은 생겼는데 그리드가 그것을 볼 수 없었다」 — 같은 결함이 두 조항에서 나왔다**: 「케이스 그리드의 탐색 변수로 지정한다」를 말하는 조항이 둘인데(`FR-504-AC8` 제도 프로파일 · `FR-905-AC5` 시계열) 두 구획이 각자 축 재료 타입을 만들고 **둘 다 `CaseVariable` 과 평행한 별개 타입**이라 `CaseGrid` 가 소비할 수 없었다. **워커 잘못이 아니다** — `layers` 계약이 `core.casegrid` 를 위에 두므로 하위 구획은 `CaseVariable` 을 알 수 없고, **변환이 위층에 있어야 하는데 그 자리가 비어 있었다.** `core/casegrid/{regulation_axis,dataset_axis}.py` 로 이었고 **한 번의 `generate()` 가 모든 케이스를 내는지**까지 붙들었다(둘째 파일이 첫째 확장점의 **두 번째 인스턴스**다). ★★ **`SC-5` 검사가 소스 문자열을 붙들고 있었다 — 열세 번째**: 환경변수를 **읽고 버리도록** 고쳐도 통과했다(소스에 `DER_DB_URL` 이라는 **글자**가 있는지만 보았고 후반부엔 단언이 아예 없었다). 순수 함수 `resolve_db_url()` 로 빼고 「무엇을 돌려주는가」와 「그 값으로 엔진이 만들어지는가」를 층마다 따로 붙들었다. ★ **`infra/` DV 9곳을 「전건 전환」하지 않았다** — 둘만 `ValidationError`(필드·사유·조치)로 바꾸고 일곱은 「파일 부재·손상은 입력 검증이 아니다」로 판정. **판정 사유가 산출물이다.** ★ **`M` 마커 도구가 함수명을 둘 틀리게 환원했다** — 낡은 행번호가 **같은 마커를 가진 다른 함수** 범위에 떨어지면 「낡음」 신호가 사라지고 틀린 함수를 자신 있게 가리킨다. 판정서의 **설명문**으로 확정했다. `Dockerfile` 이 앱 대신 `pytest` 를 띄우던 것을 고쳤다(`uvicorn app.main:app`). 「즉시」 4건은 **유지 + 사유와 닫는 조건을 마커 옆 코드에** 적었다(`.orch/` 는 추적 밖이라 여기 적으면 저장소에 안 남는다). pytest 1,083 → **1,103** · mypy 91 → **97**파일. **자동 매핑 278 그대로** |
| R19 | **분류표를 정리하고 `FR-803-AC1` 을 실물로 만들었다** (단독). ① **표 정리** — `FR-201-AC3`~`AC5` 허위 인용 삭제 · 「플러그인 DER」 6행을 `NFR-207` 로 재귀속했더니 **이미 구현·검증돼 있었다**(`discover()` · `RegistryError`) · `FR-905-AC1~AC8` 한 행을 **6행으로** · `FR-601-AC8`/`AC9` 를 쪼개니 `AC8` 은 이미 충족 · 행번호를 **전건 함수명으로**. 결과 **`I` 32 → 29 · 대조 실패 전건 0**(없는 조항 0 · 마커 없음 0 · 행번호 낡음 0) ② **`core/report/charts/feasible_region.py` 신설** — 차트 6→**7종**, 등고선+음영 실렌더 PNG. 테스트 14건. ★ **「자동 매핑 278」이 늘지 않았다 — 그것이 이 라운드의 소득이다**: `FR-803-AC1` 은 **이미 「자동 검증」으로 세어지고 있었다**(마커가 `test_phase1_dod.py`·`test_17_2_dod2.py`·`test_casegrid.py` 에 있었고 그것들은 `feasible_region()` **자료**만 검증했다. 시각화 코드는 저장소에 한 줄도 없었다). ★ **내 음영 검사가 음영이 아니라 범례 문구를 붙들고 있었고 변이 테스트가 그것을 잡았다** — 「달성 칸 수는 같고 자리만 다른」 비교로 고쳤다. `gen_traceability.py` 가 같은 파일명을 13회 반복하던 것을 「N건」으로 접었다. pytest 1,069 → **1,083** |
| R18 | **R17 이 남긴 최우선을 닫았다 — 분류표 `I` 32건을 조항 실물로 대조**(`.orch/verify_clause_citations.py` 신설 · `.orch/R18-WP29A-재검증표.md`). **32건 중 13건(41%)이 서지 않았다** — 허위·오귀속 8(`FR-201-AC3`~`AC5` 는 **조항이 없고**, `AC1`·`AC2` 는 문면이 「플러그인」과 무관하다. 실소관은 `NFR-201`·`NFR-207` / `NFR-004-M1` 의 `/scenarios` 는 **spec 전문에 0건**) · 근거 오류 3(`SC-5` 2건이 **존재하지 않는 `.github/workflows/ci.yml`** 을 근거로 들었다) · 낡음 2(`FR-402-AC2.A`·`AC7` 은 R17·R16 이 닫았다). **실제 크기는 19건**이고 넷은 R16 확장점 위에서 축소됐다. ★ **조항 정본이 하나가 아니다** — `traceability.md` 의 조항 칸은 게이트가 세는 단위라 짧고 규범 문장은 spec 본문에 있다(`NFR-104-M1` 「0.1% 이내」·`NFR-303-M1` 「필드/사유/조치」가 그렇게 **표가 옳았다**). ★ 마커를 `@pytest.mark.req` 에 다는 저장소에서는 **`ast` 함수 범위에 데코레이터를 넣어야** 한다 — 넣지 않아 22행이 「모듈수준」 오탐이었다. 코드 변경 0 |
| R15 | **`core/regulation/tariff_loader.py` 신설** — `FR-501-AC4` 를 처음으로 실제 검증. `scripts/negtest_task_mapping.py` **신설**(20종) + **CI 에 `task-mapping` 잡 신설**. 마커 「무관」 36건 전부 처분(고침 13 · 마커삭제 15 · 조항부재 8). **「좁음」 86건을 T 56 / I 32 / P 2 로 분류** — 그 `I` 중 5건이 Phase 1 DoD 자리다. 작업 목록 `9.8` 신설(v2.5). 감시기 느슨한 패턴이 **형제 구획을 삼키던 것** 수정(음성 50 → 56종) |

> ### ★ R13 이 「자동 검증됨」의 뜻을 바꿨다 — 수치가 나빠진 것이 아니다
>
> `core/report/` 의 `pdf`·`excel`·`charts` 가 **독스트링에 스스로 mock 이라고
> 적어 놓은 채** `FR-1001`~`FR-1005`(전부 Must-have · Phase 1)를 「자동
> 검증됨」으로 채우고 있었다. `pdf`·`excel` 은 실물로 만들었고, **`charts`
> (`FR-1004-AC1`)는 아직 mock 이다** — 아래 「지금 할 일」 2번이다.
>
> 그리고 **차단 수동검사 `MC-1` 이 한 번도 수행되지 않은 채 「충족」으로
> 섞여 있었다.** 지금은 갈라져 **「판정불가」**로 센다. Phase 1 인수 12항목은
> *「판정가능 11 전건 충족」*에서 **「판정가능 10 전건 충족 / 판정불가 2」**로
> 바뀌었다 — **전에 몰랐던 것을 안다고 적고 있었던 것이다.**

> ### ★ R14 가 찾은 것 — 감시기 오탐 방지가 **실전에서 꺼져 있었다**
>
> R13 이 `judge_no_output()` 에 넣은 `consumed_at_dispatch` 비교는 음성 26종이
> 감싸고 있었는데, **`dispatch.py` 의 `main()` 이 완료패턴을 넘기지 않아 그
> 필드가 실전에서 한 번도 기록되지 않았다.** 음성이 못 잡은 이유는 그것들이
> 필드를 손으로 만들어 넣어 **실전 진입점을 지나지 않기** 때문이다.
>
> **이 저장소에서 같은 형태가 다섯 번째다** (R8 복사 · R9 재구현 · R10 요약줄
> · R13 도달 불가 · R14 배선). **「음성 테스트가 있다」와 「그것이 실전 경로를
> 붙들고 있다」는 다르다.**

## 08-09 — R0 계약 개정 v1.2 (오케스트레이션 착수)

**Phase 1 잔여를 3개 에이전트(`opencode` · `agy` · `codex`)에 구획별로 배분해 진행한다.** 배분은 새로 만들지 않고 **spec §16.1 W-1 / §16.3 소유 경로표를 그대로** 쓴다 — 「에이전트별 파일 중복 수정 금지」는 이 저장소가 이미 정의해 둔 규칙이고, 새 분할을 만들면 정본과 갈린다. 라운드마다 **작업 주체와 다른 에이전트**가 소스를 고치지 않고 점검하며, 커밋은 점검 통과 후 오케스트레이터가 라운드 단위로 한다.

**R0은 단독 수행이다 (WP-0).** §16.2가 *"구현 PR에 계약 변경을 섞지 않는다"* 로 규정하고, 08-08에 병렬이 계약 구멍 5건을 드러낸 전례가 있다. 여기서 판단이 갈리면 이후 전 구획이 재작업이다.

**계약 개정 v1.2 — 「미해결」이 *착수 전 판단*으로 남긴 4건을 닫았다.**

| # | 항목 | 판단 |
|---|---|---|
| ⑥ | 요금·연료 단가의 자리 | `DispatchContext` 에 **가격 신호**로 싣는다. 「요금」이 아니라 「신호」 — 누진·TOU·특례 해석은 WP-3, 자원은 *언제 돌릴지*만 본다 |
| ⑦ | 대장을 읽는 경로 | `AssumptionProvider` 신설. **없으면 멈춘다** — `require()` 를 계약의 기본 구현으로 둬 「없을 때의 처리」가 구현마다 갈리지 않게 했다 |
| TU-6 | 누진·TOU 혼재 | **한 계량점에 둘을 동시 적용하는 요금제는 없다.** 「혼재」는 계량점이 여럿이라는 뜻 → 계량점 단위 독립 계산 + 합산 |
| B/C | 오라클 미배정 | §13.0.2 **순위 1** (spec v0.12). 분자·분모 각각 원 단위 완전 일치, 비율에 별도 오차 없음 |

> **v1.1이 「이미 갈린 것」을 모았다면 v1.2는 「갈리기 직전의 것」을 막는다.** 08-08의 다섯 구멍은 여섯 구획이 **먼저 구현한 뒤에** 드러났다. 이번 둘은 아직 아무도 구현하지 않았고, 그래서 **비용이 계약 파일 두 개다.**
>
> **잔여 부채는 숨기지 않고 테스트에 고정했다.** 자원이 아직 들고 있는 요금 인자 3건(`EV_V2G` 1 · `HeatPump` 2)은 `KNOWN_TARIFF_DEBT` 로 못박혀 **늘면 즉시 빨개진다.** 지금 0건을 요구하면 검사는 켜는 즉시 빨간불이고 `RC-HP`·`RC-EV` 검증 케이스를 함께 고쳐야 한다 — 2.7이 「미매핑 0건」을 요구하지 않은 것과 같은 판단이다. **드러난 부채는 부채이고, 드러나지 않는 부채가 문제다.**
>
> **`DER_VAULT_ROOT` 가 프로세스에 상속되지 않는 경우가 있다.** 사용자 환경변수에는 있는데 이미 떠 있던 셸은 그것을 모른다 — `--require-vault` 가 종료 코드 2로 잡았다. 08-08에 이 플래그를 신설한 이유가 정확히 이것이며, 없었다면 **저장소 사본 대조로 내려가 「통과」가 떴을 것**이다. 새 터미널을 열거나 `DER_VAULT_ROOT` 를 명시한다.

**테스트 582 → 591건 · 자동 매핑 52 → 55건 · 미매핑 219 → 216건 · 커버리지 90%.**

---

> **이 수치는 R31 종료 시 실제로 명령을 돌려 받은 값이다.** 베끼지 말고 다시
> 돌릴 것 — **이 자리가 R17 실측인 채로 열세 라운드를 지났고**, 그동안 여기
> 적힌 1,065 가 근거로 쓰였다. 08-09→08-10 에 같은 일이 다섯 라운드 있었고
> 이 파일 머리말이 그때 「세어서 적거나 적지 않는다」를 규칙으로 박았다.
> **그 규칙은 갱신에도 적용된다 — 갱신하지 않는 것도 틀린 수를 적는 것이다.**

> ### ★ R31 에 자동 매핑이 **아홉 라운드 만에 늘었다: 278 → 284**
>
> 아래 문단이 *「매핑 건수가 늘어야 하는 것은 `I`(구현 누락) 라운드다」* 라고
> 적었는데, R31 은 **조항이 늘는 라운드**였다 — spec v0.15 가 「테스트는 있는데 그
> 실패 유형을 규정한 조항이 없다」 여섯을 조항으로 메웠고(수용기준 308 → 314) 마커를
> 옮기자 그 여섯이 전부 매핑됐다. **미매핑은 8건 그대로다**(전부 Phase 2·3).
>
> 판정·근거는 `docs/decisions-2026-08-14-R31.md` §10 이다.
>
> <sub>이하 R30 시점 문면 (이력)</sub>
>
> **자동 매핑은 278건으로 R16 이래 같다 — 그것이 정상인 라운드가 있다.**
> R17(`T`)·R22~R30 처럼 「테스트가 조항의 일부만 본다」를 넓히는 라운드는
> **매핑되는 조항이 늘지 않고 조항당 검증이 깊어진다.** 매핑 건수가 늘어야
> 하는 것은 **`I`(구현 누락) 라운드**다 — 278 을 보고 「진척 없음」으로 읽지
> 말 것. (R30 도 기존 `NFR-303-M1` 마커를 썼으므로 278 그대로다.)

> ### ✔ 작업 목록의 하위 건수 — **R17 이 세어서 하나로 만들었다: 157건**
>
> R16 이 *「세 곳에서 갈린다」* 고 적어 둔 자리다. **세어 보니 157 이었다.**
>
> | 어디 | R16 까지 | 지금 |
> |---|---|---|
> | `rslt/task-*.md` 머리말 | **158** (`v2.5`) | **157** (`v2.6`) |
> | 같은 파일 본문 14행 | **155** | **157** |
> | 이 파일이 R15 까지 적어 온 값 | **157** | — (소유자는 작업 목록이다) |
>
> **머리말의 158 이 어떻게 나왔는지가 이 항목의 교훈이다** — R16 이 `9.8`
> 신설분을 **세지 않고 `+1`** 했다. 이 파일이 정한 규칙(*「세지 않은 수를
> 옮겨 적으면 그것이 다음 라운드의 계획으로 따라 들어간다 — 세어서 적거나
> 적지 않는다」*)을 **증감분에도 적용해야 한다**는 것이 R17 이 더한 것이다.
>
> **다시 갈리지 않게 하는 것은 수를 고친 것이 아니라 규칙을 박은 것이다.**
> 작업 목록 §작성 규칙에 **「하위 작업의 정의」와 세는 명령**을 넣었다 —
> `## N.0` 절의 최상위 `- **N.M**` 항목만 세고, `### 6.0.S` 공통 5단계는
> 템플릿이므로 한 번만 센다. **건수를 적을 때는 그 명령을 돌려 출력을 적는다.**
> 번호 중복도 그 명령이 함께 본다(지금 0건).

> ### ⚠ R15 가 「Phase 1 전건 충족」에 물음표를 붙였다 — **이것을 먼저 읽을 것**
>
> R14 가 마커 431건을 전수 대조하고 R15 가 그중 **「좁음」 86건의 성격을**
> 갈랐다. 결과는 **T(테스트만 좁다) 56 · I(구현이 없다) 32 · P(Phase·환경) 2** 다.
>
> **그 `I` 중 5건이 `tests/acceptance`·`acceptance2` — Phase 1 DoD 판정 그
> 자체다.** 무거운 셋:
>
> | 조항 | 무엇이 없는가 |
> |---|---|
> | `FR-803-AC1` | 「등고선 + 목표달성영역 음영」 **시각화가 저장소 어디에도 없다.** 데이터(`FeasibleCell.achieved`)는 계산되지만 렌더링이 `charts.py` 3종(`cashflow_line`·`cost_benefit_pie`·`tornado`) 밖이다 |
> | `FR-607-AC1` | 「무지원(0%) 케이스 자동 병행 산출 + 리포트 최상단 배치」의 **파이프라인 통합이 없다.** `is_baseline` 은 호출자가 손으로 넘기는 깃발일 뿐이다 |
> | `FR-402-AC2.A` | 이중계상 선택 시 **실제 거부(예외)가 없다** — 감지·라벨링뿐이다 |
>
> **「미충족」이라 단정하지 않는다.** 「좁음」은 부분 검증이므로 조항이 통째로
> 미검증인 것은 아니다. **그러나 DoD 를 떠받치는 조항의 핵심 부분이
> 미구현이라는 뜻이고**, 그것을 모른 채 「전건 충족」이라 적어 온 것이다.
> 근거는 `.orch/R15-WP26C-분류표.md` 에 조항별로 있다. **R16 의 1번이다.**

> **Phase 1 인수 12항목 중 판정가능 10 은 전건 충족이고, 2 는 판정불가다.**
> 판정불가의 정체가 중요하다 — 하나는 **차단 수동검사 `MC-1` 이 아직 수행되지
> 않았기 때문**(코드로 닫을 수 없다. 심의 경험자 3명 대상 설명 재현)이고,
> 나머지는 환경이 없어 수행 자체가 불가한 것이다. **「미충족」이 아니라
> 「아직 판정하지 않았다」이며, 그 둘을 같은 칸에 세지 않는다** (§13.0.1 ④).

> **다음 라운드에 산출물이 크게 움직일 자리는 둘이다** — `FR-1004-AC1` 차트
> (6종 중 3종은 구현조차 없다)와 `FR-205` 정산엔진(**결정 5건이 사용자
> 판단 대기**). 아래 「지금 할 일」 1·2번.

이전 요약(08-09 시점, 이력):

08-08에 **Wave 0을 전부 닫고, Wave 1 자원 6종을 쓰고, 그 병렬 수행이 드러낸 계약의 구멍을 메우고, 플러그인 자동 등록과 CI 게이트 4종을 세웠다** — 2.15 ①(표 수용기준 전개, 252 → **299건**), 16.1(전제 데이터 가정값 전환), 2.7(CI 매핑 게이트 활성화), 1.0 계약 고정, 6.0 자원 6종 + 공통설비(**병렬 수행**), **1.11 계약 개정 v1.1**, **6.7 플러그인 자동 등록**, **2.5·2.6**(NFR-105 게이트 ①②), **2.8**(NFR-208 음성 3 + 양성 2), **2.10**(NFR-203 커버리지 하한 85%), **2.16**(정본 표류 상시 감시).

**테스트 582건 · 계약 테스트 38 → 60건 · 자동 매핑 0 → 52건 · 미매핑 261 → **219건** · 커버리지 90%.**

> **병렬 수행이 계약의 구멍을 드러내고, 계약 개정이 그것을 메웠다 — 이것이 오늘의 소득이다.**
>
> 6개 구획이 **같은 계약을 서로 모른 채 구현하자** 계약이 답하지 않은 질문들이 각자 다른 답으로 메워졌다. 다섯 자원이 `capex_vat()` 를 각자 지어냈고 **ESS 하나는 아예 만들지 않아 세액이 사라져 있었다.** 같은 「2%」가 자원에 따라 `2.0`(%)과 `0.02`(소수)로 들어갔고 **어느 쪽도 오류가 아니었다.** `dispatch()` 의 부분 창 해석은 자원마다 갈렸다.
>
> **한 사람이 순차로 짰다면 일관돼 보였을 뿐 같은 구멍이 그대로 남았을 것이다.** 병렬이 비용이 아니라 진단이었다.

> **08-08 후반의 소득은 다른 종류다 — 켜 놓은 검사들이 실은 아무것도 검사하지 않고 있었다.**
>
> CI 에는 **pytest·ruff·import-linter 잡이 아예 없었다.** `status.md` 가 대체 명령으로 적어 둔 `python -m importlinter.cli lint` 는 **위반을 심어도 종료 코드 0** 을 낸다(그 모듈에 `__main__` 가드가 없다). diff-cover 는 산출물 경로가 어긋나면 **종료 코드 0** 으로 통과한다. `schtasks` 는 긴 명령을 **오류 없이 잘라** 감시기가 정본 대신 사본을 보게 만들었다.
>
> 넷 다 **초록불**이었다. 「검사가 통과했다」와 「검사가 무언가를 검사했다」는 다르다 (§13.0.1 ④) — 이 저장소가 반복해서 만나는 그 간극이다.

**CI 게이트는 이제 다 섰다. 다음은 2.4(mypy 5건 실패)와 2.9(lint 규칙)다.** `FR-102-AC1` 잔여 자원 3종은 **Phase 2·3이므로 지금 하지 않는다** — 아래 표 각주를 볼 것.

---

> <sub>이하 R31 종료 시점 문면 (이력)</sub>

> # ⏹ 2026-08-14 R31 종료 · **결정 아홉을 정하고 그 아홉을 전부 구현했다**
>
> 사용자가 「분산자원 평가 시스템을 **확장성 있게** 구현한다」는 목적에 맞춰 결정을
> 대신 내리라고 위임했고, **아홉을 확정한 뒤 우선순위표대로 여덟 작업을 완주했다.**
> 판정·근거·착수 순서는 **`docs/decisions-2026-08-14-R31.md`** 가 정본이다 —
> **여기 옮겨 적지 않는다.**
>
> ### 남은 것은 **자료**와 **사업 판단** 둘뿐이다
>
> | 무엇 | 왜 코드로 못 닫는가 |
> |---|---|
> | `FR-204-AC3` COP 곡선 | 자료가 없다. 오면 YAML 만 채운다(`templates.py` 가 `"MockCopCurve"`) |
> | `NFR-104` 외부 근거 (`Q-4`·`Q-5`) | **가정하면 자기충족 검증이 된다.** 회신에만 열린다 |
> | `Q-14`~`Q-16` 회신 | R31 이 **가정값으로 등재**해 개발은 막히지 않는다. 회신 시 값만 교체 |
> | **`MC-1` 일정** | **결정이 아니라 「언제·누가」 한 줄 기입이다.** `docs/manual-checks.yaml` 의 `planned_at`·`planned_owner` 가 `null` 이고 **그것 하나가 Phase 1 인수를 「판정불가」로 잡고 있다** |
> | **`FR-205` 구조 셋 (Phase 판단)** | `FR-205-AC1` 을 구조별로 쪼개면 **조립되지 않은 셋이 Phase 1 미매핑으로 드러난다.** 그것을 Phase 2 로 옮기는 것은 **Phase 1 이 약속하는 범위를 줄이는 사업 판단**이므로 R31 이 대신 내리지 않았다 |
>
> # ▶ R32 에 할 일 — **번호 순서대로 한다. 결정은 이미 나 있다**
>
> 아래는 **코드로 닫을 수 있는 것만** 있다(사용자 몫은 위 표에 갈라 두었다).
> 조항별 실물 근거는 **`docs/clause-recheck-2026-08-14.md`**, 결정 근거는
> **`docs/decisions-2026-08-14-R31.md`** 가 정본이다.
>
> | # | 할 일 | 왜 이 순서인가 · 무엇을 조심할 것인가 |
> |---|---|---|
> | **1** | **`CaseResult.variants` 를 채우는 생산자를 실행 경로에 배선한다** (`FR-607-AC1`) | ⚠⚠ **R31 이 남긴 빈 자리다 — 정직하게 적는다.** R31 이 담을 자리(`CaseResult.variants`)와 **표시 층**(`core/report/variant_report.py`)을 만들었으나 **그 필드를 채우는 배포 코드가 0곳**이다(`grep -rn "variants=" core/ app/` → 0건, 채우는 것은 테스트뿐). 즉 **소비자는 있고 생산자가 없다** — R26 이 형태 하나로 모은 그 결함이며 `DV-12`(R27)·`DV-5`(R30)·`FR-205`(R31)에 이은 **넷째**다. 할 일: `run_cases`/`core/casegrid/e2e_runner.py` 가 `run_order()` 를 돌며 변형별로 파이프라인을 실행하고 지표를 `variants` 에 담는다. ★ **「예외가 안 났다」로는 확인할 수 없다** — 무지원과 지원안은 지원금이 다르므로 **변형별 NPV 가 서로 달라야** 한다. 그 단언이 없으면 같은 값을 두 번 담는 구현도 통과한다 |
> | **2** | **「단일계약+관리주체 경유」 조립기** (`FR-205-AC1`) | 값은 갖춰졌다(Q-6 단가 · **Q-14 수수료 — R31 신설**). 남은 둘: ⓐ `SelfConsumption` 의 두 요금(기존/신규)을 `TariffEngine.bill_scenario()` 가 내게 배선한다 — 그 두 요금은 요금엔진이 누진·TOU 를 풀어 내는 값이라 대장에도 협상값에도 없다 ⓑ ⚠ **수수료를 실을 자리가 없다.** `SelfConsumption` 에 차감항을 넣지 **말 것** — 수수료는 편익이 아니라 **비용**이고, 편익에서 빼면 관점별 NPV 에서 그 비용이 사라진다. `core/cba/proforma.py` 에 비용 행(`fee_row` 계열)을 신설한다 |
> | **3** | **「개별 세대 직접계약」 — 정산 대상을 정하고 조립한다** | **권고가 있다(R31 판단, 확정 전).** 조항이 *「가구가 구매자와 **직접** 계약」* 이므로 판매 주체가 가구다 → `SurplusSale` + 계약단가는 협상값(`SettlementInputs`) + **`payer_by_structure` 에 `RESIDENT`**. 배타 규칙표가 자가소비와 잉여판매를 유형 A 로 두므로 **하나를 골라야 하고**, 「직접계약」의 대상은 계통에 내보내는 전력이다. **spec 에 그 한 줄을 더하고**(§16.5) 조립기를 넣는다 |
> | **4** | **「집합 PPA」 — 새 편익 클래스** | 값은 갖춰졌다(**Q-15 — R31 신설**). ⚠ **`SurplusSale` 로 대신하지 말 것** — PPA 는 발전량 **전량** 일괄 판매인데 `SurplusSale` 은 역송분만 본다. 대신 쓰면 **자가소비분이 빠져 편익이 조용히 작아진다.** `core/valuestream/aggregated_ppa.py` 신설 + `FR-401-AC2` 에 대응 편익 조항 신설(§16.5) |
> | **5** | **`FR-205-AC1` 을 구조별 키로 쪼갠다** | ★ **1~4 를 먼저 하면 사용자 판단이 필요 없어진다.** 지금 쪼개면 미조립 구조 셋이 **Phase 1 Must-have 미매핑**으로 드러나고 그것을 Phase 2 로 옮기는 것은 범위를 줄이는 사업 판단이다(위 표). **2~4 가 끝나면 미조립이 「VPP 경유」 하나만 남고 그것은 이미 Phase 2 이므로**, 그때 쪼개면 미매핑이 생기지 않는다. 쪼개는 값: 추적표가 **구조별로** 검증 상태를 보이게 된다(지금은 일곱을 한 칸으로 세어 셋만 조립된 사실이 안 보인다) |
> | 6 | `LoadedSeeds` 를 제품 경로로 옮긴다 | **지금 하지 말 것.** 시드를 소비하는 제품 코드가 0곳이라 옮기면 **호출자 없는 제품 모듈**이 하나 생긴다. 소비자가 생길 때 함께 옮긴다 |
>
> **1번이 가장 값이 크고 가장 조용한 자리다** — 지금 상태로 `build_variant_table()`
> 을 실행 결과에 부르면 `ValidationError`(변형별 결과가 없습니다)가 난다. 즉
> **기계는 옳게 거부하지만 아무도 그것을 부르지 않는다.**
>
> ### 초기화된 세션이 첫 30분에 할 일 — 이 순서로
>
> 1. `git status -sb` (비어 있어야 한다) · `gh run list --limit 2`
>    (`tests`·`source-rules` 둘 다 `success`)
> 2-b. ⚠⚠ **로컬 초록불이 CI 초록불을 뜻하지 않는다 — 로컬은 3.13, CI 는 3.11 이다**
>    (`pyproject.toml` 의 `requires-python = ">=3.11"` 이 정본이므로 **3.11 이 기준**).
>    R31 이 실제로 걸렸다: `MappingProxyType({})` 를 dataclass 기본값으로 두었더니
>    **3.12+ 는 허용하고 3.11 은 `ValueError: mutable default … use default_factory`
>    로 거부**해 CI 수집이 통째로 죽었다(31건). **로컬 전건 초록불이었다.**
>    `ruff` 도 잡지 못한다(RUF008 의 가변 목록에 `mappingproxy` 가 없다).
>
>    **구조를 바꾸는 커밋 전에는 3.11 로 한 번 돌린다:**
>    ```bash
>    conda create -y -n py311 python=3.11
>    groups=$(python -c "import tomllib;print(','.join(tomllib.load(open('pyproject.toml','rb'))['project']['optional-dependencies']))")
>    ~/miniconda3/envs/py311/python.exe -m pip install -e ".[$groups]"
>    PYTHONUTF8=1 ~/miniconda3/envs/py311/python.exe -m pytest -q
>    ```
>    ⚠ **`[dev]` 만 깔면 `sqlalchemy`·`fastapi`·`argon2` 가 없어 16건이 수집 실패한다** —
>    그것은 저장소 결함이 아니라 **내 환경의 결손**이다. CI 는 `pyproject.toml` 의
>    엑스트라 **전부**를 읽어 설치하므로 위 `groups` 를 그대로 쓴다. R31 이 이
>    구별을 한 번 틀렸다(가짜 결함 16건을 실제 결함으로 볼 뻔했다)
>
> 2. **`PYTHONUTF8=1` 을 켜고 의존성이 설치된 인터프리터를 확인한다** —
>    `where lint-imports` · `python -c "import argon2, sqlalchemy"`.
>    저장소의 `.venv` 에는 의존성이 없고, 기본 `python` 도 아닐 수 있다
>    (§「도구」의 경고 둘 — 안 지키면 **로컬에서만 6건이 죽는다**)
> 3. `python scripts/check_precommit_installed.py` — **훅이 걸려 있는가**
>    (**R30 에 걸었다.** 새 클론·새 기계에서는 다시 걸어야 한다:
>    `pip install pre-commit; pre-commit install; pre-commit install-hooks`)
>    ⚠ **커밋하는 셸의 PATH 에 그 인터프리터를 먼저 둘 것** — `ruff` 훅이
>    `python -m ruff` 라 PATH 의 첫 `python` 을 잡는다. 아니면 커밋이 막힌다
>    (R30 실측). **훅이 옳게 막은 것이므로 `.pre-commit-config.yaml` 을
>    고치지 말 것** — 실행할 수 없을 때 통과시키지 않는 것이 이 저장소의 규칙이다
> 4. **`docs/decisions-2026-08-14-R31.md` 를 읽는다** — 아홉의 판정·근거와
>    **구현하면서 무엇이 달라졌는지**가 절마다 있다. 착수 순서표는 **전건 ✔** 이다
> 5. **`NOT_YET_ASSEMBLED`(`core/valuestream/settlement.py`)를 읽는다** — 남은
>    구조 넷이 각각 무엇에 막혀 있는지가 사유로 있다. **값이 아니다**
> 6. **결정을 다시 열지 말 것.** 판정마다 근거를 적어 두었으니, 뒤집으려면
>    **그 근거가 틀렸다는 것을 먼저 보여야 한다** — 근거를 읽지 않고 다시 정하면
>    같은 판단을 두 번 하고 두 답이 저장소에 남는다
> 7. ⚠ **인계가 물려준 진단은 쓰기 전에 실물로 확인할 것.** R31 이 결정 문서 자신에
>    틀린 진단 하나를 발견했다(`except KeyError` 8곳이 요금표와 무관했다)
>
> ⚠ **오케스트레이션은 R24 에 끝났다.** 아래 문단들이 `.orch/state.md` 를
> 정본으로 가리키는 것은 **이력**이며, 그 폴더의 판정 중 아직 유효한 것은
> `docs/clause-recheck-2026-08-14.md` 로 옮겼다.
>
> <sub>이하 R18 시점 문면 (이력)</sub>
>
> **08-11 R18 종료 시점 갱신.** 라운드 배분은 `.orch/state.md` 가 정본이고,
> 아래는 **산출물 관점의 잔여**다. `.orch/` 는 git 추적 밖이므로 **그 파일이
> 없으면 이 표가 유일한 출발점**이다 (아래 §「다음 세션 시작 시」).
>
> ### ★ R18 이 이 표의 「크기」를 고쳤다 — **`I` 32건이 아니라 19건이다**
>
> R15 분류표의 `I` 32건을 조항 실물로 대조하니 **13건이 서지 않았다**(허위·오귀속
> 8 · 근거 오류 3 · 이미 닫힘 2). 이 표가 R15 이래 1번과 「나머지 27건」으로 적어
> 온 값이 그 32 에서 나왔으므로 **둘 다 아래에서 고쳤다.**
>
> **`.orch/` 를 잃어도 이 문단이 남는다** — 재대조 도구는
> `.orch/verify_clause_citations.py` 이고 그것도 추적 밖이지만, **방법은 두 줄이다**:
> ① 인용 마커를 `docs/traceability.md` **와 spec 본문**에 모두 대조한다
> ② 표의 행번호를 `ast` 로 함수명으로 환원한다(**데코레이터를 함수 범위에 포함**).

| # | 할 일 | 크기 | 왜 이 순서인가 |
|---|---|---|---|
| **1** | **`I` 1건 — Phase 1 DoD 를 떠받치는 조항의 미구현** (R15 가 5건이라 한 것 중 **넷이 닫혔다**) | **데이터 공백이라 코드로 못 닫는다** | **남은 하나는 `FR-204-AC3` 뿐이고 그것은 자료 문제다** — `core/model/templates.py:65` 가 여전히 `"MockCopCurve"` 이며 **COP 곡선 자료가 오면 YAML 만 채운다.** 닫힌 넷: ~~`FR-402-AC2.A`~~ R17 배선 · ~~`FR-803-AC1`~~ R19 가 `core/report/charts/feasible_region.py` 로 구현(차트 7종) · ~~`NFR-303-M1` 계약~~ R16 · ~~**`FR-607-AC1`**~~ **R21 이 닫았다** — `is_baseline` 깃발이 사라졌고(살아 있는 코드에 참조 0건) 합성이 `core/casegrid/incentive_cases.py`(**위층**)에 놓였다. ⚠ 그 과정에서 **조항이 요구하는 import 방향을 `layers` 가 금지한다**는 것이 드러났다(`core.incentive` → `core.casegrid` 는 역방향) — R20 이 축 타입에서 만난 형태의 **세 번째**다. 근거: `.orch/R18-WP29A-재검증표.md` · `.orch/R21-WP31{B,F}-결과.md` |
| ~~**1-b**~~ | ~~**`NFR-303-M1` 나머지 — 대장 14규칙**~~ **✔ 닫혔다 (R31)** | — | **던진다 13 · 던질 순간 없음 1(`DV-11`) · 아직 0.** R21 이 「대장에 열넷을 적어 두고 셋만 던지고 있었다」고 쓴 자리가 **열 라운드 만에** 닫혔다 — R21 4 → R22 6 → R24 11 → **R31 13**(`DV-7` 실질/명목 선언 · `DV-6` 요금표 폴백). `NOT_YET_THROWN` 이 비었으나 **상수는 남겼다** — 열다섯 번째 규칙이 대장에 들어올 때 「아직」을 놓을 자리가 없으면 그 규칙이 `THROWN` 에 억지로 들어가거나 분류에서 빠진다. `tests/contract/test_dv_rule_enforcement.py` 가 계속 강제한다 |
| **2** | **`FR-205` 정산엔진 — ✔ 결정 5건 전건 확정 (R31). 남은 것은 구현 세 갈래** | 갈래 A 는 한 라운드 | 판정·근거는 `docs/decisions-2026-08-14-R31.md` §2. **조립기로 간다**(금액 하나로 뭉치면 `core/cba/` 와 같은 화폐 흐름을 두 번 계산하는 길이 열린다) · 근거 없는 넷은 **`assumptions.yaml` Q항목 등재** · **VPP 경유는 Phase 2 로 옮긴다**(`FR-401-AC2.VPPMarket` 이 이미 Phase 2 이고 `FR-205` 는 Phase 1 인 내적 불일치) · 구조↔프로파일 정합은 **배타 규칙표에 구조 축을 신설**해 이미 배선된 `assert_no_exclusions()` 가 거부한다. `payer` `ClassVar` 는 **R16 이 `payer_by_structure` 로 이미 닫았다.** ✔ **R31 이 구조 어휘의 사본을 없앴다** — `SettlementEngine` 이 자기 목록을 들고 일곱 중 셋이 계약과 달랐고, 그 어긋남은 **편익의 지불 주체를 조용히 기본값으로 떨어뜨린다.** 갈래: **A** 직접거래·상계(배선만) → **B** 잉여직거래·단일계약(Q항목 후) → **C** 개별세대직접계약·집합PPA |
| ~~**3**~~ | ~~**`T` 56건 — 테스트를 조항 문면까지 넓힌다**~~ **닫혔다 (R17)** | — | **R17 이 5레인 병렬로 전건 처분했고, 이 행은 그 뒤 갱신되지 않은 채 네 라운드를 남아 있었다** (R25 인수에서 발견 — 위 R17 행이 「전건 처분」이라 적고 있고 분포 합이 정확히 56이다). R17 이 그 과정에서 **분류표가 없는 조항 둘을 인용하고 있던 것**과 「검사가 아무것도 붙들지 않는」 자리 둘을 함께 찾았다. ⚠ **R15 분류표(`T`)는 `.orch` 산출물이며 지금 도구로 재보면 「없는 조항 인용」이 4행·「마커가 파일 어디에도 없음」이 2행이다** — 그 표를 다시 근거로 쓰려면 먼저 그 6행을 대조해야 한다 (`python .orch/verify_clause_citations.py --class T`) |
| ~~**4**~~ | ~~**`M` 15건 — 마커를 지운다**~~ **닫혔다 (R20 + R23)** | — | **15건 전건 처분.** 마커 삭제 11건(오케스트레이터 9 · `claude2` 2) + 「즉시」 4건은 R20 이 **유지 + 사유 기록**으로 두었다 — 각 조항의 유일 매핑인데 구현 자체가 없어 대체 검증을 만들 수 없었기 때문이다(지우면 `test_17_9_dod9` 가 빨간불). **R23 이 그 넷을 구현하고 마커를 옮겼다** — R20 이 마커 옆 코드에 적어 둔 **닫는 조건 문면 그대로**이며, 뗀 자리에는 왜 뗐는지를 남겼다: ~~`FR-1101-AC2`~~ `test_private_seed.py` · ~~`FR-201-AC1`~~ `test_composition.py`+라우터+화면 · ~~`FR-504-AC3`~~ `test_profile_editing.py`+라우터+화면 · ~~`FR-704-AC4`~~ `test_perspective_report.py`. 근거: `.orch/R20-WP30F-결과.md` · `.orch/R23-결과.md` |
| ~~**5**~~ | ~~**`N` 8건 — spec 개정**~~ **✔ 여섯이 닫혔다 (R31 · spec v0.15)** | — | `FR-301-AC5`·`AC6` · `NFR-207-AC3`·`AC4` · `FR-106-AC8` · `FR-101-AC5` 신설(수용기준 308 → 314). **전부 새 요구사항이 아니다** — 구현·검증은 v0.1 이래 있었고 조항만 없어 검사들이 이웃 조항을 빌려 인용했고 매핑표는 그것을 「그 조항이 검증됨」으로 셌다. 마커 15건을 옮기자 **자동 매핑 278 → 284** 이고 미매핑은 8건 그대로다. **나머지 둘**(`FR-1101-AC2` 존재분기 · `FR-704-AC4` 관점 병렬 표시)은 R23 이 구현으로 닫았으므로 조항 신설 대상이 아니었다. 판정은 `docs/decisions-2026-08-14-R31.md` §10 |
| **6** | **`MC-1` 수동검사 — 「언제 누가」를 정한다** | 사람 일정 | `docs/manual-checks.yaml` 의 `planned_at`·`planned_owner` 가 **비어 있다.** 이것 하나가 Phase 1 인수를 「판정불가」로 잡고 있고 **코드로 닫을 수 없다**(심의 경험자 3명 대상 설명 재현) |
| ~~**7**~~ | ~~**`FR-202` 소유 결정 → 작업 목록 인용**~~ **✔ 닫혔다 (R31)** | — | 작업 목록 **7.7 신설** — 7.0 에 두고 그 하위만 Wave 1 로 앞당긴다. 근거는 R15 권고문에 이미 있었다: **소유는 도메인이 정하고 Wave 는 Phase 가 정한다.** 그 결과 `check_task_mapping.py` 의 미인용 Must-have 가 **3 → 0건**이 되어 **CI 차단으로 승격했다**(경고로 둔 사유가 사라졌다) |
| 8 | `FR-205-AC1` 스텁 — 금액이 늘 `0.0` 인데 테스트가 금액을 안 본다 | 2번에 종속 | `core/model/settlement.py` |
| 9 | Q 회신 도착 시 대장 갱신 | 작다 | `docs/assumptions.yaml` 만 고치고 `check_assumptions.py` 를 돌린다. **Q-4·Q-5 회신은 유예된 판정 4건을 여는 유일한 열쇠**다 |
| **1-c** | **`I` 잔여 12건을 조항 실물로 재검증했다 — 「이미충족」 대부분이 절반이었다** | **판정 끝 · 구현이 남았다** | **`docs/clause-recheck-2026-08-14.md` 가 정본이다** (`.orch` 가 아니라 **추적되는 문서**에 두었다 — 오케스트레이션이 끝났으므로 그 폴더는 더 늘지 않는다). ★★★ **형태가 하나다: 함수 층은 지어져 있는데 실행 경로가 그것을 부르지 않는다** — ~~`FR-402-AC2.A`(배타 거부)~~ **R27 이 배선했다** · ~~`DV-5`(분석기간 상한)~~ **R30 이 배선했다** · `FR-607-AC1`(기준선 자동 포함) · `FR-1101-AC2`(시드 로더, 게다가 `tests/ci/` 안에 있다) **둘은 아직 부르는 배포 코드가 0곳**이고, 테스트는 그 함수를 직접 부르므로 초록불이다. **새 기능을 짓기 전에 남은 두 자리를 배선하는 것이 가장 값이 크다.** ⚠ 다만 `FR-607-AC1` 은 **지표 자료형 결정이 앞선다**(변형별 결과를 `CaseResult.metrics` 에 어떻게 담는가 — 표시할 리포트 층도 아직 없다), `FR-1101-AC2` 는 **시드 덮어쓰기 의미 결정**이 앞선다. 배선만으로 닫히는 것이 아니다. 그 밖: 항진(자기참조) 검사 3건 · 이름이 하는 일과 다른 검사 1건 · 조항의 150배 허용치 상수 1건(`tolerance=0.15` ↔ `NFR-104` 0.1%) · `FR-901-AC1` 세션 만료는 **서명에 시각이 없어 검사 자체가 불가능**(만료 로직을 지워도 테스트가 전부 통과한다) · `FR-204-AC1` 템플릿이 `Model` 로 **지어지지 않는다** · `"MockCopCurve"` 는 계약상 허용 타입이 아니라 호출하면 죽는다 |
| — | ~~**`I` 나머지 — R20 이 13행을 덮었다. 남은 것은 16행**~~ **위 1-c 로 대체됐다** | — | **R20 이 `regulation` 6 · `assumption` 5 · `ci` 2 를 구현했다** (표 29행 기준). 남은 16행 중 **덮이지 않은 12행**을 `.orch/R20-WP30E2-재산정표.md` 가 3분류로 적었으나 ⚠ **그 표의 「있는 것」 칸은 근거로 쓸 수 없다** — `grep` 키워드가 걸린 첫 줄을 구현으로 적은 행이 넷 확인됐다(파일 머리에 낙인). **분류는 후보로만 쓰고 조항 실물과 대조한 뒤 파견할 것.** 세는 방법: `python .orch/verify_clause_citations.py --class I` |
| — | 미매핑 8건 · `FR-202-AC4` | — | 전부 Phase 2·3. **지금 할 것이 아니다** |
| — | 판정 불가 3건 (17.4 선행실증 · 무료호스팅 · Litestream) | — | **환경이 없다.** 작업이 남은 것이 아니다 |

> ### `FR-1004-AC1` 차트 — **R16 정정: 「3종뿐」이 아니라 0종이었다**
>
> R14·R15 는 `charts.py` 에 *「`cashflow_line`·`cost_benefit_pie`·`tornado`
> 셋뿐」* 이라 적었다. **파일 전체가 10줄이었고 그 「3종」은 영어 문자열
> 상수였다** — `matplotlib` 을 import 하지도 않으면서 독스트링이 그것을 쓴다고
> 적었고, 검증하던 테스트는 **dict 에 키가 있는지**만 보았다. 그 위에
> `FR-1001`~`FR-1005`(전부 Must-have·Phase 1)가 얹혀 있었다.
>
> **R16 이 셋을 실제 PNG 렌더로 만들고 레지스트리를 세웠다.** 남은 것은
> `FR-1004-AC1` 이 나열한 여섯 중 **셋**(일간 디스패치 스택 · 월별 에너지 수지 ·
> 모델 비교 바)과 `FR-803-AC1` 등고선+음영이며, **각각 파일 하나**다 —
> `core/report/charts/<tag>.py` 를 놓으면 등록된다. 공유 파일을 고치지 않는다.

> ### 게이트가 있는데 아무도 부르지 않던 문제는 R15 에 닫혔다
>
> `check_task_mapping.py` 는 **검사기 열 중 CI 가 부르지 않는 유일한 것**이었다.
> R15 에 `task-mapping` 잡으로 붙였다 — **검사는 경고, 감지 능력 확인(음성
> 20종)은 차단**이다. 남은 미인용 3건이 **사용자 결정 대기**라 차단으로 켜지
> 않았다: 결정 전에 차단하면 CI 가 영구히 빨간불이 되고, **그런 검사는 곧
> 무시된다.**

<details>
<summary>08-09 시점의 할 일 (이력 — 전부 지나갔다)</summary>

| # | 할 일 | 왜 지금인가 |
|---|---|---|
| **1** | **R1 — Wave 1 3구획 병렬** | `agy`/WP-2 전제(4.1~4.8, **SC-7**) · `codex`/WP-3 제도(5.1~5.7) · `opencode`/WP-13 영속성(3.1~3.7). 계약 v1.2가 요금 신호와 대장 읽기 경로를 세웠으므로 **세 구획이 서로를 기다리지 않는다**(§16.1 W-6) |
| 2 | R1b — 2.2 공개/비공개 분리, 2.14 라이선스(**SC-6**) | `FR-1101-AC2`~`AC4`. DoD 가 *"비공개 시드 없이 클론·실행·CI 통과"* 인데 **지금 그것을 확인한 적이 없다.** 2.3이 «유입을 막는» 쪽을 세웠으니 다음은 «없어도 도는가» 다 |
| 3 | **R2 착수 시 — 자원 요금 인자 3건 이관** | `KNOWN_TARIFF_DEBT` 참조. `avoided_price` 는 ctx 가 아니라 **WP-4** 로 간다(편익 화폐화). 8.0이 편익 소유권을 확정할 때 함께 정리한다 |
| 4 | **R2·R3 — `SC-1`·`SC-2`·`SC-4` 미매핑 3건** | 08-08에 §9 를 추적표에 넣자 보안 조항 8건이 들어왔고 **셋만 매핑되어 있었다.** 인증·인가·감사 로그이며 담당은 14.1·14.3·14.9(WP-11)다 |
| 5 | Q 회신 도착 시 대장 갱신 | 08-08 발송 |

</details>

> **`FR-102-AC1` 잔여 자원 3종(`VPP`·`Boiler`·`Genset`)을 목록에서 뺐다 — Phase 1 범위가 아니다.**
>
> 08-08 오전판 status.md 는 이것을 1번으로 적었으나, spec 을 직접 읽으니 **세 곳이 일치해서 반대로 말한다** —
>
> | 근거 | 내용 |
> |---|---|
> | `FR-102-AC1` 본문 | `.VPP`·`.Boiler` 줄 끝 **`[Phase 2]`**, `.Genset` **`[Phase 3]`** |
> | §12 Phase 2 목록 | *"VPP·Boiler 자원, DR·VPP 시장 편익 — FR-102, FR-401"* |
> | §13.2 말미 | *"Phase 2 자원(`VPP`,`Boiler`)과 Phase 3 자원(`Genset`)은 **해당 Phase 착수 시** 동일 규격으로 케이스 세트를 작성한다"* |
>
> Phase 1 DoD 8 도 *"**자원 6종** 각각이 §13.2 검증 케이스 전건 통과"* 이고, **작업 목록에 이 셋을 담당하는 작업이 없다**(전수 검색 확인). `check_task_mapping.py` 는 `Must-have and Phase in ("1","-")` 로 거르므로 셋은 이미 Phase 1 게이트 밖이며, 그래서 「미인용 Must-have 0건」이 성립한다.
>
> **미매핑에 이 셋이 들어 있는 것은 맞지만, 그것이 지금 해야 할 이유는 아니다.** (당시 221건, **08-10 R14 실측 8건**) 생성기는 Phase 무관하게 Must-have 미매핑을 세고, 17.9(미매핑 0건)는 **Phase 1 DoD** 다. 지금 쓰면 케이스 세트 규격이 없는 채로 자원이 들어오고, 그것을 NFR-106 이 막는다(*"케이스 세트 없이는 자원 등록이 병합되지 않는다"*).
>
> **`VPP` 에 대한 계약 판단은 유효하고, 미해결에 남겨 두었다** — 자원이 다른 자원을 품을 때 `dispatch()` 순회와 비용 이중 계상(FR-402-AC2.B)을 계약이 견디는지는 Phase 2 착수 전에 답해야 한다.

> **계약을 고칠 때 알아야 할 것 — §16.2 절차는 네 단계다.** ① 이슈 제기 → ② **WP-0이 계약·계약테스트를 한 PR로** 갱신 → ③ 영향 구획 통지 → ④ 각 구획이 갱신된 계약 테스트에 맞춘다. 08-08 개정은 ②를 커밋 `dd13817` 로, ④를 같은 커밋에서 함께 수행했다 — 단독 작업이라 ③이 자기 자신에 대한 통지였기 때문이다. **여러 사람이 붙을 때는 ②만 먼저 내보내고 ④를 구획별로 나눈다.** 그때 ②만 있는 상태는 정상적으로 빨간불이며, 그것이 «어느 구획이 아직 맞추지 않았는가» 를 보여 준다.

> **Wave 0가 끝났다.**
>
> | Wave 0 관문 | 상태 |
> |---|---|
> | 정본 대조 5종 + lock | ✔ |
> | 매핑표 생성기 (NFR-107) | ✔ |
> | 작업 목록 인용 검사 | ✔ |
> | 표 수용기준 전개 (2.15) | ✔ 08-08 |
> | 잠정 가정 대장 (16.1) | ✔ 08-08 |
> | CI 매핑 게이트 (2.7) | ✔ 08-08 — 경고에서 차단으로 |
> | **계약 고정 (1.0)** | **✔ 08-08 — 스모크 통과가 종료 조건이었다** |

> **자원 추가는 이제 파일 하나다 (`NFR-207`).** `core/der/<tag>.py` 를 만들고 `DER` 을 상속하면 `discover(core.der, DER)` 가 자동으로 찾는다. **`core/der/__init__.py` 를 비롯한 어떤 공유 파일도 고치지 않는다** — 고쳐야 하는 구조라면 6명이 같은 줄을 편집하게 되어 구획 격리가 선언만 남는다(§16.1 W-3). 「파일을 놓으면 실제로 레지스트리가 는다」를 검증 케이스가 확인한다.
>
> 구상이지만 자원 «유형» 이 아닌 공유 기반 클래스는 `REGISTRY_ABSTRACT = True` 를 **자기 클래스에** 선언한다 — 상속되면 그것을 상속한 진짜 유형까지 등록에서 빠진다.
>
> **자원을 쓸 때 알아야 할 것 네 가지.**
>
> ① **계약 테스트는 상속으로 따라온다.** `class TestPVContract(DERContractTests)` 에 `make()` 하나만 채우면 속성 11종·메서드 7종·매체 플래그·부분 창·편익 훅·운전 방법·물가상승률·잔존가치 규약이 **17건**으로 전부 검사된다 (v1.1 개정으로 11 → 17). 자원마다 손으로 쓰면 반드시 빠지고, NFR-106(레지스트리 순회 누락 검사)이 성립하지 않는다.
>
> ② **`tag` 는 spec `FR-102-AC1.<키>` 와 같은 리터럴이다.** `PV` 를 `pv` 로 낮추지 않는다 — 슬러그화·대소문자 변환도 파생이며, 파생하는 순간 조항 ID와 어긋난다.
>
> ③ **`dispatch()` 의 `ctx.steps` 는 「연초부터 연속 `steps` 스텝」이다.** 대표일 반복도, 연간 총량을 창 길이에 몰아 담는 것도 금지다. 연간 총량을 부분 창에 실을 때는 **`ctx.year_fraction`** 을 곱한다 — `/365`·`/8760` 을 직접 쓰면 15분 해상도에서 4배 어긋나고, 그 어긋남은 부분 창에서만 나타나 8760 골든 시나리오로는 잡히지 않는다. 진입부에서 **`self.check_context(ctx)`** 를 호출한다.
>
> ④ **비율은 전부 소수(0~1)다.** `escalation_rate=0.02` 가 2%다. `%` 를 받는 인자를 새로 만들지 말 것 — v1.0 에서 ESS 하나가 `inflation_pct` 를 두었고, 같은 「2%」가 다른 자원의 `0.02` 와 **100배 어긋난 채 어느 쪽도 오류가 아니었다.** 물가 계수는 `escalation_factor(year=...)` 하나만 쓴다(1년차 = 1.0).

> **게이트가 무엇을 판정하는지 오해하지 말 것 — 「미매핑 0건」이 아니다.**
>
> 판정 대상은 ⓐ spec 수용기준 선언 결함 0 ⓑ 커밋된 매핑표가 지금 spec과 일치 ⓒ 게이트 자신이 수동 대장으로 자기충족되지 않음 ⓓ 수동 스텁에 수행 기록 존재. **미매핑 0건은 17.9(Phase 1 DoD)** 이며 Wave 0 종료 기준선은 261건이었다(당시 222건 → **08-10 R14 실측 8건, Phase 1 분은 0건**). 지금 절대 0을 요구하면 CI가 영구히 빨간불이 되고, 통과시키려고 §13.0.2가 금지한 자기충족 테스트를 짓게 된다 — 게이트가 막으려던 그 상태다.
>
> **미매핑이 늘면 ⓑ가 잡는다.** 표가 달라지고 갱신본을 커밋하는 순간 증가가 diff에 남는다. 별도 기준선 파일을 두지 않은 이유다.
>
> **테스트를 쓸 때 지킬 것 두 가지** — ① `@pytest.mark.manual` 스텁은 `manual-checks.yaml`에 함께 등재한다(없으면 종료 코드 2). ② `manual-checks.yaml`에 `NFR-105`~`NFR-107`을 **등재하지 않는다** — 수동 예외 경로를 정당화하는 조항 자신이 그 경로로 "검증됨" 처리되면 아무것도 검증되지 않은 채 초록불이 뜬다. 둘 다 이제 도구가 막는다.

---

## 산출물 현황

### 문서

| 파일 | 버전 | 상태 |
|---|---|---|
| `rslt/spec-분산특구-경제성평가.md` | **v0.14** | 요구사항 정본. FR/NFR/UI 번호의 유일한 출처. v0.14 = `FR-104-AC3` 의 **`retire` 의미 다섯** 명문화 + 머리말 판본 이중 소유 제거 |
| `rslt/task-분산특구-경제성평가.md` | **v2.6** | 상위 17건 / 하위 **157**건 (R17 에 **기계로 세어 통일**). **v2.4→v2.5→v2.6 의 경위가 이 파일의 교훈이다** — R15 가 `9.8` 을 더하고 157 을 그대로 둬 1건 어긋났고, R16 이 그것을 **세지 않고 `+1`(158)** 로 고쳐 오히려 2건 어긋났다. 세는 규칙을 §작성 규칙에 박아 넣은 것이 R17 이다 |
| `rslt/spec 작성 시 원칙.md` | 1.0 | spec 작성·개정 규칙 |
| `docs/domain-rules.md` | — | 정본 사본 (바이트 동일) |
| `docs/evidence-standard.md` | — | 정본 사본 (바이트 동일) |
| `docs/README.md` | — | 사본 성격·개정 절차 |
| `docs/privacy-procedure.md` | — | 개인정보 처리 절차 (`SC-7`) |
| `docs/source-rules.lock` | 1 | **정본 판본의 단일 소유자** — spec 머리말은 v0.14 에서 이 값을 버렸다 (§16.1 W-4) |
| `docs/traceability.md` | 자동 생성 | 요구사항 **105** / 수용기준 **308** / 자동 **278** |
| `docs/manual-checks.yaml` | 1 | 수동 검증 대장 `MC-1`~`MC-8`. **8건 전부 미수행**이고 `MC-1` 만 `blocking_dod: true` 다 |
| `docs/assumptions.yaml` | 1 | **잠정 가정 대장 + 확보 현황판** — Q-1~Q-13 + 확정, **21항목** |

### 도구 (전부 동작 확인됨)

```
scripts/check_source_rules.py    정본 대조 5종
scripts/gen_traceability.py      수용기준 매핑표 생성 (NFR-107)
scripts/_specparse.py            spec 파서 — 요구사항·수용기준·§9 보안 조항
scripts/check_task_mapping.py    작업 목록 인용 검사
scripts/check_assumptions.py     잠정 가정 대장 검사 7종
scripts/check_file_size.py       NFR-206 파일 규모 (500줄) — 저장소 전체, 경고
scripts/check_test_accompaniment.py  NFR-105 게이트 ② 테스트 동반 (2.6)
scripts/check_coverage_inputs.py     NFR-105 게이트 ① 입력 검증 (2.5)
scripts/_gitdiff.py              게이트 ①② 공용 — 기준 ref 해석 (단독 소유)
scripts/vault_watch.ps1          정본 표류 상시 감시 + 작업 스케줄러 등록 (2.16)
scripts/check_hardcoded_params.py    NFR-202 전제값 복제 · NFR-205 전역 상태 (2.9)
scripts/check_disclosure.py      SC-3·SC-8 비공개 유입 차단 (2.3)
scripts/check_marker_substance.py    마커 실질 검사 (조항 마커가 무언가를 검증하는가)
scripts/check_partition_assignment.py  구획 배정 검사 (미배정·중복)
scripts/negtest_traceability.py  생성기 음성 12종
scripts/negtest_assumptions.py   가정 대장 음성 19종
scripts/negtest_file_size.py     파일 규모 음성 6 + 양성 2종
scripts/negtest_hardcoded_params.py  음성 18종
scripts/negtest_disclosure.py    음성 17종
scripts/negtest_marker_substance.py  마커 실질 음성 11종
scripts/negtest_partition_assignment.py  구획 배정 음성 2종
scripts/negtest_source_rules.py  **정본 대조 음성 25종 (R14 신설)**
scripts/negtest_task_mapping.py  **작업 목록 인용 음성 20종 (R15 신설)**
.pre-commit-config.yaml          gitleaks + 비공개 유입 + ruff (커밋 전)
.github/workflows/source-rules.yml   CI — 정본 대조(경고) + 매핑 게이트(차단)
                                     + 가정 대장(차단) + 파일 규모(경고)
                                     + 마커 실질(차단) + 구획 배정(차단)
                                     + **정본 대조 음성(차단, R14 신설)**
                                     + **작업 목록 인용(경고) + 그 음성(차단, R15 신설)**
.github/workflows/tests.yml          CI — ruff + lint-imports(차단) + pytest
                                     + 커버리지 하한 85%(차단)
                                     + 게이트 ①②(PR 한정, 차단)
tests/ci/test_ci_gates.py            NFR-105·NFR-203·NFR-204 게이트 검증
tests/ci/test_import_boundaries.py   NFR-208 음성 3 + 양성 2
tests/ci/test_license.py             라이선스 (SC-6)
tests/ci/test_marker_substance.py    마커 실질 게이트
tests/ci/test_performance_and_golden.py  성능·골든 시나리오
tests/ci/test_phase1_measurements.py     Phase 1 계측 항목
tests/ci/test_seed_fallback.py           비공개 시드 없이 도는가
tests/ci/test_traceability_gate.py       매핑 게이트 자기검사
```

> ### 오케스트레이션 도구는 `.orch/` 에 있고 **추적 대상이 아니다**
>
> lint·mypy 대상이 아니며(`ruff` = `core tests scripts`, `mypy` = `core`),
> CI 도 부르지 않는다. **그래서 여기 적어 둔다** — 적지 않으면 존재가
> 세션 사이에 사라진다.
>
> ```
> .orch/dispatch.py           지시 전송 + 착수 확인 (입력창이 비워졌는가)
> .orch/watchdog.py           레인 감시 — 완료 보고·죽음·무출력·스크롤백 축소
> .orch/negtest_watchdog.py   감시기 음성·양성 **56종** (R14 31→50 · R15 50→56)
> .orch/negtest_dispatch.py   **지시 전송 음성 17종 (R14 신설)** — 실전 진입점
>                             (`main()`)이 완료패턴을 넘기는가를 붙든다
> .orch/watch.tsv             감시 대상 — 이름·핸들·완료패턴 (+ 겪은 함정 전부)
> ```
>
> **`negtest_dispatch.py` 가 신설된 경위가 이 저장소의 반복 유형이다.**
> R13 이 감시기 오탐을 고치고 음성 26종으로 감쌌는데, **`main()` 이 완료패턴을
> 넘기지 않아 그 수정이 실전에서 한 번도 발동하지 않았다.** 음성 26종은
> 필드를 손으로 만들어 넣어 **실전 진입점을 지나지 않았다.** 그래서 새
> 음성은 `main()` 을 직접 부르고 **무엇이 넘어왔는지**를 본다.

> **`tests.yml` 이전에는 CI 에 pytest·ruff·import-linter 잡이 없었다.** 세 검사는 사람이 로컬에서 부를 때만 돌았고, 그것은 「검사가 있다」가 아니라 **「검사를 기억하면 돈다」** 다. 자원 6종이 병렬로 들어온 직후가 특히 위험했다. 별도 파일로 둔 것은 §16.4가 `.github/workflows/` 를 **잡 단위 파일 분할**로 규정하기 때문이며, `source-rules.yml` 의 경로 필터에는 `core/**` 가 없어 코드만 고친 push 는 그 워크플로를 깨우지도 않는다.
>
> **게이트 ①②는 PR 에서만 판정한다** — 기준 브랜치가 있어야 «변경분» 이 정의된다. push 에서는 조용히 건너뛰지 않고 **「미수행」이라고 요약에 적는다.** 로그에 아무것도 없으면 «통과» 와 «수행하지 않음» 이 구별되지 않는다.

> **`check_file_size.py` 를 저장소 전체로 만든 이유.** 08-08 이전에 이 검사를 쓴 것은 WP-1e 하나뿐이었고 **자기 파일 두 개만** 보았다. 나머지 자원 넷은 감시 밖이었고, 도구 파일 2건은 그 사각지대에서 **811줄·571줄까지 자라 있었다.** 자원마다 손으로 쓰는 검사는 반드시 빠진다 — 계약 테스트를 상속으로 따라오게 만든 것과 같은 이유다.
>
> **차단이 아니라 경고다.** NFR-206은 Should-have이고 측정 수단이 `M1 lint 경고`로 규정되어 있다. 차단으로 켜면 조항이 요구하지 않은 강제력을 갖고, 통과시키려고 **근거 주석을 지우는 압력**이 생긴다 — 조항의 근거(DER-VET `Params.py` 1,830줄)는 코드 스프롤을 막으려는 것이지 설명을 줄이려는 것이 아니다. 파일 분할을 마친 뒤 회귀를 막을 때 `--strict` 로 켠다.
>
> 테스트는 대상에서 뺐다. 검증 케이스는 자원 하나에 수십 건이 붙으므로 같은 상한을 걸면 케이스를 쪼개는 압력이 되고, 그것은 NFR-106(자원별 케이스 의무)과 정면으로 부딪친다.

> **음성 테스트를 저장소에 넣었다.** 08-02·08-08에도 음성 테스트를 돌렸지만 임시 실행이라 기록만 남고 재현되지 않았다. 게이트를 차단으로 켠 이상 게이트 자신이 무언가를 검사한다는 증거가 CI에 상주해야 한다 (§13.0.1 ④).

### 현재 검사 결과 — **2026-08-10 R15 종료 실측**

**베끼지 말고 다시 돌릴 것.** 명령을 함께 적어 둔 이유가 그것이다. 08-09 →
08-10 사이에 이 절이 5라운드 뒤처졌고, 그동안 **여기 적힌 수치가 근거로
쓰였다.**

```
테스트           pytest 998 passed / 3 skipped / 1 xfailed                → rc=0
                   python -m pytest -p no:cacheprovider --no-cov
                   (`-q` 를 더하지 말 것 — addopts 에 이미 있어 요약줄이 사라진다)
커버리지         core 전체 91% (하한 85%, NFR-203)                        → rc=0
mypy strict      core 76파일 전건 통과 (R15 에 tariff_loader 추가)         → rc=0
린트             ruff — core·tests·scripts 전건 통과                      → rc=0
구획 경계        import-linter 4계약 kept · 0 broken (`lint-imports`)      → rc=0

정본 대조        통과 (해시·사본·앵커·인용 정합·폐지 좌표)                 → rc=0
매핑표           요구사항 105 / 수용기준 308 / 자동 278 / 수동 8 / 스텁 4
                 Phase 미지정 9 · **Must-have 미매핑 8**(전부 Phase 2·3)
                 **MC-1(FR-1001-AC5) 미수행 · blocking_dod → 판정불가 1건** → rc=1
가정 대장        21건 · 유예 판정 4건 (17.4 · SG-4 · 16.5 실무정합 · §13.3) → rc=0
파일 규모        총 줄 수 초과 7건 (경고) · **코드 줄 수 초과 0건**        → rc=0
파라미터         NFR-202 · NFR-205 위반 0건                               → rc=0
비공개 유입      SC-3 · SC-8 위반 0건                                     → rc=0
마커 실질        위반 0건                                                 → rc=0
구획 배정        미배정 0 · 중복 0                                        → rc=0
작업 목록 인용    미인용 Must-have **3건**(`FR-202-AC1`~`AC3`, 사용자 결정 대기)
                 형식 이탈 2(전건 서술 — 정당) · 중복 27(오귀속 의심)      → rc=1 (CI 경고)

음성·양성 (scripts/)
                 traceability 12 · assumptions 19 · file_size 8
                 hardcoded_params 18 · disclosure 17 · marker_substance 11
                 partition_assignment 2 · source_rules 25
                 **task_mapping 20 (R15 신설)**                          → 전건 rc=0
음성·양성 (.orch/, CI 밖)
                 watchdog **56** (R14 31→50 · R15 50→56) · dispatch **17** → 전건 rc=0
```

> ### ⚠ `check_task_mapping.py` — **R15 에 CI 로 들어왔고 4 → 3건이 됐다**
>
> ```
> python scripts/check_task_mapping.py     → rc=1   (CI 에서 경고)
>   ✗ 미인용 Must-have 수용기준 3건
>       FR-202-AC1 · FR-202-AC2 · FR-202-AC3   ← 사용자 결정 대기 (소유·Wave)
>   · 형식 이탈 인용 의심 2건 (전건 서술 — 정당)
>   · 상위 작업 간 중복 인용 27건 (오귀속 의심 — 정당할 수도 있다)
>   · 의심 3건 — `--review` 로 인용 쌍을 대조
> python scripts/negtest_task_mapping.py   → rc=0, 20종  (CI 에서 차단)
> ```
>
> **`FR-301-AC4` 는 R15 에 작업 목록 `9.8` 로 채워 4 → 3건이 됐다.** 구현이
> 없던 것이 아니라 인용이 빠져 있었다 — R11 이 찾은 `grid_limit` 거부 검사에
> v0.14 가 조항을 준 것이고, 검증은 `tests/der/test_ess.py` 에 이미 있었다.
>
> **08-09 에는 「미인용 Must-have 0건」이었다.** spec 개정 둘(v0.13 분할 ·
> v0.14 신설)이 수용기준을 늘렸고 **작업 목록이 따라오지 않았다.**
>
> **R15 이전에는 CI 의 매핑 게이트가 `gen_traceability.py`(매핑표 최신성)만
> 돌리고 이 검사는 부르지 않았다.** 그래서 `rc=1` 이 로컬에서만 보이고 커밋은
> 초록불로 지나갔다. **R15 에 `task-mapping` 잡으로 붙였다** — 검사는 경고,
> **감지 능력 확인(음성 20종)은 차단**이다.
>
> `FR-301-AC4` 는 작업 목록 `9.8` 로 채워 4 → **3건**이 됐다. 남은 3건은
> `FR-202-AC1`~`AC3` 이고 **어느 상위 작업이 갖는가**가 Wave 순서와 부딪쳐
> **사용자 결정 대기**다. 그래서 검사를 차단으로 켜지 않았다 — 결정 전에
> 차단하면 CI 가 영구히 빨간불이 되고, **그런 검사는 곧 무시된다.**

> **매핑표의 `rc=1` 은 결함이 아니다** — 미매핑이 남아 있다는 뜻이며 Phase 1이
> 진행 중인 지금은 정상이다. 결함(`rc=2`)이면 매핑표를 아예 만들지 않는다.
> **다만 R13 부터 `rc=1` 의 뜻이 하나 늘었다** — 차단 수동검사가 미수행이면
> 그 Phase DoD 항목을 **「판정불가」**로 세고 건수를 항상 출력한다.

<details>
<summary>08-09 시점의 검사 결과 (이력 — 5라운드 뒤처진 값이다)</summary>

```
정본 대조        통과 (해시·사본·앵커·인용 정합·폐지 좌표) — 볼트 지정 실행     → 종료 코드 0
매핑표           요구사항 **105** / 수용기준 **307** / 자동 52 / 수동 8 / 스텁 0
                 Phase 미지정 9 · Must-have 미매핑 219                           → 종료 코드 1
음성·양성        생성기 8 · 대장 19 · 파일규모 8 · 파라미터 16 · 유출 17 통과   → 종료 코드 0
파라미터         NFR-202 차단 0 / 경고 1(3600 값 충돌) / 판정 안 함 17건
                 NFR-205 전역 가변 상태 0건 (`_MODE_WINDOWS` 1건 수정)          → 종료 코드 0
테스트           pytest 582건 통과 (계약 60 · CI 게이트 34 · 자원 7종 · 스모크) → 종료 코드 0
커버리지         core 전체 **90%** (하한 85%, NFR-203)                        → 종료 코드 0
구획 경계        import-linter 4계약 kept — `lint-imports` 로 실행
                 음성 3종(AC1·AC2·AC3) 각각 BROKEN 확인 + 양성 1종              → 종료 코드 0
린트             ruff — core·tests·scripts 전건 통과                             → 종료 코드 0
mypy strict      core 29파일 전건 통과 (08-08에 밀린 5건 상환)                   → 종료 코드 0
인용 검사        형식 이탈 2(전건 서술, 정당) · 범위 초과 0 · 미인용 Must-have 0
                 · 실재하지 않는 인용 0 · 중복 20(전건 정당)                     → 종료 코드 0
가정 대장        20건 (가정 13 / 비활성 4 / 불가 2 / 확정 1) · **공개 등급 18건**
                 유예 판정 4건 (17.4 · SG-4 · 16.5 실무정합 · §13.3)            → 종료 코드 0
파일 규모        총 줄 수 초과 6건 (경고) · **코드 줄 수 초과 0건** (차단 통과)
                 최대 코드 482 (gen_traceability) — 코드 스프롤 없음            → 종료 코드 0
```

</details>

> **총 줄 수 초과 6건은 전건 「설명 밀도」다 — 코드 줄 수로는 하나도 상한을 넘지 않는다.**
>
> | 파일 | 총 | 코드 |
> |---|---:|---:|
> | `scripts/gen_traceability.py` | 811 | 482 |
> | `scripts/check_source_rules.py` | 571 | 342 |
> | `core/der/heatpump.py` | 550 | 344 |
> | `core/der/ess.py` | 545 | 339 |
> | `core/der/ev_v2g.py` | 513 | 342 |
> | `core/der/pv.py` | 513 | 280 |
>
> NFR-206의 근거는 **DER-VET `Params.py` 1,830줄 유지보수 실패**이고 그것은 코드 스프롤이다. 코드 기준으로 상한을 넘는 파일이 없으므로 `--code-strict` 를 **CI 차단으로 켰다** — 지금 초록불이고, 이후 코드가 상한을 넘는 순간 그때 잡힌다.
>
> **상한값과 판정 기준은 바꾸지 않았다.** NFR-206 문면은 「500줄」이며 그것을 코드 기준으로 재정의하는 것은 spec 개정이고 §16.5 절차가 필요하다 — 검사를 통과시키려고 규칙을 고치는 것은 이 저장소가 반복해서 경계해 온 유형이다. 여기서 한 것은 **판단에 필요한 정보를 덧붙인 것**이다.

> **매핑표의 종료 코드 1은 결함이 아니다** — 미매핑이 남아 있다는 뜻이며 Phase 1이 진행 중인 지금은 정상이다. 결함(종료 코드 2)이면 매핑표를 아예 만들지 않는다.
>
> 미매핑 목록이 곧 **Phase 1 테스트 작업 목록**이다. Wave 0 종료 기준선은 **261건**이고 1.0 계약 고정으로 254, 자원 6종으로 227, 1.11 계약 개정으로 225, 6.7 플러그인 자동 등록으로 **222건**이 됐다. 표 전개로 214 → 261로 늘었을 때 **늘어난 47건은 전에도 검증되지 않았고 다만 보이지 않았을 뿐이다.**
>
> **현재 값은 `docs/traceability.md` 요약을 보라** — status.md에 적으면 테스트가 늘 때마다 두 곳이 어긋난다.
>
> 중복 인용 11 → 18건도 같은 성질이다. `FR-102-AC1` 1건이 `.PV`~`.HeatPump` 4건으로, `FR-401-AC2` 1건이 5건으로 펼쳐졌을 뿐 새 중복이 생긴 것이 아니다. 7.5(템플릿 4종)·6.0.S3(자원 단독 편익 5종)이 소유자보다 **좁게** 인용하도록 의도한 결과다.
>
> Phase 미지정 9건은 전건 Should-have다(`FR-203, FR-303, FR-503, FR-609, FR-804, FR-805, FR-903, FR-904, FR-1102`). 부록 A.1은 Must-have만 등재하므로 그 표의 「미배정 0건」과 모순이 아니다. 게이트 판정에 영향 없음.

---

## 08-08에 한 일

### 16. §9 보안 조항 8건이 추적표 밖에 있었다 (spec v0.11)

2.3에서 `@pytest.mark.req("SC-8")` 를 달자 생성기가 **«실재하지 않는 인용»** 으로 잡았다. 검사기가 옳았다 — FR·NFR·UI 는 `- **FR-101**` 형식이라 파서가 읽지만 **SC 는 표 행이라 아무것도 읽지 않았다.**

> **v0.7이 UI·NFR 34건에서 만난 것과 같은 상태다** — *"미매핑 게이트가 Must-have만 훑으므로 34건이 통째로 감시 밖에 있었다. 표기 누락 이상의 문제다."* 여기서는 **보안 조항 8건**이 그랬다. 17.9(미매핑 0건)가 이들을 세지 않으므로 **「전건 매핑」이 성립해도 인증·인가·감사 로그는 검증되지 않은 채 남는다.**

§9 표에 우선순위·Phase 열을 더하고(전건 Must-have / Phase 1 — FR-901·902·1005 가 Phase 1 이므로 §4.0 R-3 이 성립한다) 생성기가 표를 읽게 했다.

> **수용기준 ID 를 `SC-N` 그대로 쓴다.** `SC-N-AC1` 로 만들면 기존 인용과 마커가 한꺼번에 무효가 된다. 2.15가 동결한 규약이 *"키는 저자가 1회 부여하고 동결하는 리터럴"* 이고 **`SC-8` 은 이미 그 리터럴**이다 — 여기서 파생시키면 규약을 우리가 깬다. 한 행이 곧 하나의 규범 문장이므로 요구사항과 수용기준이 1:1 인 것도 사실에 맞다.

**요구사항 97 → 105 · 수용기준 299 → 307 · 미매핑 214 → 219.** 늘어난 것이 성과다 — 전에도 검증되지 않았고 다만 보이지 않았을 뿐이다.

> **SC-7 하나가 「담당은 있는데 인용이 없는」 상태로 드러났다.** 부기 7종의 `source`·`verified_at`·`applicable_scope` 가 정확히 SC-7 이 요구하는 것인데 **아무 작업도 그렇게 말하지 않고 있었다.** 4.1에 인용을 붙였다. 함께 드러난 것: **`이용조건`은 부기 7종 어디에도 없다** — 공개 단가표에 붙는 재배포·표기 의무는 SC-8 민감도 등급과 다른 축이며, 4.5 착수 시 판단한다.

> **가리기(redaction)에 실제 결함이 있었고 `SC-3` 검증 케이스가 잡았다.** 한 줄에 식별정보가 셋이면 발견이 세 건 나오는데 **각 발견이 나머지 둘을 그대로 실었다** — 세 줄을 나란히 읽으면 원문이 복원된다. 검사가 유출을 막는 대신 **옮기고** 있었다. 규칙 전건을 다시 적용해 그 줄의 모든 일치를 가리도록 고쳤다.

### 15. 2.3 비공개 유입 차단 — 켜자마자 실제 유출 3건이 나왔다

`.pre-commit-config.yaml`(gitleaks + 자체 검사 + ruff) · `scripts/check_disclosure.py` · CI `secrets` 잡. **훅과 CI 를 둘 다 둔다** — 훅은 `--no-verify` 한 줄로 우회되고 CI 는 이미 커밋된 뒤에 본다.

> **규칙을 두 곳에 두지 않는다.** 비밀 문자열(API 키·토큰)은 gitleaks 가 보고, 우리 도구는 **gitleaks 가 보지 않는 것** — 로컬 절대 경로·비공개 시드 파일·개별 식별정보 형태 — 만 본다. 같은 규칙이 두 곳에 있으면 반드시 갈리고, 갈린 쪽이 느슨하면 그쪽이 통로가 된다.

**찾아낸 것 셋 — 전부 08-03 정제(43건) 때 손으로 훑고도 남은 것이다.**

| # | 위치 | 내용 |
|---|---|---|
| ① | `status.md` | 볼트 경로가 코드블록에 리터럴로 남아 있었다. lock 에서는 뽑아냈지만 이 한 줄은 남았다 |
| ② | spec 부록 C | DER-VET 로컬 체크아웃 경로 |
| ③ | spec 부록 D | 같은 경로. *"v0.3에서 실경로로 정정"* 이라고 적혀 있었다 — **의도적으로 넣은 것**이다 |

> **한글 파일명이 검사 밖에 있었고, 음성 테스트가 그것을 잡았다.** `git ls-files` 는 비ASCII 경로를 따옴표로 감싸 바이트를 이스케이프한다(`"rslt/task-\353\266\204….md"`). 그러면 경로 끝이 `"` 가 되어 `\.xlsx$` 같은 규칙이 전부 빗나가고 **한글 이름 파일은 통째로 검사 밖**이 된다 — **이 저장소의 spec 과 작업 목록이 정확히 그 이름**이다. `-z` 로 고친 직후 위 ②③이 드러났다.
>
> 검사기 자신을 검사하지 않았다면 「유출 0건」이 계속 초록불이었을 것이고, **가장 중요한 두 문서가 영영 스캔 대상이 아니었다.**

**SC-8 민감도 등급이 대장에 아예 없었다.**

> 조항은 `공개 가능`/`비공개` 등급을 요구하고 **등급 미지정을 「비공개」로 간주**한다(안전한 기본값). 그런데 필드 자체가 없었다 — 조항대로 읽으면 **등재 20건 전부가 비공개인 채로 공개 저장소에 있었던 셈**이다.
>
> 실제 내용은 전부 공개 가능한 값이었다(공개 추세 기반 합성 대표값 13 · 값 0인 비활성 4 · 법정 세율 1). **그러나 그 사실이 어디에도 적혀 있지 않았고, 그 상태로는 다음에 진짜 업계 견적이 들어와도 아무것도 달라지지 않는다.** 18건에 등급을 부여하고 `check_assumptions.py` 가 강제한다.
>
> **가정 불가 2건에는 등급을 달지 않았다.** 값이 없어 새어 나갈 것이 없고, **값이 도착하는 시점이 등급을 정할 시점**이다 — 미리 달아 두면 회신으로 받은 업계 수치가 이미 공개 등급을 달고 들어온다.

### 14. `tax.vat_rate` — 자리가 없으면 값은 소스에 남는다

2.9가 찾아낸 `core/der/heatpump.py` 의 `vat_rate=0.1` 을 처리했다. **대장에 올릴 자리가 없었다** — 잠정 가정이 아니라 법정 세율이라 `assume`(민감도 3수준 필수)도 `default0`(값 0)도 `blocked`(값 없음)도 맞지 않았다.

> **자리가 없으면 만들지 않는 쪽이 더 흔하다.** v1.1 계약 개정에서 ESS 가 `capex_vat()` 를 아예 만들지 않아 세액이 사라졌던 것과 같은 구조다. 그래서 갈래를 늘렸다 — `track: fixed`(확정: 법령·고시로 정해져 회신을 기다릴 것이 없다).
>
> **`fixed` 는 대장에서 가장 강한 주장이라 가장 엄하게 본다.** 값 필수 · 신뢰도 `확정`/`추정` 강제(근거 없으면 결함) · **민감도 금지**. 정해진 값에 범위를 주면 «정해졌다»와 «범위가 있다»가 한 항목에 공존한다 — 제도가 바뀔 수 있다면 그것은 민감도가 아니라 `replace_when` 에 적을 일이다. 음성 4종을 더해 대장 음성 테스트가 15 → **19종**이 됐다.

**그리고 v1.1이 메우지 못하고 남긴 구멍을 하나 더 찾았다.**

> 계약 개정은 「`capex_vat()` 가 없다」를 닫았지만 **기본값의 갈림은 그대로 남아 있었다** — 여덟 중 일곱(자원 5종 + 공통설비 + Load/ThermalLoad)이 `0.0`, **히트펌프만 `0.1`** 이었다. 같은 프로포마에서 히트펌프만 세액이 잡히고 나머지 열은 0원이 되는데 **어느 쪽도 오류가 아니다.**
>
> 물가상승률이 `2.0`(%)과 `0.02`(소수)로 갈렸던 것과 정확히 같은 유형이고, 그때와 마찬가지로 **자원별 테스트가 각자 자기 기본값으로 오라클을 맞춰 두어 전부 초록불**이었다.
>
> 여덟을 `0.0` 으로 통일하고, **레지스트리를 순회해 기본값이 전건 같은지 보는 계약 테스트**를 세웠다. 손으로 목록을 적으면 자원을 추가할 때 반드시 빠지고, 빠진 자원은 검사받지 않은 채 초록불로 남는다.

> **기본값 `0.0` 은 「세율 0%」가 아니라 「주입되지 않음」이다.** 그 사실이 프로포마에 0원으로 나타나는 것이 이 설계의 대가이며, **주입을 강제하는 것은 모델 계층(WP-2 `AssumptionSet` · FR-601)의 몫**이다. 「지금 할 일」 4번에 등재했다 — 지금은 대장에 값이 있지만 **아무도 읽지 않는다.**

### 13. 2.16 정본 표류 상시 감시 — 감시기 자신이 세 번 조용히 고장 났다

`DER_VAULT_ROOT` 를 사용자 환경변수로 세우고, `scripts/vault_watch.ps1` 을 작업 스케줄러(`DER-evaluator 정본 대조`, 매일 09:10)에 등록했다. 표류를 감지하면 `%LOCALAPPDATA%\der-evaluator\DRIFT-DETECTED.txt` 를 만들고 정합하면 지운다.

**CI 로는 원리상 할 수 없는 검사다** — 볼트는 저장소 밖 로컬 경로다. 그래서 로컬 스케줄러가 유일한 수단이다.

> **만드는 과정에서 감시기가 세 번 고장 났고, 셋 다 「등록되어 있지만 아무것도 하지 않는」 형태였다.**
>
> | # | 고장 | 왜 드러나지 않았나 |
> |---|---|---|
> | ① | 스케줄러 환경의 PATH 에 conda `python` 이 없어 **로깅 전에** 죽었다 | 작업은 「결과 1」로 끝나고 **로그는 비어 있다.** 작업 목록에는 등록돼 있으므로 겉보기로는 감시 중이다 |
> | ② | 콘솔이 없으면 파이썬이 한국어 출력을 시스템 코드페이지로 인코딩하다 실패하고, `2>&1` 이 그것을 `NativeCommandError` 로 감싸며 `ErrorActionPreference='Stop'` 이 종료성 오류로 만든다 | **콘솔에서 같은 명령을 직접 돌리면 정상이다.** 재현되지 않는 고장이라 원인을 찾기 전까지는 도구를 의심하지 않는다 |
> | ③ | `schtasks` 가 261자 넘는 `/TR` 을 **「SUCCESS」와 함께 잘랐다** | 볼트 경로가 `...\faux-va` 가 되었고, 실재하지 않는 경로이므로 검사가 **저장소 사본으로 내려가 종료 코드 0(정합)** 을 냈다. **막으려던 상태를 그것을 막으려는 장치가 재현했다** |
>
> 고정한 방법 — 파이썬 경로를 인자가 아니라 **사용자 환경변수**로 넘기고(잘리지 않는다), `PYTHONIOENCODING=utf-8` 을 세우고, **등록 후 저장된 명령을 읽어 대조하며**, 실행 전에 **볼트 경로의 실재를 단언**한다.
>
> **「등록이 성공했다」는 「등록된 것이 의도한 것과 같다」가 아니다.** CODEOWNERS 에서 만난 유형(없는 팀명을 적으면 그 줄이 조용히 무시된다)과 같다.

**네 경로를 전부 실측했다** — 볼트 미설정 → 코드 2 + 마커 · 볼트 경로 부재 → 코드 2 + 마커 · 정본 변조 → 코드 1 + 마커(스케줄러 경유로도 확인) · 복구 → 코드 0 + 마커 삭제.

> **종료 코드 규약이 한 번 무너졌다.** `Write-Error` 는 `ErrorActionPreference='Stop'` 과 만나면 그 자리에서 예외를 던져 **뒤의 `exit 2` 에 도달하지 못하고 1** 이 된다. 그러면 「검사를 수행하지 못했다(2)」와 「표류를 감지했다(1)」가 같은 코드로 나온다. stderr 에 직접 쓰도록 고쳤다.

### 12. 2.5·2.6 NFR-105 CI 게이트 ①② — CI 에는 테스트 잡이 없었다

`tests.yml` 신설. **그전까지 CI 는 문서 게이트만 돌았고 pytest·ruff·import-linter 는 사람이 로컬에서 부를 때만 검사되었다.**

| 게이트 | 구현 | 판정 |
|---|---|---|
| ① 변경분 커버리지 ≥ 95% (NFR-105) | `diff-cover` + `check_coverage_inputs.py` | PR 한정, 차단 |
| ② 테스트 동반 (NFR-105) | `check_test_accompaniment.py` | PR 한정, 차단 |
| ③ 커밋 순서 (NFR-105) | 미구현 | 조항이 **경고**로 규정 — 스쿼시 병합 등 정당한 예외가 있다 |
| ④ 구획 경계 (NFR-208, 2.8) | `lint-imports` + 음성 3 · 양성 2 | 상시, 차단 |
| ⑤ 전체 커버리지 ≥ 85% (NFR-203, 2.10) | `--cov-fail-under=85` | 상시, 차단 |
| ⑥ mypy strict (NFR-204, 2.4) | `python -m mypy` | 상시, 차단 |
| ⑦ 파라미터·전역 상태 (NFR-202·205, 2.9) | `check_hardcoded_params.py` + 음성 7·양성 4·경계 5 | 상시, 차단 |
| ⑧ 비공개 유입 (SC-3·SC-5·SC-8, 2.3) | gitleaks + `check_disclosure.py` + 음성 9·양성 6·경계 2 | 상시, 차단 · **pre-commit 에도** |

> **⑦은 「수치 리터럴 금지」가 아니다.** `core/` 에만 리터럴이 495개 있고 대부분 0·1·인덱스·단위 상수다 — 전부 금지하면 통과할 수 없고, **통과할 수 없는 검사는 꺼진다.** 판정 대상은 **「대장의 값이 소스에 복제되었는가」**이며, 그것이 16.1의 셋째 장치가 막으려던 것이다.
>
> **차단(원 단위 ≥1만) · 경고(≥1천 비-원) · 「판정 안 함」(<1천) 세 단으로 가르고, 마지막 것의 건수를 출력한다.** 보지 않은 범위를 말하지 않으면 「전부 검사했고 깨끗하다」로 읽힌다. 문턱 없이 돌리면 경고가 26건 나왔고 **전부 값 충돌**이었다 — 읽히지 않는 경고는 탐지 장치가 아니다.
>
> **문턱 값 자체를 음성 테스트가 고정한다.** 올리면 진짜 복제가 경고로 내려가고 **경고는 종료 코드 0** 이다. 후퇴가 초록불로 나타나는 형태다.

> **④와 ⑤는 ①②가 대신하지 못한다.** ①은 **신규 코드만** 본다 — 기존 코드의 테스트를 지우거나 커버되던 분기를 옮기면 변경분은 깨끗한데 전체는 내려간다. 하나는 들어오는 것을, 하나는 이미 있는 것을 지킨다.
>
> **④의 음성 테스트가 요점이다.** 빈 패키지 위의 `4 kept` 는 공허하고, Wave 0의 BROKEN 실측은 임시 실행이라 재현되지 않았다. 이제 역방향 계층·형제 구획 직접 참조·계약의 구획 참조를 각각 심어 **그 계약이** 걸리는지 본다(다른 계약이 대신 걸리면 실패다). 심는 모듈 이름을 `_` 로 시작시키는 것은 **레지스트리 발견기는 `_` 를 건너뛰지만 import-linter 는 그래프를 전부 보기 때문**이다 — 그 차이가 없으면 `core/der/` 안에서 이 검사를 할 수 없다.

> **diff-cover 는 판정할 수 없을 때 통과한다 — 10.4.2 로 실측했다.** `coverage.xml` 의 경로가 저장소 상대 경로와 어긋나면 *"No lines with coverage information in this diff"* 를 출력하고 **종료 코드 0** 을 낸다. 문서만 고친 PR 에서는 그것이 옳고 `core/` 를 고친 PR 에서는 정반대의 뜻인데, **출력도 종료 코드도 같아 로그로도 구별되지 않는다.**
>
> 그래서 `check_coverage_inputs.py` 가 diff-cover **앞에서** 「변경된 `core/**/*.py` 전건이 산출물에 있는가」를 단언한다. `source = ["core"]` 때문에 **테스트가 import하지 않은 파일도 0% 로 등장하므로**, 빠졌다면 커버리지가 낮은 것이 아니라 측정이 그 파일을 보지 못한 것이다.

> **게이트 ②의 「대응」을 넓게도 좁게도 잡을 수 없었다.** 아무 테스트나 바뀌면 통과로 하면 core 여섯 개를 고치고 관계없는 테스트 한 줄을 고친 PR 이 통과한다. 파일명 규약만 인정하면 `core/contracts/der.py` ↔ `tests/contract/test_der_contract.py` 처럼 **이미 규약을 벗어난 정당한 PR 이 막히고, 막히는 게이트는 꺼진다.** 두 경로를 모두 인정하고 어느 쪽으로 인정되었는지 보고에 적는다.
>
> **import 는 `ast` 로 읽는다.** 문자열 포함으로 검사하면 *"`core.der.pv` 를 예로 들면"* 이라고 적은 주석이 대응 테스트로 세어진다 — 「반복해서 걸린 함정」의 **반대 방향**이며 결과가 더 나쁘다. 서술이 선언으로 세어지면 게이트가 조용히 초록불이 된다.

> **부수로 2.8을 배선하며 status.md 자신의 오류를 찾았다.** 이전 판이 대체 명령으로 적어 둔 `python -m importlinter.cli lint` 는 **`__main__` 가드가 없어 import만 되고 끝나며 종료 코드 0** 을 낸다. `infra/` 에 위반을 심어 실측 재현했다 — 4계약이 «검사되고 있다»고 적혀 있었지만 그 명령으로는 아무것도 검사되지 않았다.

**테스트 544 → 571건 · 자동 매핑 39 → 45건 · 미매핑 222 → 217건 · 커버리지 89.73%.**

### 11. 6.7 플러그인 자동 등록 — 중앙 등록부 0줄 (`NFR-207`)

`core/contracts/registry.py` 하나로 자원 6종(`PV`·`ESS`·`EV_V2G`·`HeatPump`·`Load`·`ThermalLoad`)과 공통설비 3종(`CEMS`·`HEMS`·`MeteringComm`)을 발견한다. 검증 11건.

> **발견기가 `core.der` 를 import하지 않는다 — 스캔할 패키지를 인자로 받는다.**
>
> ```python
> REGISTRY = discover(core.der, DER)          # 호출부가 패키지를 건넨다
> ```
>
> 그래서 NFR-208-AC3(계약은 어떤 구획도 import하지 않는다)이 그대로 성립하면서 플러그인 규약이 계약 계층에 남는다. **같은 함수가 `core/asset/` 에도 쓰이는 것은 부수 이득이 아니라 같은 원인의 결과다** — 발견기가 특정 구획을 모르기 때문이다.

**구현이 드러낸 것 셋.**

| # | 드러난 것 | 왜 위험했나 |
|---|---|---|
| ① | `StandardCommonAsset` 은 구상이지만 **유형이 아니다** | 「`tag` 없으면 기반으로 본다」로 정하면 **`tag` 를 잊은 진짜 자원도 함께 사라지고** NFR-106 순회 검사가 그 자원을 보지 않은 채 통과한다. `REGISTRY_ABSTRACT = True` 표식으로 명시 — 잊은 것과 뺀 것을 구분한다 |
| ② | 표식이 **상속되면 안 된다** | `getattr` 로 보자 CEMS·HEMS·공용 계량통신 **셋 전부가** 빠졌다. 「공통설비가 없는 단지」로 계산되고 CEMS 구축비·운영비가 통째로 사라지는데 결과는 그럴듯하다. `cls.__dict__` 로 고쳤다 |
| ③ | 참조 구현이 레지스트리를 **오염시킬 수 있었다** | `ReferencePV.tag == "PV"` 이므로 `__module__` 로 걸러내지 않으면 pytest 중 스캔에서 진짜 `PV` 와 등록 충돌이 뜬다. 구현에 잘못이 없는데도 뜨고, 피하려고 참조 구현의 `tag` 를 바꾸면 그것은 계약 위반이다 |

**M1을 기계 검사로 바꿨다.** *"신규 자원 추가 PR의 diff에 공유 파일 변경 0줄"* 은 PR을 봐야 아는 사실처럼 보이지만, 그 diff가 필요해지는 **원인은 소스에 있다.** 그래서 「공유 파일의 **코드**에 자원 이름 0건」 + 「파일 하나를 더 놓으면 실제로 레지스트리가 는다」를 함께 본다 — **둘 중 하나만으로는 부족하다.** 이름이 없어도 발견기가 고정 목록을 들고 있으면 늘지 않는다.

> **여섯 번째로 같은 함정에 걸렸다 — 아래 「반복해서 걸린 함정」에 등재했다.** 공유 파일 검사를 문자열 포함으로 쓰자 **그 검사를 설명하는 주석과 독스트링이 그대로 걸렸다.** 해법도 같다: `ast` 로 코드 식별자·문자열 리터럴만 뽑아 서술과 선언을 가른다. 제외 범위를 넓게 잡으면 코드도 함께 빠져 진짜 등록부가 있어도 초록불이 되므로, **심어 둔 등록부를 잡는지**와 **서술만 있는 파일을 오판하지 않는지**를 둘 다 검사한다.

### 10. NFR-206 저장소 전체 검사 — 사각지대에서 811줄까지 자란 파일

계약 개정으로 자원 4종이 500줄을 넘겼는데 **아무 검사도 울리지 않았다.** 이유를 보니 이 검사를 쓴 것은 WP-1e 하나뿐이었고 자기 파일 두 개만 보고 있었다.

저장소 전체를 훑자 **아무도 모르던 위반 2건**이 나왔다 — `gen_traceability.py` **811줄**(+311), `check_source_rules.py` 571줄. 자원 파일과 달리 이 둘은 **처음부터 감시 밖**이었다.

> **경고로 켰다.** NFR-206은 Should-have이고 측정 수단이 `M1 lint 경고`다. 차단으로 켜면 조항이 요구하지 않은 강제력을 갖고, 통과시키려고 **근거 주석을 지우는 압력**이 생긴다 — 이 저장소의 주석 밀도는 의도된 것이며, 조항의 근거(DER-VET `Params.py` 1,830줄 유지보수 실패)는 코드 스프롤을 막으려는 것이지 설명을 줄이려는 것이 아니다.
>
> 대상 디렉터리가 비면 **종료 코드 2**다. 검사를 수행하지 못한 것을 통과로 읽지 않는다(§13.0.1 ④) — 이 저장소가 08-08 오전에 매핑 게이트에서 만난 것과 같은 유형이다. 실측 확인했다.

### 9. 1.11 계약 개정 v1.1 — 병렬이 드러낸 구멍 5건을 계약이 답한다 (커밋 `dd13817`)

§16.2 절차 ②③④를 이행했다. **구현 PR에 섞지 않고** 계약·계약테스트·영향 구획 적응을 하나의 개정 단위로 묶었다.

| # | 구멍 | 실측한 갈림 | 개정 |
|---|---|---|---|
| ① | `dispatch()` 부분 창 해석 | docstring은 *"한 해 운전"* 인데 계약 테스트는 `steps=24` 로 호출. 근거가 없어 6종이 각자 정했다 | **연초부터 연속 `steps` 스텝**으로 확정. `year_fraction`·`is_full_year`·`hours_per_step` 신설, `steps ≤ 연간 스텝` 강제 |
| ② | 부가세 분리 자리 없음 | 다섯이 각자 `capex_vat()` 를 지어냈고 **ESS 는 아예 없었다.** 기본값도 갈렸다(0.0 vs 0.1), 연차 조건도 갈렸다(1년차만 vs 매년) | `capex_vat()` **추상** 신설 |
| ③ | `value_streams()` 훅 부재 | `RC-LD-B0` 이 상속으로 강제되지 않았다. 반환형도 `list[ValueStream]` 로 형제 구획을 참조하고 있었다 | `DER` 에 기본 `return ()`. **tag 문자열**을 돌려준다 — 객체를 돌려주면 NFR-208-AC2 위반이다 |
| ④ | 운전 방법 접근자 이름 | `mode`(3종) vs `operating_mode`(1종), `OPERATING_MODES`(3종) vs `operating_modes`(1종) | 이름 고정. 소속 검사는 계약이, **값 목록은 자원이** 소유(AC2) |
| ⑤ | 물가상승률 놓을 자리 없음 | `inflation_pct`(**%**) / `inflation_rate`(소수) / `om_escalation`(소수) | `escalation_rate`(소수) + `escalation_factor(year=)`. 1년차 = 1.0 |

함께 메운 것 넷: `DispatchResult` 에 매체별 `unmet_*` + `notes` · `salvage_value()` 명목액 명문화 · `CommonAsset` 에 `capex_vat()`·`salvage_software()`·`salvage_hardware()` 추상화 · `AllocationResult` 를 계약으로 이관.

**계약 테스트 38 → 48건 · 전체 484 → 533건 · 자동 매핑 34 → 36건 · 미매핑 227 → 225건.**

> **⑤가 가장 조용한 결함이었다.** 같은 「2%」가 ESS 에는 `2.0`, 나머지에는 `0.02` 로 들어가고 **어느 쪽도 오류가 아니다.** 20년 프로포마에서 100배 차이인데 타입도, 검증도, 테스트도 걸리지 않는다. 자원별 테스트는 각자 자기 척도로 오라클을 맞춰 두었으므로 **6개 파일 모두 초록불**이었다.

> **②의 ESS 는 「이름이 갈린 것」보다 나쁜 상태였다.** 다섯 자원은 이름이 달라 엔진이 순회 집계를 못 하는 문제였지만, ESS 는 **세액 자체가 없었다.** 프로포마의 부가세 행에서 ESS 열만 0원이 되고, 그 0은 「면세」로도 「누락」으로도 읽힌다. 계약에 자리가 없으면 만들지 않는 쪽이 더 흔하다.

> **①은 균형식이 볼 수 없는 종류의 오류를 막는다.** 같은 `steps=24` 가 자원마다 다른 시각 구간을 뜻하면 엔진이 스텝 `i` 에서 더하는 값들이 **서로 다른 시각의 값**이 된다. 총량은 그럴듯하고 NFR-102 수지 균형도 통과한다 — 시각의 어긋남은 균형식의 관심 밖이다.
>
> 계약 테스트를 **「24스텝 창의 값 = 48스텝 창의 앞 24스텝」** 으로 썼다. 구현을 들여다보지 않고도 금지된 두 해석(총량 몰아 담기 · 사이클 압축)이 전부 걸린다. **창은 관측 범위이지 물리의 변경이 아니다** — 24스텝을 요구했다는 사실이 그 24스텝 안에서 벌어지는 일을 바꿔서는 안 된다.

> **HeatPump 미충족을 「계획 대비」로 재는 것이 이 작업의 비자명한 판단이었다.** `RC-HP-X1` 의 미충족 열량이 `HeatPumpOperation` 안에만 있어 `dispatch()` 를 통과하며 사라지고 있었으므로 `DispatchResult` 에 계열을 만들었는데, 스텝별 값을 `부하 − 공급` 으로 재면 **심야 운전이 주간 부하를 심야로 옮긴 것을 미충족으로 센다.** 정격이 충분한 설비가 「연중 60% 미충족」으로 보고된다.
>
> 그래서 `_production_target` 을 `_production_plan` 으로 바꿔 **(생산 목표, 미충족 계상 기준)** 두 배열을 돌려주게 했다. 목표에 용량 상한을 미리 걸지 않는 것도 같은 이유다 — 미리 깎으면 **깎인 만큼이 계상 기준에서도 함께 사라져** 정격 부족이 미충족으로 드러나지 않는다. 스텝별 미충족의 합이 연 미충족과 같은지도 테스트가 검사한다.

> **개정이 NFR-206을 넘겼다.** 자원 4종은 개정 전에 이미 **497~500줄**로 상한이 포화였고, 계약이 요구한 것을 실을 자리가 없었다. 「지금 할 일」 1번이며 판단이 필요하다.

> **`status.md` 의 「테스트 500건」은 합산 오류였다.** 실측 484건이다(6.0 단위 446 + 계약 38). 이번 세션 첫 검사에서 드러났다.

### 8. 6.0 자원 6종 + 공통설비 — 6개 단위 병렬 수행 (작업 목록 v2.0)

작업 목록이 *"1파일 = 1작업자 = 1검증 케이스 세트"* 로 설계해 둔 대로 **6개 에이전트를 동시에** 돌렸다. 각 단위는 자기 파일 2개만 소유하고 공유 파일은 읽기만 했다(§16.1 W-1).

| 단위 | 파일 | 테스트 |
|---|---|---|
| WP-1a | `core/der/pv.py` | 66건 |
| WP-1b | `core/der/ess.py` | 44건 |
| WP-1c | `core/der/ev_v2g.py` | 39건 |
| WP-1d | `core/der/heatpump.py` | 40건 |
| WP-1e | `core/der/load.py` · `thermal_load.py` | 67건 |
| WP-1f | `core/asset/common_asset.py` | 190건(파라미터화) |

**합계 484건 전건 통과 · ruff 통과 · import-linter 4계약 kept · 전 파일 500줄 이내(NFR-206).**

> **당시 「500건」으로 적었던 것은 합산 오류다** (446 + 계약 38 = 484). 08-08 후속 세션의 첫 검사에서 드러났고, 기록에 틀린 숫자를 남기면 다음 사람이 그것을 기준선으로 쓰므로 여기서 정정한다.
>
> **「전 파일 500줄 이내」도 실은 포화 상태였다** — 자원 4종이 497~500줄이었고, 그래서 1.11 계약 개정이 요구한 것을 실을 자리가 없었다. 당시엔 그 사실이 보이지 않았다(검사가 WP-1e 두 파일만 보고 있었다).

> **자동 매핑 7 → 34건, 미매핑 254 → 227건.** 게이트를 켜 둔 채로 27건이 실제로 줄었다.

> **병렬 수행이 계약의 구멍을 드러냈다.**
>
> 6개 구획이 **같은 계약을 서로 모른 채 구현**하자, 계약이 답하지 않은 질문이 각자 다른 답으로 메워졌다 —
>
> - `capex_vat()` 를 **세 구획이 각자 지어냈다**. §13.2.2 C-1이 부가세 분리를 요구하는데 계약에 자리가 없다
> - `dispatch()` 의 「한 해」와 계약 테스트의 `steps=24` 가 충돌하는데 **부분 창 해석이 계약에 없어** 자원마다 다르게 정했다
> - `value_streams()` 훅이 없어 「부하는 편익을 만들지 않는다」가 **상속으로 강제되지 않는다**
>
> **한 사람이 순차로 짰다면 일관돼 보였을 뿐 같은 구멍이 그대로 남았을 것이다.** 병렬이 비용이 아니라 진단이었다.

> **WP-1a가 내 Wave 0 오류를 잡았다.** `ReferencePV.replacement_schedule()` 이 교체를 **수명 연도(12년차)** 에 계상해, §13.2.2 C-4의 *"수명 도달 **다음** 연도 초"*(13년차)와 한 해 어긋나 있었다. 12년차에 넣으면 **아직 살아 있는 해에 교체비가 잡힌다.** 계약 쪽 참조 구현이 틀리면 자원 6종이 그것을 베끼므로 즉시 정정했다.

> **에이전트 2개(ESS·EV_V2G)가 세션 한도로 중간에 끊겼다.** 다만 파일을 다 만든 뒤 「500줄로 줄이기」 단계에서 잘린 것이라 산출물은 온전했고, 실제로 전건 통과·줄 수 이내를 확인했다 — **보고를 믿지 않고 저장소를 직접 검사했다.**

### 7. 1.0 계약 고정 — 첫 코드 (작업 목록 v1.9)

Wave 0의 마지막 관문. **단독 수행·병렬 불가**이며, 여기가 흔들리면 이후 전부 재작업이다.

| 산출물 | 내용 |
|---|---|
| `core/contracts/units.py` | §7.5 단위 규약 · **`to_won()` — 반올림이 일어나는 유일한 지점** · `Year`(1-base) |
| `core/contracts/der.py` | `DER` 6메서드 전부 추상 · `DispatchResult`(매체 4종) · `DispatchContext` |
| `core/contracts/asset.py` | `CommonAsset` — **`DER`을 상속하지 않는다.** SW/HW 분리 계상 |
| `core/contracts/valuestream.py` | `ValueStream` · `Payer` · `ExclusionType`(A~D) |
| `core/contracts/engine.py` | `DispatchEngine` · `SystemDispatch` · 수지 균형 검증 |
| `core/contracts/schemas.py` | 경계 스키마 4종 (Pydantic, frozen) |
| `.importlinter` | 계약 4종 — NFR-208-AC1·AC2·AC3에 1:1 대응 |
| `.github/CODEOWNERS` | 구획 소유. 스캐폴드 17경로 |

**테스트 38건 통과 · import-linter 4계약 · 자동 매핑 0 → 7건 · 미매핑 261 → 254건.**

> **설계 판단 세 가지를 남긴다.**
>
> **① 6개 메서드를 전부 추상으로 뒀다.** 기본 구현을 주면 구현체가 잊어도 통과하고, 잊힌 메서드가 0원을 돌려주면 **비용이 조용히 사라진다** — 회수기간이 짧아지고 필요 지원액이 과소 산정되는데 화면상으로는 정상이다. 잊으면 인스턴스화 자체가 실패하게 뒀다.
>
> **② 반올림 지점을 `to_won()` 하나로 묶고 사사오입을 명시했다.** 은행가 반올림(파이썬 `round()` 기본값)을 쓰면 `2.5 → 2`가 되어 엑셀 대조(§13.3)에서 셀마다 1원씩 어긋난다. 0.01% 기준은 통과하므로 **테스트는 초록불인데 검산은 맞지 않는** 상태가 된다. 그리고 `won_sum()` 은 **각 항을 반올림한 뒤 더한다** — 더한 뒤 반올림하면 사람이 행별로 눈으로 더한 값과 총계가 어긋난다.
>
> **③ 참조 PV를 `core/der/` 가 아니라 `tests/contract/` 에 뒀다.** 진짜 PV는 WP-1의 Wave 1 산출물이다. Wave 0에서 `core/der/` 에 넣으면 소유 경로 침범(§16.1 W-1)이고, 더 나쁘게는 참조 구현이 "이미 있는 PV"가 되어 WP-1이 그것을 손보는 것으로 시작한다 — **계약이 구현을 규정해야 하는데 순서가 뒤집힌다.**

> **CODEOWNERS의 한계를 파일 안에 적었다.** GitHub은 실재하는 계정·팀만 인식하므로 없는 팀명을 적으면 그 줄은 **조용히 무시되고, 파일은 있는데 아무것도 강제하지 않는 상태**가 된다 — 이 저장소가 반복해서 만난 유형이다. 전 경로를 저장소 소유자에게 두고 구획 배정은 주석으로 남겼다. 격리를 실제로 강제하는 것은 import-linter다.

> **import-linter 4계약도 음성 확인을 했다.** 빈 패키지 위에서 `4 kept` 만 보면 공허하다. `core.contracts`가 구획을 참조할 때(AC3), 자원이 엔진을 참조할 때(AC1), 형제 구획을 직접 참조할 때(AC2) 각각 BROKEN이 뜨는 것을 실측했다.

> **ruff가 제 코드에서 실제 버그 후보를 짚었다** — `B023`(루프 변수 클로저). 08-08 오전에 쓴 `collect_test_markers` 의 중첩 `walk` 가 파일 루프 변수를 잡고 있었다. 지금은 즉시 호출이라 무해하지만 지연 호출로 바뀌는 순간 **마지막 파일 경로가 전 항목에 붙는다.** 함수를 밖으로 빼고 이유를 주석에 남겼다. 규칙 집합은 `pyproject.toml` 에 **명시적으로 고정**했다 — 비워 두면 ruff 판올림마다 검사 범위가 조용히 바뀐다.

### 6. 2.7 CI 매핑 게이트 활성화 — 경고에서 차단으로 (작업 목록 v1.8)

선행 4건이 모두 찼으므로 `source-rules.yml`의 `traceability` 잡에서 `continue-on-error`를 제거했다. 함께 세 가지를 처리했다.

**① `@pytest.mark.manual` 구현 — `NFR-107-AC1.manual`이 실체를 얻었다.**

생성기가 `@pytest.mark.req`만 정규식으로 긁고 있어, 아무것도 실행하지 않는 skip 스텁이 매핑표에 **`자동`으로** 표시됐다. `ast`로 다시 읽어 두 마커가 *같은 테스트에* 붙었는지 판정하게 했다 — 파일 단위 정규식으로는 그 짝을 알 수 없다. 클래스 데코레이터와 모듈 `pytestmark`도 상속시킨다(pytest가 그렇게 동작하므로 도구도 같아야 한다).

스텁만 있고 `manual-checks.yaml`에 수행 기록이 없으면 **종료 코드 2**로 막는다 — `AC1.manual`은 스텁을 매핑 형식으로 인정하지만 `AC3`은 수행 일자·수행자·결과를 요구한다. 대장 없이 스텁만 두면 "수동으로 검증됨"이 미수행을 영구히 가린다.

**② 게이트 자기참조 금지를 도구가 강제한다 (`NFR-107-AC5` ⓒ).**

`manual-checks.yaml`에 `NFR-105`~`NFR-107` 조항이 등재되면 종료 코드 2다. 08-08 오전에 spec 본문에 명문화한 규칙을 이제 기계가 막는다.

> **③ CI 구멍 1건을 찾아 닫았다 — 이것이 오늘 가장 중요한 발견이다.**
>
> 기존 잡은 `python scripts/gen_traceability.py ... || true`로 **종료 코드를 버리고** diff만 봤다. 그런데 **결함(종료 코드 2)이면 매핑표가 생성되지 않으므로 파일이 안 바뀌고, diff가 통과한다.**
>
> 즉 spec 수용기준 선언에 결함이 있을 때 **게이트가 조용히 초록불이 되는** 상태였다. 검사를 수행하지 못한 것을 통과로 읽은 것이며, 이 저장소가 반복해서 만나 온 유형(「드러나지 않는 공란은 게이트를 조용히 무력화한다」)과 정확히 같다.
>
> 결함을 심은 spec 사본으로 **옛 방식은 통과 / 새 방식은 차단**을 실측 재현한 뒤 닫았다.

**게이트 자신도 검사한다.** 잡에 `negtest_traceability.py`(음성 6종 + **양성 2종**)를 넣었다. 음성만 보면 "전부 결함으로 보고하는" 장치도 만점을 받으므로 양성 케이스를 함께 둔다. 가정 대장 잡도 같은 구조로 추가했다.

**게이트가 판정하지 않는 것을 명시했다** — 「미매핑 0건」은 이 게이트가 아니라 17.9(Phase 1 DoD)다. 지금 절대 0을 요구하면 CI가 영구히 빨간불이 되고, 통과시키려고 §13.0.2가 금지한 자기충족 테스트를 짓게 된다. 미매핑 증가는 **커밋된 매핑표와의 diff**가 잡으므로 별도 기준선 파일을 두지 않았다 — 같은 사실이 두 곳에 있으면 어긋난다.

### 5. 16.1 전제 데이터 — 회신을 기다리지 않고 가정값으로 전환 (spec v0.10 / 작업 목록 v1.7)

외부 회신 리드타임은 개발과 무관하게 흐른다. 기다리면 코드가 멈추고, 기다리지 않으면 근거 없는 수치가 코드에 박힌다. **셋째 길은 가정을 가정이라고 표시한 채 데이터로 두는 것**이며 시스템은 이미 그 구조를 갖고 있다(FR-601 `AssumptionSet` · 부기 7종 · 신뢰도 3단계).

Q-1~Q-13을 **세 갈래**로 판정하고 `docs/assumptions.yaml`에 19개 항목으로 등재했다.

| 갈래 | 건수 | 처리 |
|---|---|---|
| **가정** | 13 | 값 + 민감도 3수준 + 부기 7종. 전건 신뢰도 `가정` |
| **비활성** | 4 | Q-8·Q-8b·Q-10·Q-12 — 제도 미확인이므로 **크기를 추정하지 않고 값 0** |
| **불가** | 2 | Q-4·Q-5 — **가정하면 검증이 자기충족이 된다.** 판정 유예 |

> **「불가」 2건이 이 작업의 핵심 판단이다.**
>
> Q-4는 17.4(SG-4 ±15% 재현)의 유일한 정박점이고 Q-5는 §13.3 1차 대조군이다. **우리 계산을 재는 자를 우리가 만들면 무엇을 재도 맞는다.** 가정값을 넣으면 *"우리가 가정한 값을 우리 코드가 재현했다"* 가 되고, 그 상태는 초록불로 남아 스스로 드러나지 않는다 — **틀린 값을 넣는 것보다 나쁘다.** 틀린 값은 회신이 오면 드러난다.
>
> 그래서 **17.4·§13.3 판정을 유예**한다. 통과로도 실패로도 세지 않고 「미판정」으로 남긴다. `check_assumptions.py`가 그 칸에 값이 들어가는 것을 막고 매 실행마다 유예 목록을 출력한다.

**「나중에 변경 가능」은 세 장치로 성립한다** — 대장 하나만으로는 안 된다.

| 장치 | 역할 |
|---|---|
| `docs/assumptions.yaml` | 값·민감도·부기 7종·교체 조건의 정본. **회신 시 이 파일만 바꾼다** |
| `scripts/check_assumptions.py` | 부기 결손·근거 없는 `확정`·교체 경로 부재·정박점 오염을 잡는다. 음성 테스트 **15종 전건 감지** |
| NFR-202 lint (2.9) | 값이 소스에 리터럴로 새는 것을 막는다. **이것이 없으면 누군가 옮겨 적는다** |

부수 정정: 16.5를 4종 → **6종**(MIRR·B/C 추가)으로 늘리고, 엑셀 미확보 상태의 대체 경로를 **해석해(§13.0.2 순위 1)** 로 명시했다. 해석해는 *"산식이 맞다"* 만 보이고 *"실무 전제와 맞다"* 는 보이지 않으므로 대조군을 대체하지 않는다는 점을 함께 적었다.

> **값의 성격을 정직하게 밝힌다.** 등재한 13건은 **실측이 아니라 내 추정**이다. 공개 단가 자료를 열어 확인한 값이 아니며, 그래서 전건 `source: null` · `confidence: 가정`이고 `derivation_method`에 무엇을 근거로 그렇게 잡았는지 적었다. 회신이 오면 대부분 바뀔 것이고, **바뀌어도 코드는 바뀌지 않는다는 것**이 이 작업의 결과다.

### 4. 2.15 ① 표 수용기준 전개 — spec v0.9 / 작업 목록 v1.6

`(표)` 캡션 1건에 표 전체가 묶여 있어 **테스트 1개로 자원 9종·편익 11종·지표 13종 전체가 매핑 완료 판정**을 받을 수 있었다. NFR-107이 막으려던 상태가 게이트를 켜기도 전에 성립해 있었다. 표 10건을 검토해 **9건 전개 / 1건 폐기**했다.

| 대상 | 키 | 건수 |
|---|---|---|
| `FR-102-AC1` 자원 유형 | `.PV`~`.Genset` (`core/der/<tag>.py` 레지스트리 키) | 1 → 9 |
| `FR-401-AC2` 편익 항목 | `.SelfConsumption`~`.CarbonCredit` | 1 → 11 |
| `FR-402-AC2` 배타 4유형 | `.A` `.B` `.C` `.D` | 1 → 4 |
| `FR-601-AC2` 단독 소유 | `.cost`~`.reference` | 1 → 6 |
| `FR-601-AC5` 부기 7종 | `.value_unit`~`.confidence` (§7.2 스키마 컬럼명) | 1 → 7 |
| `FR-611-AC3` 관점별 취득원가 | `.OWNER` `.SOCIAL` `.GOV` | 1 → 3 |
| `FR-703-AC1` 경제성 지표 | `.npv`~`.fiscal-pv` | 1 → 13 |
| `FR-801-AC7` 2단계 프리셋 | `.quick` `.full` | 1 → 2 |
| `NFR-107-AC1` 매핑 형식 | `.auto` `.manual` | 1 → 2 |
| `NFR-107-AC5` 수동 분류 | **전개하지 않고 폐기** — `manual-checks.yaml`의 사본이었다 | 1 → 1 |

수용기준 **252 → 299건**, Must-have 미매핑 기준선 **214 → 261건**. 인용 14곳 교체, 범위 표기 3곳(`FR-611`·`FR-801`·`NFR-107`)을 전건 나열로 바꿨다 — **범위 표기는 점 키를 표현하지 못한다.**

함께 처리한 것: 잔존 리드인 4건(`FR-401-AC1`·`FR-402-AC1` 재정의, v0.8 `FR-402-AC3`「공통」 절 표제로 강등·폐기, `FR-611-AC2` 자립 문장화), 기준선 재실측 2곳, 조항 간 어긋남 3건(10.3 NPV 오라클·10.4 LCOE 문구·16.5 대조 케이스), 게이트 자기참조 금지 규칙, 생성기 꼬리말 정정.

> **전개가 드러낸 것 — 이것이 이 작업의 성과다.**
>
> `NFR-107-AC1`을 `.auto`/`.manual`로 가르자 **수동 검증 경로가 생성기에 구현조차 되어 있지 않다**는 사실이 미매핑으로 드러났다. 1건에 묶여 있던 동안은 자동 쪽이 채워져 있어 조항 전체가 충족된 것처럼 보였다. `FR-703-AC1`을 가르자 **B/C의 오라클이 §13.0.2 어느 순위에도 배정되어 있지 않다**는 것과, 10.3이 NPV를 순위 2로 느슨하게 선언해 왔다는 것이 함께 나왔다.
>
> **뭉뚱그린 조항은 틀린 것을 숨기는 게 아니라 *물어볼 자리*를 없앤다.**

> **작업 중 같은 함정에 다섯 번째로 걸렸다.** `FR-601-AC5`의 순번 키 불가 사유를 쓰면서 §16.5.2가 폐기한 나쁜 예를 **리터럴로 적었고**, 정본 대조가 그 줄을 잡았다. 아래 「반복해서 걸린 함정」과 같은 유형이다. 나쁜 예는 서술로 풀어 쓰고 리터럴로 적지 않는다.

한시 문서 `rslt/2.15-표수용기준-전개안.md`는 예정대로 폐기했다 (내용은 커밋 `ef72fcf`에 남아 있다).

### 0. 정본 표류 — 5일간 미탐지 (커밋 `69f57b1`)

`근거 표기 기준` 정본이 **2026-08-03T15:17:44에 개정되었는데 08-08까지 아무도 몰랐다.**

탐지 장치가 **양쪽 다 눈이 멀어 있었다**:

| 장치 | 왜 못 잡았나 |
|---|---|
| 로컬 `check_source_rules.py` | `DER_VAULT_ROOT` 미설정 → 볼트 대신 **저장소 사본으로 대조**. 사본이 오염되지 않았다는 것만 증명하고 정본 개정은 원리상 못 본다 |
| CI (`source-rules.yml`) | 볼트는 저장소 밖 로컬 경로다. **CI는 구조적으로 접근 불가** |

개정 내용 자체는 「관련 노트」 위키링크 1줄 추가였고 규범 절은 무변경이었다. §16.5.1 파생 4곳(FR-601·FR-603·UI-3·§7.2) 전수 재검토 결과 무영향. §16.5.3 절차 3~5를 이행하고 사본·lock을 재동기했다.

> **조치 필요 — 이것이 진짜 교훈이다.** 내용이 사소했던 건 운이다. 다음에 규범 절이 바뀌면 그때도 5일 늦게 안다.
> `DER_VAULT_ROOT`를 상시 설정하고, 볼트를 보는 대조를 **Windows 작업 스케줄러로 정기 실행**할 것. 오케스트레이션 도구가 필요한 일이 아니다. → **08-08 해소 (2.16)**

```powershell
# 볼트 경로는 사람마다 다르고 이 저장소는 공개 저장소다 — 리터럴로 적지 않는다 (SC-3)
python scripts/check_source_rules.py --require-vault      # DER_VAULT_ROOT 사용
python scripts/check_source_rules.py --vault "<볼트 경로>" --require-vault
```

> **이 코드블록에 실제 볼트 경로가 적혀 있었고, 2.3 검사가 그것을 잡았다.** 08-03에 lock 에서 볼트 경로를 뽑아내고 식별 정보 43건을 정제하면서도 **이 한 줄은 남아 있었다** — 사람이 훑는 정제는 반드시 남긴다.

### 1. 미인용 Must-have 23 → 1건 (커밋 `e36c447`)

23건을 spec 원문과 전수 대조해 세 갈래로 판정했다.

| 갈래 | 건수 | 조치 |
|---|---|---|
| 작업은 있고 `- 수용기준:` 필드만 비어 있던 것 | **17** | 인용 보강 (2.3·3.1·8.4·8.5·10.2·10.7·11.2·13.2) |
| 범위 누락 — 담당 작업이 없던 것 | **1** | **13.9 신설** (`FR-1003-AC2` PDF 심의자료 5부) |
| Phase 2 범위 — 이 목록에 없는 것이 정상 | **5** | 조치 없음 |

작업 목록 자신이 예측한 패턴(*"상위 작업이 범위로 포괄하나 하위 작업이 개별 인용하지 않은 조항"*)이 23건 중 17건으로 확인됐다.

### 2. 2.15 ②③ 완료 — 공란은 검사 우회였다 (커밋 `9e3d300`)

부록 A.1 배정표를 읽어 Phase를 채우고, 요약에 「Phase 미지정」을 집계하도록 했다. **Phase 미지정 43 → 9건.**

> **②가 실제로 한 일이 예상 밖이었다.** `check_task_mapping.py`는 처음부터 `Must-have and Phase in ("1","-")` 로 걸러 왔다. 그런데 **Phase가 43건 공란이라 `"-"` 예외 통로로 전부 새어 들어오고 있었다.** 채우자마자 Phase 2 조항 `FR-202-AC1`~`AC4`가 미인용 목록에서 자동으로 빠졌다.
>
> **필터가 없던 게 아니라 데이터가 없어 눈이 멀어 있었다.** 드러나지 않는 공란은 게이트를 조용히 무력화한다 — ③(미지정 집계)을 함께 넣은 이유다.

부수 수확: 부록 A.1 NFR 표의 `0 (Wave 0)` 행은 Phase가 아니라 Wave 라벨이며, 표 하단 주석이 *"§4.0 R-2 판정상 전건 Phase 1 배정"* 으로 못박고 있다. 그대로 읽으면 NFR-107·NFR-208이 본문과 충돌하는 것처럼 보인다.

### 3. FR-202 판정 정정 — Phase 데이터가 없으면 판단도 없다

1번에서 `FR-202`(N모델 동시 실행·비교)를 *"담당 작업 없음 → 신규 작업 필요"* 로 판정하고 7.7·7.8을 신설했다. **그런데 2번으로 Phase 열이 채워지자 부록 A.1이 FR-202를 Phase 2에 배정한 것이 드러나 두 작업을 제거했다.**

판정 당시 Phase가 공란이어서 근거가 없었다. **순서를 반대로 했다면(2.15 ② 먼저) 애초에 생기지 않았을 왕복**이다.

> **다만 여기서 진짜 문제가 하나 나왔다** — 17.1 DoD 1(에너지자립가구 2변형 **비교** 산출, SG-1 전반부)은 Phase 1인데 비교 기능 FR-202는 Phase 2다. 미해결 사항에 등재했다.

## 08-03에 한 일

### 1. 수용기준 ID — 순번 파생 폐지 (spec v0.8)

252건 전부가 이제 spec에서 ID를 직접 들고 있다.

```markdown
  - Acceptance Criteria:
    - **AC1** 속성: `name`, `tag`, `dt`, ...
    - **AC2** 메서드: `capex()`, `fixed_om()`, ...
```

v0.7까지는 선언 순서에서 ID를 뽑았다. 그래서 AC를 중간에 삽입하면 이후 번호가
전부 밀렸고, 작업 목록 인용과 테스트 마커가 **조용히 다른 조항을 가리켰다**
(v0.7에서 실제 7건). 파서를 고쳐서는 막을 수 없는 종류의 사고다.

| 검증 | 결과 |
|---|---|
| ID 보존 | **252건 전건 일치** — 인용 106건·수동 대장 8건 무손실 |
| 중간 삽입 | AC3·AC4 사이에 AC9 삽입 → `AC1 AC2 AC3 AC9 AC4`. **AC4가 밀리지 않음** |
| ID 누락 / 중복 | 각각 결함 보고 + 종료 코드 2. 조용한 순번 부여 없음 |

파서에서 상태 기계가 4종(`collecting`/`table_open`/인라인/암묵 보충) → 1종
(`in_field`)으로 줄었다. **번호는 연속일 필요가 없다** — AC3을 지우면
AC1·AC2·AC4로 남는 것이 정상이고, 메우려고 재배열하면 v0.7 사고를 재현한다.

전환 중 세 번 걸려서 `Validation` 필드와 NFR-103·NFR-107의 예외를 찾아냈다.

### 2. 인용 형식 이탈 검출 (작업 목록 v1.3)

`- 수용기준:` 필드로 인용 수집을 한정한 대가가 있었다 — **형식이 어긋난 선언은
집계에서 통째로 빠진다.** 6.6의 하위 단계 2건이 블록인용 안에 콜론 없이 적혀
있어 `FR-106-AC2`~`AC6`이 「미인용 Must-have」 **오탐 5건**이 되어 있었다.
없던 인용을 만드는 오류가 아니라 **있는 인용을 지우는** 오류이므로 통과 화면만
봐서는 드러나지 않는다.

조치: 해당 2건을 표준형 하위 작업 `6.6.S4`·`6.6.S5`로 승격하고,
`check_task_mapping.py`에 「형식 이탈 인용 의심」 검사를 추가했다.
**서술과 선언을 뜻으로 가르려 하지 않는다 — 표준 형식이 아니면 사람이 본다.**

미인용 Must-have 28 → **23건**.

### 3. 공개 저장소 전환 준비

- 식별 정보 **43건 정제** — 기관명·특정 실증 지역·로컬 경로·작성자
- 볼트 경로를 lock에서 제거하고 `DER_VAULT_ROOT` 환경변수 / `--vault`로 이전.
  볼트 유·무 양쪽에서 대조 통과 확인
- `README.md`(공개 대문) · `.gitignore`(SC-3 안전망) · `structure.md`(구조 지도) 신설
- **git 저장소 초기화** — 정제 완료 후에 초기화했다. 정제 전 커밋을 남기면
  공개 시 이력에 그대로 남는다

> **남은 것 하나**: `docs/domain-rules.md`에 배전사업자명 1건이 있다. 이 파일은
> 정본과 **바이트 동일**해야 하므로 여기서 고칠 수 없다. 고치려면 볼트의 정본을
> 개정하고 사본·lock을 다시 떠야 한다.

---

## 08-02에 한 일

### 1. spec v0.4 → v0.7

| 판 | 핵심 |
|---|---|
| v0.4 | **TDD 우선**(§13.0) · **자원별 검증 케이스 매트릭스**(§13.2) · **병렬 개발 구획**(§16) 신설 |
| v0.5 | 정본 재대조 — 신뢰도 `미확인`→`가정`, 부기 6종→7종, **FR-106 `CommonAsset`**·**FR-611 기지원 설비** 신설, 투입 전제(3~5 에이전트) 명시 |
| v0.6 | **인용 좌표계 재확정** — 절 번호 → 원칙 번호. 정본 개정 감지 절차(§16.5.3) |
| v0.7 | **우선순위 미지정 34건 해소** · `CommonAsset`을 `core/asset/`으로 분리(WP-1f) |

### 2. 검증 인프라 구축

세 도구 모두 **음성 테스트로 감지 능력을 확인**했다. 통과만 보고 끝내면 아무것도 검사하지 않는 장치일 수 있다(§13.0.1 ④와 같은 이유).

- `check_source_rules.py` — 음성 테스트 10종 통과
- `gen_traceability.py` — 음성 테스트 5종 통과, 3회 재실행 멱등
- `check_task_mapping.py` — 오늘 신설, 버그 2건 수정

### 3. 작업 목록 생성 (Step 2)

상위 17건 / 하위 150건. Phase 1 Must-have FR 45건 **전건 커버(기계 검증)**.

---

## 미해결

| 구분 | 항목 | 영향 | 조치 시점 |
|---|---|---|---|
| 입력 | **Q-8** V2G 계량·정산 제도 존부 | 미확인이므로 편익 **전액 0**으로 등재(비활성). 충전기 비용은 계상되므로 **현 결과는 V2G에 보수적**이다 | 16.1b 요청 대상 |
| 입력 | **Q-13** 호스팅 운영 주체·예산 | 3.6·17.7 배포 형태. 로컬 컨테이너(2.13)는 무관. 가정값 월 2만원(하단 0) | 3.6 착수 전 |
| **검증** | **Q-4 미확보 → 17.4·SG-4 판정 유예** | **가정할 수 없다** — 정박점을 우리가 만들면 자기충족 검증이 된다. 통과로도 실패로도 세지 않고 미판정으로 남긴다 | 회신 시에만 해소 |
| **검증** | **Q-5 미확보 → §13.3 1차 대조군 유예** | 동상. 16.5의 6종은 해석해(순위 1)로 대체하되 *실무 전제와의 정합*은 대체되지 않는다 | 회신 시에만 해소 |
| 검증 | **가정값 위의 골든 3종** | 16.4 기준값이 대장 가정에 묶인다 — 통과는 "어제와 같다"이지 "맞다"가 아니다. 가정 갱신 시 재산출 필요 | 16.4 착수 시 |
| 기술 | **TU-2** Litestream vs Turso | 3.6 프로토타입 후 결정 | Wave 1 |
| 기술 | **TU-6** 누진·TOU 혼재 모델링 — **설계 방침 확정 (08-09), 약관 확인은 남음** | **한 계량점에 누진과 TOU를 동시에 적용하는 요금제는 실재하지 않는다.** 「혼재」는 한 단지에 **계량점이 여럿**이라는 뜻이다 — 가구부(주택용 N개, 각각 누진) / 공용부(일반용 고압 1개, TOU) / 거래분(직접거래). 따라서 요금 엔진은 **계량점 단위 독립 계산 + 합산**으로 짓고, `FR-501-AC6`(동시 적용)은 «한 계량점에 두 요금제»가 아니라 «여러 계량점의 합산»으로 읽는다. 이렇게 두면 5.3과 5.4가 부딪치지 않고 요금 엔진을 두 번 짓지 않는다. **약관 검토는 「계량점 구성이 실제로 이러한가」를 확인하는 것으로 좁혀진다** — 다르면 여기로 돌아온다 | 5.3 진행 중 · 약관 확인 시 재검토 |
| 기술 | **TU-7** IRR·할인회수기간 셀 수식 한계 | 13.5 실현 범위. 대응책 확정 — *값 + 산식 주석 병기* | 13.5 착수 시 |
| ~~도구~~ | ~~매핑표 Phase 열 43건 공란~~ | **08-08 해소** — 부록 A.1 해석 추가. 잔여 9건은 전건 Should-have | ✔ |
| ~~도구~~ | ~~표 수용기준 10건 뭉개짐~~ | **08-08 해소** — 9건 전개 / 1건 폐기. 수용기준 252 → **299건** | ✔ |
| ~~도구~~ | ~~`NFR-107-AC1.manual` 미구현~~ | **08-08 해소** — `ast` 기반 마커 수집으로 구현. 클래스·모듈 수준 상속 포함. 수행 기록 없는 스텁은 종료 코드 2 | ✔ |
| ~~도구~~ | ~~CI 매핑 잡이 spec 결함 시 조용히 통과~~ | **08-08 해소** — `\|\| true`로 종료 코드를 버려 결함(코드 2) 시 파일이 안 바뀌고 diff가 통과했다. 실측 재현 후 폐쇄 | ✔ |
| ~~검증~~ | ~~B/C 오라클 미배정~~ | **08-09 해소 (spec v0.12)** — §13.0.2 **순위 1**. B/C는 IRR과 달리 반복해가 아니라 **두 할인 합산의 몫**이므로 닫힌 형태다. **분자·분모를 각각 원 단위 완전 일치로 판정하고 비율에는 별도 오차를 두지 않는다** — 비율에 오차를 주면 «편익도 비용도 과대» 와 «둘 다 정확» 이 같은 값을 내어 어느 쪽이 틀렸는지 말하지 못한다 | ✔ |
| 검증 | **16.5 엑셀 대조에 MIRR·B/C 누락** | 현재 4종(NPV·IRR·회수기간·융자 상환). 10.3이 대조하겠다는 5종 중 2종의 기대값 출처가 없다 | 16.5 착수 시 |
| 범위 | **`FR-611-AC3` ↔ `FR-704` 관점 어휘 불일치** | `FR-704`는 사업자/참여 주민/정부인데 `FR-611`은 사업자·주민(병합)/사회(FR-704에 없음)/정부다. 중립 키(`.OWNER`·`.SOCIAL`·`.GOV`)로 어긋남을 고착시키지 않았으나 **해소하려면 `FR-704` 개정이 선행**된다 | 10.6 착수 전 |
| 판단 | **8.3을 배타 유형별로 쪼갤 것인가** | 한 작업이 `AC2.A`의 차단 100%(음성)와 `AC2.B`~`.D`의 오탐 0(양성)을 동시에 진다. 검사 방향이 반대이고 4행이 각각 `NFR-102`·`FR-404`·`FR-704`·`FR-504`로 위임되어 네 구획에 걸린다 | 8.0 착수 시 |
| ~~계약~~ | ~~`dispatch()`의 「한 해」 ↔ 계약 테스트 `steps=24` 충돌~~ | **08-08 해소** — 「연초부터 연속 `steps` 스텝」으로 확정. `year_fraction` 신설, 「짧은 창은 긴 창의 앞부분」을 계약 테스트가 검사 | ✔ |
| ~~계약~~ | ~~`value_streams()` 훅 부재~~ | **08-08 해소** — `DER`에 기본 `return ()`. 편익 객체가 아니라 **tag 문자열**을 돌려준다(객체는 NFR-208-AC2 위반) | ✔ |
| ~~계약~~ | ~~부가세 분리 자리 없음~~ | **08-08 해소** — `capex_vat()` 추상 신설. 실측하니 **다섯**이 지어냈고 ESS 하나는 아예 없었다 | ✔ |
| ~~계약~~ | ~~그 밖에 자원 구현이 자작한 것~~ | **08-08 대부분 해소** — 운전 방법 접근자 이름 고정 · `salvage_value()` 명목액 명문화 · `escalation_rate` 통일 · `DispatchResult` 에 `unmet_*`·`notes` · `AllocationResult` 계약 이관 · `CommonAsset` SW/HW 분리 잔존가치. **요금 시계열은 남았다**(아래 행) | ✔ |
| ~~계약~~ | ~~요금·연료 단가 시계열을 놓을 자리 없음~~ | **08-09 판단 완료 (1.12 ⑥)** — `DispatchContext` 에 **가격 신호**로 싣는다. 별도 계약을 두지 않은 이유는 ctx 가 이미 *"엔진→자원 유일 통로"* 이기 때문이다. 「요금」이 아니라 「신호」인 것이 요점 — 누진·TOU·특례 해석은 WP-3 이 하고 **결과인 스텝별 단가 하나**만 실린다 | ✔ |
| 계약 | **자원의 잔여 요금 인자 3건 — 이관 대기** | 실측: `EV_V2G.avoided_price_won_per_kwh` · `HeatPump.price_profile_won_per_kwh` · `HeatPump.elec_price_won_per_kwh`. **`avoided_price` 는 운전이 아니라 편익 화폐화이므로 ctx 가 아니라 WP-4 로 간다** — 그 판단은 8.0 이 편익 소유권을 확정할 때 내려진다. 지금 0건을 요구하면 `RC-HP`·`RC-EV` 검증 케이스를 함께 고쳐야 한다. `KNOWN_TARIFF_DEBT` 가 부채를 고정하며 **늘면 즉시 빨개진다** | 8.0 착수 시 |
| ~~문서~~ | ~~**공통 비용 5종의 마커 부착이 갈렸다**~~ **R31 해소 — `FR-101-AC5` 신설** | `FR-104`는 열화·수명·교체·잔존가치 조항이라 **C-1 CAPEX·C-2 고정 O&M·C-3 변동 O&M에 대응하는 AC가 없다.** 6개 구획이 각자 `FR-101-AC2` 등으로 달았다. spec에 조항을 신설하거나 6.0.S1의 인용을 정정해야 한다. **계약 개정에 포함하지 않았다** — spec 개정이므로 §16.5 절차가 따로 필요하다 | spec 다음 개정 시 |
| ~~품질~~ | ~~NFR-206 초과 6건~~ | **08-08 판단 완료** — `ast` 로 갈라 재자 **코드 줄 수로는 전건 상한 이내**(최대 482)였다. 조항의 근거(DER-VET `Params.py` 1,830줄)가 겨냥한 코드 스프롤에 해당하는 파일은 없고 초과분은 전부 설명 밀도다. `--code-strict` 를 CI **차단**으로 켰다 | ✔ |
| ~~입력~~ | ~~`vat_rate=0.1` 이 대장에 없다~~ | **08-08 해소** — `track: fixed` 갈래를 신설하고 `tax.vat_rate` 등재(확정 · 부가가치세법 제30조). 함께 **v1.1이 남긴 기본값 갈림**(여덟 중 히트펌프만 0.1)을 통일하고 레지스트리 순회 계약 테스트로 고정 | ✔ |
| **계약** | **대장을 읽는 경로 — 계약 자리는 섰고 구현·주입은 남았다** | **08-09 (1.12 ⑦)** — `core/contracts/assumptions.py` 의 `AssumptionProvider` 가 읽기 경계를 고정했다. **없으면 `MissingAssumption` 으로 멈추며 기본값으로 메우지 않는다.** 남은 것 둘 — ⓐ 대장을 실제로 읽는 구현(WP-2, 작업 4.4) ⓑ **자원 생성 시 주입 강제**(WP-16, 작업 7.2). ⓑ 가 없으면 `vat_rate=0.0` 이 여전히 「주입되지 않음」인 채 통과한다 | WP-2 · WP-16 |
| ~~문서~~ | ~~**`SC-3`·`SC-5`·`SC-8` 이 추적표 밖이다**~~ **v0.11 해소 — 이 행이 낡아 있었다 (R31 실측)** | SC 표의 행은 조항 ID 를 갖지만 수용기준 파서가 읽는 형식(`- **AC1**`)이 아니다. `@pytest.mark.req("SC-8")` 를 달자 생성기가 «실재하지 않는 인용» 으로 잡았다(실측). **Must-have 급 요구인데 17.9 미매핑 집계에 들어오지 않으므로, «전건 매핑» 이 성립해도 보안 조항은 검증되지 않은 채로 남는다.** SC 표를 수용기준 형식으로 승격할지, 별도 축으로 셀지 판단 필요 | spec 다음 개정 시 |
| 도구 | **NFR-202 판정 사각지대 — 대장 값 17건** | \|값\| < 1,000 은 판정하지 않는다(4·5·15·30 은 인덱스·시간과 구별되지 않는다). `tariff.hv_single_contract.avg`(150원/kWh)·`fee.direct_trade_support`(5원/kWh)가 여기 있고, **요금 단가는 소스에 새기 가장 쉬운 값**이다. 요금 엔진(WP-3) 착수 시 다른 판정 수단이 필요하다 | 5.0 착수 시 |
| 품질 | **총 줄 수 초과 6건은 남는다** (경고) | 상한값·판정 기준을 바꾸지 않았기 때문이다 — NFR-206 문면은 「500줄」이고 코드 기준으로 재정의하려면 **spec 개정(§16.5 절차)** 이 필요하다. 검사를 통과시키려고 규칙을 고치는 것은 이 저장소가 반복해서 경계해 온 유형이다. 조항을 개정할지, 파일을 쪼갤지, 경고로 둘지는 **다음 spec 개정 때 판단** | spec 다음 개정 시 |
| ~~문서~~ | ~~교체 연도가 spec ↔ Wave 0 참조 구현 사이에서 갈림~~ | **08-08 해소** — `ReferencePV`가 수명 연도(12년차)에 계상해 §13.2.2 C-4(*수명 도달 **다음** 연도 초*)와 한 해 어긋나 있었다. WP-1a가 짚었고 13년차로 정정 | ✔ |
| **범위** | **17.1 DoD 1 ↔ FR-202 Phase 어긋남** | 17.1(2변형 **비교** 산출, SG-1 전반부)은 Phase 1인데 비교 기능 FR-202는 부록 A.1이 Phase 2에 배정. 7.5 템플릿 2변형을 각각 실행해 나란히 놓는 것으로 충족되는지, 아니면 FR-202-AC1·AC2를 Phase 1로 당길지 판단 필요 | 17.1 착수 전 |
| ~~도구~~ | ~~AC 단위 Phase 표기를 검사기가 못 읽음~~ | **08-08 해소** — 수용기준 단위 Phase(`[Phase N]` 줄 끝 표기) 도입. 미인용 1 → **0건** | ✔ |
| ~~운영~~ | ~~정본 표류 탐지 장치가 꺼져 있음~~ | **08-08 해소 (2.16)** — `DER_VAULT_ROOT` 사용자 환경변수 + 작업 스케줄러 매일 09:10 + 표류 마커 파일. 네 경로 실측 확인 | ✔ |
| **범위** | **분석기간을 어느 층이 소유하는가 — ✔ R30 이 절반을 닫았다** | **케이스 실행 경로는 배선됐다** — `core/casegrid/e2e_runner.py` 가 자원을 세운 직후 `check_analysis_period()` 를 부르고, `horizon_years` 인자가 검사와 프로포마 계산 **양쪽**에 쓰인다. 아래는 R24 시점 문면이며 **「부르는 코드가 0곳」은 이제 틀렸다**. 남은 것은 *사용자가 고르는* 분석기간의 소유자다. R24 가 `core/cba/proforma.py::check_analysis_period()` 로 상한(최장 자원 수명 × 2)을 **던지는 코드는 놓았으나 부르는 코드가 0곳**이었다. 값이 함수 인자(`analysis_end_year`)로만 떠다니고, 자연스러운 자리인 `Scenario` 는 **`DV-11` 이 명시적으로 금지**한다(규제 프로파일·요금표·할인율·분석기간 필드 보유 불가). 즉 「배선을 잊었다」가 아니라 **놓을 자리가 계약상 비어 있다.** 후보는 `AssumptionSet`(전제 층) 또는 케이스그리드·앱 조합 층이며, 고르면 그 층이 `check_analysis_period` 를 부르는 것으로 닫힌다 | Phase 1 인수 전 |
| ~~검증~~ | ~~`DV-4` 후반 「윤년 처리 규칙 명시」 — 강제되지 않고 테스트가 반대를 고정한다~~ | **R25 해소.** `core/contracts/units.py` 의 `LEAP_YEAR_POLICY` 가 규칙을 선언한다 — **평년(365일) 고정 · 366일 시계열은 받지 않는다 · 그 하루의 처리는 호출측 기록**. 자리를 거기로 정한 이유는 스텝 수 상수를 쓰는 쪽(`core/der/*` 계산 층과 `infra/tsstore.py` 저장 층)이 **전부 그것을 받아 가기 때문**이다. 흩어져 있던 주석 둘은 선언을 가리키게 바꿨다(사본을 만들지 않았다). 붙드는 것은 `tests/contract/test_leap_year_policy.py` — 문면 · **스텝 수가 연도를 받을 수 없다** · **윤년 행 수가 허용 목록에 없다**. 닿는지는 `test_tsstore.py::..._hands_over_the_policy` 가 본다(8784행이면 조치 자리에 선언 그대로). 변이 6종 전건 잡힘 | ✔ |
| **범위** | **`VPP` 가 계약에 앉는가 — Phase 2 착수 전 판단** | 자원이 다른 자원을 품으면 ⓐ `dispatch()` 가 하위 자원을 순회해야 하고 ⓑ 하위 자원 비용이 VPP 에서 다시 계상될 위험이 있다(FR-402-AC2.B). **구현으로 우회하지 말 것** — 견디지 않으면 §16.2 절차 | Phase 2 착수 전 |
| ~~품질~~ | ~~**mypy strict 5건 실패**~~ **해소 — 이 행이 낡아 있었다 (R31 실측)**. `python -m mypy` 가 **102파일 무결**이다(R31 종료 시 실측). 08-09 에 적힌 5건은 그 뒤 상환됐고 `heatpump.py` 의 `Year` 불일치도 남아 있지 않다. **CI `tests` 가 `mypy strict` 를 차단으로 돌리므로 다시 쌓일 수 없다** — 「아무도 돌리지 않아 쌓였다」는 원인이 사라졌다 | ✔ |
| ~~검증~~ | ~~`NFR-208-AC1`~`AC3`·`M1` 미매핑~~ | **08-08 해소 (2.8)** — 세 위반을 각각 심어 **그 계약이** BROKEN 이 되는지 확인하고, 심지 않은 상태의 `4 kept` 를 양성으로 함께 둔다. 심는 모듈은 `_` 로 시작해 레지스트리 스캔은 피하되 import 그래프에는 잡힌다 | ✔ |

---

## 다음 세션 시작 시

> # ⏹ 2026-08-14 R30 종료 · **오케스트레이션은 끝났다. 단독으로 이어간다**
>
> 사용자가 R24 를 닫으며 **워커 없이 단독 진행**으로 전환했다. 그러므로
> **아래 R15 절의 「레인 창을 다시 확보한다」부터는 더 이상 하지 않는다** —
> 이력으로 남긴다(같은 방식으로 돌아가야 할 때를 위해).
>
> ### 먼저 읽을 것 — 순서대로
>
> | 순서 | 파일 | 무엇이 있는가 |
> |---|---|---|
> | 1 | **이 파일 `## 지금 할 일` 의 「▶ R32 에 할 일」 표** | **다음에 무엇을 만들 것인가.** 여섯 줄이고 **번호 순서대로 한다.** 1번이 R31 이 남긴 빈 자리다(소비자는 있고 생산자가 없다) |
> | 2 | **`docs/decisions-2026-08-14-R31.md`** | **결정 아홉의 판정·근거.** 사용자가 「확장성 기준으로 대신 결정하라」고 위임해 R31 이 전건 확정하고 **전건 구현했다** — 그 문서의 착수 순서표는 **전건 ✔** 이므로 「다음에 할 일」은 위 1번이 갖는다 |
> | 3 | **`docs/clause-recheck-2026-08-14.md`** | `I` 잔여 12건의 **조항 실물 대조 판정**. 무엇이 절반이고 무엇이 닫혔는지, 닫으려면 무엇을 만들어야 하는지가 조항별로 있다 |
> | 4 | `git log --oneline -12` | 라운드마다 **경위를 커밋 메시지에 적었다.** 제목만 읽어도 무엇이 왜 바뀌었는지 이어진다 |
>
> **`.orch/` 는 더 이상 읽지 않아도 된다.** 추적 밖이고 오케스트레이션 산출물
> 이며, 거기 있던 판정 중 **아직 유효한 것은 위 2번 문서로 옮겼다.**
>
> ### 시작 명령 — 이 순서로
>
> ```bash
> git status -sb                                   # 비어 있어야 한다
> gh run list --limit 2                            # tests · source-rules 둘 다 success
> python scripts/check_precommit_installed.py      # 훅이 걸려 있는가 (R26 신설)
> ```
>
> **커밋할 셸의 PATH 앞에 의존성이 설치된 인터프리터를 둔다** — `ruff` 훅이
> `python -m ruff` 라 PATH 의 첫 `python` 을 잡고, 그것이 다르면 **커밋이
> 막힌다**(R30 실측). 훅이 옳게 막은 것이므로 설정을 고치지 않는다.
>
> **`PYTHONUTF8=1` 을 켜고, 의존성이 설치된 인터프리터를 쓴다** — 위 §절차의
> 경고 두 개를 먼저 볼 것. 켜지 않으면 게이트 6건이 로컬에서만 빨간불이 된다.
>
> ### 다음에 할 일 — **§「지금 할 일」의 「▶ R32 에 할 일」 표가 정본이다**
>
> **여기 옮겨 적지 않는다** — 같은 사실이 두 곳에 있으면 한쪽만 고쳐지고, 이 파일
> 머리말이 그것을 규칙으로 박아 두었다. 아래는 **결정 이력**이다.
>
> <sub>이하 R31 결정 시점 문면 (이력 — 아홉 전건 확정·구현 완료)</sub>
>
> **R31 에 아홉을 전건 확정했다.** 판정·근거·착수 순서는
> **`docs/decisions-2026-08-14-R31.md`** 가 정본이다 — **여기 옮겨 적지 않는다.**
> 같은 사실이 두 곳에 있으면 한쪽만 고쳐지고, 이 파일 머리말이 그것을 규칙으로
> 박아 두었다.
>
> **다음에 코드를 놓을 자리는 그 문서 §「착수 순서」 2번이다** — `AssumptionSet`
> 에 `price_basis`(`DV-7`) + `analysis_years`(§7.1 O-1). 둘이 같은 자리이므로
> 함께 넣고, 그것이 서야 `FR-205` 조립기(순서 3번)가 전제를 읽는다.
>
> ⚠ **아래 표는 R30 시점의 「정해야 할 것」이며 이력이다.** 아홉 행 전부
> 결정됐고, 각 행에 대한 답은 위 문서의 같은 번호 절에 있다. **표를 근거로
> 다시 결정하지 말 것** — 근거를 읽지 않고 다시 정하면 같은 판단을 두 번 하고
> 두 답이 저장소에 남는다.
>
> | # | 정해야 할 것 | 정해지면 열리는 것 |
> |---|---|---|
> | **1** | **「전체 파라미터」의 기준** — `ModelConfig` 에 목록이 없고 `DERConfig.params` 는 `dict[str, Any]` 다 | `UI-1-AC1` 마법사·고급 모드 · `FR-801-AC7` 케이스 선택 화면. **기준 없이 화면을 지으면 무엇이 「전체」인지 검증할 수 없고**, 그 자리가 R29 에 걷어낸 항진 테스트가 있던 곳이다. ★ **답을 지어낼 필요가 없을 수 있다** — 자원 파라미터는 이미 기계가 읽는다(`inspect.signature(cls.__init__)` — PV 20개·ESS 25개). **레지스트리에 등록된 자원의 시그니처를 기준으로 삼으면** 자원 파일을 놓는 순간 카탈로그가 따라오고(`NFR-207` 「파일 하나」와 어긋나지 않는다), 손으로 둔 목록이 갈릴 자리가 없다. **후보안이며 사용자 확인이 필요하다** |
> | **2** | **`FR-205` 정산 결정 5건** (+ `payer` 가 `ClassVar` 라 계약 개정이 붙는다) | 정산엔진 · `FR-205-AC1` 스텁(금액이 늘 `0.0`). 계산은 `core/valuestream/` 에 다 있고 **없는 것은 구조별로 어느 편익을 켜고 돈이 누구에게 가는가**다 — 근거 없이 채우면 도메인 규칙 발명이다. 설계서 `.orch/R13-WP24G-설계서.md` — ⚠ **`.orch/` 는 git 추적 밖이라 이 작업 사본에만 있다.** 새 클론에서 시작한다면 그 설계서는 없고, **결정 다섯의 목록은 이 행과 「지금 할 일」 2번에 남아 있다** |
> | **3** | **`DV-6` 「최근접」 기준과 「경고」 채널** | 요금표 폴백 구현. ⚠ **`_select_effective` 의 `KeyError` 타입을 바꾸면 `app/routers/` 의 `except KeyError` 8곳이 조용히 안 잡는다**(R30 재실측: `models.py` 3 · `regulation.py` 2 · `scenarios.py` 3). **그 8곳을 함께 고치는 것이 이 항목의 절반이다** — 결정만 받고 착수하면 사고가 난다 |
> | 4 | **`AssumptionSet` 에 실질/명목 선언 필드** | `DV-7` — 원문이 선언 자리를 `AssumptionSet` 으로 못 박는데 **그 필드가 스키마에 없다**. 실물은 `core/assumption/provider.py:13`(`name`·`version`·`items`·`overrides`·`reasons`) |
> | 5 | **변형별 결과를 `CaseResult.metrics` 에 어떻게 담는가** | `FR-607-AC1` 배선. **표시할 리포트 층도 함께 정해야 한다** — 지금 배선만 하면 소비자 없는 생산자가 하나 더 생긴다 |
> | 6 | **비공개 시드의 덮어쓰기 의미** | `FR-1101-AC2` — 로더를 `tests/ci/` 에서 제품 경로로 옮기는 것만으로는 닫히지 않는다 |
> | 7 | **편익 계상 내역을 리포트 어디에 놓는가** | `FR-402-AC6` — 4버킷 자료구조는 이미 있다. `_SECTIONS` 는 **`FR-1003-AC1` 의 계약**이라 §16.5 개정이 붙는다 |
> | 8 | **케이스 축 외 변수를 집계할까 고정할까** | `FR-803-AC1` — 지금 27케이스에 2축을 주면 **좌표당 3중복**이고, 프리셋 축이 문자열이라 차트가 원리상 그려지지 않는다 |
> | 9 | **사용자가 고르는 분석기간을 어느 층이 소유하는가** — `Scenario` 는 `DV-11` 이 **명시적으로 금지**한다 | ✔ **R30 이 절반을 닫아 값이 내려갔다**(전에는 3번이었다). **케이스 실행 경로는 이미 배선됐다** — `run_single_case_e2e` 가 자원을 세운 직후 `check_analysis_period()` 를 부른다. 남은 것은 *사용자가 고르는* 값의 자리뿐이고, **1번을 정할 때 함께 정해질 성격**이다(분석기간도 그 「전체 파라미터」의 하나다). 정해지면 그 층에서도 같은 함수를 지나게 한다 |
>
> **자료를 기다리는 것**(코드로 못 닫는다): `FR-204-AC3` COP 곡선 · `NFR-104`
> 외부 근거(`Q-4`·`Q-5`) · `MC-1` 일정 · 판정불가 3건.
>
> **`MC-1` 은 결정이 아니라 한 줄 기입이다** — `docs/manual-checks.yaml` 의
> `planned_at`·`planned_owner` 가 `null` 이고, **이것 하나가 Phase 1 인수를
> 「판정불가」로 잡고 있다.** 「언제·누가」만 받으면 즉시 닫힌다(수행 자체는
> 심의 경험자 3명 대상 설명 재현이라 코드로 대체할 수 없다).
>
> 각 항목의 실물 위치와 근거는 **`docs/clause-recheck-2026-08-14.md`** 에 있고,
> **그 항목들을 어떻게 정했는지는 `docs/decisions-2026-08-14-R31.md`** 에 있다.

<details>
<summary>2026-08-10 R15 시점의 인계 절차 (오케스트레이션 시절 — 이력)</summary>

> # ⏸ 2026-08-10 R15 종료 · **워커와 오케스트레이터가 모두 초기화된다**
>
> 사용자가 R15 를 닫으며 **모든 워커와 오케스트레이터 세션을 초기화**한다.
> 그러므로 **어느 세션의 문맥도 이어지지 않는다.** 아래가 출발점이다.
>
> ### 먼저 읽을 것 — 파일 둘, 순서대로
>
> | 순서 | 파일 | 있는가 |
> |---|---|---|
> | 1 | **`.orch/state.md`** — 라운드 인계(레인·핸들·완료패턴·R16 배분표·겪은 함정) | **git 추적 밖**(`.gitignore:63`). 작업 디렉터리가 그대로면 있다 |
> | 2 | **이 파일** — 산출물 판본·게이트 실측 수치·할 일 | **추적 안.** 원격에 있으므로 새로 클론해도 있다 |
>
> **`.orch/state.md` 가 없으면** (새 기계·새 클론) 그 라운드 운영 정보는
> 복원되지 않는다. 그때 필요한 것은 아래 셋이고 **전부 이 파일과 원격에 있다** —
> 이 절 · 「지금 할 일」 표 · `git log`(라운드마다 경위를 커밋 메시지에 적었다).
> **다시 만들어야 하는 것은 감시 명세뿐이다**(레인 이름 · 터미널 핸들 · 완료패턴).
>
> ### 초기화 뒤 할 일 — 이 순서로
>
> ```bash
> git log --oneline -6        # 라운드별 경위가 제목에 있다
> git status -sb              # main...origin/main, 비어 있어야 한다
> gh run list --limit 2       # 둘 다 success 여야 한다
> ```
>
> **① 레인 창을 다시 확보하고 핸들을 실물로 확인한다.** 비우는 방법이 레인마다
> 다르다 — `claude2`·`claude3`·`claude5` 는 `/clear`, `opencode`(Sisyphus)는
> **`new`**, `agy` 는 **비우는 명령이 없어 창을 닫고 새로 띄운다**(`/clear` 가
> `C:/Program Files/Git/clear` 로 해석돼 프롬프트가 된다).
>
> **② `.orch/watch.tsv` 의 둘째 필드(핸들)를 실물로 교체하고, 셋째 필드(완료
> 패턴)를 새 라운드 번호로 조인다.** 조이지 않으면 낡은 보고가 다시 매치된다.
> 핸들 비교 시 **CRLF 를 지울 것** — 안 지우면 전건 불일치로 보인다:
>
> ```bash
> grep -P "^\w+	" .orch/watch.tsv | tr -d '
' | cut -f2 | sort > /tmp/spec.txt
> orca terminal list 2>&1 | grep -oE "term_[a-f0-9-]+" | sort -u > /tmp/live.txt
> comm -23 /tmp/spec.txt /tmp/live.txt    # 비어야 한다 = 명세 핸들 전건 생존
> ```
>
> **③ `--seed` 를 하지 않는다.** 화면을 비웠으니 소비할 낡은 보고가 없다.
>
> **④ 게이트를 전건 돌려 이 파일의 수치와 대조한다** (아래 §「현재 검사 결과」).
> **베끼지 말고 다시 돌릴 것** — 08-09 → 08-10 에 이 절이 5라운드 뒤처졌고
> 그동안 여기 적힌 수치가 근거로 쓰였다.
>
> ### 레인 다섯과 그 성격 (R16 배분의 근거)
>
> | 레인 | 모델 | 무엇을 주는가 |
> |---|---|---|
> | `claude2`·`claude3` | Sonnet | **설계 판단**이 필요한 것 — 「무엇을 만들 것인가」 |
> | `claude5` | Sonnet | 「지우면 무엇이 비는가」 판단 · Phase 1 DoD 자리 |
> | **`agy`** | **Gemini 3.1 Pro (Low)** | **판정이 이미 끝난 뒤의 일**을 한 디렉터리로 좁혀 준다. **`pro-high` 는 쓰지 않는다 — 할당량 소진 위험(사용자 판단)** |
> | `opencode` | GLM-4.7 | 같은 성격. **요약을 세는 명령을 브리프에 박아 넣을 것**(두 라운드 연속 요약이 표와 어긋났다) |
>
> **`agy` 브리프에 둘을 반드시 넣는다** — 반려 6건(세 레인 중 최다)의 반복
> 유형이다: ① 검증 명령의 **`rc=` 출력을 그대로 붙일 것**(「돌렸다」가 아니라
> 출력), ② 마커는 **`-AC` 접미사 필수**(`FR-601` 아니라 `FR-601-AC4`).
>
> **`/model` 은 `agy` TUI 안에서 동작한다 — 등급을 바꾸려고 창을 버릴 필요가
> 없다.** R9~R13 이 반대로 알고 네 라운드 동안 이 레인을 놀렸다.
>
> ### 사용자 결정 대기 셋 — **코드로 닫으려 하지 말 것**
>
> | 결정 | 무엇이 막혀 있는가 |
> |---|---|
> | `FR-202-AC1`~`AC3` **소유**(Wave 와 부딪침) | `check_task_mapping.py` 가 CI 에 **차단이 아니라 경고**로 있다 |
> | `FR-205` 정산 **결정 5건**(+ `payer` `ClassVar` 계약 개정) | 위 「지금 할 일」 2번 |
> | `MC-1` **일정** | Phase 1 인수의 「판정불가」 2 중 하나 |

</details>

<details>
<summary>08-09 시점의 시작 절차 (이력 — 명령 목록은 아래 §「도구」가 최신이다)</summary>

```bash
# 0. 밤사이 정본이 바뀌었는가 — 이 파일이 있으면 다른 일을 하기 전에 읽는다
#    (있음 = 표류 감지, 없음 = 정합. 감시기가 정합 시 지운다)
ls "$LOCALAPPDATA/der-evaluator/DRIFT-DETECTED.txt"

# `--require-vault` 를 반드시 붙인다 — 볼트로 읽지 못하면 종료 코드 2다.
# 붙이지 않으면 사본 대조로 내려가 「통과」가 뜨고, 08-08까지 5일간 그랬다.
python scripts/check_source_rules.py --require-vault

# ⚠ **마커를 고쳤으면 반드시 이것을 먼저 돌리고 나서 pytest 를 본다 (R28 실측).**
#    `test_dod9_phase_1_musthave_unmapped_zero` 는 **생성된 매핑표를 읽는다** —
#    재생성하지 않으면 옛 파일을 보고 초록불을 내고, CI 는 새로 만들어 빨간불이
#    된다. R28 에 그 차이로 push 뒤에야 알았다.
python scripts/gen_traceability.py
python scripts/check_task_mapping.py
python scripts/check_assumptions.py

# 커밋 전에 멈추는 장치가 **걸려 있는가** — 선언이 아니라 설치를 본다 (R25 신설)
#    CI 는 로컬 훅이 구조적으로 없으므로 스스로 판단을 보류한다.
#    R25 에 이것이 없어서, 설정은 초록불인데 훅은 하나도 걸려 있지 않았다.
python scripts/check_precommit_installed.py

# 도구 자신의 감지 능력 — 통과만 보고 끝내지 않는다 (§13.0.1 ④)
python scripts/negtest_traceability.py    # 음성 6 + 양성 2
python scripts/negtest_assumptions.py     # 음성 15
python scripts/negtest_file_size.py       # 음성 6 + 양성 2
python scripts/negtest_hardcoded_params.py  # 음성 7 + 양성 4 + 경계 5
python scripts/negtest_disclosure.py     # 음성 9 + 양성 6 + 경계 2

# 코드 검사. CI(tests.yml)가 같은 것을 돌지만 로컬이 빠르다
pip install -e ".[dev]"                # 처음 한 번 (diff-cover 포함)
python -m pytest -q                    # 565건
lint-imports                           # 4계약 kept
python -m ruff check core tests scripts
python scripts/check_file_size.py --code-strict   # 코드 스프롤 차단 (현재 0건)
python scripts/check_hardcoded_params.py         # NFR-202·205
python scripts/check_disclosure.py               # SC-3·SC-8 비공개 유입
python -m mypy                                   # NFR-204 strict
```

> **⚠ Windows 로컬에서는 `PYTHONUTF8=1` 을 켜고 돌린다 (R24 실측).**
>
> 켜지 않으면 서브프로세스 출력을 **cp949 로 디코드**하다 죽는다 —
> `tests/ci/test_import_boundaries.py` 4건 · `test_17_10_dod10` ·
> `test_marker_substance` 의 **6건이 빨간불**이 되고, `lint-imports` 는
> 아예 실행에 실패한다(`'cp949' codec can't decode byte 0xe2`).
> **CI 는 UTF-8 이므로 무관하다** — 그래서 로컬에서만 빨갛고 원인이 코드에
> 있는 것처럼 보인다. 실제로 R24 인수에서 그 6건을 워커 산출물의 결함으로
> 의심하는 데 한 번 썼다.
>
> **인터프리터도 확인할 것 — 저장소의 `.venv` 에는 의존성이 없다.**
> `argon2`·`sqlalchemy`·`ruff`·`mypy`·`lint-imports` 는 **그것을 설치한 쪽**에
> 있다(`where lint-imports` · `python -c "import argon2, sqlalchemy"` 로
> 확인한다). `.venv/Scripts/python.exe` 로 돌리면 수집 단계에서 15건이
> `ModuleNotFoundError` 로 죽는다. `python -m pytest`
> 를 파이프에 물리면 **없는 모듈로 죽어도 종료 코드가 0** 이다(파이프의
> 마지막 명령이 rc 를 낸다) — §「명령이 없는 것을 통과로 읽지 않는다」의
> 실제 재발이며 R24 에 한 번 걸렸다.

> **`lint-imports` 를 `python -m importlinter.cli lint` 로 대체하지 말 것 — 08-08 이전 판 status.md 가 그렇게 적어 두었고, 그것은 틀렸다.**
>
> `importlinter/cli.py` 에는 `__main__` 가드가 없다. `python -m` 으로 부르면 **모듈이 import되고 그대로 끝나며 종료 코드 0** 을 낸다. 검사가 아예 돌지 않는데 초록불이다 — `infra/` 에 위반을 심고 실측 재현했다. PATH 에 없다면 `python -c "from importlinter.cli import lint_imports_command; lint_imports_command()"` 를 쓴다(같은 조건에서 음성 1 / 양성 0 확인).
>
> **`pytest` 도 같은 이유로 종료 코드를 볼 것.** `python -m pytest` 는 정상 동작한다. **명령이 없는 것을 검사가 통과한 것으로 읽지 않는다.**

> **커밋 신원은 `noreply@example.com` 으로 고정돼 있다 (저장소 로컬 `user.email`).**
>
> 공개 저장소이므로 개인 이메일이 이력에 남으면 지울 수 없다 — 08-03에 식별 정보 43건을 정제하면서 이미 익명화한 결정이며, 08-08에 **미푸시 28커밋이 개인 Gmail 로 되어 있는 것**을 push 직전에 발견해 재작성했다. 전역 설정이 다른 값이어도 이 저장소에서는 로컬 설정이 이긴다. **로컬 설정을 지우지 말 것.**

**정본 대조는 이제 볼트를 자동으로 보고, 못 보면 멈춘다 (2.16).** `DER_VAULT_ROOT` 를 사용자 환경변수로 설정해 두었으므로 `--vault` 를 생략해도 볼트를 읽는다.

> **`--require-vault` 를 신설한 이유.** 이전 판 status.md 는 *"출력 첫 줄이 `볼트 .` 이면 잘못 돌린 것"* 이라고 적어 두었다. 그 문장은 맞지만 **사람이 매번 둘째 줄을 확인하는 것에 기대는 장치**이고, 08-03 개정을 놓쳤을 때도 「볼트 미접근」 INFO 는 출력에 있었다 — **읽히지 않았을 뿐이다.** 같은 방식으로 반드시 다시 실패한다. 이제 볼트로 읽지 못한 정본이 하나라도 있으면 **종료 코드 2** 다.
>
> **CI 에서는 켜지 않는다.** 볼트는 저장소 밖 로컬 경로이므로 CI 는 구조적으로 접근할 수 없고, 거기서 켜면 조항이 요구하지 않은 영구 실패가 된다. 켜는 곳은 **볼트가 있는 환경** — 이 절차와 작업 스케줄러다.

> **밤사이의 개정은 작업 스케줄러가 본다.** `DER-evaluator 정본 대조` 가 매일 09:10 에 돌며, 표류를 감지하면 `%LOCALAPPDATA%\der-evaluator\DRIFT-DETECTED.txt` 를 만들고 정합하면 지운다. 로그는 같은 폴더의 `vault-check.log` 다. 해제는 `scripts\vault_watch.ps1 -Unregister`.

> **매핑표 생성기의 종료 코드 1은 정상이다** — 미매핑이 남아 있다는 뜻이며 Phase 1이 진행 중인 지금은 그것이 맞다(현재 값은 `docs/traceability.md` 요약을 보라). 종료 코드 **2**가 결함(spec 수용기준 선언 오류)이며, 그때는 매핑표를 아예 만들지 않는다.

그다음 **「지금 할 일」 1번**부터 착수한다 — 2.3 비공개 데이터 커밋 차단이다.

> **`.gitignore` 는 이미 있고, 그것으로 충분하지 않다는 것도 파일 안에 적혀 있다** (*"방어선이 아니라 안전망이다"*). 필요한 것은 **커밋 전에 멈추는 장치**다 — pre-commit 훅 + CI 의 gitleaks. 둘 다 필요한 이유: 훅은 로컬에서 우회할 수 있고(`--no-verify`), CI 는 이미 커밋된 뒤에 본다.
>
> **음성 테스트를 먼저 생각할 것.** 이 검사는 「탐지 0건」으로 통과하며, 그 통과가 «비밀이 없다»는 뜻인지 «규칙이 아무것도 매치하지 않는다»는 뜻인지 결과만으로는 구별되지 않는다 — 08-08에 세 번 만난 형태다. 심어 둔 가짜 자격증명을 잡는지 확인하는 케이스가 CI 에 상주해야 한다.

> **`VPP` 계약 판단은 Phase 2 착수 전에 답해야 한다 — 미해결에 남겨 두었다.** `Boiler`·`Genset` 은 연료를 쓰는 단일 설비여서 기존 계약에 그대로 앉지만, `VPP` 는 **자원 집합 + 시장참여**다(FR-102-AC1.VPP: 집합 자원 ID 목록·운영수수료·시장참여 유형). 자원이 다른 자원을 품으면 ⓐ `dispatch()` 가 하위 자원을 순회해야 하고 ⓑ 하위 자원의 비용이 VPP 에서 **다시 계상될 위험**이 있으며(FR-402-AC2.B 인과 하류 편익) ⓒ 편익 `VPPMarket` 도 Phase 2 다. **계약이 견디는지 먼저 판단하고, 견디지 않으면 §16.2 절차로 개정한다** — 구현으로 우회하지 않는다.

</details>

---

## R50 이 「다음에 집을 것」에서 내린 행 (2026-09-01)

> **본문은 고치지 않았다** — 고치면 이력이 거짓이 된다. 아래 넷(★F·★G·★H·★K)은
> **R50 이 닫은 것**이고 나머지 아홉은 R47~R49 가 닫아 취소선으로 남아 있던 행이다.
> orch-dev 1-2절 — *「`~~취소선~~` 으로 그은 행은 인계가 아니라 이력이다」*.
> R50 이 닫은 넷의 경위는 이 파일 **R50 블록**의 WP 표가 갖는다.

| # | 무엇 | 시작 자리 | 나온 곳 |
|---|---|---|---|
| **★F** | **`Payer` 열거가 「배전사업자」와 「전력시장」을 구분하지 않는다** — `NWAs`·`CP` 가 둘 다 `GRID_OPERATOR` 를 쓰는데 **CP 정산은 전력시장 쪽**이다. 계약 파일이라 **§16.2 절차** 대상 | `core/contracts/valuestream.py::Payer` | R48/WP-C |
| **★G** | **`ESS` 의 `PV_SURPLUS` 충전이 가구 부하의 동시 소비를 보지 않는다** — 자원끼리 서로를 모르므로(`NFR-208-AC1`) **ESS 충전이 부하보다 앞선다**는 모델링 가정이 암묵적으로 들어 있다. 두 수요가 PV 를 넘으면 계통 수전으로 메워져 물리적으로 부정확하지는 않으나, **그 우선순위가 어디에도 선언돼 있지 않다** | `core/der/ess.py` · `core/casegrid/e2e_runner.py` | R48/WP-B |
| **★H** | **`TOU_ARBITRAGE` 가 편익을 하나도 내지 않는다** — R48 이 **자가소비로 가던 오매핑을 뗐고**(계통에 파는 운전인데 자가소비 편익이 붙었다) 그 자리를 **비워 두었다.** `ESS.tou_arbitrage_benefit()`(= 충·방전 요금차)은 구현돼 있는데 **호출자가 단위시험 하나뿐**이다. 편익 항목(`FR-401-AC2.<키>`)을 신설해야 한다(§16.5) | `core/der/ess.py::value_streams` · `core/valuestream/` | R48/WP-A |
| **★K** | `tests/report/test_assumed_operation.py` 의 **이름이 내용과 어긋난다** — 재던 붙임 7 둘째 표가 사라졌다(머리말에는 지금 무엇을 재는지 적혀 있다). 후보 `test_shaped_run_invariants.py`. ⚠ **`test_irradiance_wired.py:9` 가 그 경로를 인용하므로 함께 움직여야 하고**, 검사 파일이 움직이면 **`gen_traceability.py` 를 다시 돌려야 한다**(로컬 `pytest` 는 못 잡고 CI 만 잡는다) | `tests/report/` | R49/WP-1·5 |
| ~~**★A**~~ | ~~붙임 7 의 둘째 표를 지운다 + 붙임 8 자가소비 사유를 둘로 가른다~~ — **R49 해소**(`b26db28`). 두 표가 같아져 거짓이 된 문장을 지웠고, 사유 둘(ⓐ `SelfConsumption` 스트림 미배선 ⓑ `Q-3` 가 가정값을 교체)을 **다른 칸에** 담았다. ⚠ **`Q-3` 가 오면 둘째 표는 돌아온다**(비교가 다시 의미를 갖는다 — 그 사실은 `dispatch_profile_section` 독스트링이 든다) | ✔ | R49/WP-1 |
| ~~**★B**~~ | ~~132.2% 를 답으로 제시하지 않는다~~ — **R49 해소**(`5f0c3ea`). 네 자리가 **「전액(100%) 지원해도 전환되지 않는다 + 남는 결손 3,156,180원 + 지원 외 수단 필요」** 를 싣는다. `DV-1` 은 **풀지 않았다.** ⚠ **요약·6.2 의 값 칸이 백분율로 시작하지 않는다** — 굵은 수는 그 칸만 떼어 인용된다(R43-G) | ✔ | R49/WP-2 |
| ~~**★C**~~ | ~~「답 없음」으로 끝내지 않는다 — 적자 요인과 그 크기를 쓴다~~ — **R49 해소**(`65917c4`·`e414a4e`·`486549f`). ⓐ **5.3 「적자는 어디서 오는가」**(합계 = 결론축) ⓑ 5.1 이 **「그 끝까지 밀면 얼마 줄어드나」** 를 크기 순으로 — **아홉을 다 밀어도 6,508,337원이 남는다**(그것이 「왜 전환 인자가 없는가」의 답이다) ⓒ 「없음」은 그대로 두고 **배치를 검사가 붙든다.** ⚠ **축은 하나도 더하지 않았다** — 판정이 든 셋이 이미 전부 축이었다 | ✔ | R49/WP-3·4·5 |
| ~~**1**~~ | ~~★★ **리포트가 조항이 요구하는 표시 여섯을 아예 내지 않는다**~~ — **R48 이 가장 무거운 `FR-404-AC1` 을 닫았다**(`727186e`). `policy_warnings()` 가 `DER` 계약에 오르고(`44b0158` · §16.2) 리포트가 상단에 인쇄한다. ⚠ **남은 다섯**(`FR-106-AC5`·`FR-602-AC2`·`FR-603-AC2`·`FR-705-AC1`·`FR-905-AC8`)은 **그대로 0곳**이다 — 아래 **25번**으로 옮겼다 | ✔ 부분 | R48/WP-D1·D2 |
| ~~**2**~~ | ~~**붙임 1 이 오버라이드를 반영하지 않는다**~~ — **R48 해소**(`30959b7`). 값을 `provider.get()` 에서 읽고 **행의 집합은 여전히 `items()` 가 짓는다**(옮기면 덮어쓰지 않은 항목이 사라져 `FR-1002-AC6` 위반). ⚠ **「덮어썼다」는 표시를 어느 조항이 소유하는가는 여전히 미결**이다 | ✔ 동작만 | R48/WP-E |
| ~~**3**~~ | ~~**부채 대장이 둘이다**~~ — **R47 해소.** 합치지 않고 `KNOWN_PARTITION_DESIGN_GAP` 으로 **의도된 차이를 명시**하고 메타 검사가 관계를 붙든다(`72c1e6d`). 차이 둘은 낡음이 아니라 **두 파일의 서로 다른 설계 판단**이었다 — 경위는 `status-history.md` R47 블록 | ✔ | R47 |
| ~~**4**~~ | ~~⏸ **이중계상 판정 셋(甲·乙·丙)**~~ — **R48 해소**(`d0ed09d`). **갈래 셋은 채택되지 않았다** — 사용자 판정은 편익을 지우는 것이 아니라 **운영방식을 규정하고 편익을 거기서 산정**하는 것이었고, 그러면 **수량 단계에서 갈려** 같은 kWh 가 두 항목에 못 들어간다. **규칙보다 강하다** | ✔ | R48/WP-H |
| ~~**6**~~ | ~~붙임 4 의 「만드는 편익」이 여전히 **선언 목록**이다~~ — **R48 해소.** 금액이 없던 표는 **붙임 4 가 아니라 본문 2.1** 이었고(`30959b7`), 붙임 4 의 「만든 자원」 열이 임자를 **`PV` 단독**으로 적던 것도 함께 고쳤다(`60f1704`) — 셋 다 **선언을 귀속으로** 바꾼 같은 형태다 | ✔ | R48/WP-E·E2 |
| ~~**13**~~ | ~~**`_reacquisitions()` 헬퍼의 반환 정렬 규약 불일치**~~ — **R47 해소**(`a332c4a`). 옛 13번의 마지막 조각이며 이로써 13번 전체가 닫혔다 | ✔ | R47 |

> ### ⬇ **R50/WP-4 추가**(2026-09-01 · 라운드를 닫은 뒤 남은 시간에 돌렸다)
>
> # ⏹ 2026-09-01 **R50/WP-4 — ★O 를 닫았다. spec·작업목록의 줄 번호 인용 셋이 낡아 있었다**
>
> R50 본체를 `main` 에 밀고 CI 초록불을 확인한 **뒤**, 종료 시각(19:54)까지 남은
> 시간으로 한 판 더 돌렸다. **판정도 골든·리포트 재산출도 부르지 않는 자리**만 골랐다 —
> 착수 순서 25번(리포트가 안 내는 표시 다섯)은 전부 리포트 출력을 바꾸므로 열지 않았다.
>
> ## 무엇이 어긋나 있었나
>
> `rslt/` 의 두 정본 문서가 저장소 **안** 파일을 `경로:줄번호` 로 가리키는데
> **셋 다 낡아 딴 것을 가리키고 있었다.** 한 라운드가 만든 것이 아니라 여러 라운드에
> 걸쳐 밀린 것이다 — **문서도 코드도 아무 오류를 내지 않는다.**
>
> | 인용 | 주장 | 그 줄에 **있던** 것 | 고친 뒤 |
> |---|---|---|---|
> | `spec:573` → `core/contracts/valuestream.py:60` | `payer` 는 `ClassVar` | **`Payer` 열거 안**(실물은 `:160`) | `ValueStream.payer` |
> | `spec:451` → `core/casegrid/e2e_runner.py:470` | `PEAK_SHAVING` 을 코드에 박았다 | **`----` 구분선** | `run_single_case_e2e()` |
> | `task:289` → `heatpump.py:446` | `Year` 불일치 | **미충족 수요 주석** | `core/der/heatpump.py` 의 `annual_operation()` |
>
> ★ **번호를 「맞는 번호」로 고치지 않았다** — 그러면 다음 편집에 또 낡는다.
> §16.5.2 가 정본 인용에서 내린 결론(*「판본을 넘어 살아남는 좌표」*)을 그대로 적용해
> **이름**으로 바꿨다.
>
> ## 막는 것을 세웠다 — **고치기만 하면 다음 라운드에 다시 들어온다**
>
> - `scripts/check_doc_line_citations.py`(185줄) — `rslt/` 두 문서의 `경로:줄번호`
>   인용 중 **그 경로가 저장소에 실재하는 것**을 잡는다
> - `scripts/negtest_doc_line_citations.py`(266줄 · **여덟 갈래**) — 검사가 실제로
>   잡는지를 변이로 잰다. `finally` 로 원복하고 **시작 시점 바이트와 대조**까지 한다
> - CI `source-rules` 잡에 배선했다 — ⚠ `|| true` 를 붙이지 않았다
>
> ★★ **면제를 「목록」으로 두지 않았다.** 목록을 두면 다음 사람이 거기에 한 줄 더
> 붙여 통과시킨다. 면제는 **규칙 둘**뿐이다 — ① 저장소에 없는 경로는 우리가 고칠 수
> 없다(`storagevet/…`·`dervet/…` 참조 구현 스냅숏 **14건**) ② 머리말 `version:`
> 블록은 **기록**이다(고치면 이력이 거짓이 된다). 절 번호(`§16.5`)·포트 번호는
> **면제가 아니라 애초에 안 잡힌다**(확장자 있는 것만 경로로 본다).
>
> ⚠ **워커가 찾은 함정 하나** — `git ls-files` 는 기본 출력에서 비ASCII 경로를
> **따옴표로 감싼다.** 그대로 읽으면 **한글 이름인 두 대상 문서 자신이** 목록에서
> 어긋난다. `-z` 로 읽는다.
>
> ⚠ **디렉터리 없는 바탕 이름도 해석한다** — `heatpump.py:446` 이 그 형태였고, 경로
> 그대로만 맞추면 이 인용이 검사 밖에 남는다. **저장소에서 유일할 때만** 해석한다.
>
> ## 실측 — **오케스트레이터가 다시 쟀다**
>
> - **고치기 전 `rc=1` · 3건 적발** / **고친 뒤 `rc=0`** — `git worktree add … HEAD
>   --detach` 로 `HEAD` 사본을 세우고 새 검사만 복사해 돌렸다(정본 트리를 되돌리지 않는다)
> - 인용 총수 **18 → 15**(저장소 안 **3 → 0** · 밖 14 그대로 · 머리말 면제 1)
> - 음성 시험 **여덟 갈래 전건 통과** · `.mutbak` 잔존 0
> - `ruff`·`mypy`·`check_task_mapping`·`check_marker_substance`·
>   `check_docstring_references`·`check_source_rules`·`check_status_round_blocks`·
>   `check_file_size --code-strict` **전건 rc=0**
> - **`core/`·`tests/`·`fixtures/golden/`·`docs/` 를 한 줄도 건드리지 않았다** —
>   결론축·리포트 불변
>
> ⚠ **spec 판본을 올리지 않았다** — 고친 것은 **인용 좌표**뿐이고 조항·수용기준 문면은
> 그대로다. 수용기준 총수도 그대로다.
>
> ## 이 WP 가 연 것
>
> **★P** — 새 검사는 **`rslt/` 두 문서만** 본다. 같은 형태가 `docs/`·
> `status-history.md`·`README` 에 있는지 **아무도 세지 않았다.**

---

## R50/WP-4 가 「다음에 집을 것」에서 내린 행 (2026-09-01)

| # | 무엇 | 시작 자리 | 나온 곳 |
|---|---|---|---|
| **★O** | spec 의 **`core/contracts/valuestream.py:60` 인용이 낡았다** — 그 줄 번호로 *「`payer` 는 `ClassVar` 다」* 를 가리키는데 실물은 **`:160`** 이고, `:60` 은 `Payer` 열거 안이다. **R50 이 만든 것이 아니라 이미 낡아 있었다**(확인함). ⚠ 줄 번호 인용은 판본을 넘어 살아남지 못한다 — §16.5.2 가 정본 인용에 대해 세운 교훈과 같은 형태이며, **이름으로 바꾸는 것**이 답이다. 저장소 전체에 같은 형태가 더 있는지 함께 세라 | `rslt/spec-분산특구-경제성평가.md` 의 `FR-205-AC1` 아래 블록인용 ② | R50 검수 |

---

## 사용자 판정으로 **기각**된 행 (2026-09-01 · 재료 정본 `docs/decisions-2026-09-01-R51.md` §5)

> **본문은 고치지 않았다.** 아래 32번은 *「보고서 첫장 요약은 현 상황에서 변경 유인
> 없음(유지)」* 로 **사용자가 닫은 것**이다. ⚠ **「유지」는 「미해결」이 아니다 —
> 다시 열지 마라.**

| # | 무엇 | 시작 자리 | 나온 곳 |
|---|---|---|---|
| **32** | 「없음」이 **1절 요약 첫 화면에는 그대로 남아 있다** — ⚠ 사용자 판단 대기(검토자가 먼저 보는 규정을 바꾸는 일이다) | `docs/report-form-심의보고서.md` 【1】 · `core/report/narrative.py::_summary_section` | R35 ③ |
