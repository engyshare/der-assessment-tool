# 기여 가이드라인

본 프로젝트에 기여해주셔서 감사합니다.

## 개발 환경 설정
의존성을 설치하려면 아래 명령을 사용하세요.
```bash
pip install -e ".[dev]"
```

## CI 검증
PR을 제출하기 전, 반드시 GitHub Actions에 정의된 다음 검증을 통과해야 합니다 (FR-1103):
- **pytest**: 모든 단위 및 통합 테스트 통과
- **ruff**: 코드 스타일 및 린트 검사 통과
- **회귀 테스트**: 골든 시나리오 3종의 수치 회귀 검증 통과

비밀 키가 포함되지 않았는지 `gitleaks` 등을 통해 커밋 전 자체 확인하시기 바랍니다 (SC-7).
