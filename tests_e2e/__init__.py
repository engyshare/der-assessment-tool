"""브라우저로 실제로 돌려 보는 검사 — 작업 15.12(E2E) · 15.10(axe 접근성).

## ★★★ 왜 `tests/` 가 아니라 저장소 뿌리의 별도 디렉터리인가

`pyproject.toml` 의 `[tool.pytest.ini_options]` 가 **`testpaths = ["tests"]`** 다.
그러므로 인자 없이 `python -m pytest` 를 돌리면 이 디렉터리는 **수집되지 않는다.**
그것이 필요한 이유는 CI 의 「의존성」 단계에 있다 — 그 단계는 `pyproject.toml` 의
optional-dependency 그룹을 **읽어서 전부** 설치하므로, `e2e` 그룹을 만들면 본
`tests` 잡에도 playwright 가 깔린다. 그런데 본 잡은 `playwright install chromium`
을 돌리지 않으므로 **브라우저가 없다.** 여기 있는 검사가 본 잡에 수집되면 본 잡이
빨간불이 된다.

⚠ **마커(`-m "not e2e"`)로 거르지 않는다.** 본 잡의 pytest 명령을 고치면 「검사가
조용히 빠지는」 통로가 생기고, CI 가 그것을 막으려고 「수집 오류 0건 확인」 단계를
두고 있다. ⚠ **`conftest.py` 의 `skip` 으로 넘기지도 않는다** — 건너뛴 검사는
초록불과 구별되지 않는다(§13.0.1 ④). 디렉터리를 나누는 것만이 **본 잡의 명령도,
이 검사의 판정도 건드리지 않는** 유일한 길이다.

⚠ 그래서 이 디렉터리는 **자기 CI 잡**(`.github/workflows/tests.yml` 의 `e2e`)에서만
돈다. 그 잡은 「몇 건을 세었나」를 확인한다 — 0건을 수집하고 통과하면 아무도 모른다.
"""
