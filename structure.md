# 저장소 구조 — 무엇이 무엇을 낳는가

> 파일 목록이 아니라 **의존 방향**을 적는다. "이 파일을 고치면 무엇이 따라 바뀌는가"에 답하는 것이 이 문서의 목적이다.
> 갱신 시점: 2026-08-03 (코드 0줄, 문서·검증 인프라만 존재)

---

## 0. 한눈에

```
   [볼트 · 저장소 밖]              [저장소]                        [생성물]

   0. rules/                    docs/*.md  ──┐
     분산자원 경제성 평가 원칙  ──▶ (바이트 동일 사본)             
     근거 표기 기준             ──▶                │
            │                                     │
            │ sha256 고정                          │
            ▼                                     │
     docs/source-rules.lock ────────────┐         │
                                        │         │
   rslt/spec-분산특구-경제성평가.md ◀───┴─────────┘
     (요구사항 정본 · FR/NFR/UI/AC의 유일한 출처)
            │                    ▲
            │ 인용               │ 인용
            ▼                    │
   rslt/task-분산특구-경제성평가.md
     (작업 목록 · 상위 17 / 하위 150)
            │
            │        docs/manual-checks.yaml (MC-1~8)
            │        tests/ (@pytest.mark.req) — 아직 없음
            ▼                 │
   scripts/gen_traceability.py ◀┘
            │
            ▼
   docs/traceability.md  ← 자동 생성 · 수동 편집 금지
```

**단일 방향 규칙**: 볼트 → 사본 → spec → 작업 목록 → 테스트. 역방향 수정은 전부 금지다. 사본을 고치면 정본이 둘이 되고, `traceability.md`를 고치면 매핑표가 spec과 어긋난다.

---

## 1. 파일별 역할

### 1.1 정본(定本) — 저장소 밖

| 볼트 경로 | 저장소 사본 | 내용 |
|---|---|---|
| `0. rules/분산자원 경제성 평가 원칙.md` | `docs/domain-rules.md` (293줄) | 원칙 1-1~7-5. 편익 중복 계상, 기준선, 시간 처리, 물리 정합성, 가정 취급, 결과 보고 |
| `0. rules/근거 표기 기준.md` | `docs/evidence-standard.md` (139줄) | 출처·기준연도·신뢰도 표기 |

사본은 **바이트 동일**이어야 한다. 머리말조차 넣지 않는다 — 넣으면 해시가 달라져 CI에서 대조가 불가능하다. 동기화 메타데이터는 전부 `docs/source-rules.lock`에 둔다.

`docs/source-rules.lock` (YAML) 이 고정하는 것:
- `sha256` — 판정 근거. **이것만이 대조 대상이다**
- `mtime` / `size_bytes` — 판본 식별 기록. 대조하지 않지만 함께 갱신한다
- `cited_anchors` / `cited_headings` — spec이 인용하는 좌표 목록. spec 본문과 **양방향** 일치해야 한다

### 1.2 spec — 요구사항 정본 (`rslt/spec-분산특구-경제성평가.md`, 2,351줄, v0.7)

FR 58 / NFR 32 / UI 7 = **요구사항 97건**, 수용기준 **252건**.

| 절 | 내용 | 비고 |
|---|---|---|
| 1~3 | 개요 · 목표 · 범위 | P-1~P-3(문제 정의), G-1~(목표), SG-1~5(성공 기준) |
| **4** | **기능 요구사항 FR-101~FR-1101** | 4.0이 우선순위·Phase의 정의. 4.1~4.11이 계층별 |
| **5** | **비기능 요구사항 NFR-101~NFR-4xx** | 5.2에 NFR-106(자원별 검증 케이스) · NFR-107(추적 매핑) |
| **6** | **UI 요구사항 UI-1~7** | 6.2. v0.7에서 우선순위 부여 |
| 7 | 데이터 모델 · 단위 규약(7.5) | |
| 10~11 | 아키텍처 · 기술 스택 | `core/der/`, `core/asset/`, `core/contracts/` |
| 12 | Phase 1~3 계획 | Phase 1 = 에너지자립가구 모델, 14~16주 |
| **13** | **테스트 요구사항** | 13.0 TDD 우선 · **13.2 자원별 검증 케이스 매트릭스**(`RC-PV-P1` 등) |
| 15 | 미확정(Q-*, TU-*) | Q-1~Q-13 외부 데이터, TU-1~TU-7 기술 불확실성 |
| **16** | **병렬 개발 구획** | 16.3 WP-0~WP-16 · 16.4 공유 파일 · **16.5 정본 동기화 절차** |
| 부록 A~D | 원 요구사항 추적 · 문제 정의 추적 · DER-VET 판정 · 참조 | |

### 1.3 작업 목록 (`rslt/task-분산특구-경제성평가.md`, 546줄, v1.2)

상위 17건(`## 1.0` ~ `## 17.0`, 그리고 `## 16.0`) / 하위 150건.
각 상위 작업 제목이 `WP-n / Wave n / 통합 선행 …` 을 함께 선언한다 — 병렬 개발의 실행 단위.

하위 작업 형식:
```
- **6.7** [T] 플러그인 자동 등록 검증 — 신규 자원 추가 시 공유 파일 변경 0줄
  - 수용기준: `NFR-207-AC1`, `NFR-207-AC2`, `NFR-207-M1`. DoD: …
```
`[T]` 테스트 / `[I]` 구현 / `[R]` 검토.

### 1.4 규칙·대장

| 파일 | 역할 |
|---|---|
| `rslt/spec 작성 시 원칙.md` (234줄) | 원칙 1-A~8-4. **spec을 어떻게 쓰는가**의 규칙. 도메인 원칙과 번호가 19개 충돌하므로 인용 시 출처 표지 필수 |
| `rslt/2.15-표수용기준-전개안.md` | **한시 문서.** 2.15 ① 작업 명세 — 표 9개 키 배정·인용 교체·부수 결함. 완료 시 폐기. 검사 도구 글롭(`spec-*.md`·`task-*.md`)에 잡히지 않는다 |
| `docs/manual-checks.yaml` | 자동화 불가 수용기준의 수동 검증 대장 MC-1~MC-8. `criterion_id`로 건다 |
| `docs/README.md` | 사본의 성격과 개정 절차 |
| `status.md` | 세션 인계용 진행 상태 |

---

## 2. ID 체계 — 5종이 공존한다

| 체계 | 예 | 출처 | 안정성 |
|---|---|---|---|
| 요구사항 | `FR-106`, `NFR-107`, `UI-3` | spec에 직접 표기 | **안정** |
| 수용기준 | `FR-106-AC3`, `NFR-101-M1` | spec에 직접 표기 (v0.8~) | **안정** |
| 작업 번호 | `6.6`, `6.0.S1`, `2.15` | 작업 목록 | 재배열 시 변동 |
| 구획·파동 | `WP-1f`, `Wave 1` | spec §16.3 | 안정 |
| 검증 케이스 | `RC-CA-C1`, `RC-PV-P3` | spec §13.2 | 안정 |
| 미확정 | `Q-4`, `TU-6`, `MC-3`, `SG-4` | spec §15 · 대장 | 안정 |

> **수용기준 ID는 v0.8에서 순번 파생 → 직접 표기로 바뀌었다.** 그 전에는 선언 순서에서 ID를 뽑았기 때문에, spec에 AC를 중간 삽입하면 이후 번호가 전부 밀려 테스트 마커와 작업 목록 인용이 조용히 어긋났다(v0.7에서 실제로 7건 발생). 지금은 `- **AC3** …` 처럼 spec이 ID를 직접 들고 있으므로 삽입해도 밀리지 않는다.

---

## 3. 검증 도구 3종

| 스크립트 | 입력 | 출력 | 무엇을 잡는가 |
|---|---|---|---|
| `check_source_rules.py` | 볼트(없으면 사본) · lock · spec | 판정 | ① 정본 해시 표류 ② 사본 오염 ③ 앵커 소실 ④ spec↔lock 인용 정합(양방향) ⑤ 폐지 좌표 인용 |
| `gen_traceability.py` | spec · `manual-checks.yaml` · `tests/` | **`docs/traceability.md`** | Must-have 수용기준 중 검증 미매핑 (NFR-107) |
| `check_task_mapping.py` | 작업 목록 · `traceability.md` | 판정 | ① 범위 초과 인용 ② 미인용 Must-have ③ 상위 작업 간 중복 인용 |

**세 도구는 목적이 다르다.** `gen_traceability`는 인용 ID가 *실재하는지*만 본다. 실재하지만 *엉뚱한 조항*을 가리키는 오귀속은 `check_task_mapping`의 「미인용 Must-have」가 남긴 **빈자리**로만 드러난다.

실행 순서 (항상 정본 대조부터):
```bash
python scripts/check_source_rules.py     # 정본이 바뀌었는지 먼저
python scripts/gen_traceability.py
python scripts/check_task_mapping.py
```

### CI (`.github/workflows/source-rules.yml`)

| 잡 | 내용 | 상태 |
|---|---|---|
| `collate` | 정본 대조 (볼트 없으므로 사본 기준) | `continue-on-error` — **경고** |
| `traceability` | 매핑표 재생성 후 커밋본과 diff | `continue-on-error` — Wave 0~1 동안 경고 |

트리거: spec/lock/사본/스크립트 push · PR · 매일 00:00 UTC(정본은 저장소 밖에서 바뀌므로 능동 확인).

---

## 4. 문서가 자기 자신을 검사하는 구조 — 반복된 함정

이 저장소의 검사 도구는 **마크다운 산문에서 선언을 골라낸다.** 그래서 *검사를 설명하는 문장*이 그 검사에 걸리는 사고가 네 번 났다(`status.md` 「반복해서 걸린 함정」).

대응 원칙 — **서술과 선언은 형식으로 구분한다.**

| 대상 | 선언 형식 | 그 밖의 위치 |
|---|---|---|
| 수용기준 ID | spec의 `- **AC3** …` | 본문 언급은 선언이 아니다 — **적용 완료** |
| 작업 → 수용기준 인용 | 작업 목록의 `  - 수용기준:` 필드 | **적용 완료.** 형식 이탈 선언은 `check_task_mapping.py`가 「형식 이탈 인용 의심」으로 잡는다 |
| 정본 원칙 인용 | 출처 표지와 같은 줄의 `원칙 N-M` | 표지 없으면 「출처 모호」로 걸린다 |

정규식을 정교하게 만들어 서술과 선언을 구별하려 하면 반드시 한쪽으로 틀린다. 형식으로 갈라 놓아야 한다.

---

## 5. 지금 상태

- 코드 **0줄**. 검증 인프라만 완성
- 수용기준 252건 전건 미매핑 — **테스트가 없으므로 정상**. 매핑표가 곧 Phase 1 테스트 작업 목록이다
- 다음 할 일과 미해결 사항은 `status.md` 참조
