# 점검 — Phase 1 전체 인수 판정 (점검자: Sisyphus)

> 2026-08-09 수행. 소스 수정 없음. 보고서 파일 1건만 생성.

## 점검 대상

`rslt/task-분산특구-경제성평가.md` §17.0 에 정의된 Phase 1 완료 조건 12항목(17.1~17.12).

---

## 판정

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 17.1 | DoD 1 — 에너지자립가구 2변형 비교 산출 | **판정 불가** | 템플릿 생성은 검증하나, **비교 산출(두 모델의 NPV/IRR/회수기간을 나란히 표시)을 수행하지 않는다.** FR-202-AC2(비교표 나란히 표시)는 미매핑 11건 중 하나이며 구현되지 않았다. 테스트는 모델 이름과 PV 단가만 확인하고 끝난다. |
| 17.2 | DoD 2 — 27케이스 90초 이내 + 히트맵 | **판정 불가** | 테스트가 `run_cases`에 **가짜 lambda**(`100.0 if discount_rate=="low" else -50.0`)를 주입한다. 실제 디스패치·CBA 파이프라인이 27케이스를 90초 이내에 완료하는지는 이 테스트로 알 수 없다. 케이스 수(27)만 검증하고 성능 기준의 실질 판정은 하지 않는다. |
| 17.3 | DoD 3 — 무지원 회수기간 + 최소 보조율 역산 | **지적** | `solve_min_subsidy_rate` 구현은 실질적(이분 탐색 + 단조성 검사). 기대값도 손계산과 일치(0.5). 다만 `build_capex_cashflows`의 is_baseline 검증은 단일 연도만 보고, `capex_vat` 분리 등 더 복잡한 현금흐름 구조를 검증하지 않는다. 핵심 로직은 유효하나 검증 범위가 좁다. |
| 17.4 | DoD 4 — 선행 실증 ±15% 대조 | 판정 불가 | `@pytest.mark.skip` 처리. Q-4가 blocked 상태이므로 미판정 보류는 **정당한 판단**이다. 통과도 실패도 아닌 유예. |
| 17.5 | DoD 5 — 영향도 순 + 산식 3중 표기 | **차단** | ① `rank_influences`는 **16줄 mock**이다 — `threshold`는 항상 `base*1.5`, `margin_pct`는 항상 `50.0`으로 하드코딩되어 있다. 실제 민감도 분석이 수행되는 것이 아니라 dict를 정렬만 한다. ② `generate_pdf`도 **16줄 mock**이다 — `"자연어"`, `"수식"`, `"대입값"`이 하드코딩된 문자열에 들어 있어 `"A" in "ABC"`와 같은 동어반복 검증이다. **구현이 mock인데 테스트가 통과한다는 것은 "아무것도 검사하지 않았다"와 같다.** |
| 17.6 | DoD 6 — 편익 계상 내역 + 배타 위반 거부 | **지적** | 배타 규칙 테이블과 `collect_exclusions` 구현은 실질적이고 테스트도 의미 있다. 다만 `DEFAULT_EXCLUSION_RULES`에 등록된 4규칙 중 테스트가 검증하는 것은 유형 A(SelfConsumption↔SurplusSale) 1쌍뿐이다. 유형 B·D가 실제로 걸리는지는 이 테스트로 확인되지 않는다. |
| 17.7 | DoD 7 — 골든 3종 회귀 + 무료 호스팅 | **차단** | ① 골든 시나리오 3종 파일이 존재하나 **기대값이 전부 `null` (TBD)** 이다. 회귀 테스트는 비교할 기준값이 있어야 성립하는데, 없으면 "파일이 있다"만 확인하는 것이 된다. ② CI 워크플로에 pytest·ruff 문자열이 있다는 것만 확인 — 게이트가 실제로 작동하는지, 적절한 인자로 호출되는지는 보지 않는다. ③ 무료 호스팅 외부 접속은 테스트에 아예 없다. |
| 17.8 | DoD 8 — 자원 6종 §13.2 케이스 전건 통과 | **지적** | `discover`로 6종 이상 자원이 등록됨을 확인하고, 각 자원에 대응 테스트 파일 존재를 확인하는 것은 유효하다. 다만 "케이스 전건 통과"가 아니라 "테스트 파일이 존재함"만 검증한다. §13.2 검증 케이스의 내용(NPV·수지 균형 등)이 전건 통과하는지는 이 테스트 범위 밖이다. |
| 17.9 | DoD 9 — Must-have 미매핑 0건 | **차단** | `docs/traceability.md` 실측: **미매핑 11건**. 테스트 자체가 `@pytest.mark.xfail(strict=False)`로 표기되어 있어 **실패가 예상된 상태**이다. xfail(strict=False)이면 테스트가 실패해도 pytest 전체 종료 코드는 0이 된다 — "미달성인데 통과로 세어지는" 구조다. 17.9는 명시적으로 Phase 1 완료 조건이므로, 미매핑 11건이 잔류하는 한 Phase 1은 끝나지 않는다. |
| 17.10 | DoD 10 — 신규 자원 공유 파일 0줄 + import-linter 0 | **지적** | 테스트가 실제로 임시 파일을 만들고 `discover`로 등록을 확인한 뒤 삭제하는 절차는 유효하다. import-linter도 실제로 호출한다. 다만 "공유 파일 미변경" 검증이 `registry.py`가 존재하는지만 보고 **git diff로 변경 사항이 없는지는 보지 않는다.** 파일 존재 확인 ≠ 변경 없음 확인. |
| 17.11 | SG-5 — 신규 자원 추가에 코어 수정 0줄 | **지적** | `core/engine/`과 `core/cba/`에 임시 파일이 없는지 확인하지만, 그 두 디렉토리에 파일을 **만든 적이 없으니까 당연히 없다.** 실제 검증은 "신규 자원이 `core/engine`·`core/cba`를 import 없이 작동하는가"이나, 테스트는 그것을 보지 않고 "내가 만들지 않은 파일이 없다"만 확인한다. |
| 17.12 | 규모 검산 재확인 | 통과 | `check_file_size.py --code-strict`를 래핑하여 실행. 코드 줄 수 500 초과 0건(총 줄 초과 7건은 전부 설명 밀도). 실제로 스크립트를 호출하므로 유효. |

---

## 차단 사유 (Phase 1 완료를 막는 것)

1. **17.5 DoD 5** — `rank_influences`·`generate_pdf` 모두 16줄 mock 구현. 실제 민감도 분석·PDF 생성이 존재하지 않는다.
2. **17.7 DoD 7** — 골든 시나리오 기대값이 전부 `null`. 회귀 테스트가 성립하지 않는다.
3. **17.9 DoD 9** — Must-have 미매핑 11건 잔류. DoD 9는 "0건"을 요구하며 이것은 Phase 1 완료 전건이다.

---

## 지적 사항 (차단은 아니나 남길 것)

### 동어반복 검증 (자기충족과 같은 효과)

1. **17.5** — `"자연어" in pdf_content["formulas"]` 는 `generate_pdf`가 하드코딩한 문자열에 대해 부분 일치를 검사하는 것이다. 구현이 고장 나면 이 테스트도 실패하겠지만, **구현이 올바른지는 전혀 검사하지 않는다.** 테스트가 구현의 출력을 기대값으로 복사한 형태이며, 이는 §13.0.2가 금지한 자기충족 검증의 완화된 형태다.

### 가짜 주입으로 성능 기준 회피

2. **17.2** — `run_cases(lambda case: {...})` 에 가짜 lambda를 주입하면 어떤 복잡도의 케이스라도 0초에 통과한다. 이것은 27케이스 성능 기준(90초)의 판정을 무효화한다.

### 검증 범위 부족

3. **17.6** — 배타 규칙 4종 중 1종(유형 A)만 테스트. 유형 B(DistributedBenefit↔SelfConsumption), D(REC↔SurplusSale, 프로파일 한정)는 검증되지 않는다.
4. **17.8** — 자원 6종의 **테스트 파일 존재**만 확인. §13.2 케이스 내용(독립 검증 값)이 전건 통과하는지는 별도 검증 필요.
5. **17.10** — 공유 파일 미변경을 "파일이 있다"로만 확인. 실제 diff 비교가 필요.
6. **17.11** — 코어 수정 0줄을 "내가 만들지 않은 파일이 없다"로만 확인. 실제 자원이 엔진·CBA와 결합하여 작동하는지 검증 안 함.

### 무호스팅 검증 누락

7. **17.7** — "무료 호스팅 외부 접속" 항목이 테스트에 존재하지 않는다.

---

## 판정 불가 항목

| # | 사유 |
|---|---|
| 17.1 | FR-202-AC2(비교표) 미구현. 템플릿 2변형 생성만 검증됨. |
| 17.2 | 가짜 lambda 주입으로 실제 파이프라인 성능을 측정하지 않음. |
| 17.4 | Q-4 blocked. skip은 정당한 유예이나, Phase 1 완료에는 이 항목의 해소가 필요한지 명세에 따라 판단 필요. |

---

## 실행한 명령과 출력

### 1. 전체 테스트

```
python -m pytest -p no:cacheprovider --no-cov -q
```

결과: **895 passed, 2 skipped, 1 xfailed, 1 xpassed** (64.74초)

- skipped 2건: 17.4(Q-4 유예) 1건 + 기타 1건
- xfailed 1건: 17.9(미매핑 11건, 기대된 실패)
- xpassed 1건: 존재하는데 기대치를 벗어난 통과 (별도 확인 필요)

### 2. 정적 검사 일람

| 명령 | rc | 비고 |
|---|---|---|
| `python -m ruff check core tests scripts infra app` | 0 | All checks passed |
| `python -m mypy` | 0 | Success: 72 source files |
| `lint-imports` | 0 | 4 kept, 0 broken |
| `python scripts/gen_traceability.py` | 1 | 미매핑 11건 잔류 (rc=1은 이 스크립트에서 정상 — 미매핑 잔재 의미) |
| `python scripts/check_task_mapping.py` | 0 | 통과 — 미인용 Must-have 0건 |
| `python scripts/check_disclosure.py` | 0 | 통과 — 비공개 유입 없음 |
| `python scripts/check_hardcoded_params.py` | 0 | 통과 — 차단 0건, 경고 2건(3600 충돌 가능, 사람 판단) |
| `python scripts/check_file_size.py --code-strict` | 0 | 통과 — 코드 줄 초과 0건 (총 줄 초과 7건은 설명 밀도) |
| `python scripts/negtest_traceability.py` | 0 | 음성·양성 8종 전건 통과 |
| `python scripts/negtest_disclosure.py` | 0 | 음성 9 + 양성 6 + 경계 2 = 17건 전건 통과 |

**정적 검사 전건 무결함.** ruff·mypy·import-linter·disclosure·file_size·hardcoded_params·negtest 전부 rc=0.

---

## 종합 판정

**Phase 1 완료 상태가 아니다.** 차단 사유 3건이 존재한다.

1. 핵심 산출 모듈 2종(`sensitivity`, `pdf`)이 mock 구현 상태 — DoD 5 실패
2. 골든 시나리오에 기대값이 없어 회귀 테스트가 성립하지 않음 — DoD 7 실패
3. Must-have 미매핑 11건 잔류 — DoD 9 실패

정적 검사(ruff, mypy, import-linter, disclosure, file_size, hardcoded_params)는 전건 통과하며, 테스트 895건도 전건 통과한다. 그러나 **통과한 테스트 중에 실제로 검증하지 않는 것이 다수 존재한다.** 이것이 이 저장소에서 가장 위험한 상태다 — 초록불이 무언가를 의미하지 않는다.

INSPECT PHASE1 | 차단 3건 | 지적 7건 | 판정불가 3건
