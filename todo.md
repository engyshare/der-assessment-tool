# 다음 할 일 — R44 (2026-08-30 갱신 · **R43 은 닫혔다**)

> ⚠ **이 파일은 정본이 아니다.** 판정 근거 · 실측 수치 · 라운드 경위는
> **`status.md` 의 「지금 할 일」 R43 블록**이 갖는다. 여기 있는 것은 *무엇을
> 다음에 집는가* 와 *어디서 시작하는가* 뿐이다 — 같은 사실을 두 곳에 적으면
> 한쪽만 고쳐진다. 두 파일이 어긋나면 **`status.md` 가 맞다.**
>
> 사람 몫(수동 검증 · 외부 자료 회신 · 배포 판단 · spec 개정)은
> **`status-human.md`** 가 소유자다. 여기 옮겨 적지 않는다.

> # ✔ R43 이 WP 열넷을 닫았다 — **결론축은 안 움직였다**
>
> `A`·`B`·`C`·`D`·`D2`·`E`·`E2`·`F`·`F2`·`F3`·`G`·`H`·`I`·`J`·`L`.
> **`K1`·`K2` 는 착수하지 않고 이 목록으로 넘어왔다**(아래 2·4·5).
>
> | 종전 `todo.md` | 결과 |
> |---|---|
> | 1 이중계상 | **재기만 했다** — 판정서를 세웠고 **사람 판정이 미결**이다 → 아래 **1번** |
> | 2 `HeatPump` 교체비 물가 계수 | **✔ 닫혔다** (WP-D) — 덤으로 잔존가치까지 (WP-D2) |
> | 3 최초 부속설비 몫 (`Q-2`) | 그대로 — 회신 대기 → 아래 **21번** |
> | 4 「지금 할 일」 블록 이력 | **✔ 닫혔다** (WP-A · 아홉 판 중 여덟을 내렸다) + **재는 검사를 세웠다** (WP-J) |
> | 5-① 「변동 O&M 미포함」 | **✔ 닫혔다** (WP-C) — 붙임 8 이 사실에서 짓는다 |
> | 5-② 인버터 몫 · 단가 대장 | **절반 닫혔다** (WP-F) — 대장·스윕 축은 섰고 **본체 취득가에서 떼는 것이 남았다** → 아래 **18번** |
> | 5-③ `salvage_row()` | **✔ 닫혔다** (WP-B) |
> | 5-④ `from_resource` 조인 키 | **✔ 닫혔다** (WP-B — 칸 이름을 두 규약으로 갈랐다) |
> | 5-⑤ `_by_year` 독스트링 | **✔ 닫혔다** (WP-A) + **재는 검사를 세웠다** (WP-L) |
> | 6 R38 잔여 | 그대로 → 아래 **21번** · 전문은 부록 A |
>
> ⏳ **미푸시 커밋 수는 `git log origin/main..HEAD --oneline | wc -l` 로 센다** — 이
> 파일에 적지 않는다. 종전 판이 적어 둔 「10건」은 **세지 않은 수였고 거짓이었다.**
>
> ⚠ **이 파일에서 이력을 뺐다** — R38 의 골든 하니스 판정 경위 · 교체비/잔존가치
> 정찰 · R35 ③ 까지의 옛 목록(종전 524줄의 3분의 2가 그것이었다). 그 경위는
> `status-history.md` 의 **R38·R35 블록**이 갖는다. **살아 있는 항목은 하나도 빼지
> 않았고** 아래 착수 순서와 부록 A 에 전부 있다. 「길이가 낡음을 만든다」 —
> 같은 이유로 R43-A 가 `status.md` 에서 종료 블록 여덟을 내렸다.
> ⚠ **지금 길이를 이 줄에 적지 않는다** — `wc -l todo.md` 로 센다. 적어 두면
> 다음에 한 줄만 고쳐도 거짓이 되고, 그것이 이 저장소가 반복해 밟은 형태다.

---

## 착수 순서

⚠ **1·18 은 결론축을 움직인다.** 「기준선을 흔드는 것은 한 번만 해야 하고, 그때
실행 경로의 행 구성이 확정돼 있어야 한다」 — 두 번 흔들지 않는다.

| # | 무엇 | 시작 자리 | 나온 곳 |
|---|---|---|---|
| **1** | ⏸ **이중계상 판정 셋(甲·乙·丙)** — 사람 판정 대기. ⚠ 乙·丙 은 **겹치지 않으니 하나만** 지운다. **丙 이상은 결손이 총사업비를 넘어 지원율로 못 메운다** | `docs/evidence/판정요구-이중계상-2026-08-29.md` · `status-human.md` 1-A | WP-E |
| **2** | `PV` 도 **두 곳이 각자 돈다** — `replacement_schedule()` 이 `_acquisitions()` 에서 파생되지 않는다. 지금은 값이 같지만 **구조가 보장하지 않는다** (= 미착수 `WP-K1`) | `core/der/pv.py` | WP-D2 |
| **3** | `Load`·`ThermalLoad` 의 잔존가치 비대칭 — 크기는 **재어 두었다** | 부채 래칫 `KNOWN_SALVAGE_IGNORES_REPLACEMENT_RATE` (2건) | WP-D2 |
| **4** | **§13.2.2 `C-5` 가 「어느 취득가」를 말하지 않는다** — 명목 재취득가로 메운 자원이 넷 (= `WP-K2` ①) | spec §13.2.2 · §16.5 절차 | WP-D2 |
| **5** | `core/incentive/calculator.py` **4곳**이 빌더 밖 `CashFlowRow` 직접 생성 (= `WP-K2` ②) | `:120` · `:131` · `:140` · `:155` | WP-B |
| **6** | 붙임 4 의 「만드는 편익」이 여전히 **선언 목록**이다 — 4.3 은 이제 귀속인데 (= `WP-K2` ③) | `core/report/method_sections.py` · `core/casegrid/attribution.py` | WP-E2 |
| **7** | 안분 **잔차를 받는 자리**(최종 몫)가 임의이며 **실물로 검증된 적이 없다**(이 케이스 잔차 0원) | `core/casegrid/attribution.py` | WP-E2 |
| **8** | **`negtest_docstring_references.py` 가 없다** — CI 가 그 검사기 **자신의 감지 능력**을 재지 않는다 | `scripts/` · `source-rules.yml` 의 `docstring-references` | WP-L |
| **9** | NFR-105 동반 검사가 **문면만 바뀐 `core/` 파일도 구현 변경으로 센다**(`has_code()` 가 파일 단위) | `scripts/check_test_accompaniment.py` | WP-L |
| **10** | 독스트링 참조 검사가 **점 표기 45건**(클래스 수신자)을 아직 안 잰다 — 클래스 색인이 필요 | `scripts/check_docstring_references.py` | WP-L |
| **11** | **자원 구성을 케이스 축으로** — 지금 러너는 PV+ESS 를 한 벌로 고정해 **0kWh 미설치안을 계산하지 못한다**(`ValidationError`) | `core/casegrid/e2e_runner.py` · `ledger_levels.py` | WP-I |
| **12** | 4.4 에 **「두 최선을 더해 읽어도 된다」**를 실을 것인가 — 교차항이 **+3원**임을 쟀다 | `core/report/capacity.py` | WP-I |
| **13** | **회수 시점이 지원율에 대해 불연속**임을 리포트에 실을 것인가 — 자료에 없던 사실이다 | 붙임 3 · 6.2 | WP-I |
| **14** | 답변서의 탐침(`.orch/R43/probe_wp_i.py`)을 **`scripts/` 로 옮겨 커밋**할 것인가 | `scripts/` | WP-I |
| **15** | 태양광 **상한 9kW 를 옮길 것인가** — 탐색 편의값임이 확인됐다(**사업 주관 측 판정**) | `core/casegrid/ledger_levels.py:157` | WP-I |
| **16** | `tariff.hv_single_contract.avg` 가 **아직 스윕 축이 아니다** — 구조가 서는 날 축이 되어야 한다(지금은 사유와 함께 면제 등재) | `core/casegrid/ledger_levels.py` 의 면제 목록 | WP-H |
| **17** | `demand_charge` 3수준이 `…avg` 의 **상대폭을 빌린 대리값**이다 — 약관 회신 시 실측으로 교체 | `docs/assumptions.yaml` (`Q-6`) | WP-H |
| **18** | ⚠ **todo 5-② 의 나머지 절반** — 최초 인버터 몫을 본체 취득가에서 뗀다. **필요한 값이 이제 대장에 있다**(R39-E 가 미룬 조건 해소). **떼면 결론축이 움직인다** | `core/der/pv.py::_acquisitions` · `capex.pv.inverter_share` | WP-F |
| **19** | 변동 O&M **배선**(빌더 + 단가 대장 + 러너) — 붙임 8 이 좌표를 갖는다 | `core/cba/proforma.py` · `e2e_runner.py` | WP-C |
| **20** | 최초 부속설비 몫이 본체 잔존가치에 남는다 (`ESS` PCS · `PV` 인버터) — **`Q-2` 회신에 묶여 있다** | `status-human.md` 가 소유자 | 종전 3 |
| **21** | R38 이 남기고 아무도 손대지 않은 일곱 — 전문은 **부록 A** | 부록 A | 종전 6 |
| **22** | 「없음」이 **1절 요약 첫 화면에는 그대로 남아 있다** — ⚠ 사용자 판단 대기(검토자가 먼저 보는 규정을 바꾸는 일이다) | `docs/report-form-심의보고서.md` 【1】 · `core/report/narrative.py::_summary_section` | R35 ③ |
| **23** | 새 PV 결론을 다시 재기 — **평탄 발전 프로파일**(야간 발전) 때문에 PV 가 좋아 보인다. 일사 곡선이 배선되면 뒤집힐 수 있다 | `core/casegrid/profiles.py::DailyShapes` · `e2e_runner.py` 의 `generation_profile` | R35 |
| **24** | 프로포마 행 목록을 `CaseOutcome` 에 실을 것인가 — R36 판정 *「검사를 세우려고 산출물 자료형을 넓히는 것은 순서가 거꾸로다」* | `core/casegrid/models.py::CaseOutcome` | R34 |
| **25** | `FR-401-AC2.VPPMarket` (Phase 2) — 자료가 아니라 **순서 문제**. `VPP` 가 계약에 앉는가가 선행 | spec `AC1.VPP` | R33 |
| **26** | `SettlementInputs.annual_generation_kwh` 를 옮긴다 — **자원 없이 부르는 호출자가 사라지면** | 그 필드 주석 | R33 |
| **27** | 얇은 리프의 남은 항목 — 그중 `FR-202` 비교표 계층은 리포트 조립 계층이다 | `.orch/R37/gap_thin_leaves.md` | R37 |
| **28** | 닫기 라운드 — **마커 49곳을 읽어 대조**. 남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나이며 **읽는 수밖에 없다**. 정규식은 `(?<!N)FR-` | R32 가 옮긴 마커 49곳 | R33 표 5 |

---

## 부록 A. R38 이 찾았으나 닫지 않은 것 — **크기 순** (착수 순서 21번의 전문)

전문은 `.orch/R38/` 의 각 `result_*.md` 이고 판정 경위는 `orch_log.md` 다.
⚠ `.orch/` 는 `.gitignore` 안이라 **다른 기계에는 없다** — 아래 요약이 정본이다.

### ① ★ `test_ess.py` 의 자기충족 검사 — **옮길 곳이 없다. 지울지 정해야 한다**

`test_rc_all_c2_geometric_series_formula_is_resource_independent` 는 단언이 한 줄이고
**`DER` 메서드를 하나도 부르지 않는다** — 검사가 오라클 산식을 스스로 계산해 상수와
견준다. 붙드는 것은 `to_won` 의 반올림 하나다. **변이로 증명했다**:
`ess.fixed_om()` 의 물가 계수를 지우니 **이 검사는 초록불**이고 13줄 아래 이웃
(`..._20y_cumulative_matches_geometric_sum`)이 **잡았다.**

- `FR-101-AC2` 인용은 거짓이고 **`AC5` 로도 갈 수 없다**(그 셋을 안 부른다) —
  그래서 R38 의 이관 16건에서 **뺐다**
- 구현을 불러 견주게 고치면 **이웃과 중복**이 된다 → 「같은 사실이 두 곳에 있으면
  한쪽만 고쳐진다」에 걸린다
- **정할 것: 지울 것인가.** 이름이 `..._is_resource_independent` 이므로 목적을
  다른 형태로 살릴지도 함께 정한다

### ② `FR-101-AC2` 의 매핑이 **한 파일에 몰렸다**

이관 뒤 남은 8건 중 조항을 실제로 지키는 것은 **5건**이고 그중 **4건이
`tests/contract/test_der_contract.py`** 다. 그 파일이 흔들리면 **Must-have · Phase 1
조항이 미매핑에 근접한다.** 이관 전에는 24건에 흩어져 보이지 않던 위험이다.

### ③ 「같은 개념을 네 이름으로 부른다」 — 훑기 함정

`AC4`(부속설비 독립 수명)의 두 번째 수명이 자원마다 이름이 다르다 —
`subcomponents`(`Load`·`ThermalLoad`) · `pump_lifetime`(`HeatPump`) ·
`inverter_lifetime`(`PV`) · `pcs_lifetime`(`ESS`). **이름으로 훑고 멈추면 `AC4`
마커 다섯을 「근거 없음」으로 오판한다** — R38 의 검증자가 실제로 한 번 그렇게 셌고
소스를 열어 정정했다.

### ④ 소스 문면 검사가 **들여쓴 지역 import 를 놓친다**

R38 이 `tests/contract/source_scan.py` 로 필터를 공용화하며 확인했다 —
`line.startswith(("import core", "from core"))` 는 **줄이 들여쓰기 없이 시작하는지만**
본다. 함수 **안**의 지역 import 는 **안 걸린다**(변이로 확인). 「함정의 반대 방향」
(과소 검출)이며 독스트링에 갈라 적었다. **정할 것: 지역 import 도 잡을 것인가** —
잡으려면 `ast` 로 `Import`/`ImportFrom` 노드를 보는 것이 옳다.

### ⑤ 왼쪽 경계를 막지 않은 `FR-` 검색 **잠재 2곳**

R38 이 저장소 전체를 훑었다 — **프로덕션 판정에 영향을 주는 활성 결함은 0건**이고
(`_specparse.py`·`gen_traceability.py`·`check_task_mapping.py`·
`check_partition_assignment.py` 전건 보호됨) 잠재 위험 2곳만 목록으로 남았다.
`.orch/R38/result_fr105_twins.md` 4-2. ★ **`.orch/_r24_brief_check.py` 에는 R21 의
처방 `(?<!N)` 이 이미 반영돼 있었다.**

### ⑥ `FR-101-AC3`ⓑ 는 **오늘 만족되고 있는가**

R38 이 `core/engine/rule_based.py::_rule_for()` 가 자원 태그 **셋을 리터럴로 안다**는
것을 `ast` 로 실측했다(`pv`·`ess`·`ev_v2g`. 레지스트리는 여섯이다). 조항은
*「신규 자원 클래스가 인터페이스만 구현하면 **코어 엔진 수정 없이** 동작」* 이다 —
**일곱째 자원이 디스패치 규칙을 필요로 하면 엔진을 고쳐야 한다.**
R38 이 세운 검사는 **「선언된 셋을 넘지 않는다」는 래칫**이며(늘면 빨간불)
「만족된다」를 주장하지 않는다. **정할 것: 규칙 선택을 자원 쪽으로 옮길 것인가.**

### ⑦ 다음 라운드가 짤 기계 그물 — 설계가 끝나 있다

R38 이 **「R37 의 두 그물에 안 걸린 세 번째 형태」** 를 찾았다 — *「인용이 여럿인데
그 전부가 다른 조항을 잰다」*. 인용 수를 세는 그물과 Phase 를 견주는 그물은 **둘 다
인용의 내용을 보지 않는다.** 그물 셋이 자기검사까지 붙어 설계돼 있고
(`.orch/R38/result_ac3_empty.md` 7절) **③-3 은 R38 이 세웠다.** 남은 둘:

- **③-1**(권함): 조항 문면이 구획을 **명시**하면 인용 중 적어도 하나는 그 구획을
  실제로 `import` 해야 한다. `AC3` 에 적용하면 **빨간불이었다**
- **③-2**(값 싸다): 한 조항의 인용 **전부**가 독스트링·오류 문면에서 다른 조항만
  적으면 후보로 올린다. ⚠ **판정이 아니라 지목이다**(독스트링은 거짓일 수 있다)

---

## 착수 전에 — 절차는 `status.md` 를 본다

- 인터프리터 · 게이트 명령 · 커밋 전 PATH — **`status.md` 「게이트」 절**
- 조항을 건드릴 때 함께 움직이는 자리 — **`status.md` 「파급 지점」 절**
- 변이 하네스 `.orch/mutate.py` — **한 번에 하나씩, 대상을 좁혀** 돌린다
  (여럿 이어 돌려 시간초과로 죽으면 `finally` 가 건너뛰어져 실물 소스에 변이가
  남는다 — R34 가 실제로 밟았다). ⚠ 옛·새 문면을 **`printf '%b'`** 로 쓴다 —
  `'%s'` 는 `\n` 을 그대로 두어 여러 줄 변이가 「대상 문면이 없다」(rc=2)가 된다
  (R35 ② 가 실제로 밟았다)
- ⚠ **검사를 새로 쓸 때 먼저 물을 것**: *이 단언에 등장하는 값 중 몇 개를 검사
  대상이 스스로 정하는가?* 전부라면 동어반복이다 — R35 ① 의 초록불 변이가 그것이었다
  (`status.md` 함정 절)
- ⚠ **수를 싣는 표에는 「그 수가 어느 조건에서 나왔는가」도 실어야 한다** —
  R35 ② 의 초록불 변이(두 끝의 값을 서로 바꿔 싣기)는 **거리만 보는 검사 일곱을
  전건 통과했다.** 값과 조건의 **짝**을 다른 층에서 검산할 것
