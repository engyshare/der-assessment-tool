# 다음 할 일 — R35 (2026-08-17 갱신 · ② 완료 후)

> ⚠ **이 파일은 정본이 아니다.** 판정 근거·실측 수치·라운드 경위는
> **`status.md` 의 「지금 할 일」** 이 갖는다. 여기 있는 것은 *무엇을 다음에
> 집는가*와 *어디서 시작하는가*뿐이다 — 같은 사실을 두 곳에 적으면 한쪽만
> 고쳐진다(이 저장소가 반복해서 잡아 온 형태다).
>
> 사람 몫(수동 검증 · 외부 자료 회신 · 배포 판단 · spec 개정)은
> **`status-human.md`** 가 소유자다. 여기 옮겨 적지 않는다.

## 0. 끝난 것 — 지우지 않고 표시만 한다

- [x] **커밋을 push 한다** — `3d71759` 까지 올라갔고 **CI 둘 다 `success`**.
      사용자 답: *「지금 push」*.
- [x] **5.1 이 비었을 때 무엇을 싣는가** (R35 ② · 위 status.md 머리 블록) —
      결론까지 남은 거리 · 결론 전환 지원율 · 전환까지 남는 거리 표.
      검사 8건 · 변이 9건 전건 빨간불.

## 1. 붙임 4 의 편익 산식이 단가를 싣지 않는다 (R35 ① 발견)

비용 쪽은 `대표일 수전 6.19kWh × 365일 × 120원/kWh` 로 **수량과 단가를 갈라**
적는데, 편익 쪽은 `대표일 1,771원 × 365일` 뿐이다 — **영향도 1위 인자의 값이
붙임 4 어디에도 없다**(붙임 1·2 에는 있다. ★ R35 ② 로 **본문 5.1 에도**
판매단가의 끝값이 실렸으나, 붙임 4 의 *산식*에는 여전히 없다).

- 자리 — `core/casegrid/e2e_runner.py::_benefit_lines` · 편익 8종
- 고치려면 **편익이 산식의 대입값을 내놓아야** 한다.
- ⚠ **러너에 태그 목록을 두지 말 것** — R34 가 `scales_with_dispatch_window`
  선언으로 없앤 형태로 되돌아간다. 목록은 편익이 늘 때 낡는다.

## 2. 1절 요약 행 목록을 검사로 붙든다 (R35 ② 발견)

**양식 【1】은 요약을 여섯 행으로 규정하는데 실물은 여덟 행을 낸다**(적정 용량 ·
잠정성이 뒤에 붙었다). 소절·붙임·표제는 `test_form_conformance.py` 가 양방향으로
대조하지만 **요약 표의 행 목록은 아무도 보지 않는다.**

- 그래서 R35 ② 는 요약에 「결론까지 남은 거리」를 **넣지 않았다** — 넣으면 그
  어긋남이 깊어지고, 검토자가 먼저 보는 절이 규정 밖에서 자란다.
- 자리 — `docs/report-form-심의보고서.md` 【1】 · `core/report/narrative.py::
  _summary_section` · `tests/report/test_form_conformance.py`(표제 검사와 같은 방식)
- 붙들고 나서 **거리 행을 요약에 올릴지** 판단한다(`MC-1` 의 첫 화면이다).

## 3. 프로포마 행 목록을 `CaseOutcome` 에 실을 것인가

R34 「붙들 수 없는 갈래」(변이 2b). 지금은 프로포마 쪽 구매 행을 조건부화해도
**밖에서 보이는 수가 한 자리도 움직이지 않아** 검사가 볼 자리가 없다.

- 자리 — `core/casegrid/models.py::CaseOutcome` · `e2e_runner.py`

## 4. 새 PV 결론을 다시 재기 — 일사 곡선

PV 가 좋아 보이는 이유는 여전히 **평탄 발전 프로파일**(야간 발전)이다. 붙임 8 이
크기를 잰다(수전 스텝에 실린 발전 연 985kWh). **일사 곡선이 배선되면 다시
뒤집힐 수 있다.** R35 는 이것을 건드리지 않았다.

- 자리 — `core/casegrid/profiles.py::DailyShapes` · `e2e_runner.py` 의
  `generation_profile`(통로는 이미 있고 쓰지 않는다)

## 5. `FR-401-AC2.VPPMarket` (Phase 2)

R33 표 4번 그대로. **자료가 아니라 순서 문제**이며 `AC1.VPP` 가 `[Phase 2]` 를
들고 있다. `VPP` 가 계약에 앉는가는 「미해결」의 그 행이 선행 판단이다.

## 6. `SettlementInputs.annual_generation_kwh` 를 옮긴다

협상값 자료형에 물리량이 남아 있다. 조립기를 **자원 없이** 부르는 자리(구조
비교·계약 테스트)가 있어 옮기지 않았고 사유는 그 필드 주석에 있다.
**그 호출자가 사라지면 옮긴다** — 지금 옮기면 그 호출부가 깨진다.

## 7. 닫기 라운드 — 마커 49곳을 읽어 대조 (R33 표 5)

**기계 몫은 끝났다.** 남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나이며
휴리스틱 둘이 전부 잡음이었다 — **읽어서 대조하는 수밖에 없다.** 그 자리는 R32 가
옮긴 마커 49곳이고, 정규식은 `(?<!N)FR-` 이다.

---

## 착수 전에 — 절차는 `status.md` 를 본다

- 인터프리터 · 게이트 명령 · 커밋 전 PATH — **`status.md` 「게이트」 절**
- 조항을 건드릴 때 함께 움직이는 자리 — **`status.md` 「파급 지점」 절**
- 변이 하네스 `.orch/mutate.py` — **한 번에 하나씩, 대상을 좁혀** 돌린다
  (여럿 이어 돌려 시간초과로 죽으면 `finally` 가 건너뛰어져 실물 소스에 변이가
  남는다 — R34 가 실제로 밟았다). ⚠ 옛·새 문면을 **`printf '%b'`** 로 쓴다 —
  `'%s'` 는 `\n` 을 그대로 두어 여러 줄 변이가 「대상 문면이 없다」(rc=2)가 된다
  (R35 ② 가 실제로 밟았다)
- ⚠ **검사를 새로 쓸 때 먼저 물을 것**: *이 단언에 등장하는 값 중 몇 개를 검사
  대상이 스스로 정하는가?* 전부라면 동어반복이다 — R35 ① 의 초록불 변이가 그것이었다
  (`status.md` 함정 절)
- ⚠ **수를 싣는 표에는 「그 수가 어느 조건에서 나왔는가」도 실어야 한다** —
  R35 ② 의 초록불 변이(두 끝의 값을 서로 바꿔 싣기)는 **거리만 보는 검사 일곱을
  전건 통과했다.** 값과 조건의 **짝**을 다른 층에서 검산할 것
