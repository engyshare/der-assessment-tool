# 다음 할 일 — R38 (2026-08-20 갱신 · **R37 은 닫혔다**)

> # ✔ R36 · R37 둘 다 `origin/main` 에 있다
>
> R36(붙임 4 편익 산식)과 R37(요약 행 · 마커 위생 · spec v0.17 · 얇은 리프 검사)이
> 끝났다. **판정 근거와 실측은 `status.md` 「지금 할 일」의 R37 블록**이 갖는다 —
> 여기 옮겨 적지 않는다.
>
> ⚠ **사람이 판단할 것이 둘 쌓였다** — spec 개정 등재 초안 2건(배타 규칙의 「구조 축」 ·
> `FR-205` 의 「선택 파라미터」 수용기준). 등재 자리는 **`status-human.md` 「7단계 — spec
> 개정 판단」** 이고 초안은 `.orch/R37/spec_amendment_draft.md` 다.
>
> ### 이 라운드가 남긴 착수 지시문 셋 — **읽으면 바로 시작할 수 있다**
>
> | 무엇 | 파일 |
> |---|---|
> | 주 지표 회수기간 결함 (**R38 ① 이 닫았다**) | `.orch/R37/brief_payback.md` · `result_payback.md` |
> | 교체비·잔존가치 (아래 **2번**) | `.orch/R37/scout_replacement.md` |
> | 일사 곡선 배선의 실측·검토 (**`main` 에 합쳤다**) | `.orch/R37/result_irradiance.md` · `review_irradiance.md` |
>
> ⚠ **`.orch/` 는 `.gitignore` 안이다** — 저장소에 들어오지 않는다. 다른 기계에서
> 이어받을 때는 **`status.md` 의 R38·R37 블록 요약이 정본**이다.

---

## 1. ~~골든 하니스가 「한 해에 행 여럿」을 만들지 않는다~~ — **R38 ② 가 쟀고 판정했다**

**판정: 지금 옮기지 않는다. 조건이 갖춰지면 옮긴다.** 아래가 그 조건이다.
실측 전문은 `.orch/R38/result_golden.md`(26KB) · 판정 사유는 `.orch/R38/orch_log.md` 5절.

**쟀더니 인계의 주장이 확증됐고, 크기가 나왔다:**

- 골든이 **단언하는 지표는 둘뿐**이다 — `npv_won`(완전 일치) · `payback_period_years`.
  ★ 그런데 그것은 **`payback_simple`** 이고 **R38 ① 이 고친 주 지표
  `payback_discounted` 는 골든의 단언에 아예 없다**(`irr`·`mirr` 도 없다)
- **(가) 에서는 행 단위·연 단위가 「정의상」 같은 값을 낸다** — 한 해에 행이 하나면
  `_flatten()` 20항 = `_by_year()` 20항. R38 이전 구현을 재현해 6조합을 재니
  **전건 「같음」**. **값이 안 움직인 것은 우연이 아니라 필연이었다**
- **(나) 로 옮기면 6조합 전부 갈리고 yaml 6개 값 전부 바뀐다.** `npv` 는 세 시나리오
  모두 정확히 **−2,836,243원**(O&M 20년 현가) · **무보조는 부호가 뒤집힌다**
  (+2,270,362 → **−565,881**) · `payback_discounted` 는 **회수 → 미회수**
- 고칠 파일은 **4개**(테스트 1 + yaml 3). 대조를 재사용하는 곳 1개는 따라오고,
  CI 게이트·앱 경로 3개는 `expected_values` 를 읽지 않아 무관하다

### 옮기지 않은 사유 — **작아서가 아니다**

1. **★ 측정한 (나) 는 비용의 하한이다** — `GridPurchase` 와 정산 수수료 행을 채우지
   못했다(단가가 없다). 지금 옮기면 **「옛 허구」를 「새 허구」로 바꾸는 것**이고,
   기준선을 흔들면서도 여전히 실행 경로와 다른 모양을 재게 된다
2. **기준선을 흔드는 것은 한 번만 해야 하고, 그때 실행 경로의 행 구성이 확정돼
   있어야 한다.** 그런데 **아래 2번(교체비·잔존가치)이 비용 행을 더 늘린다** —
   지금 옮기면 그 뒤에 또 한 번 흔들어야 한다

> ### ▶ 옮기는 조건 — **2번 배선이 끝난 직후**
> 그때 (나) 를 실행 경로에서 **직접 가져온다**(손으로 행을 세우지 않는다).
> 6개 값을 다시 뽑고 **그 사유를 커밋 메시지에 적는다.**

- ⚠ 부수 관찰(결함 아님): `mirr` 은 두 모양에서 **차이가 정확히 0** 이다 —
  `amount > 0` 을 행 단위로 걸러 **부호 뒤집힌 비용 행이 전부 걸러진다.** 함수
  독스트링이 *「PV(costs, finance_rate) = initial (t=0)」* 이라 **선언한 대로**다.
  즉 **`mirr` 을 골든에 실어도 비용 행 유무를 못 붙든다**
- R38 ② 가 함께 한 것: 「이 골든은 파수꾼이 아니다」 주석이 `_reference_metrics()`
  안에만 있고 **테스트가 실제로 부르는 `_scenario_metrics()` 에는 없었다.**
  그리고 **`tests/acceptance2/test_17_7_dod7.py` 가 그 허구 모양을 물려받는다** —
  후속(`.orch/R38/result_dod7.md`)

## 2. **교체비·잔존가치가 프로포마에 들어가지 않는다** — 정찰 끝났다

`status.md` 「미해결」의 R33 항목. 부품은 실재하는데(`ESS.replacement_schedule()` ·
`core/cba/salvage.py::salvage_value()`) **실행 경로의 비용 행은 고정 O&M 둘뿐**이다 —
R37 이 `FR-402-AC2.C` 에서, 그리고 회수기간에서 두 번 만난 **「선언과 구현이 갈린」**
형태의 셋째다.

**정찰 보고서가 있다: `.orch/R37/scout_replacement.md`** — 부품 규약 · 프로포마 통로 ·
손계산 · 파급 목록 · 사람 판단 6건까지.

### ✔ R38 ② 가 기준선을 다시 세고 갈래를 판정했다 — **정찰의 수는 틀려 있었다**

실측 전문 `.orch/R38/result_replacement_baseline.md` · 판정 사유 `orch_log.md` 5절.

| | 정찰이 적은 값 | **실물 (2026-08-20 실측)** |
|---|---|---|
| 순현재가치(무보조) | −5,156,392원 | **−5,284,586원** |
| 총사업비 | (안 적혀 있었다) | **9,800,000원** |
| 결손/총사업비 | 52.6% → 「약 59.5%」 | **53.9%** → 아래 갈래표 |

**★ 「다시 세야 한다」던 그 수가 실제로 틀려 있었다** — 정찰의 「→ 약 −5,830,240원 ·
59.5%」는 성립하지 않는다. 출처는 `docs/evidence/MC-1-검토용-리포트-2026-08-17.md`
(:20 · :117 · :118 · :161)이고, 보조 20%·80% 는 **산식으로 독립 재현**해 교차 확인했다.

**순효과 −673,848원은 그대로 성립한다 — 코드로 검산했다.** 일사 배선
(`generation_profile_kwh`)이 `replacement_schedule`·`salvage_value`·`capex` 근처에
**참조 0곳**이고 할인율·교체 연차·단가가 전부 불변이다.

### 갈래 조합표 — **결론의 방향은 어느 갈래를 골라도 안전하다**

| 갈래 조합 | 순효과 | 결론축(무보조) | 결손/총사업비 |
|---|---|---|---|
| ②가·④기존 (지금 코드) | −673,848원 | −5,958,434원 | 60.8% |
| ②가·④PV포함 | −547,640원 | −5,832,226원 | 59.5% |
| ②나·④기존 | −896,642원 | −6,181,228원 | 63.1% |
| **②나·④PV포함 ← 판정** | **−770,434원** | **−6,055,020원** | **61.8%** |

**네 칸 모두 회수 여부가 같다**(무보조 미회수 / 20% 미회수 / 80% 회수).
갈리는 것은 크기뿐이다. ⚠ 「단독 전환 인자 0건」은 **네 칸 중 한 칸만** 재계산됐다 —
나머지 셋은 정황 추론이며 **배선 후 실물 리포트로 확정해야 한다.**

### ✔ 판정된 것 둘 · ⏸ 막힌 것 하나

- **④ → PV 인버터 취득분의 잔존가치도 센다**(+126,208원). spec 1918행 산식이
  **어느 취득가인지 말하지 않고**(침묵) ESS 는 그 침묵을 「취득 시점 리셋」으로
  메우며 사유를 독스트링에 적어 두었다. **정본이 이미 있고 PV 가 안 따랐다.**
  → spec 1918행의 침묵은 **조항 개정 대상**(사람 몫 · `status-human.md`)
- **② → (나). ESS 교체비도 물가 계수를 태운다.** 대장이 `price_basis: "명목"` 을
  1회 선언하고 `DV-7` 이 강제한다 — 명목 프로포마에서 18년차 지출을 오늘의 원으로
  적으면 **그 지출만 실질이 되어 선언과 어긋난다.** 반대 논거(학습곡선)는 **실질
  추세** 항목을 요구하고 **그 항목이 대장에 없다** → 실질 추세 0 을 선언하고
  가정으로 등재한다
- **⏸ ③(교체 단가가 시스템 전체인가 배터리만인가) 는 대신 정하지 않는다** —
  `Q-2` 회신에 묶여 있고 `status-human.md:156` 이 *「⚠ ESS 는 PCS 별도인지 시스템
  일괄인지 반드시 확인」* 이라 이미 적었다. **정박점을 우리가 만들면 자기충족
  검증이 된다**(이 저장소가 `Q-4`·`Q-5` 에 세운 원칙). 순효과를 19% 움직인다
- ⚠ `docs/manual-checks.yaml` 의 `MC-1`~`MC-8` 전건이 UI·사용성이라 **이 결손을 재는
  수동 검증이 하나도 없다** — 배선 여부는 `pytest` 와 리포트 재생성으로만 판정된다

### ⚠⚠ 배선 전에 닫아야 하는 선행 — **R38 ② 가 찾은 결함**

```
e2e_runner.py:98   PV_OM_ESCALATION = 0.02      ← 이름은 「O&M」
e2e_runner.py:334    escalation_rate=PV_OM_ESCALATION,   ← PV 에만 넘긴다
pv.py:499  fixed_om    · pv.py:531 (변동분) · pv.py:563 교체비 capex   ← 셋을 함께 굴린다
```

**상수의 이름이 실제 쓰임보다 좁게 주장한다** — 이 저장소가 열여섯 번 만난 결함의
**거울상**이다(지금까지는 전부 검사가 넓게 주장하는 것이었다). ② 를 그대로 배선하면
**의미가 다른 상수를 조용히 재사용**하게 된다.
→ R38 ② 가 **배선 없이** 선행 둘만 처리했다: 상수가 자기 쓰임을 말하게 하고,
어긋남을 **래칫**으로 고정(`KNOWN_TARIFF_DEBT` 관용구 — 「늘면 즉시 빨개진다」).
결과 `.orch/R38/result_escalation_debt.md`

### 남은 것 — 배선 그 자체

- 가장 큰 개별 항목은 **ESS 20년차 잔존가치 1,707,353원**이고 **붙임 8 은 그것을 싣지
  않는다** — 지금 리포트는 이 결손을 **한쪽만** 드러낸다
- ⚠ **교체비(악화)와 잔존가치(개선)를 갈라 커밋할 수 없다** — 양식
  (`docs/report-form-심의보고서.md:252-256`)이 *「한쪽만 반영한 수를 결론으로 올리지
  않는다 … NSPM 대칭성」* 으로 금지한다. **한 단위로 간다**(판정)
- ⏸ **일회성 흐름을 붙임 4 에 어떻게 싣는가는 배선 라운드의 첫 판정이다.**
  `(가) annual_won=0` 은 **배제됐다** — 금액 칸이 0원이라 「비용이 없다」로 읽히고
  그것은 「합계만 있는 표에서는 빠진 행이 드러나지 않는다」의 재발이다.
  (나) 자료형 확장 대 (다) 표 둘로 가르기는 **실을 값이 확정된 뒤** 고른다

## 2-b. R38 이 찾았으나 닫지 않은 것 — **크기 순**

전문은 `.orch/R38/` 의 각 `result_*.md` 이고 판정 경위는 `orch_log.md` 다.
⚠ `.orch/` 는 `.gitignore` 안이라 **다른 기계에는 없다** — 아래 요약이 정본이다.

### ① ★ `test_ess.py` 의 자기충족 검사 — **옮길 곳이 없다. 지울지 정해야 한다**

`test_rc_all_c2_geometric_series_formula_is_resource_independent` 는 단언이 한 줄이고
**`DER` 메서드를 하나도 부르지 않는다** — 검사가 오라클 산식을 스스로 계산해 상수와
견준다. 붙드는 것은 `to_won` 의 반올림 하나다. **변이로 증명했다**:
`ess.fixed_om()` 의 물가 계수를 지우니 **이 검사는 초록불**이고 13줄 아래 이웃
(`..._20y_cumulative_matches_geometric_sum`)이 **잡았다.**

- `FR-101-AC2` 인용은 거짓이고 **`AC5` 로도 갈 수 없다**(그 셋을 안 부른다) —
  그래서 R38 의 이관 16건에서 **뺐다**
- 구현을 불러 견주게 고치면 **이웃과 중복**이 된다 → 「같은 사실이 두 곳에 있으면
  한쪽만 고쳐진다」에 걸린다
- **정할 것: 지울 것인가.** 이름이 `..._is_resource_independent` 이므로 목적을
  다른 형태로 살릴지도 함께 정한다

### ② `FR-101-AC2` 의 매핑이 **한 파일에 몰렸다**

이관 뒤 남은 8건 중 조항을 실제로 지키는 것은 **5건**이고 그중 **4건이
`tests/contract/test_der_contract.py`** 다. 그 파일이 흔들리면 **Must-have · Phase 1
조항이 미매핑에 근접한다.** 이관 전에는 24건에 흩어져 보이지 않던 위험이다.

### ③ 「같은 개념을 네 이름으로 부른다」 — 훑기 함정

`AC4`(부속설비 독립 수명)의 두 번째 수명이 자원마다 이름이 다르다 —
`subcomponents`(`Load`·`ThermalLoad`) · `pump_lifetime`(`HeatPump`) ·
`inverter_lifetime`(`PV`) · `pcs_lifetime`(`ESS`). **이름으로 훑고 멈추면 `AC4`
마커 다섯을 「근거 없음」으로 오판한다** — R38 의 검증자가 실제로 한 번 그렇게 셌고
소스를 열어 정정했다.

### ④ 소스 문면 검사가 **들여쓴 지역 import 를 놓친다**

R38 이 `tests/contract/source_scan.py` 로 필터를 공용화하며 확인했다 —
`line.startswith(("import core", "from core"))` 는 **줄이 들여쓰기 없이 시작하는지만**
본다. 함수 **안**의 지역 import 는 **안 걸린다**(변이로 확인). 「함정의 반대 방향」
(과소 검출)이며 독스트링에 갈라 적었다. **정할 것: 지역 import 도 잡을 것인가** —
잡으려면 `ast` 로 `Import`/`ImportFrom` 노드를 보는 것이 옳다.

### ⑤ 왼쪽 경계를 막지 않은 `FR-` 검색 **잠재 2곳**

R38 이 저장소 전체를 훑었다 — **프로덕션 판정에 영향을 주는 활성 결함은 0건**이고
(`_specparse.py`·`gen_traceability.py`·`check_task_mapping.py`·
`check_partition_assignment.py` 전건 보호됨) 잠재 위험 2곳만 목록으로 남았다.
`.orch/R38/result_fr105_twins.md` 4-2. ★ **`.orch/_r24_brief_check.py` 에는 R21 의
처방 `(?<!N)` 이 이미 반영돼 있었다.**

### ⑥ `FR-101-AC3`ⓑ 는 **오늘 만족되고 있는가**

R38 이 `core/engine/rule_based.py::_rule_for()` 가 자원 태그 **셋을 리터럴로 안다**는
것을 `ast` 로 실측했다(`pv`·`ess`·`ev_v2g`. 레지스트리는 여섯이다). 조항은
*「신규 자원 클래스가 인터페이스만 구현하면 **코어 엔진 수정 없이** 동작」* 이다 —
**일곱째 자원이 디스패치 규칙을 필요로 하면 엔진을 고쳐야 한다.**
R38 이 세운 검사는 **「선언된 셋을 넘지 않는다」는 래칫**이며(늘면 빨간불)
「만족된다」를 주장하지 않는다. **정할 것: 규칙 선택을 자원 쪽으로 옮길 것인가.**

### ⑦ 다음 라운드가 짤 기계 그물 — 설계가 끝나 있다

R38 이 **「R37 의 두 그물에 안 걸린 세 번째 형태」** 를 찾았다 — *「인용이 여럿인데
그 전부가 다른 조항을 잰다」*. 인용 수를 세는 그물과 Phase 를 견주는 그물은 **둘 다
인용의 내용을 보지 않는다.** 그물 셋이 자기검사까지 붙어 설계돼 있고
(`.orch/R38/result_ac3_empty.md` 7절) **③-3 은 R38 이 세웠다.** 남은 둘:

- **③-1**(권함): 조항 문면이 구획을 **명시**하면 인용 중 적어도 하나는 그 구획을
  실제로 `import` 해야 한다. `AC3` 에 적용하면 **빨간불이었다**
- **③-2**(값 싸다): 한 조항의 인용 **전부**가 독스트링·오류 문면에서 다른 조항만
  적으면 후보로 올린다. ⚠ **판정이 아니라 지목이다**(독스트링은 거짓일 수 있다)

## 3. 프로포마 행 목록을 `CaseOutcome` 에 실을 것인가

R34 「붙들 수 없는 갈래」 · R36 판정: **검사를 세우려고 산출물 자료형을 넓히는 것은
순서가 거꾸로다.** 지금은 조건부화해도 밖에서 보이는 수가 한 자리도 움직이지 않는다.

- 자리 — `core/casegrid/models.py::CaseOutcome` · `e2e_runner.py`

## 4. `FR-401-AC2.VPPMarket` (Phase 2)

R33 표 4번. **자료가 아니라 순서 문제**이며 `AC1.VPP` 가 `[Phase 2]` 를 들고 있다.
`VPP` 가 계약에 앉는가는 「미해결」의 그 행이 선행 판단이다.
★ R37 이 **거부 사유가 spec 이 선언한 선행 조항을 가리키는지**를 검사로 붙들었으므로,
Phase 2 에서 이 조항을 열 때 그 검사가 먼저 빨간불이 난다.

## 5. `SettlementInputs.annual_generation_kwh` 를 옮긴다

협상값 자료형에 물리량이 남아 있다. 조립기를 **자원 없이** 부르는 호출자가 있어
옮기지 않았고 사유는 그 필드 주석에 있다. **그 호출자가 사라지면 옮긴다.**

## 6. 얇은 리프의 남은 항목

R37 이 `SurplusDirectSale` 1 → 5 · `VPP` 2 → 3 으로 세웠다. 남은 것은
`.orch/R37/gap_thin_leaves.md` 에 적혀 있고, 그중 **`FR-202` 비교표 계층**(두 구조가
비교표에서 다른 줄로 나오는가)은 리포트 조립 계층이라 R37 범위 밖이었다.

---

---

# (직전) R35 ③ 까지의 목록

> ⚠ **이 파일은 정본이 아니다.** 판정 근거·실측 수치·라운드 경위는
> **`status.md` 의 「지금 할 일」** 이 갖는다. 여기 있는 것은 *무엇을 다음에
> 집는가*와 *어디서 시작하는가*뿐이다 — 같은 사실을 두 곳에 적으면 한쪽만
> 고쳐진다(이 저장소가 반복해서 잡아 온 형태다).
>
> 사람 몫(수동 검증 · 외부 자료 회신 · 배포 판단 · spec 개정)은
> **`status-human.md`** 가 소유자다. 여기 옮겨 적지 않는다.

## 0. 끝난 것 — 지우지 않고 표시만 한다

- [x] **커밋을 push 한다** — `3d71759` 까지 올라갔고 **CI 둘 다 `success`**.
      사용자 답: *「지금 push」*.
- [x] **5.1 이 비었을 때 무엇을 싣는가** (R35 ② · 위 status.md 머리 블록) —
      결론까지 남은 거리 · 결론 전환 지원율 · 전환까지 남는 거리 표.
      검사 8건 · 변이 9건 전건 빨간불.
- [x] **1절 요약 행 목록을 검사로 붙든다** (R35 ③ · 종전 2번) —
      `test_summary_rows_match_the_form` 신설 · 변이 6건 전건 빨간불.
      ⚠ **이 항목의 전제가 틀려 있었다** — 「양식 6행 · 실물 8행」은 사실이
      아니고 양식은 이미 여덟 행이었다. 낡아 있던 것은 `_summary_section`
      독스트링 한 줄이며 그 사본이 계획으로 따라 들어갔다. **실제 어긋남은
      「결합 전환 조건」↔「결론 전환 조건 (결합)」이었고 새 검사가 첫 실행에서
      잡았다.**

## 1. 「없음」이 **첫 화면에는 그대로 남아 있다** (R35 ③ 이 남긴 판단)

R35 ② 가 5.1 에서 없앤 「없음」이 **1절 요약**에는 그대로다 —
`결론 전환 인자 (단독) | 없음 (1변수 스윕 범위 내)`. 검토자가 먼저 보는 칸이다.

- ✔ **거리 행은 올리지 않기로 이미 판단했다**(R35 ③) — 요약의
  `결론 축 · 순현재가치` **−5,156,392원** 과 5.1 의 거리 **결손 5,156,392원**
  은 같은 수다. 부호만 뒤집어 두 번 싣는 것이며 「새 수를 만들지 않는다」에 걸린다.
- **올린다면 「결론 전환 지원율」 한 행**이다 — 절대액은 사업 규모를 모르면
  크기를 못 읽지만 **비율 52.6%** 는 읽힌다. 무보조·보조 80% 가 같은 값을 내는
  것이 이미 검사로 붙들려 있다.
- 자리 — `docs/report-form-심의보고서.md` 【1】(행 하나 추가) ·
  `core/report/narrative.py::_summary_section` · 리포트 재생성.
  ⚠ 행을 더하면 `test_summary_rows_match_the_form` 이 **양식을 먼저** 고치라고
  막는다 — 그것이 이 검사가 서 있는 이유다.
- ⚠ **사용자 판단 대기** — 검토자가 먼저 보는 규정을 바꾸는 일이다.

## 2. ~~붙임 4 의 편익 산식이 단가를 싣지 않는다~~ — **R36 이 닫았다 (커밋 전)**

아래 원문은 남겨 둔다. **고친 방식**: 러너에 태그 목록을 두지 말라는 이 항목의
경고대로, `ValueStream.formula()` 를 추상 메서드로 세워 **편익이 대입값을
내놓는다**. 러너는 `× 365일` 과 `= 합계` 만 붙인다.

<details><summary>원문 (R35 ① 발견)</summary>

비용 쪽은 `대표일 수전 6.19kWh × 365일 × 120원/kWh` 로 **수량과 단가를 갈라**
적는데, 편익 쪽은 `대표일 1,771원 × 365일` 뿐이다 — **영향도 1위 인자의 값이
붙임 4 어디에도 없다**(붙임 1·2 에는 있다. ★ R35 ② 로 **본문 5.1 에도**
판매단가의 끝값이 실렸으나, 붙임 4 의 *산식*에는 여전히 없다).

- 자리 — `core/casegrid/e2e_runner.py::_benefit_lines` · 편익 8종
- 고치려면 **편익이 산식의 대입값을 내놓아야** 한다.
- ⚠ **러너에 태그 목록을 두지 말 것** — R34 가 `scales_with_dispatch_window`
  선언으로 없앤 형태로 되돌아간다. 목록은 편익이 늘 때 낡는다.

</details>

## 2. 1절 요약 행 목록을 검사로 붙든다 (R35 ② 발견)

**양식 【1】은 요약을 여섯 행으로 규정하는데 실물은 여덟 행을 낸다**(적정 용량 ·
잠정성이 뒤에 붙었다). 소절·붙임·표제는 `test_form_conformance.py` 가 양방향으로
대조하지만 **요약 표의 행 목록은 아무도 보지 않는다.**

- 그래서 R35 ② 는 요약에 「결론까지 남은 거리」를 **넣지 않았다** — 넣으면 그
  어긋남이 깊어지고, 검토자가 먼저 보는 절이 규정 밖에서 자란다.
- 자리 — `docs/report-form-심의보고서.md` 【1】 · `core/report/narrative.py::
  _summary_section` · `tests/report/test_form_conformance.py`(표제 검사와 같은 방식)
- 붙들고 나서 **거리 행을 요약에 올릴지** 판단한다(`MC-1` 의 첫 화면이다).

## 3. 프로포마 행 목록을 `CaseOutcome` 에 실을 것인가

R34 「붙들 수 없는 갈래」(변이 2b). 지금은 프로포마 쪽 구매 행을 조건부화해도
**밖에서 보이는 수가 한 자리도 움직이지 않아** 검사가 볼 자리가 없다.

- 자리 — `core/casegrid/models.py::CaseOutcome` · `e2e_runner.py`

## 4. 새 PV 결론을 다시 재기 — 일사 곡선

PV 가 좋아 보이는 이유는 여전히 **평탄 발전 프로파일**(야간 발전)이다. 붙임 8 이
크기를 잰다(수전 스텝에 실린 발전 연 985kWh). **일사 곡선이 배선되면 다시
뒤집힐 수 있다.** R35 는 이것을 건드리지 않았다.

- 자리 — `core/casegrid/profiles.py::DailyShapes` · `e2e_runner.py` 의
  `generation_profile`(통로는 이미 있고 쓰지 않는다)

## 5. `FR-401-AC2.VPPMarket` (Phase 2)

R33 표 4번 그대로. **자료가 아니라 순서 문제**이며 `AC1.VPP` 가 `[Phase 2]` 를
들고 있다. `VPP` 가 계약에 앉는가는 「미해결」의 그 행이 선행 판단이다.

## 6. `SettlementInputs.annual_generation_kwh` 를 옮긴다

협상값 자료형에 물리량이 남아 있다. 조립기를 **자원 없이** 부르는 자리(구조
비교·계약 테스트)가 있어 옮기지 않았고 사유는 그 필드 주석에 있다.
**그 호출자가 사라지면 옮긴다** — 지금 옮기면 그 호출부가 깨진다.

## 7. 닫기 라운드 — 마커 49곳을 읽어 대조 (R33 표 5)

**기계 몫은 끝났다.** 남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나이며
휴리스틱 둘이 전부 잡음이었다 — **읽어서 대조하는 수밖에 없다.** 그 자리는 R32 가
옮긴 마커 49곳이고, 정규식은 `(?<!N)FR-` 이다.

---

## 착수 전에 — 절차는 `status.md` 를 본다

- 인터프리터 · 게이트 명령 · 커밋 전 PATH — **`status.md` 「게이트」 절**
- 조항을 건드릴 때 함께 움직이는 자리 — **`status.md` 「파급 지점」 절**
- 변이 하네스 `.orch/mutate.py` — **한 번에 하나씩, 대상을 좁혀** 돌린다
  (여럿 이어 돌려 시간초과로 죽으면 `finally` 가 건너뛰어져 실물 소스에 변이가
  남는다 — R34 가 실제로 밟았다). ⚠ 옛·새 문면을 **`printf '%b'`** 로 쓴다 —
  `'%s'` 는 `\n` 을 그대로 두어 여러 줄 변이가 「대상 문면이 없다」(rc=2)가 된다
  (R35 ② 가 실제로 밟았다)
- ⚠ **검사를 새로 쓸 때 먼저 물을 것**: *이 단언에 등장하는 값 중 몇 개를 검사
  대상이 스스로 정하는가?* 전부라면 동어반복이다 — R35 ① 의 초록불 변이가 그것이었다
  (`status.md` 함정 절)
- ⚠ **수를 싣는 표에는 「그 수가 어느 조건에서 나왔는가」도 실어야 한다** —
  R35 ② 의 초록불 변이(두 끝의 값을 서로 바꿔 싣기)는 **거리만 보는 검사 일곱을
  전건 통과했다.** 값과 조건의 **짝**을 다른 층에서 검산할 것
