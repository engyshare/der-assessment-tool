# 다음 할 일 — R38 (2026-08-20 갱신 · **R37 은 닫혔다**)

> # ✔ R36 · R37 둘 다 `origin/main` 에 있다
>
> R36(붙임 4 편익 산식)과 R37(요약 행 · 마커 위생 · spec v0.17 · 얇은 리프 검사)이
> 끝났다. **판정 근거와 실측은 `status.md` 「지금 할 일」의 R37 블록**이 갖는다 —
> 여기 옮겨 적지 않는다.
>
> ⚠ **사람이 판단할 것이 둘 쌓였다** — spec 개정 등재 초안 2건(배타 규칙의 「구조 축」 ·
> `FR-205` 의 「선택 파라미터」 수용기준). 등재 자리는 **`status-human.md` 「7단계 — spec
> 개정 판단」** 이고 초안은 `.orch/R37/spec_amendment_draft.md` 다.
>
> ### 이 라운드가 남긴 착수 지시문 셋 — **읽으면 바로 시작할 수 있다**
>
> | 무엇 | 파일 |
> |---|---|
> | 주 지표 회수기간 결함 (아래 **1번**) | `.orch/R37/brief_payback.md` |
> | 교체비·잔존가치 (아래 **2번**) | `.orch/R37/scout_replacement.md` |
> | 일사 곡선 배선의 실측·검토 (**`main` 에 합쳤다**) | `.orch/R37/result_irradiance.md` · `review_irradiance.md` |
>
> ⚠ **`.orch/` 는 `.gitignore` 안이다** — 저장소에 들어오지 않는다. 다른 기계에서
> 이어받을 때는 **`status.md` R37 블록의 요약이 정본**이다.

---

## 1. ★ **주 지표(할인 회수기간)가 행 단위로 계산된다** — 먼저 집을 것

**지시문이 이미 있다: `.orch/R37/brief_payback.md`** (실측 수치 · 무엇을 하는가 ·
변이 후보 · 오라클 결함까지). 판정 근거는 `status.md` R37 블록.

한 줄로: `core/cba/metrics.py::_flatten` 이 같은 해의 행을 **합치지 않아서**
`payback_discounted` 가 **그 해의 편익 행만 누적한 지점에서 반환**한다. 보조 80%
회수기간이 **1.53년 과소**이고 **사업이 나빠질 때 짧아지는 방향**으로 움직인다.

- ⚠ `payback_simple` 도 같은 결함이며 **그 오라클은 편익 1행만 넘겨 못 본다** —
  값을 고치는 것보다 **그 자리를 보는 검사를 세우는 것**이 이 라운드의 핵심이다
- ⚠ `_flatten` 은 `irr`·`mirr` 도 쓴다. 그쪽 값은 **바뀌지 않아야 한다**(전부 합하므로
  순서와 무관하다) — 바뀌면 무언가 잘못한 것이며 그것을 검사로 붙든다
- 회수 여부는 NPV 가 정하므로 **결론은 뒤집히지 않는다.** 위험은 한계 케이스에서
  1절의 두 줄이 서로를 부정하는 것이다

## 2. **교체비·잔존가치가 프로포마에 들어가지 않는다** — 정찰 끝났다

`status.md` 「미해결」의 R33 항목. 부품은 실재하는데(`ESS.replacement_schedule()` ·
`core/cba/salvage.py::salvage_value()`) **실행 경로의 비용 행은 고정 O&M 둘뿐**이다 —
R37 이 `FR-402-AC2.C` 에서, 그리고 회수기간에서 두 번 만난 **「선언과 구현이 갈린」**
형태의 셋째다.

**정찰 보고서가 있다: `.orch/R37/scout_replacement.md`** — 부품 규약 · 프로포마 통로 ·
손계산 · 파급 목록 · 사람 판단 6건까지.

- **손계산 순효과 약 −673,848원**(교체비 현가 −2,779,258 + 잔존가치 현가 +2,105,410).
  **단독 전환 인자 0건 · 미회수 · 4.4 형태는 유지**된다
- ⚠⚠ **그 정찰의 기준선이 이미 낡았다** — 손계산은 결론 축 **−5,156,392원**(일사 곡선
  **배선 전**) 위에서 했고, 배선을 합친 지금 기준선은 **−5,284,586원**이다. **순효과
  −673,848원 자체는 교체·잔존 현금흐름이므로 그대로 성립하지만**(배선은 편익·구매
  쪽을 움직였다) 「→ 약 −5,830,240원 · 59.5%」로 적힌 **합계와 비율은 다시 세야 한다.**
  세지 않고 옮겨 적으면 그것이 다음 라운드의 계획으로 따라 들어간다(이 저장소가 반복해서
  밟은 형태다 — `status.md` 함정 절)
- 가장 큰 개별 항목은 **ESS 20년차 잔존가치 1,707,353원**이고 **붙임 8 은 그것을 싣지
  않는다** — 즉 지금 리포트는 이 결손을 **한쪽만** 드러낸다
- ⚠ **부품 규약 하나를 어느 쪽으로 정하느냐가 순효과를 19%(126,188원) 움직인다** —
  정찰 §4-6 에 갈래가 적혀 있다. 사람 판단 6건 중 첫째다
- ⚠ 일사 곡선(1번과 함께) 배선 뒤 값이므로 **순서를 정해 하나씩** 넣는다. 섞으면 어느
  것이 무엇을 움직였는지 표에서 읽을 수 없다

## 3. 프로포마 행 목록을 `CaseOutcome` 에 실을 것인가

R34 「붙들 수 없는 갈래」 · R36 판정: **검사를 세우려고 산출물 자료형을 넓히는 것은
순서가 거꾸로다.** 지금은 조건부화해도 밖에서 보이는 수가 한 자리도 움직이지 않는다.

- 자리 — `core/casegrid/models.py::CaseOutcome` · `e2e_runner.py`

## 4. `FR-401-AC2.VPPMarket` (Phase 2)

R33 표 4번. **자료가 아니라 순서 문제**이며 `AC1.VPP` 가 `[Phase 2]` 를 들고 있다.
`VPP` 가 계약에 앉는가는 「미해결」의 그 행이 선행 판단이다.
★ R37 이 **거부 사유가 spec 이 선언한 선행 조항을 가리키는지**를 검사로 붙들었으므로,
Phase 2 에서 이 조항을 열 때 그 검사가 먼저 빨간불이 난다.

## 5. `SettlementInputs.annual_generation_kwh` 를 옮긴다

협상값 자료형에 물리량이 남아 있다. 조립기를 **자원 없이** 부르는 호출자가 있어
옮기지 않았고 사유는 그 필드 주석에 있다. **그 호출자가 사라지면 옮긴다.**

## 6. 얇은 리프의 남은 항목

R37 이 `SurplusDirectSale` 1 → 5 · `VPP` 2 → 3 으로 세웠다. 남은 것은
`.orch/R37/gap_thin_leaves.md` 에 적혀 있고, 그중 **`FR-202` 비교표 계층**(두 구조가
비교표에서 다른 줄로 나오는가)은 리포트 조립 계층이라 R37 범위 밖이었다.

---

---

# (직전) R35 ③ 까지의 목록

> ⚠ **이 파일은 정본이 아니다.** 판정 근거·실측 수치·라운드 경위는
> **`status.md` 의 「지금 할 일」** 이 갖는다. 여기 있는 것은 *무엇을 다음에
> 집는가*와 *어디서 시작하는가*뿐이다 — 같은 사실을 두 곳에 적으면 한쪽만
> 고쳐진다(이 저장소가 반복해서 잡아 온 형태다).
>
> 사람 몫(수동 검증 · 외부 자료 회신 · 배포 판단 · spec 개정)은
> **`status-human.md`** 가 소유자다. 여기 옮겨 적지 않는다.

## 0. 끝난 것 — 지우지 않고 표시만 한다

- [x] **커밋을 push 한다** — `3d71759` 까지 올라갔고 **CI 둘 다 `success`**.
      사용자 답: *「지금 push」*.
- [x] **5.1 이 비었을 때 무엇을 싣는가** (R35 ② · 위 status.md 머리 블록) —
      결론까지 남은 거리 · 결론 전환 지원율 · 전환까지 남는 거리 표.
      검사 8건 · 변이 9건 전건 빨간불.
- [x] **1절 요약 행 목록을 검사로 붙든다** (R35 ③ · 종전 2번) —
      `test_summary_rows_match_the_form` 신설 · 변이 6건 전건 빨간불.
      ⚠ **이 항목의 전제가 틀려 있었다** — 「양식 6행 · 실물 8행」은 사실이
      아니고 양식은 이미 여덟 행이었다. 낡아 있던 것은 `_summary_section`
      독스트링 한 줄이며 그 사본이 계획으로 따라 들어갔다. **실제 어긋남은
      「결합 전환 조건」↔「결론 전환 조건 (결합)」이었고 새 검사가 첫 실행에서
      잡았다.**

## 1. 「없음」이 **첫 화면에는 그대로 남아 있다** (R35 ③ 이 남긴 판단)

R35 ② 가 5.1 에서 없앤 「없음」이 **1절 요약**에는 그대로다 —
`결론 전환 인자 (단독) | 없음 (1변수 스윕 범위 내)`. 검토자가 먼저 보는 칸이다.

- ✔ **거리 행은 올리지 않기로 이미 판단했다**(R35 ③) — 요약의
  `결론 축 · 순현재가치` **−5,156,392원** 과 5.1 의 거리 **결손 5,156,392원**
  은 같은 수다. 부호만 뒤집어 두 번 싣는 것이며 「새 수를 만들지 않는다」에 걸린다.
- **올린다면 「결론 전환 지원율」 한 행**이다 — 절대액은 사업 규모를 모르면
  크기를 못 읽지만 **비율 52.6%** 는 읽힌다. 무보조·보조 80% 가 같은 값을 내는
  것이 이미 검사로 붙들려 있다.
- 자리 — `docs/report-form-심의보고서.md` 【1】(행 하나 추가) ·
  `core/report/narrative.py::_summary_section` · 리포트 재생성.
  ⚠ 행을 더하면 `test_summary_rows_match_the_form` 이 **양식을 먼저** 고치라고
  막는다 — 그것이 이 검사가 서 있는 이유다.
- ⚠ **사용자 판단 대기** — 검토자가 먼저 보는 규정을 바꾸는 일이다.

## 2. ~~붙임 4 의 편익 산식이 단가를 싣지 않는다~~ — **R36 이 닫았다 (커밋 전)**

아래 원문은 남겨 둔다. **고친 방식**: 러너에 태그 목록을 두지 말라는 이 항목의
경고대로, `ValueStream.formula()` 를 추상 메서드로 세워 **편익이 대입값을
내놓는다**. 러너는 `× 365일` 과 `= 합계` 만 붙인다.

<details><summary>원문 (R35 ① 발견)</summary>

비용 쪽은 `대표일 수전 6.19kWh × 365일 × 120원/kWh` 로 **수량과 단가를 갈라**
적는데, 편익 쪽은 `대표일 1,771원 × 365일` 뿐이다 — **영향도 1위 인자의 값이
붙임 4 어디에도 없다**(붙임 1·2 에는 있다. ★ R35 ② 로 **본문 5.1 에도**
판매단가의 끝값이 실렸으나, 붙임 4 의 *산식*에는 여전히 없다).

- 자리 — `core/casegrid/e2e_runner.py::_benefit_lines` · 편익 8종
- 고치려면 **편익이 산식의 대입값을 내놓아야** 한다.
- ⚠ **러너에 태그 목록을 두지 말 것** — R34 가 `scales_with_dispatch_window`
  선언으로 없앤 형태로 되돌아간다. 목록은 편익이 늘 때 낡는다.

</details>

## 2. 1절 요약 행 목록을 검사로 붙든다 (R35 ② 발견)

**양식 【1】은 요약을 여섯 행으로 규정하는데 실물은 여덟 행을 낸다**(적정 용량 ·
잠정성이 뒤에 붙었다). 소절·붙임·표제는 `test_form_conformance.py` 가 양방향으로
대조하지만 **요약 표의 행 목록은 아무도 보지 않는다.**

- 그래서 R35 ② 는 요약에 「결론까지 남은 거리」를 **넣지 않았다** — 넣으면 그
  어긋남이 깊어지고, 검토자가 먼저 보는 절이 규정 밖에서 자란다.
- 자리 — `docs/report-form-심의보고서.md` 【1】 · `core/report/narrative.py::
  _summary_section` · `tests/report/test_form_conformance.py`(표제 검사와 같은 방식)
- 붙들고 나서 **거리 행을 요약에 올릴지** 판단한다(`MC-1` 의 첫 화면이다).

## 3. 프로포마 행 목록을 `CaseOutcome` 에 실을 것인가

R34 「붙들 수 없는 갈래」(변이 2b). 지금은 프로포마 쪽 구매 행을 조건부화해도
**밖에서 보이는 수가 한 자리도 움직이지 않아** 검사가 볼 자리가 없다.

- 자리 — `core/casegrid/models.py::CaseOutcome` · `e2e_runner.py`

## 4. 새 PV 결론을 다시 재기 — 일사 곡선

PV 가 좋아 보이는 이유는 여전히 **평탄 발전 프로파일**(야간 발전)이다. 붙임 8 이
크기를 잰다(수전 스텝에 실린 발전 연 985kWh). **일사 곡선이 배선되면 다시
뒤집힐 수 있다.** R35 는 이것을 건드리지 않았다.

- 자리 — `core/casegrid/profiles.py::DailyShapes` · `e2e_runner.py` 의
  `generation_profile`(통로는 이미 있고 쓰지 않는다)

## 5. `FR-401-AC2.VPPMarket` (Phase 2)

R33 표 4번 그대로. **자료가 아니라 순서 문제**이며 `AC1.VPP` 가 `[Phase 2]` 를
들고 있다. `VPP` 가 계약에 앉는가는 「미해결」의 그 행이 선행 판단이다.

## 6. `SettlementInputs.annual_generation_kwh` 를 옮긴다

협상값 자료형에 물리량이 남아 있다. 조립기를 **자원 없이** 부르는 자리(구조
비교·계약 테스트)가 있어 옮기지 않았고 사유는 그 필드 주석에 있다.
**그 호출자가 사라지면 옮긴다** — 지금 옮기면 그 호출부가 깨진다.

## 7. 닫기 라운드 — 마커 49곳을 읽어 대조 (R33 표 5)

**기계 몫은 끝났다.** 남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나이며
휴리스틱 둘이 전부 잡음이었다 — **읽어서 대조하는 수밖에 없다.** 그 자리는 R32 가
옮긴 마커 49곳이고, 정규식은 `(?<!N)FR-` 이다.

---

## 착수 전에 — 절차는 `status.md` 를 본다

- 인터프리터 · 게이트 명령 · 커밋 전 PATH — **`status.md` 「게이트」 절**
- 조항을 건드릴 때 함께 움직이는 자리 — **`status.md` 「파급 지점」 절**
- 변이 하네스 `.orch/mutate.py` — **한 번에 하나씩, 대상을 좁혀** 돌린다
  (여럿 이어 돌려 시간초과로 죽으면 `finally` 가 건너뛰어져 실물 소스에 변이가
  남는다 — R34 가 실제로 밟았다). ⚠ 옛·새 문면을 **`printf '%b'`** 로 쓴다 —
  `'%s'` 는 `\n` 을 그대로 두어 여러 줄 변이가 「대상 문면이 없다」(rc=2)가 된다
  (R35 ② 가 실제로 밟았다)
- ⚠ **검사를 새로 쓸 때 먼저 물을 것**: *이 단언에 등장하는 값 중 몇 개를 검사
  대상이 스스로 정하는가?* 전부라면 동어반복이다 — R35 ① 의 초록불 변이가 그것이었다
  (`status.md` 함정 절)
- ⚠ **수를 싣는 표에는 「그 수가 어느 조건에서 나왔는가」도 실어야 한다** —
  R35 ② 의 초록불 변이(두 끝의 값을 서로 바꿔 싣기)는 **거리만 보는 검사 일곱을
  전건 통과했다.** 값과 조건의 **짝**을 다른 층에서 검산할 것
