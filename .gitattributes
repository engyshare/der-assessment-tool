# 해시 대조 대상 — 줄바꿈 변환 금지.
#
# 이 두 파일은 규칙 문서 원본과 **바이트 동일**해야 하며,
# docs/source-rules.lock 의 sha256 이 그것을 검사한다.
# git이 체크아웃 시 CRLF로 바꾸면 해시가 달라져, 내용은 그대로인데
# 다른 플랫폼에서 "원본 표류"로 오탐한다. 진단이 가장 어려운 종류의 오탐이다.
docs/domain-rules.md      -text
docs/evidence-standard.md -text
