# 사람이 해야 하는 일 — **순서대로**

> ## 이 파일의 몫
>
> **코드로 닫을 수 없는 것만** 모았다. 순서는 *「Phase 1 인수를 막고 있는가」* 로
> 매겼다 — 위에서부터 하면 막힌 것이 먼저 풀린다.
>
> | | 여기(`status-human.md`) | `status.md` |
> |---|---|---|
> | 무엇 | **사람·외부 자료·배포·판단** | 코드 진척 · 게이트 수치 · 다음 라운드 코드 작업 |
> | 누가 | 사용자·사업 주관 측·검토자 | 개발(세션) |
>
> ### ★★ 하고 나서 **기록해야** 센다 — 기록이 없으면 안 한 것과 같다
>
> 게이트가 읽는 창구는 **`docs/manual-checks.yaml`** 과 **`docs/assumptions.yaml`**
> 둘뿐이다. 회의에서 정하고 파일에 적지 않으면 추적표는 여전히 「미수행·가정」으로
> 세고, 다음 사람은 그것을 다시 요청한다. **각 항목의 「기록」 칸이 그 자리다.**
>
> 반대도 금지다 — **하지 않은 것을 기록하지 않는다.** `status: 수행` 을 손으로 적으면
> 그 순간 이 저장소의 판정 체계가 무의미해진다(그래서 R32 는 `MC-1` 에 **일정만**
> 적고 상태는 「미수행」으로 두었다).
>
> **현재 위치**: Phase 1 Must-have 조항은 **전건 매핑됐다**(미매핑 8건은 전부
> Phase 2·3). 그런데 **Phase 1 인수 판정은 「판정불가」**다 — 아래 1~3단계가 그 이유다.

---

## 1단계 — **Phase 1 인수를 막고 있는 것** (`MC-1` 하나)

`MC-1` 은 8건의 수동 검증 중 **유일한 차단 항목**(`blocking_dod: true`)이다.
이것이 미수행인 동안 Phase 1 DoD 5(SG-3)는 **통과도 실패도 아닌 「판정불가」**다.

### ✔ 1-A. 선행 개발이 **끝났다** (R33, 2026-08-15) — 리포트가 나온다

종전에는 검토자에게 줄 리포트를 산출하는 경로가 없었다(`app/routers/` 에
엔드포인트 0건 · `generate_pdf()` 가 시나리오가 아니라 금액 두 개만 받았다).
**R33 이 조립기와 출구를 놓았다.** 이제 명령 한 줄로 파일이 나온다:

```bash
PYTHONUTF8=1 ~/miniconda3/python.exe -m app.run.report_cli \
    --scenario scenario_unsubsidized --out docs/evidence/MC-1-리포트.md
```

서버로 받으려면 `GET /reports/golden/scenario_unsubsidized` (`?fmt=json` 은 재현용).

리포트는 **한국어 마크다운 한 장**이며 절의 순서가 조항이다 — ① 결론 ②
결론을 뒤집는 인자 ③ 영향도 순위(부기 7종 동반) ④ 지원 유무 비교 ⑤ 산식
3중 표기 ⑥ 전 가정 목록 ⑦ 재현 정보. `MC-1` 이 검토자에게 시키는 두 질문의
재료가 ②·③·⑤ 에 있다.

> ⚠ **리포트를 손으로 고쳐 검사에 쓰지 말 것.** 고친 것이 리포트인지 사람인지
> 갈리지 않는다. 대장이나 코드를 고쳤으면 위 명령을 다시 돌려 새로 뽑는다.

> ### 사람 몫이 하나 늘었다 — **전제·구성의 타당성 검토** (2026-08-15)
>
> 사용자가 갈라 두었다: *「전제와 구성은 보고서 **검토 결과의 신뢰도**와 관련된
> 사항이며 `MC-1` 과 **별도로** 수행해야 하는 부분」*. 정확한 구분이다 —
> `MC-1` 은 *「리포트가 인과를 나르는가」*(사람의 이해)를 재고, 이것은
> *「그 인과가 딛고 선 전제가 맞는가」*(결과의 신뢰도)를 본다. 기계도 `MC-1` 도
> 보지 않는 축이며 **사업 주관 측만 판정할 수 있다.**
>
> | 무엇을 확인하나 | 리포트의 어느 자리 |
> |---|---|
> | 3kW PV + 10kWh ESS 가 **대표 구성으로 맞는가** | 2.1 평가 대상 |
> | 운전 전략이 맞는가 — 지금은 **PV 전량 판매(자가소비 0%) + ESS 피크 저감**이다 | 2.1 · 붙임 4 |
> | 첨두 기본요금 8,320원/kW·월이 맞는가 (`Q-6` 대상) | 붙임 2 |
> | ~~★ **계통으로 내보낸 같은 전력을 잉여 판매와 첨두 절감으로 두 번 셀 것인가** (2026-08-29 · R43) — 갈래 셋(甲 실행 거부 · 乙 결손 90.7% · 丙 106.8%)과 4.3 귀속을 **`docs/evidence/판정요구-이중계상-2026-08-29.md`** 가 재어 두었다.~~ → **해소 (2026-08-31)** · 사용자 판정 — **`docs/decisions-2026-08-31-R48.md`**. 갈래 셋은 채택되지 않았고, **운영방식을 규정해 수량 단계에서** 갈랐다. ⚠ **R48 이 결론축을 재산출한다 — 옛 수를 인용하지 말 것** | 붙임 4 · 붙임 7 · 4.3 |
> | 분석기간 20년 · 할인율 4.5% 를 이 사업 성격으로 정한 근거 | 2.3 · 5.2 |
>
> ⚠ **이 판정은 `MC-1` 의 통과·미달과 무관하다.** 전제가 틀려도 리포트가 인과를
> 나르면 `MC-1` 은 통과이고, 그 반대도 성립한다. 섞으면 어느 쪽이 실패했는지
> 말할 수 없다.
>
> **리포트가 지금 무엇이라고 말하는가**(대장 base 수준 · 무보조): 「20년 안에
> 회수되지 않는다(**NPV −12,591,162원** · 결손/총사업비 **약 128.5%**)」이며,
> **`Q-1`·`Q-2` 회신이 결론을 좌우한다**는 것이 수치로 나온다(5단계 우선순위의
> 근거) — 대장이 스스로 밝힌 변동 범위 안에서 결론을 뒤집는 인자가 있고 그중
> 신뢰도가 `가정` 인 것이 있어 리포트가 상단에 잠정성을 경고한다.
>
> ⚠ **이 수는 2026-08-15 이후 네 번 내려갔고 2026-09-02 에 처음 올라갔다** —
> 전력 구매 비용 배선(−865,881 → −5,284,586) · 교체비·잔존가치 배선(→ −6,066,881 ·
> R39) · ESS 교체비 물가 계수(→ −6,289,675 · R40) · **ESS 운전 축을 §11 판정으로
> 다시 세우고 가구 부하를 본 실행에 세움**(→ −12,956,180 · R48) ·
> ★ **낮 전기를 「집 우선」으로 배분**(→ **−12,591,162** · R51/WP-6, 세 시나리오
> 모두 **+365,018원**).
> **네 번의 하락은 「사업이 나빠진 것」이 아니라 종전 수가 실제로 나가는 비용을
> 빠뜨리고 있었던 것**이고, ★ **이번 상승은 사용자 판정(*「지산지소 모델의 경우에는
> 집에서 우선 사용하는 것이 취지에 맞음」*)대로 가구가 그 스텝의 태양광을 먼저 쓰게
> 되어 계통 수전이 준 것**이다 — 사업 조건이 좋아진 것이 아니라 **운전을 취지에 맞게
> 고친 것**이다.
> 위 「전제·구성의 타당성 검토」는 이 수를 놓고 한다 — 옛 수가 적힌 인쇄물이
> 있으면 다시 뽑을 것. **재산출 근거**: `docs/evidence/MC-1-검토용-리포트-2026-09-02.md`
> (매니페스트 `f1cb56db9808bbd5`).
>
> ⚠ **사람이 판단할 것이 둘 늘었다**(R51 이 열었다 · 전문은 `status.md` 의 ★R·★T):
> ① **`REC` 단가를 무엇으로 확보할 것인가** — 배선은 R51 이 했고 값 근거가 없어
> **값 0(기본 비활성)** 으로 두었다. 켜면 **+1,054,033원 · 결손의 약 8%**(70원/kWh
> 탐침)이며 **REC 만으로는 결론이 뒤집히지 않는다.** ⚠ 지금은 `Q-*` 번호가 없어
> **§16.6 확보 현황판에 나타나지 않는다** — 나타나지 않는 결손은 아무도 요청하지
> 않는다. spec §15.1 에 행을 여는 것이 그 해소이며 **spec 개정은 사람 몫**이다
> ② **리포트가 「태양광 자가소비율 0%」를 그대로 인쇄한다** — 사실이나 「집 우선」이
> 기본이 된 지금 검토자는 *「집에서 하나도 안 쓴다」* 로 읽는다. 서식 판단이다

- ★ **지방정부 담당자 문의 9건 중 아직 답하지 못한 것** (가-3① 구성 근거 · **가-4 자산 처분·재투자 방침** · 가-5 미반영 3건의 방향과 일정 · 가-6② 부가세 부담 주체 · 가-9 요금 인상률 배선 일정) — 항목마다 **누가 정하는가와 무엇이 오면 풀리는가**를 `docs/evidence/답변-지방정부담당자-2026-08-29.md` **4절**이 적었다. 가-3①·가-4·가-6② 는 **사업 주관 측** 판정이다

### 1-B. `MC-1` 실시 — 리포트가 나오면 바로

| | |
|---|---|
| **무엇을 재나** | `FR-1001-AC5` — *「비개발자 검토자가 리포트만 보고 **회수기간의 근거**와 **결론 전환 조건**을 설명할 수 있다」* |
| **왜 사람인가** | 측정 대상이 **사람의 이해**다. 출력물의 형식이 아니라, 그것을 읽은 사람이 인과를 재구성할 수 있는지를 본다 |
| **방법** | 심의 경험자 **3명** 대상 설명 재현 테스트 |
| **진행** | ① 리포트만 준다(구두 설명·부연 금지 — 설명을 보태면 리포트가 아니라 사람을 측정한 것이 된다) ② 두 가지를 말하게 한다: **「이 회수기간이 왜 이 값인가」** · **「어떤 가정이 바뀌면 결론이 뒤집히나」** ③ 답을 그대로 적는다(요약하지 말 것 — 어디서 막혔는지가 산출물이다) |
| **통과 기준** | **3명 중 3명**이 주요 인자와 결론의 인과를 설명(§14 KPI). 2명이면 **미달**이며 리포트를 고쳐 다시 한다 |
| **기록** | `docs/manual-checks.yaml` 의 `MC-1` — `status: 수행` · `verdict: 충족`/`미충족` · `performed_at` · `performed_by` · `sample`(3명의 소속·경력 수준) · `result_note`(막힌 지점) · `evidence`(기록 파일 경로) |
| **끝나면 열리는 것** | Phase 1 DoD 5 판정 · 「차단 미수행 1건」이 0 이 된다 |
| **잠정 일정** | `planned_at: 2026-09-11` — R32 가 넣은 **잠정값**이다. 실제 담당자·일자를 정해 교체할 것 |

> **미충족이 나오면 그것도 성과다.** 리포트가 인과를 나르지 못한다는 뜻이고,
> 고칠 자리는 `core/report/` 다. 「통과시키려고 설명을 보태는 것」이 이 검사가 막으려는
> 바로 그 행동이다.

---

## 2단계 — **회신이 와야만 열리는 검증 둘** (요청서를 보내는 일)

둘 다 **가정으로 대체할 수 없다.** 우리가 정박점을 만들면 *우리 계산을 우리 가정으로
검증하는* 자기충족이 되고, 그것은 검사가 아니라 통과 의식이 된다. 그래서 저장소는
이 둘을 **통과로도 실패로도 세지 않고 `skip`** 으로 남겨 두었다(정직한 상태다).

| # | 무엇 | 누구에게 | 정확히 무엇을 요청하나 | 오면 |
|---|---|---|---|---|
| **2-A** | **`Q-4`** 선행 실증 발표자료의 **원 계산 가정** | 정책 소관 기관 | 발표된 결과 수치가 아니라 **그 수치를 만든 가정**(단가·이용률·분석기간·할인율·지원조건). 결과만 오면 대조군이 되지 못한다 | `docs/assumptions.yaml` 의 `oracle.prior_demo_assumptions` 를 `track: blocked` → 실값으로. `tests/acceptance/test_phase1_dod.py::test_dod4…` 의 `@pytest.mark.skip` 을 걷는다 → **DoD 4(SG-4) 판정** |
| **2-B** | **`Q-5`** 현행 엑셀 모형 **1건** | 사업 주관 측 | **셀 수식이 열린** 파일. 값만 있는 PDF·이미지는 대조군이 되지 못한다 | `oracle.legacy_excel_model` 등재 → §13.3 **1차 대조군** 성립(16.5 6종 대조) |

> 지금은 6종 지표를 **해석해(순위 1)**로 검증한다 — 산식은 맞다는 뜻이다. 대조군이
> 여는 것은 다른 층이다: **「우리 산식이 실무가 쓰는 전제와 같은 것을 보고 있는가」.**

---

## 3단계 — **배포 판단** (DoD 7 후반부)

무료 호스팅 **외부 접속** 검증은 실제 배포 환경에서만 성립한다
(`tests/acceptance2/test_17_7_dod7.py::…free_hosting_external_access_cannot_be_judged`
가 그 사실을 명시하며 `skip` 한다). 로컬 컨테이너(`Dockerfile`)는 이미 있고 **무관**하다.

| 순서 | 정할 것 | 메모 |
|---|---|---|
| 3-A | **운영 주체·예산** (`Q-13`) | 대장 가정값 월 2만원(하단 0). 사업 주관 측 확인 사항 |
| 3-B | **`TU-2` Litestream vs Turso** | 무료 티어 제약 대응. 3.6 프로토타입 뒤 결정하는 것으로 적혀 있다 |
| 3-C | 배포 후 **외부 접속 실측** | 접속 URL·확인 일시를 기록하고 위 `skip` 을 걷는다 |

---

## 4단계 — **R32 잠정 판정 확인** (읽고 「그대로 간다 / 고친다」만 정하면 된다)

R32 가 사용자 위임에 따라 **대신 정한 것들**이다. 근거는
**`docs/decisions-2026-08-14-R32.md`** 가 정본이며 절마다 「무엇이 오면 바뀌나」와
**고칠 자리 한 곳**을 함께 적어 두었다. **되돌리려면 그 근거를 먼저 반박해야 한다** —
근거를 읽지 않고 다시 정하면 같은 판단이 두 번 저장소에 남는다.

| § | 무엇을 정했나 | 확인 포인트 | 바뀌면 고칠 자리 |
|---|---|---|---|
| §1 | 「개별 세대 직접계약」의 정산 대상 = **잉여**, 판매 주체 = **가구** | 실제 계약이 자가소비분까지 포함하는 **결합형**인가 | 조항 문면 + 조립기 한 곳 (대장 개정 없음) |
| §2 | 관리 수수료 모수 = **신규(설비 후) 요금** | 관리규약이 **기존 요금** 기준인가 | 곱셈 한 줄 + 손계산 오라클 |
| §3 | 집합 PPA 지불 주체 = **사업자**(집합 대표) | 정산금이 **세대로 직접 배분**되는가 | `payer_by_structure` 한 줄 |
| §4 | `FR-205-AC1` 을 구조 7건으로 전개 (Phase 범위 **축소 없음**) | 「VPP 경유」를 Phase 2 로 두는 것에 동의하는가 | spec `AC1.VPP` |
| §5 | `MC-1` 일정 2026-09-11 · 담당 「평가 프로그램 담당」 | **실제 담당자·일자** | `manual-checks.yaml` 두 칸 |
| §6 | COP 곡선(`FR-204-AC3`)은 **가정값을 등재하지 않는다** | 곡선을 가정해서라도 넣을 것인가(권하지 않음 — 함수는 민감도로 드러나지 않는다) | 오면 YAML 만 채운다 |
| §7 | `NFR-104` 외부 근거(`Q-4`·`Q-5`)는 **가정 불가** | — (대체 경로 없음) | 2단계와 같다 |

---

## 5단계 — **값 회신** (개발은 막히지 않는다 · 오는 대로 교체)

전부 **가정값으로 등재**돼 있어 지금도 계산은 돈다. 회신이 하는 일은 *신뢰도를
「가정」에서 올리는 것*이다. **오면 값과 부기(출처·최종확인일·신뢰도)를 함께 고친다** —
값만 바꾸면 「출처 없는 확정」이 되어 `check_assumptions` 가 막는다.

| Q | 무엇 | 누구에게 | 메모 |
|---|---|---|---|
| `Q-1` | 옥상 PV · 벽면 BIPV 설치단가 | 공공 단가 자료 또는 **업계 견적 3사 이상** | 옥상형·벽면형 **별도** 견적 |
| `Q-2` | ESS(신품·사용후) · V2G 충전기 · 히트펌프 단가 | 업계 견적 | ⚠ ESS 는 **PCS 별도인지 시스템 일괄인지** 반드시 확인 · 히트펌프는 **COP 곡선(외기온 함수)을 함께** 받을 것(→ §6 해소) |
| `Q-3` | 가구 부하 시계열 | 실증 가구 | ⚠ **`16.3` 개인정보 처리 절차가 선행**한다 — 익명화되지 않은 원본은 저장소·DB 에 반입하지 않는다(`SC-3`) |
| `Q-5b` | 전기요금 장기 인상률 | 배전사업자 중장기 재무전망 · 전력수급기본계획 | 확보돼도 **「추정」을 넘기 어렵다**(20년 전망) |
| `Q-6` | 고압 단일계약 평균단가 | 배전사업자 **약관** | `TU-6`(계량점 구성이 실제로 가구부/공용부/거래분으로 갈리는가) 확인과 **함께** 처리 |
| `Q-7` | 거래지원수수료 | 제도 시행세칙 · 배전사업자 공시 | |
| `Q-9` | 모듈러 주택 설치비 증분 | 사업 기획 자료 | |
| `Q-14` | 관리주체 경유 수수료 | 관리규약 표본 · 운영 방침 | → 4단계 §2 판정이 함께 확정된다 |
| `Q-15` | 집합 PPA 계약단가 | PPA 중개 사업자 견적 · 전력거래소 공표 실적 | 약관요금 **대비 비율**로 등재돼 있다 |
| `Q-16` | 잉여 직거래 판매단가 | 전력거래소 SMP 공표 실적 | 연도별 실적을 쓰면 **시계열 항목으로 승격**한다 |
| `Q-17` | **교체 설비단가의 실질 추세** (2026-08-29 · R41 신설) | **`Q-2` 와 같은 업계 견적** — 「교체 시점 단가」를 함께 받으면 닫힌다 | 우선순위 3 · **지금 0%/년 으로 두고 있으며 그것은 교체비를 과대 계상하는 쪽**이다. 값이 오면 결론이 **좋아지는 방향**으로 움직인다 |
| `Q-8`·`Q-8b` | V2G 계량·정산 **제도 존부** / 배터리 열화 보상 | 제도 소관 기관 · 계통운영기관 · 배전사업자 | 지금은 **편익 0 · 비활성**(`track: default0`)이라 **결과가 V2G 에 보수적**이다. 확인되면 `assume` 으로 전환 |
| `Q-10` | DR 정산금 ↔ 요금 인센티브 **배타 여부** | 계통운영기관 · 배전사업자 | 배타면 규칙표에 한 행 |
| `Q-12` | 분산편익 크레딧 화폐화 | (Phase 3 판단) | 해외 VOS 연구 검토 뒤 |
| `Q-11` | 벽면 BIPV 이용률 | **외부 요청 불요** | 일사량 데이터로 시뮬레이션하면 닫힌다(개발 몫) |

---

## 6단계 — **나머지 수동 검증 7건** (`MC-2`~`MC-8` · 차단 아님)

전부 **웹 UI 를 대상으로** 한다. UI 가 검수 가능한 상태가 된 뒤에 몰아서 하는 편이
낫다 — 지금 하면 화면이 바뀔 때마다 다시 해야 한다.

| MC | 무엇 | 방법 | 통과 기준 |
|---|---|---|---|
| `MC-2` | 비개발자가 튜토리얼 **30분 내** 첫 결과 | 사용자 **5명** 태스크 수행, 시작~첫 결과 실측 | 5명 중 **4명 이상** 30분 내 |
| `MC-3` | 입력 필드 툴팁(단위·설명·기본값 출처) | 전 입력 필드 순회 체크리스트 | 누락·오기 **0건** |
| `MC-4` | 오류 메시지 3요소(필드·사유·조치) | `DV-1`~`DV-13` 위반 케이스별 메시지 확인 | 3요소 누락 **0건** |
| `MC-5` | 1366×768 가로 스크롤 없음 | 해당 뷰포트로 주요 화면 순회 | 스크롤·레이아웃 붕괴 **0건** |
| `MC-6` | `가정` 노란 배지 + 영향도 병기 | 육안 검수 | `가정` **전 항목** 배지 |
| `MC-7` | 한국어 우선(영어는 지표명만) | 전 화면 영어 표기 순회 | 지표명 외 영어 **0건** |
| `MC-8` | WCAG 2.1 AA · **키보드만으로 완주** | axe-core 스캔 + 마우스 없이 마법사→결과→내보내기 | AA 위반 0 · 핵심 3 시나리오 완주 |

**기록**: 각 항목의 `status`·`verdict`·`performed_at`·`performed_by`·`sample`·
`result_note`·`evidence` (`MC-1` 과 같은 칸이다).

---

## ⚠ 오케스트레이터가 승인 없이 정한 것 (사후 확인 필요)

> ### 이 절이 왜 있나
>
> **2026-09-03(R56)에 사용자가 판정했다** — *「사람 승인이 필요한 자리(결론축을
> 움직이는 수정 · spec 개정 · 배포 판단)에서 멈추지 말고 진행하되, 승인 없이
> 네가 정한 것을 전부 기록하라」*. 그 기록 자리가 여기다.
>
> ⚠ **이 절의 항목은 「끝난 일」이 아니라 「사람이 아직 안 본 일」이다.**
> 확인했으면 행 머리에 `✔` 와 **확인한 날짜**를 적는다 — 지우지 마라.
> 되돌릴 것이면 「되돌리는 법」 칸을 그대로 따르면 된다.
>
> ⚠ **판정 근거를 여기 베끼지 마라** — 라운드 경위는 `status-history.md` 가,
> 설계 스케치는 `docs/적정용량-산출방법-검토.md` 가 정본이다.

| 날짜 | 무엇을 정했나 | 왜 | 되돌리는 법 |
|---|---|---|---|
| 2026-09-03 (R56) | **계절 축의 갈래를 「계절별 대표일 4×24」로 정했다** — 월별(12×24)·8760 전량을 쓰지 않았다 | 스케치 6절 ② 의 셋 중 자료 요구가 가장 작다. 12×24 는 자료가 3배인데 **지금 계절 자료가 하나도 없어** 이득이 없고, 8760 은 스텝이 24 → 8,760(**365배**)이 되어 27케이스 그리드 전체에 곱해진다 | **구조가 계절 수를 못 박지 않았다** — `core/casegrid/profiles.py` 어디에도 `4` 가 없다. 자산(`fixtures/profiles/representative-day.yaml`)의 `seasons:` 를 12개로 적으면 월별이 된다. 8760 으로 가려면 자산 대신 시계열을 그대로 바인딩한다(각 항목의 `replace_when` 이 그 조건을 적어 둔다) |
| 2026-09-03 (R56) | **계절이 형상만이 아니라 「연간 총량 중 그 계절의 몫」(`Season.share`)도 갖게 했다** | 형상만 계절별로 두고 총량을 **일수에 비례**해 나누면 겨울 하루 발전량과 여름 하루 발전량이 **같아진다.** 그러면 판정 §4 의 *「발전특성을 고려할 수 있도록 설계」* 가 성립하지 않는다 (스케치 6절 ④ *「겨울철 일사가 여름철보다 약하다」*) | `Season.share` 를 지우고 `DailyShape.spread()` 의 `total * season.share` 를 일수 비례로 바꾼다. ⚠ 그러면 `tests/casegrid/test_seasonal_axis.py` 의 대조군(`test_equal_shares_would_flatten_the_seasons`)이 **무엇을 잃었는지 말해 준다** |
| 2026-09-03 (R56) | ★★ **자산에 계절 실측값을 넣지 않았다 — 축만 세웠다** | 계절별 일사·부하 실측이 없고 이 저장소는 **값을 지어내지 않는다.** 넣으면 결론축이 「지어낸 계절 곡선」 위에 선다 | **되돌릴 것이 없다 — 결론축이 한 원도 안 움직였다.** 오케스트레이터가 `HEAD` 와 대조해 확인했다: 부하·발전 둘 다 **8,760 원소 · 비트 단위로 다른 원소 0개.** 채우고 싶으면 아래 「계절 자료」 행이 무엇이 필요한지 적는다 |
| 2026-09-03 (R56) | **새 `Q-*` 번호를 만들지 않았다** — 계절 자료 요청을 대장 `q_ref` 로 등재하지 않았다 | `scripts/check_assumptions.py` 의 「Q 목록 대조」가 대장과 spec §15.1 을 **양방향으로** 요구한다. `rslt/` 를 고치지 않는 채로 새 번호를 쓰면 **빨간불이다** — R51/WP-2 가 실제로 밟았고 경위가 `docs/assumptions.yaml` 의 그 항목 주석에 있다 | spec §15.1 표에 번호를 먼저 올린 뒤(§16.5 절차) 자산의 `q_ref` 를 채운다. 그때까지 요청은 아래 「계절 자료」 행이 갖는다 |
| 2026-09-03 (R56) | **spec 을 개정하지 않았다** — 판정 §4 의 *「계절별로 … 고려할 수 있도록 설계되야 함」* 이 조항으로 올라가야 하는지 정하지 않았다 | 조항 신설은 수용기준 총수와 추적표·DoD 집계를 함께 움직인다(7단계의 다른 행들이 같은 형태다). **구조는 이미 그 요구를 만족하지만 그것을 조항으로 셀지는 사람 몫**이라고 보았다 | 7단계에 안건으로 올려 §16.5 절차를 탄다. 코드는 이미 서 있으므로 **조항을 뒤에 얹는 순서**이며 코드를 되돌릴 필요가 없다 |
| 2026-09-03 (R56) | ★★ **★관점공백의 크기를 「연 +19,853원」으로 적었다가 스스로 정정했다** — 그 돈은 **이미 결론축에 있다**(자가소비분이 `GridPurchase` 를 줄이는 방식으로 계상된다). 실측: `GridPurchase` 121,167원/년 = **실제 수전만** · 부하 전량이면 359,379원/년 | 붙임 8 의 그 수를 **무엇에 대한 차이인지 묻지 않고** 로드맵으로 옮겨 적었다. 다음 라운드가 그 행을 믿고 `SelfConsumption` 을 편익으로 배선하면 **이중 계상을 커밋했을 것이다**(`FR-402-AC1`) | 되돌릴 것이 없다 — **문서만 고쳤고 결론축은 안 움직였다**(`87af495`). ⚠ **남은 판정은 「그 절감이 누구 지갑인가」이며 그것은 사람 몫이다** — 주민 지갑으로 돌리면 총합은 그대로이고 **사업자 결론축은 최대 연 238,234원 규모로 나빠진다** |
| 2026-09-03 (R56) | **오케스트레이터 자리를 `cl3` 로 넘기지 않고 유지했다** — 5시간 창이 **3%** 로 `orch-dev` 6절 하드스톱에 닿았는데도 | ⓐ **인계본은 이미 완성·푸시·CI 초록불**이라 6절이 지키려는 것(*「더 내려가면 인계 자체를 못 한다」*)이 **이미 확보됐다** · ⓑ 5시간 창이 **11:40 에 리셋**되고 주간은 **11:59 에 리셋**된다 · ⓒ **사용자가 「다음 오케스트레이터에게 이관하여 계속 돌리지 마라」고 정했고 보고도 내 몫**이다 | 넘기려면 `cl3`(주간 88% · 5시간 91% · 주간 리셋 09-09 로 사슬이 가장 길다)가 후임이다. `orch-dev` 8절 순서를 따르고 **선임 `/goal` 을 먼저 해제**한다 |
| 2026-09-03 (R56) | 미반영 항목 **「계절·요일 변동」을 둘로 쪼개지 않았다** (계절 / 요일) | 본문 3.4 표는 항목마다 한 행이고 **본문 분량 예산의 여유가 1줄**이다(오케스트레이터 실측 **218줄 / 상한 219**). 쪼개면 예산을 넘고, **상한을 올리는 것은 이미 다섯 번 밀린 자리**다 | 쪼개려면 **본문에서 다른 줄을 붙임으로 내린 뒤** 쪼갠다. 상한(`tests/report/test_overview_sections.py` 의 `<= 219`)을 올리는 것으로 풀지 마라 |

---

## 계절 자료 — **오면 축이 채워진다** (2026-09-03 · R56 신설)

★ **R56 이 계절 축을 세웠다. 값만 비어 있다.** 아래 둘이 오면 자산의 `seasons:`
를 채우는 것만으로 반영된다 — **코드는 한 줄도 고치지 않는다.**

⚠ **`Q-*` 번호를 아직 달지 않았다** — 위 표의 네 번째 행이 그 이유다.

| 무엇 | 누구에게 | ⚠ 반드시 함께 받을 것 |
|---|---|---|
| **계절별 일사** (발전 형상) | 지역 TMY 등 시간별 일사 자료 | ⚠⚠ **계절별 대표일 형상만으로는 부족하다 — 「연간 발전량 중 계절별 몫」을 함께 얻어야 한다.** 몫이 없으면 겨울 하루와 여름 하루의 발전량이 같아져 *「겨울철 일사가 여름철보다 약하다」* 를 담지 못한다. TMY 는 시간별 값을 주므로 **계절 경계로 적산하면 두 값이 함께 나온다** |
| **계절별 가구 부하** | 실증 가구 (**`Q-3` 과 같은 자료**) | 같은 이유로 **「연간 사용량 중 계절별 몫」**이 함께 필요하다 — 형상만으로는 냉난방으로 늘어난 여름·겨울 사용량을 표현하지 못한다. ⚠ **`16.3` 개인정보 처리 절차가 선행**한다(`SC-3`) |

⚠ **8760 실측이 오면 계절로 접지 말고 그대로 바인딩한다** — 계절 접기는
자료가 계절 단위로만 있을 때의 차선이다. 각 형상의 `replace_when` 이 이 갈림을 적는다.

⚠ **부하와 발전은 같은 계절 달력**(계절 이름과 일수)을 적어야 한다 — 다르면
같은 인덱스가 서로 다른 날을 가리키므로 읽는 쪽이 **거부한다.**

---

## 7단계 — **spec 개정 판단** (사람이 정해야 코드가 따라간다)

코드로 「맞는 답」을 고를 수 없는 것들이다. 정해지면 `spec §16.5` 개정 절차를 탄다.

| 무엇 | 무엇을 정해야 하나 |
|---|---|
| ★ **`FR-1001-AC5` 문면 — 「리포트만 보고」** (2026-08-15 신설) | 조항은 *「비개발자 검토자가 **리포트만 보고** 설명할 수 있다」* 인데, 사용자가 **리포트를 정형 출력으로 한정**했다(해설·판정 문장을 싣지 않는다 — 양식 0절). 판정 이유: 서비스 런타임에 LLM 이 없으므로 리포트의 모든 문장은 미리 박아 둔 고정 문면이고, 그것은 구성이 바뀌면 **틀린 문장을 계속 인쇄한다.** 사용자 방침은 *「프로그램이 필요한 정보를 가독성 있게 제공하고 **결과물을 사람이 평가**」* 다. 그러면 안내문을 사람이 작성해 함께 주게 되는데 그 순간 **「리포트만 보고」가 성립하지 않는다.** 정할 것 — ⓐ 조항을 *「리포트와 그 안내 자료로」* 로 고칠 것인가 ⓑ 정형 출력만으로 `MC-1` 을 시도하고 미달 시 다시 볼 것인가 ⓒ `MC-1` 실시 조건에 「구두 설명·부연 금지」를 어떻게 다시 적을 것인가 |
| `FR-611-AC3` ↔ `FR-704` **관점 어휘 불일치** | `FR-704` 는 사업자/참여 주민/정부, `FR-611` 은 사업자·주민(병합)/사회/정부다. 해소하려면 **`FR-704` 개정이 선행**된다 |
| **`SC` 표(보안 8건)를 수용기준으로 승격할 것인가** | 지금은 추적표 밖이라 「전건 매핑」이 성립해도 **보안 조항은 세어지지 않는다** |
| **`FR-1003` 내보내기 형식에 「사람이 읽는 문서」가 없다** (R33 발견) | 조항이 열거하는 것은 XLSX·PDF·JSON·CSV 넷인데, **`MC-1` 이 실제로 요구하는 것은 검토자가 그대로 읽는 한 장**이다. R33 이 낸 마크다운 리포트는 그래서 **가리킬 조항이 없다** — 마커를 달지 않고 두었다(달면 「PDF 를 냈다」는 거짓 진술이 된다). `AC` 를 신설할지, 기존 PDF 항목을 「심의자료 문서(형식 무관)」로 고칠지 |
| `NFR-206` **「500줄」 문면** | 실측은 코드 줄 기준 전건 이내(최대 482)이고 초과분은 설명 밀도다. 조항을 코드 기준으로 고칠지, 파일을 쪼갤지, 경고로 둘지 |
| **8.3 을 배타 유형별로 쪼갤 것인가** | 한 작업이 `AC2.A`(차단 100% · 음성)와 `AC2.B~D`(오탐 0 · 양성)를 동시에 진다 — 검사 방향이 반대다 |
| **17.1 DoD 1 ↔ `FR-202` Phase** | 2변형 **비교**는 Phase 1 인데 비교 기능 `FR-202` 는 Phase 2 배정이다. 템플릿 2건을 나란히 놓는 것으로 충족되는지, `AC1`·`AC2` 를 Phase 1 로 당길지 |
| ★ **배타 규칙의 「구조 축」이 조항에 없다** (2026-08-20 · R37 발견) | 구현(`docs/exclusion-rules.yaml`)은 배타 규칙을 **계약구조로 한정**하는데 `FR-402-AC4` 의 필드 목록에 `적용 계약구조` 가 없다. 그래서 `tests/valuestream/test_exclusion_structure_axis.py` 의 태그 셋이 **갈 곳이 없다**(지금 `FR-205-AC1.NetMetering` 을 달고 있으며 상계거래 정산과 무관하다). 정할 것 — ⓐ `AC4` 필드 목록에 「적용 계약구조」를 더할 것인가 ⓑ `FR-205` 아래 「구조·배타 규칙 정합」 서브 조항을 세울 것인가. **문면·개정안 초안은 `.orch/R37/spec_amendment_draft.md` 안건 ②** |
| ★ **`FR-205` 의 「선택 파라미터」를 재는 수용기준이 없다** (2026-08-20 · R37 발견) | 조항 본문(spec 483행)은 *「계약·거래 구조를 **선택** 파라미터로 지원해야 한다」* 인데 수용기준은 **리프 일곱(각 구조의 정산)뿐**이고 **무접미 `FR-205-AC1` 은 실재하지 않는다**(실측 — v0.16 이 `AC1` 을 일곱으로 전개했다). 그래서 「구조를 주지 않으면 종전대로 계산된다」를 재는 검사(`test_e2e_settlement_wiring.py:137`)가 **조항 없이 떠 있고**, 지금 `.NetMetering` 을 달고 있다. R37 은 **옮길 자리가 없어 그대로 두었다** — 없는 ID 를 만들어 넣는 것이 더 나쁘다. 정할 것 — 「무구조 베이스라인」 AC 를 신설할 것인가(수용기준 총수 321 → 322 이며 추적표·DoD 집계가 함께 움직인다). **초안은 같은 파일 안건 ③** |
| ★ **spec v0.17 머리말의 「비용 검사 9건」이 실물과 다르다** (2026-08-20 · R38 발견) | v0.15 는 *「비용 검사 **9건**이 산식을 검증하면서 `FR-101-AC2` 를 인용하고 있었다」* 며 그 9건을 `AC5` 로 옮겼고, v0.17 머리말이 그 진술을 나른다. **실물은 26건이었다** — R38 이 `FR-101` 계열 인용 52건을 훑어 **`AC2` 를 인용하면서 §13.2.2 C-1~C-3 산식을 재는 것이 17건 더** 남아 있음을 찾았다(`.orch/R38/result_clause_label.md` 4-C(ㄱ) · 판정을 다른 레인이 독립 검증했다). 판정 근거 셋 — v0.15 가 세운 기준(`AC2` = 메서드의 **존재**, `AC5` = **산식**) · 이미 옮겨진 9건 · **`test_load.py`(옮겨졌다) 대 `test_thermal_load.py`(안 옮겨졌다)를 나란히 놓으면 눈으로 갈린다.** 정할 것 — ⓐ 남은 17건을 옮기고 **머리말의 「9건」을 정정**할 것인가(총 26건) ⓑ 옮기지 않을 근거가 있는가. ⚠ **코드만 고치면 머리말이 남은 건수와 어긋난 채 남는다** — 그래서 spec 정정이 함께 필요하며 §16.5 절차다. 마커 17건 이관은 추적표 집계를 움직인다 |
| ★ **조항이 없는 검증 대상 둘** (2026-08-20 · R38 발견 · 기존 `FR-101-AC5` 행의 연장) | 검사가 있는데 **인용할 조항이 없어** 이웃 조항을 빌려 쓰는 자리가 둘 더 있다 — ⓐ **금액·기간·비율의 자료형·단위 규약**(§7.5): 인용할 조항이 없다 ⓑ **부호 규약**(양수 = 내보냄): 조항이 없다. ⚠⚠ **처음 등재할 때 셋이라 적었고 하나가 틀렸다** — 「교체비 계상 시점」은 **조항에 있다**(spec **412행** `FR-104-AC3` 의 「`retire` 의 의미」 ① *「`replace` 가 기본값이다. **수명 도달 다음 해 초에 교체비를 계상하고** 계속 쓴다」* — v0.14 가 넣었다). 검증자가 잡았다. **그대로 두었으면 다음 라운드가 이미 있는 것을 신설하는 §16.5 안건을 만들었을 것이고**, v0.15 가 `FR-101-AC5` 로 메운 공백과 겉모습이 같아 더 그럴듯해 보였을 것이다. 「미해결」의 *「`FR-104` 는 … C-1·C-2·C-3 에 대응하는 AC 가 없다」* 행과 **같은 공백이며 세 갈래 더 있다는 것이 R38 훑기의 소득**이다. v0.15 가 같은 형태 여섯을 조항으로 메워 **자동 매핑이 아홉 라운드 만에 처음 늘었다**(278 → 284) — 같은 절차가 여기에도 쓸 수 있다. 정할 것: 셋을 조항으로 세울 것인가, 아니면 「빌려 쓰는 것이 정당하다」를 어디에 적을 것인가 |
| ★ **`FR-104-AC3`(spec §414 ③)의 「어느 EOL 인가」가 침묵이다** (2026-08-29 · R39 발견) | 조항은 *「EOL 前에는 `retire` 와 `replace` 의 잔존가치가 같다」* 고 적는데 **어느 EOL 인지 말하지 않는다.** PV 의 첫 EOL 은 **인버터 12년**이고 분석기간은 20년이므로 — 「본체 EOL(25년)」로 읽으면 20년차는 EOL 前이라 둘이 같아야 하고, 「첫 EOL(12년)」로 읽으면 이미 EOL 後라 갈리는 것이 맞다. **지금 구현은 뒤쪽**이며 차액은 13년차 인버터 재취득분 285,354원이다. R39 는 **대신 정하지 않고** 그 차액을 부채 래칫으로 고정했다(`tests/der/test_pv.py::test_retire_salvage_matches_the_body_only_oracle_and_diverges_from_replace`) — 조항이 「같아야 한다」로 개정되면 그 검사가 빨간불이 되어 알린다. 정할 것 — ⓐ `AC3` 에 *「어느 EOL 인가」* 를 명문화할 것인가 ⓑ 자원마다 다르게 둘 근거가 있는가. ⚠ 위 `C-5` 행과 **같은 뿌리**다(둘 다 「부속설비 재취득이 있을 때」를 조항이 다루지 않는다) — 함께 개정하는 편이 낫다 |
| ★ **`C-5` 잔존가치 산식의 「취득가」가 어느 취득인지 조항이 말하지 않는다** (2026-08-20 · R38 발견) | §13.2.2 `C-5` 는 *「`취득가 × 잔존수명 / 총수명` 을 최종연도에 계상 후 할인」* 이고 예시는 `4,500,000 × 5/25` 다 — **본체가 분석기간보다 오래 사는 경우(25년 > 20년)만 다룬다.** 분석기간 **안에서 교체 취득이 일어난 자원**의 취득가가 최초 취득가인지 교체 취득가인지 **조항이 침묵한다.** 그 침묵을 구현이 자원마다 다르게 메웠다 — `ESS` 는 교체 취득분을 세고(`ESS._acquisitions` 독스트링이 *「새 배터리가 통째로 사라진다」* 로 사유까지 적었다) **`PV` 인버터(13년차 취득)는 세지 않는다.** 순효과가 **−673,848원 ↔ −547,640원(19%)** 으로 갈린다. **R38 은 「PV 도 센다」로 판정했다** — 같은 조항·같은 산식인데 자원마다 다르게 처리하는 것은 「자원 구현이 자작한다」 형태이고 ESS 쪽에 이미 사유 있는 정본이 있다. **그 판정은 코드를 통일하는 것이고, 조항의 침묵 자체는 사람 몫이다.** 정할 것 — ⓐ `C-5` 에 *「분석기간 내 교체가 있으면 **마지막** 취득가와 그 취득 시점부터의 잔존수명」* 을 명문화할 것인가 ⓑ 자원별로 다르게 둘 근거가 있는가(있으면 그것을 적어야 한다). ⚠ **명문화하지 않으면 다음 자원이 또 다르게 구현한다** — 지금 갈린 이유가 조항의 침묵이다 |

---

## 기록 방법 — 복사해서 쓰는 예

**수동 검증을 끝냈을 때** (`docs/manual-checks.yaml`):

```yaml
  - id: "MC-1"
    ...
    status: "수행"                 # 미수행 → 수행
    verdict: "충족"                # 충족 / 미충족
    performed_at: "2026-09-11"
    performed_by: "홍길동(주관부서)"
    sample: "심의 경험자 3명 — 소속·경력 수준"
    result_note: "3/3 설명 재현. 2명이 「전환 조건」을 토네이도 차트로 짚었고 1명은 …"
    evidence: "docs/evidence/MC-1-2026-09-11.md"
```

**값 회신이 왔을 때** (`docs/assumptions.yaml`): 값과 **부기를 함께** 고친다 —
`value` · `source`(출처 문면) · `verified_at` · `confidence`(`가정`→`추정`/`확정`) ·
`derivation_method`(표본·산출 방법) · 필요하면 `sensitivity` 3수준.

**둘 중 무엇이든 고쳤으면 돌린다** (`PYTHONUTF8=1` + `~/miniconda3/python.exe`):

```bash
PY=~/miniconda3/python.exe
PYTHONUTF8=1 $PY scripts/check_assumptions.py       # 부기 7종·신뢰도·교체 경로 정합
PYTHONUTF8=1 $PY scripts/gen_traceability.py        # 수동 검증 상태가 추적표에 반영된다
PYTHONUTF8=1 $PY -m pytest -q                       # 골든 3종이 가정값에 묶여 있다 — 값이 바뀌면 여기서 드러난다
```

> ⚠ **골든 시나리오 3종의 기준값은 대장 가정에 묶여 있다.** 가정값을 갱신하면 회귀가
> 빨간불이 되는데 그것은 결함이 아니라 **기준값 재산출이 필요하다는 신호**다
> (`fixtures/golden/`). 통과는 「어제와 같다」이지 「맞다」가 아니다.

---

## 이 파일을 갱신하는 규칙

- 한 건이 끝나면 **지우지 말고** 그 행 머리에 `✔` 와 **기록 위치**를 적는다 —
  다음 사람이 「했는가」를 파일 하나로 확인할 수 있어야 한다.
- 새로 「사람 몫」이 생기면 여기 더한다. **`status.md` 에 적지 않는다** — 그쪽은 코드
  진척의 소유자이고, 같은 사실이 두 곳에 있으면 한쪽만 고쳐진다.
- 판정 근거는 `docs/decisions-*.md` 가 정본이다. 여기에는 **무엇을 확인할 것인가**만
  적고 근거를 베끼지 않는다.
