# 오케스트레이터 판정 — 2026-09-04 (R59)

> ⚠ **이 문서는 사용자 판정이 아니다.** `docs/decisions-*.md` 의 다른 문서들은
> 사용자가 정한 것을 옮긴 것인데, 이 문서는 **오케스트레이터가 판단한 것**이다
> (2026-09-03 사용자 상시 판정: *「오케스트레이터가 합리적으로 판단하고, 끝난 뒤
> 판단 결과와 근거를 안내한다」*). 되돌릴 수 있는 것만 담는다.

## §1 무엇을 판정했나 — **③-2 지목 47건 중 인용 5건 이상인 12건**

`scripts/audit_clause_selfreference.py` 는 *「조항 X 의 인용이 1건 이상인데 어느
인용도 자기 문면에 X 를 적지 않고, 그런데 어떤 인용은 다른 조항 Y 를 적는다」*
는 자리를 **지목**한다. 지목은 판정이 아니다 — 독스트링은 거짓일 수 있다.

**대조를 손으로 하지 않았다.** 이 저장소는 전수 대조를 손으로 쓰게 했을 때
**조항 문면을 지어냈다.** 그래서 R59 는 순서를 뒤집었다:

> ① 도구를 세운다(`scripts/audit_clause_dossier.py`) → ② 도구가 **spec 원문에서**
> 조항 문면을 뽑아 인용과 나란히 낸다 → ③ 그 대장에서만 인용하며 판정한다

⚠ **대장은 `.orch/` 안이라 다른 기계에 없다.** 다시 만드는 명령:

```
python scripts/audit_clause_dossier.py --from-audit --min-citations 5 --out <경로>
```

도구가 빠뜨리지 않았음을 실측했다 — **대장에 빠진 인용 0건** ·
**원문과 다른 조항 문면 0건**(대장의 조항 문면 각 줄이 spec 그 줄의 부분
문자열인가를 대조).

기준선 실측(2026-09-04): **인용 있는 조항 302건 · 인용 1,489건 · 후보 47건** ·
최다 인용 `NFR-303-M1` **171건**. 인용 5건 이상 후보는 **12건**이다.

## §2 판정표 — 후보마다 한 행

⚠ 「조항이 요구하는 것」·「인용이 재는 것」 칸은 **전부 대장에서 복사한 문장**이다.

| 후보 조항 | 인용 건수 | 조항이 요구하는 것 (대장에서 인용) | 인용이 재는 것 (대장에서 인용) | 판정 | 근거줄 |
|---|---|---|---|---|---|
| `FR-1002-AC2` | 13 | *「**영향도 산출 방식**: 각 인자를 **합리적 변동 범위**(전제의 신뢰구간, 없으면 기본 ±20%)에서 변동시켜 주 지표가 움직인 폭으로 측정한다. 케이스 그리드를 실행하지 않은 단일 실행에서도 **자동 1변수 스윕으로 영향도를 산출**한다」* | #13 *「영향폭 계산 (metric_fn = 항등 함수): dominant: |high - low| = |1000 - 1| = 999 … 따라서 dominant > mid > tiny 순이어야 한다」* · #3 *「★ **영향도 스윕도 지원을 반영한다.**」* | **맞다** | 대장 `dossier_ge5.md:33`(조항) · `:224`(#13) · `:70`(#3) |
| `FR-1002-AC4` | 23 | *「**결론 전환 강조**: 합리적 변동 범위 안에서 목표 달성 여부가 뒤바뀌는 인자는 **최상단에 별도 강조**한다. 이것이 정책 판단에서 가장 중요한 정보다」* | #18 *「결론을 뒤집는 인자가 **맨 앞**이다.」* + `assert flip < ranking` · #21 *「결론이 뒤집히는 임계값이 올바르게 찾아지는지 확인한다」* + `assert row["flips_conclusion"] is True` | **맞다** | 대장 `:248`(조항) · `:540`(#18) · `:573`(#21) |
| `FR-101-AC5` | 25 | *「`capex()`·`fixed_om()`·`variable_om()` 의 **산식은 §13.2.2 의 `C-1`(설비단가 × 용량 + 부대비, **부가세는 별도 행**) · `C-2`(고정 O&M + 물가상승률) · `C-3`(변동 O&M × 처리량)을 따른다.** 메서드의 **존재**는 `AC2` 가, **산식**은 이 조항이 요구한다」* | 25건 전부 `RC-ALL-C1/C2/C3` 오라클 대조다 — #1 *「`RC-ALL-C1` 산식 원문 (§13.2.2): `단가 × 용량 + 부대비`」* · #2 *「`RC-ALL-C1` 부가세 분리」* · #18 *「`RC-ALL-C2` 오라클(§13.2.2, 등비수열 합): `A × ((1+i)^n − 1) / i`」* · #20 *「`RC-ALL-C3` 오라클(§13.2.2): 발전 1,314 kWh × 5원 = **6,570원/년**」* | **맞다** | 대장 `:630-633`(조항) · `:637`(#1) · `:648`(#2) · `:849`(#18) · `:870`(#20) |
| `FR-102-AC1.ThermalLoad` | 10 | *「`ThermalLoad` 열부하 (난방·급탕) — 8760 열부하 시계열 또는 난방도일 기반 추정」* | #1 *「HDD 2,500 × 0.8 kWh/HDD = **2,000.0 kWh**.」*(난방도일 경로) · #3 *「8760 열부하 시계열 직접 입력 경로.」* — **조항이 적은 두 입력 경로를 각각 잰다** | **맞다** | 대장 `:955`(조항) · `:959`(#1) · `:981`(#3) |
| `FR-401-AC2.AggregatedPPA` | 8 | *「`AggregatedPPA` 집합 PPA 수익 — **발전량 전량** × PPA 계약단가(약관요금 × 비율)」* | #4 *「① 손계산 오라클 — 10,000 kWh × 127.5 원/kWh = **1,275,000 원**. 127.5 = 약관요금 150 × 비율 0.85.」* · #5 *「② ★★ 디스패치를 바꿔도 값이 같다 — **잉여판매의 사본이 아니다.**」*(=「전량」) | **맞다** | 대장 `:1066`(조항) · `:1113`(#4) · `:1122`(#5) |
| `FR-504-AC3` | 15 | *「**웹에서 편집**: `admin` 권한 사용자가 웹 UI에서 프로파일을 생성·복제·수정할 수 있다. 파일 수정이나 재배포를 요구하지 않는다」* | #1 *「세 조작이 각각 경로를 갖고, admin 이 지나면 성공한다.」* · #3 *「편집이 저장소 파일을 건드리지 않고, **같은 앱**의 다음 조회에 보인다.」* + `assert after == before, ("편집이 저장소 파일을 바꿨다 — 조항은 「파일 수정이나 재배포를 요구하지 않는다」고 정한다 …")` | **맞다** | 대장 `:1173`(조항) · `:1177`(#1) · `:1207`(#3) |
| `FR-703-AC1.npv` | 7 | *「**NPV** 할인 순현재가치 — 할인율 파라미터화 (공공 4.5~5.5% 기본). 오라클 §13.0.2 **순위 1 / 원 단위 완전 일치**」* | #7 *「NPV — §13.0.2 순위 1, 원 단위 완전 일치 (== 비교, approx 없음). 오라클: 닫힌 형태 등비수열 합. NPV = -1,000,000 + 300,000 × ((1 - 1.05^-5) / 0.05) = -1,000,000 + 1,298,843 = 298,843.」* + `assert int(val) == int(expected)` | **맞다** | 대장 `:1358`(조항) · `:1440`(#7) · `:1385`(#3) |
| `FR-704-AC4` | 7 | *「세 관점이 하나의 리포트에 병렬 표시」* (상위 `FR-704`: *「경제성을 **사업자 / 참여 주민 / 정부(재정)** 3개 관점으로 분리 산출」*) | #3 *「지표 한 줄에 관점 넷의 값이 열 순서대로 실린다 — 「병렬 표시」의 실물.」* · #2 *「리포트 **하나**에 관점 넷이 있고 순서가 **사용자 판정** 순서다.」* | **맞다** ⚠ | 대장 `:1460`(조항) · `:1456`(상위) · `:1483`(#3) · `:1473`(#2). ⚠ **조항은 「세」인데 인용은 넷을 요구한다** — ⑥ 참조 |
| `FR-801-AC7.quick` | 5 | *「**빠른 탐색 (기본값)** — 결합 집합 1(설비단가·시공비) 3수준 × 할인율 3 × 전기요금 인상률 3 = **27 케이스**, 예상 실행시간 **81초** … DV-10 임계(500) 미만이므로 경고 없이 실행된다」* | #3 의 `assert` 가 조항의 낱낱을 그대로 짚는다 — `assert quick.case_count() == len(("low","base","high")) ** 3` · `assert independent == {"discount_rate","tariff_escalation"}` · `assert quick.execution_plan().estimated_seconds == 81.0` · `assert quick_plan.requires_confirmation is False` | **맞다** | 대장 `:1547`(조항) · `:1587`(#3 머리 · `assert` 는 `:1596`~) |
| `NFR-105-AC1` | 24 | *「모든 계산 코드는 **테스트 우선(TDD)** 으로 작성되어야 한다. 구현보다 그 구현을 규정하는 실패 테스트가 먼저 존재해야 한다.」* | 24건 전부 **게이트 ①·②의 판정 논리**다 — #2 *「「아무 테스트나 바뀌면 통과」로 구현하면 core 파일 여섯 개를 고치고 관계없는 테스트 한 줄을 고친 PR 이 통과한다.」* · #19 `assert "check_test_accompaniment.py" in everything, ("게이트 ②(테스트 동반)를 CI 가 부르지 않습니다 …")` · `assert "diff-cover" in everything, ("게이트 ①(변경분 커버리지)를 CI 가 부르지 않습니다 (작업 2.5)")`. **「실패 테스트가 먼저 존재했는가」(순서)를 재는 인용은 24건 중 0건이다** | **어긋났다** | 대장 `:1646`(조항) · `:1659`(#2) · `:1834`(#19 머리 · `assert` 는 `:1846`~) |
| `NFR-107-M1` | 16 | *「CI가 spec 수용기준 목록과 마커·YAML을 대조하여 미매핑 0건 확인. 구현: `scripts/gen_traceability.py`」* | #1 *「Phase 1 DoD 9 is satisfied when no Phase 1 Must-have criteria remain unmapped.」* · #13 `test_traceability_render_reports_unmapped_must_have_criteria` · #4 *「req 마커가 있는데 무조건 pytest.skip()만 하면 위반. 「판정 못 한다」면서 매핑표에는「검증했다」로 세어집니다.」*(대조의 실질을 지킨다) | **맞다** | 대장 `:1915`(조항) · `:1919`(#1) · `:2030`(#13) · `:1947`(#4) |
| `NFR-303-M1` | 171 | *「오류 메시지 리뷰 체크리스트」* (상위 `NFR-303`: *「입력 검증 오류 메시지는 **어떤 필드가 / 왜 / 어떻게 고쳐야 하는지**를 한국어로 제시해야 한다.」*) | #64 *「문자열에도 셋이 들어가고, **구조로도 따로 꺼낼 수 있다.**」* + `assert err.as_dict() == {"field": …, "reason": …, "action": …, "rule": "DV-2"}` · #63 *「**셋 중 하나가 비면 예외가 만들어지지 않는다.**」* · #171 *「MC-4: 오류 메시지가 필드·사유·조치 3요소를 이해 가능하게 설명하는지 확인한다.」* | **맞다** (메타 조항) | 대장 `:2076`(조항) · `:2072`(상위) · `:2809`(#64) · `:2799`(#63) · `:3966`(#171) |

**「불명」 0건.** 대장만으로 열두 건 모두 정해졌다.

### ★ `NFR-303-M1` 이 메타 조항이라는 판단의 근거 — **대장에서 인용한다**

브리프가 조심하라고 한 자리다. 근거 셋 모두 대장 안에 있다.

- **조항이 규약 자체를 정한다.** 상위 `NFR-303` 문면이 *「어떤 필드가 / 왜 / 어떻게
  고쳐야 하는지」* 라는 **세 요소**를 정하고, `M1` 은 그 리뷰 체크리스트다. 저장소의
  모든 거부 검사가 그 셋을 단언하므로 온 저장소가 인용하는 것이 정상이다.
- **171건 중 165건은 다른 조항 ID 를 아예 적지 않는다**(대장 실측). ③-2 의 「다른
  조항을 적는다」 신호는 나머지 **5건**에서만 나오고, 그 5건이 적은 것은
  `FR-802-AC3`·`FR-801-AC4`·`FR-607-AC1`·`FR-402-AC2.A`·`FR-402-AC5`·`FR-105-AC5` —
  전부 **그 검사가 함께 재는 도메인 조항**이지 「NFR-303 대신 잰 것」이 아니다.
  예: #12(대장 `:2210`) 는 *「구조로 꺼내 `field`·`rule`·어긋난 길이(3, 2)가 사유에 있는지 단언한다」*
  라며 `DV-9` 의 세 요소를 재면서 동시에 `FR-802-AC3` 오라클을 짚는다.
- **저장소 스스로 경계를 적어 두었다.** `FR-401-AC2.AggregatedPPA` 의 인용 #7 이
  *「이 검사는 `NFR-303-M1` 을 재지 않는다 — `ValueError` 발생만 확인하고
  field·reason·action 3요소를 검사하지 않는다.」* 라고 적는다(대장 `:1145`).
  **「3요소를 단언하는 것」이 곧 `NFR-303-M1` 을 재는 것**이라는 뜻이며, 이 판정의
  기준을 저장소가 이미 그렇게 쓰고 있다.

### ★ `NFR-105-AC1` 을 「어긋났다」로 본 근거와 그 한계

조항 문면(대장 `:1646`)이 요구하는 것은 **순서**다 — *「구현보다 그 구현을 규정하는
실패 테스트가 **먼저** 존재해야 한다」*. 인용 24건이 재는 것은 **동반 여부와 커버리지**다.

⚠ **다만 마커를 옮겨 고칠 수 없다.** `NFR-105` 의 Measurement 1·2·3 은 spec 에
번호 목록(`1.` `2.` `3.`)으로 적혀 있어 `_specparse.CRITERION`(`- **M1** …`)에
걸리지 않는다. 실측:

```
NFR-105 ['NFR-105-AC1']
NFR-107 ['NFR-107-AC1.auto', 'NFR-107-AC1.manual', 'NFR-107-AC2', 'NFR-107-AC3',
         'NFR-107-AC4', 'NFR-107-AC5', 'NFR-107-M1']
```

즉 **`NFR-105-M1`·`M2` 라는 조항 ID 가 아예 없어서** 검사 24건이 갈 자리가 `AC1` 뿐이다.
그래서 이것은 「마커를 잘못 달았다」가 아니라 **spec 쪽 결손**이며, 고치는 것은
spec 개정(§16.5)이자 **다음 WP 의 오케스트레이터 판정 자리**다. 이 WP 에서는 고치지 않았다.

### ★ ③-2 지목의 **오탐 원천 둘** — 판정하며 드러났다

1. **합성 픽스처의 조항 ID 가 「적은 조항」으로 세어진다.** `NFR-107-M1` 의 인용
   16건 중 9건이 `FR-101-AC1` 을 적은 것으로 나오는데, 실물은 검사기에 먹일
   **합성 소스 문자열** 안이다 — `tests/ci/test_marker_substance.py:83` 의
   `"@pytest.mark.req('FR-101-AC1')\n"`. `_citations._mentions()` 가 함수 본문의
   문자열 상수를 보므로 픽스처 데이터가 주장으로 세어진다.
2. **약식 표기가 「자기 ID 를 안 적음」으로 세어진다.** `FR-1002-AC2`·`AC4` 의 인용
   #12/#20 독스트링은 *「13.1: 임계값 산출 및 여유폭 표시 (FR-1002-AC1, AC2, AC4).」*
   로 **자기 ID 를 실제로 적고 있다.** 정규식 `MENTION` 이 완전 ID 만 잡으므로
   `AC2`·`AC4` 는 안 걸리고 `FR-1002-AC1` 만 「다른 조항」으로 남는다.

두 가지 모두 **지목 도구를 고치라는 말이 아니다**(2절에 따라 손대지 않았다).
다음 라운드가 나머지 35건을 볼 때 같은 형태를 다시 만난다는 기록이다.


## §3 결론 — **열둘 중 하나가 어긋났고, 그것은 마커 문제가 아니라 spec 결손이다**

- **맞다 11건.** 자기 조항 ID 를 문면에 적지 않은 것은 **서술 습관**이며,
  지목 신호가 곧 결함이 아니라는 것을 열한 건이 실물로 보인다
- **어긋났다 1건 — `NFR-105-AC1`.** 조항이 요구하는 것은 **순서**다
  (*「구현보다 그 구현을 규정하는 실패 테스트가 **먼저** 존재해야 한다」*).
  인용 24건이 재는 것은 **변경분 커버리지와 테스트 동반**이다.
  **순서를 재는 인용은 24건 중 0건**이다
- **불명 0건**

### ★★ `NFR-105` 의 뿌리 — Measurement 셋이 **조항 ID 가 되지 못했다**

spec 의 `NFR-105` 는 Measurement 를 **번호 목록**(`1.` `2.` `3.`)으로 적는다.
바로 아래 `NFR-106` 은 같은 자리를 `- **M1** …` 로 적는다.
`scripts/_specparse.py` 의 `CRITERION` 은 후자만 잡으므로 **`NFR-105-M1`·`M2`·`M3`
라는 조항 ID 가 아예 없다.** 실측:

```
NFR-105 ['NFR-105-AC1']
NFR-107 ['NFR-107-AC1.auto', 'NFR-107-AC1.manual', 'NFR-107-AC2', 'NFR-107-AC3',
         'NFR-107-AC4', 'NFR-107-AC5', 'NFR-107-M1']
```

그리고 `NFR-105-AC1` 의 문면은 **요구사항 문장의 글자 그대로 복사본**이다.
⇒ 검사 24건이 갈 자리가 `AC1` 뿐이었고, **재는 것과 적는 것이 어긋난 채로
게이트가 초록불**이었다. 이것이 이 저장소가 반복해 밟은 형태다 —
*「검사가 있고 통과하는데 조항과 다른 것을 재고 있다」*.

## §4 ③-2 지목의 **오탐 원천 둘** — 나머지 35건을 볼 때 다시 만난다

1. **합성 픽스처의 조항 ID 가 「적은 조항」으로 세어진다.** `NFR-107-M1` 인용
   16건 중 9건이 `FR-101-AC1` 을 적은 것으로 나오는데, 실물은 검사기에 먹일
   **합성 소스 문자열** 안이다(`tests/ci/test_marker_substance.py`).
   `_citations._mentions()` 가 함수 본문의 문자열 상수를 보므로 **픽스처 데이터가
   주장으로 세어진다**
2. **약식 표기가 「자기 ID 를 안 적음」으로 세어진다.** *「(FR-1002-AC1, AC2, AC4)」*
   처럼 적으면 정규식이 완전 ID 만 잡아 `AC2`·`AC4` 는 안 걸린다

⚠ **지목 도구를 고치라는 말이 아니다.** 오탐을 줄이려고 신호를 좁히면 진짜를
놓친다 — `audit_` 가 차단이 아닌 이유와 같은 판단이다. **기록으로 남긴다.**

## 정정 이력

| 날짜 | 대상 | 변경 | 사유 |
|---|---|---|---|
| 2026-09-04 | 전체 | 신설 | R59/WP-2 가 대장 생성기를 세우고 인용 5건 이상 후보 12건을 판정했다. **판정표가 `.orch/` 에만 있으면 사라지므로 정본 층으로 옮긴다** |
