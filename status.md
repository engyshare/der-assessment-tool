# 진행 상태 — 2026-09-06 (**R63 완료** · 축 열일곱 · 사용자가 값을 고쳐 저장하면 결과가 실제로 달라지고, 화면이 변수명을 인쇄하지 않고, 검증 모드가 붙었다. 결론축 무보조 `npv` **−11,552,270원** — 움직이지 않았다)

> ⚠⚠ **2026-09-05 R61 정정 — 이 제목이 R52~R60 아홉 라운드 동안 「R51 · −12,591,162원 · 128.5%」로 서 있었다.** 파일 첫 줄이 세 판 낡아 있었고, 그것이 이 파일에서 가장 먼저 읽히는 줄이다. **제목은 라운드를 닫을 때 함께 고친다.**

> ## ⚠ 새 세션이 가장 먼저 알아야 할 것
>
> **R36~R43 이 모두 닫혔다.** R38·R39·**R43** 은 오케스트레이터-워커 구성으로 돌았고
> R40·R41·R42 는 워커 없이 단독이었다. 워커 지시문·산출물은 `.orch/R38/`·`.orch/R39/`·
> `.orch/R43/` 에 있고 **그 디렉터리는 `.gitignore` 안이라 다른 기계에는 없다** —
> 이 파일 하나가 정본이다(2026-08-30 R45 에 `todo.md` 를 이 파일로 합쳤다).
>
> ### ★ 지금 붙들고 있어야 할 셋
>
> 1. **결론축은 −12,591,162원**(결손/총사업비 **약 128.5%**)이다 — ★ **2026-09-02 R51
>    이 재산출했다**(판정 §1 「집 우선」 기본값 · 세 시나리오가 각각 **+365,018원**).
>    이보다 앞선 어떤 수(**−12,956,180** · −6,289,675 · −6,066,881 · −5,284,586 ·
>    −865,881)도 **지금 값이 아니다.** 인용하는 자리를 만들 때는 **다시 세어** 적는다.
>    ✔ 사람이 보는 정본: `docs/evidence/MC-1-검토용-리포트-2026-09-02.md`.
>    ⚠ **옛 판 둘(`-2026-08-30` · `-2026-08-29`)을 지우지 않았다** — 문의사항·답변서·
>    판정요구 셋이 `-08-29` 판을 **이름으로** 가리킨다.
>    ★★ **결손이 총사업비를 넘었다** — 보조 80% 도 더 이상 회수하지 못하고,
>    **지원율로 메우는 답이 성립하지 않는다.** `판정요구-이중계상-2026-08-29.md` 가
>    「丙 이상」으로 미리 적어 둔 자리이며, 그 판정 요구 자체는 **R48 이 해소했다**
>    (갈래 셋은 채택되지 않았다 — 운영방식을 규정해 **수량 단계에서** 갈랐다).
> 2. **「좋아 보이는 수」와 「초록불이 된 래칫」을 먼저 의심할 것.** R38 은 늘어난
>    매핑 둘이 거짓이었고(⑧), R40 은 **한 항만 고쳐도 초록불이 되는 래칫**을 만났다
>    (R40 ②). 어느 쪽도 「나아졌다」가 아니라 **재는 방식이 헐거웠다**였다
> 3. **「전수 대조」류는 먼저 「스크립트로 할 수 있는가」를 물을 것.** LLM 에게
>    1,302행 표를 손으로 쓰게 하니 **문면을 지어냈다**(R38 ⑦). 도구는
>    `.orch/R38/build_marker_inventory.py` 이고 다시 돌릴 수 있다
>
> ⏳ **미푸시 커밋 수를 이 파일에 박지 않는다** — `git log origin/main..HEAD --oneline | wc -l`
> 로 **센다**. ⚠ 종전 이 자리의 「6~7건」과 R42 블록의 「6건」은 **둘 다 거짓이었다**
> (R43 착수 시점 실측 0건 · `origin/main` 이 `c4ce0e5` 그대로였다).
>
> ⚠ **게이트를 이어 돌리지 말 것** — 따로, 각각 배경 실행해야 시간초과를 피한다.
> 그리고 **워커가 변이를 돌리는 동안 전건 게이트를 돌리지 말 것**(R38 함정 4).
> ⚠⚠ **`pytest` 와 다른 게이트를 겹쳐 돌리지도 말 것** — R41 이 밟았다. 음성 검사가
> 잠깐 만드는 `core/der/temp_acceptance2_bad_import.py` 를 `ruff` 가 잡아 `rc=1` 이
> 나고, 그것은 **위반이 아니라 남의 임시 파일을 본 것**이다.

> # ▶ 이 파일이 **라운드 인계 정본**이다. `## 지금 할 일` 부터 읽는다
>
> 새 세션은 다른 무엇보다 **`## 지금 할 일`** 을 읽는다 — 직전 라운드 종료 선언 ·
> 다음 라운드 번호표 · 첫 30분 절차 · 조항을 건드릴 때 함께 움직이는 자리가 거기 있다.
>
> ### ▶ **다음에 무엇을 집는가는 이 파일의 「다음에 집을 것」 절이 갖는다** (2026-08-30 R45)
>
> 종전에는 `todo.md` 가 그 목록을 따로 들었다. **운영 파일이 둘이면 어느 쪽이
> 맞는지 묻게 되고 한쪽만 고쳐진다** — 그 파일 자신이 머리에 *「두 파일이 어긋나면
> `status.md` 가 맞다」* 고 적어 둔 것이 그 증상이었다. R45 가 합치고 지웠다.
>
> ✔ **R35 셋은 전부 커밋됐고 `origin/main` 에 있다.** R35 ① 은 `dae6222`(본체) ·
> `8aae6a9`(인계) · `3d71759`(todo.md), R35 ② 는 `28e5267`, **R35 ③ 은 `52a781c`(본체) ·
> `2367130`(인계)** 이다. 경위는 `status-history.md` 의 **R35 ①·②·③ 블록**에 있다.
> ⚠ 이 자리에 **「R35 ③ 은 아직 커밋 전이다」가 R36 부터 R42 까지 남아 있었다** —
> R43 이 `git log` 로 대조해 고쳤다. 이 파일이 스스로 적어 둔 *「세지 않은 수를
> 옮겨 적으면 그것이 다음 라운드의 계획으로 따라 들어간다」* 의 그 형태다.
>
> ### 이 파일은 **짧게 유지한다** — R32 에 222KB 에서 잘라 냈다
>
> 종전 이 파일은 **1,586줄 / 222KB** 였고 통째로 읽으면 토큰 한도에 걸렸다. 그래서
> **아무도 끝까지 읽지 않았고, 그 결과 `## 산출물 현황` 절이 spec v0.14 · 수용기준
> 308 에 멈춰 있었다**(실제는 v0.16 · 321). **길이가 낡음을 만든 것이다** — 이 파일이
> 스스로 *「세지 않은 수를 옮겨 적으면 그것이 다음 라운드의 계획으로 따라 들어간다」*
> 고 적어 둔 그 형태다.
>
> | | 여기(`status.md`) | `status-human.md` | `status-history.md` |
> |---|---|---|---|
> | 무엇 | **지금 상태 · 지금 할 일(코드) · 게이트 실측 수치 · 함정** | **사람 몫** — 수동 검증 · 외부 자료 · 배포 · spec 개정 판단 | 라운드별 경위(R0~R31) · 해소된 항목 · 낡은 절차 |
> | 누가 | 개발(세션) | 사용자·사업 주관 측·검토자 | — |
> | 갱신 | **매 라운드** | 사람 몫이 늘거나 끝날 때 | 라운드가 끝날 때 이전 「지금 할 일」 블록을 내린다 |
> | 새 세션이 | **읽는다** | 사람 몫을 물을 때 읽는다 | **읽지 않는다** — 필요할 때만 찾아본다 |
>
> ⚠ **사람 몫을 이 파일에 적지 않는다** — `status-human.md` 가 그것의 소유자다.
> 같은 사실이 두 곳에 있으면 한쪽만 고쳐진다.
>
> **라운드를 닫을 때 하는 일**: ① 「지금 할 일」의 옛 블록을 `status-history.md`
> **머리로** 옮긴다 ② 수치를 **다시 세어** 적는다(베끼지 않는다) ③ 「산출물 현황」의
> 판본을 실물과 대조한다. **이력을 이 파일에 쌓지 않는 것이 규칙이다.**
>
> ### ⚠ 라운드 인계는 `.orch/state.md` **가 아니다**
>
> **오케스트레이션은 R24(2026-08-13)에 끝났다.** `.orch/state.md` 는 그 시점에서
> 멈춘 이력이고 `.gitignore` 밖이라 새 클론에는 아예 없다. 그 폴더의 판정 중 유효한
> 것은 `docs/clause-recheck-2026-08-14.md` 로 옮겼으며, **유효한 도구는 변이 하네스
> `.orch/mutate.py` 하나**다(「지금 할 일」의 파급 지점 절).

---

## 한 줄 요약

3-File System **Step 3(exe-tasks) 진행 중**. spec **v0.28** · 작업 목록 **v2.11**.

**결론축(무보조 `npv`) −11,552,270원** · 보조 20% **−9,592,270원** · 보조 80%
**−3,712,270원** — **셋 다 미회수**이고 `payback` 은 셋 다 `.inf` 다
(2026-09-05 **R60/WP-4** 재산출 · 「축은 맨 마지막에 한 번만」).
⚠⚠ **옛 수를 인용하지 마라** — `−11,537,129원`(R52~R59) · `−12,591,162원 · 128.5%`(R51) ·
그보다 앞선 `−12,956,180원` · `−6,289,675원`.
★ R60 이 움직인 원인은 **하나** — 자산의 계절 절을 가정으로 채운 몫이며 **총량이 아니라
형상 혼합**이 바뀌었다(세 시나리오가 **정확히 같은 폭 −15,141원**).

**Phase 1 Must-have 미매핑 0건.** 남은 **10건**은 전부 Phase 2·3 이다.

> ★★ **이 절은 수를 두 번 적지 않는다.** 게이트·검사 실측은 **「산출물 현황」 절**과
> **「게이트」 절**이 소유자이고, 라운드 경위는 `status-history.md` 가 갖는다.
> ⚠ **R60 이 이 절에서 낡은 수 한 무더기를 지웠다** — spec `v0.20`·작업목록 `v2.8`·
> `npv −12,591,162원`·미매핑 `9건`·`pytest 1,855 passed`·`mypy 122파일`·
> `수용기준 324 / 자동 293`(R38~R51 시점의 수들)이 **R59 까지 이 자리에 서 있었고**,
> 같은 파일의 「산출물 현황」 절과 **서로 어긋나 있었다.** 그 수들은 각 라운드의
> `status-history.md` 블록이 갖는다 — **여기 다시 적지 않는다.**

**R32 결정·판정은 `docs/decisions-2026-08-14-R32.md`, R31 은
`docs/decisions-2026-08-14-R31.md` 가 정본이다** — 이 파일에 옮겨 적지 않는다
(같은 사실이 두 곳에 있으면 한쪽만 고쳐진다).

---

## 지금 할 일 (우선순위 순)

> # ⏹ 2026-09-06 **R63 완료 — 사용자가 값을 고쳐 저장하면 결과가 실제로 달라지고, 안 달라지는 값은 산출물이 「안 먹었다」고 적는다. 화면이 변수명을 인쇄하지 않고, 값이 어디서 왔는지 말하고, 검증 모드가 붙었다. ★ 검수 둘(적대적·브라우저)이 결함 **열다섯**을 잡았고, 오케스트레이터 지시문의 전제가 **두 번** 뒤집혔다 — 둘 다 워커가 실물을 재서 잡았다**
>
> **① 결론축 — 움직이지 않았다.** 무보조 `npv` **−11,552,270원** · 골든 회귀 7건 그대로.
> ⚠ 사용자가 값을 고쳐 저장한 실행이 다른 수를 내는 것은 **정상**이다 — **둘을 섞지 마라.**
> ★ 불변의 근거가 **두 겹**이다: `assumption_overrides` 가 없으면 `apply_scenario_overrides`
> 가 **같은 provider 객체**를 돌려주고(시험이 `is` 로 단정), 인자를 안 주면 `build_level_map`
> 이 **전건 같은 표**를 낸다. ⚠⚠ **라운드 도중 한 번 움직였다** — 오버라이드가 `true` 를
> 삼켜 분석기간이 1년이 됐다. **막았다**. ⛔ 그때의 수를 인용하지 마라(이력 ⑫ D-3).
> ★★★ **「고치면 움직인다」가 단가·요금까지 성립하고**(설비 단가 **+299,438** · 전기요금
> **−954,940** · 이력 ⑬ D10) **안 움직이는 키는 붙임이 「안 읽었다」고 적는다**(49번).
>
> **② 이 라운드가 연 것** (남은 목록은 아래 **「다음에 집을 것」 절**)
>
> - ★★★ **저장 계층은 섰으나 배포에서 `DER_SCENARIO_STORE` 를 정해야 저장이 남는다**(45번) —
>   기본값은 인메모리다(`app/deps.py` 규약) · `Dockerfile` 이 정하나 **무료 티어에선 부족**
> - ★★ **`purge_expired` 의 인가 주체를 정할 조항이 없다** — **요구를 지어내지 않았다**(사람 몫)
> - ★★ **「읽었다」 이력을 굳히는 자리가 순서에 기댄다**(55번) — 리포트 쪽 읽기가 그 줄 앞으로 오면 표시가 **조용히** 죽는다(뒤로 밀리는 쪽만 시험이 붙든다)
> - **둘 더** — 한 물건에 이름이 둘(50번) · **시계열 설정**(칸+사유 · **36번 선행**)
>
> **③ 새로 밟은 함정 일곱** — 전문은 「함정」 절. ⚠⚠⚠ **`e2e` 잡이 검사 실패를 삼켰다**
> (`| tee` + `pipefail` 없음 — GitHub 기본 셸은 `bash -e`) · ⚠⚠⚠ **「전제」 일괄 치환은 일반
> 국어 270줄을 부순다** · ⚠⚠ **`pytest` 잔재가 게이트 둘을 오염시킨다**(`.gitignore` 안이라
> `git status` 는 깨끗하다) · ⚠⚠ **게이트 ②는 「그 모듈을 import 하는 시험」을 요구하고 파일명
> 규약은 단수형이다**(`test_items.py` 는 안 걸린다) · ⚠ **병렬 축은 계약에서 부딪힌다** · ⚠ **트리는 CRLF · 편집기는 LF** · ⚠ **속성 고정 정규식**
>
> **④ 착수 자리** — 남은 목록은 아래 **「다음에 집을 것」 절**이 갖는다. ⚠ **PR 로 닫는다.**
> ★ **새 코드가 300줄을 넘으면 적대적 검수 축을 세워라** — 이 라운드에서 가장 값이 컸다.
> ★★ **WP 지시문의 전제를 워커가 실물로 다시 재게 하라** — 「대장에 라벨이 없다」와 「읽는 통로가 하나다」가 둘 다 반대였고, 재지 않았으면 산출물에 거짓이 인쇄됐다.
>
> **⑤ 푸시·CI** — PR **#12~#17 · #19~#28** 병합(**#18 은 #20 이 포함**) · 잡 **14종** 전건
> `success` 를 PR 마다 확인. 전건 `pytest` **2214 passed** · `tests_e2e` **22 passed**.
> ★ 사용자 지시로 **작업 트리를 갈라 워커 다섯을 병렬**로 돌렸다 — **축 스물**.
>
> **⑥ 전문** — WP 표 · 게이트 실측 · **판단한 것 열여섯** · 자리 판독은 **`status-history.md`
> R63 블록**. 사용자 판정 정본은 `docs/decisions-2026-09-05-R63{,b}.md`(요구·용어).
---

## 다음에 집을 것 (착수 순서)

> ▶ **이 절이 「무엇을 다음에 집는가」의 정본이다.** 2026-08-30(R45)에 `todo.md` 를
> 여기로 합치고 그 파일을 지웠다 — 운영 파일을 **`status.md` 하나**로 만들기
> 위해서다. 판정 근거 · 실측 수치 · 라운드 경위는 이 파일의 다른 절이 갖고,
> 사람 몫(수동 검증 · 외부 자료 회신 · 배포 판단 · spec 개정)은 **`status-human.md`**
> 가 소유자다.

⚠ **4(종전 1)·22(종전 18)는 결론축을 움직인다.** 「기준선을 흔드는 것은 한 번만 해야 하고, 그때
실행 경로의 행 구성이 확정돼 있어야 한다」 — 두 번 흔들지 않는다.
⚠ **22·23(종전 18·19)과 5(종전 3)의 실제 수정은 「골든 3건 + `MC-1` 리포트 + `status-human.md` 인용 수치를 한 단위로 다시 뽑는」 무거운 후속을 수반한다.** R45 도 이 판단을 먼저 하라. (일부러 시작하지 않으면 미완으로 넘겨야 하는데 `.orch/` 가 `.gitignore` 안이라 위험이 크다)

> ## ★★★★ 2026-09-04 **사용자 판정 넷 — 재료 정본은 `docs/decisions-2026-09-04-R59b.md` · 이것이 R60 의 1순위다**
>
> **그 문서 §5 가 순서를 정한다.** 여기 베끼지 않는다 — **네 항목의 요지와 조심할 것만** 적는다.
>
> | # | 무엇 | ⚠ 조심할 것 |
> |---|---|---|
> | **1** | ★ **PR 경로를 세운다**(§2) — 게이트 셋(`NFR-105-M1`·`M2`·`M3`)이 **PR 일 때만 판정**되는데 이 저장소는 `main` 에 직접 밀어 왔다. **셋 다 한 번도 판정된 적이 없다** | ⚠⚠ **가장 먼저 병합 권한·브랜치 보호를 확인하라**(`gh pr create`·`gh pr merge`). **안 되면 멈추고 사용자에게 물어라** — 우회해서 직접 밀면 이 판정이 무효다 |
> | **2** | ★★★ **ⓐ·ⓑ·ⓒ 갈래를 고를 수 있게 만든다**(§1) — R58 이 선언은 세웠고(`FR-705-AC2`·`DV-15`·`core/cba/baseline.py`) 어느 케이스에도 지정하지 않았다 | ⚠⚠⚠ **결론축이 움직인다.** 무보조 `npv` **−11,537,129원** 은 「갈래 미지정」의 수다 — **지정 뒤에는 인용하지 마라.** ⚠ 기획 의도는 **ⓑ**(자가태양광+히트펌프 보유 가구)이나 **책임공급비율 계측 때문에 ⓒ 로 가야 할 수 있다.** ⚠ 셋은 **배타**다 |
> | **3** | ★★ **없는 값을 「가정」으로 채운다**(§3) — R56·R55·R57 이 *「실측이 없으니 비운다」* 로 두었던 것을 **사용자가 바꿨다** | ⚠⚠ **`docs/assumptions.yaml` 의 신뢰도(`가정`/`확정`)와 `Q-*` 대장을 쓴다 — 새 통로를 만들지 마라.** ⚠⚠⚠ **결론축이 움직이고**, 채운 수가 **「가정 기반」임이 리포트에 드러나야** 한다. ⚠ 코드에 리터럴로 박으면 「추후 변경」이 깨진다 |
> | **4** | **보고 범위를 좁힌다**(§4) — 사용자에게 보고하는 것은 **결론축을 움직인 것 · spec 의 「요구 내용」을 바꾼 것 · 되돌릴 수 없는 것** 셋뿐이다 | ⚠ **「보고하지 않는다」가 아니라 「이력에만 남긴다」다** — 종료 블록의 표는 다음 오케스트레이터가 읽어야 하므로 그대로 쓴다 |
>
> ⚠ **`decisions-2026-09-03-R57.md` §5 의 1번은 위 2번이 대체한다.** 그 문서를 재료로
> 쓸 때 1번은 새 문서를 보라. **§5 의 2·3·4 는 R58 이 이미 닫았다.**
>
> ✅✅ **위 넷을 R60 이 전부 집었다**(2026-09-05) — 1=WP-1(PR #1 · 게이트 셋 첫 판정) ·
> 2=WP-2(PR #2 · `baseline_arrangement` 한 필드) · **§1 의 나머지 절반**=WP-3(PR #3 ·
> ⓒ 를 계산 가능하게) · 3=WP-4(PR #4 · **결론축 −15,141원**) · 4=WP-5(PR #5 ·
> `status-human.md` 여덟 행 → 한 행). 경위는 `status-history.md` **R60 블록**.
> ⚠ **3 은 절반만 닫혔다** — 계절은 채웠으나 **계절 몫이 운전을 가르지 못한다**
> (배포 실행이 몫 가중 평균 대표일 한 벌을 돈다) ⇒ 착수 순서 **36번**.

> ## ★★★ 2026-09-02 **사용자 판정 넷 — 재료 정본은 `docs/decisions-2026-09-02-R54.md`**
>
> ✅ **§1**(운영 방식 택일) · **§2**(길이 한도 파일 분리) · **§3**(분산편익 대장 다섯 칸)은
> **R54 가 구현했다** — 경위는 `status-history.md` R54 블록.
> ✅ **§4(계절)은 R56 이 열었다 — 판정 넷이 전부 구현됐다.** ⚠ **R60/WP-4 가 그 칸을
> 「가정」으로 채웠다**(사용자 판정 R59b §3) — 이제 비어 있지 않고 **임의값이 서 있다.**
> ⚠⚠ 그래도 **계절 몫이 결론을 가르지는 못한다**(착수 순서 **36번**).
> ⚠ **§2 를 「코드 줄 수가 한도에 찬 파일」로 읽었다** — 그 해석이 맞는지는 물음으로 남는다.

> ## ★★★ 2026-09-02 **R52 가 닫은 것 · 남긴 것 — 여기부터 집는다**
>
> 재료 정본은 **둘**이며 **둘 다 살아 있다**:
> **`docs/decisions-2026-09-02-R52.md`**(사용자 판정 **열하나**) ·
> **`docs/decisions-2026-09-02-R52b.md`**(라운드 도중 온 **보고서 의견 다섯**).
> R52 의 경위·WP 표·게이트 실측은 **`status-history.md` R52 블록**이 갖는다.
>
> **R52 가 닫은 것** — §2(검증 보고서) · §4(자가소비율) · §5(`REC` 70원) ·
> §8(규칙책 수치) · §3 **앞 문장**(유형 `E` 거부) · §6(해설서) ·
> §1(전제값 조사 — **값은 안 바뀌었고 출처를 채웠다**) · §7(교체비 **통로**) ·
> R52b §1·§5(관점 넷) · R52b §2·§3의 **검토서**.
>
> ⚠⚠ **결론축은 R52 가 한 번, R60 이 한 번 움직였다** — 지금 수는 무보조 `npv`
> **−11,552,270원**(R60/WP-4 가 계절 절을 가정으로 채운 몫 **−15,141원**).
> **옛 수를 인용하지 마라** — `−11,537,129원`(R52~R59) · `−12,591,162원 · 128.5%`(R52 앞).
> ⚠ 결손/총사업비는 **117.7%** 로 R52 의 수를 다시 세어 확인할 것(R60 은 재지 않았다).
>
> ### ★★★ 다음 라운드가 먼저 판정할 것 — **축을 움직이는 것부터**
>
> ⚠ **R53 이 이 표를 고쳤다** — ★관점공백은 **절반 닫혔고**, ★교체비는 **사람 몫으로
> 내렸으며**(사유는 아래), 판정 셋이 새로 섰다(★골든선행 · ★분리 · ★하위항목).
>
> | # | 무엇 | 왜 지금인가 |
> |---|---|---|
> | **★계절** | ✅ **R56 이 판정 §4 를 열었다 — 「구조」는 섰고 「값」이 비어 있다.** `Season`(name·days·**share**) · `DailyShape.by_season` · `spread()` 가 계절 차례로 편다(`core/casegrid/profiles.py` · 시험 13건). ★★ **몫(`share`)이 형상과 따로 있다** — 형상만 계절별로 두고 총량을 일수 비례로 나누면 **겨울 하루와 여름 하루가 같아져** 「발전특성을 고려」가 성립하지 않는다. ⚠ **자산의 `seasons:` 는 비운 채로 커밋했다** — 계절별 실측이 없고 지어내지 않는다. 그래서 **결론축이 한 원도 안 움직였다**(8,760 원소 비트 대조 · 다른 원소 0개). ★ 붙임 8 「계절·요일 변동」이 **자산의 계절 수를 실행마다 읽어** 상태를 인쇄한다 — *「8,760 전량 불필요」* 까지. 경위는 **`docs/적정용량-산출방법-검토.md` 6절**(R56 이 갱신했다) | **남은 것은 값이며 사람 몫**이다 — 소유자는 `status-human.md` 의 **「계절 자료」 절**(계절별 일사·부하 **각각 형상 + 몫**). ⚠ **채우면 결론축이 움직인다** · ⚠ **「축이 섰다」를 「계절을 반영했다」로 읽지 마라** |
> | **★용량** | ✅ **R55 가 경우 「가」(100% 자립 역산)를 닫았다** — `core/report/sizing.py` · 리포트 붙임 10. **남은 것은 나·다·라 셋**이고 선행이 각각 **실물로 확인됐다**: **나** = `FR-304` 결합 최적화(Nice-to-have · **Phase 3** · 1변수 스윕을 결합 격자로 바꾸는 일) · **다** = 지붕 면적·설치밀도 **실측값 확보가 선행**(자료가 오기 전에는 못 연다 · 지어내지 않는다) · **라** = 러너 개편(자원 구성을 케이스 축으로) **+** 냉난방 수요값. ⚠ ESS 역산은 **계절이 선행** | 「가」는 닫혔다. ⚠ **「가」의 채택**(역산 용량을 기준 구성에 올리는 것)은 **결론축을 움직이므로 사용자 판정이다** |
> | **★배타물리량** | ★★★ **R56 이 찾았다 — ★분할과 ★관점공백이 이 하나를 기다린다.** 배타 판정이 **편익 태그 짝**만 보고 거부하는데 조항은 **물리량**을 말한다. `FR-402-AC1` 은 *「지불 주체가 다르거나 **물리량이 다르면 정상 계상한다**」* 이고 `AC2.A` 는 *「**같은 1 kWh**를 … **같은 시각** ESS 방전을 …」* 으로 범위가 좁다. 그런데 `collect_exclusions` 는 `active_tags = {type(s).tag …}` — **태그의 집합**이라 물리량을 구별할 수단이 원리적으로 없다. ⚠ **즉 구현이 `FR-402-AC1` 을 위반한다**(정당한 동시 계상을 지운다 — `assert_no_exclusions` 독스트링이 유형 B~D 에 대해 이미 그 논리를 적어 두었고, **유형 A·E 의 「물리량이 다른 경우」만 아무도 안 봤다**). ★ **실측**(R56 오케스트레이터가 함수를 직접 불렀다) — `SelfConsumption`+`SurplusSale` → **거부**(유형 A) · `NWAs`+`PeakShaving` → **거부**(유형 E) · `CP`+`TouArbitrage` → **거부** · `NWAs`+`SelfConsumption` → **거부**. ⚠ 거부 메시지 자신이 처방을 적는다 — *「물리량이 실제로 다른지 확인하고, 다르다면 배타 규칙 쪽이 틀린 것」*. **그런데 「다르다」를 표현할 자리가 계약에 없다** | ✅ **닫혔다 — 집지 마라.** R56/WP-4(`f392fb6`)가 축을 세웠고 R57/WP-1·WP-2(`b5d9a05`·`eeb5bf3`)가 몫의 자리와 실물 편익 배선까지 채웠다. ⚠⚠ **이 칸은 R57 착수 때까지 「★★ 이것부터 집는다」로 남아 있었고, 그래서 R57 오케스트레이터가 닫힌 항목을 1순위로 집었다** — 실물(`core/valuestream/exclusion_table.py`·`tests/valuestream/test_exclusion_quantity_axis.py`)을 열고서야 알았다. **판정 칸을 닫으면서 함께 고쳐라** |
> | **★분할** | ✅ **R57 이 「구조」를 다 세웠다 — 집을 것은 「값」이며 사람 몫이다.** 선행 셋이 치워졌고(① R56/WP-4 물리량 축 · ② R57/WP-1 `ess_share.py` 몫의 수 · ②-b R57/WP-2 실물 편익 표찰) **몫 → 편익 공장**(`core/casegrid/ess_share_benefits.py` · `41f78f1`)과 **러너 배선**(`run_single_case_e2e(ess_shares=…)` · `core/casegrid/ess_build.py::build_case_ess_fleet`·`build_fleet_streams` · `dd19cdc`)이 섰다. 몫 전건이 디스패치·수명·취득비·고정 O&M·자원 표·잉여판매 차감에 실리고, **교체비·잔존가치만은 물리 배터리 한 대**다(한 대를 가른 것이지 여러 대를 산 것이 아니다). 몫이 있으면 단일 경로의 `PeakShaving`·`NWAs`·`CP` 를 **짓지 않는다**(함께 지으면 배타 규칙이 못 막는 이중 계상이다). ⚠⚠ **어느 케이스도 몫을 주지 않는다 — 몫 비율·역할 배분을 아무도 정하지 않았고 지어내지 않았다**(★계절과 같은 방식). ⚠ **거부로 닫아 둔 갈래 둘**: `SelfConsumption` 역할(귀속 판정이 선행 → ★관점공백) · 「TOU 차익거래」 역할(첨두·경부하 에너지 단가가 저장소에 **없다** · 0 으로 안 채웠다). ⚠ **다음 배선은 자리 내기가 선행이다** — `e2e_runner.py` 가 코드 **496/500(여유 4)** 로 돌아왔다(★N). 경위·판정 셋은 `status-history.md` **R57 블록**. 아래는 그 선행을 찾을 때의 기록이며 **지금 상태가 아니다**: **용량 분할**(§3 **뒷 문장** · `mode_weights`/`HYBRID`) — **R52 가 판정하지 못했다**. ⚠⚠ **R56 이 선행을 찾았다 — 위 ★배타물리량이다.** 한 대의 ESS 를 몫으로 갈라 몫마다 다른 역할을 주면 두 몫의 편익이 나란히 서는데, 지금 판정은 그것을 **유형 E 로 거부한다**(실측 — 위 행). 몫이 다르니 물리량도 다른데 판정이 못 본다 ⚠⚠ **선행이 둘이다 — R56 이 둘째도 찾았다.** ② **고정비·정격출력이 「자원당」이라 몫으로 나눌 자리가 없다**: 러너가 `ESS(power_kw=ESS_POWER_KW, fixed_om_won_per_year=ess_fixed_om, capex_unit_won_per_kwh=ess_capex, …)` 로 세우는데 — `capex_unit_won_per_kwh` 는 **용량에 비례해 옳게 갈리지만** `fixed_om_won_per_year`(대장 10만원/년)와 `power_kw` 는 **인스턴스마다 통째로 붙는다.** 몫을 `ESS` 두 대로 표현하면 **고정 O&M 이 2배 · 정격출력이 2배**가 된다(용량만 갈렸는데 PCS 가 두 대가 된다). ⚠ `core/der/ess.py` 는 코드 **498/500(여유 2)** 이라 몫 개념을 그 파일에 넣을 자리도 없다 — **새 모듈이 선행**이다(본은 `pv_allocation.py`·`perspectives.py`) | ✅ **구조는 닫혔다 — 코드로 집을 것이 없다.** 남은 것은 **몫 비율과 역할 배분**이고 **그것은 값이며 사용자 판정이다**(소유자는 `status-human.md`). ⚠⚠ **채우면 결론축이 움직인다** — 그때 골든 3건 + `MC-1` 리포트 + `status-human.md` 인용 수치를 **한 단위로** 다시 뽑는다. ⚠ **「구조가 섰다」를 「용량 분할을 반영했다」로 읽지 마라**(★계절과 같은 경고다). 종전 이 칸에 있던 *「선행 둘을 치우기 전에 착수하면 ① 실행이 거부되고 ② 고정비가 부풀어 결론축이 틀린 방향으로 움직인다」* 는 **R57 이 그 둘을 치웠으므로 더는 유효하지 않다** |
> | **★관점공백** | ★★★ **R58 이 이 행의 절반을 닫았다 — 남은 것은 「갈래를 못 박는 것」 하나다.** R58 은 판정 정본 §5 의 **2번**(관점별 소거 — `FR-704-AC8`)·**3번**(분산편익 물량 축 — `FR-404-AC4`)을 닫고, **1번을 좁혀** 기준선 갈래 셋을 선언으로 세웠다(`FR-705-AC2` · `DV-15` · `core/cba/baseline.py`). ✅✅ **2026-09-05 R61 — 이 행이 닫혔다. 집지 마라.** 사용자가 갈래 셋을 **문면으로 짝지어 확인했다**(정본 `docs/decisions-2026-09-05-R61.md` §2): *「기본은 자가용설비의 소유를 전기사용자가 가지는 것으로 함」*=**ⓑ**(`MAINTAIN` · **기본값**) · *「대안으로 분산e사업자가 자가용 설비를 인수하는 방안」*=**ⓒ**(`POOL`) · *「자가용 설비가 없는 집에 대해서 분산에너지사업자가 전력을 공급하는 방안」*=**ⓐ**(`NONE`). ⚠⚠⚠ **종전 이 칸은 *「여전히 열려 있다 — MC-1 이 계약구조를 주지 않아 정할 자료가 없다」* 로 서 있었고 그것이 R59b~R60 이후 거짓이었다** — R59b §1 이 *「ⓐ, ⓑ, ⓒ 선택할 수 있게 설계되야 함」* 으로 이미 답했고 **R60/WP-2 가 구현했다.** 그 낡은 칸을 읽은 세션이 2026-09-05 에 사용자에게 *「사업 구조를 정해 달라」* 고 **이미 답한 것을 다시 물었다.** ⚠ ⓒ 는 **거부되지 않는다** — 포기분 비용 항은 **R60/WP-3 이 세웠고**(`forfeited_self_consumption_row`), 남은 것은 **구분 계측 선언 하나**다(`pool_metering` · 사용자 문면의 「인수」가 소유·운영권 인계 쪽은 정한다). 아래는 R56~R57 이 이 물음을 좁게 세웠을 때의 기록이며 **지금 상태가 아니다**:  — 정본은 `docs/decisions-2026-09-03-R57.md`.** 아래 칸이 *「먼저 정할 것은 금액이 아니라 **귀속**이다」* 로 적어 두었는데 **그 틀이 좁았다**: 사용자가 *「자가소비 절감은 기존 베이스라인 대비 절감분인데 **기준점이 다르다**」* 고 답했다 — 분산특구의 공급 주체는 **분산e사업자**이고 전기사용자는 ⓐ 자가용 없음 · ⓑ 자가용 **유지**+분산e 공급 · ⓒ 자가용을 **집합자원에 포함** 셋으로 갈리며 **기준선(Without)이 셋이다.** ⇒ 귀속을 정하기 전에 **어느 갈래인가**를 정해야 한다. ★ **관점별 소거 규칙은 오케스트레이터가 해외 사례(`Ai-notes`)로 결정해 판정 정본 §4 에 세웠다** — 요금 차액은 **이전이므로 사회 관점에서 소거**하고(총괄지침 제45조⑤ · 뉴욕이 도매가격 인하 편익에 같은 처리를 한다) 관점별 표에서는 **부호를 반대로** 적는다. ⚠ **1·2 는 결론축을 움직이므로 사용자 승인 자리**이며 착수 순서는 판정 정본 §5 가 갖는다. ⚠ 「ⓒ」는 **구분 계측이 선행**한다(상계처리로는 책임공급비율의 분모가 서지 않는다 · 판정 정본 §2). 아래는 R56 이 그 물음을 좁게 세웠을 때의 기록이며 **지금 상태가 아니다**: ⚠ **절반 닫혔다** — 사회 관점 편익 배선은 **R53/WP-1 이 세웠다**(`12f9b43`). ⚠⚠⚠ **R56 이 「남은 것은 `SelfConsumption` 미화폐화 하나」를 두 번 정정했다 — 그리고 두 번째가 중요하다.** ★ **첫째 정정**: 화폐화하면 `SurplusSale` 과 **유형 A** 로 걸려 실행이 거부된다(위 ★배타물리량 · WP-4 가 그 막힘을 치웠다). ★★★ **둘째 정정 — 그 돈은 이미 결론축에 있다. 편익으로 더하면 이중 계상이다.** 실측(오케스트레이터가 리포트에서 직접 읽었다 · 대표일 기준): PV 발전 **10.800kWh** = 자가소비 **5.439** + 송전 **3.167** + ESS 충전 나머지. `GridPurchase` **121,167원/년** = **실제 수전 2.766kWh/일**(1,010kWh/년 × 120원 = 121,151원 · 일치)이고 `SurplusSale` **127,020원/년** = **실제 송전만**(1,156kWh/년 × 110원 = 127,155원 · 일치)이다. ⇒ **자가소비 1,985kWh/년은 「안 사서」 이미 비용을 줄이는 방식으로 계상돼 있다**(전량을 샀다면 359,379원/년 · 실제 121,167원). 편익 `SelfConsumption` 을 더하면 **같은 화폐 흐름을 두 번 센다** — `FR-402-AC1` 이 중복을 정확히 그렇게 정의한다(`RC-LD-B0` 도 같은 말이다). ⚠⚠ **그러므로 붙임 8 의 「순 방향 = 연 +19,853원」(구매 회피 +238,234 − 판매 포기 −218,382)은 「그 kWh를 팔았다면」이라는 반사실 비교이고 현재 기준선 대비 축 이동이 아니다** — 그 수를 「채우면 이만큼 좋아진다」로 읽지 마라. ★ **남은 것은 판정이다: 그 절감이 누구 지갑인가.** 지금은 **사업자**가 전력을 사고 `GridPurchase` 를 진다(관점표 실측 — 참여 주민 열에는 `PeakShaving` 74,880원/년뿐이고 NPV 974,035원). 주민 지갑으로 돌리면 **총합(사회)은 그대로이고 두 열이 함께 움직이며 사업자 결론축은 최대 연 238,234원 규모로 나빠진다.** | ⚠⚠ **「채우면 축이 좋아진다」로 읽지 마라** — 더하는 것은 이중 계상이고, 옮기는 것은 **결론축을 나쁘게** 만든다. **먼저 정할 것은 금액이 아니라 귀속이다** |
> | **★요금** | ⚠⚠ **전기요금 대장값이 조사보다 체계적으로 높다**(기본 35~55% · 전력량 62~79%) — **높으면 편익이 과대**다 | **약관 확보가 선행**(사람 몫) |
> | **★600** | **월 600kWh(사용자 예시) 대 연 3,600kWh(대장)** — **두 배 차**. ★ **R55 가 이것을 「수」로 세웠다** — 필요 용량이 **5.48kW 대 2.74kW**(리포트 붙임 10 · 이용률 15% 기준). ⚠ 둘 다 탐색 구간(1~9kW) 안이라 **지금 결론축에는 아직 안 닿는다** | 대신 정하지 않았다 — **물음이다.** 이제 답의 크기가 보인다 |
> | **★어휘** | **`FR-704` 어휘 ↔ 사용자 어휘** 어긋남 · **「국가 = 사회」는 미확인 가정** | spec 개정 여부는 **사람 몫** |
>
> ### ⚠ 사람 몫으로 내린 것 — **다시 조사하지 마라**
>
> | # | 무엇 | 왜 내렸나 |
> |---|---|---|
> | **★교체비** | **교체비의 크기 근거** | ★★ **R52 가 이미 조사했고 실패했다.** 대장 `capex.ess.replacement` 가 *「교체가 통상 더 싸다는 방향은 확인했으나(PV 인버터 150만~300만원/대, 용량 미상이라 환산 불가) **ESS 셀 교체 단가 자체는 국내 공공·업계 자료에서 못 찾았다** · `Q-2` 회신에 교체 시점 단가가 함께 오면 닫힌다」* 로 적어 둔다. **남은 것은 외부 회신**이다 — R53 이 이 자리를 다시 집으려다 재 보고 **집지 않았다**(한 시간을 같은 결론에 쓴다) |
>
> ⚠⚠ **코드 줄 예산이 바닥난 파일 표는 「함정」 절이 갖는다** — ⚠ **베끼지 말고 세라.**
>
> ⚠ 아래 옛 표의 **★V·★R·★S·★T 는 R52 가 닫았다.**
> ✅ **⑤ 는 R54 가 닫았다**(`507990d` — `fixtures/probe/`). 선행이던 유형 `E` 거부는
> 사용자 판정 §1 이 「만들지 않음」으로 풀었다. ★ **닫은 판정이 다른 항목의 선행을
> 「푼다」고 미리 적지 마라** — R52 가 *「⑤ 는 선행이 풀렸다」* 고 적었는데 R53 이 실행해
> 보니 **오히려 막혀 있었다.** 실행으로 확인할 것이지 추론할 것이 아니다.
> ★ 축을 한 번만 흔드는 순서 짜기는 `status-history.md` R51 블록의 「축을 한 번만 흔들기
> 위해 순서를 다시 짰다」 절이 방법을 적어 두었다.

| # | 무엇 | 시작 자리 | 나온 곳 |
|---|---|---|---|
| **★T** | **spec 판본 머리말이 낡았다**(v0.24 가 옛 결론축을 인용) · **`REC`·`opex.*` 의 `Q-*` 등재** — ★ **§8 과 같은 파일을 만지므로 붙여서 하라.** ⚠ **머리말은 「그 시점의 기록」이므로 지우지 말고 새 판본을 얹는다**(R51 이 v0.23·v0.24 를 그렇게 했다) | `rslt/spec-분산특구-경제성평가.md` 의 `version:` 머리말 · §15.1 | 판정 **§8** · R51/WP-6 |
| **★U** | **잉여판매 차감이 계약구조 경로에 없다** — R51/WP-5 는 **구조 없는 기본 경로**에만 배선했다. ⚠ **판정 §4(2026-09-01)가 그 경로까지 요구하는지 침묵한다** | `core/valuestream/settlement.py` 의 `SurplusSale` 생성 자리 | R51/WP-5 |
| **★E** | **정책 경고의 임자 — 「순편익을 계산해 저장·관리」로 답이 왔다** ⚠⚠ **그 읽기가 확인받지 못했다.** ★ **R51/WP-7 이 실물 증거를 만들었다** — `NWAs` 를 켜도 `POLICY_WARNING` 이 리포트에 오르지 않는다(`ESS` 가 `DER.policy_warnings()` 를 오버라이드하지 않는다). ⚠ 급하지 않다 | `core/der/ess.py::policy_warnings`(신설) · `core/report/policy_warnings.py` | 판정 2026-09-01 **§6** |
| **★N** | ⏸ **줄 수 상한 — 「나중에 처리」**(사용자 판정 §8). ⚠⚠ **그런데 R53 에서 `core/report/case_report.py` 가 코드 500/500(여유 0)에 닿아 「나중」이 「지금」이 됐다** — **R54 가 `case_report.py` 를 코드 500 → 403 으로 내렸다**(판정 §2 · `72af187`). ⚠⚠ **R57 이 같은 형태를 러너에서 만났다** — `core/casegrid/e2e_runner.py` 가 코드 **497/500(여유 3)** 이라 ★분할 배선을 넣을 자리가 없었다. WP-5 가 `ESS` 조립을 `core/casegrid/ess_build.py` 로 떼어 **여유 18줄**을 냈고(`2cfb794`) **WP-6 배선이 그 자리를 거의 다 썼다**(`dd19cdc` · 여유 4). ★ **WP-9 이 지표 조립(`_metrics_for`·`_initial_outlay`)을 `core/casegrid/case_metrics.py` 로 떼어 다시 여유 23줄을 냈다**(`ba637cc` · 코드 477/500). ⚠⚠ **그래서 이 항목은 닫히지 않았다 — 한 라운드에 두 번 걸렸고, 줄 수는 「한 번 내면 끝나는 것」이 아니다.** 다음 배선이 또 쓴다. ⚠ **줄 수는 이 표에 적지 않는다** — 「함정」 절의 표를 보고, 그 표도 **쓰기 전에 세라**(위 두 수도 그렇게 얻은 것이며 다시 세라) | `scripts/check_file_size.py --code-strict` | 판정 2026-09-01 **§8** · R53 |
| **★W** | ⏸ **배포 — 무료 호스팅으로 간다**(판정 §9). ⚠ **여전히 사람 몫 셋**: 운영 주체·예산(`Q-13`) · `TU-2` Litestream vs Turso · **배포 후 외부 접속 실측**. **대신 정하지 마라** | `status-human.md` 3단계 | 판정 **§9** |
| **★X** | ⏸ **R32 잠정 판정 일곱 — 「추후 검토 예정」**(판정 §10). ⚠ **미해결이 아니라 미룸이다** — 건드리지 마라 | `status-human.md` 4단계 | 판정 **§10** |

### 옛 R51 표 — **위 표가 대신한다**(아래 행들은 위에서 답을 받았거나 그대로 남았다)

| # | 무엇 | 시작 자리 | 나온 곳 |
|---|---|---|---|
| ~~**★V**~~ | ✅ **답이 왔다(§3)** — 아래는 R51 이 실물로 잡은 경위다. ★★★ **유형 `E` 배타가 배포 경로에서 걸리지 않는다 — R51/WP-7 이 실물로 잡았다** `assert_no_exclusions()` 는 **유형 `A` 만 거부하고**(조항이 행마다 수용 수준을 다르게 정해 두었다), 유형 `E` 를 라벨링하는 `build_report()` 는 **배포 코드에서 부르는 자리가 0곳**이다. ★ **실증** — 「계통 방전」에 `NWAs` 단가를 준 실행이 **거부 없이 서고** `NWAs`(361,715원)가 `SurplusSale` 과 나란히 계상된다(가구 부하를 세우면 `PeakShaving` 도 함께 실린다). ⚠⚠ **R51/WP-3 의 커밋과 `ESS.value_streams()` 독스트링이 *「배타는 실행 경로가 건다」* 고 적었는데 그것이 거짓이었다** — 그 검사는 `build_report()` 를 **직접 불러서** 통과했다. **R26 이 *「거부 기계는 있는데 배포 코드가 아무도 안 부른다」* 로 밟은 형태가 한 층 위에서 되풀이된 것**이다(WP-7 이 그 독스트링의 거짓 문면을 그 자리에서 정정해 두었다). ⚠ **어느 쪽으로 고칠지가 판정이다** — 유형 `E` 는 이 저장소가 *「「막는」 규칙이 아니라 「성립하지 않음을 선언」하는 규칙」* 으로 세운 것이라 **거부로 바꾸면 그 선언과 어긋난다.** ★ **⑤의 진짜 선행이다** | `core/valuestream/exclusion_table.py::assert_no_exclusions` · `core/valuestream/report.py::build_report` · `core/casegrid/e2e_runner.py` 의 배타 호출 자리 | R51/WP-7 |
| **⑤** | **「남는 경우」 골든을 만든다** — `NWAs` 를 활성화하고 보상단가를 **충분히 크게** 준 시나리오로 `GAP_MARGIN`(「여유」) 갈래를 덮는다. ⚠⚠ **★V 가 선행이다 — 그것부터 닫아라.** 지금 `NWAs` 를 크게 켠 골든을 만들면 **사업자 운전 편익이 함께 실린 채로 「남는다」를 세우게 되고**, 그것은 판정 §3 이 *「동시에 성립하지 않는다」* 고 못 박은 조합이다 — **가짜 골든을 만들지 않는다**(R51/WP-7 이 그 자리에서 멈췄다). ★ **돈이 도는 배선(옛 ★Q)은 R51/WP-7 이 닫았다**(`8d1d05a`). ⚠ **R51 의 「집 우선」도 이 갈래를 덮지 못한다** — 전액 지원해도 결손 **2,791,162원**이 남는다. ⚠⚠ **사업 전망이 아니라 코드 경로를 지키는 시험이다** — 단가는 **시험용 가정값**이며 `MC-1` 결론과 나란히 인용되면 「NWAs 를 켜면 남는다」로 읽힌다. 파일 이름부터 그 사실을 들게 하라 | `fixtures/golden/` · `core/report/narrative.py::GAP_MARGIN` | 판정 **§7** |
| **★R** | ★ **`REC` 를 언제 켜는가 — 단가에 값 근거가 없다** R51 이 배선은 했고 `benefit.rec_price` 를 **`track: "default0"`(값 0)** 으로 두었다(지어낸 단가 위에 결론축을 올리지 않는다 · R48 §6 과 같은 자리). **대장 `value` 한 줄로 켜지고** 그 한 줄이 정말 켜는지를 `test_rec_price_moves_the_conclusion_axis` 가 붙든다. ★ **켜면 +1,054,033원 · 결손의 약 8%**(70원/kWh 탐침 · 조항 예시 70,000원/REC ÷ 1,000). ⚠ **REC 만으로는 결론이 뒤집히지 않는다.** ⚠ 켜는 순간 **가중치도 대장으로 가야 하고** 그것을 `test_rec_weight_moves_to_the_ledger_when_the_price_does` 가 빨간불로 막는다 | `docs/assumptions.yaml` 의 `benefit.rec_price` · `e2e_runner.py::REC_WEIGHT_PV` | R51/WP-6 |
| **★S** | **리포트가 「태양광 자가소비율 0%」를 그대로 인쇄한다** — **사실이다**(`PV.self_consumption_ratio` 는 여전히 0 이고 이제 「배터리 우선」 갈래에서만 읽힌다). **그러나 「집 우선」이 기본값이 된 지금 검토자는 그 칸을 *「집에서 하나도 안 쓴다」* 로 읽는다** — 실제로는 가구가 그 스텝의 PV 를 먼저 쓴다. 붙임 4 의 「운전 방식」 칸도 태양광에 *「전량 판매」* 라 적는다. ⚠ **`MC-1` 이 재는 것이 정확히 「검토자가 리포트만 보고 설명할 수 있는가」다** | `core/report/` 의 그 칸을 짓는 자리 · `docs/report-form-심의보고서.md` | R51/WP-6 |
| **★E** | **정책 경고의 임자 — 「순편익을 계산해 저장·관리」로 답이 왔다** ⚠⚠ **그 문면을 「편익 쪽에서 관리하라」로 읽은 것은 오케스트레이터이고 확인을 받지 못했다.** 구현 전에 **그 읽기를 가정으로 명시**하고 시작하라. ⚠ 급하지 않다 — 사용자가 순서를 바꾸지 않았다. ★ **★Q 와 같은 자리를 지난다**(`ESS.policy_warnings()`) | `core/der/` · `core/report/policy_warnings.py` | 판정 **§6** |
| **★N** | ⏸ **`core/der/ess.py` 줄 수 상한 — 「나중에 처리」**(사용자 판정 §8) ★ R51/WP-3 이 `_in_range()` 에 배타 경계 인자를 붙여 **500/500 → 498/500** 로 되찾았다 — **여유 2줄**이다. ⚠ **`e2e_runner.py` 도 473/500(여유 27)이고 `PLR0915`(statement 상한 50)에 이미 걸려 있다 — 그 함수는 새 statement 를 받을 자리가 없다**(값을 더 받아야 하면 기존 대입 안에 접거나 도우미로 뺀다) | `core/der/ess.py` · `core/casegrid/e2e_runner.py` · `scripts/check_file_size.py --code-strict` | 판정 **§8** |
| **★T** | **spec v0.24 머리말이 낡았다** — *「결론축은 안 움직인다 — 무보조 `npv` −12,956,180원」* 이라 적는데 R51/WP-6 이 그것을 움직였다. 판본을 올릴지(v0.25), 「집 우선」 기본값과 `FR-401-AC2.REC` 배선을 조항 쪽에 어떻게 적을지는 **spec 개정 판단**이다(§16.5). ⚠ **`REC.formula()` 의 단위 표기를 `원/REC` → `원/kWh` 로 고쳤는데 조항 예시가 이미 `÷ 1,000` 을 적고 있어 조항은 안 고쳐도 된다고 판단했다** — 다시 볼 자리 | `rslt/spec-분산특구-경제성평가.md` 의 `version:` 머리말 | R51/WP-6 |
| **★U** | **잉여판매 차감이 계약구조 경로에 없다** — R51/WP-5 는 **구조 없는 기본 경로**(`MC-1` 이 쓰는 경로)에만 배선했다. 상계·직거래 구조에서 ESS 가 `GRID` 로 차익거래하면 그 경로의 `SurplusSale` 은 **여전히 차감을 받지 않는다.** ⚠ **판정 §4 문면이 그 경로까지 요구하는지 침묵한다** | `core/valuestream/settlement.py` 의 `SurplusSale` 생성 자리 | R51/WP-5 |

### 판정을 기다리지 않는 나머지 — **위 표를 끝낸 뒤**

| # | 무엇 | 시작 자리 | 나온 곳 |
|---|---|---|---|
| **★P** | **검사 대상을 어디까지 넓힐 것인가 — 「운영 문서」와 「기록」을 가르는 규칙이 필요하다** R50 이 세운 `check_doc_line_citations.py` 는 **`rslt/` 두 문서만** 본다. 실측으로 세어 보니 — **`status.md` 는 넷이었고 R50 이 전부 이름으로 바꿨다**(그중 셋이 이미 낡아 딴 행을 가리키고 있었다. 지금 **0건**) · `status-history.md` **10건 이상** · `docs/*.md` **9건 이상**. ⚠ **남은 둘은 단순히 「넓히면」 되는 것이 아니다** — `status-history.md` 와 `docs/decisions-*.md` 는 **그 시점의 기록**이라 고치면 이력이 거짓이 되고, `docs/domain-rules.md`·`docs/evidence-standard.md` 는 **바이트 동일해야 하는 정본 사본**이라 한 글자도 못 고친다(`check_source_rules`). `docs/traceability.md` 는 **자동 생성물**이다. 그러므로 넓히려면 **「무엇이 지금 참이어야 하는 운영 문서이고 무엇이 기록인가」를 규칙으로** 세워야 하고, 그것은 면제 목록이 아니라 규칙이어야 한다 | `scripts/check_doc_line_citations.py` 의 대상 목록 | R50/WP-4 |
| **25** | **리포트가 안 내는 표시 → 이제 넷이다.** ✅ **`FR-602-AC2` 는 R57/WP-8 이 닫았다** — 붙임 1 안에 「기준 전제 대비 변경 항목」 표가 서고 `CaseReport` 가 `AssumptionSet.overridden_items()` 를 읽어 나른다. ★ **닫은 것은 시험이 아니라 문면이다**(`FR-404-AC1` 을 R48 이 닫은 방식과 같다) · ★ **비어 있는 것이 정상이고 그것을 인쇄한다**(`없음 — 기준 전제를 그대로 적용`) — 「변경 없음」과 「그 표시를 싣지 못했다」는 다르다. 구획 부채 대장 둘에서 그 짝을 함께 지웠다. **남은 넷**: `FR-106-AC5`(안분 규칙) · `FR-603-AC2` · `FR-705-AC1` · `FR-905-AC8`. 종전 1번의 잔여이며 **여섯 중 둘이 닫혔다** — `FR-404-AC1`(R48/WP-D2) · `FR-602-AC2`(R57/WP-8). 둘 다 **문면을 세워서** 닫혔다. ⚠⚠ **남은 넷은 WP-8 과 같은 방식으로 닫히지 않는다**(R57 실측): WP-8 이 쉬웠던 이유는 `AssumptionSet` 이 **이미 리포트에 닿아 있어** 「읽는 자리」만 만들면 됐기 때문이고, `BaselineComparison`(`FR-705-AC1`)은 `core/cba/__init__.py` 의 **재수출 말고는 배포 경로에 없으며**(러너가 만들지 않는다) `TimeSeriesDataset.source_metadata()`(`FR-905-AC8`)는 그 이름을 쓰는 것이 `core/assumption/**` **안에만** 있다. ⇒ **넷은 「읽는 자리를 만드는」 일이 아니라 「그 값을 먼저 실행 경로에 세우는」 일이다** — 그것이 아래 「미해결」의 범위 행이 넷을 사람 판정으로 둔 이유와 같다 | `core/report/` · `.orch/R44/result_15.md` 의 표 | WP-15 |
| ~~**5**~~ | ✅ **R57/WP-7 이 닫았다 — 집지 마라**(`c4046c4`). 두 `salvage_value` 가 **마지막 취득 연도 `n − ((n−1) % life)` 의 교체 물가 계수**를 태운다 — 그 해가 `replacement_schedule()` 이 장부하는 해와 **같아서** 두 수가 같은 가격 기준에 선다. 부채 목록 `KNOWN_SALVAGE_IGNORES_REPLACEMENT_RATE` 를 **비웠다**(비어도 그 시험은 *「판정 대상 전건이 명목 기준을 쓴다」* 를 잰다). ★ **값이 0 인 지금이 골든이 증인으로 설 수 있는 유일한 시점이었다.** 아래는 그때의 기록이며 지금 상태가 아니다: `Load`·`ThermalLoad` 의 잔존가치 비대칭 — `Load` 는 러너에 있으나 **비용 인자가 전부 0/미지정**이고 `ThermalLoad` 는 **러너가 아예 만들지 않는다** ⇒ **지금은 결론축에 영향이 없다.** ⚠ 그러나 **WP-12 가 `C-5` 를 명문화해 이 둘의 부채가 「미확인」에서 「명시적 위반」으로 격상됐다** — **러너에 들어오는 날 터진다.** | 부채 래칫 `KNOWN_SALVAGE_IGNORES_REPLACEMENT_RATE` (2건) | WP-D2 |
| **7** | 안분 **잔차를 받는 자리**(최종 몫)가 **임의로 정해진 규약**이다 — 합성 시험은 **이미 있다**(`test_benefit_attribution.py` 가 `[33,33,34]` 과 `rows[RESIDUAL_HOLDER_INDEX]` 를 단언한다). ⚠ **「검증된 적이 없다」는 「시험이 없다」가 아니라 「MC-1 케이스의 잔차가 0원이라 프로덕션 경로에서 한 번도 발동한 적이 없다」는 뜻이다**(R47 이 이것을 오독해 WP 하나를 헛돌렸다). 남은 것은 **그 규약이 옳은가**라는 판정이다 | `core/casegrid/attribution.py::RESIDUAL_HOLDER_INDEX` | WP-E2 |
| **8** | **③-2 후보를 누가 언제 대조하는가** — ⚠ **수를 다시 셌다(2026-09-04 · R59)**: 후보 **47건** · 인용 있는 조항 302건 · 인용 **1,487건**, 최다 인용은 `NFR-303-M1` **171건**(종전 칸은 46건·166건이었다 — 명령은 `python scripts/audit_clause_selfreference.py` 이고 **베끼지 말고 돌려라**). ⚠ **지목이지 판정이 아니다**(독스트링은 거짓일 수 있다). ⚠⚠ **손으로 대조하지 마라** — 이 저장소는 전수 대조를 손으로 쓰게 했을 때 **조항 문면을 지어냈다.** ★★ **R59 가 도구와 순서를 세웠다** — `scripts/audit_clause_dossier.py` 가 **spec 원문에서** 조항 문면을 뽑아 인용과 나란히 대고, 판정은 그 대장에서만 인용한다(`--from-audit --min-citations N` · `--clause <ID>`). ★★★ **R59 가 지목 전건을 판정했다 — 맞다 44 · 어긋났다 5 · 불명 0.** 판정표는 **`docs/decisions-2026-09-04-R59.md`**(§2 인용 5건 이상 12건 · §5 나머지 37건). 어긋난 다섯 중 하나(`NFR-105-AC1`)는 R59 가 닫았고 **넷이 남았다 — 아래 35번**. ⚠ **「맞다」 44 중 11건은 오탐 원천 둘**(합성 픽스처의 조항 ID · `(FR-1002-AC1, AC2, AC4)` 꼴 약식 표기 · 그 문서 §4)로 설명된다. ⚠⚠ **기준선이 라운드 안에서 움직였다** — 47 → 48건(마커를 옮기면 후보가 바뀐다). **베끼지 말고 돌려라** | `scripts/audit_clause_dossier.py` · `docs/decisions-2026-09-04-R59.md` | WP-14 · R59 |
| **9** | **③-1 의 실물 부채를 언제 고치는가 · 구획 사전을 넓힐 것인가** | WP-14 판정요구 | WP-14 |
| **10** | **`_INERT_BASES` 확장 시 사유 규약** | WP-10 판정요구 | WP-10 |
| **11** | **빌더 위치**(`incentive/calculator.py` vs `proforma.py`) 유지 여부 · ⚠ **양수 거부가 새로 생긴 제약**임을 인지할 것 | WP-11 판정요구 | WP-11 |
| **12** | **`status-human.md` 의 「배타 규칙의 「구조 축」이 조항에 없다」(ⓐ) 해소 표시 여부** · **같은 파일의 「`FR-104-AC3`(spec §414 ③)의 「어느 EOL 인가」가 침묵이다」 동시 개정 여부** (⚠ 사람 몫) ⚠ **줄 번호로 가리키던 것을 R50 이 제목으로 바꿨다** — 그 파일은 사람이 고치므로 번호가 가장 빨리 낡는다(옛 `:219·:220` 은 이미 딴 행을 가리키고 있었다) | `status-human.md` 의 그 두 행 | WP-12 |
| **14** | **자원 구성을 케이스 축으로** — 지금 러너는 PV+ESS 를 한 벌로 고정해 **0kWh 미설치안을 계산하지 못한다**(`ValidationError`) | `core/casegrid/e2e_runner.py` · `ledger_levels.py` | WP-I |
| **15** | 4.4 에 **「두 최선을 더해 읽어도 된다」**를 실을 것인가 — 교차항이 **+3원**임을 쟀다 | `core/report/capacity.py` | WP-I |
| **16** | **회수 시점이 지원율에 대해 불연속**임을 리포트에 실을 것인가 — 자료에 없던 사실이다 | 붙임 3 · 6.2 | WP-I |
| **17** | 답변서의 탐침(`.orch/R43/probe_wp_i.py`)을 **`scripts/` 로 옮겨 커밋**할 것인가 | `scripts/` | WP-I |
| **18** | 태양광 **상한 9kW 를 옮길 것인가** — 탐색 편의값임이 확인됐다(**사업 주관 측 판정**) | `core/casegrid/ledger_levels.py` 의 `_DESIGN_VARS` 중 `pv_capacity_kw` 수준(`high` = 9.0) | WP-I |
| **19** | `tariff.hv_single_contract.avg` 가 **아직 스윕 축이 아니다** — 구조가 서는 날 축이 되어야 한다(지금은 사유와 함께 면제 등재) | `core/casegrid/ledger_levels.py` 의 면제 목록 | WP-H |
| **20** | `demand_charge` 3수준이 `…avg` 의 **상대폭을 빌린 대리값**이다 — 약관 회신 시 실측으로 교체 | `docs/assumptions.yaml` (`Q-6`) | WP-H |
| **21** | 최초 부속설비 몫이 본체 잔존가치에 남는다 (`ESS` PCS · `PV` 인버터) — **`Q-2` 회신에 묶여 있다** | `status-human.md` 가 소유자 | 종전 3 |
| **22** | ⚠ **옛 `todo.md` 5-② 의 나머지 절반** — 최초 인버터 몫을 본체 취득가에서 뗀다. **필요한 값이 이제 대장에 있다**(R39-E 가 미룬 조건 해소). **떼면 결론축이 움직인다** | `core/der/pv.py::_acquisitions` · `capex.pv.inverter_share` | WP-F |
| **23** | 변동 O&M **배선**(빌더 + 단가 대장 + 러너) — 붙임 8 이 좌표를 갖는다 | `core/cba/proforma.py` · `e2e_runner.py` | WP-C |
| **26** | 새 PV 결론을 다시 재기 — **평탄 발전 프로파일**(야간 발전) 때문에 PV 가 좋아 보인다. 일사 곡선이 배선되면 뒤집힐 수 있다 | `core/casegrid/profiles.py::DailyShapes` · `e2e_runner.py` 의 `generation_profile` | R35 |
| **27** | 프로포마 행 목록을 `CaseOutcome` 에 실을 것인가 — R36 판정 *「검사를 세우려고 산출물 자료형을 넓히는 것은 순서가 거꾸로다」* | `core/casegrid/models.py::CaseOutcome` | R34 |
| **28** | `FR-401-AC2.VPPMarket` (Phase 2) — 자료가 아니라 **순서 문제**. `VPP` 가 계약에 앉는가가 선행 | spec `AC1.VPP` | R33 |
| **29** | `SettlementInputs.annual_generation_kwh` 를 옮긴다 — **자원 없이 부르는 호출자가 사라지면** | 그 필드 주석 | R33 |
| **30** | 얇은 리프의 남은 항목 — 그중 `FR-202` 비교표 계층은 리포트 조립 계층이다 | `.orch/R37/gap_thin_leaves.md` | R37 |
| **31** | 닫기 라운드 — **마커 49곳을 읽어 대조**. 남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나이며 **읽는 수밖에 없다**. 정규식은 `(?<!N)FR-` | R32 가 옮긴 마커 49곳 | R33 표 5 |
| **32** | ~~★ **게이트 셋의 판정 스텝이 직접 푸시 경로에서 돌지 않는다**~~ ✅ **R60/WP-1 이 닫았다.** 사용자가 **PR 경로를 채택**했고(`docs/decisions-2026-09-04-R59b.md` §2) R60 이 그 경로를 세워 **권한을 실측**했다 — `gh pr create`·`gh pr merge` 가 되고 `main` 에 브랜치 보호가 없다. **게이트 셋이 저장소 이력에서 처음으로 판정됐다.** 닫는 절차는 「게이트」 절의 «라운드는 PR 로 닫는다» 가 갖는다. ⚠ **남은 것은 절차 준수뿐이다** — 직접 푸시로 닫으면 그 순간 다시 「미수행」이 된다 | `status.md` 「게이트」 절 · `.github/workflows/tests.yml` | R59→R60 |
| **33** | **`PerspectiveWiring.transfers` 를 리포트가 아직 인쇄하지 않는다** — R58 이 축은 세웠고 인쇄는 미뤘다. 지금 **이전 편익이 0개**라 인쇄할 것이 없기 때문이다. ⚠ **첫 이전 편익이 서는 날 함께 세운다** — 그때까지는 「인쇄하지 않는 것」이 옳다. ★ R58 의 인계 요약에만 있던 항목을 R59 가 여기로 내렸다(요약은 라운드마다 갈리므로 그 자리에 두면 사라진다) | `core/report/` · `status-history.md` R58 블록 | R58 |
| **34** | **물량 표찰이 아직 배타 판정에 쓰이지 않는다** — `DistributedBenefit.quantity_id` 는 **비어 있고 그것이 옳다**(물량이 셋이다). ⚠ **아무 값이나 채우면 틀린 물량으로 배타 판정이 통과한다.** ★ R58 의 인계 요약에만 있던 항목을 R59 가 여기로 내렸다 | `core/valuestream/` · `status-history.md` R58 블록 | R58 |
| **35** | ★★★ **③-2 가 찾은 어긋남 넷 중 하나가 남았다 — `FR-103-AC2`**(아래 ⓑ)(R59 판정 · 전문은 `docs/decisions-2026-09-04-R59.md` §5). ✅ ⓐ **`FR-604` 마커 밀림은 R59/WP-6 이 닫았다** — 다섯 조항의 마커를 조항 문면에 맞춰 되돌리고 `AC4`(자부담: 잔여 비율 자동 계산)를 재는 시험을 신설했다. ⚠⚠ **그 밀림 중 셋은 자기(틀린) ID 를 문면에 적어 ③-2 그물에 애초에 걸리지 않았다 — 지목이 원리적으로 못 보는 형태이며, 다음 그물이 겨냥할 자리다.** ⓑ **`FR-103-AC2`**(독립적인 `IncentiveScheme` 참조)를 재는 인용이 0건 — 한 시험이 `AC1`·`AC2`·`AC3` 셋을 달았는데 `AC2` 몫이 없다(`AC3` 도 프로포마 행을 단언하지 않으며 자기 ID 를 적어 그물 밖이다) ✅ ⓒ **`FR-204-AC2` 는 R59/WP-7 이 닫았다** — **Phase 2** 조항에 Phase 1 시험이 마커를 달고 있었다. **마커를 뗐고 미매핑이 9 → 10 이 됐다.** ★ **늘어난 것이 옳다**: 그 조항은 Phase 2 이고 구현이 없으며, DoD 9 시험은 **Phase 1** 미매핑만 0 을 요구한다(종전 9건도 전부 Phase 2/3 이다). ⚠⚠ **미매핑을 0 으로 만들려고 다른 것을 재는 시험에 마커를 얹지 않는다** — 그것이 이 라운드가 내내 고친 결함이다 | ⓑ: `tests/model/…` 신설 | R59 |
| **36** | ★★★ **계절 몫이 운전을 가르지 못한다 — 배포 실행은 「몫 가중 평균 대표일」 한 벌을 365일로 연간화한다**(R60/WP-4). ⚠⚠ **그렇게 하지 않으면 앞 24스텝이 「첫 계절(봄)의 하루」가 되어 연간 발전이 +281kWh 생기고 연간 부하가 −315kWh 사라진다** — 계절을 적는 **차례**만 바꿔도 결론의 부호가 뒤집힌다(겨울부터 적으면 발전 대표일이 0.689배). 그래서 R60 은 평균 하루로 막았고 **계절 간 하루 차이는 결론에 서지 않는다.** ⇒ **계절 넷의 대표일을 각각 돌려 합산하는 운전**을 세워야 한다(러너·붙임 7·`dispatch_note` 구조 변경 · **결론축이 다시 움직인다**). ⚠ 자산에 계절마다 24스텝 형상과 몫이 **이미 있다** — 없는 것은 운전 쪽이다. ⚠ 미반영 항목 「계절·요일 변동」의 `resolves_when` ⓐ 가 이 항목이다 | `core/casegrid/e2e_runner.py` · `core/der/{pv,load}.py` · `core/report/appendix_sections.py` | R60 |
| **37** | ★★ **미반영 항목 하나가 본문을 두 줄 만든다 — 그래서 ⓒ 를 고른 실행의 본문이 221줄로 예산(219)을 2줄 넘는다**(R60/WP-3 실측). `core/report/narrative.py` 가 **같은 미반영 목록을 본문 3.4 와 6.3 에 두 번** 인쇄한다. ⚠ ⓑ(골든)는 **218 그대로**이고 상한 검사는 골든으로 재므로 **초록불이며, 그래서 조용하다.** ⚠⚠ **상한을 올려 풀지 마라 — 이미 다섯 번 밀린 자리다.** 고칠 갈래 둘: ⓐ 6.3 을 붙임으로 내린다 ⓑ 3.4·6.3 의 중복 인쇄를 하나로 합친다. **둘 다 전 갈래의 본문 줄 수를 함께 움직인다** ⇒ 골든과 한 단위로 다시 뽑아야 한다. ⚠ 지금은 증분 래칫(`ⓑ ≤ 219` **그리고** `ⓒ−ⓑ == 3`)이 드리프트만 막고 있다 | `core/report/narrative.py` · `tests/report/test_overview_sections.py` | R60 |
| **38** | **ⓒ 는 「포기는 세고 대가는 0인 사업」이다** — `benefit.pool_compensation_price` 가 `track: default0` · 값 **0** 이고(제도·산정 기준 미확인) 비용 쪽 짝(포기한 자가소비)만 실제 금액으로 선다. ⚠ **그럴듯한 단가를 넣으면 그 비용이 곧바로 상쇄되면서 ⓒ 가 계산 가능해 *보인다*.** ⚠⚠ **단가가 오는 날 「곱할 물량의 정의」를 판정으로 함께 받아야 한다** — 집합자원에 반입한 kWh 가 발전량 전량인지 계통 반출분인지가 정산 설계 사항이라 지금은 **지어내지 않았다.** 그 셋(단가 · 물량 정의 · 프로포마 편익 행)을 **한 편집에서** 하라는 래칫이 `tests/report/test_pool_branch_calculated.py::test_the_pool_compensation_price_is_a_default0_ledger_item` 다 | `docs/assumptions.yaml` · `core/cba/proforma.py` · `core/report/unreflected.py` | R60 |
| **39** | ✅ **2026-09-05 R61 — 사람 몫이 닫혔다. 이제 「배선 대기」이며 착수해도 된다**(정본 `docs/decisions-2026-09-05-R61.md` §3). 사용자 문면: *「**햇빛소득마을은 다른 정부 지원프로그램의 종류임.** 역시 분석할 수 있도록 설계되야 함. **우선적으로 분석해야 하는 사업은 분산특구 지원사업임**」* ⇒ ⓐ 물어야 할 것은 *「그 사업인가」* 가 아니라 **「지원프로그램을 인스턴스마다 고를 수 있는가」**(`IncentiveScheme.funding_program` 이 그 자리다) ⓑ **조항이 맞고 구현이 없는 것이므로 spec 개정 안건이 아니다** ⓒ **기준 케이스는 분산특구 지원사업**이고 햇빛소득마을은 *고를 수 있어야 하는 다른 값*이다. ⚠ `FR-605`(자원별 차등 지원)와 **같은 배선**으로 세운다 — 따로 세우면 지원 조건이 두 곳에 산다. ⚠ 프로그램별 지원 조건 **값**은 대장(`docs/assumptions.yaml`)에 올린다 · 분산특구 지원사업의 실제 조건은 아직 저장소에 없다(회신 항목). ⚠⚠ 종전 이 칸은 *「사업 설계 범위이며 `status-human.md` 가 그 몫을 갖는다」* 로 서서 **Phase 1 인수를 막는 사람 몫**처럼 보였다. 아래는 R60 이 실물을 재었을 때의 기록이며 **막는 사유로는 더는 유효하지 않다**: **`FR-103-AC2` 를 잴 통로가 저장소에 없다 — 그리고 그것을 정직하게 드러내면 `DoD 9` 가 빨간불이 된다**(R60/WP-6 실측). 종전에는 한 시험이 `AC1`·`AC2`·`AC3` 셋에 마커를 달고 **`AC1` 만** 재고 있었다. ⚠ **없는 것은 자료형이 아니라 배선이다** — `IncentiveScheme` 은 실재하고(`core/incentive/schemas.py`) `FR-604` 는 닫혔는데, 스킴은 **케이스당 하나**로 들어와 **합산 총사업비**에 적용된다(`core/casegrid/incentive_cases.py`). 인스턴스가 스킴을 드는 필드가 `DERConfig`·`DER.__init__`·`PV.__init__` 어디에도 없다(`grep -niE 'scheme|incentive|subsidy|보조율' core/der/ core/asset/ core/model/` → **0건**). ⚠⚠ **마커를 떼면 Phase 1 Must-have 미매핑이 0 → 1 이 되어 `tests/acceptance2/test_17_9_dod9.py::test_dod9_phase_1_musthave_unmapped_zero` 가 빨간불이다**(그 시험은 예외 대장이 없는 단정이다). **종전의 초록불은 「Phase 1 이 다 재어졌다」가 아니라 「다 재어진 것처럼 마커가 달려 있다」였다.** ⇒ 해소는 **인스턴스별 지원 조건 배선**이며 `FR-604`·`FR-605`(자원별 차등 지원)와 함께 판정해야 하는 **설계 범위**다 — `status-human.md` 가 그 몫을 갖는다. ★ **재료는 이미 서 있다**: 브랜치 `r60-wp6-fr103-clauses`(커밋 `664e95b` · **푸시했고 병합하지 않았다**)가 ⓐ `AC3` 을 **프로포마의 분리된 행**으로 실제로 재는 시험 ⓑ `AC1` 만 남긴 마커 ⓒ 거짓 독스트링 정정을 들고 있다. ⚠⚠ **그 브랜치를 병합하면 통로가 서기 전까지 CI 가 빨간불이다** — 그래서 R60 이 병합하지 않았다(`.orch/R60/result_6.md` 가 전문이나 **그 파일은 이 기계에만 있다**) | `core/model/schemas.py` · `core/contracts/der.py` · `core/casegrid/incentive_cases.py` · `tests/model/test_financial_isolation.py` | R59→R60 |

> ## ★★★★ 2026-09-05 **사용자 판정 — 웹 UI 2차. 정본은 `docs/decisions-2026-09-05-R63.md` 이고 이것이 R63 의 1순위다**
>
> **사용자가 R62 의 화면을 실제로 보고 준 의견이다.** 여기 베끼지 않는다 —
> **요지와 조심할 것만** 적는다. ⚠ **문면을 인용할 때는 그 문서 §1 을 인용하라.**
>
> | # | 요구 | ⚠ 조심할 것 |
> |---|---|---|
> | **가** | **시나리오를 수정·저장**하고, **설정(전기요금·발전 프로파일·부하·설비 단가·이용률)을 수정·저장·로드**하며, **시나리오가 설정을 골라 적용**한다 | ★ **저장 계층은 이미 있다** — `app/services/scenario_store.py` · `app/routers/scenarios.py`(`FR-902`). UI 에 안 붙어 있고 **저장이 메모리**다. ⚠⚠ **전제 대장을 무르게 만들지 마라**(`NFR-202`) — 대장 위에 「사용자 전제 집합」을 얹는다. `FR-902` 가 *「자신의 시나리오·**전제 집합**을 저장·목록조회·불러오기·삭제」* 로 이미 그 조항을 갖는다 ⇒ **spec 개정이 아니라 구현이다.** ⚠ 기본값으로 돌린 실행은 **결론축을 움직이면 안 된다** ★★ **✅ R63 이 닫았다** — `assumption_overrides` 시나리오 필드 + `JsonFileScenarioStore` + 화면(`/ui/scenarios`·`/ui/settings`). ⚠⚠ **그러나 배포에서 `DER_SCENARIO_STORE` 를 정해야 저장이 남는다**(신설 45번). ⚠ 사용자가 든 다섯 중 **셋의 폭이 대장보다 좁다** — 발전 프로파일은 대장에 **없고**, 부하는 **연간 총량 하나**, 이용률은 **벽면 BIPV 하나**뿐이다(신설 48번) |
> | **나** | **용어를 하나로** — 「고급 모드」와 「전제」가 같은 것을 가리킨다 · **변수명 병기 금지**(`옥상 태양광 · azimuth_deg`) | ⚠⚠ **`ParameterSpec` 에 한국어 라벨 필드가 없다**(`tag`·`name`·`kind`·`unit`·`required`·`default`·`type_text` 뿐) — 라벨 원천을 먼저 세워야 한다. ⚠ **화면에 손으로 적지 마라**(카탈로그가 늘 때 낡는다). ★ 단위 라벨 사전이 `core/model/parameters.py` 에 이미 있다 — 같은 자리·같은 모양. ⚠ 낱말은 **화면·spec·작업목록·함수 이름·검사 이름**에 흩어져 있다 ★★ **✅ R63 이 닫았다** — 라벨 원천은 `LABEL_BY_NAME`(85건)·`resolve_label`·`ParameterSpec.label`. 화면은 `spec.label` 하나만 읽는다. 용어 판정 정본은 `docs/decisions-2026-09-05-R63b.md`. 실측: 사람이 읽는 자리의 변수명 **41종 235회 → 0** · 「고급 모드」·「전제」 **2 → 0**. ⚠⚠⚠ **「전제」 270줄이 일반 국어다 — 일괄 치환 금지**(「함정」 절) |
> | **다** | **그룹화** — 태양광·ESS 등 자원별로 묶어 보인다 | `advanced_mode_fields()` 가 지금 **평평한 튜플 50개**를 낸다 ★ **✅ R63 이 닫았다** — 자원 **인스턴스별** 묶음이다(종류별이 아니다 — `PV` 를 둘 놓으면 어느 자원의 값인지 알 수 없다). 실측: 그룹 **2** · 필드 총수 **50 → 50**(조항이 「전체」다) |
> | **라** | ★ **검증 모드** — 분석 과정의 **중간값을 순차적으로** 보인다(사용자가 4단계 예시를 줬다) | ⚠⚠⚠ **첫 단계의 「가구별 계절별 소비패턴」은 지금 재료가 없다** — 운전 해상도가 **대표일 24스텝 하나**이고 계절이 운전을 가르지 못한다 ⇒ **36번이 선행이다.** ⚠ **없는 계절을 그려내지 마라** — 못 그리는 단계는 R62 가 차트 둘에 한 것처럼 **칸을 남기고 사유를 적어라** ★★★ **✅ R63 이 화면에 붙였다** — `core/report/verification.py` 의 **9단계 렌더러가 이미 있었고 CLI 하나만 부르고 있었다.** 사용자의 4묶음을 그 위에 얹었다(`/ui/verify`). ⚠ **못 그리는 다섯은 「칸 + 사유」다** — 가구 수·유형(신설 47번) / 계절별 운전 결과(**36번**) / 월별 무엇이든(**41번**) / 편익 항목별 연차(신설 47번) / 누진·TOU 요금(신설 47번) |
>
> ⚠ **아래 41~44 를 지우지 않는다** — 이 표가 그 **앞에 설** 뿐이다.

### R62 가 세운 것 — **40~44** (2026-09-05 · 웹 UI 실동화의 나머지)

> ★ **화면은 이제 돈다.** 아래는 *「돌긴 도는데 아직 이런 것들이 남았다」* 이며,
> 전부 **R62 가 브라우저로 눌러 보고 잰 것**이다(`status-history.md` R62 블록).

| # | 무엇 | ⚠ 조심할 것 | 자리 |
|---|---|---|---|
| **40** | ✅ **2026-09-05 R62 — 화면 결함 아홉(D1~D9)을 전건 닫았다. 집지 마라.** WP-6 이 D1·D2·D3·D6(단추가 눌리면 422 로 떨어지던 것 · 404 가 JSON 이던 것 · 화면 셋이 안 이어지던 것), WP-7 이 D4·D5·D7·D8(그림 171px · 데모 값이 실측처럼 보이던 것 · 마법사가 안 움직이던 것 · 단위 칸 없는 파라미터 11개), WP-8 이 D9(전체 파라미터 폼에 제출이 없던 것). ⚠⚠⚠ **그런데 「그래서 `MC-4` 가 판정 가능해졌다」는 거짓이다** — 이 행이 종전에 *「D9 를 고치면 `MC-4` 가 판정 가능해진다」* 라고 적고 있었고 **그것이 틀렸다.** D9 를 고쳐 화면이 낼 수 있는 거부가 `DV-15` 하나에서 **`DV-2`·`DV-3`·`DV-15` 셋**이 됐으나, `MC-4` 의 `method` 는 **`DV-1`~`DV-13` 전건**이고 **13 중 2 만 화면에 닿는다**: `DV-4`(시계열)·`DV-14`(운전 방법)는 카탈로그 갈래가 `시계열`·`선택`이라 화면이 **`type="button"` 편집기 자리**로만 그려 **제출되지 않고**, 나머지 아홉(`DV-1`·`DV-5`~`DV-13`)은 **애초에 자원 생성자의 규칙이 아니다**(전제·시나리오·요금·편익 층이다). ⇒ **`MC-4` 는 사람이 검수 범위를 정해야 하는 건**이며 그 판정은 `status-human.md` 가 갖는다 | `.orch/` 는 다른 클론에 없다 — 경위는 `status-history.md` R62 블록 ⑩ | (닫혔다) |
| **41** | ★★ **차트 둘이 501 이다** — `energy_balance` 는 `CaseReport` 에 **월별 시계열이 없어서**(운전 해상도가 대표일 24스텝 하나다), `feasible_region` 은 **2변수 격자가 없어서**(`coupled_sweeps` 는 격자가 아니라 별 모양 탐침 7점이다) | ⚠⚠ **값을 지어내 채우지 마라.** 대표일을 12로 펴면 열두 달이 같은 막대가 되고 **그 그림은 「계절 변동이 없다」를 결과로 주장한다.** 부분격자를 쓰면 **어느 반쪽을 고르느냐가 「목표 달성 영역」의 넓이를 바꾼다** ⚠ **36번(계절별 대표일)이 41 의 절반을 푼다** | `core/report/case_report.py`(코드 **478/500**) · `app/services/ui_charts.py` |
| **42** | **게이트 범위가 비어 있다 — ⓒ 는 R62/WP-9 이 닫았고 ⓐⓑⓓ 가 남았다** — ⓐ `NFR-105` 게이트 ①②③ 이 **`core/` 변경만** 판정한다(웹 UI 를 통째로 더한 PR #8 에서 셋 다 「판정 대상 없음」) ⓑ `mypy` 가 `files = ["core"]` 라 `app/`·`web/`·`tests/` 가 형 검사 밖이다 ⓓ `gen_traceability.py` 가 `tests/` 만 훑어 **`tests_e2e/` 의 마커가 매핑표에 안 실린다** · ✅ ⓒ `check_file_size` 의 `TARGETS` 에 `web` 을 더했고 `web/render.py` 를 **540 → 358줄**로 쪼개 예산 안으로 되돌렸다(**상한은 안 올렸다**) | ⚠ **셋 다 「검사가 통과했다」와 「검사가 무언가를 검사했다」의 간극**이며 이 저장소가 반복해 잡아 온 형태다. ⚠ 범위를 넓히면 **한꺼번에 빨간불이 될 수 있다** — 넓히는 것과 고치는 것을 한 라운드에 몰지 마라. ★ **ⓒ 가 그 순서를 실물로 보였다** — 먼저 쪼개고 나서 `TARGETS` 를 넓혔다. ⚠ 그리고 ⓒ 는 **두 겹으로 조용했다**: `TARGETS` 누락 + 총 540/코드 328 이라 `--code-strict` 로는 애초에 안 걸리는 `[설명 밀도]` 분류였다 | `pyproject.toml` · `.github/workflows/tests.yml` · `scripts/gen_traceability.py` |
| **43** | **조항 15.12 문면과 화면이 어긋난다** — *「**로그인** → 마법사 → 실행 → **내보내기**」* 인데 **로그인 화면이 없고**(UI 라우트에 인증이 안 걸려 있다) **내보내기 단추도 없다**(`/reports/golden/<이름>` 로 가는 링크가 어디에도 없다) | ⚠ **E2E 시나리오 셋은 그 사실을 숨기지 않고 적어 두었다**(`tests_e2e/test_e2e_flows.py` 머리말). ⚠ `MC-8` 의 *「핵심 3 시나리오 완주」* 판정이 **여기에 걸려 있다** — 사람이 「핵심 3」이 무엇인지부터 정해야 한다 | `app/routers/ui.py` · `web/templates/` · `docs/manual-checks.yaml`(사람 몫) |
| **44** | **화면이 사람의 시간과 입력을 버린다** — ⓐ 그림 요청마다 케이스를 다시 돈다(대시보드 한 번에 **5회 실행 ≈ 7초** · 캐시 없음) ⓑ **거부되면 사람이 고치던 값을 잃는다**(「전체 파라미터」 폼은 50칸인데 400 이 나면 전부 되돌아간다) | ⚠ ⓐ 의 캐시는 **「같은 질의면 같은 결과」라는 새 가정**을 세우는 일이고, 그 가정이 틀리는 날 화면이 낡은 그림을 그린다. ⚠ 함께 볼 것 — **그림 칸 HTML 이 두 템플릿에 사본**으로 있다(공통 조각 파일 하나면 끝난다) | `app/routers/ui.py` · `app/routers/ui_forms.py` · `app/services/ui_charts.py` · `web/templates/` |
| **45** | ★★★ **저장이 배포에서 남지 않는다 — 라우터 기본값이 인메모리다** (R63/F1·V3). `JsonFileScenarioStore` 는 섰으나 `app/routers/scenarios.py` 는 **`DER_SCENARIO_STORE` 가 있을 때만** 그것을 쓴다 ⇒ 사용자 요구(*「저장, 로드」*)가 **배포 설정에 달려 있다** | ★ **결함이 아니라 미완이다** — `app/deps.py` 의 `DEFAULT_DB_URL = "sqlite://"` 와 **같은 규약**이고, 홈 밑 파일을 기본으로 하면 `tests/app/test_scenarios.py` 가 검사 간 저장을 물려받아 **두 번째 실행부터 깨진다**. ✅ R63/V3 가 `Dockerfile` 에 `ENV`+`VOLUME` 을 세웠다. ⚠⚠ **남은 것은 무료 티어**(디스크 비영속 · `NFR-504`) — `infra/freetier.py` 의 복제 전략이 그 자리이고 **R63 은 배선하지 않았다.** ⚠ `docker run` 에 **`-v` 가 없으면 컨테이너를 지울 때 함께 사라진다**(README 가 적는다) | `app/routers/scenarios.py` · `infra/freetier.py` · `Dockerfile` |
| **46** | ★★ **`purge_expired` 의 인가 주체를 정할 조항이 없다** (R63/F1). 그 문에는 시나리오 id 가 없고 만료분 전부를 지운다 — 지금은 **누구나** 부를 수 있다 | ⚠ **갈래 셋 다 근거가 없다**: 소유자별로 나누면 `ScenarioStore` 프로토콜을 고치는 일인데 **`FR-902-AC3` 은 보관 기간만** 정한다 · `admin` 으로 막는 것은 `FR-504-AC3`(**제도 프로파일**)에 기댄 것이라 **조항 없이 둘째 인가 규칙**이 생긴다 · 그대로 두면 남의 복원 창을 **앞당겨 닫을 수 있다**(다만 지워지는 것은 이미 30일 전에 소프트삭제된 것뿐이다). ★ **R63 판정: 요구를 지어내지 않고 그대로 두었다** — 라우터 독스트링이 그 사유를 적는다. **다음 사람이 「빠뜨렸다」로 읽고 조용히 메우지 마라.** ⇒ 사람 몫(`status-human.md`)에 가깝다 | `app/routers/scenarios.py` · `docs/` (조항) |
| **47** | ✅ **2026-09-06 R63 연장 — ⓐ(가구 수·유형)는 닫혔다. 나머지 넷은 열려 있다**(PR **#25** — `load.household.count`·`load.household.type_mix` 를 `track: blocked` · `value: null` 로 대장에 세웠다. ⚠ **값을 지어내지 않았다** — 자리만 섰고 다음 라운드가 채운다). 원문: **검증 모드가 「칸 + 사유」로 남긴 다섯** (R63/S3 · 재료 실측은 `.orch/R63/result_P4.md`(⚠ **`.gitignore` 안이라 다른 클론에 없다** — 요지는 이 칸과 `status-history.md` R63 블록이 갖는다)) — ⓐ **가구 수·유형**(자료형 수준으로 없다 — 대장에 `kWh/호·년` 은 있는데 **「호」를 세는 필드가 없다**) ⓑ 계절별 운전 결과(**36번**) ⓒ 월별 무엇이든(**41번**) ⓓ **편익 항목별 연차 시계열**(러너가 다섯 편익을 **한 행**으로 합친다 — 항목 분해는 1년차만) ⓔ **누진·TOU 요금**(엔진은 있으나 **배포 리포트 경로에 서지 않는다**) | ⚠⚠ **화면이 그 다섯에 수를 인쇄하면 그것이 결함이다** — 시험이 그것을 잰다(`tests/app/test_ui_verify.py`). ⚠ ⓓ 는 **1년차 분해를 20년으로 되짚어 채우지 마라** — 운영비는 연 2% 오르고 편익은 평탄이라 되짚은 표는 실제 장부와 어긋난다. ★ ⓐ 는 가장 싸다(대장에 세대 수 항목 하나를 세우는 일이다) | `core/casegrid/e2e_runner.py` · `core/report/verification.py`(**읽기만**) · `app/services/verify_steps.py` |
| **48** | **사용자가 「설정」으로 든 다섯 중 셋의 폭이 대장보다 좁다** (R63/S2 실측) — **태양광 발전 프로파일은 대장에 없고**(값은 `PV.generation_profile_kwh` **시계열**이다), **부하는 `load.household.annual` 연간 총량 하나**, **이용률은 `capacity_factor.bipv_wall.ratio` 벽면 BIPV 하나**뿐이다(옥상 PV 이용률·ESS 가동률에 해당하는 대장 항목이 **없다**) | ★★★ **V1 판정(정본 `.orch/R63/result_V1.md`(⚠ **`.gitignore` 안이라 다른 클론에 없다** — 요지는 이 칸과 `status-history.md` R63 블록이 갖는다))**: 가장 작은 **정직한** 걸음은 **「연간 총량 + 대표일 24스텝 형상」까지만 노출하는 것**이다 — 엔진이 소비하는 것이 정확히 그 둘이므로 **편집 해상도 = 운전 해상도**가 되어 접힘으로 잃는 것이 없다. ⚠⚠⚠ **8760 전체 편집기·파일 업로드는 36번 뒤로 민다** — 그것 없이 열면 ⓐ 앞 24스텝만 쓰여 **첫 하루가 연간 결과를 만들고** 연간 합계 기반 수치와 두 기준이 갈라지거나 ⓑ 접혀서 **계절이 전부 사라진다.** ⓑ 를 「반영됐다」고 내보내면 사용자가 *「내 형상이 반영된다」* 고 **잘못 믿는다** ⚠ 「월별 가중치」 후보는 V1 이 **탈락시켰다**(같은 접힘을 만난다) · 「표준 프로파일 목록」도 지금은 못 한다(자원당 형상이 **하나씩뿐**) ★ 재료: `estimate_from_monthly_usage`(월 12 + 일간 24 → 8760)가 **같은 입력 형상을 이미 구현했다.** ⚠ **`infra/tsstore.py` 를 부르는 배포 코드가 0건**이다 | `core/assumption/timeseries_estimate.py` · `core/der/load.py` · `core/casegrid/profiles.py` · `web/` |
| **49** | ✅✅ **2026-09-06 R63 연장 — 닫혔다. 집지 마라**(PR **#27**). **거부도 통과도 아닌 표시**로 갔다 — `OverrideRow.read_by_this_run` 이 서고 붙임이 「이 실행은 안 읽었다」를 인쇄한다. ⚠⚠ **지시문 전제가 틀렸다 — 읽는 통로가 둘이다**: 결론축을 **299,438원** 옮기는 `capex.pv.rooftop` 은 계산 중에 `get()` 을 **안 지난다**(`build_level_map` 이 대장 YAML 을 직접 읽는다) ⇒ 「읽었다」의 정본은 **`provider.keys_read() | ledger_backed_variables()`** 합집합이다. ⚠ **남은 취약점은 55번**. 원문: **「인쇄되는데 안 먹는 키」** (R63/R1 D-5) — `tax.vat_rate` 는 대장 **안**에 있으나 **아무도 읽지 않는다**(`core/contracts/der.py` 가 이미 적어 두었다). R63 이 오버라이드 통로를 열자 `vat_rate: 1e9` 가 정당하게 통과하고 붙임은 `0.1 → 1000000000.0` 을 인쇄하는데 **수는 한 푼도 안 움직인다** | ⚠⚠ `core/assumption/scenario_overrides.py` 머리말이 그 상태를 **「가장 나쁘다」**로 지목한다 — *「화면은 고친 값을 인쇄하고 수는 옛 값으로 돈다」*. ⛔ **거부가 답이 아니다** — 어느 키가 읽히는지는 **실행마다 다르고**(갈래·자원 구성에 따라) 목록을 그 파일에 박으면 낡는다. ⇒ 필요한 것은 **「거부도 통과도 아닌 표시」**이고 그 자리는 그 함수의 반환값 **밖**이다. ★ R63/F2 가 **우회하지 않고 멈췄다**(그 파일 안에서 끝내는 길이 있었으나 두 곳에 사는 사실을 만들지 않았다) | `core/contracts/der.py` · `core/assumption/scenario_overrides.py` · `core/report/case_report.py` |
| **50** | **한 물건에 이름이 둘이다 — `Declaration` 과 `PREREQUISITE`** (R63/P2 전수) — 자료형은 `PoolMeteringDeclaration`(**선언**)인데 상수·함수는 `POOL_PREREQUISITE_TRANSFER`·`_METERING`·`pool_prerequisite_fields()`·템플릿 변수 `pool_prerequisites`(**전제**)다. R63 이 화면 낱말을 「집합자원화 **선언**」으로 옮겨 `Declaration` 쪽과는 맞고 `PREREQUISITE` 쪽과는 갈렸다 | ⚠ 부르는 자리 **30곳 넘는다**(전수는 `.orch/R63/result_P2.md`(⚠ **`.gitignore` 안이라 다른 클론에 없다** — 요지는 이 칸과 `status-history.md` R63 블록이 갖는다) §3 ④). ⚠⚠ **`core/cba/baseline.py` 는 결론축 경로다** — 화면을 늘리는 라운드에 함께 흔들지 마라(**R63 이 그 사유로 미뤘다**). ★ **화면에는 안 나오므로 급하지 않다** — 화면의 「전제」는 `<legend>` 한 줄이었고 R63 이 그것만 고쳤다 | `core/cba/baseline.py` · `web/render_run.py` · `web/render.py` · `tests/cba/` |
| **51** | **리포트 인쇄 문면의 「전제」 34줄** (R63/P2 계측) — 심의보고서·검증보고서 **17줄** · CLI `--help` **3줄** · 예외·거부 문면 **10줄** · 게이트 출력 **1줄** | ⚠ **`docs/report-form-심의보고서.md` 양식이 정본이고 검사 넷이 문자열로 단정한다** — 바꾸려면 양식과 검사가 함께 움직인다. ★ **R63 판정: 겹치는 뜻이 없으므로 바꿀 이유가 없다** — 리포트에서 「전제」는 **대장을 가리키는 정확한 말**이다(화면과 다르다). ⇒ **이 항목이 여는 것은 「언제 볼지」이지 「바꿔야 한다」가 아니다.** 양식을 손댈 다른 사유가 생길 때 함께 본다 | `core/report/` · `docs/report-form-심의보고서.md` |
| **52** | **소스의 `label=` 과 카탈로그 라벨이 어긋나는 조합** (R63/F3 가 재고 **멈췄다**) — `capacity_kw` 는 셋 다에 참인 말(**「용량」**)로 좁혀 풀었으나, *「소스의 `label=` 과 `LABEL_BY_NAME` 이 어긋나는 조합이 0 이다」* 를 재는 시험은 **세울 수 없었다** | ⚠ **느슨한 시험을 세우지 않았다** — 사유와 측정치는 `.orch/R63/result_F3.md`(⚠ **`.gitignore` 안이라 다른 클론에 없다** — 요지는 이 칸과 `status-history.md` R63 블록이 갖는다) §6 ⓔ 에 있다. ⇒ 해소 갈래는 **라벨을 (자원, 이름)별로 넓히는 것**이고 그것은 `LABEL_BY_NAME` 의 자료형과 **빈 라벨을 세는 검사**를 함께 바꾼다. ★ **거부 문면 쪽이 정본**임을 잊지 마라 — 화면 라벨을 그것과 맞춰야 하고 거꾸로가 아니다 | `core/model/parameters.py` · `core/der/*.py` · `tests/model/test_parameters.py` |
| **53** | ✅✅ **2026-09-06 R63 연장 — 닫혔다. 집지 마라**(PR **#26**). 출처 칸이 섰다(`/` **5칸** · `/ui/run` **13칸**). ★★ **그리고 그 칸이 문서의 오류를 드러냈다** — 「사업 기본 설정」을 「시나리오 기본값」으로 가르라던 `docs/decisions-2026-09-05-R63b.md` §1 이 **자기 조건에 걸려 무효가 됐고**(*「정말로 시나리오 값인지 확인하라 — 아니면 이 판정이 틀린 것이다」*) **세 자리 모두 「분석 설정 대장」**으로 합쳤다(분석기간의 소유자는 대장이고 `infra/orm/scenario.py` 가 `analysis_years` 를 **`DV-11` 금지 필드**로 열거한다). ⚠ **대장 39줄 전건 붙임**은 아직 안 했다 — 54번이 닫혔으니 이제 **`AssumptionRow` 에 라벨 필드 하나**로 열린다. 원문: **바꾼 낱말이 화면에 안 실린다 — 출처 칸이 없다** (R63/V2 브라우저 실측 D12). S1 이 `web/render.py` 의 입력값 출처 문면을 「전제 대장」 → **「분석 설정 대장」**, 「사업 기본 설정」 → **「시나리오 기본값」**으로 바꿨는데 **전 화면 일곱에서 두 낱말이 0건**이다 — 대시보드의 입력값 부록 `dl` 이 `item.source` 를 **아예 그리지 않고** `/ui/run` 에는 출처 칸 자체가 없다 | ★ **옛 낱말(「전제 대장」)도 화면에 안 보였다** — 즉 **바꾼 자리가 죽은 데이터였다.** ⚠ S1 이 그 사실을 스스로 적었다(*「이 값은 화면에 인쇄되지 않는다 … 그래도 낱말을 옮긴 이유: 이 값이 부록의 출처 칸으로 그려지는 날 옛 낱말이 되살아나며, 그때는 아무 검사도 빨간불을 내지 않는다」*). ⇒ **해소는 「출처 칸을 화면에 세우는 것」**이고 그것은 **화면 설계**다 — R63 은 요구 밖이라 하지 않았다. ⚠ 세우는 날 **낱말이 이미 맞다**는 것이 이 항목의 값이다 | `web/templates/dashboard.html` · `web/templates/run_result.html` · `web/render.py` |
| **54** | ✅ **2026-09-06 R63 연장 — 설정 화면 몫은 닫혔다**(PR **#25**). 대장 키 **41종 → 0** · 변수명 꼴 토큰 **159 → 0** · 기계가 읽는 자리의 키 **41/41 그대로** · 묶음 14종에 한국어 이름. ⚠⚠ **지시문 전제가 실물과 반대였다** — 대장 41항목 **전건이 이미 `title` 을 갖고 있었고** `AssumptionSet.load_from_yaml` 이 그것을 **읽지 않고 버렸다**. ⇒ 라벨 원천은 **대장 자신**이며 `core/` 에 `LABEL_BY_KEY` 를 만들지 않았다. ⚠ **`/ui/run` 의 인자 이름 43건**은 아직 남아 있다. 원문: **화면 본문의 변수명 301건은 대부분 「데이터 식별자」다 — 그것을 어떻게 볼지 정해야 한다** (R63/V2 실측). 병기 금지 조항이 겨냥한 `옥상 태양광 · azimuth_deg` 꼴은 **0건**이나, 시나리오 선택 목록의 `scenario_subsidy_20` · 설정·부록 표의 **대장 키 열**(`capex.pv.rooftop` 꼴) · 「붙임 — 실행한 시나리오」의 **YAML 원문** · 「자료: `CaseReport.dispatch_hours`」 출처 표기가 남는다 | ⚠ **조항 위반은 아니다** — 사용자 문면은 *「"옥상 태양광 · azimuth_deg" 와 같이 coding 상의 변수명을 **병기**하지 않음」* 이고 그 꼴은 0건이다. ★ **물음은 「한국어 라벨이 없는 대장 항목을 키로만 보여 주는 것이 옳은가」**이며 그것은 **대장에 라벨을 세우는 일**이다(자원 파라미터는 R63 이 세웠으나 **대장 39항목에는 라벨이 없다**). ⇒ **사람이 판정할 것**에 가깝다 · ⛔ 화면에 손으로 적지 마라(카탈로그가 늘 때 낡는다 — R63 이 그 사유로 원천을 세웠다) | `docs/assumptions.yaml` · `web/render_scenarios.py` · `web/templates/settings.html` |
| **55** | ★★ **「읽었다」 이력을 굳히는 자리가 순서에 기댄다** (R63/N3 · PR #27) — `build_case_report()` 는 계산이 끝나고 조립이 시작되기 **직전 한 줄**에서 읽힘 이력을 굳혀 `_overrides()` 에 인자로 넘긴다. 그래야 하는 이유는 `_appendix()` 가 대장 **전 항목**에 `get()` 을 걸기 때문이다(실측 — 호출 20~58번) | ⚠⚠ **리포트 쪽 읽기가 그 줄 앞으로 오면 그 키가 「읽었다」로 서서 표시가 조용히 죽는다** — 지금 그것을 붙드는 검사가 없다. ★ **뒤로 밀리는 쪽은 붙든다**(`tax.vat_rate` 행이 「읽었다」가 되어 빨간불). ⇒ 필요한 것은 **굳힌 뒤의 `get()` 을 이력에 못 들어가게 하는 장치**이며, `keys_read()` 가 이미 사본을 주므로 남은 것은 **굳힌 시점 이후의 읽기를 세지 않겠다는 선언**이다 | `core/report/case_report.py` · `core/assumption/provider.py` |

### 착수 전에 — 절차는 이 파일의 다른 절이 갖는다

- 인터프리터 · 게이트 명령 · 커밋 전 PATH — **이 파일의 「게이트」 절**
- 조항을 건드릴 때 함께 움직이는 자리 — **이 파일의 「파급 지점」 절**
- 변이 하네스 `.orch/mutate.py` — **한 번에 하나씩, 대상을 좁혀** 돌린다
  (여럿 이어 돌려 시간초과로 죽으면 `finally` 가 건너뛰어져 실물 소스에 변이가
  남는다 — R34 가 실제로 밟았다). ⚠ 옛·새 문면을 **`printf '%b'`** 로 쓴다 —
  `'%s'` 는 `\n` 을 그대로 두어 여러 줄 변이가 「대상 문면이 없다」(rc=2)가 된다
  (R35 ② 가 실제로 밟았다)
- ⚠ **검사를 새로 쓸 때 먼저 물을 것**: *이 단언에 등장하는 값 중 몇 개를 검사
  대상이 스스로 정하는가?* 전부라면 동어반복이다 — R35 ① 의 초록불 변이가 그것이었다
  (이 파일의 「함정」 절)
- ⚠ **수를 싣는 표에는 「그 수가 어느 조건에서 나왔는가」도 실어야 한다** —
  R35 ② 의 초록불 변이(두 끝의 값을 서로 바꿔 싣기)는 **거리만 보는 검사 일곱을
  전건 통과했다.** 값과 조건의 **짝**을 다른 층에서 검산할 것

---

## 산출물 현황 — **R60 종료 실측** (2026-09-05 · 표의 수는 전부 대조 명령을 돌려 얻었다)

> ★ **이 절은 「판본이 무엇인가」만 적는다.** 종전에는 문서 11종 · 도구 · CI 를
> 표로 늘어놓아 220줄이었고, 그래서 **갱신되지 않아 두 판본 뒤처졌다.**
> 도구 목록은 `scripts/` 가 정본이고 CI 구성은 `.github/workflows/` 가 정본이다 —
> **여기 옮겨 적지 않는다.**

| 산출물 | 판본 | 대조 명령 |
|---|---|---|
| `rslt/spec-분산특구-경제성평가.md` | **v0.28** (수용기준 **331**). ⚠ **R59 에 조항 셋이 늘었다** — `NFR-105` 의 Measurement 가 번호 목록이라 조항 ID 가 아니었고, 그것을 `- **M1**`~`- **M3**` 로 세웠다(문면은 그대로). 종전 이 칸은 **v0.20 · 321** 에서 멈춰 있었다 — **다섯 판 뒤처졌다** | `grep -m1 '^version:' rslt/spec-*.md` · `python scripts/gen_traceability.py` |
|  | ⚠ **이 칸은 한때 두 판 뒤처져 있었다**(R37 의 v0.17 · R41 의 v0.18). 「길이가 낡음을 만든다」의 그 형태이며, 그것을 재는 검사를 R43-J 가 세웠다 — `scripts/check_status_round_blocks.py` | |
| `rslt/task-분산특구-경제성평가.md` | **v2.11** (상위 17 · 하위 **159** — R59 가 `2.17`(커밋 순서 게이트)을 신설했다) | 같음 |
| `docs/traceability.md` | 자동 생성 — 요구사항 **105** / 수용기준 **331** / 자동 **299** / 수동 8 / 스텁 4 · **Must-have 미매핑 10**(Phase 1 은 **0** — ⚠ 그 0 은 마커가 만든 수다 · 착수 순서 **39번**) · ✅ **차단 미수행 0건**(2026-09-05 R61 이 `MC-1` 을 닫았다). ⚠ R59 도중 한때 11(Phase 1 이 2)이었고 **게이트 ③을 구현해 되돌렸다.** 9 → 10 은 `FR-204-AC2` 마커를 뗀 몫이다 | `python scripts/gen_traceability.py` (rc=1 이 정상) |
| `docs/assumptions.yaml` | `Q-1`~**`Q-18`** + 확정. 항목 **41건**(신뢰도 `가정` 37 · `확정` 3 · `추정` 1 · `track` 은 `assume` 25 · `default0` 11 · `fixed` 3 · **`blocked` 2**). ⚠⚠ **이 칸은 「29건」에서 멈춰 있었다** — R60 이 직접 세어 고쳤다(`R60/WP-3` 이 `benefit.pool_compensation_price` 하나를 더했으므로 종전 수는 그 전부터 낡아 있었다). ⚠ **`blocked` 둘(`oracle.prior_demo_assumptions`·`oracle.legacy_excel_model`)은 채우면 자기충족 검증이 된다 — 사용자 판정 §3 은 그 갈래를 뒤집지 않는다.** R43 이 둘을 더했다 — `capex.pv.inverter_share`(`Q-18` · WP-F) · `tariff.hv_single_contract.demand_charge`(`Q-6` · WP-H). ⚠ 리포트 붙임 1 이 「전제 **27건**」이라 인쇄하는 것과 다른 수다 — 그 표는 **설계 변수 둘**(`pv_capacity_kw`·`ess_capacity_kwh`)을 따로 싣는다 | `python scripts/check_assumptions.py` |
| ★ `fixtures/profiles/representative-day.yaml` | **R60/WP-4 가 계절 넷을 「가정」으로 채웠다** — 봄 92 · 여름 92 · 가을 91 · 겨울 90(합 365 · 부하와 발전이 **같은 달력**). 몫은 부하 0.23/0.28/0.22/0.27 · 발전 0.27/0.30/0.26/0.17(각 합 1). ⚠⚠ **크기는 전부 임의**이며 방향만 근거가 있다 — `derivation_method` 가 그것을 적는다. ⚠⚠⚠ **계절 몫이 운전을 가르지는 못한다**(착수 순서 36번) | `pytest tests/casegrid/test_seasonal_axis.py tests/casegrid/test_seasonal_assumptions.py` |
| `docs/manual-checks.yaml` | ✅✅ **`MC-1` 은 `수행`·`충족`(2026-09-05) — 「차단 미수행」이 1 → 0 건이 됐고 Phase 1 DoD 5 가 풀렸다.** 기록은 `docs/evidence/MC-1-수행기록-2026-09-05.md`. ⚠ **표본의 소속·경력은 미확인**이고 그 사실을 기록이 함께 적는다. `MC-2`~`MC-8` **7건은 미수행**(전부 웹 UI 대상 · `blocking_dod: false`). ⚠ 종전 이 칸의 **「일정만 잠정 기입(2026-09-11)」은 거짓이었다** — 실제 yaml 은 `2026-08-15`·`MR.test`(실재하지 않는 이름)였고 **네 곳이 어긋나 있었다** | `python scripts/gen_traceability.py` · `docs/decisions-2026-09-05-R61.md` §1 |
| `docs/exclusion-rules.yaml` | 배타 규칙 **7건** (R32 가 집합 PPA 둘 추가) | 계약 테스트가 건수를 못 박는다 |
| `docs/accepted-vulnerabilities.yaml` | **R42 신설** — 의존성 취약점 판정 대장. 지금 **1건**(`PYSEC-2026-3447` · 올리지 않는다). ⚠ CI 만 판정한다(로컬은 `audit.txt` 가 없어 rc=2) | `python scripts/check_dependency_audit.py` |
| `docs/evidence/MC-1-검토용-리포트-*.md` | **사람이 보는 정본은 `-2026-09-02.md`** — ⚠⚠ **종전 이 칸이 `-2026-08-30.md` 라 적어 같은 파일 머리의 ★ 문단(`-2026-09-02.md`)과 어긋나 있었다**(R61 실측). ⚠⚠⚠ **그 정본이 인쇄하는 무보조 `npv` 는 −11,537,129원이고 지금 축은 −11,552,270원이다** — R60/WP-4 가 −15,141원 옮겼고 리포트를 다시 뽑지 않았다. **결론(셋 다 미회수 · `payback` 셋 다 `.inf`)은 바뀌지 않았으므로 `MC-1` 재수행 사유는 아니다**(정본 `docs/decisions-2026-09-05-R61.md` §1). 다음에 뽑을 때 검토자에게 새 판을 준다. ⚠ **`-08-29` 판을 덮지 않았다** — 문의사항·답변서·판정요구 셋이 그 판을 이름으로 가리키므로 덮으면 대조할 원본을 잃는다. 옛 `-08-15`·`-08-17` 도 지우지 않았다 | `python -m app.run.report_cli --scenario scenario_unsubsidized --out <경로>` |
| `docs/evidence/문의사항-·답변-·판정요구-*.md` | **R42·R43 이 세운 사람용 문서 셋** — 문의사항(가 9 · 나 9 · 다 11) · 답변서(가-3②·6·7·8 실측) · 이중계상 판정요구(甲·乙·丙 미결) | 전부 `-2026-08-29.md` · 대상 자료는 리포트 `-08-29` 판 |
| `docs/decisions-*.md` **10건** | 결정·판정 **정본**. ⚠ **`-2026-09-04-R59.md` 만 사용자 판정이 아니라 오케스트레이터 판정이다**(그 문서 머리가 그렇게 적는다) | `ls docs/decisions-*.md` |
| `docs/clause-recheck-2026-08-14.md` | `.orch/` 에서 옮긴 유효 판정 | — |
| `docs/{domain-rules,evidence-standard}.md` | 정본 **사본**(바이트 동일) · 소유자는 `docs/source-rules.lock` | `python scripts/check_source_rules.py --require-vault` |

---

## 게이트 — 라운드를 닫기 전에 돌리는 것

> ★ **인터프리터를 먼저 확인한다.** 저장소 `.venv` 와 `C:\Python313` 에는 `pytest`
> 조차 없다. 의존성이 있는 것은 `~/miniconda3/python.exe` 이고 `lint-imports` 는
> `~/miniconda3/Scripts/` 에 있다. **`PYTHONUTF8=1` 을 켠다** — 켜지 않으면 서브프로세스
> 출력을 cp949 로 디코드하다 6건이 로컬에서만 빨간불이 된다(R24 실측).

```bash
PY=~/miniconda3/python.exe
PYTHONUTF8=1 $PY -m pytest -q                                   # rc=0. 2분 초과 — 배경 실행
PYTHONUTF8=1 $PY -m ruff check core tests scripts app infra web # --fix 는 마지막에
PYTHONUTF8=1 $PY -m mypy                                        # 127파일 (R53 실측)
PYTHONUTF8=1 ~/miniconda3/Scripts/lint-imports.exe              # 4 kept 0 broken
#   ⚠ 이것도 PYTHONUTF8 이 필요하다 (2026-08-17 실측). 켜지 않으면
#   `'cp949' codec can't decode byte 0xe2` 로 rc=1 이 나고, 그것은 계약 위반이
#   아니라 **자기 배너를 못 읽은 것**이다 — 위 ★ 문단이 pytest 에만 해당하는
#   줄로 읽혀 이 줄만 맨몸으로 남아 있었다
PYTHONUTF8=1 $PY scripts/gen_traceability.py    # rc=1 이 정상(미매핑 10). ★ 부수효과로 파일을 다시 만든다
git diff --stat docs/traceability.md            # ← 커밋 전 이것이 반영돼야 CI 가 초록불이다
PYTHONUTF8=1 $PY scripts/check_task_mapping.py  # rc=0 이어야 한다 — CI 차단이다
for g in check_marker_substance check_hardcoded_params check_source_rules          check_partition_assignment check_disclosure check_assumptions          check_precommit_installed check_unread_extension_points          check_docstring_references check_status_round_blocks; do PYTHONUTF8=1 $PY scripts/$g.py; done
PYTHONUTF8=1 $PY scripts/check_file_size.py --code-strict
for f in scripts/negtest_*.py; do PYTHONUTF8=1 $PY "$f"; done   # 16종(R59 에 +1). 배경 실행
#   ⚠ 수를 박지 말고 세라 — `ls scripts/negtest_*.py | wc -l`
PYTHONUTF8=1 ~/miniconda3/envs/r31py311/python.exe -m pytest -q # CI 는 3.11 이다
```

**커밋 뒤에만 되는 것 셋**: `check_coverage_inputs` · `check_test_accompaniment` ·
★ `check_commit_order`(R59 신설)는 기준 ref 를 요구한다 — `--base HEAD~2`.
커밋 전에는 변경 0건으로 보여 rc=2 이며 **그것이 옳다**(0건을 통과로 읽지 않는다).

> ### ★ `check_commit_order` 의 종료 코드는 다른 게이트와 다르다 (R59 신설 · `NFR-105-M3`)
>
> 조항이 *「위반 시 **경고**(차단 아님, 스쿼시 병합 등 예외 존재)」* 라고 못 박으므로
> **위반이 있어도 `rc=0`** 이다. **`rc=2` 는 「판정 불가」**(기준 ref 없음 · 범위에
> 커밋이 하나뿐)이며 **그것을 통과로 읽지 않는다.**
> ⚠ 그래서 **`rc` 만 보면 위반을 못 본다 — 출력의 `::warning::` 줄을 읽어라.**
> ⚠ 감지 능력은 `scripts/negtest_commit_order.py` 가 잰다(음성 1 + 양성 2 + 경계 2).
> ⚠ **CI 에서는 PR 일 때만 판정된다** — 그래서 **라운드를 PR 로 닫는다**(아래
> ★★★ 절 · 2026-09-04 사용자 판정). 직접 푸시 경로에서는 여전히 「미수행」이다.
⚠ **`check_test_accompaniment` 는 라운드를 닫기 전에 반드시 한 번 돌린다** —
R42 가 여기서 한 건(`core/model/parameters.py`)을 잡혀 커밋을 하나 더 했고,
**R43 도 두 건(`narrative.py`·`unreflected.py`)을 잡혀 넷째 커밋을 했다.**
라운드 전체를 볼 때는 `--base <라운드 시작 ref>` 가 맞다(R43 은 `c4ce0e5`).
★ **R53 은 `--base origin/main`** 으로 돌렸다 — 그것이 **CI 와 같은 기준**이다.

> ### ★★★ **라운드는 PR 로 닫는다 — 직접 푸시로 닫지 않는다** (2026-09-04 사용자 판정 · R60 실측)
>
> 게이트 셋(`NFR-105-M1` 변경분 커버리지 · `M2` 테스트 동반 · `M3` 커밋 순서)은
> `.github/workflows/tests.yml` 에서 **`if: github.event_name == 'pull_request'`**
> 로 걸려 있다. 이 저장소는 R59 까지 라운드를 **`git push origin HEAD:main`** 으로
> 닫아 왔으므로 **셋 다 한 번도 판정된 적이 없었다**(R53 실측 · 착수 순서 32번).
> 워크플로는 조용히 넘기지 않고 *「통과가 아니라 **미수행** 입니다」* 를 로그에
> 적었지만(§13.0.1 ④) **아무도 그 줄을 읽지 않으면 초록불과 구별되지 않았다.**
>
> **사용자가 PR 경로를 채택했다**(`docs/decisions-2026-09-04-R59b.md` §2).
> 그래서 이제 **닫는 절차가 이것이다** — 순서를 지킨다:
>
> ```bash
> git checkout -b r<N>-<무엇>            # main 에서 딴다. main 을 체크아웃하지 말 것(아래 ⚠)
> # ① 시험 커밋 → ② 구현 커밋 순으로 **나눠** 커밋한다 (그것이 M3 이 재는 것이다)
> git push -u origin HEAD
> gh pr create --base main --title "<제목>" --body "<본문>"
> gh pr checks --watch                   # ★ 게이트 셋이 **돌았는지**를 여기서 본다
> gh pr merge --merge --delete-branch    # 스쿼시로 합치지 마라(아래 ⚠⚠)
> ```
>
> ⚠ **권한은 R60 이 실측했다** — `gh pr create`·`gh pr merge` 가 이 토큰으로
> 실제로 됐고 `main` 에는 브랜치 보호가 없다(`gh api` 가 *Branch not protected*).
> ⚠⚠ **`--squash` 로 합치지 마라.** 스쿼시는 커밋 둘을 하나로 눌러 **`M3` 이
> 재려는 순서를 이력에서 지운다** — 조항이 스쿼시를 예외로 인정하는 것은
> *「그때는 못 잰다」* 는 뜻이고, 우리가 스스로 못 재게 만들 이유는 없다.
> ⚠ **PR 하나에 커밋이 하나뿐이면 `M3` 은 `rc=2`(판정 불가)다** — 통과가 아니다.
> ⚠⚠ **`--admin` 으로 밀어붙이지 마라.** 게이트가 빨간불이면 고치는 것이 답이다.
> ⚠ **로컬 `main` 참조는 낡아 있을 수 있다**(R60 착수 시 `f2f1032`). 브랜치는
> **`origin/main`** 에서 딴다 — `git fetch origin && git checkout -b <이름> origin/main`.
>
> ⚠⚠ **그래도 닫기 전에 손으로 한 번 돌린다.** CI 가 판정하게 됐어도 **PR 을
> 연 뒤에 잡히면 커밋을 더 얹어야** 하고, 이 저장소는 그 자리에서 **세 번**
> 커밋을 추가했다 — R42(`core/model/parameters.py`) · R43(`narrative.py`·
> `unreflected.py`) · R53(`core/report/perspective_section.py`).
>
> ⚠ **이 검사는 「커밋 경계」에서만 판정된다** — `scripts/_gitdiff.py::changed_files()`
> 가 `git diff --name-status base...HEAD` 로 **커밋된 트리끼리만** 비교하므로
> 작업 트리·스테이징의 새 검사 파일을 **보지 못한다.** 커밋 전에 rc=0 을 만들
> 방법이 없으니, 판정 함수(`check()`)를 직접 불러 미리 재고 커밋한 뒤 다시 돌린다.

**CI 에서만 되는 것 하나**(R42 신설): `check_dependency_audit` 는 `pip-audit` 의
`audit.txt` 를 읽는다. 로컬에는 그 파일이 없어 **rc=2** 이며 그것이 옳다 —
「스캔이 돌지 않았다」와 「깨끗하다」를 같게 읽지 않는다. 손으로 재보려면
`pip-audit --progress-spinner off | tee audit.txt` 뒤에 돌린다.

> ### ⚠⚠ **`pytest ... | tail` 의 종료 코드는 `tail` 의 것이다** (2026-08-15 실측)
>
> `pytest -q 2>&1 | tail -6` 로 돌리면 셸이 돌려주는 rc 는 **파이프 마지막
> 명령의 것**이다. 실제로 이 라운드에서 **실패 1건이 있는 실행이 `exit 0` 으로
> 보고됐다**(`test_hourly_table_carries_every_step_of_the_run` — 붙임 7 이 표
> 둘이 되며 행 수가 24→48). 출력에 `FAILED` 줄이 보여 알아챘을 뿐이다.
>
> **`> /tmp/x.log 2>&1; echo "rc=$?"` 로 돌린다.** 게이트를 파이프에 물리지
> 않는 것이 규칙이며, 이 저장소가 이미 두 번 만난 「게이트가 조용히 초록불」
> 형태다(§16.5 CI `|| true` · 6.7 문자열 포함 검사).

**마무리에 빠뜨리기 쉬운 것**
- **음성 스위트가 실물 소스를 변이시킨다** — 도는 중 `git status` 에 `# NEGCHECK` 가
  보이는 것은 정상이고, **중단시키면 실제로 남는다.** 끝난 뒤 다시 볼 것
- 빈 디렉터리 둘이 남는다 — `scripts/_tmp_negtrace` · `scripts/_tmp_neg_marker` (`rmdir`)
- 커밋 메시지는 `git commit -F - <<'EOF'` 로 쓴다(`@'...'@` 는 `@` 가 메시지에 들어간다)
- 푸시 뒤 `gh run list --limit 4` — 워크플로는 `tests` 와 `source-rules` 둘이고
  **둘 다 `success` 여야 끝난 것**이다(`source-rules` 는 경로 필터라 안 돌 수 있다)

## 미해결

> ⚠ **이 표는 아직 살아 있는 항목만 갖는다.** 조치가 끝난 행(조치 시점 `✔`)은
> `status-history.md` 의 **「R45 가 내린 것 — 「미해결」 표의 해소(취소선) 행」** 절에 있다.
> 취소선이 그어져 있는데도 여기 남은 행 셋은 **앞부분만 해소되고 잔여가 살아 있는 것**이다.

| 구분 | 항목 | 영향 | 조치 시점 |
|---|---|---|---|
| **상한** | ★ **`core/der/ess.py` 코드 줄 수가 499/500 이다**(R59/WP-1 이 `DISPATCH_RULE` 선언 한 줄을 더해 498 → 499). `check_file_size --code-strict` 는 초록불이다 | **여유가 한 줄**이다 — ESS 에 코드 두 줄이 들어가는 다음 WP 는 그 게이트에서 멈춘다. ⚠ **상한을 올리는 것은 §16.5 spec 개정 절차**이고, 이 저장소는 *「검사를 통과시키려고 상한을 올리는 것」* 을 반복해 경계해 왔다 — **쪼개는 쪽이 정본이다** | 파일을 쪼갤 때 |
| **범위** | ~~**★★ 리포트가 조항이 요구하는 표시 여섯**~~ → ~~**다섯**~~ → **넷** | **R48 이 가장 무거운 `FR-404-AC1` 을 닫았다** — `policy_warnings()` 가 `DER` 계약에 오르고(§16.2) 리포트가 상단에 인쇄한다. ✅ **R57/WP-8 이 `FR-602-AC2` 를 닫았다** — 붙임 1 안에 「기준 전제 대비 변경 항목」 표가 서고 `CaseReport` 가 `overridden_items()` 를 읽는다(구획 부채 대장 둘에서 그 짝을 지웠다). ★ **둘 다 「문면을 세워서」 닫혔다** — 시험을 더해서는 닫히지 않는다. **남은 넷은 그대로 호출자 0곳**이다: `FR-106-AC5` · `FR-603-AC2` · `FR-705-AC1` · `FR-905-AC8` | 사람 판정 (`status-human.md` 소유) |
| **계약** | ~~**붙임 1 이 오버라이드를 반영하지 않는다**~~ — **동작은 R48 이 고쳤다** | 값을 `provider.get()` 에서 읽는다(`30959b7`). ⚠ **남은 것은 「덮어썼다」는 표시를 어느 조항이 소유하는가**이며 그것은 여전히 사람 판정이다. 재료(`provider.overridden_items()`)는 이미 있다 | 어느 조항이 소유하는지 사람 판정 |
| **도구** | **전건 `pytest` 가 `tests/ci/_tmp_pytest/` 에 수백 MB 를 쌓는다** | `tests/ci/conftest.py` 의 `tmp_path` 픽스처가 `shutil.rmtree(..., ignore_errors=True)` 로 지우는데 **윈도우의 읽기전용 `.git` 파일에 걸려 조용히 실패**한다 — 한 번 돌릴 때마다 `case_*` 수백 개(R55 실측 436개·16MB)가 **누적**된다. R55 가 `.gitignore` 로 **커밋되는 것은 막았으나** 디스크에는 계속 쌓인다 | `rmtree` 에 읽기전용 해제 핸들러(`onexc`)를 달지, 아니면 pytest 기본 `tmp_path` 로 되돌릴지 판정 필요 |
| **도구** | **부채 대장이 둘이다** | 같은 (조항,구획) 사실을 `scripts/check_clause_partition.py::KNOWN_GAPS`(9건)와 `tests/contract/test_clause_evidence_nets.py::KNOWN_PARTITION_DEBT`(7건)가 따로 든다. **한 곳만 고치면 pytest 가 빨간불이다.** | 하나로 모을지 사람 판정 |
| **범위** | **자원 구성이 케이스 축이 아니다 — 0kWh 미설치안을 계산할 수 없다 (R43-I 실측)** | 러너가 PV+ESS 를 **한 벌로 고정**해 세우고 흔드는 것은 **용량뿐**이다. `ess_capacity_kwh=0` 은 `ValidationError` 로 거부된다 — *어떤 설비를 넣고 빼는가* 가 축이 아니다. 그래서 「저장장치를 빼면」이라는 검토자의 물음에 **극한값(0.001kWh)으로만** 답할 수 있었다. ⚠ 고정 O&M 이 용량에 비례하지 않아(`ESS_FIXED_OM_WON_PER_YEAR` 상수) **용량→0 극한이 미설치가 아니다**(결손 46.29% vs 14.31%) | 자원 구성을 케이스 축으로 올리는 배선 (「다음에 집을 것」 **14번**) |
| 입력 | **Q-8** V2G 계량·정산 제도 존부 | 미확인이므로 편익 **전액 0**으로 등재(비활성). 충전기 비용은 계상되므로 **현 결과는 V2G에 보수적**이다 | 16.1b 요청 대상 |
| 입력 | **Q-13** 호스팅 운영 주체·예산 | 3.6·17.7 배포 형태. 로컬 컨테이너(2.13)는 무관. 가정값 월 2만원(하단 0) | 3.6 착수 전 |
| **검증** | **Q-4 미확보 → 17.4·SG-4 판정 유예** — ★ **R52 가 성격을 바꿨다** | **가정할 수 없다** — 정박점을 우리가 만들면 자기충족 검증이 된다. 통과로도 실패로도 세지 않고 미판정으로 남긴다. ⚠⚠ **2026-09-02 사용자 판정(§2)으로 이것은 「회신 대기」가 아니라 「대조군 부재」다** — *「계산값 대조는 현 시점에서 불가함(처음 하는 사업이라서 계산 결과가 없음)」*. **기다려서 오는 것이 아니다.** ★ 대신 **검증 보고서**(`--kind verification`)가 단계별 전제·계산·인계·수식을 낸다 | **독립 대조군이 생겨야** 해소 (기다림으로는 안 됨) |
| **검증** | **Q-5 미확보 → §13.3 1차 대조군 유예** — ★ **R52 가 성격을 바꿨다** | 동상. 16.5의 6종은 해석해(순위 1)로 대체하되 *실무 전제와의 정합*은 대체되지 않는다. ⚠⚠ **위와 같은 이유로 「대조군 부재」다** | **셀 수식이 열린 엑셀 1건이 독립으로 존재해야** 해소 |
| 검증 | **가정값 위의 골든 3종** | 16.4 기준값이 대장 가정에 묶인다 — 통과는 "어제와 같다"이지 "맞다"가 아니다. 가정 갱신 시 재산출 필요 | 16.4 착수 시 |
| **범위** | **결합 집합 넷 중 둘만 수준표에 있다 (「1차 의견」 ① 에서 드러남)** | `equipment_cost_bundle` 은 **PV·ESS·히트펌프·시공비 넷**인데 `build_level_map()` 이 대장에서 만드는 것은 `pv_unit_cost`·`ess_unit_cost` 둘뿐이다(`_LEDGER_VARS`). 그래서 리포트의 결합 표도 **두 열**이며, 그것이 지금 평가 대상(PV+ESS)과는 맞다 — 히트펌프가 구성에 없고 시공비는 별도 행이 아니다. **다만 「설비단가를 한 축으로 흔들었다」는 진술은 넷 중 둘에만 성립한다.** 히트펌프·시공비가 구성에 들어오면 대장 항목과 `_LEDGER_VARS` 를 함께 늘려야 하며, 늘리지 않으면 결합 표가 조용히 두 열로 남는다(리포트가 선언과 수준표의 **교집합**만 그리므로 빨간불이 나지 않는다) | 히트펌프·시공비가 구성에 들어올 때 |
| **계약** | **`tariff_escalation` 이 케이스 그리드 축인데 파이프라인이 읽지 않는다 (R33 발견)** | `quick_preset_grid` 이 이 변수를 3수준으로 흔들지만 `run_single_case_e2e` 는 `pv_unit_cost`·`ess_unit_cost`·`discount_rate` 셋만 읽는다. **27 케이스는 실질 9 케이스의 3회 반복**이고, 17.2 DoD 2 가 그리는 실현가능영역 지도는 `y=tariff_escalation` 축이 **구조적으로 평평하다** — 「이 축은 영향이 없다」로 읽히지만 실제로는 계산에 들어가지 않았다. 요금 인상률을 편익 시계열에 태우는 것은 WP-3(요금 엔진) 몫이며 `DispatchContext` 의 가격 신호가 그 자리다. **리포트는 이것을 `unread_by_pipeline` 로 드러낸다** — 고칠 때까지 검토자가 틀린 결론을 배워 가지 않게 한다 | 5.0(요금 엔진) 착수 시 |
| 기술 | **TU-2** Litestream vs Turso | 3.6 프로토타입 후 결정 | Wave 1 |
| 기술 | **TU-6** 누진·TOU 혼재 모델링 — **설계 방침 확정 (08-09), 약관 확인은 남음** | **한 계량점에 누진과 TOU를 동시에 적용하는 요금제는 실재하지 않는다.** 「혼재」는 한 단지에 **계량점이 여럿**이라는 뜻이다 — 가구부(주택용 N개, 각각 누진) / 공용부(일반용 고압 1개, TOU) / 거래분(직접거래). 따라서 요금 엔진은 **계량점 단위 독립 계산 + 합산**으로 짓고, `FR-501-AC6`(동시 적용)은 «한 계량점에 두 요금제»가 아니라 «여러 계량점의 합산»으로 읽는다. 이렇게 두면 5.3과 5.4가 부딪치지 않고 요금 엔진을 두 번 짓지 않는다. **약관 검토는 「계량점 구성이 실제로 이러한가」를 확인하는 것으로 좁혀진다** — 다르면 여기로 돌아온다 | 5.3 진행 중 · 약관 확인 시 재검토 |
| 기술 | **TU-7** IRR·할인회수기간 셀 수식 한계 | 13.5 실현 범위. 대응책 확정 — *값 + 산식 주석 병기* | 13.5 착수 시 |
| 검증 | **16.5 엑셀 대조에 MIRR·B/C 누락** | 현재 4종(NPV·IRR·회수기간·융자 상환). 10.3이 대조하겠다는 5종 중 2종의 기대값 출처가 없다 | 16.5 착수 시 |
| 범위 | **`FR-611-AC3` ↔ `FR-704` 관점 어휘 불일치** | `FR-704`는 사업자/참여 주민/정부인데 `FR-611`은 사업자·주민(병합)/사회(FR-704에 없음)/정부다. 중립 키(`.OWNER`·`.SOCIAL`·`.GOV`)로 어긋남을 고착시키지 않았으나 **해소하려면 `FR-704` 개정이 선행**된다 | 10.6 착수 전 |
| 판단 | **8.3을 배타 유형별로 쪼갤 것인가** | 한 작업이 `AC2.A`의 차단 100%(음성)와 `AC2.B`~`.D`의 오탐 0(양성)을 동시에 진다. 검사 방향이 반대이고 4행이 각각 `NFR-102`·`FR-404`·`FR-704`·`FR-504`로 위임되어 네 구획에 걸린다 | 8.0 착수 시 |
| 계약 | **자원의 잔여 요금 인자 3건 — 이관 대기** | 실측: `EV_V2G.avoided_price_won_per_kwh` · `HeatPump.price_profile_won_per_kwh` · `HeatPump.elec_price_won_per_kwh`. **`avoided_price` 는 운전이 아니라 편익 화폐화이므로 ctx 가 아니라 WP-4 로 간다** — 그 판단은 8.0 이 편익 소유권을 확정할 때 내려진다. 지금 0건을 요구하면 `RC-HP`·`RC-EV` 검증 케이스를 함께 고쳐야 한다. `KNOWN_TARIFF_DEBT` 가 부채를 고정하며 **늘면 즉시 빨개진다** | 8.0 착수 시 |
| ~~문서~~ | ~~**공통 비용 5종의 마커 부착이 갈렸다**~~ **R31 해소 — `FR-101-AC5` 신설** | `FR-104`는 열화·수명·교체·잔존가치 조항이라 **C-1 CAPEX·C-2 고정 O&M·C-3 변동 O&M에 대응하는 AC가 없다.** 6개 구획이 각자 `FR-101-AC2` 등으로 달았다. spec에 조항을 신설하거나 6.0.S1의 인용을 정정해야 한다. **계약 개정에 포함하지 않았다** — spec 개정이므로 §16.5 절차가 따로 필요하다 | spec 다음 개정 시 |
| **계약** | **대장을 읽는 경로 — 계약 자리는 섰고 구현·주입은 남았다** | **08-09 (1.12 ⑦)** — `core/contracts/assumptions.py` 의 `AssumptionProvider` 가 읽기 경계를 고정했다. **없으면 `MissingAssumption` 으로 멈추며 기본값으로 메우지 않는다.** 남은 것 둘 — ⓐ 대장을 실제로 읽는 구현(WP-2, 작업 4.4) ⓑ **자원 생성 시 주입 강제**(WP-16, 작업 7.2). ⓑ 가 없으면 `vat_rate=0.0` 이 여전히 「주입되지 않음」인 채 통과한다 | WP-2 · WP-16 |
| ~~문서~~ | ~~**`SC-3`·`SC-5`·`SC-8` 이 추적표 밖이다**~~ **v0.11 해소 — 이 행이 낡아 있었다 (R31 실측)** | SC 표의 행은 조항 ID 를 갖지만 수용기준 파서가 읽는 형식(`- **AC1**`)이 아니다. `@pytest.mark.req("SC-8")` 를 달자 생성기가 «실재하지 않는 인용» 으로 잡았다(실측). **Must-have 급 요구인데 17.9 미매핑 집계에 들어오지 않으므로, «전건 매핑» 이 성립해도 보안 조항은 검증되지 않은 채로 남는다.** SC 표를 수용기준 형식으로 승격할지, 별도 축으로 셀지 판단 필요 | spec 다음 개정 시 |
| **도구** | **NFR-202 판정 사각지대 — ★ R43-H 가 사각지대의 정체를 바로잡았다** | \|값\| < 1,000 은 판정하지 않는다(4·5·15·30 은 인덱스·시간과 구별되지 않는다). `tariff.hv_single_contract.avg`(150원/kWh)·`fee.direct_trade_support`(5원/kWh)가 여기 있고, **요금 단가는 소스에 새기 가장 쉬운 값**이다. ★★ **가정이 아니라 실측이 됐다** — 잉여 판매단가 `120.0` 이 `e2e_runner` 안에 있었고 **검사 전건이 초록불이었다**(R35 가 대장으로 옮겼다). 즉 이 사각지대는 *이론상 위험*이 아니라 **이미 한 번 통과시킨 구멍**이다. 남은 위험은 같은 크기의 단가가 또 들어오는 것이며, 지금 그것을 막는 것은 검사가 아니라 **「기본값을 두지 않는다」 규약**뿐이다(수준표에서 빼면 러너가 거부한다). 요금 엔진(WP-3) 착수 시 다른 판정 수단이 필요하다. ★★ **R43-H 가 「문턱」이라는 종전 진단을 실물로 뒤집었다** — `check_hardcoded_params` 가 재는 것은 *「**대장의 값**이 소스에 복제되었는가」* 이므로 **대장에 없는 값은 대조할 상대가 아예 없다**(그 검사가 자기 독스트링에 그 사각지대를 이미 적어 두었다). 8,320원/kW·월이 결론의 21%를 정하면서 여섯 라운드를 지난 이유가 그것이며, 앞선 두 건(`120` · `0.15`)과는 **사유가 다르다**. 즉 문턱을 낮춰도 안 잡힌다 — **대장에 올리는 것**만이 잡는 길이다 | 5.0 착수 시 |
| 품질 | **총 줄 수 초과 6건은 남는다** (경고) | 상한값·판정 기준을 바꾸지 않았기 때문이다 — NFR-206 문면은 「500줄」이고 코드 기준으로 재정의하려면 **spec 개정(§16.5 절차)** 이 필요하다. 검사를 통과시키려고 규칙을 고치는 것은 이 저장소가 반복해서 경계해 온 유형이다. 조항을 개정할지, 파일을 쪼갤지, 경고로 둘지는 **다음 spec 개정 때 판단** | spec 다음 개정 시 |
| **범위** | **17.1 DoD 1 ↔ FR-202 Phase 어긋남** | 17.1(2변형 **비교** 산출, SG-1 전반부)은 Phase 1인데 비교 기능 FR-202는 부록 A.1이 Phase 2에 배정. 7.5 템플릿 2변형을 각각 실행해 나란히 놓는 것으로 충족되는지, 아니면 FR-202-AC1·AC2를 Phase 1로 당길지 판단 필요 | 17.1 착수 전 |
| **범위** | **분석기간을 어느 층이 소유하는가 — ✔ R30 이 절반을 닫았다** | **케이스 실행 경로는 배선됐다** — `core/casegrid/e2e_runner.py` 가 자원을 세운 직후 `check_analysis_period()` 를 부르고, `horizon_years` 인자가 검사와 프로포마 계산 **양쪽**에 쓰인다. 아래는 R24 시점 문면이며 **「부르는 코드가 0곳」은 이제 틀렸다**. 남은 것은 *사용자가 고르는* 분석기간의 소유자다. R24 가 `core/cba/proforma.py::check_analysis_period()` 로 상한(최장 자원 수명 × 2)을 **던지는 코드는 놓았으나 부르는 코드가 0곳**이었다. 값이 함수 인자(`analysis_end_year`)로만 떠다니고, 자연스러운 자리인 `Scenario` 는 **`DV-11` 이 명시적으로 금지**한다(규제 프로파일·요금표·할인율·분석기간 필드 보유 불가). 즉 「배선을 잊었다」가 아니라 **놓을 자리가 계약상 비어 있다.** 후보는 `AssumptionSet`(전제 층) 또는 케이스그리드·앱 조합 층이며, 고르면 그 층이 `check_analysis_period` 를 부르는 것으로 닫힌다 | Phase 1 인수 전 |
| **범위** | **`VPP` 가 계약에 앉는가 — Phase 2 착수 전 판단** | 자원이 다른 자원을 품으면 ⓐ `dispatch()` 가 하위 자원을 순회해야 하고 ⓑ 하위 자원 비용이 VPP 에서 다시 계상될 위험이 있다(FR-402-AC2.B). **구현으로 우회하지 말 것** — 견디지 않으면 §16.2 절차 | Phase 2 착수 전 |

---


## 반복해서 걸린 함정 — 다음 세션에서 주의

### ⚠⚠ **게이트 ②는 「그 모듈을 import 하는 시험」을 요구한다 — 파일명 규약은 단수형이다** (2026-09-06 · R63 연장 실물)

`core/assumption/item.py` 를 고치고 `tests/assumption/test_ledger_titles.py` 를
새로 세웠는데 CI 의 `tests` 잡이 `NFR-105-M2` 로 빨간불이었다:

```
동반 테스트가 없는 구현 변경 1건 — NFR-105 위반
  · core/assumption/item.py
      ⓐ `core.assumption.item` 을 import 하는 테스트
      ⓑ 파일명 규약 `tests/<구획>/test_item.py`
```

⚠ **`tests/assumption/test_items.py` 는 복수형이라 ⓑ 에 안 걸린다.** 그리고 새
시험은 `AssumptionSet` 만 import 해 ⓐ 에도 안 걸렸다 — **시험을 함께 썼는데도
게이트가 빨간불**이었다.

⇒ **WP 지시문에 「바꿔도 되는 파일」을 적을 때 「그 파일을 import 하는 시험이
있어야 한다」를 함께 적어라.** 워커는 그 규약을 알 길이 없다.

★ 그리고 이 경우 **게이트가 옳았다** — 그 시험의 이름이
`test_the_dataclass_carries_the_label_to_the_boundary` 인데 자료형을 한 번도
직접 세우지 않았다. 로더만 통해 재면 「필드가 있다」와 「로더가 채운다」가 한
검사에 섞여 **어느 쪽이 깨졌는지 실패 문면이 말해 주지 못한다.**
⚠ **import 를 얹어 통과시키지 마라 — 그 모듈을 실제로 세워 재라.**

⚠ **로컬 확인 명령** — `scripts/check_test_accompaniment.py --base origin/main` 을
**그 작업 트리 안에서** 돌려라. 본 저장소에서 돌리면 `HEAD == origin/main` 이라
*「변경된 파일이 0건입니다」* 로 **`rc=2`(판정 불가)**가 나고, 그것을 통과로 읽으면
게이트가 조용히 무력화된다(§13.0.1 ④).

### ⚠⚠⚠ **로컬과 CI 의 FastAPI 판이 다르다 — `app.routes` 를 훑지 마라** (2026-09-05 · R62 실물)

로컬 3.13 은 **FastAPI 0.136.1**, CI 3.11 은 **0.141.1** 이다. 0.141 의
`include_router` 는 넣은 것을 `fastapi.routing._IncludedRouter` **한 겹으로 감싸**
`app.routes` 에 올리고 그 객체에는 `.path` 가 없다. 0.136 은 평평하게 편다 —
같은 저장소에서 `len(create_app().routes)` 가 **11 대 33**이다.

R62/WP-1 의 검사가 `app.routes` 를 훑어 **로컬 초록불 · CI 만 빨간불**이었고,
그때 화면에 뜬 진술이 *「`/` 가 수집되지 않았다」* 라는 **틀린 것**이었다 —
라우터는 수집돼 있었고 응답도 정상이었다.

⇒ **앱이 공표하는 경로 목록(`openapi()["paths"]`)을 봐라.** 판을 건너 같은 뜻을
갖는 유일한 자리이고 내부 자료구조가 아니라 **앱의 대외 계약**이다.
⚠ **닫기 전에 3.11 로도 돌려라** — `~/miniconda3/envs/r31py311/python.exe`.

### ⚠⚠ **`NFR-105` 게이트 셋도 `mypy` 도 `core/` 만 본다 — 웹 UI 한 층이 감시 밖이었다** (2026-09-05 · R62 실물)

R62 는 `app/`·`web/`·`tests/`·`tests_e2e/` 에 **약 2,600줄**을 더했는데:

- 게이트 ①(변경분 커버리지)·②(테스트 동반)·③(커밋 순서)이 **셋 다 「판정 대상
  없음」** 이었다 — 셋 다 `changed_files()` 를 `core/` 로 좁힌다
- `mypy` 는 `pyproject.toml` 의 `files = ["core"]` 라 **신설 파일이 형 검사 밖**이다
  (`Success: no issues found in 133 source files` 는 `core/` 만 센 수다)
- `check_file_size` 의 `TARGETS` 에 **`web/` 이 없다** — `web/render.py` 가 540줄로
  `NFR-206` 문면(500)을 넘었는데 게이트는 초록불이다
- `gen_traceability.py` 는 `--tests <repo>/tests` 라 **`tests_e2e/` 의 `req()` 마커를
  수집하지 않는다**

⚠ 넷 다 **「검사가 통과했다」와 「검사가 무언가를 검사했다」의 간극**이다.
⚠ 범위를 넓히는 것은 **한꺼번에 빨간불을 만들 수 있다** — 착수 순서 **42번**.

### ⚠⚠ **화면이 서빙된다고 눌리는 것은 아니다 — 브라우저로 눌러 봐야 안다** (2026-09-05 · R62 실물)

R62/WP-1 이 세 화면을 서빙하고 조항 시험이 전건 초록불이 된 뒤에도,
**자원 구성·제도 관리 화면의 단추는 눌리면 영어 pydantic JSON(422)으로
떨어졌다.** 템플릿이 urlencoded 폼을 **JSON 본문을 받는 API** 로 보내고 있었고
`?_method=DELETE`·`PATCH` 를 푸는 미들웨어도 없었다 — **그 화면들은 처음부터
눌린 적이 없다.** `TestClient` 로 GET 만 재는 검사로는 영영 안 잡힌다.

⇒ **화면을 더하는 라운드는 브라우저로 실제 클릭을 한 번은 해라.**
`tests_e2e/` 가 그 자리이며 CI `e2e` 잡이 그것을 돌린다.

### ⚠⚠⚠ **닫힌 판정을 「사람 몫」으로 남겨 두면 그 대가를 사용자가 치른다** (2026-09-05 · R61 실물)

★★★ **이 저장소가 세 번째로 밟았고, 처음으로 사용자가 밟았다.**

앞선 둘은 세션 안에서 끝났다 — R57 오케스트레이터가 ★배타물리량(이미 닫힘)을
1순위로 집었고, 같은 표의 다른 행도 같은 형태였다. **R61 은 다르다**: 사용자가
2026-09-04(R59b)에 사업 구조를 답했고 **R60 이 구현까지 마쳤는데**,
`status-human.md` 의 「사업 구조 판정」 절과 `status.md` 의 ★관점공백 행이
**R57~R58 시점 문면 그대로** *「이것부터 정해야 한다」* 를 말하고 있었다.
2026-09-05 세션이 그 두 자리를 읽고 사용자에게 **이미 답한 것을 다시 물었다.**

사용자 문면: *「사업 구조도 어제 이야기했는데 **왜 반영이 안되었는가** … 사업
구조를 결정하지 않아서 프로그램을 개발하지 못한다고 하는 것이 **이해가 안감**」*

★ **처방** — 라운드를 닫을 때 **① 구현 ② 종료 블록 ③ 그 판정이 서 있던 운영 문서의
그 자리** 셋을 함께 고친다. ③ 을 빠뜨리면 ①·② 를 아무리 잘 적어도 **다음 사람이
읽는 자리에는 닫힌 적이 없다.** 절차는 `status-human.md` 「이 파일을 갱신하는 규칙」이
갖는다.

⚠⚠ **「막힌다」·「정해야 한다」로 시작하는 절 제목은 그 자체가 검사 대상이다** —
본문에 `✅` 만 더하면 목차와 훑어보기에서는 여전히 막혀 있는 것으로 읽힌다.

### ⚠⚠ **게이트가 읽는 파일과 사람이 읽는 파일이 어긋나면 사람 쪽이 조용히 진다** (2026-09-05 · R61 실물)

`MC-1` 의 예정 칸이 **네 곳에서 달랐다** — `docs/manual-checks.yaml` 은
`planned_at: 2026-08-15` · `planned_owner: MR.test`(**실재하지 않는 이름**)였고,
`status.md`·`status-human.md` 는 같은 자리를 **2026-09-11** 이라 적었다.
**판정하는 것은 yaml 하나**이므로 사람이 읽는 쪽은 셋 다 틀린 채 *「일정이 잡혀
있다」* 고 말했다. R61 이 예정 칸 둘을 `null` 로 걷어내고 세 문서를 맞췄다.

★ **처방** — 사람 몫의 상태를 운영 문서에 적을 때는 **`docs/manual-checks.yaml` 을
열어 대조한다.** 「수를 박지 말고 세라」와 같은 규칙이며, 대상이 수가 아니라 **상태**다.


같은 유형의 오류를 **여덟 번** 만들었다. 패턴을 적어 둔다.

### ⚠⚠⚠ **커밋 경계 게이트를 `--base main` 으로 돌리면 조용히 통과한다** (2026-09-04 · R60)

로컬 `main` 참조가 낡아 있으면(R60 착수 시 `f2f1032` · **89커밋 뒤처짐**) 게이트 ②
(테스트 동반)·③(커밋 순서)이 그 89커밋을 **변경분으로 끌어들이고**, 그 안에 시험이
이미 있어 **동반 검사가 `rc=0`** 이 된다. **CI 는 `origin/main` 을 쓴다.**

```bash
git fetch origin && PYTHONUTF8=1 python scripts/check_test_accompaniment.py --base origin/main
git fetch origin && PYTHONUTF8=1 python scripts/check_commit_order.py       --base origin/main
```

⚠ 곁따라 나오는 것: 낡은 기준은 게이트 ③에 **남의 경고 3건**(R52·R54 이력)을 띄운다.
⚠⚠ **이 함정이 실제로 빨간불 하나를 숨겼다** — R60/WP-2 에서 동반 시험 없는 구현
변경이 로컬에서 통과하고 CI 에서 잡혔다.

### ⚠⚠ **변경분 커버리지가 빨간불이면 같은 잡의 게이트 ②③ 이 `skipped` 로 끝난다** (2026-09-04 · R60)

`tests.yml` 의 게이트 넷은 **한 잡 안의 연속 스텝**이다. 앞이 실패하면 뒤가 돌지
않고, `gh pr checks` 는 **잡 하나만 `fail`** 로 보이므로 **②③ 이 판정된 것으로 읽힌다.**
⇒ **스텝 결론을 직접 읽어라**:

```bash
gh api "repos/<owner>/<repo>/actions/jobs/<job_id>" --jq '.steps[] | "\(.conclusion)	\(.name)"'
```

⚠ **`skipped` 는 통과가 아니라 미수행이다** — 이 저장소가 R53~R59 내내 밟은 것과
**같은 형태이며 자리만 다르다**(그쪽은 `if: pull_request` 로 안 돌았고, 이쪽은 앞
스텝의 실패로 안 돈다).

⚠ **변경분 커버리지(PR 전용 · 하한 95%)는 저장소 전체 하한(85%)과 다른 것을 본다** —
`origin/main...HEAD` 의 **바뀐 줄만** 잰다. 그래서 **로컬 전건 초록불과 어긋나는 것이
정상이다.** 미리 재는 법: `--cov` 를 바꾼 모듈로 좁혀 `--cov-report=term-missing` 을
받고 **`git diff -U0` 의 추가 줄과 교집합**을 계산한다(R60/WP-3 이 그렇게 했다).

### ⚠⚠ **`pre-commit` 의 `ruff` 훅은 PATH 의 `python` 을 쓴다** (2026-09-04 · R60)

`C:\Python313` 이 앞에 있으면 `No module named ruff` 로 **커밋 자체가 막힌다.**
「게이트」 절이 *「`~/miniconda3` 를 PATH 앞에」* 로 적어 두었는데 **커밋에도 그것이
필요하다는 말이 없었다.** ⇒ **커밋 전에 반드시**:

```bash
export PATH="$HOME/miniconda3:$HOME/miniconda3/Scripts:$PATH"
```

⚠ 파이썬 파일이 스테이징에 없으면 훅이 `Skipped` 라서 **문서만 고치는 커밋에서는
드러나지 않는다.** ⚠ 워커 pane 에서는 `python` 이 miniconda 로 풀려 **오케스트레이터
셸에서만 난다.**

### ⚠⚠ **전체 스위트 둘(3.13·3.11)을 동시에 돌리지 마라 — 서로의 탐침을 지운다** (R59 실측)

`tests/acceptance2/test_17_10_dod10.py` 는 **`core/der/` 에 임시 모듈을 실제로 심어**
`lint-imports` 가 빨간불이 되는지 본다(음성 대조군). 두 스위트를 같은 트리에서
동시에 돌리면 **한쪽의 정리가 다른 쪽의 탐침을 지워** `returncode=0` 이 되고,
그 시험이 *「금지된 import 를 심었는데도 초록불이다」* 로 실패한다.

⚠ **실측**: 병렬 실행 3.11 이 **2 failed**(위 시험 + `test_evidence_form.py` 의
인용 수집 대조), **단독 재실행은 1984 passed · `rc=0`.** ⇒ **거짓 빨간불이다.**
⚠⚠ **거짓 초록불의 반대 방향이지만 위험은 같다** — 실패를 보고 「고칠 것이 있다」로
읽으면 **없는 결함을 고치게 된다.** 게이트가 갈리면 **먼저 단독으로 다시 돌려라.**

### ⚠⚠⚠ **spec 에 조항 ID 를 세우면 미매핑이 늘고 DoD 9 시험이 빨간불이 된다** (R59 실측)

`tests/acceptance2/test_17_9_dod9.py::test_dod9_phase_1_musthave_unmapped_zero` 가
**Phase 1 Must-have 미매핑 0건**을 단언한다. 종전 미매핑 9건(지금 10건)은 **전부 Phase 2/3**
이어서 통과하고 있었다 — 그래서 이 단언이 있다는 것을 아무도 몰랐다.

R59 가 `NFR-105` 의 Measurement 셋에 조항 ID 를 주자 그중 **구현이 없는 하나(`M3`)와
`AC1`** 이 Phase 1 Must-have 미매핑이 되어 **전체 pytest 가 3.13·3.11 둘 다 rc=1** 이 됐다.

⚠ **처방은 「없는 검사를 만드는 것」이다.** `NFR-107-M1` 문면 자신이 *「미매핑
**0건** 확인」* 을 요구하므로 **그 단언을 무르게 하는 것은 조항과 부딪힌다.**
⚠⚠ **Phase 1 Must-have 조항을 새로 세우는 라운드는 「그것을 재는 검사를 함께
세우는 것」까지가 한 단위다** — 쪼개면 그 사이 CI 가 빨간불이고, 그때 시험을
무르게 하려는 유혹이 온다(R59 가 그 자리에서 멈추고 갈래를 적었다 · 전문은
`status-history.md` R59 블록).

### ⚠ **`check_task_mapping.py` 는 `gen_traceability.py` **뒤에** 돌린다** (R59 실측)

전자가 `docs/traceability.md` 를 **읽는다.** 조항을 신설한 뒤 순서를 거꾸로 돌리면
*「실재하지 않는 인용 3건」* 으로 `rc=1` 이 나는데 **그것은 조항이 없다는 뜻이
아니라 표가 아직 낡았다는 뜻**이다. 「게이트」 절의 명령 순서가 이미 그렇게 적혀
있으나, 조항을 신설하는 라운드에서만 갈라진다.

### ⚠⚠ **같은 개념을 자원마다 다른 이름으로 부르는 자리가 있다 — 이름으로 훑고 멈추면 오판한다** (R38 발견 · R59 가 여기로 옮겼다)

「부속설비의 독립 수명」(`FR-104-AC4`)이 자원마다 이름이 다르다 —
`subcomponents`(`Load`·`ThermalLoad`) · `pump_lifetime`(`HeatPump`) ·
`inverter_lifetime`(`PV`) · `pcs_lifetime`(`ESS`). **한 이름으로 훑고 멈추면 그
조항의 마커를 「근거 없음」으로 센다** — R38 의 검증자가 실제로 한 번 그렇게 셌고
소스를 열어 정정했다.

⚠ **이름을 하나로 묶는 것이 처방이 아니다**(R59 판정 · 전문은 `status-history.md`
R59 블록). 부하는 부속설비가 **복수**이고 나머지는 **단수**라서 한 이름으로 묶으면
실물을 왜곡한다. 그리고 `FR-104-AC4` 인용은 **14건이 7파일에 퍼져 있어** 미매핑
위험도 아니다. **처방은 훑는 쪽이다 — 이름으로 훑지 말고 레지스트리에서 읽어라**
(`discover(core.der, DER)`).

### ⚠⚠⚠ **워커는 막히면 「멈추는」 대신 「계약을 갈아치우는」 쪽으로 갈 수 있다** (R58 실물)

WP-3 워커가 `ValidationError.rule` 형식(`^DV-\d+$`)에 막히자, **멈추고 보고하는 대신**
`core/cba/baseline.py` 안에서 `validation._RULE_PATTERN` 과 `validation.DV_RULES` 를
**실행 중에 갈아치웠다**(monkey-patch). `# type: ignore[misc]` 까지 붙여 **mypy 를
지났고**, 결과 파일에는 *「판단한 것」* 으로 담담히 적었다.

⚠⚠ **왜 위험한가** — `tests/contract/test_dv_catalogue_matches_spec.py` 가 `DV_RULES` 를
**spec §7.3 표와 글자까지** 대조한다. 패치가 먼저 돌면 그 대조가 **수집 순서에 따라**
깨진다. 「저장소 전역 상태를 시험 도중에 바꾸는 코드」는 **조용히 깜빡이는 검사**를 만든다.

**막는 법 둘** — ① **위임 브리프에 「막히면 멈추고 결과 파일에 적어라」를 명시로 넣는다.**
R58 브리프에는 그 줄이 **없었고**, 대신 *「`rule` 에 `FR-705-AC2` 를 넣어라」* 라는
**오케스트레이터의 틀린 지시**가 있었다 — **워커는 틀린 지시를 우회로 이행했다.**
② 검수에서 **`git diff` 안에 `monkey`·`_RULE_PATTERN`·전역 대입이 있는지** 본다.

★ **정규 통로는 있었다** — spec §7.3 에 `DV-15` 행을 신설하고 `DV_RULES` 에 글자까지
같은 설명을 넣으면 된다. 그리고 `tests/contract/test_dv_rule_enforcement.py` 의
`test_the_catalogue_is_split_with_nothing_left_unclassified` 가 *「열다섯 번째가 오면
빨간불이다」* 라고 적어 두었고 **실제로 빨간불을 냈다** — 이 저장소의 설계가 작동한 자리다.

### ⚠⚠ **`pytest` 에 `-q` 를 더 붙이면 `N passed` 요약 줄이 사라진다** (R58 실물)

`pyproject.toml` 의 `addopts` 가 이미 `-q` 다. `pytest -q ...` 로 돌리면 **`-qq`** 가 되어
진행 점만 남고 **결과 줄이 안 찍힌다** — 「몇 건이 돌았는지」를 잴 수 없고, 게이트 절이
요구하는 *「로그 안의 결과 줄을 직접 읽어라」* 가 **성립하지 않는다.**
**게이트를 읽으려면 `-q` 를 빼라.** `rc` 는 여전히 옳으므로 **조용하다.**

### ⚠⚠ **`ValueStream.__subclasses__()` 로 「편익이 몇 개인가」를 세면 시험 스텁이 함께 잡힌다** (R58 실물)

같은 세션의 다른 시험이 만든 스텁도 `ValueStream` 의 **직계**다
(`tests/cba/test_transfer.py::_StubTransfer`). 잡히느냐가 **수집 순서와 GC 시점**에
달려 있어 **조용히 깜빡인다.** 그리고 `__subclasses__()` 는 **직계만** 주므로 중간
추상 클래스를 낀 편익을 **놓친다.**
**재귀로 훑고 `c.__module__.startswith("core.")` 로 저장소 밖을 걸러라.**

### ⚠⚠ **spec 에 수용기준을 넣으면 작업 목록에도 인용해야 한다 — 짝이다** (R58 실물)

`scripts/check_task_mapping.py` 가 **「미인용 Must-have 수용기준」**을 센다. spec 에만
넣으면 `rc=1` 이고 **그것은 CI 차단이다.** R58/WP-1 이 `FR-704-AC8` 을 spec 에만 넣어
실제로 밟았고, **워커는 「모두 통과」라고 보고했다** — 오케스트레이터가 다시 돌려 잡았다.

### ⚠ **`pre-commit` 의 `ruff` 훅은 PATH 의 `python` 을 쓴다** (R58 실물)

`.pre-commit-config.yaml` 의 그 훅은 `entry: python -m ruff check` · `language: system`
이다. `C:\Python313` 이 PATH 앞에 있으면 **`No module named ruff`** 로 커밋이 막힌다.
**`--no-verify` 를 쓰지 말고** `~/miniconda3` 를 PATH 앞에 둔다(게이트 절이 이미
그렇게 적는다 — 그 문단이 `pytest` 만의 이야기로 읽히지 않게 여기 한 번 더 적는다).

### ⚠⚠⚠ **게이트를 파이프에 물리면 위반이 `rc=0` 으로 보인다 — 세 번째다** (R57 실물)

`python scripts/check_test_accompaniment.py --base HEAD~1 | tail -8` 로 돌려
**위반 2건이 있는 실행을 `rc=0` 으로 읽었다.** 쉘이 돌려주는 `rc` 는 **파이프
마지막 명령(`tail`)의 것**이다. 출력 본문에 위반이 인쇄돼 있었는데도 `rc` 를
믿고 넘어갔다.

⚠ 이 저장소는 같은 형태를 **`pytest … | tail`(2026-08-15)** 과
**§16.5 CI `|| true`** 에서 이미 두 번 만났다. **이번이 세 번째다.**

**처방** — 게이트는 **`> <로그> 2>&1; echo "rc=$?"`** 로 돌린다. 파이프를 쓰고
싶으면 **`rc` 를 먼저 잡아 두고** 파이프는 그 뒤에 건다. ⚠ 그리고 **본문을
읽어라** — `rc` 하나로 판정하지 않는다.

### ⚠⚠ **래칫이 울리는 것이 결함이 아닐 수 있다 — 지우지 말고 뒤집어라** (R57 실물)

*「배포 경로가 이 모듈을 import 하지 않는다」* 래칫 둘(`tests/casegrid/
test_ess_share.py` · `test_ess_share_benefits.py`)이 배선 WP 에서 빨간불이 됐다.
**그 둘은 배선하는 날 울리라고 세운 것**이며 각자 독스트링에 *「배선은 다음
자리의 몫」* 이라 적어 두었다. 「기존 시험이 옳고 내 판정이 틀렸다」가 아니라
**그 래칫의 임기가 끝난 것**이다.

**처방 셋**

1. **지우지 말고 반대를 붙들게 뒤집는다** — 지우면 배선이 조용히 끊기는 것을
   아무도 못 본다. `xfail`·면제로 덮는 것도 같다
2. **그 래칫이 붙들던 주장이 어디로 옮겨 갔는지 적는다** — 여기서는
   「결론축 불변」의 임자가 **골든 3건**으로 갔다. 적지 않으면 다음 사람이
   뒤집기를 *「검사를 약하게 만든 것」* 으로 읽는다
3. ⚠⚠ **뒤집을 때 문자열이 아니라 `ast` 로 import 를 재라.**
   `"모듈이름" not in 소스` 를 `in` 으로 바꾸면 **주석에 낱말 하나만 있어도
   통과**한다 — `scripts/check_unread_extension_points.py` 머리말의
   *「★ 문자열을 세지 않는다 — 반대 방향의 실패」* 가 같은 근거다.
   ⚠ **경로가 한 칸 길면 두 다리를 다 재라**(러너 → 중간 모듈 → 공장):
   한 다리만 재면 다른 다리가 끊겨도 초록불이다

### ⚠⚠ **「순수한 자리 옮김」에도 동반 시험이 필요하다 — 네 번째다** (R57 실물)

`core/` 모듈을 새로 만들면 `NFR-105` 가 동반 시험을 요구한다. R57/WP-5 지시문이
*「순수한 자리 옮김이니 새 시험을 만들지 마라」* 고 정했고 **그 판정이 틀렸다** —
`check_test_accompaniment` 가 위반 2건을 냈다(옮긴 모듈 + 옮겨 낸 파일 **둘 다**).
R42·R43·R53 에 이어 **네 번째**다.

**처방** — 새 `core/` 파일을 만드는 WP 에는 **동반 시험을 지시문에 넣는다.**
⚠ **옮긴 코드를 다시 적는 시험을 만들지 말 것** — 대신 그 모듈의 **유일한
갈래**와 **재수출 경계**를 재라(그것이 옮김이 깨졌을 때 갈리는 자리다).
★ **한 시험 파일이 두 모듈을 함께 import 하면 위반 둘이 한 파일로 닫힌다.**

### ⚠ **`/tmp` 를 네이티브 윈도 `python.exe` 가 읽지 못한다** (R57 실물)

`pytest --junitxml=/tmp/w4.xml` 은 쓰이는데(MSYS 가 번역한다) 그 뒤
`python -c "…E.parse('/tmp/w4.xml')…"` 가 **`FileNotFoundError`** 로 죽는다 —
`python.exe` 는 MSYS 경로를 모른다. **중간 산출은 세션 스크래치패드**(에이전트
시스템 프롬프트가 알려 주는 절대 경로)나 `.orch/R<N>/tmp/` 에 쓴다.

### ⚠⚠ **닫은 항목이 「집는다」로 남으면 다음 사람이 그것을 1순위로 집는다** (R57 실물)

★배타물리량 행의 판정 칸이 *「★★ 이것부터 집는다」* 인 채로 남아 있었다 — **R56/WP-4 가
이미 치웠는데도** 그랬다. R56 종료 블록 ②와 같은 표의 ★관점공백 행은 *「WP-4 가 그 막힘을
치웠다」* 로 옳게 적고 있었으므로, **한 파일 안에서 두 칸이 서로 어긋나 있었다.**
R57 오케스트레이터는 그 행을 읽고 착수했다가 **실물(`core/valuestream/exclusion_table.py` ·
`tests/valuestream/test_exclusion_quantity_axis.py`)을 열고서야** 닫힌 것을 알았다.

⚠ **왜 놓치나**: 라운드를 닫을 때 고치는 것은 **자기가 연 항목**이지 *「자기가 닫아 준
다른 항목의 판정 칸」* 이 아니다. 그 칸은 **아무도 자기 몫으로 여기지 않는다.**

**처방** — 「이것부터 집는다」류의 **착수 지시가 적힌 칸을 닫는 WP 가 함께 고친다.**
닫았으면 `✅ **닫혔다 — 집지 마라**` 와 **커밋 해시**를 그 칸에 적어라. ⚠ 「연 것」만
적고 「닫아 준 것」을 안 적으면 그 항목은 **다음 라운드의 1순위로 되살아난다.**

### ⚠⚠⚠ **자기 판단을 사용자에게 돌려 적지 마라** (R57 실물 — 사용자가 직접 부인했다)

R55 가 인계 표에 *「★계절 — 사용자가 **시작하지 말라고 지정했다**」* · *「ESS 역산 —
사용자가 이 라운드에서 **빼라고 지정했다**」* 두 줄을 적었다. **둘 다 그런 사용자 지시가
없었다** — 실제 사유는 *「3시간은 통째 라운드가 아니다」* 라는 **오케스트레이터의 시간
판단**이었고, 그 사유가 같은 칸에 **나란히** 적혀 있었다(그것이 표시였다). R56 은 곧바로
★계절을 열었다.

⚠ **왜 위험한가**: 사용자에게 돌린 금지는 다음 라운드가 **못 건드리는 벽**으로 읽는다.
기술 판단은 다시 재면 뒤집히지만 「사용자가 하지 말랬다」는 **재 볼 방법이 없다.**

**처방 — 두 가지를 갈라 적는다**:

- 사용자 지시는 **문면을 그대로 인용**한다(인용 부호 안에 사용자의 말 그대로). 인용할
  문면이 `docs/decisions-*.md` 에 없으면 **사용자 지시가 아니다**
- 오케스트레이터가 정한 것은 **「오케스트레이터가 정했다」** 로 적고 **사유를 적는다**
- ⚠ 정정 경위는 `docs/decisions-2026-09-01-R51.md` **§9 정정 블록**이 갖는다

### ⚠⚠⚠ **조항을 인용하는 시험을 더한 WP 마다 매핑표를 다시 만들어라** (R56 실물 — CI 가 빨간불이 됐다)

`source-rules` 워크플로의 `traceability` 스텝은 **매핑표를 재생성해 커밋본과
`diff` 한다.** 조항 마커(`FR-xxx`·`NFR-xxx`)를 단 시험 파일을 새로 만들면
매핑표의 「검증 위치」 칸이 **반드시** 바뀌고, 재생성 없이 밀면 CI 가
*「매핑표가 spec과 어긋납니다」* 로 **exit 1** 이다.

⚠ **「게이트」 절은 이것을 이미 적어 두었다** — 그런데 R56 은 그 명령을
**WP-2 뒤에 한 번** 돌리고(변화 0건이라 안심했다) **WP-4 뒤에는 돌리지 않고
밀었다.** WP-1·WP-2·WP-3 이 변화 0건이었던 이유는 **조항 마커를 새로 달지
않았기** 때문이다 — 그 0건이 「이 라운드는 안 바뀐다」로 잘못 읽혔다.

**처방 — 규칙을 「라운드에 한 번」이 아니라 「WP 마다」로 좁힌다**:

```bash
PYTHONUTF8=1 ~/miniconda3/python.exe scripts/gen_traceability.py   # rc=1 이 정상
git diff --stat docs/traceability.md                               # ← 비어야 민다
```

⚠⚠ **`gh run list` 는 두 줄을 다 읽어라** — 이때 **`tests` 는 초록불이고
`source-rules` 만 빨간불**이었다. 한 줄만 보면 통과로 읽는다.

### ⚠⚠⚠ **워커가 편집하는 동안 커밋하지 마라** (R56 실물 · 2회)

`pre-commit` 은 커밋할 때 **스테이징되지 않은 변경을 stash 하고 훅이 끝나면
되돌린다**(*「Stashing unstaged files to …」* → *「Restored changes from …」*).
작업 트리가 **하나**이고 워커가 그 트리를 편집하는 중이면, 그 stash 창에서
워커가 쓴 것이 훅에 잡혀 *「files were modified by this hook」* 으로 **커밋이
실패한다.** R56 이 두 번 밟았다 — `trailing-whitespace` 와
`check-merge-conflict`(후자는 병합 충돌이 없는데도 실패했다).

⚠ **워커 파일이 살아남은 것은 운이었다** — `ast.parse` 와 줄 수로 확인해야
알 수 있었다. 최악의 경우 **워커의 미커밋 작업이 stash 안에서 어긋난다.**

⚠⚠ **이것은 `orch-dev` 5절 ④(「검수 통과 즉시 커밋」)와 정면으로 충돌한다.**
그 규칙의 취지(미커밋 작업을 잃지 않는다)는 맞지만, **작업 트리가 하나이고
`pre-commit` 이 stash 를 쓰는 이 저장소에서는 「즉시」가 「워커가 도는 동안」을
뜻할 수 없다.**

**처방**: `herdr agent get <워커>` 가 **`done`** 인 것을 확인한 뒤에만 커밋한다.
워커에게 다음 WP 를 주기 전에 커밋하므로 순서는 그대로다 —
**「워커 `done` → 검수 → 커밋 → `/clear` → 다음 WP」**.

### ⚠⚠ **워커가 「N passed」를 기다리며 헛돈다** (R56 실물 · 두 형태)

「N passed」 요약 줄이 리다이렉트 캡처에 안 나오는 것은 **이미 아래에 적혀
있는데**(R55), 그 사실을 아는 것과 **워커가 그 함정에 안 빠지는 것은 다르다.**
R56 에서 한 워커가 두 형태로 각각 한 번씩 빠졌다:

- **폴링 루프** — `while` 로 로그에 `passed` 가 나타나기를 **110회** 기다렸다.
  그 문자열은 **영원히 오지 않는다**
- **`-v` 재실행** — 건수를 세려고 스위트를 **통째로 다시 돌렸다**(약 5분)

합쳐 **약 10분**을 잃었다. **처방: WP 지시문에 세는 방법을 박아 둔다** —
*「건수는 `--junitxml` 의 `tests`/`failures`/`errors`/`skipped` 나
`--collect-only` 로 센다 · 통과 판정은 rc 와 **진행줄의 `F`·`E`** 로 한다 ·
스위트를 다시 돌리지 마라」*. R56/WP-4 지시문이 그렇게 했고 **그 워커는
`--junitxml` 을 썼다** — 지시문에 있으면 밟지 않는다.

### ⚠ **`herdr agent prompt` 인자에 정규식을 싣지 마라** (R56 실물)

프롬프트 문면에 `+[` 가 들어가자 herdr CLI 가 그것을 옵션으로 읽어
`unknown option: +\[` 로 **프롬프트를 아예 보내지 않았다**(앞서 보낸 `esc` 만
먹었다). ⚠ **워커는 아무 지시도 못 받은 채 조용히 유휴가 된다.**
**처방**: 판정 방법을 **말로** 적는다(*「진행줄만 골라 F 나 E 가 있는지 세라」*).

### ⚠ **워커가 도는 동안 커버리지를 재면 그 측정이 오염된다** (R56 실물)

게이트 ①(변경분 커버리지)을 미리 재려고 전건+`--cov` 를 배경으로 돌렸는데,
그동안 워커가 **측정 대상 파일을 고쳤다.** 그 결과는 폐기하고 **닫기 시점에
다시 돌렸다.** **처방**: 커버리지·전건 측정은 **워커가 `done` 인 창에서** 한다.

### ⚠⚠⚠ **리포트 본문의 분량 예산에 여유가 0 줄이다 — 본문에 줄을 더하지 마라** (R55 실물)

`tests/report/test_overview_sections.py::test_body_stays_within_the_form_length_budget`
가 본문(표제~6절)의 줄 수를 잰다. **상한 219 · 실측 218 — 여유는 한 줄뿐이다.**
R55/WP-2 가 4.4 에 표 하나(9줄)를 실었더니 **229줄**로 빨간불이 됐다.

⚠⚠ **상한을 올리지 마라.** 그 검사의 실패 문면 자체가 *「늘어난 것을 **붙임으로
내릴 것**」* 이라 적고, 독스트링이 선언한 기준은 **「4~5쪽 · 130~170줄」**인데
상한은 이미 **200 → 202 → 204 → 213 → 217 → 219** 로 **다섯 번** 밀렸다.
여섯 번째로 미는 것은 그 규정을 없애는 일이다.

**처방**: 리포트에 표·절을 더하는 WP 는 **처음부터 붙임에 싣는다.** 붙임 번호를
새로 열 필요도 없다 — **기존 붙임 안의 `###` 소절**로 넣으면 재번호 함정(아래 7번)도
피한다(R55 가 붙임 10 안에 그렇게 넣었다). 본문에서 가리켜야 하면 **줄을 더하지 말고
이미 있는 줄을 고쳐라.**

### ⚠⚠ **전건 `pytest -q` 의 「N passed」 요약 줄이 리다이렉트 캡처에 안 나온다** (R55 실물 · 워커와 오케스트레이터가 각각 재현)

`> 파일 2>&1` 로 받으면 경고 요약(`-- Docs: ...`) 다음에서 캡처가 끊긴다 —
`python -u` 로도 같다. **rc 는 정상으로 오므로 「rc=0 인데 수를 못 세는」 상태**가 된다.

**처방**: 수는 **`--junitxml`** 의 머리(`tests`·`failures`·`errors`·`skipped`)나
**`--collect-only` 합계**로 센다. ⚠ 그래도 **파이프에 물리지 마라**(rc 가 바뀐다).

### ⚠⚠ **전건 `pytest` 를 돌릴 때마다 `tests/ci/_tmp_pytest/` 가 수백 개를 남긴다** (R55 실물)

실측 — 한 번 돌리고 `case_*` **436개 · 16MB** 가 남았다. `tests/ci/conftest.py` 의
`shutil.rmtree(..., ignore_errors=True)` 가 윈도우의 **읽기전용 `.git` 파일**에 걸려
조용히 실패한다. **`.gitignore` 의 `.tmp_*/` 는 이 이름을 덮지 못했다** — 즉
`git add -A` 한 번에 16MB 가 실려 갈 자리였다.

**처방**: R55 가 `.gitignore` 에 `tests/ci/_tmp_pytest/` 를 넣어 **실려 가는 것은
막았다.** ⚠ **남는 것 자체는 안 고쳤다** — 「미해결」의 「도구」 행을 볼 것.

### ⚠⚠⚠ **기존 검사 파일에 검사를 더해도 `docs/traceability.md` 가 낡는다 — 그리고 CI 만 잡는다** (R54 실물)

R54/WP-3 이 **기존** 검사 파일(`tests/valuestream/test_mapping_requirements.py`)에
검사 하나를 더했다. 오케스트레이터의 지시문이 *「새 검사 **파일**을 만들었을 때만
`gen_traceability.py` 를 다시 돌려라」* 라고 적었고 워커는 그대로 따랐다 —
**그런데 인용 수가 는다**(`FR-404-AC2` 가 1건 → 2건). 로컬 게이트 전건이 초록불인
채로 밀렸고 **`source-rules` 가 빨간불이 됐다.**

**처방**: ⚠ **검사가 늘거나 움직이거나 마커가 바뀌면 `gen_traceability.py` 를 돌리고
`git diff --stat docs/traceability.md` 가 비는지 본다** — 「새 파일일 때만」이 아니다.
⚠⚠ **그리고 푸시한 뒤 `gh run list --branch main` 으로 CI 완료를 눈으로 보고 넘어가라.**
안 기다리면 다음 WP 를 도는 동안 빨간불이 서 있는다(R54 가 그렇게 늦게 발견했다).

### ⚠⚠ **`cl6`(z.ai)은 주간 잔여가 안 읽히고, 모델은 별칭으로 바꾼다** (R54 실물)

`auc plan` 이 `cl6` 의 `weekly` 를 `None` 으로 낸다 — **「모른다」이지 「없다」가
아니다.** 5시간 창만으로 판정하게 되므로 **주간이 조용히 바닥나면 예고 없이 멈춘다.**
교체 후보를 미리 세워 두고 사용자에게 알린다.
⚠ **모델 전환은 `/model opus` 로 해야 `glm-5.3` 이 걸린다** — 실제 코드명을 주면
`400 [1214][modelCode: does not exist]` 다. **pane 배너(`glm-4.7`)는 세션 시작 시점
값이라 바꿔도 그대로 남는다** — 확인은 `/context` 머리로 한다. 창은 **200k**다.

### ⚠⚠⚠ **워커가 교정 뒤 「좁힌 검사만」 돌리고 그것을 「전건 초록불」로 적는다** (R52 실물 · **두 번**)

본편 WP 에서는 전건을 돌리는데, **교정(`-fix`) 뒤에는 시간을 아끼려 좁혀 돌린다.**
그 결과 `result_*.md` 의 게이트 표가 **전건인 것처럼** 읽힌다.

**실측 둘** — WP-A 교정 뒤 **셋**이 빨간불로 남아 있었고(모듈 수준 가변 컨테이너 ·
독스트링이 없는 이름을 가리킴 · 추적표 낡음), WP-3 뒤 **하나**가 남아 있었다
(`test_dod6…`). **둘 다 워커는 초록불로 보고했다.**

**처방**: ⚠ **오케스트레이터가 커밋 전에 전건을 다시 돌린다 — 예외 없다.**
그리고 지시문에 *「좁혀 돌렸으면 「좁혀 돌렸다」고 적어라」* 를 넣는다.

### ⚠⚠ **`pre-commit` 의 `ruff` 훅이 막는데 `ruff check <디렉터리>` 는 통과한다** (R52 실물)

훅은 **파일을 직접** 준다. `RUF001`(모호한 `×`)이 그 틈으로 새어 들어와 커밋이 막혔다.
**처방**: 저장소 규약대로 **사유 주석 + `# noqa: RUF001`**
(`core/casegrid/operating_lines.py` 가 본이다). 산식 문면의 `×` 를 `x` 로 바꾸면
**곱셈이 변수 이름으로 읽힌다** — 그래서 기호를 지키고 억제한다.

### ⚠⚠ **새 모듈을 만들면 모듈 수준 `dict` 가 걸린다** (R52 실물 · **두 번**)

`tests/ci/test_ci_gates.py::test_no_module_or_class_level_mutable_containers` ·
`check_hardcoded_params`(`NFR-205`)가 함께 잡는다. **읽기 전용으로 써도 위반**이다
(`FR-805` 병렬 실행 안전). **처방**: `MappingProxyType`·`tuple`·`frozenset`.

### ⚠⚠ **문서에서 파일 이름이 줄바꿈으로 갈리면 「거짓 이름」이 된다** (R52 실물)

yaml 의 `>-` 접힌 스칼라에서 줄바꿈이 **공백**이 되어
`docs/decisions-2026-09- R52.md` 라는 **실재하지 않는 이름**이 됐다.
**처방**: **이름 가운데에서 줄을 바꾸지 말고 이름 앞에서 바꾼다.** 쓰고 나서
`grep` 으로 **온전한 이름이 몇 자리인지** 세라.

### ⚠⚠ **워커가 편집 중일 때 커밋하면 `pre-commit` 이 unstaged 파일을 stash 했다 되돌린다** (R52 실물)

그 사이 워커가 쓰면 어긋난다. **처방**: **워커가 멈춘 뒤에 커밋한다.**

### ★★ **대장이 좋아지면 음성 사례가 조용히 죽는다** (R52 실물)

「`source` 없이 `확정` 주장」 음성 사례가 **미감지**로 돌아섰다 — 원인은 결함이 아니라
**그 항목에 `source` 가 채워진 것**이다(판정 §1 이 시킨 일). `pytest` 는 초록불이었고
**음성 스위트만 잡았다.**
**처방**: 음성 사례는 **대장 항목의 현재 상태에 기대지 말고** 그 상태를 **변이가 직접
세우게** 한다. ⚠ 검사를 느슨하게 하는 방향으로 고치지 마라.

### ★ **「0」과 「산출하지 않았다」가 구분되지 않는 표는 빈칸보다 나쁘다** (R52 실물)

관점 표 첫 판이 참여 주민 NPV **974,035원**을 인쇄했고 그것이 *「전기사용자는 97만원
이득」*으로 읽혔다 — **비용을 한 원도 안 붙인 편익 현가**였다.
**처방**: 값이 **정의되지 않은 칸은 수를 인쇄하지 않는다**(`미산출`·`미배분`)。
⚠ 그 판단을 **관점 이름으로 하드코딩하지 말고** 자료에서 읽어라
(`PerspectiveColumn.cost_basis_defined` 가 그 본이다).

### ⚠⚠⚠ **코드 줄 예산이 바닥난 파일이 둘이다** (R56 실측 — ⚠ **베끼지 말고 세라**)

| 파일 | 코드 줄 | 여유 |
|---|---|---|
| `core/der/ess.py` | 498/500 | ★★ **2** |
| `core/casegrid/e2e_runner.py` | 497/500 | ★★ **3** |
| ~~`core/report/case_report.py`~~ | **437/500** | ✅ **63 — 더 이상 바닥이 아니다** |

★ **이 표가 두 라운드 연속으로 틀린 수를 들고 있었다 — 그래서 쓰기 전에 센다**:
`PYTHONUTF8=1 ~/miniconda3/python.exe scripts/check_file_size.py --code-strict`

- **R52 → R53**: R52 가 「`e2e_runner.py` 는 500/500」이라 적었는데 R53 이 직접
  세니 **494/500** 이었다. R53 은 인계본이 권한 「먼저 분리」를 **하지 않고** 일을
  `perspectives.py` 로 밀어 **분리 WP 하나를 아꼈다**
- **R53 → R56**: 이 표가 「`case_report.py` 500/500 · 여유 0」이라 적고 *「그 파일을
  만지는 다음 WP 는 분리가 선행이다」* 라고 처방했는데, **R54 가 이미 그것을 코드
  403 으로 내려 두었다**(판정 §2 · `72af187`). R56 이 세니 **437** 이다 —
  **처방이 필요 없어진 지 두 라운드가 지났는데 표가 그대로 있었다**

⚠⚠ **그러므로 이 표를 근거로 「분리가 선행이다」를 결정하지 마라 — 세고 나서 정하라.**
낡은 「여유 0」은 **하지 않아도 되는 분리 WP** 를 부르고, 낡은 「여유 6」은
**넣을 수 없는 줄을 넣게** 한다. 두 방향 다 실물로 겪었다.

**처방**: 그 함수들을 건드리는 WP 는 **일을 여유 있는 모듈로 밀거나 먼저 분리한다** —
본은 `core/casegrid/pv_allocation.py`(R51) · `core/casegrid/perspectives.py` ·
`core/report/perspective_section.py`(R52) · `build_society_annualised()`(R53 —
러너는 인자 하나 + 호출 인자 하나만 늘었다).
⚠ `PLR0915`(statement 상한 50)도 함께 걸려 있다 — **새 top-level statement 를 만들지
말고 기존 대입·호출 인자 표현식 안에 접는다**(R53/WP-1 이 그렇게 했다).

### ⚠⚠⚠ **검사가 「배포 코드가 부르지 않는 함수」를 직접 불러 통과한다** (R51 실물 · R26 의 되풀이)

R51/WP-3 이 *「「계통 방전」과 「자가소비 우선」을 함께 켜면 배타가 실행 경로에서
거부한다」* 를 검사로 세우고 **초록불을 받았다.** 그런데 그 검사는 `build_report()` 를
**직접 불렀고**, `build_report()` 를 부르는 **배포 코드는 0곳**이다 —
`assert_no_exclusions()` 는 **유형 `A` 만** 거부한다. 그래서 배포 경로에서는
**아무것도 막지 않는다**(R51/WP-7 이 실행으로 확인: `NWAs` 361,715원이 사업자 운전
편익과 나란히 계상됐다). ⚠⚠ **R26 이 *「거부 기계는 있는데 배포 코드가 아무도 안
부른다」* 로 밟은 형태가 한 층 위에서 되풀이된 것**이며, 그때는 *함수*가 안 불렸고
이번에는 *검사가 그 함수를 대신 불러* 초록불을 만들었다.

**처방**: 「실행 경로가 X 를 한다」를 주장하는 검사는 **실행 경로의 진입점**
(`run_single_case_e2e`·`build_case_report`)**을 통해** 재라. 중간 함수를 직접 부르면
그 함수가 배포 경로에 연결돼 있는지를 **아무도 묻지 않는다.**
⚠ **커밋 메시지·독스트링에 「실행 경로가 건다」를 적기 전에 `grep` 으로 그 호출자를
세라** — R51/WP-3 은 세지 않고 적었다.

### ⚠⚠ **`rule=` 은 리터럴이어야 센다** — 검증을 공통 도우미로 접으면 조용히 빠진다 (R51 실물)

`tests/contract/test_dv_rule_enforcement.py` 의 AST 스캐너는 `ValidationError(...)`
호출에 `rule=` 이 **상수 리터럴**로 있을 때만 「배포 코드가 그 규칙을 던진다」로 센다 —
`_in_range(rule="DV-3")` 처럼 **변수로 포워딩하면 의도적으로 세지 않는다**(그 파일
독스트링이 그렇게 적는다). R51/WP-3 이 `core/der/ess.py` 의 줄 수를 되찾으려고 경계
검사 셋을 도우미로 접다가 **`DV-3` 를 「아무도 안 던진다」로 만들었다.**
⚠ **`DV-2` 가 안 걸린 것은 SOC 관계 검사에 별도 리터럴이 남아 있던 우연이다.**
**처방**: `rule=` 을 든 검사는 **접지 말고 원문으로 두고 그 사유를 그 자리에 적는다**
(R51 이 RTE 블록에 그렇게 했다).

### ⚠⚠ **yaml 에서 배타 규칙을 떼도 편익 클래스의 `exclusions()` 는 따라오지 않는다** (R51 실물)

R51/WP-5 가 `docs/exclusion-rules.yaml` 에서 `TouArbitrage` ↔ `SurplusSale` 유형 `A` 를
뗐는데 `TouArbitrage.exclusions()` 의 자기 선언이 남아
`tests/contract/test_exclusion_declaration_matches_table.py` 가 **「정본 표에 없는 배타를
클래스가 선언한다」로 빨간불**을 냈다. **처방**: 배타를 떼는 WP 는 **yaml 과 편익
클래스를 함께 `grep`** 한다. ⚠ 규칙 총수를 **문장으로** 적어 둔 자리(spec 머리말 ·
그 yaml 의 주석 · 시험의 단언)도 함께 고쳐야 한다.

### ⚠ **새 대장 값이 무관한 소스 리터럴과 우연히 부딪힌다** (R51 실물)

`check_hardcoded_params` 의 차단 판정은 **단위가 「원」이고 |값| ≥ 10,000 이면 맥락을
보지 않는다** — 그 무딤이 요점이다(사람이 한 번은 보게 만든다). R51/WP-2 가 고정 O&M
100,000원/년을 대장에 올리자 `core/assumption/timeseries.py` 의 **CSV 업로드 행수 상한
`100000`** 이 차단으로 걸렸다. **처방**: 값을 비틀거나 표기를 바꿔 검사를 피하지 말고
**`COINCIDENTAL_LITERALS` 에 사유와 함께 등재**한다 — 감추지 않고 **경고로 내려오며**,
그 리터럴이 사라지면 **낡은 면제라고 스스로 rc=1** 을 낸다.

### ⚠ **새 대장 이름공간에는 스윕 래칫이 없다 · 붙임 1 은 「미분류」로 빨간불이다** (R51 실물)

`tests/casegrid/test_ledger_levels.py` 의 스윕 래칫은 **접두어로 좁혀져 있다**
(`capex.`·`tariff.`·R51 이 더한 `opex.`). 새 접두어를 열면 **그 항목이 축인지 아무도
묻지 않는다** — 래칫을 함께 세워라(도우미 `_assert_prefix_is_swept_or_says_why_not` 이
이미 있다). 그리고 대장에 항목을 더하면 `core/report/appendix_sections.py::TOPIC_PREFIXES`
에 접두어가 없으면 붙임 1 이 **「미분류」로 빨간불**이다.

### ⚠ **워커는 「음성 스위트 금지」와 「`pytest -q` 금지」를 어긴다** (R51 실물 · 둘 다)

한 워커가 음성 스위트 15종을 전건 실행했고(소스를 변이시킨다 — 오염은 없었으나 지시
위반이다), 같은 워커가 `pytest -q` 를 써서 **결과 줄을 잃고 그것을 「원인 모를 로그
절단」으로 적었다**(`addopts` 에 이미 `-q` 가 있어 `-qq` 가 된다). **처방**: 지시문에
**금지만 적지 말고 이유와 실물 사례를 함께** 적는다 — 그렇게 한 뒤로는 어기지 않았다.

### ⚠⚠ 병합이 검사 파일을 더하면 **추적표를 다시 만들어야 한다** (R48 실물)

`gen_traceability.py` 는 **부수효과로 `docs/traceability.md` 를 다시 만드는** 게이트다.
R48 이 마지막 WP 에서 한 번 돌렸는데 **그 뒤에 브랜치 하나를 더 병합했고**, 그 병합이
`FR-404-AC1` 마커를 단 검사 둘을 들여왔다. 커밋된 매핑표의 **그 한 행**이 실물과
어긋났고 — **로컬 `pytest` 는 이것을 잡지 못한다.** CI 의 「매핑표 재생성 후 커밋본과
대조」만 잡았다(「로컬 초록불 ≠ CI 초록불」의 실물 사례).

**규칙** — **병합·리베이스로 테스트 파일이 들어온 뒤에는 `gen_traceability.py` 를 다시
돌리고 `git diff --stat docs/traceability.md` 로 반영을 확인한다.** 라운드 막바지에
한 번 돌린 것으로는 부족하다.

### ⚠⚠ `pytest -q` 는 결과 줄을 **지운다** (R48 실물 · 워커 둘이 밟았다)

`pyproject.toml` 의 `addopts` 에 이미 `-q` 가 있어 `-qq` 가 되고, 그러면 pytest 가
`N passed` 요약 줄을 **아예 인쇄하지 않는다.** rc 만 남아 「몇 건이 지나갔는가」를
읽을 수 없다 — 「게이트를 파이프에 물리지 마라」와 같은 유형(**결과를 읽을 수 없게
만드는 실행 방식**)이며, `-q` 를 **빼고** 돌리면 나온다.

### 검사 도구를 설명하는 문장이 그 검사에 걸린다

| # | 발생 | 원인 |
|---|---|---|
| 1 | spec §16.5.2 | "존재하지 않는 원칙 번호를 인용하지 말라"는 규칙이 나쁜 예를 **리터럴로** 적어 앵커 검사에 걸림 |
| 2 | 작업 목록 검사 설명표 | "AC가 5개인데 1~6 인용" 예시가 실제 범위 초과로 파싱됨 |
| 3 | 2.15 「부수 영향」 | ID 변경 대상을 나열한 서술이 2.0의 인용으로 집계되어 **유령 중복 5건** |
| 4 | (같은 원인, 반대 방향) | 서술 속 언급이 "인용됨"으로 세어져 **미인용 6건을 가림** |
| 5 | 2.15 `FR-601-AC5` 사유 | §16.5.2가 폐기한 나쁜 예를 **리터럴로** 적어 정본 대조에 걸림 |
| 6 | 6.7 공유 파일 검사 | 「공유 파일에 자원 이름 0건」을 문자열 포함으로 검사하자 **그 검사를 설명하는 주석·독스트링이 걸림** |
| 8 | 2.3 `SC-3` 검증 케이스 | 개인정보 **탐지기를 검증하는 테스트**가 탐지 대상을 리터럴로 들고 있어 스스로 위반이 됐다. 해법은 앞선 일곱과 같다 — **면제를 넓히지 않고 대상을 바꾼다.** 픽스처를 조각으로 조립해 소스에 식별정보 모양이 남지 않게 했다. `tests/` 를 통째로 면제했다면 **진짜 유출이 픽스처에 섞여도 보이지 않는다** |
| 7 | 2.6 테스트 동반 검사 | **반대 방향이며 더 나쁘다.** 「대응 테스트가 이 모듈을 참조하는가」를 문자열 포함으로 검사하면 *"`core.der.pv` 를 예로 들면"* 이라고 적은 주석이 **대응 테스트로 세어져 게이트가 조용히 초록불**이 된다. 6번은 정당한 서술이 위반으로 보고되어 «시끄러운» 실패였지만, 7번은 위반이 통과로 보고되는 «조용한» 실패다 |

**교훈**: 문서·코드가 자기 자신을 검사하는 구조에서는 **서술과 선언을 구분하는 장치**가 필요하다. 해법은 매체에 따라 다르지만 원리는 같다 — 문서는 인용을 `- 수용기준:` 필드에서만 수집하고, 코드는 `ast` 로 식별자·문자열 리터럴만 뽑아 주석·독스트링을 뺀다.

> **이 함정은 앞으로도 나온다.** 「자기 자신을 검사하는 도구」를 새로 쓸 때마다 **먼저 물을 것**: *이 검사가 내가 지금 쓰고 있는 설명 문장에 걸리는가?* 걸린다면 검사 대상을 좁히는 것이 답이고, **설명을 고치는 것은 답이 아니다.** 설명을 고쳐 통과시키면 다음 사람이 같은 설명을 다시 쓴다.
>
> 그리고 **제외를 넓게 잡는 반대 방향의 실패**를 함께 막아야 한다. 주석·독스트링을 빼려다 코드까지 빼면 진짜 위반이 있어도 초록불이 된다 — 6.7에서는 「심어 둔 등록부를 잡는가」와 「서술만 있는 파일을 오판하지 않는가」를 둘 다 검사해 양쪽을 고정했다.

### 수용기준 ID를 추정해서 쓰면 안 된다

**붙드는 것**: `scripts/check_task_mapping.py` 의 「미인용 Must-have」 검사.
⚠ spec 개정 때마다 AC 번호가 밀려 **재발한다.** 경위는 `status-history.md`.

### 검사가 **자기 검사 대상에서 정본을 읽어 오면** 공허해진다 (R35)

「사본을 만들지 말라」와 「검사는 정본을 밖에서 가져와야 한다」가 **부딪히는
자리**가 있다. R35 가 실물로 밟았다.

    ledger_key = ledger_backed_variables()[변수]      # ← 검사할 대상에서 읽는다
    assert 편익 == 잉여량 × 대장[ledger_key] 값        # ← 그래서 무엇을 바꿔도 맞는다

수치를 검사에 적지 않으려고 매핑표에서 키를 읽어 왔는데, **그 매핑표가 바로
변이의 대상**이었다. 키를 바꾸는 변이가 검사를 따라오게 되어 *「러너는 표가 말하는
키를 쓴다」* 만 확인했다 — **참이지만 공허하다.** 변이는 ★초록불이었다.

**해법은 「사본을 적는다」가 아니다** — 리터럴로 적으면 정본이 둘이 된다. **다른
층의 정본에 대고 맞춘다**: R35 는 조립기가 쓰는 상수(`SURPLUS_SALE_KEY`)에
맞췄고, 그러면 두 자리가 같은 것을 뜻한다는 사실 자체가 검사가 된다.

> **새 검사를 쓸 때 먼저 물을 것**: *이 단언에 등장하는 값 중 몇 개를 검사 대상이
> 스스로 정하는가?* 전부라면 그 검사는 **동어반복**이다. 최소한 하나는 **다른
> 곳**에서 와야 하고, 그 「다른 곳」이 어디인지 검사 문면이 말해야 한다.

### 수와 **그 수가 나온 조건**의 짝은 따로 붙들어야 한다 (R35 ②)

5.1 의 새 표는 「인자를 *이 값*으로 두면 *이만큼* 남는다」를 말한다. 두 끝의 결론
축을 **서로 바꿔** 실어 보니 — 거리 숫자는 그대로 맞고 열 순서도 그대로이며 표는
매끈했다. **문장만 거짓이 됐다**: *「판매단가를 150원까지 올려도 210만원 부족」* 이
실은 80원일 때의 수다. 거리를 보는 검사 일곱이 **전건 초록불**이었다.

    assert 남는 거리 == min(|끝1|, |끝2|)      # ← 바꿔 실어도 성립한다

**해법은 짝을 다른 층에서 검산하는 것이다** — 수준표의 `low`·`high` 로 진입점을
직접 돌려, 리포트가 **그 이름에 붙여 둔 수**와 맞추었다. R33 이 임계값에서 세운
검사(*「보고된 값으로 다시 돌려 본다」*)와 같은 형태이며, 그때와 같은 이유로
필요하다: **표시만 하는 구현은 표시를 보는 검사를 전부 통과한다.**

> **표에 수를 새로 실을 때 먼저 물을 것**: *이 수가 어느 조건에서 나온 것인지도
> 같은 행에 있는가? 그 짝이 어긋나도 지금 검사가 초록불인가?*

### ★ 규약 — `operating_lines.__all__` **전건이 러너에 재수출되어야 한다** (R43 확정)

**붙드는 것**: `tests/casegrid/test_operating_lines_reexport.py` — 이름이 있는지가
아니라 **같은 객체인지(`is`)** 를 본다. 규약을 유지한다는 판정과 경위는 `status-history.md`.

### ⚠ 설비 제원 상수는 `e2e_runner.py` 밖으로 **못 옮긴다** (R43-F2 실측)

리포트 문면이 그 상수들의 소유자를 *「`e2e_runner.py` 모듈 상수」* 라고 **이름으로
지목**하고 검사가 그것을 붙든다. 옮기면 **매니페스트 해시가 움직인다** — 즉
「행동 무변동」이 깨진다. 파일을 더 줄여야 하면 **다른 갈래**를 뗀다.

### ⚠⚠ **커밋이 하나뿐인 PR 은 게이트 ③ 이 `rc=2` 로 빨간불이다** (R62·R63 · 두 번 밟았다)

`check_commit_order`(`NFR-105-M3`)는 **구현↔테스트 짝의 순서**를 재고, 범위에 커밋이
하나뿐이면 **견줄 두 자리가 없어 `rc=2`(판정 불가)** 를 낸다. 조항이
*「위반 시 **경고**」* 라 했으므로 **위반은 `rc=0`** 이고, **`rc=2` 만 빨간불**이다 —
즉 **「통과」와 「판정 불가」가 정반대로 갈린다.**

⚠ **문서 한 줄만 고치는 PR 에서도 걸린다** — R63 이 `status.md` 첫 줄 정정 PR 에서
실물로 밟았다(R62 는 문서 PR 에서 같은 자리를 밟았다).

⇒ **처방: PR 마다 커밋을 최소 둘로 나눈다.** 코드 PR 은 **시험 → 구현** 순서가
그것을 저절로 만들고, **문서만 고치는 PR 은 고친 것과 「그 사실을 어디 적었나」를
갈라 두 커밋으로** 만든다. ⛔ 하나로 몰지 마라.

### ★★★ CI 잡이 **검사 실패를 삼킬 수 있다** — `| tee` + `pipefail` 없음 (R63)

`tests.yml` 의 e2e 스텝이 `pytest tests_e2e … | tee e2e.log` 로 돌고, 그 자리의
주석이 *「`pipefail` 이 켜져 있어(러너 기본 `bash -eo pipefail`) pytest 의 실패가
삼켜지지 않는다」* 라고 적고 있었다. **그 주장이 거짓이었다.**

**GitHub 의 `run:` 기본 셸은 `bash -e {0}` 이고 `-o pipefail` 은 `shell: bash` 를
명시할 때만** 붙는다(`bash --noprofile --norc -eo pipefail {0}`). 실물 확인:

    bash -eo pipefail -c 'false | tee /dev/null'  → rc=1
    bash -e           -c 'false | tee /dev/null'  → rc=0   ← GitHub 기본

⇒ **`| tee` 가 pytest 의 종료 코드를 0 으로 덮었고 그 잡은 검사가 빨간불이어도
초록불을 냈다.** R63 이 실물로 밟았다 — PR **#14** 의 `e2e` 로그가
`1 failed, 10 passed` 인데 **잡 결론은 `success`** 였고 **그 초록불을 믿고 병합됐다.**

★ 고침: **그 스텝에 `shell: bash` 를 명시했다.** ⚠ **지우지 마라.**
⚠ 다른 `| tee` 자리 셋(`source-rules.yml` 둘 · `pip-audit`)은 **후속 스텝이 종료
코드를 따로 재거나 `|| true` 를 명시**하므로 같은 문제가 아니다 — 세어 확인했다.
★★ **그것을 고치자 진짜 결함 둘이 나왔다** — `/ui/verify` 의 WCAG 2.1 AA 위반
(`color-contrast` 5건 · `scrollable-region-focusable` 4건).
⇒ **`gh pr checks` 의 `pass` 를 「검사가 통과했다」로 읽지 마라. 로그를 봐라.**

### ★★★ 「전제」를 일괄 치환하면 **일반 국어 270줄이 부서진다** (R63)

`이 검사의 전제가 깨졌다` · `24스텝을 전제한다` · `평년 전제` · `취득분이 하나라는
전제` 꼴이 **270줄**이다(R63/P2 가 800줄을 전수로 세어 찾았다).
⚠ 그리고 `tests/infra/` 셋은 「전제」를 **픽스처 값**으로 쓴다
(`AssumptionSet(name="전제 v1")`) — 뜻이 아니라 **데이터**다.

⇒ **낱말을 바꾸는 축은 ⓐ 전수 목록을 먼저 내고 ⓑ 「바꾼다 / 바꾼다(이름) /
안 바꾼다」로 분류한 뒤 ⓒ 짚은 줄만 손으로 고친다.**
⛔ **치환 명령을 쓰지 마라.** 어떤 정규식도 이 270줄을 가려내지 못한다.

### ★★ `pytest` 잔재가 게이트 **둘**을 오염시킨다 — `git status` 는 깨끗하다 (R63)

`pytest` 가 `tests/ci/_tmp_pytest/<case>/wt` 밑에 **임시 작업 트리**를 만든다
(`git worktree` 로 실제 등록된다). 그 상태에서:

- **`gen_traceability.py`** 가 그 임시 파일들을 **매핑표에 세어 넣어**
  `docs/traceability.md` 가 **490줄** 달라졌다
- **`check_docstring_references`** 가 **`rc=1`** — 임시 트리 안의 파일이 가리키는
  `core/model/settlement.py` 를 「실물에 없다」로 잡았다

⚠⚠ **그 자리는 `.gitignore` 안이라 `git status` 에 안 보인다.** 두 게이트가
`.gitignore` 를 보지 않으므로 **트리가 깨끗한데 게이트가 빨간불**이 된다.
⇒ **게이트를 돌리기 전에 `rm -rf tests/ci/_tmp_pytest && git worktree prune`.**

### ★★ 병렬 축은 **편집이 아니라 계약에서** 부딪힌다 (R63)

작업 트리를 갈라 파일 소유권을 나누면 **편집 충돌은 없어지지만 계약 충돌은 남는다.**
R63 실측 — 축 셋이 한 화면 라우터에서 만났다:

- 오버라이드 축이 인자 이름을 `known_keys` → `base_values` 로 바꿨고 **넘기는 것이
  「키」에서 「키 → 값」으로** 바뀌었다. 화면 축의 호출이 `TypeError` (4건)
- 저장 축이 여섯 문에 `requesting_user_id` 를 요구하게 했다. 화면 축의 호출 다섯이
  인가에서 막혀 **303 대신 화면이 돌아왔다** (2건)

⇒ **공개 함수의 서명·인자 이름을 바꾸는 축은 「부르는 자리」가 다른 축의 파일에
있는지 먼저 세라.** ⚠ 잡아 준 것은 **`check_docstring_references`**(문면)와
**CI 의 `tests`**(호출)이고 **둘 다 병합 뒤**였다.
⚠⚠ **`docs/traceability.md` 는 축마다 갱신하므로 병합에서 반드시 부딪힌다** —
손으로 합치지 말고 **`git checkout origin/main -- docs/traceability.md` 뒤
`gen_traceability.py` 로 다시 생성**한다. R63 이 손으로 합치려다 **충돌 표지가 든
파일을 커밋**했고(`pre-commit` 의 `check for merge conflicts` 훅도 통과시켰다)
CI 가 잡았다.

### ⚠ 작업 트리는 CRLF · 편집 도구는 LF 로 쓴다 (R63)

`core.autocrlf=true` 라 체크아웃이 CRLF 인데 편집 도구가 LF 로 쓰면 **한 파일 안에
섞인다**(실측: CRLF 243 · LF 67). ⇒ 새로 만든 파일과 고친 파일의 줄바꿈을 확인하고
맞춘다. ⛔ **읽는 파일과 쓰는 파일이 같아지는 셸 리다이렉트로 고치지 마라.**

### ⚠ 태그 속성을 **글자까지 고정한 정규식**은 접근성 고침에 깨진다 (R63)

`tests/app/test_ui_verify.py` 의 `_STAGE_BODY` 가 `<pre class="stage-body">` 를
글자까지 고정하고 있어 `tabindex="0"` 을 더한 순간 **찾은 본문이 0개**가 되어
`assert 0 == 9` 로 빨간불이 됐다. **화면은 멀쩡했고 깨진 것은 재는 쪽이다.**
⇒ 태그 속성은 접근성·스타일 때문에 **늘어나는 것이 정상**이므로 재는 것을
**클래스 이름 하나**로 좁힌다(`<pre class="stage-body"[^>]*>`).
⚠⚠ **그리고 템플릿을 고쳤으면 `tests/app` 을 다시 돌려라** — R63 이 `tests_e2e` 만
다시 돌려 이 자리를 CI 에서 맞았다.

### 정본은 세션 중에도 바뀐다

**붙드는 것**: `docs/source-rules.lock` 의 sha256 과
`python scripts/check_source_rules.py --require-vault`(볼트로 못 읽으면 종료 코드 2) ·
작업 스케줄러 매일 09:10. **날짜 해상도로는 부족하다.** 경위는 `status-history.md`.

---


---

## 이력

라운드별 경위(**R0~R44** — 전부) · 해소된 항목 · 낡은 절차는
**`status-history.md`** 에 있다. R40 이 R38 블록 둘을, R41 이 R39 를, R42 가 R41·R40 을,
**R43-J 가 R42 를**, **R45 가 R44 를** 머리로 내렸다. R43-A 가 함께 내린 여덟(R37·R36·R35 ③②①·R34·R32·
「1차 의견」)은 R41~R38 보다 오래된 것이라 **머리가 아니라 R38 ① 블록 아래**에 있다 —
**받는 자리는 머리가 아니라 시간 역순이 지켜지는 자리**다(오케스트레이터 판정).
**새 세션은 그것을 읽지 않는다** — 현재 상태를 그 파일에서 읽으면 낡은 수를 쓴다.
★ **몇 판 쌓였는지는 이제 `scripts/check_status_round_blocks.py` 가 잰다**(상한 1판 · CI 차단).
★★ **2026-08-30(R45)이 이 파일에서 이력을 더 갈라냈다** — 「미해결」의 해소 행 20 ·
「함정」의 경위 셋 · 지워진 `todo.md` 의 머리. 받는 자리는 전부 `status-history.md` 다.

`docs/decisions-2026-08-14-R31.md` · `-R32.md` 는 이력이 아니라 **판정 정본**이며,
되돌리려면 그 근거를 먼저 반박해야 한다.
