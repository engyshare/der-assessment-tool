# 진행 상태 — 2026-08-20 (**R36 완료 · R37 진행 중** · 편익이 자기 대입값을 내놓는다)

> ## ⚠ 새 세션이 가장 먼저 알아야 할 것
>
> **R36 은 닫혔다** — 남아 있던 게이트 둘을 2026-08-20 에 돌려 **음성 스위트 11종
> rc=0 · Python 3.11 rc=0**(실패 0)을 확인하고 커밋했다: `7fd39fe`(본체 22곳).
> ⚠ **둘을 이어 돌리지 않았다** — 따로, 각각 배경 실행해야 10분 시간초과를 피한다.
>
> **R37 은 오케스트레이터-워커 구성으로 돈다**(herdr 세션 `dev`). 배분·판정은
> 아래 「지금 할 일」 R37 블록이 갖고, 워커 지시문과 산출물은 `.orch/R37/` 에 있다
> (그 디렉터리는 `.gitignore` 안이라 저장소에 들어오지 않는다).

> # ▶ 이 파일이 **라운드 인계 정본**이다. `## 지금 할 일` 부터 읽는다
>
> 새 세션은 다른 무엇보다 **`## 지금 할 일`** 을 읽는다 — 직전 라운드 종료 선언 ·
> 다음 라운드 번호표 · 첫 30분 절차 · 조항을 건드릴 때 함께 움직이는 자리가 거기 있다.
>
> ### ▶ **다음에 무엇을 집는가는 `todo.md` 가 목록으로 갖는다** (2026-08-17 신설)
>
> `todo.md` 는 **착수 순서와 시작 자리**만 갖는 짧은 체크리스트다 — 판정 근거 ·
> 실측 수치 · 라운드 경위는 **여기(`status.md`)가 정본**이며 그 파일에 옮겨 적지
> 않는다. 두 파일이 어긋나면 **이 파일이 맞다.**
>
> ✔ **R35 ② 분량까지 `origin/main` 에 올라갔다** — R35 ① 은 `dae6222`(본체) ·
> `8aae6a9`(인계) · `3d71759`(todo.md) 이며 **CI 둘(`tests` · `source-rules`)
> 전건 `success`**(2026-08-17 13:19 실행), R35 ② 는 `28e5267` 이다.
> **R35 ③ 은 아직 커밋 전이다.**
>
> ### 이 파일은 **짧게 유지한다** — R32 에 222KB 에서 잘라 냈다
>
> 종전 이 파일은 **1,586줄 / 222KB** 였고 통째로 읽으면 토큰 한도에 걸렸다. 그래서
> **아무도 끝까지 읽지 않았고, 그 결과 `## 산출물 현황` 절이 spec v0.14 · 수용기준
> 308 에 멈춰 있었다**(실제는 v0.16 · 321). **길이가 낡음을 만든 것이다** — 이 파일이
> 스스로 *「세지 않은 수를 옮겨 적으면 그것이 다음 라운드의 계획으로 따라 들어간다」*
> 고 적어 둔 그 형태다.
>
> | | 여기(`status.md`) | `status-human.md` | `status-history.md` |
> |---|---|---|---|
> | 무엇 | **지금 상태 · 지금 할 일(코드) · 게이트 실측 수치 · 함정** | **사람 몫** — 수동 검증 · 외부 자료 · 배포 · spec 개정 판단 | 라운드별 경위(R0~R31) · 해소된 항목 · 낡은 절차 |
> | 누가 | 개발(세션) | 사용자·사업 주관 측·검토자 | — |
> | 갱신 | **매 라운드** | 사람 몫이 늘거나 끝날 때 | 라운드가 끝날 때 이전 「지금 할 일」 블록을 내린다 |
> | 새 세션이 | **읽는다** | 사람 몫을 물을 때 읽는다 | **읽지 않는다** — 필요할 때만 찾아본다 |
>
> ⚠ **사람 몫을 이 파일에 적지 않는다** — `status-human.md` 가 그것의 소유자다.
> 같은 사실이 두 곳에 있으면 한쪽만 고쳐진다.
>
> **라운드를 닫을 때 하는 일**: ① 「지금 할 일」의 옛 블록을 `status-history.md`
> **머리로** 옮긴다 ② 수치를 **다시 세어** 적는다(베끼지 않는다) ③ 「산출물 현황」의
> 판본을 실물과 대조한다. **이력을 이 파일에 쌓지 않는 것이 규칙이다.**
>
> ### ⚠ 라운드 인계는 `.orch/state.md` **가 아니다**
>
> **오케스트레이션은 R24(2026-08-13)에 끝났다.** `.orch/state.md` 는 그 시점에서
> 멈춘 이력이고 `.gitignore` 밖이라 새 클론에는 아예 없다. 그 폴더의 판정 중 유효한
> 것은 `docs/clause-recheck-2026-08-14.md` 로 옮겼으며, **유효한 도구는 변이 하네스
> `.orch/mutate.py` 하나**다(「지금 할 일」의 파급 지점 절).

---

## 한 줄 요약

3-File System **Step 3(exe-tasks) 진행 중**. spec **v0.16** + 작업 목록 **v2.8**.

**Phase 1 Must-have 미매핑 0건.** 남은 미매핑 8건은 전부 Phase 2·3 이다.
**수집 1,581건 · `pytest` rc=0**(2026-08-18 R36 실측 · **3.11 은 미실행**) · 게이트 `rc=0`
(`ruff` 0건 · `mypy` **114파일** 무결 · `lint-imports` **4계약 kept 0 broken**
(⚠ `PYTHONUTF8=1` 이 필요하다 — 게이트 절) ·
`check_marker_substance` 0건 · `check_file_size --code-strict` 0건 ·
`check_hardcoded_params` 0건 · `check_source_rules` 0건 ·
`check_partition_assignment` 0건 · `check_disclosure` 0건 · `check_assumptions` 통과 ·
`check_precommit_installed` 통과 ·
**`check_task_mapping` 0건 — R31 에 CI 차단으로 승격했고 R32 의 조항 전개에서
실제로 빨간불을 냈다**(실재하지 않는 인용 1 + 미인용 Must-have 7)).
**요구사항 105건 / 수용기준 321건 / 자동 291 / 수동 8 / 수동 스텁 4.**
**배타 규칙 7건**(`docs/exclusion-rules.yaml`) · **Python 3.11(CI 버전)에서도 rc=0**.

> **R32 결정·판정은 `docs/decisions-2026-08-14-R32.md`, R31 은
> `docs/decisions-2026-08-14-R31.md` 가 정본이다** — 이 파일에 옮겨 적지 않는다
> (같은 사실이 두 곳에 있으면 한쪽만 고쳐진다).


---

## 지금 할 일 (우선순위 순)

> # ⏹ 2026-08-20 **R37 완료 — 검증이 없는 조항이 「검증됨」으로 세어지고 있었다**
>
> 사용자가 **6시간 부재**를 알리며 *「사람이 확인해야 하는 사항은 합리적으로 판단해
> 확인한 것으로 처리하고 계속 진행」* 하라고 지시했다. 그래서 이 라운드는 판정을 미루지
> 않았다 — **대신 내린 판정과 그 사유를 아래 표에 적는다. 되돌리려면 사유를 반박한다.**
>
> 오케스트레이터-워커 구성으로 돌았다(herdr 세션 `dev` · 워커 8대 · 지시문과 산출물은
> `.orch/R37/` 에 있고 그 디렉터리는 `.gitignore` 안이다). 커밋 셋 — 요약 행 ·
> 마커 위생 + spec **v0.17** · 얇은 리프 검사.
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 | 검증 |
> |---|---|---|
> | **1절 요약에 「결론 전환 지원율」 한 행** — 검토자가 먼저 보는 칸의 「없음」에 크기를 준다 | 양식 【1】 · `narrative.py::_summary_section` · `test_conclusion_gap.py` | 변이 **6건 전건 빨간불** · **독립 검토 문제 0건** |
> | **`FR-402-AC2.C` 를 정직하게 닫았다** — 거짓 인용 제거 + 조항 Phase 를 3 으로 명확화 | `test_exclusion.py` · spec 672행 | `check_task_mapping` **rc=0** · 미매핑 8 → **9** |
> | **과잉 태그 7건 제거 · 1건 교체** | `test_settlement*.py` · `test_aggregated_ppa.py` · `test_exclusion*.py` | 매 건 **잔여 인용 실측** — 유일 인용을 잃는 조항 0건 |
> | **얇은 리프 둘에 구조 고유 검증 4건** | `test_surplus_direct_sale_*.py` · `test_vpp_rejection_matches_spec_precursor.py` | 변이 **5건 전건 빨간불** |
>
> ### ★★★ 이 라운드의 요점 — 매핑표가 「검증됨」으로 세던 조항에 검증이 없었다
>
> R33 이 *「기계 몫은 끝났다. 남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나이며
> **읽어서 대조하는 수밖에 없다**」* 고 적어 둔 것을 읽었다. **실재했다.**
>
> `FR-402-AC2.C`(같은 tCO2 에 배출권 수익과 사회적 탄소비용을 동시 계상하지 않는다)의
> 인용은 **전체가 1건**이었고, 그 테스트는 자가소비·REC·첨두절감·열비용절감 **넷의 동시
> 계상이 정당함**을 본다 — `tCO2` 도 배출권도 관점 전환도 코드에 없다. **그 테스트의
> 독스트링은 스스로 「그것이 `FR-402-AC1` 이다」 라고 적고 있었다.**
>
> ★ 함께 실측된 것: `docs/exclusion-rules.yaml` 에 **유형 C 규칙이 0건**이다
> (A·A·B·D·D·A·A). 검사만 없는 것이 아니라 **구현도 없다.** 그리고 그것이 결함이 아닌
> 이유가 있다 — 조항의 주제인 `CarbonCredit` 은 v0.1부터 **`[Phase 3]`**(spec 643행)이다.
> **어긋난 것은 구현이 아니라 조항의 Phase 표기였다.**
>
> ### ★★ 1차 가설이 틀렸고 **검증자가 그것을 잡았다**
>
> 오케스트레이터의 1차 가설은 *「그 조항을 재는 검사가 마커 없이 다른 곳에 있다」*
> (부하·열부하 자원이 `value_streams()` 를 갖지 않는 것)였고 **기각됐다** — 그것은
> *같은 관점 안에서 두 자원이 같은 물리량을 이중 계상*하는 문제이고 `AC2.C` 는 *관점을
> 바꿔가며 두 산식으로* 화폐화하는 것을 막는 조항이다(그 관점 전환은 조항이 스스로
> `FR-704` 에 위임한다). **다른 원리다.**
>
> ⚠ 가설대로 마커를 옮겼다면 **틀린 자리가 다시 「검증됨」으로 세어졌을 것이다.**
> 조사자와 검증자를 가른 것이 값을 냈다 — 다음 라운드도 그렇게 한다.
>
> ### 일반화 — 같은 형태가 더 있는가 (**둘 다 0건**)
>
> | 방향 | 범위 | 결과 |
> |---|---|---|
> | 인용이 **1건뿐인 조항** (실측 133건 중 배타·관점 계열 8건) | `audit_singles.md` | **어긋남 0건** — 여덟 전부 그 하나가 조항을 정확히 잰다 |
> | **Phase 1 Must-have 인데 대상이 Phase 2·3** 인 조항 | `phase_mismatch_sweep.md` | **신규 0건** — 후보 셋은 「횡단 조항」·「예시」·「별개 대상(물리량 vs 화폐화)」로 정상 |
>
> ⇒ **고립된 한 건이었다.** 「전수 대조를 더 해야 한다」로 남기지 않고 두 방향으로 재서
> 닫았다.
>
> ### 판정 — **가드 검사에 일곱 구조를 다 태그한 것은 어긋남이 아니다**
>
> 샤드 A 판독 둘이 정면으로 갈렸다(어긋남 **22/28 vs 0/28**). **리프별 「구조 고유 검증」
> 건수를 실측해 판정했다** — 가드 5건(어휘·구획·소유자·거부)을 뺀 수:
>
> | 리프 | 구조 고유 | | 리프 | 구조 고유 |
> |---|---|---|---|---|
> | `NetMetering` | 12 | | `AggregatedPPA` | 3 |
> | `ManagerEntity` | 10 | | `VPP` | 2 → **3** (R37) |
> | `HouseholdDirect` | 6 | | `SurplusDirectSale` | 1 → **5** (R37) |
> | `DistrictDirectTrade` | 5 | | | |
>
> **가드만으로 매핑된 리프는 0건이었다.** 그래서 22건을 떼지 않았다 — 떼면 매핑이 얇아
> 보이는 것을 고칠 뿐이고 실제 구멍은 없다. **대신 얇은 둘에 검사를 세웠다.**
>
> ### 실측 — 게이트 (2026-08-20 · R37 · **전건**)
>
> `pytest` **rc=0** · **Python 3.11 rc=0(실패 0)** · **음성 스위트 11종 rc=0** ·
> `ruff` 0건(신규 검사에서 E501 한 건이 났고 고쳤다) · `mypy` **114파일** 무결 ·
> `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(정상) ·
> **`check_task_mapping` rc=0** · `check_*` 9종 rc=0 ·
> `check_file_size --code-strict` rc=0 · 잔여 `.mutbak` **0건**.
>
> **추적표**: 요구사항 105 · 수용기준 **321**(불변) · 자동 **290** · 수동 4 ·
> **Must-have 미매핑 9**(전건 Phase 2·3 · Phase 1 은 0건).
>
> ### 사람이 판단할 것 — **spec 개정 등재 초안 2건**
>
> `.orch/R37/spec_amendment_draft.md` 에 문면·사유·개정안·파급까지 적혀 있다. 이 라운드는
> **필요해서 생긴 개정 하나(Phase 표기)만** 실행하고 **둘은 실행하지 않았다** — 조항이
> *요구하는 것*을 바꾸는 일이므로 사람 판단이다.
>
> 1. **배타 규칙의 「구조 축」이 조항에 없다** — 구현은 규칙을 계약구조로 한정하는데
>    `FR-402-AC4` 의 필드 목록에 `적용 계약구조` 가 없다.
> 2. **`FR-205` 의 「선택 파라미터」를 재는 수용기준이 없다** — 조항 본문(483행)은
>    「선택 파라미터로 지원해야 한다」인데 AC 는 리프 일곱뿐이고 **무접미 `FR-205-AC1` 은
>    실재하지 않는다**(실측). 그래서 「구조를 주지 않으면 종전대로 계산된다」를 재는 검사가
>    **조항 없이 떠 있다**(`test_e2e_settlement_wiring.py:137` · 지금 `.NetMetering` 을
>    달고 있으며 상계거래와 무관하다). **옮길 자리가 없어 그대로 두었다** — 없는 ID 를
>    만들어 넣는 것이 더 나쁘다.
>
> ### 함께 돈 것 — **일사 곡선 (todo 4) 은 별도 worktree 에 있다**
>
> `D:/Dev/260802_DER_evaluator_wt_sun` · 브랜치 **`r37-irradiance`**. 정찰에서 드러난
> 것: **기본 경로는 `daily_shapes` 를 넘기지 않아 24스텝 전부 0.45kWh 를 낸다 — 심야에도
> 태양광이 발전한다.** 형상 자산은 이미 있고(`fixtures/profiles/representative-day.yaml` ·
> 야간 가중치 정확히 0.0) 붙임 7 이 그것으로 「가정 운전」 표를 그리지만 **프로포마·NPV 는
> 평탄한 쪽으로 돈다.**
>
> ⚠ **이 분량을 `main` 에 합치지 않았다.** 이유 셋 — ⓐ 검토자가 보는 **머릿수(순현재가치·
> 회수기간)가 움직이는** 변경이라 사용자가 실측을 먼저 보는 것이 옳다 ⓑ 골든(16.4)·수용
> 검사가 값을 못 박고 있어 함께 갱신해야 하며 그 건수와 사유를 사람이 확인할 자리다
> ⓒ 같은 파일(`test_conclusion_gap.py`)을 이 라운드가 이미 고쳐 리베이스가 필요하다.
> **다음 라운드의 시작점이며 `todo.md` 1번이다.**

> # ⏹ 2026-08-18~20 **R36 완료 — 편익이 자기 대입값을 내놓는다** (붙임 4)
>
> `todo.md` 종전 2번. 사용자가 *「push하고 진행」* 으로 지시했고 R35 ③ 은 push ·
> CI 둘 `success` 까지 끝냈다. 이어 이 항목을 집었고 **08-18 세션이 할당량으로
> 끝나 커밋 전에 멈췄다.** 08-20 에 게이트 둘을 돌려 닫았다 — 본체 `7fd39fe`.
>
> ★ **둘을 이어 돌리지 않았다.** 08-18 의 10분 시간초과는 이어 돌린 탓이며,
> 따로 배경 실행하면 Python 3.11 은 약 2분, 음성 스위트 11종은 약 6분이다.
>
> ### 무엇이 있었나
>
> 양식 §3 붙임 4 는 *「편익별 산식·**대입값**(발전량·단가)」* 을 요구하는데 실물은
> 갈래마다 **`대표일 1,771원 × 365일`** 한 줄이었다 — 곱해서 나온 금액만 있고
> 무엇에 얼마를 곱했는지가 없다. 같은 리포트의 **비용** 쪽은 같은 자리에
> `대표일 수전 6.19kWh × 365일 × 120원/kWh` 로 갈라 적는다.
>
> ★ **양식을 고칠 일이 아니었다** — 양식은 이미 그것을 요구하고 있었고 구현이
> 따라가지 못한 것이다. 그래서 `docs/report-form-심의보고서.md` 는 손대지 않았다.
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **`ValueStream.formula()` 추상 메서드** 신설 — 편익이 자기 대입값을 내놓는다 | `core/contracts/valuestream.py` |
> | 편익 **8종**이 각자 산식 구현 — 곱 · 차 · 합 · 섞임 **네 모양** | `core/valuestream/*.py` |
> | 러너가 **산식을 짓지 않는다** — `× 365일` 과 `= 합계` 만 붙인다 | `e2e_runner.py::_benefit_line` |
> | 첨두 절감도 **같은 통로로** — 라벨·산식이 러너에 박혀 있던 마지막 예외였다 | 같음 |
> | 탐침 표를 계약 검사들이 **공유** | `tests/contract/valuestream_probes.py` |
> | 검사 **9건** 신설 (계약 4 · 러너 3 · 진입점 1 · 붙임 4 회귀 1) | `test_benefit_formula.py` · `test_benefit_line_rendering.py` · `test_annualisation_convention.py` · `test_surplus_sale_price_wiring.py` |
> | 리포트 · 추적표 재생성 | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md` · `docs/traceability.md` |
>
> ### ★★★ 실측 — 붙임 4 가 지금 싣는 것
>
> ```
> 이전  잉여전력 판매   대표일 1,771원 × 365일 = 646,415원
> 이후  잉여전력 판매   대표일 잉여 역송 16.10kWh × 판매단가 110원/kWh × 365일 = 646,415원
>
> 이전  첨두 절감      월 감축 2.00kW × 8,320원/kW·월 × 12개월 = 199,680원
> 이후  기본요금(피크) 절감  월 감축 합계 24.00kW·월 (12개월 · 월평균 2.00kW)
>                        × 기본요금 8,320원/kW·월 = 199,680원 (연간 수량으로 산정 · 연간화 없음)
> ```
>
> ★ **금액은 한 자리도 바뀌지 않았다.** 바뀐 것은 *「왜 그 금액인가」* 를 붙임 4 가
> 답하게 된 것뿐이다. R35 ① 이 영향도 1위로 실측한 **판매단가 110원/kWh 의 값이
> 이제 리포트에 있다.**
>
> ### ★★ 함께 드러난 잠재 결함 둘
>
> **① 첨두 산식이 월별 균일을 가정하고 있었다.** 옛 문면은 `peak_reduction_kw`
> **한 달치**를 인쇄하고 12를 곱했다. 지금 러너가 `[같은 값] * 12` 를 넘기므로
> 맞지만, **월별이 갈리는 순간 산식만 거짓이 되고 금액은 맞는다.** 새 문면은
> 실제 열두 값의 **합**을 읽는다.
>
> **② `DistributedBenefit` 은 두 계약 검사에서 아무것도 재지 않고 지나가고
> 있었다.** 탐침이 `sub_items=None` 이라 연간화 검사는 *「전부 0원이면 건너뛴다」*
> 로 빠졌고, 산식 검사는 대조할 값이 없었다. 다섯 항목에 **서로 다른** 값을 넣어
> 둘 다 실제로 재게 했다.
>
> ### ★★★ 변이 — **11건 중 둘이 초록불이었고 그 자리가 요점이다**
>
> | # | 변이 | 판정 | 잡은 검사 |
> |---|---|---|---|
> | 1 | 잉여판매 산식에서 **단가를 뺀다** | 빨간불 3건 | 대입값 · 흔들기 · 붙임 4 회귀 |
> | 2 | 산식을 **탐침 값이 박힌 상수 문자열**로 | 빨간불 **1건** | `…reads_the_value_rather_than_reciting_it` **만** |
> | 3 | 편익이 **연간화를 함께** 적는다 | 빨간불 | `…leaves_annualisation_and_the_total_to_the_caller` |
> | 4 | ★ 러너가 문면에서 **`× 365일` 만 뺀다**(금액은 그대로) | ★**초록불** → 검사 신설 후 **빨간불** | `…printed_formula_says_whether_it_was_annualised` **신설** |
> | 5 | 산식의 수량을 **다른 창**에서 읽는다 | 빨간불 다수 | 붙임 4 회귀 외 |
> | 6 | 첨두 산식이 합계 대신 **월평균만** 싣는다 | 빨간불 | 대입값 |
> | 7 | 분산편익 산식이 **합계만** 싣는다 | 빨간불 | 대입값 |
> | 8 | 직접거래 산식이 **수수료 항을 뺀다** | 빨간불 2건 | 대입값 · 흔들기 |
> | 9 | ★ 러너가 **산식을 버리고 금액만** 싣는다(연간 갈래) | ★**초록불** → 검사 신설 후 **빨간불** | `test_benefit_line_rendering.py` **신설 3건** |
> | 10 | 문면의 합계가 실린 금액과 **다르다** | 빨간불 | `…states_the_total_it_was_given` |
> | 11 | 분산편익 산식이 **항목 하나를 빠뜨린다** | 빨간불 | 대입값 |
>
> ### ★★★ M4 가 드러낸 것 — **금액이 맞아도 문장은 거짓일 수 있다**
>
> 러너가 문면에서 `× 365일` 만 빼면 `대표일 잉여 역송 16.10kWh × 판매단가
> 110원/kWh = 646,415원` 이 실린다. **금액은 맞는데 산술이 닫히지 않는다**
> (16.10 × 110 = 1,771). 검토자가 손으로 검산하면 365배 어긋난 값을 얻고,
> 그러면 **틀린 것은 리포트가 아니라 자기 계산이라고 읽는다.**
>
> ⚠ **대입값을 싣게 한 것이 이 위험을 새로 만들었다.** 금액만 싣던 동안 문면은
> 아무것도 주장하지 않았다 — 이제 문면이 **스스로 산술을 주장**하고, 그 주장은
> 금액과 **따로** 틀릴 수 있다. R35 ② 의 *「두 끝의 값을 서로 바꿔 실어도 거리만
> 보는 검사 일곱이 통과했다」* 와 같은 형태이며, 답도 같다 — **다른 층에서**
> 검산한다(기대값은 레지스트리의 `scales_with_dispatch_window` 선언, 실물은
> 인쇄된 문면).
>
> ### ★★★ M9 가 드러낸 것 — **고친 결함의 반대쪽 가지가 비어 있었다**
>
> 계약 검사는 `formula()` **의 출력**을 보고 붙임 4 회귀 검사는 **잉여판매 한
> 갈래**를 본다. 그 사이 *「러너가 그것을 쓰는가」* 가 비어 있어서, **연간 편익
> 갈래**의 문면을 옛 `연 199,680원` 으로 되돌려도 전건 초록불이었다. 창 비례
> 갈래에서 고친 것과 **똑같은 결함**이 반대쪽에서 살아 있던 것이다.
>
> ⚠ 그 검사는 비공개 도우미(`_benefit_line`)를 직접 부른다 — `CaseOutcome` 이
> 편익 **인스턴스**를 내보내지 않아 진입점만으로는 두 문면을 나란히 둘 수 없다.
> 내보내게 고치는 것은 **밖에서 보이는 수가 한 자리도 움직이지 않는 변경**이라
> R34 가 「붙들 수 없는 갈래」로 미뤄 둔 그 항목이며(`todo.md` 프로포마 항목),
> **검사를 세우려고 산출물 자료형을 넓히는 것은 순서가 거꾸로다.**
>
> ### 실측 — 게이트 (08-18 아홉 + **08-20 둘**, 전건 통과)
>
> `pytest` **수집 1,581건 rc=0**(1,572 → +9) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(정상) ·
> `check_*` **9종** rc=0 · `check_file_size --code-strict` rc=0 ·
> 잔여 `.mutbak` **0건**.
> ✔ **음성 스위트 11종 rc=0** · ✔ **Python 3.11 실패 0 · rc=0** (2026-08-20 실측).
>
> ⚠ **3.11 실행은 수집 건수를 세지 않았다.** `-q` 진행 표시에 테스트가 인쇄한
> 표준출력이 섞여 점을 셀 수 없었고, **세지 않은 수를 옮겨 적지 않는다**는 이
> 파일의 규칙에 따라 「실패 0 · rc=0」까지만 적는다. 건수가 필요하면
> `--tb=no -p no:randomly` 없이 다시 세는 것이 아니라 `-q > log` 의 마지막 줄을
> 확인할 것.
>
> ---

> # ⏹ 2026-08-18 **R35 ③ 완료 — 요약 표의 행 목록을 검사로 붙들었다** (【1】)
>
> `todo.md` 2번. 사용자가 *「2번부터 시작해줘」* 로 지시했다.
>
> ### ★★★ 먼저 — **todo 2번의 전제가 틀려 있었다**
>
> `todo.md`·R35 ② 인계는 *「양식 【1】은 여섯 행을 규정하는데 실물은 여덟 행을
> 낸다」* 고 적었다. **양식은 이미 여덟 행이었다**(`적정 용량`·`잠정성` 포함 ·
> `bc4a8cc` 부터). 여섯이라 적혀 있던 곳은 **`narrative.py::_summary_section`
> 의 독스트링 한 줄**뿐이고, 그 낡은 사본이 **두 라운드의 계획으로 따라
> 들어갔다.** 이 파일이 스스로 적어 둔 *「세지 않은 수를 옮겨 적으면 그것이
> 다음 라운드의 계획으로 따라 들어간다」* 의 실례다 — 이번에는 `status.md` 가
> 아니라 **소스 독스트링**이 그 자리였다.
>
> ⚠ 그래서 그 독스트링은 이제 **행을 세지 않는다.** 목록의 소유자는 양식이고,
> 소스는 그 사실과 대조 검사의 이름만 가리킨다.
>
> ### 진짜 어긋남 — **양식이 자기 자신과 갈려 있었다**
>
> | 양식 【1】 | 실물 | 양식 §2 `6.2` |
> |---|---|---|
> | 「**결합** 전환 조건」 | 「**결론** 전환 조건 (결합)」 | 「**결론** 전환 조건」 |
>
> 새 검사가 **첫 실행에서** 잡았다. 실물·6.2 쪽으로 **양식**을 고쳤다(한쪽이
> 둘과 갈려 있으면 그쪽이 낡은 것이다).
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **요약 표 행 목록 대조 검사** 신설 — 양식 【1】을 파싱해 실물 `## 1. 요약` 표의 첫 열과 **행 수·순서·이름**을 맞춘다 | `tests/report/test_form_conformance.py::test_summary_rows_match_the_form` |
> | ★ 접두 규칙에 **구분자 조건**을 더했다 — 접두를 뗀 나머지가 `""` · ` · ` · ` (` 중 하나로 시작해야 한다 | 같음 `::_assert_prefix_rows` |
> | 표 첫 열 읽기를 표제 검사와 **공유**로 뺐다 | 같음 `::_first_column` · `::_slice_between` |
> | 양식 【1】 행 이름 정정 · 머리말에 「표의 행 목록도 붙든다」 선언 | `docs/report-form-심의보고서.md` |
> | 낡은 「여섯 행」 독스트링 제거 | `core/report/narrative.py::_summary_section` |
>
> ### ★★★ 왜 `startswith` 만으로는 부족한가 — **M4 가 초록불이었다**
>
> 실물 첫 행의 이름을 「결론」 → 「**결론 축 · 순현재가치**」로 바꾸면 **같은
> 이름의 행이 둘**이 되는데, 「결론」은 그 이름의 접두이므로 **접두 검사는
> 여덟 행 전건 통과한다**(2026-08-18 실측). 구분자 조건을 걸면 나머지가
> ` 축 · 순현재가치` 라 걸린다.
>
> ⚠ **순서 뒤바꿈은 접두 검사도 잡는다**(맞바꾼 반대쪽에서 걸린다 — M3).
> 이 규칙이 사는 값은 **이름이 자라는 변이** 쪽이다. 검사 독스트링에 그렇게
> 적었다 — 처음에 「순서 뒤바꿈을 못 잡는다」고 적었다가 실측으로 고쳤다.
>
> ### 변이 — **여섯 전건 빨간불**
>
> | # | 변이 | 대상 | 판정 |
> |---|---|---|---|
> | 1 | 요약에서 `잠정성` 행을 지운다 | 실물 | 빨간불 |
> | 2 | 양식에 없는 `비고` 행을 더한다 | 실물 | 빨간불 |
> | 3 | `결론` 과 `결론 축` 의 순서를 맞바꾼다 | 실물 | 빨간불 |
> | 4 | ★ `결론` 행이 `결론 축` 의 이름을 달고 나온다 | 실물 | 빨간불 (**접두 검사만이면 초록불**) |
> | 5 | 양식 쪽 행 이름을 「결합 전환 조건」으로 되돌린다 | **양식** | 빨간불 |
> | 6 | 실물 행 이름을 `미계상 항목` 으로 바꾼다 | 실물 | 빨간불 |
>
> ★ 1·6 은 **이 검사 하나만** 빨간불이었다 — 그 자리를 보던 것이 없었다는 증거다.
>
> ### ★★★ 판단 — **거리 행은 요약에 올리지 않는다**
>
> `todo.md` 2번이 붙들고 나서 판단하라 한 것에 답한다. **올리지 않는다** —
> 요약의 `결론 축 · 순현재가치` 가 **−5,156,392원**이고 5.1 의 「결론까지 남은
> 거리」가 **결손 5,156,392원**이다. **같은 수를 부호만 뒤집어 두 번 싣는 것**이며,
> 양식이 요약에 금지한 *「새 수를 만들지 않는다」* 에 걸린다.
>
> ★ **요약에 없는 것은 거리가 아니라 「무엇에 대한 거리인가」다** — 총사업비
> 대비 **52.6%**, 그리고 그것과 같은 수인 **결론 전환 지원율 52.6%**. 절대액은
> 사업 규모를 모르면 크기를 못 읽지만 비율은 읽힌다. 요약의
> `결론 전환 인자 (단독)` 칸은 여전히 **「없음 (1변수 스윕 범위 내)」** 이고,
> R35 ② 가 5.1 에서 없앤 그 「없음」이 **첫 화면에는 그대로 남아 있다.**
>
> ⚠ **이번 라운드에서 하지 않았다** — 요약에 행을 더하는 것은 검토자가 먼저
> 보는 규정을 바꾸는 일이라 사용자 판단으로 올린다(`todo.md` 새 1번).
>
> ### 실측 — 게이트 (2026-08-18 · R35 ③)
>
> `pytest` **수집 1,572건 rc=0**(3.11 도 rc=0) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상 ·
> `docs/traceability.md` 변경 없음) · `check_task_mapping` rc=0 · `check_*` 7종 rc=0 ·
> `check_file_size --code-strict` rc=0 · **음성 스위트 11종 rc=0** ·
> 잔여 `.mutbak` 0건.
>
> ⚠ **리포트를 재생성하지 않았다** — 이번 변경은 검사 · 양식 문면 · 독스트링뿐이고
> 렌더러의 출력 바이트를 건드리지 않았다. `docs/evidence/` 는 그대로다.
>
> ---

> # ⏹ 2026-08-17 **R35 ② 완료 — 「없음」 한 줄을 거리로 바꿨다** (5.1)
>
> 사용자가 todo.md 「0. 결정 대기」 둘에 답했다 — **push 를 먼저** 하고(위 ✔ ·
> CI 둘 `success`), 다음 항목은 **1번(5.1 이 비었을 때 무엇을 싣는가)**. 닫았다.
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **결론까지 남은 거리**를 5.1 이 싣는다 — 결론 축의 절대값 · 방향 라벨(`결손`/`여유`) · **총사업비 대비 비율**. 분모는 **무지원 기준선의 초기지출**이다(지원 후 초기지출을 쓰면 같은 사업의 결손 비율이 지원율에 따라 달라진다) | `case_report.py::{conclusion_gap_won,total_project_cost_won}` · `narrative.py::_gap_lines` |
> | ★ **결론 전환 지원율** — 그 거리를 `t=0` 일시 지원으로 환산한 값. 환산은 **한 함수**(`break_even_subsidy_rate`)가 하고 본문과 붙임 3 이 그것을 함께 쓴다 | 같음 · `_formulas` |
> | **전환까지 남는 거리 표**(7열) — 인자를 검토 범위 **끝까지** 밀었을 때 0 선까지 남는 거리. 방향은 인자마다 적어 두지 않고 **결론에서 읽는다** | `narrative.py::{_remaining_gap_table,_flip_direction_end}` · `InfluenceEntry.npv_low`·`npv_high` 신설 |
> | 붙임 3 에 **네 번째 산식** — 「결론 전환 지원율」 3중 표기 | `_formulas` · 양식 §3 붙임 3 행 |
> | 양식 5.1 에 **전환 인자 0건일 때 싣는 것**을 규정(세 항목 · 분모·표의 성격까지) | `docs/report-form-심의보고서.md` |
> | 검사 8건 신설 · 변이 9건 전건 빨간불 | `tests/report/test_conclusion_gap.py` |
> | 리포트 재생성 (본문 163 → **173줄** · 상한 200) | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md` |
>
> ### ★★★ 왜 「없음」으로는 안 되나
>
> **「없음」은 *조금 모자란다* 와 *두 배 모자란다* 를 같은 글자로 적는다.** 5.1 이
> 양식에서 지는 물음은 *「무엇이 얼마가 되면 뒤집히는가」* 이고, 뒤집히는 인자가
> 0건이면 그 물음은 **「그럼 얼마나 모자란가」** 로 남는다. 그 답이 이제 수로 있다 —
> 결손 **5,156,392원**은 **총사업비 9,800,000원의 52.6%** 이며, 그것이 곧
> *「보조율 52.6% 에서 0 이 된다」* 다.
>
> ⚠ **판정을 싣지 않았다**(양식 0절). 「부족하다」·「가깝다」는 작성자의 말이므로
> 방향은 `결손`·`여유` 라벨 한 칸이 지고 크기는 수가 진다.
>
> ### ★★★ 실측 — 5.1 이 지금 싣는 표 (무보조 · 실물 대장)
>
> | 인자 | 사용값 | 전환 방향 끝 | 그 끝의 남은 거리 |
> |---|---|---|---|
> | `surplus_sale_price` | 110 원/kWh | **150** | **2,098,749원** |
> | `grid_purchase_price` | 120 원/kWh | 80 | 3,981,021원 |
> | `ess_unit_cost` | 500,000 원/kWh | 400,000 | 4,156,392원 |
> | `pv_unit_cost` | 1,600,000 원/kW | 1,280,000 | 4,196,392원 |
> | `tariff_escalation` | 2.5 %/년 | **—** | 5,156,392원 (기준과 같다) |
>
> ★ 첫 행이 R35 ① 이 손으로 적어 둔 *「상단 150 에서도 약 −210만원」* 과 같은 수다 —
> 그 수가 이제 **리포트에 실린다.**
>
> ★ **변동폭 순위와 남는 거리 순위는 서로를 함의하지 않는다.** 변동폭이 커도 반대
> 끝에서 시작하면 끝까지 밀어도 더 멀다. 지금 실물에서는 판매단가가 둘 다 1위지만
> 그것은 우연이며, 그래서 이 표는 붙임 2 의 변동폭 표와 **다른 표**다.
>
> ★ `tariff_escalation` 의 **방향 칸이 `—`** 인 것이 요점이다. 두 끝의 결론 축이
> 같으면 「전환 방향」이 없고, 한쪽을 적으면 *그쪽으로 밀면 가까워진다* 를 함의한다.
> 사실은 `산출` 열의 미반영 라벨이 나른다.
>
> ### ★★★ 변이 — **아홉 전건 빨간불 · 아홉 번째가 검사를 하나 더 만들었다**
>
> | # | 변이 | 판정 | 잡은 검사 |
> |---|---|---|---|
> | 1 | 거리 두 줄을 지운다 | 빨간불 2건 | `…how_far_the_conclusion_is` · `…recovering_scenario…` |
> | 2 | 전환 지원율에서 **현 지원율을 빼먹는다** | 빨간불 | `…agrees_across_two_support_levels` **만** |
> | 3 | 총사업비 분모를 **지원 후 초기지출**로 | 빨간불 2건 | 위 + `…formula_carries_the_same_number` |
> | 4 | 전환 방향을 **반대 끝**으로 읽는다 | 빨간불 2건 | `…shows_the_gap_that_remains` · `…recovering_scenario…` |
> | 5 | 미반영 라벨을 **항상 1변수 스윕**으로 | 빨간불 | `…not_shown_as_a_closed_gap` |
> | 6 | 붙임 3 산식이 **본문과 다른 수**를 싣는다 | 빨간불 | `…formula_carries_the_same_number` |
> | 7 | 남는 거리 표를 **싣지 않는다** | 빨간불 3건 | 위 셋 |
> | 8 | 거리에서 **절대값을 뗀다** | 빨간불 2건 | `…how_far…`(`gap > 0` 단언) · 미반영 |
> | 9 | ★ **두 끝의 결론 축을 서로 바꿔 싣는다** | ★**초록불 7건** → 검사 신설 후 **빨간불** | `…endpoint_values_are_paired_with_the_run` **신설** |
>
> ★★ **9번이 드러낸 것 — 거리만 보는 검사는 거짓 문장을 통과시킨다.** 두 끝의 NPV 를
> 서로 바꿔 실으면 거리 숫자는 그대로 맞고 열 순서도 그대로인데 **문장만 거짓**이 된다:
> *「판매단가를 150원까지 올려도 210만원 부족」* 이 실은 **80원일 때의 수**다. 그래서
> 짝을 **다른 층에서** 검산한다 — 수준표(`build_level_map`)의 `low`·`high` 로 진입점
> (`run_single_case_e2e`)을 직접 돌려 리포트가 그 이름에 붙여 둔 수와 맞추었다.
> R33 이 임계값에서 세운 검사(*「보고된 값으로 다시 돌려 본다」*)와 같은 형태다.
>
> ⚠ 2번도 같은 결의 확인이다 — **한 시나리오만 보는 검사로는 잡을 수 없었다.** 어떤
> 잘못된 환산도 자기 시나리오에서는 자기 자신과 맞는다. 무보조(0%)와 보조 80% 가
> **같은 전환 지원율**을 내야 한다는 대조가 그 자리를 붙든다.
>
> ### ⚠ 함께 고친 검사 둘 — 허용 라벨을 **목록으로** 넓혔다
>
> `test_combined_scenarios.py` 의 두 검사가 5.1 의 **모든** 인자 행에 `1변수 스윕`
> 라벨을 요구했다. 새 표의 미반영 인자 행은 붙임 2 와 같은 라벨(`파이프라인 미반영`)을
> 쓰므로 허용을 둘로 넓혔다. **「무엇이든 있으면 통과」로 넓히지 않았다** — 라벨을
> 지우는 변이는 여전히 빨간불이며, 그것이 그 검사가 지키는 것이다.
>
> ### 하지 않은 것 — **1절 요약에는 거리를 넣지 않았다**
>
> 요약의 전환 인자 칸은 여전히 「없음 (1변수 스윕 범위 내)」이다. 거리를 요약에도
> 넣으려면 양식 【1】의 행 목록을 함께 고쳐야 하는데, **그 목록은 이미 실물과 어긋나
> 있고**(양식 6행 · 실물 8행) 어느 검사도 그것을 붙들지 않는다. 요약 행을 늘리는 것은
> 그 어긋남을 깊게 만드는 일이므로 **양식 【1】을 검사로 붙드는 일과 함께** 해야
> 한다 — `todo.md` 에 올렸다.
>
> ### 실측 — 게이트 (2026-08-17 · R35 ②)
>
> `pytest` **수집 1,571건 rc=0**(3.11 도 rc=0) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상 ·
> 파일 재생성 반영 5줄) · `check_task_mapping` rc=0 · `check_*` 8종 rc=0 ·
> `check_file_size --code-strict` rc=0 · **음성 스위트 11종 rc=0** ·
> 잔여 `.mutbak` 0건 · 빈 디렉터리 둘 `rmdir`.
>
> ⚠ **위 R35 ① 블록의 「수집 1,562건」은 한 건 낮았다** — 같은 명령을 이 파일
> **없이** 다시 세면 1,563 이고, 새 검사 8건을 더해 1,571 이다. 이 절의 규칙대로
> **베끼지 않고 다시 셌더니** 드러났다.

> # ⏹ 2026-08-17 **R35 ①「잉여 판매단가를 대장으로」 완료**
>
> R34 가 「남은 판단」 1번으로 남긴 것을 닫았다. 사용자가 *「push 먼저 하고 1번
> 시작」* 이라 지시했으며 **커밋 3건은 `origin/main` 에 올라갔다**(R34 분량).
>
> ### 무엇을 했나
>
> | 무엇 | 어디에 |
> |---|---|
> | **잉여 판매단가를 수준표로 올렸다** — 종전 `e2e_runner` 안의 `SurplusSale(sale_price_won_per_kwh=120.0)` **리터럴**. 새 케이스 변수 `surplus_sale_price` · 대장 키는 **기존 항목 `tariff.surplus_direct_sale`(Q-16 · 110원/kWh · 80~150)** 이며 새 항목을 세우지 않았다(같은 물리량의 단가가 대장에 둘이 되므로) | `ledger_levels.py::_LEDGER_VARS` · `e2e_runner.py` |
> | **기본값을 두지 않았다** — 수준표에서 빼면 `_resolve` 가 거부한다. `horizon_years`(R31)·`grid_purchase_price`(R34)와 같은 판단 | 같음 |
> | 대장 부기 갱신 — `energy_only` 의 「우연히 같다」 경고를 **해소 기록**으로, `surplus_direct_sale` 의 `applicable_scope` 에 기본 경로를 명시하고 **`impact` 를 「중간」 → 「최상」** 으로(아래 실측이 근거다) | `docs/assumptions.yaml` |
> | 검사 6건 신설 · 변이 5건 전건 빨간불 | `tests/casegrid/test_surplus_sale_price_wiring.py` |
> | 리포트 재생성 · 추적표 재생성 | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md` · `docs/traceability.md` |
>
> ### ★★★ 왜 「대장 키를 `tariff.surplus_direct_sale` 로」 인가
>
> 구조를 주지 않은 기본 경로는 **상계도 특구 직접거래도 아닌 판매**이며 그것이 그
> 항목의 `applicable_scope` 문면 그대로다. 약관요금(`…avg` 150원/kWh)을 쓰면
> *소매가로 파는 사업*이 되고, 그 값은 `_net_metering` 이 **회피한 요금**으로
> 쓰는 것이다(`settlement.py::TARIFF_KEY`). 즉 세 단가는 각각 다른 것을 뜻한다 —
> **사는 값**(`energy_only` 120) · **파는 값**(`surplus_direct_sale` 110) ·
> **회피한 값**(`avg` 150).
>
> ### ★★★ 실측 — **영향도 1위가 바뀌었다** (리포트 재생성 후 재판독)
>
> | | R34 (판매단가 = 리터럴 120) | R35 (대장 110) |
> |---|---|---|
> | 순현재가치 (무보조) | −4,391,980원 | **−5,156,392원** |
> | 순현재가치 (보조 80%) | +3,448,020원 | **+2,683,608원** |
> | 1년차 편익 | 904,860원 | **846,095원** (잉여판매 705,180 → **646,415**) |
> | 영향도 1위 | `grid_purchase_price` (2,644,579원) | **`surplus_sale_price` (5,350,876원)** |
> | `pv_capacity_kw` 한계 기여 | +451,092원/kW | **+322,898원/kW** (단조 증가 · 최선 9kW — 형태 유지) |
> | `ess_capacity_kwh` 한계 기여 | −290,977원/kWh | **−326,699원/kWh** (단조 감소 · 최선 2kWh — 형태 유지) |
> | 단독 전환 인자 | 0건 | **0건** (상단 150 에서도 약 −210만원) |
>
> ★ **리터럴이던 동안 이 인자는 영향도 표에 오르지 못했다** — 흔들 수 없는 값은
> *영향이 없는 값과 같은 모양*으로 보인다. 그런데 재어 보니 **변동폭이 종전 1위의
> 두 배**이고 기준선 순현재가치와 같은 크기다. R33 이 `tariff_escalation` 에서
> 만난 형태의 **거울상**이다: 그쪽은 축에 있는데 파이프라인이 읽지 않아 「영향
> 0원」으로 실렸고, 이쪽은 파이프라인이 읽는데 축에 없어 **아예 실리지 않았다.**
>
> ⚠ **`NFR-202` 검사가 이 리터럴을 잡을 수 없었다** — 판정이 |값| ≥ 1,000 만 보므로
> 120 은 사각지대다(「미해결」의 그 행). 즉 이번 작업은 검사를 통과시키는 일이
> 아니라 **검사가 볼 수 없는 자리를 없애는 일**이었다.
>
> ✔ **차익거래의 우연이 해소됐다.** 두 단가가 120 으로 같은 동안 저장장치의 심야
> 충전·주간 판매는 왕복효율(90%) 손실만큼만 순손실이었다. 지금은 **사는 값이 파는
> 값보다 10원/kWh 비싸므로** 효율 손실 이전에 이미 손실이며, 저장장치 한계 기여가
> 더 내려갔다. **부호는 뒤집히지 않았다** — R34 의 결론(ESS 는 줄이고 PV 는 키운다)이
> 그 우연 위에 서 있던 것은 아니었다.
>
> ### ★★★ 변이 — **하나가 초록불이었고 그 자리가 요점이다**
>
> | # | 변이 | 판정 | 잡은 검사 |
> |---|---|---|---|
> | 1 | 판매단가를 리터럴 120 으로 되돌린다 | 빨간불 3건 | `…reaches_the_npv` · `…measured_surplus_times_the_price` · `…from_the_ledger` |
> | 2 | 빠진 판매단가를 기본값으로 메운다 | 빨간불 | `…refuses_to_run_without_a_sale_price` |
> | 3 | 대장 키를 **약관요금**(`…avg`)으로 바꾼다 | ★**초록불** → 검사 신설 후 **빨간불** | `…prices_the_surplus_like_the_surplus_structure` **신설** |
> | 4 | 사는 값과 파는 값을 **한 대장 줄**로 묶는다 | 빨간불 2건 | 위 + `…two_prices_are_separate_ledger_items` |
> | 5 | 대장 연결을 지운다 | 빨간불 3건 | 위 셋 |
>
> ### ★★★ 변이 3 이 드러낸 것 — **검사가 자기 검사 대상에서 정본을 읽어 왔다**
>
> `test_the_deploy_path_prices_the_surplus_from_the_ledger` 는 대장 키를
> `ledger_backed_variables()` 에서 **읽어** 와 그 항목의 값과 대조했다. 그래서
> `_LEDGER_VARS` 의 그 줄을 약관요금으로 바꾸는 변이가 **아무 검사도 빨간불로
> 만들지 않았다** — 검사가 변이를 따라가 *「러너는 표가 말하는 키를 쓴다」* 만
> 확인한 것이다. **참이지만 공허하다.**
>
> 그 변이가 통과하면 잉여를 **소매가로 파는 사업**이 되고, 저장장치 차익거래가
> 항상 본전이 된다. 금액은 그럴듯하고 아무 예외도 나지 않는다.
>
> 고친 방법은 **사본을 만들지 않는 쪽**이다 — 키를 검사에 리터럴로 적으면 대장
> 키를 옮길 때 두 곳을 고쳐야 한다. 대신 `settlement.py::SURPLUS_SALE_KEY`
> (조립기가 「잉여 직거래」에 쓰는 그 상수)에 대고 맞췄다. 두 자리가 같은 판매를
> 다른 단가로 세면 **구조 비교(`FR-202`)가 구조의 차이가 아니라 배선의 차이를
> 재게 된다** — 그 사실이 이제 검사 문면에 있다. 함께 `!= TARIFF_KEY` 도 본다.
>
> ⚠ **값의 크기는 단언하지 않았다.** 「파는 값이 사는 값보다 싸다」는 지금 실측이지
> 도메인 규칙이 아니다 — 두 범위(80~150 · 80~170)가 겹치므로 단언하면 근거 없는
> 규칙을 발명하게 된다. 실측은 대장 부기와 이 표에 적었다.
>
> ### 남은 판단 — R35 의 다음 항목들 (R34 목록에서 1번을 뺀 것)
>
> 1. **5.1 이 비었을 때 무엇을 싣는가** — 전환 인자 0건이면 검토자가 *「얼마나
>    부족한가」* 를 5.1 에서 읽을 수 없다. 지원 필요액을 올릴지 판단.
>    **6.2 는 R34 가 「없음」 행으로 닫았다.** ★ R35 실측으로 **붙임 2 의 변동폭이
>    실제 우선순위를 말하기 시작했다**(판매단가 535만원) — 5.1 판단의 재료가 늘었다
> 2. **프로포마 행 목록을 `CaseOutcome` 에 실을 것인가** — R34 「붙들 수 없는 갈래」
> 3. ➕ **붙임 4 의 편익 산식이 단가를 싣지 않는다** (R35 발견). 비용 쪽은
>    `대표일 수전 6.19kWh × 365일 × 120원/kWh` 로 **수량과 단가를 갈라** 적는데,
>    편익 쪽은 `대표일 1,771원 × 365일` 뿐이어서 **판매단가가 붙임 4 어디에도
>    없다**. 즉 영향도 1위 인자의 값을 검토자가 산식에서 확인할 수 없다(붙임
>    1·2 에는 있다). 고치려면 편익이 산식의 **대입값**을 내놓아야 하고, 그것은
>    편익 8종이 함께 움직일 때 할 일이다 — 러너에 태그 목록을 두면 R34 가 없앤
>    형태(`scales_with_dispatch_window` 이전)로 되돌아간다
> 4. `FR-401-AC2.VPPMarket`(Phase 2) — R33 표 4번 그대로
> 5. **`SettlementInputs.annual_generation_kwh` 가 협상값 자료형에 남아 있다** —
>    사유는 그 필드 주석에. 호출자가 사라지면 옮긴다
> 6. ⚠ **새 PV 결론을 그대로 믿지 말 것** — 이유는 여전히 **평탄 발전 프로파일**
>    (야간 발전)이며 붙임 8 이 크기를 잰다. 일사 곡선이 배선되면 다시 뒤집힐 수
>    있다. **R35 는 이것을 건드리지 않았다**
>
> ### 실측 — 게이트 (2026-08-17 · R35)
>
> `pytest` **수집 1,562건 rc=0**(3.11 도 rc=0) · `ruff` 0건 · `mypy` **114파일**
> 무결 · `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상 ·
> 파일 재생성 반영) · `check_task_mapping` rc=0 · `check_*` 8종 rc=0
> (`check_unread_extension_points` 포함) · `check_file_size --code-strict` rc=0 ·
> **음성 스위트 11종 rc=0** · 잔여 `.mutbak`·`# NEGCHECK` 0건.

> # ⏹ 2026-08-17 R34 종료 · **미확인 셋을 닫았다 — 게이트 전건 rc=0 · 변이 일곱 전건 빨간불**
>
> R34 의 본 작업(계통 전력 구매 비용 배선 · 집합 PPA 통로 · 365배 결함)은 앞
> 세션이 놓았고 **확인하지 못한 채 중단했다**. 이 세션이 닫은 것이 아래다.
>
> | # | 확인한 것 | 결과 |
> |---|---|---|
> | **1** | `pytest` 전건 | **수집 1,554건 · rc=0.** 직전 실행의 `rc=1`(계약 6건)은 닫혔다 — `__init_subclass__` 검사 순서 교체와 `_Stub` 선언이 실제로 들었다 |
> | **2** | 새 계약 검사 `tests/contract/test_annualisation_convention.py` | **4건 전건 통과**(`_PROBES` 표로 편익 8종을 세우는 부분 포함). `ruff` 가 이 파일에서 3건(import 정렬 · `RUF001` 둘)을 냈고 고쳤다 |
> | **3** | 게이트 전건 | 전부 `rc=0` — 아래 「실측」 절 |
> | **4** | **변이 6건 + 갈래 하나** | **셋이 초록불이었다.** 검사 다섯을 세우고 다시 심어 전건 빨간불 — 아래 표 |
> | **5** | 실물 리포트 재생성 + **눈으로 읽기** | `docs/evidence/MC-1-검토용-리포트-2026-08-17.md`. **낡은 문면 둘 + 빈 표 하나**를 찾아 고쳤다 |
>
> ### R34 가 놓은 것 — **셋** (앞 세션 · 이 세션이 게이트로 확인했다)
>
> | 무엇 | 어디에 |
> |---|---|
> | **계통 전력 구매 비용을 배선했다** — 새 대장 항목 `tariff.hv_single_contract.energy_only` 120원/kWh(**전력량요금만**. 실효단가 `…avg` 150원/kWh 를 그대로 쓰지 않은 이유: 그 값은 *기본요금 안분 포함* 이고 실행 경로가 그 기본요금을 이미 **첨두 절감 편익**(8,320원/kW·월)으로 세고 있어 **같은 성분이 비용·편익 양쪽에 들어간다**. 범위 80~170 · `q_ref` 는 **`Q-6` 을 그대로** 쓴다: 확보 대상이 같은 약관 하나이고, 새 Q 를 세우면 §16.6 현황판이 실제보다 결손이 많아 보인다) · 수준표 변수 `grid_purchase_price` · 비용 행 생성기 `energy_purchase_row()` · `CostLine` 신설 · 붙임 4 비용 항목 표 · 4.3 잔차 행 | `docs/assumptions.yaml` · `ledger_levels.py` · `core/cba/proforma.py` · `core/casegrid/{models,e2e_runner}.py` · `core/report/method_sections.py` |
> | **집합 PPA 통로** — `_with_model_generation()` 이 `PV.annual_generation_kwh(year=1)` 을 물어 조립기에 넣는다. **호출자가 값을 함께 주면 거부한다**(정본이 둘이 되므로) | `core/casegrid/e2e_runner.py` |
> | ★★★ **그 통로를 열자 365배 결함이 드러났다** — 러너가 정산 편익 **전건**에 365를 곱하고 있었다. 그 암묵 규약은 잉여판매·상계에서만 맞았고, 생성자에서 **연간** 수량을 받는 편익에서는 365배가 됐다(집합 PPA 502,605원/년 → **183,450,825원**). **아무 예외도 나지 않았다.** `ValueStream` 에 **`scales_with_dispatch_window`** 를 기본값 없이 신설하고 러너가 그 선언을 읽게 했다 | `core/contracts/valuestream.py` · 편익 8종 · `e2e_runner.py` |
>
> ### ★★★ 실측 — **결론이 뒤집혔다** (오늘 재생성한 리포트에서 다시 읽었다)
>
> | | 배선 전 | 배선 후 (2026-08-17 리포트) |
> |---|---|---|
> | 순현재가치 (무보조) | −865,881원 | **−4,391,980원** |
> | 1년차 운영비 | 200,000원 | **471,073원** (구매 271,073원 신설) |
> | `pv_capacity_kw` 한계 기여 | −61,681원/kW · 단조 감소 · 최선 1kW | **+451,092원/kW · 단조 증가 · 최선 9kW** |
> | `ess_capacity_kwh` 한계 기여 | +190,445원/kWh · 단조 증가 · 최선 23kWh | **−290,977원/kWh · 단조 감소 · 최선 2kWh** |
> | 영향도 1위 | `ess_unit_cost` | **`grid_purchase_price`** (변동폭 2,644,579원) |
> | 단독 전환 인자 | 있었다(할인율 3.44% 등) | **0건** — 검토 범위 안에서 0 선을 넘기는 인자가 없다 |
>
> **두 설계 변수의 부호가 모두 반대로 돌아섰다.** 종전 「ESS 를 키울수록 좋다」는
> *공짜로 받아 파는 기계*가 만든 결론이었다. ⚠ 새 PV 결론도 그대로 믿지 말 것 —
> 아래 「남은 판단」 6.
>
> ⚠ 전환 인자가 0건이 되며 **검사 넷이 무력화됐다**(셋은 `set() != set()` 로 정당한
> 상태를 빨간불로, 둘은 순회 0회로 조용한 초록불). `tests/report/conftest.py` 의
> **탐침 대장**(`flip_probe_assumptions` — 검토 범위 셋만 넓히고 기준선 수치는 실물과
> 같다)으로 갈랐다.
>
> ### ★★★ 변이 — **셋이 아무것도 붙들지 못했다**
>
> | # | 변이 | 첫 판정 | 조치 | 재판정 |
> |---|---|---|---|---|
> | 1 | `energy_purchase_row` 호출을 지운다 | 인계가 지목한 검사 셋(미반영 둘·서술)은 **전건 초록불**. 넓혀 돌리니 전환 인자 검사 둘이 빨간불이었으나 **값이 움직여 깨진 것**이고 구매 비용을 이름으로 붙드는 검사는 0건 | `test_the_purchase_price_reaches_the_npv`(단가 0 → 200 에 NPV 가 줄어드는가) · `test_the_cost_total_and_the_itemised_lines_agree` **신설** | **빨간불 2건** |
> | 2 | 비용 **항목**을 조건부로 (`if not annual_purchase_won: 행을 뺀다`) | — (검사가 없어 새로 만들고 심었다) | `tests/casegrid/test_grid_purchase_cost_row.py` **신설** — 단가 0 으로 금액 0을 만들고 **행의 존재**를 본다 | **빨간불** |
> | 2b | **프로포마 행**을 조건부로 | ★초록불 | **만들지 않았다** — 아래 「붙들 수 없는 갈래」 | 초록불(설계상) |
> | 3 | 러너가 `scales_with_dispatch_window` 를 무시하고 전건에 365 | 빨간불 | — | — |
> | 4 | `AggregatedPPA.scales_with_dispatch_window` 를 `True` 로 거짓 선언 | 빨간불 2건(선언·실물 대조 + 진입점) | — | — |
> | 5 | `_with_model_generation` 이 호출자 입력을 **조용히 덮어쓴다** | — (검사가 없었다) | `test_the_caller_may_not_hand_the_runner_a_second_generation_number` + **양성 짝**(용량 3배 → PPA 편익 3배) 신설 | **빨간불** |
> | 6a | 붙임 4 **비용 항목 표**를 지운다 | ★초록불 — `tests/report` **전건**에서 아무도 잡지 않았다 | `test_each_cost_item_shows_its_amount_and_formula` 신설 (편익 쪽 검사의 반대편) | **빨간불** |
> | 6b | 4.3 **자원 미귀속 잔차 행**을 지운다 | ★초록불 — 같음 | `test_the_resource_table_carries_the_costs_that_belong_to_no_resource` 신설 | **빨간불** |
>
> ⚠ **하네스가 경고한 그대로 밟았다** — 변이를 여럿 이어 돌리다 **2분 시간초과로
> 죽자 `finally` 가 건너뛰어져 `core/report/method_sections.py` 에 `if False:` 가
> 남았다.** `.mutbak` 로 원복했다. 변이는 **한 번에 하나씩, 대상을 좁혀** 돌린다.
>
> ### ★★ 붙들 수 없는 갈래 — **변이 2b 는 관측 가능한 차이를 만들지 않는다**
>
> 프로포마 쪽 구매 행을 `if annual_purchase_won:` 로 조건부화해도 **밖에서 보이는
> 수가 한 자리도 움직이지 않는다**: 그 갈래가 성립하는 것은 금액이 0일 때뿐이고,
> 0원 행을 빼도 합계·NPV·회수기간이 같다. `CaseOutcome` 이 현금흐름 **행 목록**을
> 내지 않으므로 검사가 볼 자리가 없다.
>
> **판단이 필요하다** — 그 자리를 검사로 붙들려면 `CaseOutcome` 에 프로포마 행을
> 실어야 한다. 지금은 러너의 주석과 `basis.costs` 쪽 검사만이 규약을 지킨다.
>
> ### ✔ 실물을 눈으로 읽고 찾은 셋 — **전부 「배선했더니 문장이 거짓이 됐다」**
>
> | 어디 | 무엇이 거짓이었나 | 어떻게 고쳤나 |
> |---|---|---|
> | 3.2 계산 규약 「프로포마 비용 행」 | *「고정 운영비」* — 계통 전력 구매가 비용 행이 된 뒤로 거짓 | **실린 항목에서 지어 낸다**(`basis.costs` 의 라벨) — 구성이 바뀌면 함께 움직인다 |
> | 붙임 8 「계통 전력 구매 비용」 판정 문면 | *「금액 미정량 (구매 단가 없음)」*·*「구매 단가(소매 요금) 부재」* — 단가는 대장에 있다 | **금액을 잰다**(수량 × 단가) · 사유를 「행이 없다」로 · 해소 조건을 「행 배선」으로 |
> | `case_report.py` 가정 운전 주석 | *「자가소비 절감은 소매 단가가 없어 계상할 수 없다」* (인계가 지목한 남은 한 곳) | 진짜 사유로 갱신 — **배타 규칙 유형 A** 와 **부하 미확보(`Q-3`)**. 단가는 있고 붙임 8 이 그 단가로 실제로 잰다 |
>
> ➕ **6.2 「결론 전환 조건」이 머리만 남은 빈 표로 인쇄됐다** — 전환 인자가 0건이
> 되며 행이 하나도 없었다. 검토자에게 빈 표는 *「없다」* 와 *「싣지 못했다」* 를
> 구별해 주지 않는다(1절 요약은 이미 「없음 (검토 범위 내)」로 적고 있었다).
> `NONE_IN_RANGE` 상수를 세워 두 자리가 같은 문면을 쓰게 하고,
> `test_the_flip_condition_table_is_never_left_empty` 로 붙들었다.
>
> ### 실측 — 게이트 (2026-08-17)
>
> `pytest` **수집 1,554건 rc=0** · `ruff` 0건 · `mypy` **114파일** 무결 ·
> `lint-imports` 4 kept 0 broken · `gen_traceability` rc=1(미매핑 8 — 정상) ·
> `check_task_mapping` rc=0 · `check_*` 7종 rc=0 · `check_file_size --code-strict`
> rc=0 · **음성 스위트 11종 rc=0** · **CI 버전(3.11) rc=0**.
>
> ⚠ **`lint-imports` 가 `PYTHONUTF8=1` 없이 rc=1 이었다** — 계약 위반이 아니라
> 자기 배너를 cp949 로 디코드하다 죽은 것이다. 게이트 절의 그 줄을 고쳤다.
>
> ★★ **커밋 뒤 `check_test_accompaniment --base HEAD~1` 이 실제로 빨간불을 냈다** —
> 붙임 4·4.3 을 새로 붙든 검사가 `render_markdown` 만 거쳐 **`method_sections` 를
> 이름으로 참조하지 않았다**. 그 상태에서는 그 파일을 고치는 사람이 이 검사를
> 함께 보지 않는다. 절을 만드는 함수(`resource_detail_section` ·
> `cost_benefit_section`)에 **직접 묻고** 그 결과가 문서에 실리는지를 따로 보게
> 고쳤다 — 검사가 더 강해졌다(절이 문서에서 빠지는 것도 함께 붙든다).
>
> ★★ **같은 검사가 `core/cba/proforma.py` 도 잡았다** — `energy_purchase_row()` 를
> 신설했는데 **그 함수를 부르는 검사가 하나도 없었다**(러너 경유로만 닿았다).
> `tests/cba/test_proforma.py` 에 셋을 세웠다: 라벨이 「전력 구매」를 나르는가 ·
> 연도 범위 전건에 같은 금액인가(에스컬레이션 없음이 지금은 옳다 — 편익 쪽이
> 아직 안 올라간다) · **음수 구매 비용을 거부하는가**. 셋째가 요점이다 —
> 통과시키면 *「전력을 사면 돈을 받는 사업」* 이 되어 **저장장치를 키울수록
> 좋아지는 결과**가 부호만 바꿔 되돌아온다. **변이(가드 무력화) 심어 빨간불 확인.**
>
> ### 남은 판단 — **목록은 위 R35 블록이 갖는다**
>
> R34 가 남긴 여섯 중 **1번(잉여 판매단가를 대장으로)은 R35 가 닫았다.** 나머지
> 다섯은 위 「남은 판단 — R35 의 다음 항목들」로 옮겼다 — **여기 다시 적지 않는다**
> (같은 목록이 두 곳에 있으면 한쪽만 고쳐진다). 그 자리에 R35 가 찾은 것 하나
> (붙임 4 의 편익 산식이 단가를 싣지 않는다)가 더 붙었다.
>
> ⚠ 아래 R34 표의 수치(순현재가치 −4,391,980원 등)는 **그 시점의 실측**이며
> 판매단가 배선 뒤 값은 위 R35 표에 있다. 두 표를 견주는 것이 이 파일의 용도다.

> # ⏹ 2026-08-14 R32 종료 · **R32 표 1~5 를 완주하고 사용자 몫을 잠정 확정했다**
>
> 사용자가 *「모든 작업에 우선순위를 정하고 그 순위대로 완료하라」* 와
> *「사용자가 판단해야 하는 부분은 합리적으로 판단하고 추후 검토 과정에서 변경될 수
> 있음을 기재하라」* 고 지시했다. **R32 표의 1~5 를 순서대로 전건 완료했고**, R31 이
> 「사업 판단·자료 공백」으로 갈라 두었던 것을 판정해
> **`docs/decisions-2026-08-14-R32.md`** 에 근거·**「무엇이 오면 바뀌나」**와 함께 적었다.
>
> | # | 무엇을 했나 | 어디에 |
> |---|---|---|
> | **1** | **변형 생산자를 실행 경로에 배선**(`FR-607-AC1`) — `CaseOutcome` 신설, 진입점이 **인자 없이 항상** 변형을 산출한다(깃발로 두면 R21 이 없앤 `is_baseline` 형태가 된다) | `core/casegrid/{models,execution,e2e_runner}.py` · `tests/casegrid/test_variant_production_wiring.py` |
> | **2** | **「단일계약+관리주체 경유」 조립** — 요금엔진(`bill_scenario`)이 두 요금을 내고, 수수료(`Q-14`)는 **비용 행**(`fee_row`) | `core/valuestream/settlement.py` · `core/cba/proforma.py` · `tests/valuestream/test_settlement_manager_structure.py` |
> | **3** | **「개별 세대 직접계약」** — 정산 대상을 **잉여·판매 주체 가구**로 결정하고 **조항에 적었다**(spec v0.16) | `settlement.py::_household_direct_contract` · `surplus_sale.py` · `tests/valuestream/test_settlement_household_contract.py` |
> | **4** | **「집합 PPA」 편익 클래스 신설** + `FR-401-AC2.AggregatedPPA` 조항 신설 + 배타 2행 | `core/valuestream/aggregated_ppa.py` · `docs/exclusion-rules.yaml` · `tests/valuestream/test_aggregated_ppa.py` |
> | **5** | **`FR-205-AC1` 을 구조 7건으로 전개** — 수용기준 314 → **321**, **미매핑은 8건 그대로** | spec `FR-205` · 작업 목록 7.3·8.1 · 마커 49곳 |
> | 6 | `LoadedSeeds` 이동은 **하지 않았다** — 소비자가 0곳이라 옮기면 호출자 없는 제품 모듈이 생긴다(R32 표가 지시한 그대로) | — |
>
> ### ★★★ R32 가 찾은 것 — **셋 다 「선언·계산은 있는데 읽는 쪽이 없다」**
>
> | 무엇 | 어떻게 드러났나 |
> |---|---|
> | **비용 행이 NPV 를 늘리고 있었다** | 수수료를 비용으로 넣자 **수수료율을 올릴수록 NPV 가 커졌다.** `CashFlowRow` 는 부호 규약이 없고 `npv()` 는 부호 있는 합을 보는데 진입점이 비용을 그대로 넘겼다 — **고정 O&M 이 편익으로 더해지고 있었다.** 비용 항목이 O&M 둘뿐일 때는 아무도 밟지 않았다 |
> | **`CaseVariant.overrides()` 소비자 0곳** | 순회가 `baseline` 깃발만 봤다. 변형이 둘일 때는 두 기계가 같은 답을 내고 **셋째에서 갈린다** — 보조율 15% 변형이 입력 지원안과 **똑같은 NPV** 를 낸다 |
> | **편익 `exclusions()` 소비자 0곳** | 클래스 선언에서 자가소비 배타를 **지우는 변이가 전건 초록불**이었다. 정본은 YAML 이므로 선언이 그것과 어긋나지 못하게 래칫을 세웠다 |
>
> **인수 변이 16건을 직접 심었고 전건 빨간불**이며, **각 변이가 서로 다른 검사에
> 잡혔다**(같은 한 줄에서만 잡히면 경로를 따로 붙들지 않는다는 뜻이다).
>
> # ▶▶ **지금 바로 할 일 — 리포트 「1차 의견」 대응 6건** (2026-08-15 접수)
>
> 사용자가 리포트에 **1차 의견**을 냈고 판정까지 마쳤다. **`/clear` 이후 이
> 절부터 읽고 번호 순서대로 진행한다.** 판정 근거와 실측은 아래 표에 있다.
>
> ⚠ **사용자 지시 둘**:
> ① *「전제와 구성은 보고서 검토 결과의 **신뢰도**와 관련된 사항이며 `MC-1` 과
> **별도로** 수행할 부분」* — 즉 3kW PV·10kWh ESS 구성이 타당한가는 이 작업의
> 범위가 아니다. 섞지 말 것.
> ② *「현 단계에서는 **`MC-1` 의 해소에 집중**」*.
>
> # ⏹⏹ **「1차 의견」 6건 전건 완료 · 2026-08-15**
>
> **① 부터 ⑥ 까지 닫았다.** 그 과정에서 사용자가 **방향을 바꾸는 지시**를 냈고
> 그것이 이 라운드에서 가장 큰 변경이다 — 아래 「정형 출력」 절을 **먼저** 읽는다.
>
> ### ★★★ 방향 전환 — **리포트는 정형 출력이며 해설을 싣지 않는다**
>
> 사용자 지시: *「보고서는 프로그램에 따른 정형화된 결과를 출력하는 것이 아닌가?
> LLM 이 설명을 추가하는 방식은 의도한 것이 아님」* · *「이 프로그램이 web 에서
> 제공할 때 LLM 을 이용하지 않기 때문」*.
>
> **근거가 타당하다.** 런타임에 모델이 없으면 리포트의 모든 문장은 미리 박아 둔
> 고정 문면이고, 고정 문면은 데이터를 따라가지 못해 **구성이 바뀌는 순간 계속
> 인쇄되는 틀린 문장**이 된다(이 저장소가 이미 `_lifetime_caveat` 에서 같은
> 판단을 한 번 내렸다). 게다가 해석 문장은 **어떤 검사도 참·거짓을 판정하지
> 못하고**, 심의 문서에서 작성 주체가 불분명하다. `FR-1005-AC1`(비트 단위 동일
> 재실행)도 생성 시점 모델 호출과 양립하지 않는다.
>
> **기준선은 「글자 vs 숫자」가 아니라 「프로그램이 참임을 보증할 수 있는가」다.**
> 부호 규약·연간화 규약·용어 **정의**·`산출: 1변수 스윕` 같은 라벨은 고정 문면
> 이지만 **계산의 정의**이므로 남는다. 걷어낸 것은 판정·권고·독법 지시다.
>
> | 하던 말 | 지금 자리 |
> |---|---|
> | 「이 표는 단독 기여다」 | `산출` 열에 `1변수 스윕` |
> | 「영향 없음으로 읽지 말 것」 | `산출` 열에 `파이프라인 미반영 (변동폭 0원)` |
> | 「그만큼 결과에 관대하다」 | 붙임 8 `방향` 열에 `반영 시 결과 악화` |
> | 「건의 — 다음에 무엇을 할 것인가」 | **삭제.** 6절은 판정·전환 조건·미해소 항목 표만 |
>
> **양식 0절이 이 규정의 정본이다**(`docs/report-form-심의보고서.md`). 렌더러가
> 그것을 따르며, 검사 넷이 인용문(`>`)과 독법 지시 어휘를 붙든다
> (`test_operation_appendices.py`·`test_unreflected.py`·`test_overview_sections.py`).
>
> ⚠ **남은 결과 하나 — `FR-1001-AC5`(MC-1) 문면과의 어긋남.** 조항은 *「비개발자
> 검토자가 **리포트만 보고** 설명할 수 있다」* 인데, 사용자 판정은 *「프로그램이
> 필요한 정보를 가독성 있게 제공하고 **결과물을 사람이 평가**」* 다. 안내를 사람이
> 작성해 함께 준다면 「리포트만 보고」가 성립하지 않는다. **코드로 정할 일이
> 아니므로 `status-human.md` 에 spec 개정 후보로 올렸다.**
>
> ### ★★ 이 라운드가 찾은 것 — **계통에서 산 전력이 공짜였다**
>
> 붙임 7(시간대별 표)을 **실제로 내 보고서야** 드러났다. ESS 가 심야 여섯 스텝에서
> 충전하고 그 전력이 **계통에서 들어온다**(대표일 6.19kWh · 연간화 2,259kWh).
> 프로포마의 비용 행은 고정 O&M 둘뿐이므로 **그 전력을 값 없이 쓰고 있다.**
>
> ⚠ **종전 판정이 틀렸다.** 표 위의 「가구 부하가 없으므로 구매 자체가 없다」는
> **부하와 구매를 같은 것으로 본 것**이며, 부하가 없어도 ESS 충전은 구매를 만든다.
> `_purchase_item` 의 판정 조건도 처음에 「수전이 0인가」로 잡았다가 같은 이유로
> 결함을 놓칠 뻔했다 — 조건은 **「수전이 있는데 비용 행이 없는가」**여야 한다.
> 지금은 붙임 8 이 방향 **`반영 시 결과 악화`** 로 싣고 수량을 재어 적는다.
> `tests/report/test_unreflected.py::test_grid_purchase_without_a_cost_row_is_reported_as_adverse`
> 가 붙든다. **크기가 작지 않다** — 단가 150원/kWh 를 가정하면 연 약 34만원으로
> 첨두절감 편익(199,680원)보다 크다. 「미해결」 표에 올렸다.
>
> ### 「1차 의견」 ②~⑥ 이 놓은 것
>
> | # | 무엇 | 어디에 |
> |---|---|---|
> | **2** | **붙임 6 — 디스패치 규칙과 우선순위.** 규칙 7 전건 · `FR-302-AC1` 문면 · 자원별 배정 · 순위 · 가격 신호 연동. **순서는 실행이 쓴 것**(`CaseOutcome.rule_order`)이며 리포트가 기본 상수를 다시 읽지 않는다 | `core/report/dispatch_sections.py` |
> | **3** | **붙임 7 — 시간대별 운전.** 스텝별 자원 전력·계통 수·송전 + 합계. 송전 합계가 붙임 4 잉여판매 산식의 수량과 이어진다 | 같음 · `dispatch_notes.build_hourly_profile()` |
> | **4** | **2.1 머리의 평가 대상 요약.** 값(자원·금액·기간·할인율·보조율·편익 갈래)이 **전부 계산에서** 온다 — 고정된 것은 조사와 이음말뿐 | `method_sections._target_summary()` |
> | **5** | **붙임 1 을 주제별로.** 대장 키 접두어가 주제축이며 **신뢰도는 열 + 주제 머리 내역**으로 남겼다(둘 다 필요). 선언에 없는 접두어는 **`미분류`로 드러난다** | `appendix_sections.TOPIC_PREFIXES` |
> | **6** | **절충안 그대로.** 요약·3.4 는 항목명 + 방향, 크기·사유·해소 조건은 **붙임 8**. 항목을 **재어** 만든다 — 수명·편익 갈래·운전 결과에서 | `core/report/unreflected.py` |
>
> **배포 호출자 0곳이던 부품 둘이 배선됐다** — `build_dispatch_notes()`(`FR-105-AC4`)
> 와 24스텝 운전 결과. `FR-105-AC4` 는 *「리포트에 표기한다」* 인데 매핑표에서
> 「자동」이면서 **표기하는 리포트가 없었다.**
>
> ⚠ **`charts/dispatch_stack.py` 는 여전히 호출자 0곳이며 그것이 옳다** — 그
> 차트는 `load`(부하 곡선)를 필수 입력으로 요구하고 이 파이프라인에 가구 부하가
> 없다. 붙임 7 이 그 사실을 「미산출」로 싣는다. 부하가 오면 차트가 표를 대체한다.
>
> ### ✔ 마무리 — **양식과 실물을 대조해 넷을 닫았다** (2026-08-15)
>
> *「최종 생성된 보고서와 양식이 부합하는가」* 를 조항별로 실측했다. **골격은
> 부합**했다 — 본문 6절 · 붙임 9종의 번호·순서·이름 전건 일치, 0절 금지 사항
> (인용문 `>` · 독법 지시 · 제언) **실측 0건**, 본문 **168줄**(양식 130~170).
> 그런데 **세부 넷이 어긋나 있었고, 그중 셋은 양식이 낡은 것이었다.**
>
> | # | 어긋난 것 | 어느 쪽을 고쳤나 |
> |---|---|---|
> | **1** | 양식 【표제】가 요구하는 **작성일·작성 주체가 실물에 없었다** | **실물.** 작성 주체를 넣고 **작성일은 양식에서 뺐다** — 생성 시점을 찍으면 `FR-1001-AC5`(비트 단위 동일 재실행)가 깨진다. 「언제의 수인가」는 **대장 판**(입력의 시점)과 **매니페스트 해시**(입력의 동일성)가 답한다 |
> | **2** | 양식은 5.1 에 **출처** 열을 요구 · 실물은 붙임 2 로 내림 | **양식.** 본문 표가 일곱 열을 넘으면 화면 밖으로 나가 임계값이 안 읽힌다. **신뢰도는 본문에 남긴다** — 임계값을 읽는 즉시 확정인지 가정인지 알아야 한다 |
> | **3** | 양식 붙임 2 는 **임계값·여유**를 요구 · 실물은 본문 5.1 에만 | **양식.** 지금 양식대로면 같은 수를 두 곳에 적게 되고 **한쪽만 고쳐진다** |
> | **4** | 양식은 붙임 3 이 산식의 자리 · 실물은 **편익 산식이 붙임 4**(제원 상세) | **양식.** 편익 산식의 대입값이 전부 자원 제원이라 붙임 3 에 두면 같은 제원을 두 붙임에 나누어 적게 된다. 결정이 테스트 주석에만 있었다 |
>
> ### ★★ 왜 넷이 남아 있었나 — **양식이 스스로를 강제하지 못했다**
>
> 양식 머리말은 *「양식을 고치면 `narrative.py` 의 절 순서가 함께 움직인다」* 고
> **선언만** 하고 있었다. 개별 절을 보는 검사는 있었지만 **둘을 통째로 대조하는
> 기계가 없었고**, 그래서 붙임을 셋 늘리는 동안 양식이 조용히 낡았다. 이
> 저장소가 반복해서 잡아 온 **「선언은 있는데 읽는 쪽이 없다」와 같은 형태**이며,
> 이번에는 그 대상이 코드가 아니라 **서식 규정**이었다.
>
> **`tests/report/test_form_conformance.py`** 가 양식을 **파싱해서** 본문 절
> (`### 【N】`) · 소절(`- **N.M …**`) · 붙임 표 · 표제 표를 뽑고 렌더러 출력과
> **양방향으로** 맞춘다(한쪽에만 있으면 빨간불). 이름은 **앞부분 일치**로 본다 —
> 실물이 덧붙이는 「— 무엇에 답하는 절인가」까지 양식에 베끼면 문장 하나 다듬을
> 때마다 두 파일이 함께 움직여야 해서 **결국 검사를 통과시키려고 양식을 고치게**
> 된다.
>
> **변이 6건 전건 빨간불**이며 **검사 넷에 나뉘어** 잡혔다(① 양식만 붙임 9 이름
> 변경 ② 양식이 소절 6.3 을 규정에서 제외 ③ 렌더러가 작성 주체를 되돌림
> ④ 렌더러만 5.1 이름 변경 ⑤ 양식만 6절 이름 변경 ⑥ 양식이 작성일을 다시 요구).
> ⚠ ⑥ 은 **처음에 초록불이었다** — `printf '%s'` 가 `\n` 을 리터럴로 넣어 표
> 한 행이 되었기 때문이고, 변이 자체가 성립하지 않은 것이었다. **초록불을 그대로
> 통과로 읽지 말 것**(`printf '%b'` 나 히어독으로 넘긴다).
>
> ### ✔✔ **적정 용량 검토를 신설했다 — 4.4 · 붙임 10** (2026-08-15)
>
> 사용자 지적: *「ESS, heatpump, p2h 등은 **적정 용량 검토가 선행**되어야 함」*.
> 확인해 보니 리포트는 그 물음을 **묻지도 답하지도 못하는 상태**였다 — 용량이
> `e2e_runner` 의 **모듈 상수**여서 어느 케이스 축에도 없었다. 27 케이스를 다
> 돌려도 3kW·10kWh 한 값이고 민감도 표에도 오르지 않는다.
>
> | 무엇 | 어디에 |
> |---|---|
> | **설계 변수** 라는 셋째 갈래를 세웠다 — 대장 변수(틀릴 수 있다)·모형 파라미터(고른다)와 다르다. **사업자가 고르는 값**이다 | `ledger_levels.py::_DESIGN_VARS` · `design_variables()` · `design_levels()` |
> | 러너가 용량을 `level_map` 에서 읽는다. **기본 상수를 남기지 않았다** — 남기면 수준표를 고쳐도 옛 용량이 쓰이고 NPV 만 조용히 달라진다 | `e2e_runner.py` |
> | **형태를 재고 답을 고르지 않는다** — 단조 증가/감소/내부 최대. 단조면 「적정값 미정」이며 그것은 **용량을 제한하는 것이 모델에 없다**는 측정 결과다 | `core/report/capacity.py` |
> | 제약에 걸린 점을 **빼지 않는다.** 빼면 표가 매끈해지고 제약이 사라진 것처럼 보이는데, **제약이야말로 적정 용량을 정한다** | 같음 |
> | 설계 변수는 **5.1 에서 뺐다** — 확보 대상(자료)과 설계 결정은 받는 사람이 다르다. 할인율을 5.2 로 가른 것과 같은 판단 | `case_report.py::_influences` |
>
> ### ★★★ 실측이 말한 것 — **PV 는 줄이고 ESS 는 키우라고 나온다**
>
> | 설계 변수 | 한계 기여 | 형태 | 구간 내 최선 |
> |---|---|---|---|
> | `pv_capacity_kw` | **-61,681원/kW** | 단조 감소 | 1kW (구간 하한) |
> | `ess_capacity_kwh` | **+190,445원/kWh** | 단조 증가 | **23kWh → NPV +1,936,149원 (회수)** |
>
> ⚠⚠ **이 답을 그대로 믿으면 안 되며, 왜 그런지가 이번 라운드의 핵심이다.**
> ESS 를 키울수록 좋아지는 이유는 **심야에 계통에서 받은 전력이 공짜이기
> 때문**이다(구매 비용 행 부재 — 붙임 8). 즉 ESS 가 *「공짜로 받아 120원에 파는
> 기계」* 가 되어 있다. 그래서 **순서는 「적정 용량」이 아니라 「구매 비용 배선」
> 이 먼저**다. 리포트는 이 사실을 스스로 싣는다 — 4.4 가 형태를, 붙임 8 이
> 구매 비용 미계상을, 붙임 10 이 점별 값과 걸린 제약을 낸다.
>
> ⚠ PV 가 단조 감소인 이유도 함께 봐야 한다 — 잉여판매 단가 120원/kWh 로는
> 160만원/kW 를 회수하지 못하며, **평탄 발전 프로파일** 때문에 심야 발전이
> 충전에 흡수돼 팔리지도 않는다(붙임 8 신설 항목).
>
> ### ✔ 붙임 8 에 **일중 발전 프로파일 (평탄)** 신설
>
> 붙임 7 을 눈으로 읽다 **00~01시 태양광 0.45kWh**(야간 발전)를 발견했다. 러너가
> PV 에 이용률 하나만 주어 하루 발전량을 **24스텝에 균등 배분**한 결과다. 수량을
> 재어 싣는다 — **수전 스텝에 실린 발전량 2.70kWh/일 (연간화 985kWh)**. 방향은
> `방향 미측정` 이다: 발전을 주간으로 옮기면 송전(편익)과 수전(비용)이 **함께**
> 늘고, 구매 비용이 미계상이라 순 방향은 그 항목의 배선 여부에 달렸다.
> **「PV 가 평탄하다」를 문장으로 박지 않았다** — *「스텝별 출력이 전부 같은 발전
> 자원이 있는가」* 를 매 실행 재며, 일사 곡선이 배선되면 행이 스스로 사라진다.
>
> ### 본문 분량 — 양식 상한에 닿아 붙임으로 내렸다
>
> 4.4 가 들어오며 본문이 178줄이 됐다(양식 130~170). 양식이 정한 대로 **붙임으로
> 내렸다** — 4.3 의 「편익 갈래」 표를 **붙임 4**(제원·산식과 같은 자리)로,
> 「한계 기여」 정의를 **붙임 9**(용어)로. 지금 **170줄**이다.
>
> **변이 8건 전건 빨간불**(양식 정합 6 · 용량 6 중 겹침 포함)이며 검사 다섯에
> 나뉘어 잡혔다. ⚠ 그중 하나가 **검사 자체의 구멍을 드러냈다** — 붙임 번호
> 정규식이 `\d` 한 자리라 **붙임 10 이 파싱되지 않아** 「붙임 10 을 지운다」가
> 초록불이었다. `\d+` 로 고쳤다. **변이를 심지 않았으면 새 검사가 새 붙임을
> 붙들지 못하는 채로 남았다.**
>
> ### ✔ **부하·일사 곡선을 가정해 반영했다 — 붙임 7 둘째 표** (2026-08-15)
>
> 사용자 판정: *「운전·붙임까지 (프로포마 불변)」*. 그 범위로 닫았다.
>
> | 무엇 | 어디에 |
> |---|---|
> | **형상 자산** — 대표일 24스텝 정규화 가중치(부하·일사) + 출처·신뢰도·`replace_when`. **대장이 아니다** — 값이 아니라 배분이라 대장 항목의 꼴(`value`+3수준)에 맞지 않는다 | `fixtures/profiles/representative-day.yaml` |
> | 로더 — 정규화·검증. **없으면 메우지 않고 비운다** (기본 형상으로 메우면 「자산이 비었다」와 「이 형상을 골랐다」가 구별되지 않는다) | `core/casegrid/profiles.py` |
> | 러너가 형상을 받는다 — PV 는 이용률 대신 시계열, `Load` 자원 추가. **리포트가 자원을 다시 세우지 않는다** — 같은 진입점을 한 번 더 부른다 | `e2e_runner.py::_household_load` |
> | 붙임 7 이 **두 표**를 나란히 + 차이 표 | `dispatch_sections.py` |
> | 붙임 8 의 **자가소비·구매비용 두 행이 「미정량」에서 정량으로** | `unreflected.py::_assumed_quantities` |
>
> ### ★★★ 실측 — **수전이 2.15배로 늘어난다**
>
> | 대표일 합계 | 파이프라인 | 형상 가정 | 차이 |
> |---|---|---|---|
> | 계통 송전 | 16.10 kWh | 13.36 kWh | **-2.74** |
> | 계통 수전 | 6.19 kWh | 13.31 kWh | **+7.12** |
> | 계통 수전 (연간화) | 2,259 kWh | **4,859 kWh** | **+2,600** |
> | 자가소비 (min(발전,부하)) | — | 5.44 kWh/일 (연 **1,985kWh**) | — |
>
> **단가 150원/kWh 를 가정하면 미계상 구매비용이 연 약 73만원**이며, 이는 연
> 편익 총액(904,860원)에 육박한다. 자가소비 절감(연 1,985kWh)이 그 반대편에
> 있지만 **소매 단가가 없어 계상할 수 없다.**
>
> ### ★★ 왜 프로포마를 건드리지 않았나 — **한쪽만 반영하면 한 방향으로 틀린다**
>
> 부하를 편익 계산에 태우면 `grid_export` 가 줄어 **잉여판매가 자동으로
> 감소**한다. 그런데 그 대가인 자가소비 절감은 ⓐ 배타 규칙 유형 A
> (`SelfConsumption` ↔ `SurplusSale`)가 동시 계상을 막고 ⓑ 소매 단가를 줄 요금
> 엔진이 없어 **계상할 수 없다.** 그대로 넣으면 편익만 빼는 것이 되어 사업에
> 불리하게 틀린다 — **NSPM 대칭성**이며 양식 4절에 금지 조항으로 적었다.
> `test_assumed_operation.py::test_the_assumed_run_does_not_move_the_conclusion`
> 이 *「결론이 가정 운전 위에 서 있지 않은가」* 를 붙든다.
>
> **변이 4건 전건 빨간불**(형상이 총량을 바꿈 · 가정 운전 미실행 · 결론을 가정
> 운전 위에 세움 · 자산 부재 시 메움). ⚠ 그중 하나가 처음에 **rc=2**(대상 문면
> 없음)로 나왔다 — 통과로 읽지 않고 문면을 다시 떠서 심었다.
>
> ### 실측 — 게이트 (2026-08-15)
>
> `pytest` **수집 1,526건 rc=0**(양식 정합 검사 5건 추가) · `ruff` 0건 ·
> `mypy` **112파일** 무결 ·
> `lint-imports` 4 kept 0 broken · `check_hardcoded_params` rc=0(**`RULE_TEXT` 를
> `MappingProxyType` 으로 고친 뒤** — `NFR-205` 가 잡았다) ·
> `check_task_mapping` rc=0 · `gen_traceability` 요구사항 105 / 수용기준 321
> **변동 없음**(분류가 바뀐 조항이 없고 검사 건수만 늘었다) ·
> **CI 버전(3.11)에서도 rc=0** (`~/miniconda3/envs/r31py311`).
>
> ⚠ **3.11 을 반드시 돌린 이유**: `CaseOutcome` 에 필드 셋(`resources`·
> `dispatch`·`rule_order`)을 더했다. 이 자료형은 R31 이 **3.12 에서는 보이지
> 않고 3.11 에서만 수집 오류가 나는** 형태를 실제로 겪은 자리다(`mappingproxy`
> 기본값). 이번에는 기본값을 두지 않아 걸리지 않았다.
>
> ⚠ **음성 스위트 10종은 돌리지 않았다** — 실물 소스를 변이시키므로 커밋 직전에
> 돌리는 것이 규칙이다. 커밋 전에 돌릴 것.
>
> ---
>
> | # | 할 일 | 판정과 근거 |
> |---|---|---|
> | ~~**1**~~ | ✔ **결합 시나리오 표를 5.1 에 — 2026-08-15 완료** | 아래 「1차 의견 ① 이 놓은 것」 절. **판정은 그대로 유효했고 실측이 표에 그대로 실렸다** |
> | ~~**2**~~ | ✔ **엔진 규칙 → 붙임 6 — 2026-08-15 완료** | 위 표 |
> | ~~**3**~~ | ✔ **시간대별 디스패치 → 붙임 7 — 2026-08-15 완료** | 위 표. **여기서 「계통 전력 구매 비용 미계상」이 드러났다** |
> | ~~**4**~~ | ✔ **평가 대상 요약 → 2.1 머리 — 2026-08-15 완료** | 위 표 |
> | ~~**5**~~ | ✔ **전제 주제별 묶음 → 붙임 1 — 2026-08-15 완료** | 위 표 |
> | ~~**6**~~ | ✔ **미반영 항목 절충안 → 3.4 + 붙임 8 — 2026-08-15 완료** | 위 표. **사용자 확인 대기 해소** — 「마저 완료해줘」로 진행 승인 |
> | **1** *(원문)* | **결합 시나리오 표를 5.1 에 넣는다** | ★★ **여섯 중 가장 중요. 서식이 아니라 방법론 결함이다.** 의견 원문: *「ESS, PV 단가가 같이 움직여도 동일한 결과가 나오는가?」* → **아니다.** 실측(as_planned NPV): 둘 다 low **+1,094,119(회수)** · PV만 low +94,119 · ESS만 low +134,119 · 기준 −865,881 · 둘 다 high −2,825,881. **효과가 정확히 더해진다**(둘 다 `t=0` CAPEX 로만 들어가 서로 곱해지지 않는다 — 요금 인상률이 배선되면 깨진다). 지금 리포트는 「각각 17~18% 하락 필요」로 보이지만 **함께 20% 낮아지면 여유 있게 회수**된다 → **사업에 불리하게 읽히고 있다.** ⚠ **저장소가 이미 결합으로 판정해 두었다** — `quick_preset_grid()` 의 `equipment_cost_bundle` 이 PV·ESS·히트펌프·시공비를 한 축으로 묶는다. 27케이스는 결합으로 보는데 **리포트 민감도만 독립**이다. 1변수 스윕은 「단독 기여」임을 명시할 것 |
> | **2** | **엔진 규칙을 붙임에 낸다** | 의견: *「규칙 기반 엔진이 적용되었다는데 규칙이 붙임에 기재되지 않으면 내용을 이해할 수 없음」* — 옳다. 재료 있음: `core/engine/rule_based.py::rule_for()` · `_ordered_resources()` 로 자원별 규칙과 실행 순서를 열거할 수 있다 |
> | **3** | **시간대별 디스패치 표를 붙임에 낸다** | 디스패치가 24스텝을 갖고 있고 `core/report/dispatch_notes.py::build_dispatch_notes()` 와 `charts/dispatch_stack.py` 가 이미 있다. **⚠ 둘 다 배포 호출자 0곳** — 이번 라운드에서 여섯 번째 「부품은 있는데 읽는 쪽이 없다」 |
> | **4** | **평가 대상 300자 요약**을 2.1 머리에 | 지금 2.1 이 **표부터 시작**해 「무엇을 평가하는가」가 문장으로 없다 |
> | **5** | **전제를 주제별로 묶는다** | 지금 붙임 1 은 **신뢰도별**이고 의견은 **주제별**(설비단가/요금/제도/분석조건)을 요구한다. **둘 다 필요** — 주제로 묶고 신뢰도를 열로 |
> | **6** | **미반영 사항 — 절충안** | 의견은 「붙임으로 별도 기재」인데 **일부 반대했고 사용자에게 절충안을 제시한 상태**다. 이유: ⓐ NSPM 이 「정량화 못한 영향을 결과와 함께」 요구 ⓑ 교체비(불리)와 요금 인상률(유리)이 **반대 방향**이라 붙임으로 내리면 −865,881원을 확정 수치로 읽는다. **절충안**: 요약·3.4 에 항목명과 **방향만** 한 줄씩, 크기·근거·해소 조건은 **붙임 7** 전문. ⚠ 진행 전 사용자 확인 |
> | — | ~~구매비용·방전편익 세부 항목~~ | ❌ **서식이 아니라 모델에 없다.** 러너에 **가구 부하가 없어** 계통 전력 구매 비용 항목 자체가 없고 PV 자가소비율은 0%(전량 판매)다. 편익은 잉여판매·첨두절감 둘뿐. `Q-3`(가구 부하 시계열) 미확보이고 `16.3` 개인정보 절차가 선행한다. **표를 잘 그려 해결되지 않는다** — 리포트가 「계산되지 않았다」를 명시하는 것이 정직한 처리. ⚠ 의견의 예시(*자가소비 전량 · SMP 0 시 충전 후 저녁 피크 방전*)는 **우리 운전 전략과 다르다**(전량 판매 + 피크 저감) — 그것은 위 ①「전제·구성」 축이다 |
>
> **작업 뒤에는 반드시** `python -m app.run.report_cli --out "docs/evidence/MC-1-검토용-리포트-2026-08-15.md"` 로 실물을 다시 뽑고 **눈으로 읽는다** — 이번 라운드의 결함 넷(절대 경로 · 지수 표기 · 값/단위 어긋남 · 과잉 정밀도)이 **전부 실물을 뽑아 보고서야** 드러났다.
>
> ### ✔ 「1차 의견」 ① 이 놓은 것 — **결합 시나리오** (2026-08-15)
>
> | 무엇 | 어디에 |
> |---|---|
> | **결합 선언을 밖으로** — 리포트가 「무엇이 함께 움직이는가」를 읽어 갈 통로. **두 프리셋의 합집합**이다(전체 탐색에만 있는 `trade_price_bundle` 이 조용히 빠지지 않게) | `core/casegrid/grid.py::coupled_variable_sets()` |
> | **결합 스윕** — 묶음마다 「단독 / 동반」을 나란히 재고 **상호작용 잔차를 함께** 나른다 | `core/report/combined.py` |
> | **다변수 탐침** — `_Sweeper.conclusion_at_many()`. 1변수 스윕이 그것의 특수한 경우가 되게 했다(갈라 두면 결합 쪽만 변형을 읽지 않는 어긋남이 생긴다) | `core/report/case_report.py` |
> | **5.1 에 표 · 1절 요약에 한 줄** + 단독 기여 명시 부기 | `core/report/narrative.py` |
> | **양식 개정** — 5.1 에 「단독 기여 명시 + 결합 시나리오 동반」을 규정으로 적었다. 절 순서가 조항이므로 렌더러와 함께 움직인다 | `docs/report-form-심의보고서.md` |
>
> **실측이 판정과 정확히 같았다**(as_planned NPV): 기준 −865,881 · PV만 low
> +94,119 · ESS만 low +134,119 · **동반 low +1,094,119(회수)** · 동반 high
> −2,825,881. **상호작용 잔차 0원.**
>
> ### ★ ① 에서 배운 것 — **「더해진다」를 코드가 단정하면 배선된 날 조용히 틀린다**
>
> 잔차가 0 이므로 **결합 행을 단독 합으로 채워 넣는 구현도 오늘의 실물에서
> 옳게 보인다.** 그래서 「더해진다」를 상수로 두지 않고 **재어** 적는다 —
> 검사는 *인자가 서로 곱해지는 탐침*을 주입해 잔차가 실제 값으로 나오는지
> 본다(`build_coupled_sweeps(bundles=...)` 가 그 주입구다). 지금 대장 값으로는
> 그런 조합을 만들 수 없으므로, 이 검사가 없으면 그 결함은 **요금 인상률이
> 배선되는 날에야** 틀린 수로 나타난다.
>
> **변이 8건을 심었고 전건 빨간불**이며 **서로 다른 검사에** 잡혔다
> (① 결합 행을 단독 합으로 ② `additive` 를 상수 True 로 ③ 묶음을 리포트가 직접
> ④ 결합 선언이 quick 만 읽음 ⑤ 동반 행이 한 인자만 옮김 ⑥ 단독 기여 부기 삭제
> ⑦ 요약에서 결합 줄 삭제 ⑧ 결합 표를 5.1 밖으로).
>
> ⚠ **다음 사람이 알아야 할 것**: 「1차 의견」 2~6 은 **아직 그대로**다.
> 6번은 여전히 **사용자 확인 대기**다.
>
> # ▶ R33 에 할 일 — 번호 순서대로
>
> | # | 할 일 | 왜 이 순서인가 · 무엇을 조심할 것인가 |
> |---|---|---|
> | ~~**1**~~ | ✔ **「읽는 배포 코드가 있는가」 검사 — 2026-08-17 완료** | `scripts/check_unread_extension_points.py` · CI 차단. **선언 125건 중 37건이 읽는 배포 코드 0곳**이며 부채 목록으로 얼어붙였다 — 늘어도 줄어도 빨간불이다. 목록에 `exclusions`·`value_streams`·`notes`·`unmet_*`·`year_fraction` 이 있다: **R32 가 「해소했다」고 적은 `exclusions` 도 실제로는 테스트만 읽고 있었다.** `ast` 로 속성 접근만 세므로 주석·독스트링은 후보가 아니고, `assert` 안에서만 읽히는 것은 따로 표시한다(`python -O` 에서 사라진다 · R24). 음성 스위트가 **양방향**을 고정한다 — 변이 2건 빨간불 · 오판 방지 1건 초록불 |
> | ~~**2**~~ | ✔ **`FR-401-AC2.AggregatedPPA` 를 실행 경로에 태운다 — 2026-08-17 R34 완료** (판정은 「사용자 입력이 아니라 모형이 계산한다」였고, 그 통로를 열자 **365배 결함**이 드러났다 — 위 R34 블록. ⚠ **이 줄은 R34 가 취소선을 긋지 않아 R35 까지 「할 일」로 남아 있었다**) | 조립기는 섰으나 **`run_single_case_e2e` 는 `annual_generation_kwh` 를 받을 통로가 없다**(구조를 주면 조립기까지 가지만 발전량은 `SettlementInputs` 로만 온다). 즉 **케이스 그리드에서 집합 PPA 케이스를 돌릴 수 없다** — R32 가 조립 층까지만 닫았고 그 사실을 여기 적는다. 파이프라인이 디스패치 전량을 알므로 그것을 넘길지, 사용자 입력으로 둘지가 판단이다(전량은 `DispatchResult` 에서 복원되지 않는다 — `aggregated_ppa.py` 독스트링) |
> | ~~**3**~~ | ✔ **`MC-1` 이 볼 「리포트 한 장」 — 2026-08-15 완료** | 아래 「R33 ③ 이 놓은 것」 절 |
> | **4** | **「VPP 경유」** | `FR-401-AC2.VPPMarket`(Phase 2)이 선행이다. **자료가 아니라 순서 문제**이며 `AC1.VPP` 가 `[Phase 2]` 를 들고 있다 |
> | **5** | **닫기 라운드** — 마커 전수 재대조 · **기계 몫은 끝났고 사람 몫이 남았다 (2026-08-17)** | 실측: 마커-테스트 쌍 **1,241건 · spec 에 없는 ID 0건** · spec 파싱 결함 0 · `gen_traceability` 매달린 참조 0 · `check_task_mapping` rc=0. 계약구조 키 일곱도 전부 spec 선언(`AC1.HouseholdDirect` 꼴)과 이어진다. **남은 위험은 「ID 는 실재하는데 다른 조항을 가리킴」 하나**이고 그것은 기계가 잡지 못한다 — 시도한 휴리스틱 둘이 **전부 잡음**이었다: ⓐ 조항 문면과 테스트 이름의 어휘 겹침(조항은 한국어·테스트는 영어라 1,241 중 573이 겹침 0) ⓑ 요구사항별 테스트 구획 분포(`NFR-303`·`FR-402` 처럼 **원래 횡단하는 조항**이 상위를 차지). ★ **읽어서 대조하는 수밖에 없으며 그 자리는 R32 가 옮긴 마커 49곳**이다. 정규식은 `(?<!N)FR-` (R21 이 `NFR-402-AC1` 오염으로 배웠다) |
> | 6 | `LoadedSeeds` 를 제품 경로로 | **지금 하지 말 것** — 소비자 0곳. 소비자가 생길 때 함께 옮긴다 |
>
> ### ✔ R33 ③ 이 놓은 것 — **리포트 산출 경로** (2026-08-15)
>
> | 무엇 | 어디에 |
> |---|---|
> | **조립기** — 골든 시나리오 1건 → 실행 → 영향도(1변수 스윕) · 3중 표기 산식 · 부기 7종 · 가정 부록 · 매니페스트를 한 자료형으로 | `core/report/case_report.py` |
> | **렌더러** — 한국어 마크다운 한 장. **절의 순서가 조항이다**(`FR-1002-AC1`·`UI-7`) | `core/report/narrative.py` |
> | **출구 둘** — `GET /reports/golden/{name}`(`?fmt=json`) · `python -m app.run.report_cli --out <경로>` | `app/routers/reports.py` · `app/run/report_cli.py` |
> | **대장→수준표를 배포 코드로** — 종전에는 `tests/acceptance2/test_17_2_dod2.py` 안에만 있었다 | `core/casegrid/ledger_levels.py` |
> | **`CaseBasis` 신설** — 산식의 **대입값**이 경계를 넘는다. 지표만으로는 「왜 이 값인가」에 답할 수 없다 | `core/casegrid/models.py` · `e2e_runner.py` |
> | **심의보고서 양식** — 우수사례 셋(NSPM for DERs · KDI 예타 표준지침 · 분산특구 가이드라인)을 대조해 뽑았다. **절의 순서 자체가 규정**이며 생성기와 함께 움직인다 | `docs/report-form-심의보고서.md` |
> | **본문/붙임 렌더러** — 본문 6절(실측 218줄) + 붙임 6종. `NFR-206` 이 522줄로 잡아 **본문/붙임 경계**로 갈랐다 | `core/report/{narrative,appendix_sections,method_sections,_format}.py` |
> | **실물 산출물** — `MC-1` 검토자에게 건네는 파일. 손으로 고치지 말고 명령으로 다시 뽑는다 | `docs/evidence/MC-1-검토용-리포트-2026-08-15.md` |
>
> ### ★★★ R33 이 찾은 것 — **넷 다 「리포트를 실제로 내 보아야만」 드러났다**
>
> | 무엇 | 어떻게 드러났나 |
> |---|---|
> | **임계값 이진탐색이 돌지 않고 있었다** | `_find_flip_threshold` 의 기본 허용오차가 **절대 `0.5`** 라, 구간 폭이 `0.03` 인 할인율에서는 첫 줄에서 `hi-lo < tol` 이 성립해 **탐색이 한 번도 돌지 않고 구간의 중점을 임계값으로 돌려주었다.** 실패를 알리지 않으므로 리포트는 *「할인율 3.75% 에서 뒤집힌다」* 를 자신 있게 싣는다(실제 3.44%). 종전 검사는 구간이 `0~200` 이어서 **우연히** 이 결함을 밟지 않았다 |
> | **`tariff_escalation` 이 파이프라인에 배선돼 있지 않다** | 스윕에서 **영향폭이 정확히 0원**으로 나왔다. `quick_preset_grid` 의 축인데 `run_single_case_e2e` 가 읽지 않는다 — 즉 **27 케이스는 실질 9 케이스가 3번 반복**된 것이고, DoD 2 의 실현가능영역 지도는 `y=tariff_escalation` 축이 **구조적으로 평평하다**. 고치지 않고 **리포트가 드러내게** 했다(`unread_by_pipeline`) — 「영향 없음」으로 읽히면 검토자가 틀린 결론을 배워 간다 |
> | **케이스 지표에는 지원이 반영되지 않는다** | 러너 규약상 `metrics` 는 총사업비 기준이라 **보조율 80% 와 무보조가 같은 수**다. 리포트가 그것을 결론으로 적으면 *「80% 를 지원해도 결과가 같다」* 가 된다. 결론·산식·스윕 **셋 다** 변형(`as_planned`)을 읽게 했다 |
> | **교체비·잔존가치가 프로포마에 없다** | 0절 자원 표를 넣고 나서야 **ESS 수명 17년 · 분석기간 20년**이 나란히 보였고, 비용 행을 세어 보니 고정 O&M 둘뿐이었다. `replacement_schedule()`·`salvage_value()` 는 있고 **부르는 배포 코드가 0곳**이다 — 조항 `FR-104-AC2`·`AC4` 는 단위 테스트로 「자동」이었다. 리포트가 수명과 분석기간을 견주어 스스로 짚게 했다 |
> | **`FR-1001-AC5` 를 자동으로 만들 뻔했다** | 새 테스트에 `req("FR-1001-AC5")` 를 달자 매핑표에서 그 조항이 「수동 + MC-1(미수행)」에서 **「자동」으로 바뀌었다** — Phase 1 인수를 막는 유일한 차단 표시가 사라진 것이다. 마커를 뗐다. spec §16.5 가 막으려는 자기충족과 같은 형태다 |
>
> **변이 9건을 심었고 전건 빨간불**이며 **각 변이가 서로 다른 검사에** 잡혔다.
> 그중 하나(「스윕만 케이스 지표로 되돌린다」)는 **처음에 초록불이었고**, 무보조
> 시나리오에서는 두 값이 같아 드러나지 않는다는 것이 그때 밝혀져 검사를 더했다.
>
> ### 코드로 닫을 수 없는 것 → **`status-human.md`** 가 소유자다
>
> 수동 검증 8건 · 외부 자료 회신 · 배포 판단 · spec 개정 판단은 **그 파일에 순서대로**
> 있다(무엇을 · 누구에게 · 통과 기준 · **끝나면 어디에 기록하는가**). **여기 옮겨 적지
> 않는다.** 요약만: **Phase 1 인수를 막는 것은 `MC-1` 하나**이며 **그 선행(리포트
> 산출 경로)은 위 3번으로 닫혔다** — 이제 사람이 `MC-1` 을 시작할 수 있다. 그리고
> `Q-4`·`Q-5` 는 **가정으로 대체할 수 없어** DoD 4 와 §13.3 대조군이 `skip` 으로
> 남아 있다(정직한 상태다). 판정 근거는 `docs/decisions-2026-08-14-R32.md`.
>
> ### 초기화된 세션이 첫 30분에 할 일
>
> 1. `git status -sb`(비어 있어야 한다) · `gh run list --limit 4`
>    (`tests`·`source-rules` **둘 다** `success`)
> 2. **★ 의존성이 설치된 인터프리터는 `~/miniconda3/python.exe` 다** (2026-08-14 실측).
>    저장소 `.venv` 와 `C:\Python313` 에는 `pytest` 조차 없다 —
>    `PYTHONUTF8=1 ~/miniconda3/python.exe -m pytest` 로 돈다.
>    `lint-imports` 는 `~/miniconda3/Scripts/lint-imports.exe`
> 3. **CI(3.11) 대조용 환경이 이미 있다: `~/miniconda3/envs/r31py311`.**
>    구조를 바꾸는 커밋 전에 `PYTHONUTF8=1 ~/miniconda3/envs/r31py311/python.exe -m pytest -q`
>    (R32 도 돌렸고 rc=0 이었다). 새로 만들지 말 것 — 만드는 데 20분이 든다
> 4. **`docs/decisions-2026-08-14-R32.md` 를 읽는다** — 잠정 판정 일곱과
>    **「무엇이 오면 바뀌나 · 고칠 자리」**가 절마다 있다. **되돌리려면 근거를
>    먼저 반박해야 한다**
> 5. ⚠ **`pytest` 요약 줄을 `tail` 로 찾지 말 것** — 리포터가 `N passed` 를 내지
>    않는다. 세려면
>    `python -m pytest --collect-only -q | awk -F': ' '/^tests\/.*: [0-9]+$/{s+=$NF} END{print s}'`
> 6. ⚠ **음성 스위트가 실물 소스를 변이시킨다** — 돌는 중에 `git status` 를 보면
>    `# NEGCHECK` 가 남은 것처럼 보인다. **끝난 뒤에 다시 볼 것**(중단시키면 실제로
>    남는다). R32 는 10종 전건 rc=0 이며 잔여 변이 0건을 확인했다
>
> ### ⚠ R32 가 실제로 걸린 파급 지점 — **조항을 건드리면 여기가 함께 움직인다**
>
> 조항 하나를 쪼개는 데 **문서 셋과 검사 다섯**이 따라왔다. 다음 사람이 같은 일을
> 할 때 순서를 몰라 게이트를 하나씩 밟지 않도록 좌표를 적는다.
>
> | 무엇을 바꾸면 | 함께 고쳐야 하는 곳 | 안 고치면 |
> |---|---|---|
> | **수용기준을 쪼개거나 신설** | ① `rslt/task-…md` 의 **인용**(`- 수용기준:` 줄) ② 테스트 마커 ③ spec 머리말 판본·수용기준 총수 ④ 작업 목록 머리말 판본 | `check_task_mapping` **차단**(R31 이 올렸다). R32 에 「실재하지 않는 인용 1 + 미인용 Must-have 7」로 실제로 빨간불이었다 |
> | **편익 클래스 신설** | ① `core/valuestream/__init__.py`(import·`__all__`) ② `docs/exclusion-rules.yaml` ③ `tests/acceptance/test_phase1_dod.py` 의 `_create_valuestream_for_tag()` **분기**와 `EXPECTED_RATIONALES` ④ `FR-401-AC2.<클래스명>` 조항 | DoD 6 가 규칙표 전건을 순회하며 인스턴스를 만들므로 **`ValueError: 지원하지 않는 편익 태그`** 로 죽는다 |
> | **배타 규칙 추가** | `tests/contract/test_exclusion_rules_contract.py` 의 **건수 단언**(지금 7) | 규칙이 조용히 사라지는 것을 막으려고 못 박아 둔 자리다 |
> | **조립기 추가** | `tests/valuestream/test_settlement.py` 의 `len(assembled)`(지금 **6**) · `NOT_YET_ASSEMBLED` 에서 제거 | 두 표의 합집합이 일곱이어야 하는 래칫이 잡는다 |
>
> **★ 마커 어휘 — `req()` 에 넣을 수 있는 것은 「수용기준 ID」뿐이다.**
> R32 가 셋을 틀렸고 `gen_traceability` 가 **매달린 참조**로 잡았다:
> `DV-6`(대장 규칙은 AC 가 아니다 — 독스트링에 적고 마커는 이웃 `NFR-303-M1`) ·
> `FR-703-AC1`(**`.npv`/`.bcr`** 로 쪼개져 있다) · `FR-801`(요구사항이지 AC 가 아니다).
>
> **★ 계약구조 키 일곱** (spec v0.16 · 저자 부여 리터럴이며 구조명에서 파생하지 않는다):
> `FR-205-AC1.HouseholdDirect` · `.ManagerEntity` · `.DistrictDirectTrade` ·
> `.NetMetering` · `.SurplusDirectSale` · `.AggregatedPPA` · `.VPP`(Phase 2).
>
> **★ 변이 하네스는 `.orch/mutate.py` 다** — `.orch/` 는 git 추적 밖이고 **R24 에서
> 멈춘 오케스트레이션 이력**이지만 **이 도구는 여전히 유효하다**(R32 가 16건에 썼다):
> ```
> printf '%s' '<옛 문면>' > /tmp/o ; printf '%s' '<새 문면>' > /tmp/n
> PYTHONUTF8=1 ~/miniconda3/python.exe .orch/mutate.py "<라벨>" <대상파일> /tmp/o /tmp/n <pytest 대상...>
> ```
> 문면을 **파일로** 넘긴다(인자로 넘기면 셸이 따옴표를 건드려 「대상 문면이 없다」가
> 나고 그것이 미성립인지 깨짐인지 구분되지 않는다). **못 찾으면 rc=2** 이며 통과로
> 읽지 않는다. `<파일>.mutbak` 가 남았으면 원복이 건너뛰어진 것이다.
> **`.orch/` 가 없어졌으면 하네스를 다시 만들 것** — 인수 조건이 *「보고를 믿지 않고
> 변이를 직접 심는다」* 이고, R32 의 결함 셋은 **전부 변이로만 드러났다.**
>
> **★ 커밋 전에 PATH 를 맞춘다** — 훅의 `ruff` 가 `python -m ruff` 라 PATH 의 첫
> `python` 을 잡는다: `export PATH="$HOME/miniconda3:$HOME/miniconda3/Scripts:$PATH"`
> (**절대 경로를 적지 말 것** — `check_disclosure` 가 SC-3 위반으로 커밋을 막는다.
> R32 가 이 줄에서 실제로 막혔다).
> 훅이 막으면 **`.pre-commit-config.yaml` 을 고치지 말 것**(실행할 수 없을 때 통과시키지
> 않는 것이 이 저장소의 규칙이다). 커밋 메시지는 `git commit -F - <<'EOF'` 로 쓴다.
>
> **★ 커밋 뒤 두 검사는 기준 ref 를 요구한다** — `check_coverage_inputs` ·
> `check_test_accompaniment` 는 `--base HEAD~2`(본체+인계 두 커밋일 때)로 돌린다.
> 커밋 전에는 변경 0건으로 보여 rc=2 이며 그것이 옳다.
>

---

## 산출물 현황 — **R32 종료 실측** (2026-08-14)

> ★ **이 절은 「판본이 무엇인가」만 적는다.** 종전에는 문서 11종 · 도구 · CI 를
> 표로 늘어놓아 220줄이었고, 그래서 **갱신되지 않아 두 판본 뒤처졌다.**
> 도구 목록은 `scripts/` 가 정본이고 CI 구성은 `.github/workflows/` 가 정본이다 —
> **여기 옮겨 적지 않는다.**

| 산출물 | 판본 | 대조 명령 |
|---|---|---|
| `rslt/spec-분산특구-경제성평가.md` | **v0.16** (수용기준 **321**) | `head -4` 머리말 |
| `rslt/task-분산특구-경제성평가.md` | **v2.8** (상위 17 · 하위 **158**) | 같음 |
| `docs/traceability.md` | 자동 생성 — 요구사항 **105** / 수용기준 **321** / 자동 **291** / 수동 8 / 스텁 4 | `python scripts/gen_traceability.py` (rc=1 이 정상) |
| `docs/assumptions.yaml` | `Q-1`~`Q-16` + 확정 — R31 이 `Q-14`~`Q-16` 신설 | `python scripts/check_assumptions.py` |
| `docs/manual-checks.yaml` | `MC-1`~`MC-8` **전건 미수행** · `MC-1` 은 **일정만 잠정 기입**(2026-09-11) | 결정 문서 R32 §5 |
| `docs/exclusion-rules.yaml` | 배타 규칙 **7건** (R32 가 집합 PPA 둘 추가) | 계약 테스트가 건수를 못 박는다 |
| `docs/decisions-2026-08-14-R31.md` · `-R32.md` | 결정·판정 **정본** | — |
| `docs/clause-recheck-2026-08-14.md` | `.orch/` 에서 옮긴 유효 판정 | — |
| `docs/{domain-rules,evidence-standard}.md` | 정본 **사본**(바이트 동일) · 소유자는 `docs/source-rules.lock` | `python scripts/check_source_rules.py --require-vault` |

---

## 게이트 — 라운드를 닫기 전에 돌리는 것

> ★ **인터프리터를 먼저 확인한다.** 저장소 `.venv` 와 `C:\Python313` 에는 `pytest`
> 조차 없다. 의존성이 있는 것은 `~/miniconda3/python.exe` 이고 `lint-imports` 는
> `~/miniconda3/Scripts/` 에 있다. **`PYTHONUTF8=1` 을 켠다** — 켜지 않으면 서브프로세스
> 출력을 cp949 로 디코드하다 6건이 로컬에서만 빨간불이 된다(R24 실측).

```bash
PY=~/miniconda3/python.exe
PYTHONUTF8=1 $PY -m pytest -q                                   # rc=0. 2분 초과 — 배경 실행
PYTHONUTF8=1 $PY -m ruff check core tests scripts app infra web # --fix 는 마지막에
PYTHONUTF8=1 $PY -m mypy                                        # 103파일
PYTHONUTF8=1 ~/miniconda3/Scripts/lint-imports.exe              # 4 kept 0 broken
#   ⚠ 이것도 PYTHONUTF8 이 필요하다 (2026-08-17 실측). 켜지 않으면
#   `'cp949' codec can't decode byte 0xe2` 로 rc=1 이 나고, 그것은 계약 위반이
#   아니라 **자기 배너를 못 읽은 것**이다 — 위 ★ 문단이 pytest 에만 해당하는
#   줄로 읽혀 이 줄만 맨몸으로 남아 있었다
PYTHONUTF8=1 $PY scripts/gen_traceability.py    # rc=1 이 정상(미매핑 8). ★ 부수효과로 파일을 다시 만든다
git diff --stat docs/traceability.md            # ← 커밋 전 이것이 반영돼야 CI 가 초록불이다
PYTHONUTF8=1 $PY scripts/check_task_mapping.py  # rc=0 이어야 한다 — CI 차단이다
for g in check_marker_substance check_hardcoded_params check_source_rules          check_partition_assignment check_disclosure check_assumptions          check_precommit_installed; do PYTHONUTF8=1 $PY scripts/$g.py; done
PYTHONUTF8=1 $PY scripts/check_file_size.py --code-strict
for f in scripts/negtest_*.py; do PYTHONUTF8=1 $PY "$f"; done   # 11종. 배경 실행
PYTHONUTF8=1 ~/miniconda3/envs/r31py311/python.exe -m pytest -q # CI 는 3.11 이다
```

**커밋 뒤에만 되는 것 둘**: `check_coverage_inputs` · `check_test_accompaniment` 는
기준 ref 를 요구한다 — `--base HEAD~2`. 커밋 전에는 변경 0건으로 보여 rc=2 이며
**그것이 옳다**(0건을 통과로 읽지 않는다).

> ### ⚠⚠ **`pytest ... | tail` 의 종료 코드는 `tail` 의 것이다** (2026-08-15 실측)
>
> `pytest -q 2>&1 | tail -6` 로 돌리면 셸이 돌려주는 rc 는 **파이프 마지막
> 명령의 것**이다. 실제로 이 라운드에서 **실패 1건이 있는 실행이 `exit 0` 으로
> 보고됐다**(`test_hourly_table_carries_every_step_of_the_run` — 붙임 7 이 표
> 둘이 되며 행 수가 24→48). 출력에 `FAILED` 줄이 보여 알아챘을 뿐이다.
>
> **`> /tmp/x.log 2>&1; echo "rc=$?"` 로 돌린다.** 게이트를 파이프에 물리지
> 않는 것이 규칙이며, 이 저장소가 이미 두 번 만난 「게이트가 조용히 초록불」
> 형태다(§16.5 CI `|| true` · 6.7 문자열 포함 검사).

**마무리에 빠뜨리기 쉬운 것**
- **음성 스위트가 실물 소스를 변이시킨다** — 도는 중 `git status` 에 `# NEGCHECK` 가
  보이는 것은 정상이고, **중단시키면 실제로 남는다.** 끝난 뒤 다시 볼 것
- 빈 디렉터리 둘이 남는다 — `scripts/_tmp_negtrace` · `scripts/_tmp_neg_marker` (`rmdir`)
- 커밋 메시지는 `git commit -F - <<'EOF'` 로 쓴다(`@'...'@` 는 `@` 가 메시지에 들어간다)
- 푸시 뒤 `gh run list --limit 4` — 워크플로는 `tests` 와 `source-rules` 둘이고
  **둘 다 `success` 여야 끝난 것**이다(`source-rules` 는 경로 필터라 안 돌 수 있다)

## 미해결

| 구분 | 항목 | 영향 | 조치 시점 |
|---|---|---|---|
| 입력 | **Q-8** V2G 계량·정산 제도 존부 | 미확인이므로 편익 **전액 0**으로 등재(비활성). 충전기 비용은 계상되므로 **현 결과는 V2G에 보수적**이다 | 16.1b 요청 대상 |
| 입력 | **Q-13** 호스팅 운영 주체·예산 | 3.6·17.7 배포 형태. 로컬 컨테이너(2.13)는 무관. 가정값 월 2만원(하단 0) | 3.6 착수 전 |
| **검증** | **Q-4 미확보 → 17.4·SG-4 판정 유예** | **가정할 수 없다** — 정박점을 우리가 만들면 자기충족 검증이 된다. 통과로도 실패로도 세지 않고 미판정으로 남긴다 | 회신 시에만 해소 |
| **검증** | **Q-5 미확보 → §13.3 1차 대조군 유예** | 동상. 16.5의 6종은 해석해(순위 1)로 대체하되 *실무 전제와의 정합*은 대체되지 않는다 | 회신 시에만 해소 |
| 검증 | **가정값 위의 골든 3종** | 16.4 기준값이 대장 가정에 묶인다 — 통과는 "어제와 같다"이지 "맞다"가 아니다. 가정 갱신 시 재산출 필요 | 16.4 착수 시 |
| **범위** | **가구 부하가 모델에 없다** (R33 발견 · ⚠ **행 제목의 후반 「구매 비용 항목 자체가 없다」는 R34 에 해소됐다** — `energy_purchase_row()` 가 있고 붙임 4 가 항목을 싣는다. 남은 것은 **부하**다) | `run_single_case_e2e` 에 부하가 없고 PV 자가소비율은 0%(전량 판매)다. 그래서 편익은 **잉여판매·첨두절감 둘뿐**이다. 구매 비용은 **저장장치 충전분에 대해서는 계상된다**(R34) — 낼 수 없는 것은 *가구가 쓰는 전력*의 구매 비용이며, 그것이 없으므로 자가소비 절감 편익도 없다. 서식 문제가 아니라 모델 범위다. `Q-3`(가구 부하 시계열) 미확보이며 `16.3` 개인정보 처리 절차가 선행한다(`SC-3`). 그때까지 리포트는 **「계산되지 않았다」를 명시**하는 것이 정직한 처리다 | Q-3 회신 · 16.3 절차 완료 시 |
| ~~도구~~ | ~~`build_dispatch_notes()` 호출자 0곳~~ | **2026-08-15 해소** — 붙임 6·7 이 읽는다(`core/report/dispatch_sections.py`). `DispatchStack` 차트는 **여전히 0곳이며 그것이 옳다**: `load`(부하 곡선)가 필수 입력인데 파이프라인에 가구 부하가 없다. 붙임 7 이 「미산출(부하 곡선 부재)」로 싣는다 | ✔ |
| ~~계약~~ | ~~**계통에서 산 전력의 비용이 프로포마에 없다** (2026-08-15 발견 — 붙임 7 을 내고서야)~~ **R34 해소 (2026-08-17)** — `tariff.hv_single_contract.energy_only` 120원/kWh 등재 · `energy_purchase_row()` 배선 · 1년차 구매 271,073원. 아래는 발견 당시 문면이며 **「단가 150원 가정 시 연 34만원」은 낡았다**(실효단가가 아니라 한계단가를 쓴 이유는 그 대장 항목의 주석에 있다). ★ R35 가 **파는 쪽 단가**도 대장으로 올려 짝을 맞췄다 | ESS 가 심야 여섯 스텝에서 충전하고 **그 전력이 계통에서 들어온다**(대표일 **6.19kWh** · 연간화 **2,259kWh**). 비용 행은 고정 O&M 둘뿐이므로 **그 전력을 값 없이 쓰고 있다.** ⚠ **종전 판정 「가구 부하가 없으므로 구매 자체가 없다」가 틀렸다** — 부하와 구매를 같은 것으로 본 것이며, 부하가 없어도 ESS 충전이 구매를 만든다. 크기가 작지 않다: 단가 150원/kWh 가정 시 연 약 34만원으로 **첨두절감 편익(199,680원)보다 크다.** 반영하면 NPV 가 더 나빠진다. 리포트는 붙임 8 에 **`반영 시 결과 악화`** 로 수량과 함께 싣는다(`tests/report/test_unreflected.py`). 고치려면 요금 엔진(WP-3)의 구매 단가가 필요하다 | 5.0(요금 엔진) 착수 시 · Phase 1 인수 전 판단 |
| **계약** | **교체비·잔존가치가 프로포마에 들어가지 않는다 (R33 발견)** | `FR-104-AC2`(EOL 도달 시 교체비 계상)·`AC4`(잔존가치)는 매핑표에서 「자동」인데, 세고 있는 것은 `ESS.replacement_schedule()`·`core/cba/salvage.py::salvage_value()` 를 **직접 부르는 단위 테스트**다. 실행 경로(`e2e_runner`)의 비용 행은 **고정 O&M 둘뿐**이며 교체·잔존가치 행이 없다. 지금 구성은 ESS 수명 17년 · 분석기간 20년이라 **교체가 한 번 필요한데 그 비용이 빠져 있고, 그만큼 현 결과는 ESS 에 관대하다**(잔존가치도 함께 빠져 방향은 상쇄되나 크기는 다르다). R32 가 셋, R33 이 앞서 둘 만난 「부품은 있는데 읽는 쪽이 없다」와 같은 형태다. **리포트가 이 사실을 스스로 짚는다** — 수명과 분석기간을 견주어 판정하며 `tests/report/test_narrative.py` 가 그것을 붙든다 | Phase 1 인수 전 |
| **범위** | **결합 집합 넷 중 둘만 수준표에 있다 (「1차 의견」 ① 에서 드러남)** | `equipment_cost_bundle` 은 **PV·ESS·히트펌프·시공비 넷**인데 `build_level_map()` 이 대장에서 만드는 것은 `pv_unit_cost`·`ess_unit_cost` 둘뿐이다(`_LEDGER_VARS`). 그래서 리포트의 결합 표도 **두 열**이며, 그것이 지금 평가 대상(PV+ESS)과는 맞다 — 히트펌프가 구성에 없고 시공비는 별도 행이 아니다. **다만 「설비단가를 한 축으로 흔들었다」는 진술은 넷 중 둘에만 성립한다.** 히트펌프·시공비가 구성에 들어오면 대장 항목과 `_LEDGER_VARS` 를 함께 늘려야 하며, 늘리지 않으면 결합 표가 조용히 두 열로 남는다(리포트가 선언과 수준표의 **교집합**만 그리므로 빨간불이 나지 않는다) | 히트펌프·시공비가 구성에 들어올 때 |
| **계약** | **`tariff_escalation` 이 케이스 그리드 축인데 파이프라인이 읽지 않는다 (R33 발견)** | `quick_preset_grid` 이 이 변수를 3수준으로 흔들지만 `run_single_case_e2e` 는 `pv_unit_cost`·`ess_unit_cost`·`discount_rate` 셋만 읽는다. **27 케이스는 실질 9 케이스의 3회 반복**이고, 17.2 DoD 2 가 그리는 실현가능영역 지도는 `y=tariff_escalation` 축이 **구조적으로 평평하다** — 「이 축은 영향이 없다」로 읽히지만 실제로는 계산에 들어가지 않았다. 요금 인상률을 편익 시계열에 태우는 것은 WP-3(요금 엔진) 몫이며 `DispatchContext` 의 가격 신호가 그 자리다. **리포트는 이것을 `unread_by_pipeline` 로 드러낸다** — 고칠 때까지 검토자가 틀린 결론을 배워 가지 않게 한다 | 5.0(요금 엔진) 착수 시 |
| 기술 | **TU-2** Litestream vs Turso | 3.6 프로토타입 후 결정 | Wave 1 |
| 기술 | **TU-6** 누진·TOU 혼재 모델링 — **설계 방침 확정 (08-09), 약관 확인은 남음** | **한 계량점에 누진과 TOU를 동시에 적용하는 요금제는 실재하지 않는다.** 「혼재」는 한 단지에 **계량점이 여럿**이라는 뜻이다 — 가구부(주택용 N개, 각각 누진) / 공용부(일반용 고압 1개, TOU) / 거래분(직접거래). 따라서 요금 엔진은 **계량점 단위 독립 계산 + 합산**으로 짓고, `FR-501-AC6`(동시 적용)은 «한 계량점에 두 요금제»가 아니라 «여러 계량점의 합산»으로 읽는다. 이렇게 두면 5.3과 5.4가 부딪치지 않고 요금 엔진을 두 번 짓지 않는다. **약관 검토는 「계량점 구성이 실제로 이러한가」를 확인하는 것으로 좁혀진다** — 다르면 여기로 돌아온다 | 5.3 진행 중 · 약관 확인 시 재검토 |
| 기술 | **TU-7** IRR·할인회수기간 셀 수식 한계 | 13.5 실현 범위. 대응책 확정 — *값 + 산식 주석 병기* | 13.5 착수 시 |
| ~~도구~~ | ~~매핑표 Phase 열 43건 공란~~ | **08-08 해소** — 부록 A.1 해석 추가. 잔여 9건은 전건 Should-have | ✔ |
| ~~도구~~ | ~~표 수용기준 10건 뭉개짐~~ | **08-08 해소** — 9건 전개 / 1건 폐기. 수용기준 252 → **299건** | ✔ |
| ~~도구~~ | ~~`NFR-107-AC1.manual` 미구현~~ | **08-08 해소** — `ast` 기반 마커 수집으로 구현. 클래스·모듈 수준 상속 포함. 수행 기록 없는 스텁은 종료 코드 2 | ✔ |
| ~~도구~~ | ~~CI 매핑 잡이 spec 결함 시 조용히 통과~~ | **08-08 해소** — `\|\| true`로 종료 코드를 버려 결함(코드 2) 시 파일이 안 바뀌고 diff가 통과했다. 실측 재현 후 폐쇄 | ✔ |
| ~~검증~~ | ~~B/C 오라클 미배정~~ | **08-09 해소 (spec v0.12)** — §13.0.2 **순위 1**. B/C는 IRR과 달리 반복해가 아니라 **두 할인 합산의 몫**이므로 닫힌 형태다. **분자·분모를 각각 원 단위 완전 일치로 판정하고 비율에는 별도 오차를 두지 않는다** — 비율에 오차를 주면 «편익도 비용도 과대» 와 «둘 다 정확» 이 같은 값을 내어 어느 쪽이 틀렸는지 말하지 못한다 | ✔ |
| 검증 | **16.5 엑셀 대조에 MIRR·B/C 누락** | 현재 4종(NPV·IRR·회수기간·융자 상환). 10.3이 대조하겠다는 5종 중 2종의 기대값 출처가 없다 | 16.5 착수 시 |
| 범위 | **`FR-611-AC3` ↔ `FR-704` 관점 어휘 불일치** | `FR-704`는 사업자/참여 주민/정부인데 `FR-611`은 사업자·주민(병합)/사회(FR-704에 없음)/정부다. 중립 키(`.OWNER`·`.SOCIAL`·`.GOV`)로 어긋남을 고착시키지 않았으나 **해소하려면 `FR-704` 개정이 선행**된다 | 10.6 착수 전 |
| 판단 | **8.3을 배타 유형별로 쪼갤 것인가** | 한 작업이 `AC2.A`의 차단 100%(음성)와 `AC2.B`~`.D`의 오탐 0(양성)을 동시에 진다. 검사 방향이 반대이고 4행이 각각 `NFR-102`·`FR-404`·`FR-704`·`FR-504`로 위임되어 네 구획에 걸린다 | 8.0 착수 시 |
| ~~계약~~ | ~~`dispatch()`의 「한 해」 ↔ 계약 테스트 `steps=24` 충돌~~ | **08-08 해소** — 「연초부터 연속 `steps` 스텝」으로 확정. `year_fraction` 신설, 「짧은 창은 긴 창의 앞부분」을 계약 테스트가 검사 | ✔ |
| ~~계약~~ | ~~`value_streams()` 훅 부재~~ | **08-08 해소** — `DER`에 기본 `return ()`. 편익 객체가 아니라 **tag 문자열**을 돌려준다(객체는 NFR-208-AC2 위반) | ✔ |
| ~~계약~~ | ~~부가세 분리 자리 없음~~ | **08-08 해소** — `capex_vat()` 추상 신설. 실측하니 **다섯**이 지어냈고 ESS 하나는 아예 없었다 | ✔ |
| ~~계약~~ | ~~그 밖에 자원 구현이 자작한 것~~ | **08-08 대부분 해소** — 운전 방법 접근자 이름 고정 · `salvage_value()` 명목액 명문화 · `escalation_rate` 통일 · `DispatchResult` 에 `unmet_*`·`notes` · `AllocationResult` 계약 이관 · `CommonAsset` SW/HW 분리 잔존가치. **요금 시계열은 남았다**(아래 행) | ✔ |
| ~~계약~~ | ~~요금·연료 단가 시계열을 놓을 자리 없음~~ | **08-09 판단 완료 (1.12 ⑥)** — `DispatchContext` 에 **가격 신호**로 싣는다. 별도 계약을 두지 않은 이유는 ctx 가 이미 *"엔진→자원 유일 통로"* 이기 때문이다. 「요금」이 아니라 「신호」인 것이 요점 — 누진·TOU·특례 해석은 WP-3 이 하고 **결과인 스텝별 단가 하나**만 실린다 | ✔ |
| 계약 | **자원의 잔여 요금 인자 3건 — 이관 대기** | 실측: `EV_V2G.avoided_price_won_per_kwh` · `HeatPump.price_profile_won_per_kwh` · `HeatPump.elec_price_won_per_kwh`. **`avoided_price` 는 운전이 아니라 편익 화폐화이므로 ctx 가 아니라 WP-4 로 간다** — 그 판단은 8.0 이 편익 소유권을 확정할 때 내려진다. 지금 0건을 요구하면 `RC-HP`·`RC-EV` 검증 케이스를 함께 고쳐야 한다. `KNOWN_TARIFF_DEBT` 가 부채를 고정하며 **늘면 즉시 빨개진다** | 8.0 착수 시 |
| ~~문서~~ | ~~**공통 비용 5종의 마커 부착이 갈렸다**~~ **R31 해소 — `FR-101-AC5` 신설** | `FR-104`는 열화·수명·교체·잔존가치 조항이라 **C-1 CAPEX·C-2 고정 O&M·C-3 변동 O&M에 대응하는 AC가 없다.** 6개 구획이 각자 `FR-101-AC2` 등으로 달았다. spec에 조항을 신설하거나 6.0.S1의 인용을 정정해야 한다. **계약 개정에 포함하지 않았다** — spec 개정이므로 §16.5 절차가 따로 필요하다 | spec 다음 개정 시 |
| ~~품질~~ | ~~NFR-206 초과 6건~~ | **08-08 판단 완료** — `ast` 로 갈라 재자 **코드 줄 수로는 전건 상한 이내**(최대 482)였다. 조항의 근거(DER-VET `Params.py` 1,830줄)가 겨냥한 코드 스프롤에 해당하는 파일은 없고 초과분은 전부 설명 밀도다. `--code-strict` 를 CI **차단**으로 켰다 | ✔ |
| ~~입력~~ | ~~`vat_rate=0.1` 이 대장에 없다~~ | **08-08 해소** — `track: fixed` 갈래를 신설하고 `tax.vat_rate` 등재(확정 · 부가가치세법 제30조). 함께 **v1.1이 남긴 기본값 갈림**(여덟 중 히트펌프만 0.1)을 통일하고 레지스트리 순회 계약 테스트로 고정 | ✔ |
| **계약** | **대장을 읽는 경로 — 계약 자리는 섰고 구현·주입은 남았다** | **08-09 (1.12 ⑦)** — `core/contracts/assumptions.py` 의 `AssumptionProvider` 가 읽기 경계를 고정했다. **없으면 `MissingAssumption` 으로 멈추며 기본값으로 메우지 않는다.** 남은 것 둘 — ⓐ 대장을 실제로 읽는 구현(WP-2, 작업 4.4) ⓑ **자원 생성 시 주입 강제**(WP-16, 작업 7.2). ⓑ 가 없으면 `vat_rate=0.0` 이 여전히 「주입되지 않음」인 채 통과한다 | WP-2 · WP-16 |
| ~~문서~~ | ~~**`SC-3`·`SC-5`·`SC-8` 이 추적표 밖이다**~~ **v0.11 해소 — 이 행이 낡아 있었다 (R31 실측)** | SC 표의 행은 조항 ID 를 갖지만 수용기준 파서가 읽는 형식(`- **AC1**`)이 아니다. `@pytest.mark.req("SC-8")` 를 달자 생성기가 «실재하지 않는 인용» 으로 잡았다(실측). **Must-have 급 요구인데 17.9 미매핑 집계에 들어오지 않으므로, «전건 매핑» 이 성립해도 보안 조항은 검증되지 않은 채로 남는다.** SC 표를 수용기준 형식으로 승격할지, 별도 축으로 셀지 판단 필요 | spec 다음 개정 시 |
| **도구** | **NFR-202 판정 사각지대 — 대장 값 17건. ★ R35 에 실물로 걸렸다** | \|값\| < 1,000 은 판정하지 않는다(4·5·15·30 은 인덱스·시간과 구별되지 않는다). `tariff.hv_single_contract.avg`(150원/kWh)·`fee.direct_trade_support`(5원/kWh)가 여기 있고, **요금 단가는 소스에 새기 가장 쉬운 값**이다. ★★ **가정이 아니라 실측이 됐다** — 잉여 판매단가 `120.0` 이 `e2e_runner` 안에 있었고 **검사 전건이 초록불이었다**(R35 가 대장으로 옮겼다). 즉 이 사각지대는 *이론상 위험*이 아니라 **이미 한 번 통과시킨 구멍**이다. 남은 위험은 같은 크기의 단가가 또 들어오는 것이며, 지금 그것을 막는 것은 검사가 아니라 **「기본값을 두지 않는다」 규약**뿐이다(수준표에서 빼면 러너가 거부한다). 요금 엔진(WP-3) 착수 시 다른 판정 수단이 필요하다 | 5.0 착수 시 |
| 품질 | **총 줄 수 초과 6건은 남는다** (경고) | 상한값·판정 기준을 바꾸지 않았기 때문이다 — NFR-206 문면은 「500줄」이고 코드 기준으로 재정의하려면 **spec 개정(§16.5 절차)** 이 필요하다. 검사를 통과시키려고 규칙을 고치는 것은 이 저장소가 반복해서 경계해 온 유형이다. 조항을 개정할지, 파일을 쪼갤지, 경고로 둘지는 **다음 spec 개정 때 판단** | spec 다음 개정 시 |
| ~~문서~~ | ~~교체 연도가 spec ↔ Wave 0 참조 구현 사이에서 갈림~~ | **08-08 해소** — `ReferencePV`가 수명 연도(12년차)에 계상해 §13.2.2 C-4(*수명 도달 **다음** 연도 초*)와 한 해 어긋나 있었다. WP-1a가 짚었고 13년차로 정정 | ✔ |
| **범위** | **17.1 DoD 1 ↔ FR-202 Phase 어긋남** | 17.1(2변형 **비교** 산출, SG-1 전반부)은 Phase 1인데 비교 기능 FR-202는 부록 A.1이 Phase 2에 배정. 7.5 템플릿 2변형을 각각 실행해 나란히 놓는 것으로 충족되는지, 아니면 FR-202-AC1·AC2를 Phase 1로 당길지 판단 필요 | 17.1 착수 전 |
| ~~도구~~ | ~~AC 단위 Phase 표기를 검사기가 못 읽음~~ | **08-08 해소** — 수용기준 단위 Phase(`[Phase N]` 줄 끝 표기) 도입. 미인용 1 → **0건** | ✔ |
| ~~운영~~ | ~~정본 표류 탐지 장치가 꺼져 있음~~ | **08-08 해소 (2.16)** — `DER_VAULT_ROOT` 사용자 환경변수 + 작업 스케줄러 매일 09:10 + 표류 마커 파일. 네 경로 실측 확인 | ✔ |
| **범위** | **분석기간을 어느 층이 소유하는가 — ✔ R30 이 절반을 닫았다** | **케이스 실행 경로는 배선됐다** — `core/casegrid/e2e_runner.py` 가 자원을 세운 직후 `check_analysis_period()` 를 부르고, `horizon_years` 인자가 검사와 프로포마 계산 **양쪽**에 쓰인다. 아래는 R24 시점 문면이며 **「부르는 코드가 0곳」은 이제 틀렸다**. 남은 것은 *사용자가 고르는* 분석기간의 소유자다. R24 가 `core/cba/proforma.py::check_analysis_period()` 로 상한(최장 자원 수명 × 2)을 **던지는 코드는 놓았으나 부르는 코드가 0곳**이었다. 값이 함수 인자(`analysis_end_year`)로만 떠다니고, 자연스러운 자리인 `Scenario` 는 **`DV-11` 이 명시적으로 금지**한다(규제 프로파일·요금표·할인율·분석기간 필드 보유 불가). 즉 「배선을 잊었다」가 아니라 **놓을 자리가 계약상 비어 있다.** 후보는 `AssumptionSet`(전제 층) 또는 케이스그리드·앱 조합 층이며, 고르면 그 층이 `check_analysis_period` 를 부르는 것으로 닫힌다 | Phase 1 인수 전 |
| ~~검증~~ | ~~`DV-4` 후반 「윤년 처리 규칙 명시」 — 강제되지 않고 테스트가 반대를 고정한다~~ | **R25 해소.** `core/contracts/units.py` 의 `LEAP_YEAR_POLICY` 가 규칙을 선언한다 — **평년(365일) 고정 · 366일 시계열은 받지 않는다 · 그 하루의 처리는 호출측 기록**. 자리를 거기로 정한 이유는 스텝 수 상수를 쓰는 쪽(`core/der/*` 계산 층과 `infra/tsstore.py` 저장 층)이 **전부 그것을 받아 가기 때문**이다. 흩어져 있던 주석 둘은 선언을 가리키게 바꿨다(사본을 만들지 않았다). 붙드는 것은 `tests/contract/test_leap_year_policy.py` — 문면 · **스텝 수가 연도를 받을 수 없다** · **윤년 행 수가 허용 목록에 없다**. 닿는지는 `test_tsstore.py::..._hands_over_the_policy` 가 본다(8784행이면 조치 자리에 선언 그대로). 변이 6종 전건 잡힘 | ✔ |
| **범위** | **`VPP` 가 계약에 앉는가 — Phase 2 착수 전 판단** | 자원이 다른 자원을 품으면 ⓐ `dispatch()` 가 하위 자원을 순회해야 하고 ⓑ 하위 자원 비용이 VPP 에서 다시 계상될 위험이 있다(FR-402-AC2.B). **구현으로 우회하지 말 것** — 견디지 않으면 §16.2 절차 | Phase 2 착수 전 |
| ~~품질~~ | ~~**mypy strict 5건 실패**~~ **해소 — 이 행이 낡아 있었다 (R31 실측)**. `python -m mypy` 가 **102파일 무결**이다(R31 종료 시 실측). 08-09 에 적힌 5건은 그 뒤 상환됐고 `heatpump.py` 의 `Year` 불일치도 남아 있지 않다. **CI `tests` 가 `mypy strict` 를 차단으로 돌리므로 다시 쌓일 수 없다** — 「아무도 돌리지 않아 쌓였다」는 원인이 사라졌다 | ✔ |
| ~~검증~~ | ~~`NFR-208-AC1`~`AC3`·`M1` 미매핑~~ | **08-08 해소 (2.8)** — 세 위반을 각각 심어 **그 계약이** BROKEN 이 되는지 확인하고, 심지 않은 상태의 `4 kept` 를 양성으로 함께 둔다. 심는 모듈은 `_` 로 시작해 레지스트리 스캔은 피하되 import 그래프에는 잡힌다 | ✔ |

---


## 반복해서 걸린 함정 — 다음 세션에서 주의

같은 유형의 오류를 **여덟 번** 만들었다. 패턴을 적어 둔다.

### 검사 도구를 설명하는 문장이 그 검사에 걸린다

| # | 발생 | 원인 |
|---|---|---|
| 1 | spec §16.5.2 | "존재하지 않는 원칙 번호를 인용하지 말라"는 규칙이 나쁜 예를 **리터럴로** 적어 앵커 검사에 걸림 |
| 2 | 작업 목록 검사 설명표 | "AC가 5개인데 1~6 인용" 예시가 실제 범위 초과로 파싱됨 |
| 3 | 2.15 「부수 영향」 | ID 변경 대상을 나열한 서술이 2.0의 인용으로 집계되어 **유령 중복 5건** |
| 4 | (같은 원인, 반대 방향) | 서술 속 언급이 "인용됨"으로 세어져 **미인용 6건을 가림** |
| 5 | 2.15 `FR-601-AC5` 사유 | §16.5.2가 폐기한 나쁜 예를 **리터럴로** 적어 정본 대조에 걸림 |
| 6 | 6.7 공유 파일 검사 | 「공유 파일에 자원 이름 0건」을 문자열 포함으로 검사하자 **그 검사를 설명하는 주석·독스트링이 걸림** |
| 8 | 2.3 `SC-3` 검증 케이스 | 개인정보 **탐지기를 검증하는 테스트**가 탐지 대상을 리터럴로 들고 있어 스스로 위반이 됐다. 해법은 앞선 일곱과 같다 — **면제를 넓히지 않고 대상을 바꾼다.** 픽스처를 조각으로 조립해 소스에 식별정보 모양이 남지 않게 했다. `tests/` 를 통째로 면제했다면 **진짜 유출이 픽스처에 섞여도 보이지 않는다** |
| 7 | 2.6 테스트 동반 검사 | **반대 방향이며 더 나쁘다.** 「대응 테스트가 이 모듈을 참조하는가」를 문자열 포함으로 검사하면 *"`core.der.pv` 를 예로 들면"* 이라고 적은 주석이 **대응 테스트로 세어져 게이트가 조용히 초록불**이 된다. 6번은 정당한 서술이 위반으로 보고되어 «시끄러운» 실패였지만, 7번은 위반이 통과로 보고되는 «조용한» 실패다 |

**교훈**: 문서·코드가 자기 자신을 검사하는 구조에서는 **서술과 선언을 구분하는 장치**가 필요하다. 해법은 매체에 따라 다르지만 원리는 같다 — 문서는 인용을 `- 수용기준:` 필드에서만 수집하고, 코드는 `ast` 로 식별자·문자열 리터럴만 뽑아 주석·독스트링을 뺀다.

> **이 함정은 앞으로도 나온다.** 「자기 자신을 검사하는 도구」를 새로 쓸 때마다 **먼저 물을 것**: *이 검사가 내가 지금 쓰고 있는 설명 문장에 걸리는가?* 걸린다면 검사 대상을 좁히는 것이 답이고, **설명을 고치는 것은 답이 아니다.** 설명을 고쳐 통과시키면 다음 사람이 같은 설명을 다시 쓴다.
>
> 그리고 **제외를 넓게 잡는 반대 방향의 실패**를 함께 막아야 한다. 주석·독스트링을 빼려다 코드까지 빼면 진짜 위반이 있어도 초록불이 된다 — 6.7에서는 「심어 둔 등록부를 잡는가」와 「서술만 있는 파일을 오판하지 않는가」를 둘 다 검사해 양쪽을 고정했다.

### 수용기준 ID를 추정해서 쓰면 안 된다

작업 목록 초판에서 **7건이 실재하지만 다른 조항**을 가리켰다. ID가 존재하므로 기존 검사는 통과했다. 그대로 마커를 붙였다면 **엉뚱한 조항이 초록불, 맞는 조항은 미매핑**이 되어 NFR-107이 막으려던 상태가 된다.

원인은 spec v0.7에서 AC가 중간 삽입되어 번호가 밀린 것이다. **개정 때마다 재발한다.** `check_task_mapping.py`의 「미인용 Must-have」 검사가 그 그림자를 잡는다.

### 검사가 **자기 검사 대상에서 정본을 읽어 오면** 공허해진다 (R35)

「사본을 만들지 말라」와 「검사는 정본을 밖에서 가져와야 한다」가 **부딪히는
자리**가 있다. R35 가 실물로 밟았다.

    ledger_key = ledger_backed_variables()[변수]      # ← 검사할 대상에서 읽는다
    assert 편익 == 잉여량 × 대장[ledger_key] 값        # ← 그래서 무엇을 바꿔도 맞는다

수치를 검사에 적지 않으려고 매핑표에서 키를 읽어 왔는데, **그 매핑표가 바로
변이의 대상**이었다. 키를 바꾸는 변이가 검사를 따라오게 되어 *「러너는 표가 말하는
키를 쓴다」* 만 확인했다 — **참이지만 공허하다.** 변이는 ★초록불이었다.

**해법은 「사본을 적는다」가 아니다** — 리터럴로 적으면 정본이 둘이 된다. **다른
층의 정본에 대고 맞춘다**: R35 는 조립기가 쓰는 상수(`SURPLUS_SALE_KEY`)에
맞췄고, 그러면 두 자리가 같은 것을 뜻한다는 사실 자체가 검사가 된다.

> **새 검사를 쓸 때 먼저 물을 것**: *이 단언에 등장하는 값 중 몇 개를 검사 대상이
> 스스로 정하는가?* 전부라면 그 검사는 **동어반복**이다. 최소한 하나는 **다른
> 곳**에서 와야 하고, 그 「다른 곳」이 어디인지 검사 문면이 말해야 한다.

### 수와 **그 수가 나온 조건**의 짝은 따로 붙들어야 한다 (R35 ②)

5.1 의 새 표는 「인자를 *이 값*으로 두면 *이만큼* 남는다」를 말한다. 두 끝의 결론
축을 **서로 바꿔** 실어 보니 — 거리 숫자는 그대로 맞고 열 순서도 그대로이며 표는
매끈했다. **문장만 거짓이 됐다**: *「판매단가를 150원까지 올려도 210만원 부족」* 이
실은 80원일 때의 수다. 거리를 보는 검사 일곱이 **전건 초록불**이었다.

    assert 남는 거리 == min(|끝1|, |끝2|)      # ← 바꿔 실어도 성립한다

**해법은 짝을 다른 층에서 검산하는 것이다** — 수준표의 `low`·`high` 로 진입점을
직접 돌려, 리포트가 **그 이름에 붙여 둔 수**와 맞추었다. R33 이 임계값에서 세운
검사(*「보고된 값으로 다시 돌려 본다」*)와 같은 형태이며, 그때와 같은 이유로
필요하다: **표시만 하는 구현은 표시를 보는 검사를 전부 통과한다.**

> **표에 수를 새로 실을 때 먼저 물을 것**: *이 수가 어느 조건에서 나온 것인지도
> 같은 행에 있는가? 그 짝이 어긋나도 지금 검사가 초록불인가?*

### 정본은 세션 중에도 바뀐다

2026-08-02 하루에 볼트의 정본이 **세 번** 바뀌었고 세 번 다 사람이 뒤늦게 발견했다. `docs/source-rules.lock`의 sha256으로 감지한다. **날짜 해상도로는 부족하다.**

---


---

## 이력

라운드별 경위(R0~R31) · 해소된 항목 · 낡은 절차는 **`status-history.md`** 에 있다.
**새 세션은 그것을 읽지 않는다** — 현재 상태를 그 파일에서 읽으면 낡은 수를 쓴다.

`docs/decisions-2026-08-14-R31.md` · `-R32.md` 는 이력이 아니라 **판정 정본**이며,
되돌리려면 그 근거를 먼저 반박해야 한다.
