"""WP-15(CI·배포) 소유 검증 케이스 — spec §16.3 `tests/<구획명>/`.

WP-15 는 `core/` 하위에 구획 디렉터리를 갖지 않으므로(소유 경로가 `.github/`·
`Dockerfile`·`pyproject.toml` 이다) 테스트 디렉터리 이름을 패키지명에서 파생할
수 없다. §16.3 이 `tests/<구획명>/ 각 구획 소유` 로만 규정하고 있어 이름을
저자가 정했다 — `tests/ci/` 다.
"""
